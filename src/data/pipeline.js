// App pipeline — the owner's ops view of UIL4B.
//
// Surfaced in the Admin "Pipeline" tab. Where the Board tab tracks each
// feature MODULE, this tracks the app as a whole: its current condition, the
// PROCESSES (workstreams) moving through the pipeline, and the prioritised
// next-to-do queue. Edit this file to keep the pipeline current — it is the
// single source the tab renders from (no backend, no serverless function).
//
// Keep it honest: only list conditions you can verify and tasks that are real.
//
// This file owns EXECUTION: order, progress, blockers and known-unfixed bugs.
// It does not own gate baselines (docs/reference/build-and-verify.md), founder
// verdicts on proposals (docs/PROPOSALS.md), the record of decisions already made
// (CHANGELOG.md), owner console work (docs/OWNER-ACTIONS.md) or release history
// (CHANGELOG.md). Link, don't restate — copied numbers drift.

// ── Current app condition ──────────────────────────────────────────────────
// status: 'good' | 'watch' | 'blocked'
export const APP_CONDITION = [
  { id: 'build', label: 'Build', value: 'Passing', status: 'good', detail: 'vite build clean' },
  { id: 'lint', label: 'Lint', value: '0 errors', status: 'good', detail: 'Advisory warnings tracked separately' },
  { id: 'functions', label: 'Serverless fns', value: '12 / 12', status: 'blocked', detail: 'FULL. delete-account.js took the last slot; the next endpoint must replace one or fold into api/_lib/. tests/unit/account-deletion.test.js fails the build if the count goes over' },
  { id: 'create', label: 'Create surface', value: 'Live', status: 'good', detail: 'Colour, typography, library and imagery workflows are shipped; UI Component Builder and AI Studio remain in the workshop' },
  { id: 'discover', label: 'Discover surface', value: 'Live · 6 of 8', status: 'good', detail: 'Palette Library, Gradient Library, Font Gallery, Icon Library, Prompt Library and Curated Resources are all live and prerendered; Inspiration and Collections still carry soon. DISCOVER_GROUPS in toolTree.js holds the flags. Community PUBLISHING is a separate thing and is deliberately not built - #377 re-scoped it and section 3.9 of docs/OWNER-ACTIONS.md records that nothing publishes without founder approval' },
  { id: 'learn', label: 'Learn surface', value: 'Live · 7 guides', status: 'good', detail: 'Seven guides are published and prerendered - colour-contrast, type-scales, colour-spaces, theme-systems, brand-colour, font-loading, typeface-metrics - with a per-topic index, search over their text and a section share card (#431, 2026-09-09). Earlier record: Five guides are published and prerendered - colour-contrast, type-scales, colour-spaces, theme-systems, brand-colour. Two of the eight roadmap topic areas are delivered (UI Themes, Brand Colour Guide) and six still carry soon. src/data/learnIndex.js is the article list and LEARN_ROADMAP in toolTree.js is the derived to-write list - read those rather than this row' },
  { id: 'design-system', label: 'Design system', value: 'v2.8 shipped', status: 'good', detail: 'Typography, Palette/Tint consistency, authored previews and UI System Mode all released in #200' },
  { id: 'ci', label: 'CI gates', value: 'NOT RUNNING', status: 'blocked', detail: 'No GitHub Actions run has STARTED since 2026-09-04, on any branch including main. It is a BILLING failure, not a test failure - the annotation reads: the job was not started because recent account payments have failed or your spending limit needs to be increased. Both jobs die in 1-3 seconds. READ PR STATUS ACCORDINGLY: gh reports these PRs as UNSTABLE, which looks like broken code and actually means the gate never started, so a reader triaging by check status draws the wrong conclusion about every open PR at once. Until the bill is paid the ONLY evidence a change is green is a local lint/build/test:unit/test:users run pasted into the PR body. Owner action, section 1.2 of docs/OWNER-ACTIONS.md. Baselines: docs/reference/build-and-verify.md' },
  { id: 'deploy', label: 'Production deploy', value: 'Shipping', status: 'good', detail: 'CORRECTED 2026-09-13 — this row said Blocked and claimed nothing merged since 2026-09-02 had reached uil4b.com. Both halves were false and had been for some time, which mattered because a reader planning a release drew the opposite conclusion from the truth. EVIDENCE: the Vercel deployments API lists SEVEN production deployments on 2026-09-11 alone (7074e2f5, 781592aa, 79da8273, 7a9336ff, 56b14960, 107dd260, 364f495f) and one for every merge on 2026-09-13 through 1adb0712, all state READY, target production. An anonymous GET https://uil4b.com/ returns 200 in 0.9 s and serves the current build — the stylesheet hash matches the local dist byte for byte, which is the cheap way to prove WHICH commit is live (the entry chunk hash moves with env vars and is a worse signal). THE STATED CAUSE IS ALSO GONE: dist carries no ffmpeg-core-*.wasm at all and the whole deploy is 5.3 MB, against the 35 MB and 32,129,114-byte single file this row described, so perf/ffmpeg-core-off-origin landed and fast origin transfer is no longer the constraint. WHAT IS STILL TRUE is the CI row below it, separately verified: GitHub Actions has not run since 2026-09-04, every job dies in 3-4 s, and the annotation reads the job was not started because recent account payments have failed or your spending limit needs to be increased. That is billing, section 1.2 of docs/OWNER-ACTIONS.md, and it is why gh reports PRs as UNSTABLE when the gate never started. Deploys and CI are independent here: deploys ship, CI does not run.' },
  { id: 'phase', label: 'Current phase', value: 'Post-v2.8 hardening', status: 'watch', detail: 'Everything through #403 is on main and NONE of it is deployed - read the deploy and CI rows above before you read any PR check status. This row said #249 when it was last written. The Firestore rules publication blocker is founder-confirmed closed. All four proposals (P-001…P-004) now carry founder verdicts and have shipped, so no approved feature batch is in flight; the queue below is hardening, accessibility, instrumentation and the large unbuilt surfaces. Three entries that had already shipped were corrected to done/partial on 2026-08-15 — check status before starting anything here' },
]

// ── Pipeline stages (left → right flow) ─────────────────────────────────────
export const PIPELINE_STAGES = [
  { id: 'backlog', label: 'Backlog', color: 'var(--t3)' },
  { id: 'in-progress', label: 'In progress', color: 'var(--brand)' },
  { id: 'review', label: 'Review', color: 'var(--warn)' },
  { id: 'qa', label: 'QA', color: 'var(--accent-soft)' },
  { id: 'shipped', label: 'Shipped', color: 'var(--ok)' },
]

// ── Active processes / workstreams ──────────────────────────────────────────
// stage: one of PIPELINE_STAGES ids · progress: 0–100
export const PIPELINE_PROCESSES = [
  {
    id: 'typography-activation',
    name: 'Typography activation + homepage integration',
    area: 'Product',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in #200 (v2.8). Font Gallery, Font Pair Finder and Type Scale are live Create tools on a shared Google Fonts catalogue, and Typography is the fifth homepage workbench tab. Detail in CHANGELOG.md.',
    updated: '2026-08-05',
  },
  {
    id: 'palette-consistency',
    name: 'Palette/Tint consistency + defect pass',
    area: 'Product',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in #200 (v2.8). Gutters, toolbar layout shifts, seed/swatch mismatch, control sizing, Temperature and HCT behaviour, directional swap, per-swatch contrast, multi-insert menus and Palette ↔ Tint continuity. Detail in CHANGELOG.md.',
    updated: '2026-08-05',
  },
  {
    id: 'ui-system-mode',
    name: 'Premium UI System Mode',
    area: 'Product',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in #200 (v2.8). Perceptual 100–900 Brand, Success, Warning, Error, Information and Neutral scales from one 500 seed, with WCAG evidence and CSS/DTCG/Tailwind exports. Free users preview; editing, expanded scenes and export are Pro-gated.',
    updated: '2026-08-05',
  },
  {
    id: 'colour-hifi-previews',
    name: 'High-fidelity colour previews',
    area: 'Product',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in #200 (v2.8). The Palette preview applies the active colours to authored UI, brand and graphic scenes; additional scenes are visibly Pro-gated.',
    updated: '2026-08-05',
  },
  {
    id: 'colour-tool-defects',
    name: 'Founder-reported colour-tool defects',
    area: 'Product',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in #190–#193. The adjust sliders no longer compound, warming crosses the hue antipode correctly, the gradient inspector caps to the canvas height, the Saturation slider responds across its whole range, and right-clicking the between-colours plus inserts several colours at once within the plan caps.',
    updated: '2026-07-29',
  },
  {
    id: 'ci-quality-gates',
    name: 'Quality gates in CI',
    area: 'Quality',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in #188. Every pull request now runs lint, build, the unit suite, the Firestore rules tests that pin the entitlement lock, and the Playwright user-simulation suite. #189 removed the flaky offline-banner test that reddened the first run. Baselines: docs/reference/build-and-verify.md.',
    updated: '2026-07-29',
  },
  {
    id: 'design-system-scales',
    name: 'Page-gutter and layering scales',
    area: 'Design system',
    stage: 'in-progress',
    progress: 90,
    summary: 'The shared gutter and layering scales hold the Palette toolbar across the 961–999px transition. The toolbar hover labels moved into the buttons\' own flow in the founder batch-2 branch, so the label-clipping item that sat in this queue is closed — below 961px the labels are simply pinned open. Still open: the broader Community popover/z-index sweep.',
    updated: '2026-08-07',
  },
  {
    id: 'release-readiness-review',
    name: 'Release readiness review',
    area: 'Release',
    stage: 'shipped',
    progress: 100,
    summary: 'v2.8 shipped in #200 and the founder bug batch in #202. The Firestore-rules publication blocker is founder-confirmed closed and per-route metadata shipped in #198. Analytics environment guards, upgrade/activation instrumentation and the live external checks are tracked as their own queue items, not as an open review.',
    updated: '2026-08-05',
  },
  {
    id: 'dead-code-sweep',
    name: 'Dead-code sweep',
    area: 'Codebase health',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in #196. Removed 6 unrouted pages, 7 unreferenced components and their orphaned CSS — 3,284 deletions, zero insertions. Every file was confirmed unreferenced across src/ and tests/ first; the lazily-imported tool pages a naive grep misses were explicitly checked and kept. ESLint warnings fell 34 → 32 at that commit; the current baseline lives in docs/reference/build-and-verify.md.',
    updated: '2026-07-29',
  },
  {
    id: 'homepage-chaos-to-calm',
    name: 'Homepage chaos → calm proof',
    area: 'Product',
    stage: 'shipped',
    progress: 100,
    summary: 'The base experience shipped in #185; the Typography extension shipped in #200. Eleven stable tool links resolve into five honest workbench modes with icon-led satellites and a reduced-motion-safe merge/splash. The field-metric and assistive-technology passes are still unrun — their budgets now live on the homepage-field-metrics item in the queue.',
    updated: '2026-08-05',
  },
  {
    id: 'account-switch-reliability',
    name: 'Account-switch reliability',
    area: 'Account',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in #183. Root cause was signing out before authenticating the target account; the session now survives until the new credential commits. Real Google OAuth popup behaviour still needs one manual founder pass.',
    updated: '2026-07-28',
  },
  {
    id: 'plans-lifetime-build',
    name: 'Plans redesign + lifetime billing',
    area: 'Billing',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in #184 with security review PASS. Premium Plus was removed, One-off was added and degrades honestly until a Stripe price exists. The Firestore rules are founder-confirmed published, so client-side entitlement escalation is closed; the owner must still verify webhook event subscriptions before creating the lifetime price. Subscription-chargeback revocation is founder-approved and now queued as its own P1 security item below.',
    updated: '2026-08-05',
  },
  {
    id: 'founder-bug-batch',
    name: 'Founder bug batch — palette + gradient',
    area: 'Product',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in #202. Adjust slider handles became a lens onto their own track, icon-button hover labels lay out at their real width, a gradient stop is created and placed in one press-and-drag gesture, Randomise respects the selected colour system, and a gradient can be submitted for review as an explicitly unpublished local queue entry.',
    updated: '2026-08-05',
  },
  {
    id: 'public-ui-premium',
    name: 'Premium public UI release',
    area: 'Product',
    stage: 'shipped',
    progress: 100,
    summary: 'Shipped in v2.7 with the connected living-preview home, editorial mega-menu, shared CTA and footer.',
    updated: '2026-07-28',
  },
  {
    id: 'colour-tools-polish',
    name: 'Colour tools workflow polish',
    area: 'Product',
    stage: 'shipped',
    progress: 100,
    summary: 'Gradient, tint, semantic and palette workflow refinements shipped with recovery controls and responsive workbenches.',
    updated: '2026-07-28',
  },
  {
    id: 'library-workflow',
    name: 'Icon + Emoji library workflow',
    area: 'Product',
    stage: 'shipped',
    progress: 100,
    summary: 'The unified library command header, keyboard modes, mobile layout and visible network state are shipped.',
    updated: '2026-07-28',
  },
  {
    id: 'canonical-route-qa',
    name: 'Public route truth release',
    area: 'Quality',
    stage: 'shipped',
    progress: 100,
    summary: 'Router/navigation/sitemap truth shipped in d210a0d; per-route canonical, Open Graph/Twitter metadata and noindex for Soon routes followed in #198.',
    updated: '2026-07-31',
  },
  {
    id: 'static-og-card',
    name: 'Static social-share card',
    area: 'Growth',
    stage: 'shipped',
    progress: 100,
    summary: 'The static public social-share card shipped to main in d210a0d. Per-route share images were explicitly out of scope for #198.',
    updated: '2026-08-05',
  },
  {
    id: 'discover-buildout',
    name: 'Discover build-out',
    area: 'Product',
    stage: 'review',
    progress: 65,
    summary: 'CURATED FIRST, community later — founder decision, 2026-09-05. Five galleries are now live (gradients, palettes, prompts, fonts, icons) and /discover/resources ships the 22 hand-picked external resources that had sat unrendered since Slice 2. The remaining work is CURATION and two tool gaps, not architecture; user publishing is [community-backend], deliberately deferred. QUALITY PASS 2026-09-11 [quality-learn-discover-2026-09-11]. /discover/prompts was the last gallery still rendering its own empty state instead of the shared LibraryEmpty: a bare <p> with no way out and a role=“status” region that rendered the empty string, so a filtered-to-nothing grid was both a dead end and silent to a screen reader while its three siblings announced a count and offered “Clear filters”. It already used LibraryToolbar and LibraryFilterGroup, so this was an unfinished migration, not a new pattern; the sentence is unchanged and no copy was invented. Its locked-Pro sr-only heading was also an h3 under the page h1 (h1→h3→h2) and is now an h2. On /discover/resources the seven lead-card links measured 23.0px at 320 and 390, one pixel under SC 2.5.8. STILL WAITING ON THE FOUNDER and unchanged here: /discover has no lede at all (h1 is the bare word “Discover”), and the Palette and Gradient mastheads still run the shared “…with a point of view … make it yours.” template.',
    updated: '2026-09-05',
  },
  {
    id: 'learn-content',
    name: 'Learn content library',
    area: 'Product',
    stage: 'in-progress',
    progress: 62,
    summary: 'The "understand the why" surface. The article structure is built and SEVEN reference guides are published at /learn/<slug> — contrast and WCAG, modular type scales, colour spaces, dark and light themes, choosing a brand colour, what a typeface’s metrics decide, what the reader sees before the font arrives — each threshold quoted from a linked clause, each figure computed on the page, each guide ending at the tool that does the thing. The three roadmap rows those guides answer (UI Themes, Brand Colour Guide, Typography Guide) no longer say Soon; five topics remain unwritten, and whether three of them (SEO, Marketing, AI assistants) belong in Learn at all is an open founder question after the scope decision. QUALITY PASS 2026-09-11 [quality-learn-discover-2026-09-11], structure and typography only — no teaching content written, the three founder-call topics untouched, the row stays where it is. The article SHELL carried a layout defect on all seven guides at every width from 1080 up: learn-article.css promised in its own comment that “the crumb, the h1 and the first paragraph share one left edge” and measured at 1440 they were 667.2, 693.6 and 561.6, because --lart-measure was an unregistered custom property (so 56ch re-resolved to 580.72px on the prose and 369.6px on the 11px mono crumb) and .lart-head’s narrow-layout max-width was never reset (so the h1 sat in a 317px column, indented 132px past its own body). Both fixed; the h1 column is now the full 581px measure. Also: .lart-aside ran 79 characters a line and .lart-next-lede 76 against the body’s correct 67, both now 72; and every guide’s Sources links were 19.0px against WCAG 2.5.8’s 24px minimum, now compliant at every width.',
    updated: '2026-09-07',
  },
  {
    id: 'community-backend',
    name: 'Community publishing backend',
    area: 'Product',
    stage: 'backlog',
    progress: 15,
    summary: 'DEFERRED BY DECISION (founder, 2026-09-05), not blocked on an unknown. Discover’s end state is user publishing; it waits until there are users to fill it, and when it is built NOTHING is publicly visible until the founder approves it — an approval queue, chosen over publish-instantly-remove-on-report on personal-liability grounds. Needs Firebase publishing, accounts, the queue and abuse handling; api/ is at 12/12 Vercel functions with two tests failing the build on a 13th.',
    updated: '2026-09-05',
  },
  {
    id: 'public-route-prerender',
    name: 'Public-route prerendering',
    area: 'Growth',
    stage: 'shipped',
    progress: 100,
    summary: 'The matrix is explicit in scripts/route-matrix.mjs: 28 prerendered, 70 excluded, every exclusion carrying a derived reason. Canonical and noindex now come from the runtime’s own routeMeta.js functions rather than from a copy of the rule.',
    updated: '2026-09-03',
  },
  {
    id: 'billing-config',
    name: 'Stripe retention + coupon config',
    area: 'Infra',
    stage: 'backlog',
    progress: 0,
    summary: 'Owner-gated: create the retention coupon and enable cancellation in the Stripe Customer Portal. Code is ready; config is manual.',
    updated: '2026-07-28',
  },
]

// ── Next-to-do queue (prioritised) ──────────────────────────────────────────
// priority: 'P0' | 'P1' | 'P2' · effort: 'S' | 'M' | 'L' · status: 'todo' | 'doing' | 'review' | 'blocked'
export const NEXT_TODO = [
  { id: 'fix-projects-empty-390-2026-09-13', title: 'The projects empty state puts its only control on the phone screen it ships to', priority: 'P1', effort: 'S', area: 'Quality', status: 'done', note: 'MEASURED ON PRISTINE origin/main, signed in with an empty project list, on the built preview — not read off the code. Since the founder stopped seeding a “Default Project” (#73-founder-calls-0910 § 2) this panel is the FIRST SCREEN of the product for a new signup, and on every phone width in the suite its only control was below the fold. BEFORE, “Create your first project”: 390x844 y=829..873, centre 851 in an 844px viewport — 29px of the control past the edge; 360x800 y=842..886 — the whole control past the edge; 320x844 y=886..930; 320x720 y=886..930 — 210px past the edge; 320x568 y=886..930. 1280x800 y=626..665 and 414x896 y=830..874 were already clear, which is why nothing had caught it. WHAT WAS ABOVE IT at 390: masthead 72..242, the NEXT|TIP band 258..516, the tabs 548..584, the card opening at 604 — 604px of page before the panel. THE DIAGNOSIS THAT CAME IN (centre at 851 in 844) was correct and understated it: at 320 the control’s TOP was 886, 42px clear of the bottom edge of an 844-tall screen and 166px clear of a 720-tall one. THE TEST WAS RIGHT AND THE LAYOUT WAS WRONG. elementFromPoint is viewport-relative and returns null for a point outside the viewport, so a control below the fold fails 73’s hit test with “something paints over the empty state’s button” — which is what sent three lanes looking for an overlay that was not there. The assertion was not relaxed; an explicit above-the-fold assertion now runs before it and prints the geometry. THE FIX, two halves, both content-driven rather than width-gated. (1) While the account holds nothing, the NEXT|TIP band renders BELOW the empty state instead of above it. Nothing is removed and the order follows the account’s STATE, so it is the same at 320 and at 1440 and reading order stays invariant across breakpoints. The band is 258px at phone widths because it collapses to one column at <=860px — widest exactly where vertical space is scarcest — and on an empty account its NEXT column reads “Nothing saved yet. A palette is the fastest…”, which is the empty state’s own sentence one screen earlier without the control. “Orientation before inventory” is right when there IS an inventory; with none, the empty state is the orientation. (2) The card’s padding was an inline 48 that applied unchanged at 320, where it left 176px of line for a sentence naming two tools: it wrapped to five lines and the card grew from 319px tall at 390 to 340 at 320 — the panel got taller the less room it had. It is a class now, 48 from 641px up (unchanged, so desktop card height is 292 before and after) and 24 below. AFTER, same measurements, every width hit-testing to the button: 390x844 y=494..538 (306px clear); 360x800 y=505..549 (251px); 320x844 y=505..549 (295px); 320x720 y=505..549 (171px); 320x568 y=505..549 (19px); 1280x800 y=464..503 (297px); 1440x900 y=464..503 (397px). Card height 319→249 at 390 and 340→271 at 320. THE SAME CLASS, SWEPT. /projects’ OTHER empty state had a defect of the same family and it was shipping: two filters can empty the list (the search box and the folder chips) and the sentence named only the search, so tapping “Marketing” on an account with no marketing projects rendered “No projects match “”.” — an empty pair of curly quotes — in a panel holding zero controls, measured at 390 (y=815..919) and 1280 (y=585..689). The sentence now names whichever filter is responsible and the reset sits inside the panel with role=“status”, which is the rule LibraryEmpty already states for every Library browse surface. LibraryEmpty itself is not reused: it belongs to styles/deferred/library.css, which #456 keeps out of this route’s chunk. REPORTED, NOT FIXED: the four Discover galleries (/discover/palettes, /discover/gradients, /discover/prompts, /discover/resources) put LibraryEmpty’s reset 151–430px below the fold at 390 and 320 after a search that matches nothing. Different condition and mitigated by design — it follows an action the reader took, the grid it replaces was already below the fold at those widths, and LibraryEmpty carries role=“status” precisely because “on a long page it happens below the fold”. Those are four other lanes’ files. GUARDS. 73-founder-calls-0910 runs at 390 and 1280 only, so the widths where this was worst had no test at all; 52-user-home now measures the control’s geometry with no scrolling at 320x568, 320x720, 360x800 and 390x844, and four tests cover the filtered-empty panel. MUTATION, at the call site, each seen red and each proved to have reached dist/. M1 Projects.jsx `const accountIsEmpty = !resolving && projects.length === 0` → `false`: red at 320x720 (165px past) and 360x800 (62px past), green at 390 — chunk Projects-B71-YAm3.js c1396b02→Projects-CQ3NsSnI.js 4c7e7222, and the signed-in DailyBand render site appears twice in the shipped chunk and once in the mutated one. M2 projects.css `.uh-empty{padding:24px}` → 48px: red at 320x568 by 26px — chunk projects-CjihUxVm.css 7812307b→projects-CA7ygHuA.css b122816e, needle `@media (width<=640px){.uh-empty,.uh-filtered{padding:24px}` absent. M3 the folder sentence reverted: red on “names the FOLDER” — Projects-8jLoS7lu.js 2c95034e, needle `No projects in ` absent. M4 the reset’s onClick → `() => {}`: red on “the way out is IN the panel” — Projects-DsCpTAuz.js 15c44f0f, dist handler `onClick:()=>{R(``),ge(`all`)}` → `onClick:()=>{}`. All four restored byte-exact, proved with git diff. RUBRIC, /projects: dimension 5 interaction & states 5→9, dimension 7 adaptive layout 4→9. NOT COVERED AND NOT CLAIMED: real assistive-technology announcement of the new role=“status” panel (the attribute is asserted, a screen reader was not run), 320x568 in landscape, and the Discover galleries above.' },
  { id: 'fix-owner-email-not-public-2026-09-13', title: 'The founder’s personal email address was shipped to every visitor of every page', priority: 'P0', effort: 'S', area: 'Security', status: 'done', note: 'DONE 2026-09-13 (fix/owner-email-not-public). MEASURED AGAINST LIVE PRODUCTION, NO CREDENTIALS SENT: curl https://uil4b.com/assets/constants-KnpCfm5i.js returned 200, 1945 bytes, and that file contained the founder’s personal email address TWICE. Quoted here BY DESCRIPTION ONLY and never written out, the same way [landing-page-orphaned] handles its markers — this file is served through GET /api/ai?backlog=1 behind requireAdmin(), so it does not ship, but a row that spelled the address would be one import away from making its own claim false. The chunk was in the modulepreload list of all 39 prerendered shells, so every anonymous visitor fetched it in the FIRST REQUEST WAVE, on every route, signed in or not. Two defects in one: a personal address harvestable by any scraper on every page, and the exact account to phish or credential-stuff in order to reach /admin, named for the attacker. The address alone grants nothing — it is still a step, and the repository handed it over for free. WHAT CHANGED: src/utils/constants.js holds a SHA-256 digest instead of the address. BOTH occurrences are gone — ADMIN_EMAILS became ADMIN_EMAIL_DIGESTS, and OWNER_HANDLES, which was KEYED by the same address, is re-keyed by the same digest. Hashing only the first would have fixed nothing, and the mutation run below proves the guard sees that. NO SECURITY BOUNDARY MOVED. The client check only decides which surface to RENDER; /api/verify-admin and isAdminEmail in api/_lib/plans.js still read a verified Firebase ID token and are untouched, and api/_lib/admin.js keeps its plaintext because it is never bundled. The digest is not a secret and is not claimed to be one: the scope string is public and anyone who already knows the address can confirm it in a line. What it removes is the DISCLOSURE, which was the whole defect. WHY NOT AN ENV VAR: import.meta.env.VITE_* is substituted by Vite as a STRING LITERAL at build time, so a variable holding the address ships the same plaintext in the same chunk — it relocates the source of the leak rather than closing it. It only works if the variable holds a digest, which is this, plus a deployment step that can silently go missing and lock the founder out of /admin on a build nobody notices. A constant beats configuration here. WHY A HAND-WRITTEN SYNCHRONOUS SHA-256 AND NOT crypto.subtle: the latter is async, and all six call sites decide what to render DURING RENDER — a route guard, a nav item, a command-palette entry, a table row. Going async would have traded a leak for a flash of the wrong surface, which is a change to what a visitor sees. Forty lines of arithmetic, checked against node:crypto across the 55/56/64-byte padding boundaries and on multibyte input. THE FOUNDER-GATED FILE, FOUND THE HARD WAY: src/contexts/SubscriptionContext.jsx is one of the four files tests/unit/firebase-deferral.test.js gates, and rewriting its inlined check broke git apply --check on docs/design/firebase-deferral-gated.patch and failed six tests across two suites. It is byte-identical to origin/main again, and constants.js keeps an ADMIN_EMAILS export for it as a MEMBERSHIP TEST rather than a list: .includes(address) still answers correctly and there is nothing in it to index, iterate or print. Returning digests under the old name would have been worse than either — .includes(anAddress) would quietly answer false and the founder would lose his Pro entitlement on a green build. The three call sites that were NOT gated (CommandPalette.jsx, PillNav.jsx, Admin.jsx) now call isAdminEmail(), which constants.js always intended. When the gate lifts, that last call becomes isAdminEmail() and the shim goes with it. THE PUBLIC IDENTITY IS UNTOUCHED AND PROVED IN A BROWSER, not by reading code: name Dylan Coleman, the crown, the publicHandle, the UIL4B founder tooltip and publicId uil4b-founder all still resolve. Against vite preview of a --mode test build, 12/12 checks: the founder lands on /admin and sees ADMIN MODE; RequireAdmin admits him to /style-guide and sends a signed-in Pro account to /projects instead; a signed-in non-admin gets no ADMIN MODE surface; a community card seeded with the founder’s address renders Dylan Coleman with a 12.8px-wide crown, the UIL4B founder tooltip on hover and aria-label “Dylan Coleman, UIL4B founder”; the address is in neither the rendered DOM nor localStorage after the migration; and of 59 text responses the browser received on the home page, zero carried it. NOTE FOR THE NEXT AGENT: the brief called App.jsx:156 the /admin route guard. It is not — /admin is wrapped in RequireAuth; RequireAdmin at :156 guards /style-guide. A negative control aimed at /admin fails for that reason and not because the gate is broken. THE GUARD: tests/unit/owner-email-not-public.test.js walks the BUILT output — all of dist/, HTML, JS, CSS, JSON, txt and xml — matching anything ADDRESS-SHAPED rather than searching for one address, so the next one that creeps in is caught too. Only two published role addresses (legal@ and privacy@ on the site’s own domain, legally required contact points) and RFC 2606 reserved domains are allowed, and the uil4b.com pair is allowed by EXACT address, so a personal mailbox on the site’s own domain would still fail. The final label must be alphabetic, or @ffmpeg/core@0.12.6 reaches the bundle as core@0.12.6 and reads as an email. FOUR POSITIVE CONTROLS, because “no address found” is what a broken probe also looks like and this repo has had ten unit tests go vacuous reading the wrong file: scanning an empty directory must THROW and the test proves it by scanning one; the pattern must find a planted third-party address in a fixture and the allowlist must refuse it; the raw substring search must find the address in a fixture before it is trusted to report dist/ clean; and the real scan must still find the allowlisted addresses. It also EXECUTES the shipped constants chunk — minified, so exports are single letters and each is called for what it returns — to prove the founder is still recognised and the owner record still comes back whole. Robust to both build modes: green against the production build and against the --mode test build test:users leaves behind. MUTATION-PROVEN, TWICE, BOTH RESTORED BYTE-EXACT (git diff empty, chunk hash back to baseline). (1) OWNER_HANDLES re-keyed by the plaintext with the lookup following it, so the product still WORKS and only the address leaks — the exact “fixes nothing” regression: three tests red, the constants chunk hash moved from 1218084a to 937ca742 and the needle in dist/ flipped from 0 files to 1, while the crown and admin tests stayed green, which is the point. (2) The crown field deleted: the two identity tests red, chunk hash cb31d73a. Anchors were whole code-only lines, both verified unique first. GATES: lint 0 errors / 22 warnings (ceiling not raised); build 39 route shells; test:unit 1885/1886; test:users 1155 passed, 13 skipped (all 12-ui-system-builder.spec.js), 2 failed — 73-founder-calls-0910 at 390px light and dark, the known pre-existing red another lane owns. ONE OTHER RED, NOT MINE AND NOT NEW: “dist/llms.txt advertises the apex and nothing else” compares dist/llms.txt byte-for-byte with public/llms.txt. The two are identical in content and differ only in line endings — core.autocrlf=true and no .gitattributes, so public/llms.txt is checked out CRLF on Windows while scripts/llms-txt.mjs writes LF. Branch-independent: that generator imports none of the files this branch touches and public/llms.txt is unmodified. It would pass on a LF checkout. COST: the constants chunk goes 1945 → 3677 bytes raw for the inline SHA-256, paid once on every route because the chunk is preloaded. Worth it against a personal address on every page.' },
  { id: 'perf-critical-css-2026-09-13', title: 'Split the render-blocking stylesheet so a route pays only for the CSS it can reach', priority: 'P1', effort: 'M', area: 'Performance', status: 'done', note: 'MEASURED 2026-09-13 with the repo’s own instrument, scripts/home-field-metrics.mjs, 10 cold runs each, on the profile that item names in full: Playwright devices[Pixel 5] (393x851 CSS px, DSF 2.75, isMobile); CDP Network.emulateNetworkConditions at latency 150 ms / down 204800 B/s / up 96000 B/s — real packet shaping, not Lighthouse simulation; CDP Emulation.setCPUThrottlingRate 4x; HTTP cache disabled; a NEW BROWSER CONTEXT PER RUN; production `npm run build` under `vite preview`. All four configurations were measured in one session on one machine, because a before from one machine and an after from another is not a comparison. THE DEFECT: src/styles/global.css compiled to ONE 593,650-byte stylesheet that blocked the first paint of every route, and no route used more than 6.5% of it. Playwright CSS coverage on the same throttled profile: / used 38,739 of 593,650 = 6.5%; /plans 23,970 = 4.0%; /create/palette 26,318 = 4.4%. #424 had already lifted thirteen per-page families out of it; what was left was the global sheet. THE FIX, by the same mechanism #424 used: a rule moves out when NO eagerly reachable module names its class, into one of nine src/styles/deferred/*.css sheets imported by the lazy chunks that can reach it. Placement is from the MODULE GRAPH, never from coverage — coverage is per-route and per-state, so a rule that only fires on hover, in dark mode or on a route the crawl never entered reads as 0% used. The 755 class names no module in src/ names at all are NOT deleted: each travels with the family it physically sits among, and any one matching a template prefix an eager module builds is held critical instead. BEFORE -> AFTER, render-blocking stylesheet: 593,650 raw / 97,185 gzip -> 283,207 raw / 49,120 gzip (−52.3% raw, −49.5% gzip, 48,065 gzip bytes off the critical path). Coverage of the render-blocking sheet: / 6.5% -> 13.7% (the 38,739 used bytes are unchanged, the unused 554,911 -> 244,468); /plans 4.0% -> 7.9%; /create/palette 4.4% -> 8.4%. Every route also downloads less CSS in total: /plans ~600 KB -> 374 KB, /create/palette -> 449 KB. FIELD METRICS, mean / worst of 10 on that profile. main 364f495f: FCP 2554.4 / 2572 ms, LCP 3540.0 / 3564 ms. THIS CHANGE ALONE: FCP 1656.8 / 1680 ms, LCP 3300.8 / 3324 ms — FCP −897.6 ms, LCP −239.2 ms. main + the gated Firebase deferral (docs/design/firebase-deferral-gated.patch, VITE_DEFER_FIREBASE=1): FCP 2207.6 / 2236 ms, LCP 2936.0 / 2972 ms. THIS CHANGE + that deferral: FCP 1527.2 / 1544 ms, LCP 2705.2 / 2720 ms. CLS 0 and interaction response ≤ 72 ms in all four. LCP STILL MISSES THE 2500 ms BUDGET BY 205 ms with both changes, and the reason is measured rather than guessed: the LCP element is SPAN.home-hero-line-in in every run of every configuration, and it cannot paint until the entry chunk has arrived, evaluated, rendered and laid out the page. The stylesheet is what FCP was paying for — hence −898 ms there — and LCP takes only the share of it that is contended bandwidth. The remaining levers are in the JS, not the CSS: the entry chunk is still 455,395 raw / 140,025 gzip, and [homepage-field-metrics] already names the two locale chunks, brandGuidelines, styleGuideExport, learnIndex, paletteGallery and userHome that a public visitor modulepreloads before doing anything. A truthful 7 out of 10 on site Performance, not an 8. WHAT DOES NOT MOVE, AND WHY. Moving a rule out moves it LATER: the deferred sheets cascade after the whole critical sheet, and the bundler puts them after the per-page sheets too. That repaints any declaration whose only claim on an element is that it was written second, and four routes proved it — .tt-panel{padding:24px} began beating pages/tint.css, and .legal-h--tight{margin-bottom:8px} lost to .legal-h, which shares no class name with it and lands on the same <h2>. So a declaration another sheet contends for is held in the critical sheet: 110,191 bytes of it. Contention is read PER ELEMENT — from the markup, and from the rendered DOM over all 40 prerendered routes plus six signed-in ones at four widths, including ancestor chains, because an inherited font-size:.92em is repainted by a rule that never names its class — and resolved to a fixpoint, because holding one block back creates contention for the next. Against the critical sheet source position answers the question (a rule already after its contender stays after it); against another deferred sheet there is no answer, so any contention there is disqualifying. THE DOM PASS WAS TOO NARROW AT FIRST and 25-defect-sweep M6 caught it: .lbry-grid and .grg-grid land on one element via a template literal and both set grid-template-columns, /discover/gradients was not one of the 25 snapshot routes, and the gallery went to two 201px columns with 78 of 100 names truncated. Widening the crawl fixed it and is why the crawl is now the whole route list. EVIDENCE. tests/user-sim/64-computed-style-snapshot.spec.js — 25 routes, two widths, ~65,000 computed values — 26 passed against the baseline it was given, NOT regenerated. Gates: lint 0 errors / 22 warnings; build prints 39 route shells; test:unit 1864 passed 0 failed; test:users 1141 passed, 13 skipped (all in 12-ui-system-builder), 2 failed — 73-founder-calls-0910 “the empty state is what a new account sees” at 390px light and dark, which fail IDENTICALLY on a build of 364f495f with no change applied (the button’s centre sits at y=851 in an 844px viewport, and the layout is byte-identical either side), so they are pre-existing and not this change. GUARDED. tests/unit/home-asset-budget.test.js now measures the render-blocking sheet from a REAL build — 300,000 raw / 52,000 gzip — walks the static import graph from src/main.jsx and fails if it reaches src/styles/deferred at all, and requires each deferred sheet to be substantial and imported. MUTATION-VERIFIED at the call site: import ‘./styles/deferred/colour.css’ added to src/main.jsx moved the chunk hash index-T7AMvMzE.css -> index-DSS_xeJ7.css, stopped colour-*.css being emitted, put cs-pv-header (a selector that lives only in colour.css) into dist/assets/index-*.css where it is otherwise absent, and took the sheet to 362,148 raw / 61,979 gzip; both new tests red, the other five green; restored byte-exact and green again. ALSO FIXED: ten unit tests read src/styles/global.css directly rather than through tests/unit/appStylesheets.js and the split made 17 of their assertions VACUOUS rather than red — the exact failure that file was written after. Eight now read ALL_CSS; the two that report a file and a line read a new ALL_STYLESHEETS.' },
  { id: 'perf-hero-lcp-2026-09-13', title: 'The homepage headline paints with the stylesheet instead of waiting for the entry chunk', priority: 'P1', effort: 'M', area: 'Performance', status: 'done', note: 'MEASURED 2026-09-13 with the repo’s own instrument, scripts/home-field-metrics.mjs, 10 cold runs per configuration, every configuration in ONE session on ONE machine against origin/main 4129a914 — a before from one machine and an after from another is not a comparison. THE PROFILE, stated in full because a latency number without one is not a field metric: Playwright devices[Pixel 5] (393x851 CSS px, DSF 2.75, isMobile); CDP Network.emulateNetworkConditions at latency 150 ms, down 204800 B/s, up 96000 B/s — real packet-level shaping, not Lighthouse simulation; CDP Emulation.setCPUThrottlingRate 4x; HTTP cache disabled; a NEW BROWSER CONTEXT PER RUN; production `npm run build` under `vite preview` on a port verified free in the same command. THE DEFECT. [perf-critical-css-2026-09-13] cut FCP by 927 ms and LCP by 162 ms, and that gap WAS the diagnosis: first paint and largest paint were waiting on two different things. `grep -c home-hero-h1 dist/index.html` was 0 — the served shell painted `.boot-shell`, a skeleton whose `.boot-title` is an empty <i> — so the LCP element, SPAN.home-hero-line-in (the hero headline, in every run of every build), could not exist until 455,541 raw / 140,931 gzip of entry chunk had arrived, evaluated, hydrated React and laid the page out. The site already prerendered 39 route shells; scripts/prerender.mjs fixed the HEAD of each and left the body a skeleton. THE FIX. prerender.mjs now writes the real <h1> into the served `/` and `/home` shells, out of HERO_HEADLINE in src/data/positioning.js — imported, never restated — in the markup src/pages/Home.jsx renders, element for element, so the font, size, weight, line-height, letter-spacing, wrap points and position all come from ONE set of rules in src/styles/global.css rather than from a copy of them. BEFORE -> AFTER, mean / worst of 10: LCP 4119.6 / 4252 ms -> 1721.6 / 1756 ms (−2398 ms mean); FCP 1696.4 / 1716 -> 1721.6 / 1756; CLS 0 with 0 shifts either side. LCP NOW EQUALS FCP IN EVERY SINGLE RUN — the headline paints with the stylesheet. THE 2500 ms BUDGET IS MET, with 744 ms of headroom on the WORST run, and met WITHOUT the founder’s gated Firebase deferral. WITH THAT DEFERRAL as well (docs/design/firebase-deferral-gated.patch, VITE_DEFER_FIREBASE=1, same session): main + deferral LCP 3209.2 / 3584 ms, still well over; this change + deferral LCP 1588.8 / 1612 ms, with FCP the same figure again. ONE HONEST DEBIT, AND IT IS NOT THIS CHANGE’S. Interaction response worst came in over the 200 ms budget on THREE of the four configurations in this session, the untouched baseline among them: 216 ms baseline, 232 ms this change, 240 ms main + deferral, 208 ms this change + deferral. The same two unflagged builds measured 136 ms and 120 ms worst earlier the same day, before forty agents were on the machine. A budget that a build MISSES and its own baseline misses harder is machine load, not a regression — but it is recorded here rather than quietly dropped, and it wants re-measuring on a quiet machine before anybody acts on it. WHAT A VISITOR SEES BETWEEN FIRST PAINT AND HYDRATION: the headline, settled, exactly where it will stay, over a fixed skeleton nav standing in for the real one, with the `Loading UIL4B` live region still at the foot. Nothing under the headline is guessed at — the sub-line, the command bar and the buttons simply arrive beneath it, and the headline does not move when they do. AND WHY THE ENTRANCE DOES NOT RUN THERE, which was measured rather than assumed. With the shell hero left running the `home-hero-clip-up` entrance, three runs all read the same: clip-up START 1664 ms [shell], DONE 2734 ms [shell], START 3529 ms [react], DONE 4951 ms [react] — the headline rose, sat still for seven hundred milliseconds, then dropped and rose again, finishing five seconds in. That is a double-render with a good number attached. SUPPRESSING ONLY THE SECOND RUN DOES NOT WORK, and the reason is worth writing down: Chrome records a text element’s LCP size on its FIRST painted frame and never re-sizes it, so an animated shell headline is recorded at a 2369 px² SLIVER (the clip-up starts at translate 110% behind `.home-hero-line`’s overflow:hidden — tests/unit/hero-first-paint.test.js owns that geometry), and a settled hydrated headline is then a LARGER candidate that walks LCP straight back to hydration, silently, with the page looking identical. Both copies paint the settled box or neither does. So the `/` and `/home` shells are served with `data-hero-prepainted` on <html>, global.css turns the clip-up off for that hero alone, and src/pages/Home.jsx drops the attribute on unmount so arriving at `/` later in the session — from another route, into a page that is already there — is a real arrival and animates exactly as before. prefers-reduced-motion is untouched and still wins in both directions. The recorded LCP candidate is now one per load at 30883 px² instead of 2369 px². CLS HELD AT 0 AT EVERY WIDTH, checked directly rather than inferred. The hero was read in BOTH states of one page load — entry bundle held, then released — at 360, 390, 480, 768, 1024, 1280, 1440 and 1920: position, box and the five typographic properties that decide where a headline wraps are IDENTICAL at every width, with 0 layout shifts at every width. One real difference was found that way and fixed: `.boot-shell` declares its own Manrope stack and a class on a nearer ancestor beats `body{font-family:var(--font)}`, so the two headlines were inheriting different fallback chains — identical while the self-hosted face loads, divergent exactly when it fails, which is when a re-wrap would show. `font-family:inherit` on the homepage variant means one declaration feeds both. TWO VISITORS MUST NOT SEE THE PRE-PAINTED HEADLINE, AND THE ACCEPTANCE SUITE FOUND BOTH. A returning signed-in visitor is redirected off `/` by App.jsx from the synchronous `vs-session` hint, and 57-signed-in-session.spec.js samples every frame for `.home-hero` and requires it never appears; the shell now asks the same hint by the same key, answering a deliberately narrower question than rootDestination() — not "where does this visitor go" but "might this document not end up being the sales page". And ANY visitor handed the wrong shell: vercel.json rewrites each prerendered URL to its own file, but `vite preview`, which the suite runs against, has no such table and serves dist/index.html for EVERY path — /privacy came back with the homepage’s title AND its headline, and 47-lazy-route-readiness caught it as 67 characters of content on a route that had rendered nothing. So the gate checks the URL it was actually served at rather than trusting a routing table, which also means a future rewrite mistake costs nothing: the page falls back to exactly the skeleton it shipped before. NO FLASH, AND NOT BY LUCK: the removal runs from a MutationObserver, whose callback is a microtask and therefore lands before the rendering step that would paint the node or fire a requestAnimationFrame — which is precisely what that spec measures — and `html:not([data-hero-prepainted]) #boot-shell .home-hero{display:none}` is the belt, so the headline cannot paint even with the script never running. GUARDED. tests/unit/home-shell-hero.test.js is tests/unit/index-html-pricing.test.js for a sentence instead of a price: neither index.html nor the generator may hold a copy of the founder’s line, what the shell emits must equal heroHeadlineText(), and the element chain is read OUT OF Home.jsx so the two headlines cannot drift apart. The gate’s two values are pinned against the modules that own them, SESSION_HINT_KEY and HOME_SHELL_ROUTES, so a rename fails the build instead of silently turning the gate off. 04-premium-home.spec.js carries the shell-versus-React geometry comparison at 390 and 1440 — the pair 64-computed-style-snapshot uses — and waits for the stylesheet before reading, because reading earlier measures the UA’s 32px bold h1 and would pass on a state the browser never paints. 65-hero-first-paint.spec.js moved with the mechanism rather than being deleted: a cold load must run the clip-up ZERO times and record the headline at FULL size, and the frame-0 clip geometry it was written for is now asserted on the in-session arrival, which is the path that still uses it. MUTATION-VERIFIED AT THE CALL SITE, TWICE, each on a whole line of code rather than an expression — a regex on these files finds the COMMENT first, because the comments quote their own code. (1) `await writeFile(path.join(dist, index.html), homeShell)` -> `shell`: dist/index.html sha256 f1dd6f1c -> 2b7c098c, the `home-hero-line-in` needle 2 -> 0 and `data-hero-prepainted` 5 -> 4; the two cold-load tests in 65 and both geometry tests in 04 went red, the in-session and reduced-motion tests stayed green as the control. (2) the whole suppression rule `html[data-hero-prepainted] .home-hero-line-in{animation:none}` -> `.home-hero-line-OFF`: the emitted sheet moved global-BsL6c4IS.css -> global-D7375gjS.css, sha256 7778a53e -> f2116c85, that needle 1 -> 0 with `.home-hero-line-OFF` 0 -> 1, and dist/index.html unchanged at 2 — so the break was isolated to the CSS; 65’s two cold-load tests red, its other two green. Both restored byte-exact, `git diff` empty, rebuilt, green again. GATES: lint 0 errors / 22 warnings, the ceiling unchanged; build prints 39 route shells; test:unit 1886 passed 0 failed; test:users 1168 passed, 13 skipped (all of them in 12-ui-system-builder), 0 FAILED. The 73-founder-calls-0910 pair at 390px that three lanes reproduced on pristine main is gone from this run — #458 fixed it on origin/main, which this branch is rebased onto. 64-computed-style-snapshot passed on the baseline it was given and was NOT regenerated. THE VERDICT, honestly. The homepage LCP budget is met with headroom and without the gated patch, so the thing that was holding site Performance under 8 is cleared. That is not the same as certifying an 8: only the homepage was measured here, the entry chunk is still 455,541 raw / 140,931 gzip, and every other route still pays for it. The next lever is the JavaScript, not the HTML.' },
  { id: 'quality-account-2026-09-11', title: 'Rate the account and money surfaces out of 10 and raise everything under 8', priority: 'P1', effort: 'M', area: 'Quality', status: 'done', note: 'Rated /login, /projects, /settings, /checkout, /checkout/return, /feedback, /community, /admin and /onboarding out of 10 on the shared rubric, by rendering the built preview at 320/390/430/768/1024/1097/1120/1136/1280/1440/1920 in both themes, signed out, free-with-no-projects, free-with-projects, at the 3-project cap, Pro, admin, payment-succeeded, webhook-not-landed, no-session, offline, reduced motion and keyboard-only. FOUR things scored under 8 and are fixed; the rest scored 8+ and were left alone. (1) /checkout dimension 1 truth 3→8: with /api/get-prices unreachable the order summary painted an EMPTY amount above “per year” and a note reading “USD · null/mo” — the word null, in accent colour, on the screen where money changes hands. useProPrice reports loaded:true the moment the fetch SETTLES, including by failing. Now “Unavailable” plus Plans.jsx’s own outage sentences and its Retry live pricing control, so the two steps of one funnel cannot describe one outage two ways. No new copy was written. (2) /projects dimension 1 truth 3→8: the folder row printed “3 folders · Upgrade for 10” beside FOUR folder chips. FOLDERS is a fixed array and the repo has no folder-creation control at all (no addFolder/createFolder/newFolder anywhere), so neither number was true and the upgrade sold something Pro does not receive either — paying changed the string and nothing else. Claim and its orphan folderLimit removed; nothing replaces it. The project cap, which IS enforced by ProjectContext, still states itself. (3) /checkout/return + /checkout invalid-selection dimension 6 accessibility 6→8: every state rendered an <h2> as the page’s ONLY heading, including the one a person lands on straight after paying. Eight of ten account surfaces carry exactly one h1; these two carried none. Promoted, declarations unchanged so the paint is identical at 22px. (4) /admin dimension 6 accessibility 5→8: the “This device only” badge hard-coded color #854d0e while its own background is color-mix(var(--warn) 16%), which does flip with the theme. Pixel-measured off the painted screenshot: 5.55:1 light, 1.77:1 DARK, on 10px bold text. Now var(--warn-strong), the token global.css already carries for exactly this — 5.96:1 light, 7.90:1 dark. Evidence after: 231 rendered rows across 21 states × 11 widths show ZERO horizontal overflow; 200% zoom clean on all nine surfaces; 0 focus stops without a visible ring across 10 surfaces; settings’ five inactive panels are display:none + hidden with no phantom tab stops; reduced motion clean on /onboarding. STILL UNDER 8 AND WAITING ON THE FOUNDER, because the fix needs a sentence that does not exist in positioning.js or anywhere in the UI: /feedback dimension 1 scores 6 — a SIGNED-OUT submission carries email:’’ (Feedback.jsx builds the payload from user?.email), the form has no contact field, and the confirmation still reads “Your submission has been received and will be reviewed”. A person reports a bug signed out and there is no way to reach them, and the page never says so. Needs either a contact field with a label, or one honest sentence — founder’s call which. Also flagged, not decided: whether folders should be an entitlement at all (nothing creates one today), and OWNER-ACTIONS §2.3’s open question on the two Pro documents stays open — no surface in this lane states it either way.' },
  { id: 'quality-last-cells-2026-09-11', title: 'Close the last four cells under 8, and say why the remainder cannot move', priority: 'P1', effort: 'M', area: 'Quality', status: 'done', note: 'THE FOUR CELLS THAT WERE STILL UNDER 8 AFTER #447-#452. Three closed by engineering, one is the founder’s and is named below. Every number re-measured before anything was edited, on the built preview, at 320/390/430/768/1024/1097/1120/1136/1280/1440/1920 in both themes, reduced motion on and off, signed out and signed in. (1) /feedback purpose & content 6 → 9. A SIGNED-OUT SUBMISSION COULD NOT BE ANSWERED AND NOTHING SAID SO. Captured off the real request: {"type":"feature","subject":"[feature] Submission","message":"…","email":"","source":"feedback-form"} — five fields, none of which identifies the sender, on a form whose own type list offers "Help Request". /api/support has validated, capped and printed an `email` since it was written; only the form never offered anywhere to type one, so THE API CONTRACT IS UNCHANGED — same five keys, one of them can now be non-empty when signed out. Added: an optional Email field with the hint "Optional — but without it there is no way to reply to you.", a client-side check against the server’s own rule (utils/contactEmail.js, held to api/support.js by tests/unit/contact-email.test.js — which caught a real disagreement on its first run: the server exempts the empty string by identity, so a field holding three spaces was a 400 the page would have reported as a dropped connection), and a confirmation that states which of the two happened — "A reply can reach you at <address>." or "No email address went with it, so this one is one-way." Signed in it says "Sending as <address>", which is the sentence FeedbackModal.jsx already used for the same fact. (2) /create/gradient interaction & states 7 → 9. The stop hex reported border-width 0px, background rgba(0,0,0,0) and padding 0px in all 22 width/theme cells, beside a position field in the same row that has always had a box — one row answering "is this editable?" two ways. #450 left it because the value has 12.8px of slack at 320 and a box costs 14px. That measurement was right and the conclusion was not: below 481 `.ggn-stop` is a five-column grid and `.ggn-stop-pos` is pinned to column 3, so columns 4-5 of ROW 1 are empty — 58px of it — at every width under 481. `grid-column:3/-1` spends that instead: 80px → 144px, 14px to the box, 62.8px left over, row height unchanged at 93px. The field now takes the same four declarations its neighbour has, capped at 132px above 480 so a seven-character value is not painted in a 328px box. Five references read on Mobbin first (Gamma, Later, Zoho CRM, Runway, MagicPath) — all box the hex, and Later does it in a 200px sidebar. (3) /create/palette code quality 7 → 8. 3,528 lines in one component. NOT a decomposition: the largest piece that could leave without changing a pixel — the eighteen preview scenes and the four components that paint them, 280 lines — moved to src/components/palette/PalettePreview.jsx and src/data/palettePreviewScenes.js. 3,528 → 3,254. Every one of them is pure, prop-only, hook-free and reads nothing of the page’s state, which is why it could go; the icon set, the HCT picker and the board stay, because each is used by the page directly or reads its state. EQUIVALENCE IS THE EVIDENCE: 64-computed-style-snapshot passes on /create/palette against a baseline that was NOT regenerated for this move. 8 and not 9 — the file is still 3,254 lines and the next piece out is a real decomposition. (4) /login dimensions 3 and 4, 7 → 8. FOUNDER-BLOCKED AT 8. "Welcome Back" could title any product on the internet and no agent wrote a replacement — he has rejected agent copy on this dialog three times. What was non-copy and is now fixed: LoginRoute painted a `.fg-loader` unconditionally, so a ring turned behind the dialog for as long as it was open — a loading indicator for a page that has finished loading, 146px down, behind a 6px blur, and the only thing on screen behind the form; it now renders only while AuthContext is still resolving. And the dialog’s <h2> sat 8px above the primary action, against a measured rhythm of label→input 6, field→field 16, button→divider 20, divider→form 20, form→links 14 — so the title was grouped more tightly than any two form fields; it now takes 20px, the dialog’s own largest interval, with the max-height:460px landscape exemption restated after it so the 133px that block claws back at 844x390 is not spent (measured: the exemption is worth exactly 12px there). STILL BELOW 9 AND WHY. /login 3 and 4 sit at 8 and cannot move further from here: the sign-in state suppresses `.ui-login-aside` by a deliberate product rule ("a visitor who clicked Log in already knows why they are here"), so what is left is a competent, generic auth form whose distinguishing element is a heading only he can write. FOR THE FOUNDER, one question: does "Welcome Back" stay? A 9 needs a line that names UIL4B or names what signing in returns you to. Nothing has been written in the meantime. ALSO FOUND, NOT FIXED, SO THE NEXT SWEEP DOES NOT RE-INVESTIGATE: FeedbackModal.jsx carries the same one-way defect as /feedback did — `email = user?.email || \'\'`, no contact field, and "Sending as …" rendered only when there IS a user — and it is the form four surfaces and the right-click menu point at. Same fix, different surface; out of this lane’s four cells and left whole rather than half-done.' },
  { id: 'quality-colour-tools-2026-09-11', title: 'Every aspect of the six colour tools rated out of 10, and everything under 8 raised', priority: 'P1', effort: 'M', area: 'Quality', status: 'done', note: 'DONE for every dimension this lane could move without founder copy; the four that stay below 8 are named at the end and three of them are his call. THE SIX COLOUR TOOLS RATED OUT OF 10 ON EIGHT DIMENSIONS, BY RENDERING. Each surface was drawn at 320, 390, 430, 768, 1024, 1097, 1120, 1136, 1280, 1440 and 1920 in both themes, reduced motion on and off, signed out, signed in free with a saved project, at the free cap of three, as Pro, with a long project name, and with every panel and popover open — 132 structural cells, 703 text nodes measured for contrast per theme, a tab walk of all 64 stops, and 200% zoom reflow. SCORECARD, before -> after (purpose / navigation / copy / visual / states / a11y / adaptive / code). /create/color 9/9/9/8/8/9/9/9, unchanged — nothing scored under 8. /create/palette 7/8/8/9/8/8/9/7 unchanged: its two low marks are a missing sentence and a 3,528-line component, neither of which this lane may write. /create/semantic-color 9/8/8/7->8/5->8/5->9/9/6->8. /create/tint 9/9/9/8/8/9/9/8, unchanged. /create/gradient 9/8/8/8/7/6->9/9/8. /create/contrast 9/9/8/9/8/9/9/9, unchanged. WHAT MOVED, and the measurement behind each. (1) /create/semantic-color a11y 5 -> 9. The "Live UI proof" preview renders a real <input>, a real switch and three real <button>s per role so a colour can be seen behaving as a 1px border and as a 40px fill — and renders the whole set TWICE, light and dark. They do nothing. tabIndex={-1} had taken them out of the tab order and LEFT THEM IN THE ACCESSIBILITY TREE, so a screen reader met "Project slug, edit text, aurora-design-system", "Use anyway, button", "Update card, button" and "Learn more, button", then met the identical eight again in the dark copy. They were also counted as targets and failed: .stc-sc-ghost 71.7x21.0 and .stc-sc-link 54.3x14.0 in all 22 width/theme cells. The scene list is `inert` now — the treatment Toast.jsx already uses for a hidden toast, and the answer ContrastChecker.jsx has always given the same question (its specimen buttons are <span class="cc-spec-btn">, never <button>). Nothing is lost to a non-visual reader: the section paragraph 40 lines above already says what the scenes demonstrate, and it is unchanged. NOTHING PAINTED MOVED — there is no [inert] rule in any stylesheet here, and 64-computed-style-snapshot passed all 25 routes unregenerated. (2) /create/semantic-color states 5 -> 8. The Add-to-Project disclosure held none of the contract src/hooks/usePopover.js exists to give. Measured 2026-09-11 at 1440x900 signed in free: the trigger reported aria-expanded=null, aria-controls=null, aria-haspopup=null; Escape left it open; a press anywhere else on the page left it open; and while it was stuck open it covered the Material and Tailwind bundle cards. It uses usePopover now — Escape closes and hands focus back to the trigger, an outside press closes, opening moves focus into the field — and the trigger carries aria-expanded / aria-haspopup="dialog" / aria-controls. The project-name input had NO accessible name at all, only placeholder="Project name...", so a screen reader read "edit text, blank"; the placeholder string is now written once and used as both. (3) /create/semantic-color states, the free cap. The refusal went out through toast?.(err.message), which is the SUCCESS toast: measured signed in free with three saved projects, class="toast toast-success show" on a white ground reading "Free plan saves up to 3 projects — go Pro for unlimited.", held 1.8 seconds, nothing to press. That is the fault #436 named and #437 fixed on the Palette Builder and on /projects — this page still had it. It renders SaveRefusal now, in ProjectContext’s own words, under the field it refused, with "See what Pro adds" to /plans, and the typed name survives. The two save paths (Enter in the field, and the Save button) were separate handlers that could diverge; they are one commitSaveProject(). (4) /create/semantic-color visual + code 7/6 -> 8/8. The whole save/load block was THIRTY-ONE INLINE STYLE OBJECTS — fontSize: 10, padding: "3px 10px", letterSpacing: ".08em", width: 280 — the one region on the page not built from its own .stc-* classes, and the reason the Load chips measured 99.3x22.0 and 101.1x22.0 against a 24px minimum (the inline padding beat .btn-s). Every value is a token in src/styles/pages/semantic-color.css now and every control clears 24px. principle-design-tokens: page-level code should rarely depend directly on primitives. (5) /create/gradient a11y 6 -> 9. input.ggn-stop-hex is a real editable text field and measured 17.0px tall at every width above 640 and 21.0px at every width below it, in both themes, against WCAG 2.5.8’s 24px. Neither exception reaches it — it is an <input>, so Inline does not apply, and its two row-neighbours (the swatch trigger and the position field) are immediately adjacent, so Spacing does not either. min-height:24px, which costs NO horizontal width: the widest the value ever needs is 67.2px in an 80.0px box at 320, and an <input> centres its value, so the glyphs did not move. The rule’s own note records what a border would have cost — the field clipped "#7C3AED" to "#7C3AE" the last time it was too narrow — and the test asserts scrollWidth == clientWidth at all eleven widths. TWO MEASUREMENTS THAT LOOKED LIKE DEFECTS AND ARE NOT, recorded so the next sweep does not re-raise them. .cpk-hue read 23.6px inside a freshly opened picker: the popover rises in at scale(.985) and settles at 24.00px, which is why every read here waits on getAnimations() rather than on a timeout. .ggn-handle is 20x20 at every width and CONFORMS under 2.5.8’s Spacing exception — rendered, the three handles sit 364.4px apart and the nearest target of any other kind is 113.3px away, so a 24px circle centred on each intersects nothing; five gradient editors read on Mobbin first (Gamma, Framer, Jitter, Synthesia, Canva) all use a small handle on a thin track, and a 24px puck would be worse. WHAT CAME OUT CLEAN AND IS NOT CHANGED: zero horizontal overflow in all 132 cells and at 200% zoom on all six; zero contrast failures in either theme across 703 text nodes per theme (the probe was mutation-proved on every route by injecting a 2.81:1 node and catching it each time); a visible focus indicator on all 64 tab stops; every copy action answering with a toast AND a polite live region. STILL BELOW 8, and why. (a) /create/palette purpose 7 — the page’s only heading is a 15px h1 "Palette" in the toolbar and no sentence states the model, while its meta description promises "Generate a professional colour palette from one seed colour". Every sibling tool has that sentence and 58-tool-lede pins three of them. It needs one line from the founder; an agent writing it is the thing he has thrown out three times. (b) /create/palette code 7 — 3,528 lines in one component; splitting it changes behaviour and belongs in its own change, not in a rating pass. (c) /create/gradient states 7 — the stop’s hex is editable but wears no field affordance while its own row-sibling, the position input, has a border and a ground; five Mobbin references put the stop hex in a real field. Not changed because the width budget at 320 is 12.8px and a border plus inline padding needs at least that, so the fix is a layout decision for the row rather than a property. (d) FOUNDER CALL, no change made: the Contrast Checker’s live preview carries specimen small print reading "Free while in beta. Terms apply." — it is a specimen inside a labelled preview, but it is a claim-shaped sentence about a product, and UIL4B is not free-while-in-beta (Free plus Pro at $7/month, planLadder.js). Worth his eye. ALSO OBSERVED, NOT THIS LANE’S FILE: button.pgal-stripe in src/components/discover/PaletteGalleryGrid.jsx is 285x20 and the stripes are stacked adjacent, so neither 2.5.8 exception applies; it appears inside the Palette Builder’s Explore panel as well as on /discover/palettes. HELD IN tests/user-sim/75-colour-tools-quality.spec.js, 29 tests. MUTATION TALLY: 7 call-site mutations, 7 red, control green after each restore, every restore proved byte-exact by SHA-256 rather than by git checkout. M1 `inert` removed from .stc-scene-list -> 12 red of 29. M2 the trigger’s three aria attributes removed -> 1 red. M3 aria-label removed from the project-name field -> 1 red. M4 setSaveError -> toast?.() -> 1 red. M5 min-height:24px removed from input.ggn-stop-hex -> 11 red. M6 .stc-save-chip back to padding:3px 10px;font-size:10px -> 8 red. M7 usePopover(saveMenuOpen) -> usePopover(false) -> 1 red. THE SPEC’S OWN SETTLE HELPER IS WORTH READING before writing another: waiting for document.getAnimations() to be finished timed out on 25 of these 29 tests, because two kinds never finish — infinite ones (.stc-scene-ico--spin) and SCROLL-DRIVEN ones (.rail-overflow fades its edges with animation-timeline: scroll(self inline), on .ggn-presets and .plb-toolbar-group among others, so playState stays "running" for as long as the rail exists and getTiming().duration is the string "auto"). Both are skipped by what they are, not by which element carries them. VAULT PRINCIPLES CITED: principle-accessible-design (every control has a name, role and feedback state; a dialog dismisses on Escape), principle-design-tokens (semantic tokens are the public layer; page code should not reach for primitives), principle-ai-slop-diagnostic (system coherence — similar controls must not change treatment without a semantic reason), principle-website-engagement (purpose clarity on arrival). MOBBIN read before each change: Salesforce Experience Builder, Outseta Embeds, Ferndesk and Chatbase all render a live preview as a depiction rather than an operable form; Gamma, Framer, Jitter, Synthesia and Canva all give a gradient stop’s hex a real field height.' },
  { id: 'quality-palette-purpose-2026-09-11', title: 'The last cell under 8: the Palette Builder stops announcing itself as five colour swatches', priority: 'P2', effort: 'M', area: 'Quality', status: 'partial', note: 'THE LAST CELL UNDER 8 AFTER #447-#453. /create/palette purpose & content 7 → 7: HALF OF IT WAS STRUCTURAL AND IS FIXED, THE OTHER HALF IS ONE SENTENCE ONLY THE FOUNDER CAN WRITE, AND IT IS SPELLED OUT AT THE END OF THIS NOTE. THE FINDING THAT CAME IN, from quality-colour-tools-2026-09-11: the page’s only heading is a 15px h1 "Palette" in the toolbar and no sentence states the model. Both halves were re-measured from scratch before anything was edited, on the built preview at 320/390/430/768/1024/1097/1120/1136/1280/1440/1920 in both themes, reduced motion on and off, signed out, signed in free, at the free cap of three and as Pro. WHAT THE MEASUREMENT ADDED, and it is bigger than the finding said. Read off Chrome’s OWN ACCESSIBILITY TREE rather than off the markup, the landmark list for this route was: navigation(Primary) | main | contentinfo | region("PRIMARY #664BB7") | region("SECONDARY #CE70B4") | region("ACCENT #B39CFF") | region("SUBTLE #E6E0EC") | region("DEEP #352565") | navigation(Footer). FIVE OF THE NINE LANDMARKS ON THE BUSIEST TOOL IN THE PRODUCT WERE COLOUR SWATCHES, named with raw hex, and the count GREW with the palette — pressing Add made a tenth landmark, up to PRO_MAX. The board the page exists to produce was anonymous. The global adjust strip had been given the name "Global palette adjustments" in source and a reader never heard it, because a <footer> that is a descendant of <main> maps to the generic role and a generic element takes no accessible name. None of this is visible in the markup: nothing in the source says "region". The role came from the ELEMENT — a <section> WITH an accessible name is a landmark and one without is generic — which is why both the purpose rating and the a11y rating (8, unchanged, not re-scored here) walked past it. WHY THAT IS A PURPOSE DEFECT AND NOT ONLY AN ACCESSIBILITY ONE. The landmark list is how a non-visual reader answers "what is this page made of" on arrival, which is the first clause of dimension 1 in the rubric and principle-website-engagement’s sixth dimension, purpose clarity — identity and primary tasks explicit on arrival. On this route the answer was five hex codes. principle-accessible-design lists landmark order beside heading hierarchy under screen-reader-ready for the same reason. THE GAP AGAINST THE FIVE SIBLINGS, measured at 1440x900. /create/color h1 "One colour system, start to finish." 95.04px/800, 1 h2, 5 h3. /create/semantic-color h1 "Semantic Colours" 64px/500, 3 h2, regions named "See each role do its job" and "Canonical, predictable token names". /create/tint h1 "Tint Scale Generator" 64px/500, 3 h2, regions named "Choose source colours", "Tune the system", "Evaluate the system". /create/gradient h1 "Gradient Generator" 64px/600, 3 h2, region "Shape the gradient" plus complementary "Refine & export". /create/contrast h1 "Colour Contrast Checker" 36px/500, 1 h2. /create/palette: ONE heading in the entire document, h1 "Palette" at 15px/700, 51.8x24.8, identical at all 22 width/theme cells and identical signed out, free and Pro; ZERO h2. So the siblings use <section aria-labelledby> to make a SECTION a landmark named by its own h2, and this page used <section aria-label> to make an ITEM a landmark named by a hex. Same element, two meanings, which is the failure principle-ai-slop-diagnostic names under system coherence. WHAT WAS CHANGED, and none of it is a word. (1) Each .plb-col is a grouping role instead of a named <section>, so a swatch is an item in the board rather than a landmark of the page. (2) .plb-board carries a grouping role and takes its name from the h1 by reference rather than from a second typed string, so the artefact is findable and cannot drift from the page heading. (3) The adjust strip carries a grouping role, which is what DELIVERS the name already written on it; the string is untouched. (4) THE HOMEPAGE HAD THE SAME FAULT, because HomeWorkbench.jsx renders the same board: five of the ten regions on / were swatches, sitting among the five real ones named by its own h2s. Fixed the same way, in the same commit, because leaving it would mean one board with two answers. THE CORRECTION WAS ALREADY IN THIS REPO: HomeWorkbench.jsx has painted .plb-board with a grouping role and a name since it was written, and .plb-badges has grouped its two contrast readings that way since launch — so this is the board agreeing with itself, not a new invention. AFTER, re-measured the same way. /create/palette landmarks 9 → 4 (navigation Primary, main, contentinfo, navigation Footer) in ALL 44 cells — 11 widths x 2 themes x reduced motion on and off — and unchanged signed in free at the cap and as Pro; zero hex-named landmarks anywhere; zero horizontal overflow at every width. Adding a sixth colour now leaves the count at 4 (it was 9 → 10). Homepage 14 → 9, with its own five h2-named regions intact at 390 and 1440. WHY THE CELL STAYS AT 7 ANYWAY, honestly. Nothing a sighted visitor sees has changed, and the finding’s second half is still true. main.innerText on arrival at 1440 is twenty-four control labels and fifteen data values and NOT ONE SENTENCE: Palette / SYSTEM / Auto / Image / Explore / Preview / VISION TYPE / Normal / Gradient / Randomise / Space / Undo / Reset / History / Save / export / five colour names, hexes and role labels / ADD / HUE 0deg / SATURATION 0% / TONE 0% / TEMPERATURE 0 / Copy CSS. At 390 even the role labels drop out, so the page shows the word Palette and five colours and nothing relates them. The h1 is also 15px/700 — SMALLER than the 16px body text, and the exact size and weight of .plb-hex, the hex readout inside each swatch, so the page’s name is set identically to a colour’s value. That is the rubric’s 7 anchor word for word: works, but a rough edge a user would notice, a vague label. Raising it is a visual decision inside a row whose geometry 53-palette-toolbar-fit pins at 57px, not a heading fix, so it was left and named. WHAT WAS DELIBERATELY NOT WRITTEN, AND WHY. No description, tagline, subtitle or what-this-does line was added. positioning.js was checked rather than assumed: all four founder lines are product-level (forget-the-app-name, bookmark-folders, build-and-export, one-unified-location) and none of them says anything about a seed colour or a harmony system, and the module states in its own header that a new claim needs him to say it. SURFACE_LINE has no slot that fits. So there was nothing legitimate to borrow. THE H1 TEXT WAS ALSO LEFT ALONE ON EVIDENCE, not timidity: the tool carries three names — h1 "Palette", document title "UI L4B | Palette Generator", and every cross-link to it ("Continue in Palette Builder" on /, "Open in the Palette Builder" on /discover/palettes) — and it is tempting to make the h1 match the title. It must not. The anti-slop sweep recorded under brand-anti-ai-audit already deleted "Palette Generator" from the 404 suggestions as WRONG and set the rule that the h1 is the SURFACE NAME, and the surface name in toolTree.js is "Palette". Renaming the h1 would restore a string a previous lane removed on purpose. EXACTLY WHAT THE FOUNDER NEEDS TO WRITE, so this is a decision and not a research task. ONE sentence in the shape 58-tool-lede-states-the-model.spec.js already pins for the three tools that have one: name the two facts the artefact is made of, then say that everything below is those two facts. His three existing ones are "One base colour and one curve. Every step below is that colour at a measured tone." (tint), "Colour stops and where each one sits. Everything below is those two facts, as CSS, Tailwind or SVG." (gradient) and "Two families — one for headings, one for body. Every preview below is those two, together." (font-pair). The two facts here are the seed colour and the colour system, and the thing a visitor cannot infer from the screen is that every column is derived from those two. AND HE ALSO HAS TO SAY WHERE IT GOES, which is the real reason an agent cannot finish this: unlike its five siblings this page has no hero to put it under, the toolbar row is 57px carrying seven distinct font sizes from 9.5px to 16px, and there is no slot. Sentence plus slot is a design call on a full-bleed canvas, not a heading fix. HELD IN tests/user-sim/82-palette-purpose.spec.js, 7 tests, asserted on the COMPUTED accessibility tree through CDP rather than on markup — a markup-shaped assertion would pass the day someone restores the named <section> and adds the role by hand, and fail the day someone finds a different correct answer. The probe is mutation-proved inside its own run: one test injects a named <section> and a grouping div of its own and fails unless the probe reports the first as a landmark and the second as not, so a probe that stopped resolving names cannot make every other test pass on an empty result. Two positive controls guard each absence (the five columns are really on screen, and they still CARRY their colour names) and one fences the siblings, so a later sweep cannot harmonise /create/tint, /create/gradient and /create/semantic-color onto this branch’s answer and delete their landmark maps. MUTATION TALLY: 4 call-site mutations, 4 red, every restore proved byte-exact by SHA-256, and every mutation proved to have REACHED THE BUILT OUTPUT before the spec was believed — the chunk’s content hash changed and the needle was absent from all 140 emitted js assets, not just from the one chunk it was expected in. M1 the board column back to a named <section> → 2 red. M2 the board stops taking its name from the h1 → 1 red. M3 the adjust strip loses the role that delivers its name → 1 red. M4 the homepage column back to a named <section> → 1 red. TWO ANCHORS HAD TO BE LENGTHENED MID-ROUND and it is worth recording why: M2 on its own attribute left a reverse anchor of ">", which matched 1224 times, and M4’s close tag matched a second block further down HomeWorkbench.jsx. Both were caught by the driver asserting exactly one occurrence in BOTH directions rather than only on the way in. GATES: lint 0 errors / 22 warnings, build printed 39 route shells, unit 1861 passed 0 failed, users 1149 passed / 13 skipped (all 12-ui-system-builder) / 1 failed. THE ONE FAILURE WAS A DELIVERY FAILURE, NOT A REGRESSION: 68-core-tools-breakpoints reported "build asset request(s) never arrived"; re-run alone it is 18 passed, 0 failed, and it exercises the typography and tint tools, which this branch does not touch. 64-computed-style-snapshot passed all 25 routes UNREGENERATED — the baseline is untouched because a role attribute has no rule in any stylesheet here and <section> and <div> are both display:block before .plb-col sets its own, so NOTHING PAINTED MOVED on any route including /. 10-home-chaos-to-calm 36, 04-premium-home 3, 65-hero-first-paint 3, 11-palette-recovery 18, 53-palette-toolbar-fit 5, 21-reflow-320 11, 58-target-size-24 5, all green. VAULT PRINCIPLES CITED: principle-website-engagement (dimension 6, purpose clarity — identity explicit on arrival; and its rule that typography serves reading rather than branding, which is what a 15px h1 under 16px body breaks), principle-accessible-design (screen-reader-ready names landmark ORDER beside heading hierarchy, and its second audit question — does a screen reader navigate logically from heading 1 to content), principle-ai-slop-diagnostic (system coherence: similar treatment without a semantic reason). MOBBIN read before the change, on how a creation tool titles a workspace and a set of swatches: Mural, Discord, V7 and Canva all put ONE heading over the whole set and give the individual swatches a name and a value and no structure of their own; Arcade ("Colors" then "Palette 1"), Gamma ("Theme 01" then "Theme palette"), Squarespace ("COLORS" then "Edit Palette") and MagicPath ("Manage design systems" plus one model sentence, then "COLORS") all name the SET and leave the swatch as an item; Stitch and Sketch the same; Craft is the counter-example, a bare canvas with a floating toolbar and no title at all, which is the shape this page is closest to and the reason its h1 was not grown into a hero.' },
  { id: 'doc-audit-2026-09-10', title: 'Every document re-checked against the code, and one question the founder had already answered', priority: 'P2', effort: 'M', area: 'Docs', status: 'done', note: 'DONE 2026-09-10. Documentation pass over every file in docs/, plus README.md, on the founder ask to clean up the documents. EVERY CLAIM CHECKED AGAINST THE CODE RATHER THAN AGAINST THE PREVIOUS VERSION OF THE DOCUMENT, which is the whole difference between this pass and the three before it. THE WORST FIND, and it is doc-authority-map failure mode 6 for the SECOND time: OWNER-ACTIONS 2.1 asked the founder to pick a homepage hero shape by opening ?hero=a, ?hero=b or ?hero=c. He RETIRED that exploration himself on 2026-09-07 (his words, in the Home.jsx and TryItMark.jsx comments: Retire it, V2 hero decides) and the three sketches and the switch were deleted from src/ in the same change. So the item was impossible AND already answered, and it survived four merged PRs -- one of which re-derived the hero shape is still open from a proposal instead of from the code. PROPOSALS P-006 was PENDING the whole time too. Both closed; the rule in the map now says to grep the code before leaving an open question standing. OWNER-ACTIONS CORRECTIONS: 1.1 said our half of the deploy fix was in flight on perf/ffmpeg-core-off-origin -- it landed 2026-09-06 and the deploy is 5.6 MB. 1.3 promised the applier finds a Java 21 for you; it looks for one at a fixed path and stops if it is not there, AFTER the four changes are already written to his files. 4.6 asked only for a Stripe coupon, but api/create-portal.js reads process.env.STRIPE_RETENTION_COUPON and the name RETAIN50 appears nowhere in the code -- and its cost of doing nothing understated the truth: a Pro subscriber has no way to cancel AT ALL today, which is why Cancel any time came off /plans. SIX FOUNDER DECISIONS ADDED that the #429-#439 audits surfaced and nobody wrote down where he would look: the export entitlement (2.3), the condition banners over the Palette Builder sticky toolbar (2.4), four small design calls -- gradient stop handle, emoji tab pill, semantic-colour scene buttons, the 8px SOON tag (2.5), and whether SEO, Marketing and AI assistants belong in Learn (2.6). The two sentence lists -- the founder-note four and the anti-slop audit longer one -- are reconciled into one 2.2, each slot with the file it lives in and what stands there today. RE-MEASURED RATHER THAN ASSUMED: 2.1 (muted ink) is GENUINELY still open. The brief for this pass suspected #423 and #434 had settled it; they had not. #423 own record says the current homepage reaches NEITHER walk, 0 of 8,000 palettes, because derivePreviewRoles pre-clamps muted, and #434 fixes are the FREE badge and Remove in the tool menus, not the preview. 19.1 percent and 3.02 percent both stand. THE ONE THING DELIBERATELY NOT DECIDED, and it is flagged in three places instead: README.md and docs/reference/positioning.md both say every export format is free with a visible footer credit and Pro removes the credit -- founder-approved 2026-08-20, with that document explicitly saying any surface implying exports are Pro-only was the thing that was wrong. src/config/exportFormats.js marks book and guidelines pro: true, proOnlyFormats() returns both, Plans.jsx prints them as Not included for Free, and Home.jsx PRO_INCLUDES lists them under Everything in Free, plus -- beside a separate line for the credit removal, so the homepage sells both models on one screen. The doc claim is still exactly right for html, md, png and jpeg, all pro: false and all still passing watermark: !isPro; the two Pro documents shipped AFTER the decision. Both sentences kept, both flagged, neither edited to match the other. It is a decision about what Pro sells: OWNER-ACTIONS 2.3. OTHER DEFECTS CORRECTED: RELEASE-READINESS said Learn is live -- five published guides against seven in learnIndex.js (the same figure had already gone stale in llms.txt), carried the rules suite at 37 pass in a row three sections below its own 105, said the moderator role works everywhere except the two files in 1.3 when 1.3 covers four files and deliberately does NOT cover api/verify-admin.js, and listed the same command as items 3 and 11 with two different times against it. architecture.md typed api/_lib helper list had gone stale a SECOND time -- aiGeneration.js missing -- in the very paragraph naming moderators.js as proof the list goes stale; replaced with ls api/_lib, per the #312 pattern. positioning.md Export and Handoff names two live: false formats and one, Style Dictionary, that does not exist anywhere. THE FIVE AUGUST DOCUMENTS each gained a dated status header and NONE was deleted, because each records why something was decided. anti-slop-and-hero and homepage-spec and homepage-patterns are SUPERSEDED RECORDS -- the first critiques a homepage that never existed because #264 never landed, and its Option A hero line is now one of the rejected headlines 56-founder-rejected-headlines walks; all three stay because P-008, P-009, P-010 and P-011 cite them and deleting one would leave a live proposal citing nothing. motion-reference is marked LIVE, the only one: it is the founder own and records taste, which nothing in the product can date. defect-register is a LIVE INDEX -- re-checked, the citations it resolves are still in the tree. GUARD ADDED: tests/unit/doc-file-references.test.js, the first thing in this repository that fails when a document names a file that is not there. Reads every .md under docs/ plus README.md and CLAUDE.md, extracts repo-rooted paths in backticks, fails if the file is absent. Narrowed twice so a red is always real: repo-rooted paths only (a bare tokens.css is a generic noun), and an explicit TOMBSTONES list for documents that name a deleted file BECAUSE it is deleted -- the CHANGELOG.md case the map says not to repair. It found aiGeneration.js missing on its first run and then caught five dead paths in prose written during this same pass. It does NOT close the whole of failure mode 2 and says so: a path that exists is not a path a route reaches, so IconLibrary.jsx would still pass. Three mutations, three red, all restored byte-exact. STRANDED BRANCH ADJUDICATED: docs/consolidation, three commits, never pushed. WRITE-OFF, nothing brought forward, and the verdict is written into doc-authority-map so nobody re-derives it. Its merge-base is 2026-07-30, 266 commits behind main. Every document it deletes was already deleted by another route. The two it edits most heavily -- docs/BUILD-PLAN.md and docs/DECISIONS-NEEDED.md -- are MODIFICATIONS on that branch and main deleted both deliberately in #208, one week after it forked, so rebasing would resurrect exactly what that pass removed. Its only two edits worth having (the README Google Sheets row losing its dead docs/google-sheets-setup.md link and gaining GOOGLE_SHEETS_WEBHOOK_SECRET, and discover.md Workspace to Create plus the 2026-07-31 Firebase note) are ALREADY on main, arrived at independently -- checked line by line. Its api/support.js hunk is product code and was not read. NOT DONE, deliberately: no product code and no copy on a page was changed. Two things a doc is right about and the code is not are recorded as findings rather than fixed -- the pipeline deploy condition row still says the ffmpeg fix is in flight on perf/ffmpeg-core-off-origin when it landed, and that row renders in the Admin pipeline view (the phrase this note first used for that board collided with a getByText assertion in 57-signed-in-session, which is a real reminder that pipeline notes are RENDERED TEXT and not a private log -- and the reword was then LOST once in a rebase and had to be re-applied, which is the same hazard one layer up).' },
  { id: 'ai-brand-starter', title: "Beta: generate a palette, a font pairing and a type scale from a prompt", priority: 'P1', effort: 'M', area: 'AI', status: 'done', note: 'DONE, and it is a REVIVAL rather than a new feature - which is the finding. Founder request 2026-09-06: "lets include an beta feature that \'uses AI\' to generate a pallete, font, and more from a prompt from a user. free users will get 1 free usage, paid get more." WHERE IT ALREADY LIVED. /create/auto-builder existed, and src/data/moduleBoard.js filed it under area \'AI\' as a "Dormant alpha \'describe your business -> UI\' implementation; public activation is deferred", with its next step recorded verbatim as "AI mode deferred to stay under Vercel 12-function limit - revisit". So the answer to "where does this live" was written down before the request arrived, and the blocker it names is gone: /api/ai is a consolidated multi-task route, so a generation is a MODE inside it and costs zero functions (api/ still holds exactly 12 .js files; account-deletion.test.js fails the build on a 13th). The tool MOVED from the UI Component Builder group into AI Studio, was flipped soon:false, relabelled Brand Starter and badged Beta. The route and the id are unchanged, so no redirect was needed and every existing link still lands. WHAT WAS DELETED. The 671-line page behind that route. It was unreachable (absent from CreateTool\'s LIVE_TOOLS, so the URL rendered the workshop state; nothing imported the module, and eslint here cannot see an unused React component - it was found by grep). It also could not have shipped: it called itself AI and was a string hash (hashSeed) choosing between six hard-coded mood profiles, it painted a "decorative gradient orb" at blur(40px), and it rendered a fake marketing hero with invented copy ("Professional services you can trust") over the generated colours. That is the anti-slop bar\'s "decorative product mock-ups contain implausible data" plus "glow, blur, gradients layered as a generic premium signal", on the one surface where the founder\'s own "AI slop" worry is sharpest. WHAT "AND MORE" IS: exactly one further artefact, the TYPE SCALE. Palette, fonts and type scale are the first three steps of the brand-kit walkthrough (BRAND_KIT_STEPS), and they are the three a description can honestly answer. Icons were rejected because /create/icons reads the Iconify catalogue over the network and a model naming icon ids it cannot see would be inventing them; a gradient was rejected because it is decoration rather than a system part and partsPresent() does not read one. NOTHING IS AUTO-APPLIED. The result could write straight into ProjectContext\'s design and appear in every tool at once; it deliberately does not, because that store holds work in progress and overwriting it from a text box is the founder\'s own "I open the palette builder and it has added many colours and it\'s a different swatch" in another form. Each artefact carries an explicit hand-off through machinery that already existed and is already tested: paletteBuilderUrl() for the palette (the same ?c= URL the Discover galleries and the homepage starters use - a real URL, so it survives a reload and can be shared), setPairDraft() for the fonts and setScaleDraft() for the scale. No new hand-off mechanism, and no destination tool changed. Both slot stagers are guarded with navigatesThisTab(), so a Ctrl/Cmd-click cannot strand a draft in a tab that never navigates. THE ALLOWANCE IS ONE TABLE, TWO SURFACES. src/config/aiGeneration.js is what /plans and the tool print; api/_lib/aiGeneration.js is what the server enforces; tests/unit/ai-generation-truth.test.js IMPORTS BOTH and fails the build on any disagreement - the same arrangement api/_lib/plans.js has with src/config/plans.js, and the reason /plans no longer types a figure it should be importing. Free is 1 per ACCOUNT for ever (bucket uid_life), Pro is PRO_MONTHLY_GENERATIONS=20 on the same uid_mYYYY-MM bucket every other AI task uses. Both live in daily-usage, which appears nowhere in firestore.rules and is therefore default-denied to every client - the same property provider-health relies on, and what kept this clear of a founder-gated rules change. The two plans are told DIFFERENT things when they run out, because telling a free user their allowance "resets on the 1st" is a lie they act on by returning in a month to the same wall. A FAILURE COSTS NOTHING, and that is structural rather than promised: every failure path in runBrandStarter answers on res itself and returns nothing, and the handler\'s increment sits AFTER the early-return guard. Mutation testing pins the ordering, and the rendered spec proves it the only way that actually proves it - fail, then succeed. COST CONTROLS: brief capped at 500 chars and floored at 12 (a too-short brief is refused before any provider call, so it costs neither money nor a unit), maxTokens 900, temperature 0.5, and NO RETRY - a malformed answer is refused rather than re-asked, because retrying doubles spend on the request most likely to fail twice. The model may only pick fonts from a 34-family shortlist that a unit test checks against src/data/fallbackFonts.js, family, category and both weights, so "the model must pick from families that exist" is checked rather than hoped for; anything off the list is REFUSED, never substituted, because a silent substitution shows the user a typeface they did not get. The whole answer is rebuilt field by field (the sanitizeCommunitySubmission rule), so an invented key cannot ride along. IT SHARES THE PROVIDER RACE with generate-prompt on purpose - a private second path would be a second thing that can fail silently, and the provider-health apparatus exists because the first one did. Same OpenRouter-primary/Gemini-fallback order, same counters, same once-a-day operator email, and the response carries `provider` which the page renders as the same badge both prompt tools use. MEASURED, NOT CLAIMED: the result states the contrast ratio between the generated Text and Background roles using the product\'s own contrastRatio(), and says plainly when it FAILS. A generator that grades its own homework generously is worse than one that does not grade it. MOBBIN: Customer.io Design Studio (Beta chip beside the feature name; an inline failure banner naming the real cause with the existing values untouched below it) drove the badge and the error state; HubSpot Generate brand voice (a live 541/10,000 counter, a BETA nav badge, a "Review and edit" final step landing in the real Brand Kit fields) drove the character counter and framing the result as a draft; Bloom\'s Design Language (colours as a bare swatch row, fonts as Aa Bb Cc specimens with the family named beneath) drove specimens on hairline rules instead of three equal cards; Base44 (limit notice as a thin strip on the composer with one text link) drove the quota wall. Krea AI\'s full-screen modal of three gradient pricing cards was looked at and REJECTED as the model for that state. STATES, all rendered and all asserted: idle, too-short, loading (words, no shimmer, no skeleton), result, provider failure, quota-exhausted, offline (the brief is kept), signed-out (AuthGate - metering follows the account, so there is no anonymous path to a provider call). GATE: lint 0 errors / 25 warnings (baseline 25); build 36 route shells, UP ONE from 35 because /create/auto-builder stopped being Soon and became eligible for a crawlable shell - vercel.json, sitemap.xml and the OG cards were regenerated by their own scripts rather than hand-edited; unit 1459 pass 0 fail; users 605 pass 0 fail with the 13 expected skips in 12-ui-system-builder. MUTATION-VERIFIED 21/22 with a green no-op control on both halves (14/14 unit, 7/8 rendered). Two findings from the mutation runs, both kept: the unit control went RED first time because a source-text regex used a bare \\n against a CRLF checkout, which had made all fourteen "kills" meaningless; and `if (used >= bucket.limit && false)` survived an ordering assertion, so the guard\'s exact shape is now pinned as well as its position. The one documented non-kill is /plans typing the literal the derivation returns - the rendered DOM is identical either way, and that mutation IS killed by the unit half. FOR THE FOUNDER TO DECIDE, none of it blocking: (1) PRO_MONTHLY_GENERATIONS = 20 is a defensible reading of "paid get more", not his instruction - it is one named constant in src/config/aiGeneration.js and its mirror. (2) The URL still says /create/auto-builder while the tool is called Brand Starter; renaming it means a legacy redirect and an SEO call. (3) The free allowance is one per account FOR EVER - a monthly refill would be more generous and cost more. (4) A downgraded Pro user falls back to the lifetime bucket, which will already read spent, so they get none; that is defensible but it is a pricing decision. (5) The tool writes nothing into the open project by design - if he wants an Apply to my project button, that is a separate change with its own overwrite-confirmation.' },
  { id: 'stripe-subscription-period-end-null', title: 'The webhook reads current_period_end off a Subscription object that no longer carries it', priority: 'P1', effort: 'S', area: 'Billing', status: 'done', note: 'DONE, and the field was being read off the wrong object exactly as recorded. RE-VERIFIED IN THE INSTALLED SDK before touching anything: node_modules/stripe/cjs/apiVersion.js still pins 2026-05-27.dahlia, SubscriptionItems.d.ts:54 declares current_period_end: number as a REQUIRED field on the ITEM, and every occurrence in Subscriptions.d.ts is a list filter (current_period_end?: RangeQueryParam | number) or docstring prose. So api/stripe-webhook.js:49\'s sub.current_period_end read undefined on every SDK path and currentPeriodEnd was written NULL on healthy renewals. THE FIX IS ONE READER, in api/_lib/billing.js: subscriptionPeriodEnd() takes the MAXIMUM current_period_end across sub.items.data (not items[0] - a multi-cycle subscription has several period ends and understating it revokes a paying customer a cycle early), falling back to the legacy top-level field so a webhook endpoint still pinned to a pre-basil API version keeps working. The whole subscription document moved with it into subscriptionDocFields()/writeSubscriptionDoc(), because api/checkout-status.js now has to write the identical document (see [checkout-status-subscription-reconcile]) and two hand-written copies drift. PROVED WITHOUT LIVE STRIPE, three ways: the SDK type declarations are read by the test itself, so the day Stripe moves the field again this fails and says so; the fixtures are the dahlia and pre-basil shapes; and the WIRING test imports api/stripe-webhook.js\'s own writeSubscription (db is now injectable) and asserts the document THAT function hands Firestore. MUTATION: reverting writeSubscription to the pre-fix inline body turned 2 of 9 red - the call-site assertion and \'the stale-period safety net actually fires\', which is the one that mattered; byte-exact restore returned 9/9. The safety net at api/_lib/plans.js:112 and SubscriptionContext.jsx:71 is armed again, so a subscription whose webhooks stop arriving no longer stays Pro on status alone. Settings\' renews/access-until date and billingState.js\'s cancel banner come back with it. The ordering gate that billing-state.test.js guarded moved file with the document shape and was re-pointed, not weakened - it now asserts the gate in api/_lib/billing.js AND that the webhook delegates rather than building a second copy, and the behavioural half is asserted at the call site. tests/unit/subscription-period-end.test.js.' },
  { id: 'checkout-status-subscription-reconcile', title: 'A paid subscription checkout has no fallback if the webhook never lands', priority: 'P2', effort: 'S', area: 'Billing', status: 'done', note: 'DONE. The one-off path reconciled inside a transaction and the subscription path did nothing at all, so a checkout whose checkout.session.completed AND customer.subscription.created deliveries both failed left a customer who had paid on Free - while /checkout/return told them flatly \'Your Pro subscription is active\'. api/checkout-status.js now calls reconcileSubscriptionCheckout() from api/_lib/billing.js after the lifetime block, writing through the SAME writeSubscriptionDoc the webhook uses so the two paths cannot drift (that is why the document shape moved - see [stripe-subscription-period-end-null]). IT MAY ADD ACCESS, NEVER FABRICATE IT, and each refusal is a test: ownership is still the 403 above it on the session\'s server-stamped firebaseUid; Stripe\'s own status decides the outcome, so an incomplete subscription reconciles to FREE; a customer mismatch refuses loudly and writes nothing; and accessRevoked is an absolute veto, because Stripe commonly still reports active through a dispute and a re-read must not become the way around the sticky flag. A Stripe failure here is logged with the message and type, never swallowed. THE HONESTY HALF: the endpoint now returns subscriptionActive alongside entitlementActive, and src/pages/CheckoutReturn.jsx gates activationPending on it for BOTH modes - a subscription that has not attached yet reads \'Your payment is confirmed. Pro access is still being attached to this account\' instead of a claim that is not true yet. PROVED WITHOUT LIVE STRIPE with a Stripe double and a recording Firestore double; the reconciled document is asserted deep-equal to subscriptionDocFields(), so a drift between the two writers fails here. MUTATION: stubbing the reconcile out of checkout-status.js turned it red, and separately reverting CheckoutReturn.jsx to gating on mode === payment alone turned it red; both restored byte-exact to green. tests/unit/subscription-reconcile.test.js.' },
  { id: 'stripe-product-description-overclaims', title: 'The Stripe product description promises 1,000 AI actions a day; the server allows 30', priority: 'P1', effort: 'S', area: 'Billing', status: 'done', note: 'DONE - the CODE half, which was the whole of the agent-doable work; the founder half is confirmed present and accurate in docs/OWNER-ACTIONS.md (it was NOT there, and has been added as 4.12 with the four required fields). TWO false claims, not one: \'1,000 AI actions per day\' against a server that enforces 30, and \'full design JSON export\' - json in src/config/exportFormats.js has never carried live: true, so it was the same unbuildable file /plans was corrected for, still being sold on the receipt. That is the worst place for either: Stripe prints the PRODUCT description on the embedded checkout, on the emailed receipt and on the invoice, so it is the one copy of the promise that reaches a customer with money already in hand. THE MECHANISM IS THE FIX, and it is the exportFormats.js pattern applied to the receipt: api/_lib/plans.js exports proProductDescription(), which derives the sentence from dailyLimitFor/monthlyLimitFor on the same PLANS.pro object the server enforces, and api/setup-stripe.js\'s PRODUCT_DESCRIPTION is that call. tests/unit/stripe-product-truth.test.js fails if the number in the sentence is not the number enforced, if the client mirror AI_LIMITS.pro disagrees, if any format exportFormats.js does not mark live is named, if what it DOES sell is not both built and gated, or if setup-stripe.js goes back to typing the string. AND THE HALF THE REVIEW CALLED THE FOUNDER\'S: findOrCreateProduct only ever CREATED, so once the product existed the description on every future receipt was frozen at whatever the code said the day it was first run - which is precisely how the old sentence outlived every fix to the constant. It now calls stripe.products.update when a live product\'s description differs, touching the description key and nothing else, so a name or price tuned in the dashboard is not undone. Asserted with a Stripe double, three ways: it corrects a stale product, it leaves a correct one completely alone, and it still creates when there is none. MUTATION: reverting PRODUCT_DESCRIPTION to the typed string turned 4 of 10 red; reverting findOrCreateProduct to create-only turned 2 red; both restored byte-exact to green. WHAT REMAINS FOR THE FOUNDER: nothing runs that update until they POST /api/setup-stripe (or edit the description in the dashboard), so the live product keeps the old sentence until they do - OWNER-ACTIONS 4.12.' },
  { id: 'get-prices-swallows-stripe-failure', title: '/api/get-prices serves fallback prices silently when Stripe fails', priority: 'P3', effort: 'S', area: 'Billing', status: 'done', note: 'DONE. api/get-prices.js:82 was catch { return responseFromLive() } - a revoked STRIPE_SECRET_KEY or a Stripe outage served the DEFAULT_PRICES table to every visitor with source: fallback in the JSON and nothing at all server-side, and the handler\'s outer catch that does log is unreachable because fetchPrices never throws. Confirmed by reading the control flow and then by driving it. BOTH silent catches are now loud, not just the one filed: the money path logs console.error with message, type, code and statusCode; and the resolveLivePrice fallthrough (line 27) logs a warn naming the interval and the env key, because a STRIPE_PRICE_* id pointing at a deleted or wrong-account price looked identical to the var not being set at all. Nothing about the RESPONSE changed - visitors still get a price, which is exactly why this was invisible. PROVED WITHOUT LIVE STRIPE AND WITHOUT A STUB: getStripeServer() throws when STRIPE_SECRET_KEY is unset and it is called INSIDE the try, so the test unsets the key and drives the real shipped catch through the real handler, then asserts the response is still a 200 fallback AND that the log line names the endpoint and carries the error message. A source assertion bans every bare catch { in the file, with a positive control proving the pattern can match what it bans. MUTATION: putting catch { back on all three sites turned 2 of 9 red; byte-exact restore green. tests/unit/billing-failure-visibility.test.js.' },
  { id: 'ai-quota-check-not-transactional', title: 'The AI daily/monthly quota is read, then incremented after the provider call, outside any transaction', priority: 'P2', effort: 'M', area: 'Security', status: 'done', note: 'Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. api/ai.js:896-898 reads used and monthUsed with two plain gets, decides at 907-921, calls the provider, and only then increments at 932-936 with merge sets. N requests fired together at used = limit - 1 all read the same count, all pass, and all run - the cap is a soft cap, off by the concurrency. That matters here more than usual because api/_lib/plans.js derives every limit downward from ONE shared free-tier provider bucket, so an overshoot is paid by every other user. Verified by reading the sequence; there is no test because the path needs Firestore. FIX: reserve the unit inside a runTransaction before the provider call and refund it on a provider failure, or accept the softness and say so in plans.js. FIXED 2026-09-07 (PR fix/security-quartet-0906). Both metered paths in api/ai.js - the Brand Starter bucket and the daily/monthly pair - now go through runMeteredTask in api/_lib/aiGeneration.js, which RESERVES the unit inside a Firestore transaction BEFORE the provider call and refunds it on every path where the runner did not produce a generation. The write is counts[i] + 1, derived from the value the transaction read, NOT FieldValue.increment: an unconditional increment carries no read version and would commit from a stale count, so the transaction would guard nothing. Both ceilings are reserved in ONE transaction, month first, so a partial reservation cannot fill the monthly ceiling from requests nobody ran. Proved by tests/unit/ai-quota-transaction.test.js: two concurrent requests against a bucket of one, on a fake store that models Firestore read-version conflict and retry, with a barrier forcing both reads before either commit - exactly one passes and the bucket ends at one. Three controls make that falsifiable: the OLD read-then-increment shape transcribed into the same harness lets BOTH through and overshoots to two, a limit of two admits both callers, and four wiring assertions pin the api/ai.js call sites. 18 of 18 green, mutation sweep 16/16 caught. Residual, stated plainly: a process killed between the reservation and the refund leaves the unit spent - strictly smaller than the hole it replaces, and it fails toward refusing rather than toward overspending a shared bucket.' },
  { id: 'firestore-feedback-create-open-to-anyone', title: 'firestore.rules lets anyone create feedback documents directly, bypassing /api/support entirely', priority: 'P1', effort: 'S', area: 'Security', status: 'blocked', note: 'Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. firestore.rules: match /feedback allow create: if true. No auth, no field allowlist, no size bound, no rate limit - a script can write unlimited documents of any shape up to 1 MiB straight into the collection the admin queue renders, and none of [support-abuse-hardening] applies because that limiter lives in the route, not in the rules. VERIFIED THE CLIENT DOES NOT NEED IT: grep of src/ finds no addDoc/setDoc on a feedback collection; every submission goes through fetch(/api/support) (FeedbackModal.jsx:213), which writes with the Admin SDK and bypasses rules anyway. So the rule guards nothing and opens everything. FIX: allow create: if false (or delete the line) - and run npm run test:rules, then publish from the console. firestore.rules is founder-gated AND the tool refuses to stage it (see the moderator-role row), so the one-line diff is in the review PR body for the founder to apply. CLOSED PENDING THE FOUNDER (2026-09-07, PR fix/security-quartet-0906). The one-line diff is written out in the pull request body under FOUNDER APPROVAL NEEDED, section 1: allow create: if true becomes allow create: if false, with the reason on the rule. NOT applied in the tree - firestore.rules is founder-gated AND the auto-mode classifier refuses to stage it, the same block recorded in docs/OWNER-ACTIONS.md section 1.3. IT IS TESTED ANYWAY: tests/rules/pending-firestore-rules.mjs applies the exact diff to an in-memory copy and tests/rules/firestore-rules-pending.test.js runs the Firestore emulator against it - 37 of 37 green, including a positive control proving the admin queue still reads, updates and deletes, and that the server-side /api/support write still lands. Mutation sweep 15/16 caught. The diff composes with the moderator role held diff (both touch this file, on different lines); the combined result is printed in the PR body. AN APPLIER EXISTS NOW, 2026-09-10. `npm run apply:gated` applies this diff for the founder, reading the anchored replacements straight out of tests/rules/pending-firestore-rules.mjs - the same module the emulator suite already runs against, so there is no second copy to drift. It composes with the moderator role and the per-project sync rule in a fixed order the command handles (per-project, moderator, these bounds), because two of the three are unified diffs whose context the others move. Verified on a scratch clone against the COMBINED result: npm run test:rules 105/105, including the pending suite\'s own 37. Idempotent, refuses a dirty tree, commits nothing. STATUS UNCHANGED - blocked until the founder runs it and publishes the rules.' },
  { id: 'firestore-signed-in-writes-unbounded', title: 'Any signed-in account can write unlimited, unvalidated documents to three collections', priority: 'P2', effort: 'M', area: 'Security', status: 'blocked', note: 'Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. firestore.rules: community-prompts and community-submissions check authorUid and status on create and nothing else - no request.resource.size bound, no key allowlist, no count; analytics-daily allows create and update by any signed-in user for ANY document id (not only {YYYY-MM-DD}) with any fields. One free account can fill the project with 1 MiB documents, and inflate or corrupt every counter the admin dashboard reads (the rules file already records that counter inflation is an accepted limitation - the size and id bounds are not). Verified by reading the rules against the client writers (SubmitPromptPanel.jsx:88, communityQueueApi.js:15, analytics.js:106). FIX: request.resource.size < N, keys().hasOnly([...]) on the client-writable shapes, and a day-id regex on analytics-daily. Gated + tool-refused file; diff in the review PR body. CLOSED PENDING THE FOUNDER (2026-09-07, PR fix/security-quartet-0906). Diff in the PR body under FOUNDER APPROVAL NEEDED, section 2, tested against the emulator the same way as the feedback row. ONE CORRECTION TO THE FILED FIX: request.resource.size does NOT exist in Firestore rules - byte size is a Cloud Storage feature, and Map.size() counts entries, not bytes. So the bound is built from what Firestore rules can actually measure: keys().hasOnly plus hasAll for an exact shape on community-prompts and community-submissions, String.size() caps on every client-written string (deliberately generous - none of the four inputs in SubmitPromptPanel carries a maxLength, so these caps become the product limit), a payload key allowlist and list-length caps on community-submissions, and on analytics-daily a YYYY-MM-DD document-id regex, a day-field-matches-id check and a 500-entry field cap. Counter INFLATION is still possible and is still the accepted limitation the rules file already records - the shape, id and growth bounds are the part that was not. AN APPLIER EXISTS NOW, 2026-09-10. Same command as [firestore-feedback-create-open-to-anyone] - `npm run apply:gated` applies both halves of #418\'s held diff together, from tests/rules/pending-firestore-rules.mjs rather than from a retyped copy, and runs the emulator against the combined result (105/105). tests/unit/gated-patch-applier.test.js additionally asserts what these bounds must NOT take away: the billing lock on users/{uid}, the owner-email gate on the analytics-daily read, and the continued absence of a provider-health block. STATUS UNCHANGED - blocked until the founder runs it and publishes the rules.' },
  { id: 'storage-rules-no-content-type', title: 'Storage lets a signed-in user upload any content type under community-media', priority: 'P3', effort: 'S', area: 'Security', status: 'done', note: 'Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. storage.rules allows write under community-media/{uid}/** on auth + own uid + size < 25 MB, with no contentType check, and read is public by design. A signed-in account can therefore host text/html or an SVG with script at a firebasestorage.googleapis.com URL under the project’s name. The uploader (utils/mediaUpload.js) only ever sends images and video. Verified by reading the rules against the one caller. FIX: request.resource.contentType.matches(image/.*|video/.*) on the write rule. FIXED 2026-09-07 (PR fix/security-quartet-0906). storage.rules is NOT on the founder-gated list, so this one is applied in the tree: the community-media write rule now requires request.resource.contentType.matches((image|video)/.*). Covered by a new tests/rules/storage-rules.test.js against the Storage emulator - 21 of 21 green, every refusal paired with the identical bytes succeeding as image/webp so a broken rules file cannot pass the suite. firebase.json gained a storage emulator port and npm run test:rules now starts firestore,storage. Mutation sweep 4/4 caught; a fifth mutation showed an explicit contentType != null guard was indistinguishable from its own absence (matches() on null is an evaluation error, which denies) so that line was removed rather than shipped untested. KNOWN GAP, recorded not overlooked: image/svg+xml still passes, because imageProcessing.js passes SVG through unconverted and refusing it would divert those uploads to the 900KB base64-in-Firestore fallback. A test pins that acceptance so narrowing it later is a decision rather than a silent breakage.' },
  { id: 'project-sync-single-document', title: 'Project sync pushes the whole list into one Firestore document and swallows the day it stops fitting', priority: 'P1', effort: 'M', area: 'Account', status: 'partial', note: 'DELIVERED 2026-09-08 in two halves, and the split is the honest part: the SILENT FAILURE and the DELETES ship now on the existing rule; the DOCUMENT SHAPE waits on ONE founder approval. WHAT SHIPS TODAY. (a) The empty catch is gone. ProjectContext\'s pull and push both report into src/utils/syncStatus.js, and src/components/SyncNotice.jsx renders it on EVERY route (mounted in App.jsx beside Toast, not on /projects — the person whose sync has stopped is by definition busy editing, and that is a tool page). Two levels: \'error\' (sync is not happening; stays until fixed; carries Try again) and \'notice\' (sync happened and changed something under you; dismissible). Every failure sentence says the work is still saved on this device BEFORE it says anything else, and names its cause — too-large / permission-denied / offline / unknown are four different sentences, not one generic one. (b) THE CEILING IS MEASURED BEFORE THE WRITE, not discovered after it: writeRemoteProjects() refuses at DOC_BYTE_BUDGET (900 KB) and reports \'N KB of a 1 MB limit\'. Measured on this branch: an empty project 148 B synthetic (684 B for a real default, as the review had it), a 32 KB-logo project 32953 B, 31 of them fit under 1 MiB and 32 do not — the review\'s ‘roughly thirty’ confirmed, and we refuse at 28. (c) DELETES PROPAGATE. deleteProject() writes a tombstone to a NEW localStorage key vs-project-tombstones (its own key, because every reader of vs-projects counts that array — the save cap included — and a soft-deleted record in it would refuse a save for a project the user had thrown away), the push carries them as a `deleted` map on the same document (additive, merge-safe, no schema break, no rule change), and the pull applies them. A project EDITED after it was deleted elsewhere survives and its tombstone is dropped: resurrecting work beats destroying it. Tombstones prune at 180 days. (d) A push now runs ONCE after every pull. Before this the push effect ran in the same commit the pull had just suppressed and nothing rescheduled it, so a signed-in session pushed NOTHING until the user next edited — which meant a device holding unsynced projects kept holding them, and a user could not learn sync was broken without editing something first. ‘Is my work reaching my account?’ has to be answerable on load; it costs one small write per session. WHAT WAITS ON THE RULE. users/{uid}/projects/{projectId}, one document per project, with users/{uid}/sync/projects demoted to a ~50 B-per-project index (~17,000 projects inside the same 1 MiB). Written, unit-tested against a fake db, and INERT behind PER_PROJECT_SYNC_ENABLED = false in src/utils/projectSync.js, because firestore.rules is founder-gated and a client writing there today would be refused — which is the exact defect this change abolishes. The rule is `match /projects/{projectId} { allow read, write: if isOwner(); }` inside the existing match /users/{userId} block: the SAME trust boundary sync/{docId} already has, granting the owner nothing new and a stranger nothing at all. Diff committed UNAPPLIED at docs/design/per-project-sync-rules.patch. MIGRATION, SAFE TO RUN TWICE. migrateToPerProject() is write-if-newer and NEVER deletes the v1 document — `list` survives as a frozen pre-migration copy of everything the account had; only index/deleted/v are merged over it. Second run reports migrated:0, skipped:N and leaves the store BYTE-IDENTICAL (asserted by snapshot compare with a fixed clock, with a positive control that a run WITH work to do does change it). It resumes from half-done without stamping an older copy over a newer one, and does not resurrect a tombstoned project. EVIDENCE. tests/unit/project-sync.test.js 31 tests, 6/6 mutation-verified (budget refusal, tombstone application, stale-remote refusal, migration skip, error classification, per-document write) with a green no-op control first and every file restored byte-exact. tests/unit/per-project-sync-rule.test.js 6 tests, 5/5 mutation-verified — it fails if the patch goes stale, if the patch applies but grants no rule, if PER_PROJECT_SYNC_ENABLED opens before the rule lands, if the rule lands and the gate stays shut, or if the patch reaches a second file. tests/user-sim/62-project-sync-states.spec.js 8 tests, 7/7 mutation-verified at the CALL SITE in ProjectContext.jsx (restoring either empty catch, dropping the conflict report, dropping the tombstone write, not passing tombstones to the push, ignoring remote tombstones on the pull, removing the post-pull push). The first of those SURVIVED the first draft: with the whole path denied the push reported the failure a moment after the pull swallowed it, so the pull\'s own catch was never exercised — signIn() now takes deny: [{path, ops}] and a read-only denial isolates it. tests/rules/per-project-sync.test.js is WRITTEN AND UNRUN: the Firestore emulator will not start on this host (Java 8; it needs 11+), which blocks the pre-existing rules suite identically — environmental, not a regression. FIXTURE WORK, reusable: signIn() gained deny (path substrings, optionally op-scoped, answering FirebaseError instead of data — the one thing #407\'s in-memory double could not express, since a write through it always succeeds) and projectTombstones; window.__UIL4B_TEST_STORE__ exposes what actually reached the account, so ‘the delete propagated’ is read off the push rather than inferred from the screen. STILL TRUE AFTER THIS: on the v1 path the 1 MiB ceiling exists — it is now announced and priced instead of silent, and the shape that removes it is one approval away. PRIOR FINDING (2026-09-06 review), kept verbatim: Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. ProjectContext.jsx:389-395 setDoc(users/{uid}/sync/projects, { list: [...every project...] }) inside catch {} (396-398). MEASURED: a default project serialises to 684 bytes, so the 1 MiB ceiling is about 1,533 EMPTY projects - but a project carrying a brand logo is capped at 32 KB by brandLogo.js (its own comment does this arithmetic), so roughly thirty logo projects fill the document. Pro has no project cap. Past the ceiling every push fails silently: the user keeps editing, localStorage keeps working, nothing says sync stopped, and a new device pulls the last list that fit. Three further properties, all read off the code: merge: true does NOT merge inside an array, so two devices pushing concurrently replace each other’s whole list (last writer wins); deletes never propagate and are resurrected by the next pull (documented as v1 at line 67-68); and the pull is a one-shot getDoc on login, not a listener. FIX: one document per project under users/{uid}/projects/{id} with tombstones for deletes, and a visible sync-error state instead of the empty catch.' },
  { id: 'firestore-sync-last-writer-wins', title: 'useFirestoreSync applies every remote snapshot over local without comparing timestamps', priority: 'P2', effort: 'M', area: 'Account', status: 'done', note: 'FIXED 2026-09-08. shouldApplyRemote(remoteUpdatedAt, localUpdatedAt) now decides every apply, and it lives in src/utils/projectSync.js rather than in the hook for the reason the defect existed at all: the hook imports React and the Firebase broker, so a decision inside it could not be reached by a unit test. THE FIX IS A LOCAL CLOCK, NOT A BIGGER SUPPRESSION WINDOW. localStampRef is stamped the instant a local change is announced — not when the 2 s debounce fires — because the review\'s step 2 happens INSIDE that debounce and a stamp written at push time would still be behind device A\'s. A remote snapshot older than the stamp is refused, logged with both timestamps, and dispatched as vs-sync-refused for anyone debugging it. The pushed _updatedAt is the moment of the EDIT, not of the push, so a racing device cannot read this one\'s edit as two seconds in its own future. Missing remote _updatedAt counts as 0: pushToFirestore has always written the field, so a document without one is new or corrupt, and neither is worth a known-newer local edit — while a fresh device has local 0, and 0 < 0 is false, so a first sync still applies. WHAT THE USER SEES ON A REFUSAL: nothing, and that is correct — the newer copy is the one on their screen, nothing was lost, and there is no action to offer. The conflicts a user IS told about are projects, reported by ProjectContext through the same utils/syncStatus store. BOTH EMPTY CATCHES ARE GONE (push, pull, AND the onSnapshot error callback, which was `() => {}`): a permission-denied on the sync document is no longer indistinguishable from an empty one — it reports on the \'preferences\' channel and renders in SyncNotice. EVIDENCE: the review\'s exact three-step sequence is a test (tests/unit/project-sync.test.js, \'the review’s three-step sequence is refused at step 2\') with a positive control that a genuinely NEWER snapshot still applies — without it the test would pass for a device that ignores its account entirely. Mutation M3 (accept every snapshot regardless of clock) turns that test and \'a remote document with no timestamp cannot overwrite a device that has one\' red; restored byte-exact. PRIOR FINDING (2026-09-06 review), kept verbatim: Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. src/hooks/useFirestoreSync.js:101-108 applyRemoteData writes every synced key straight into localStorage and never reads remote._updatedAt, while pushToFirestore (33-40) writes _updatedAt on every push. Sequence, read off the code: device B edits vs-current-design; within its 2 s debounce device A’s push arrives as a snapshot; B is not suppressed (suppressRef only covers B’s own push), so A’s older data overwrites B’s localStorage; B’s debounce then fires, getLocalData reads the overwritten value, and B pushes A’s data back as its own. B’s edit is gone from both ends. Also: pullFromFirestore (55) and the first onSnapshot callback (57-64) both apply on mount, and both catches at 39 and 49 are empty, so a permission-denied on the sync document is indistinguishable from an empty one. FIX: compare _updatedAt before applying, keep the newer side, and log the refusal.' },
  { id: 'dead-source-files-2026-09-06', title: 'Twenty-six src files reach no chunk in the production build', priority: 'P2', effort: 'M', area: 'Quality', status: 'done', note: 'Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. METHOD, because reasoning about imports is not proof: npx vite build --sourcemap to a scratch outDir, then the union of every .map file’s sources against find src -name *.js* - a module that contributes no code to any chunk is absent from every map. 275 source files, 249 in a chunk, 26 in none. THIRTEEN PAGE COMPONENTS with no importer anywhere in src/: AiPromptGenerator, AutoBuilder, BoxShadowGenerator, DocsAI, DocsBrand, DocsDesign, DocsMarketing, DocsSEO, DocsSocial, DocsThemes, LandingPromptGenerator, UIBuilder (about 336 KB of source between them). SIX COMPONENTS: DiscoverCard (no importer), DocsTOC (only the dead Docs pages), UiShadeEditor, UiSystemLab, UiSystemMatrix (only UiSystemBuilder) and UiSystemBuilder itself, which PaletteBuilder.jsx:1093 says is deliberately unimported. FIVE DATA FILES: communityFonts, namedColors, sections.jsx (no importer), landingPromptRules and promptRules (only dead pages). THREE UTILS: extractColors (no importer), aiProvider and uiSystem (only dead pages/components). The list was cross-checked by grepping every importer. Nothing under tests/unit imports any of them (word-matches on sections and aiProvider are prose). The verdict is the founder’s, per the precedent on [dead-preview-modal-and-topbar] and [colorstudio-dead-sections]: delete, or route. The UI System quartet is the one set with a stated reason to stay. DONE 2026-09-06. SEVENTEEN of the twenty-six deleted; nine kept, each for a stated reason. RE-MEASURED on this branch rather than trusted from the review: a clean --sourcemap build gives 289 source modules, 274 in a chunk, 26 in none - the same 26 by count, but NOT the same 26 by name, because AutoBuilder had already gone and the two deferral seams had arrived. PROVED TWICE BEFORE ANYTHING WAS TOUCHED. The sourcemap union is the primary proof and is stronger than a grep - a module contributing no code to any chunk is absent from every one of the 114 .map files. A marker scan of a clean dist confirmed it: two markers per file, each verified present in its own source first (a scan for a string nobody wrote passes trivially) and each verified unique, across all 188 dist files including 116 JS assets and all 41 HTML shells - zero hits. POSITIVE CONTROL by the identical command in the identical directory: markers from AppFooter, NotFound and CuratedResources were all FOUND. The markers are described here and never quoted, because pipeline.js bundles into a chunk and a note quoting them would ship them and falsify its own claim - the trap this board has now recorded three times. DELETED: the seven unrouted pre-Learn Docs drafts plus DocsTOC (imported only by them); BoxShadowGenerator, LandingPromptGenerator, UIBuilder; discover/DiscoverCard, which nothing imported and which CuratedResources.jsx already carried a comment explaining it would not use; and communityFonts, namedColors, sections.jsx, extractColors, landingPromptRules. KEPT, AND WHY. AiPromptGenerator.jsx is the only caller of the OpenRouter path and docs/OWNER-ACTIONS.md 4.5 puts a live choice to the founder - deleting it would decide that by default - so it stays, and data/promptRules.jsx stays with it because that page is its only importer and taking it would leave option A unbuildable. The UI System quartet and utils/uiSystem.js stay per PaletteBuilder.jsx. firebaseAccess.lazy.js and oneTapMount.lazy.jsx are NOT dead: they are the VITE_DEFER_FIREBASE seams named in vite.config.js, unbuilt in an unflagged build, which is the one place this method needs a human reading the config. THE IMPORT SWEEP WAS THE EASY HALF. Four load-bearing references, each proven load-bearing by deleting the files first and watching the suite go red - four failures, no more and no fewer. ai-provider-path.test.js read a deleted page from disk by path and failed with ENOENT rather than an assertion; the page is dropped from the loop and the TEST KEPT, still guarding the provider badge on the page the OpenRouter decision is about. firebase-deferral.test.js listed it in EXPECTED_STATIC_FIREBASE_IMPORTERS and its vanished assertion caught it. price-consistency.test.js carried four NOT_A_PLAN_PRICE entries for the drafts mock money, and "no allowlist entry outlives the thing it excuses" went red on all four - the third time that assertion has paid for itself. Its raw population floor also failed, and needed a measurement rather than a nudge: same allowlist either side, files 300 to 283, occurrences 51 to 29, CHECKED 10 to 10. The unchanged number is the load-bearing one - every price actually compared against the ladder is still here, and what left was 22 allowlisted fake amounts in files nothing could render. Floor 30 to 20; checked >= 6 untouched. ONE ORPHANED RECORD: dataExport.js described the storage key for the deleted builder intro, whose only writer was UIBuilder.jsx - a purpose entry for a key nothing writes made the privacy table disclose storage the app does not use. THE CSS WENT IN A SEPARATE COMMIT, BY PARSING. Rolldown tree-shakes JavaScript; nothing tree-shakes a stylesheet, so every one of these selectors was still downloaded by every visitor on every route - measured, not assumed, by finding the class markers alive in index-*.css while the prose markers were absent from all 116 JS assets. 150 selectors removed and four emptied @media wrappers. The doomed list was taken TWICE: the first cut ran against the tree before the deletion, so any class a test comment happened to mention counted as live and was spared - three were, and were genuinely dead. The second pass ran after, split every styled class into LIVE (44), PRE-EXISTING ORPHANS left alone (65, including dsc-rail-track, whose orphaning 27-motion-guards.spec.js documents) and NEWLY ORPHANED (93), and read what the deleted files used to name out of git rather than from memory. pipeline.js is EXCLUDED from the live corpus: it is prose about the code, and counting it would make every class these notes discuss look live - the self-falsifying-prose trap running backwards. is-paid and is-primary were NOT taken: orphaned by the same measurement but generic state names, and they only ever appeared compounded with a dsc- half that goes anyway. The dry run earned its keep twice - it is the same shape of remover as the one on [landing-page-orphaned], whose first draft would have deleted four @font-face and two @property blocks, and it caught its own off-by-one prelude cut. No reduced-motion block was touched, so no guard pair was half-removed; no @keyframes was newly orphaned (47 before, 47 after). global.css 1,173,333 to 1,154,516 bytes, built 703.24 to 684.92 kB, gzip 114.55 to 112.21. Braces balanced, CRLF preserved, zero lone LF, and the remover refuses to write on any of those failing or on a removal it cannot account for. RE-CHECKED AFTER A REBASE onto #415 and #416, which both touched global.css: every removed line either names a doomed class or is one of the four emptied wrappers, and there is zero overlap with the 49 classes those two PRs added. A clean rebase is textual; this is the check that it was also correct. eslint could not have found any of this: it runs no-unused-vars with varsIgnorePattern ^[A-Z_] and is blind to an unused React component. Lint 0 errors / 25 warnings either side, unchanged, because none of the seventeen carried one.' },
  { id: 'admin-chunk-is-the-backlog', title: 'Three quarters of Admin-*.js is src/data/pipeline.js shipped as a runtime string', priority: 'P2', effort: 'S', area: 'Performance', status: 'done', note: 'Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. MEASURED from the sourcemap of the production build: Admin-*.js is 693 KB minified / 257 KB gzip, seven modules, 771,846 source bytes, of which src/data/pipeline.js is 584,753 (76%) and Admin.jsx 138,539. The engineering log - every note on this board - is parsed by the admin’s browser on every visit to /admin, and grows with every row anyone adds, including this one. It is lazy-loaded, so visitors never pay it; the admin does, and so does every trap the notes on [landing-page-orphaned] record about grepping dist for a string that only this file contains. FIX: import pipeline.js with a dynamic import() from the Pipeline tab alone, or emit it at build time as a JSON asset the tab fetches, so Admin-*.js carries the dashboard and not the diary. DONE 2026-09-07. THIS ROW AND [admin-chunk-ships-the-ops-notes] ARE THE SAME FINDING, filed twice - by #405 and by #406, a day apart, from the same measurement. They are merged here: this row is the survivor and carries both, and that one is closed as a duplicate of this one with its own text kept intact. Neither is deleted, because a board that quietly loses a row loses the evidence that the same thing was found twice, which is itself the useful fact - it was that visible. WHAT THE DUPLICATE ADDED, kept because it is the better-argued half: why it is P2 and not P1 (the chunk is lazy and admin-only, so no ordinary visitor fetches it and it is not in the first wave measured on homepage-field-metrics - it is a byte-mass and origin-transfer problem that grows on its own); and the note that nobody had yet measured how much of the chunk was prose versus code, which is now answered: 84%. FIXED by giving PipelineBoard a dynamic import() instead of Admin.jsx a static one. MEASURED, clean build either side: Admin-*.js 776,482 to 113,900 bytes raw (-85%), 287,073 to 29,755 gzip (-90%). The notes become pipeline-*.js at 663,549 / 258,382, paid for only by an admin who opens that one tab. The chunk had grown from the 693 KB both rows recorded to 776 KB by the time this was picked up, which is the row proving its own last sentence. A DYNAMIC IMPORT RATHER THAN A BUILD-TIME JSON, deliberately. Emitting the notes as a JSON asset would take them out of the JavaScript graph entirely and cache them separately - a real advantage - but it needs a generator, and a generator can go stale. An agent who edits pipeline.js and does not re-run it would leave the founder’s own board showing yesterday’s backlog while every check stayed green. import() cannot go stale. pipeline.js DOES NOT MOVE: every composition script and several tests/unit/*.test.js import it by that exact path. THE FIRST GUARD WRITTEN FOR THIS WAS WRONG, AND ITS MUTATION RUN SAID SO. It scanned the emitted Admin-*.js for a backlog marker and asserted absence. It passed - and it passed WITH THE STATIC IMPORT PUT BACK, because rolldown emits pipeline.js as its own chunk either way, so a re-added static import does not move bytes INTO Admin-*.js, it adds a static EDGE to the chunk holding them. Same 663 KB, fetched and parsed before the route can render, invisible to a file-content scan. That is exactly the failure vite.config.js records for the Firebase deferral: the code looked deferred, the build stayed green, and the bytes were still in the first wave. tests/unit/admin-chunk-carries-no-backlog.test.js now walks the CHUNK GRAPH from Vite’s own JS API - chunk.imports for static edges, chunk.dynamicImports excluded - so it measures the thing that decides the question, with no regex over minified output. Positive control: the same walk following dynamic edges too MUST reach the backlog, or the tab renders nothing and the main assertion passes for free. Also asserted: the backlog is not reachable statically from any ENTRY chunk (worse than where it started), and a 250 KB ceiling on the Admin chunk for a regression by some other large eager import. MUTATION-VERIFIED: restoring the static import turns the graph test red with both controls still green; restored byte-exact afterwards, checked by sha256. AND A BROWSER TEST, because a green build is not a rendered page and a deferral that renders nothing passes a graph walk perfectly. "The Pipeline tab loads the backlog when it is opened, and not before" in 57-signed-in-session.spec.js signs in as the founder, watches network requests, asserts the chunk is NOT fetched while Overview is showing, clicks Pipeline, and then requires both the rendered rows and the request. The two halves are each other’s control: "no request yet" would be true of a wrong URL matcher, and the same matcher must then fire. — AND IT WAS THE WRONG AXIS. This row measured WEIGHT and closed on weight; the chunk it created was served to anyone who asked, with no login, no cookie and no Authorization header, for as long as it existed. See [backlog-shipped-to-the-public]. The test named above was green throughout, and its own positive control was asserting the leak was present.' },
  { id: 'contrast-search-one-directional-callers', title: 'fixForeground and fixBackground walked one direction, and SEVEN live call sites leaned on them', priority: 'P2', effort: 'M', area: 'Colour', status: 'done', note: 'DONE (this PR). EVERY CALLER, ENUMERATED, because the title said four and it was seven. grep over src/, api/, scripts/ and tests/: seven live call sites, ALL of them in src/utils/workbenchInk.js - readableInk :96 and :97, labelGround :132, cardGrounds :173 and :176, mutedInk :221 and :233. NOTHING under api/ or scripts/. src/pages/ContrastChecker.jsx:101 is PROSE in a comment describing the defect, not a call. This row’s own body already listed all seven; only the headline undercounted. PER-SITE MEASUREMENT, taken THROUGH each function rather than through the helper, over the review’s 216-colour grid, the 16,200 colours the generator can emit, and 8,000 palettes driven through derivePreviewRoles in both modes. (1) readableInk :96 - the walk is REACHED 570 of 16,200 times (408 per 4,000 light-mode palettes, 249 dark) and returned its INPUT on 100% of those reaches: every single time it was asked, it did nothing. The pole fallback then cleared AA every time, so THIS site’s mask was real for contrast and false for fidelity - what shipped was #000000 where #121212 passes, up to 7 lightness units farther from the house ink than the smallest passing move (#D1571F, #9966CC, #1E9044 among them). (2) readableInk :97 - DEAD, and it was hiding :96. `softer` and `other` are #141414 and #FFFFFF, both hue 0 saturation 0, so the two walks searched ONE identical grey axis and the first is never null; the loop always returned at or before it. Because each shadowed the other, reverting EITHER alone changed nothing across 48,681 colours and no mutation could catch a regression in either. Deleted, which is what makes :96 testable. (3) labelGround :132 - UNREACHABLE, proved and then measured. readableInk lists both absolute poles, and contrast(black,C)=(L+.05)/.05 and contrast(white,C)=1.05/(L+.05) cross at L=.1789 where both read 4.579:1, so one pole always clears 4.5 and the ternary always takes bg. Reached 0 times over the 216-grid, 0 over the 16,200, 0 over 8,000 palettes. (4) cardGrounds :173 and :176 - REACHED 8,000 (light) and 8,286 (dark) times per 4,000 palettes and NEVER a no-op, yet the mask is real and STRUCTURAL rather than lucky: fixBackground took its direction from the INK, and the ink here is always an achromatic pole - #141414 at luminance .0069, #FFFFFF at 1.0 - both unambiguously on the correct side of 0.5, so the threshold that is wrong near the .179 crossover was never consulted near it. Output byte-identical either way: 0 differences over the 46,656 grid cross-product, 41,580 realistic (bg, surface+6L) pairs swept across the crossover band, and 8,000 palettes; 0 AA failures on either side; 0 cases where the shipped grounds moved farther than needed. (5) mutedInk :221 and :233 - the mask was NOT real. Over the 216-grid, every ink against every ground, 1,071 of 46,656 (2.30%) shipped an ink under 4.5:1 that a both-directions search clears - worst #000066 on #6666FF, which shipped #141414 at 4.31:1 where #000047 gives 4.50:1 - plus 1,114 inks with lower contrast than the smallest passing move (worst gap 0.31) and 16,112 that discarded the muted hue where it could have been kept. The reason the pole fallback does not cover it: step 3’s poles are #141414/#FFFFFF, not #000000/#FFFFFF, and THOSE curves cross at only 4.295:1, so there is a band of grounds where neither house pole clears 4.5 and the walk is the only thing that can save it. THE CURRENT HOMEPAGE REACHES NEITHER WALK - 0 of 8,000 palettes in both modes, because derivePreviewRoles pre-clamps muted and cardGrounds hands back grounds a pole already clears - so this site’s mask looked total and was not: it depends on two upstream functions mutedInk does not control, while the guarantee in its own heading is stated over EVERY ground. WHAT SHIPPED. All seven routed through nearestPassingLightness (:97 deleted as dead), and fixForeground/fixBackground DELETED from utils/colors.js - not given the fall-back-when-the-walk-fails this row proposed. A fallback keeps the wrong rule alive, and that is exactly how this came back three times: #395, #393 and #405 each replaced the walk at ONE call site and left the helper standing for the next author to reach for. A tombstone comment in colors.js names what they did, why 0.5 is not the crossover, and why they are gone rather than deprecated. A LIKE-FOR-LIKE SWAP REGRESSED STEP 3, caught by measurement before it shipped and worth recording because it is the opposite of the headline bug. mutedInk step 3 walks each pole against each ground IN SEQUENCE, so clearing g2 can break g1. The old one-way search hid that oscillation by usually doing nothing - a walk that returns its input cannot oscillate - and giving it a search that actually moves exposed it. On [#787878, #000000], which no ink can clear, the sequential walk lands on #757575 at worst 1.04:1 where the plain unwalked #FFFFFF is worth 4.42:1. Fixed by making every intermediate AND every unwalked pole a candidate chosen by worst-ground contrast, with #000000 added to the poles for the 4.295 reason above. Verified over 961,016 cases: 0 regressions against what shipped, 1,071 single-ground rescues, 198,800 in the dead band, 31,217 in the card shape. STEP 3’S WALK IS NOW INERT, measured rather than assumed, and that is why no mutation test can reach it: over 783,872 cases that actually reach step 3, switching its direction back changes whether the ink clears AA in 0 of them, and the 1,080 cases where it picks a different grey score IDENTICALLY (#454545 and #757575 both 2.19:1 on [#000000, #33CC66]). Kept rather than deleted because the candidate collection makes it strictly monotone - it can only ever raise the worst-ground contrast - and inert today is not inert after the next change. TESTS, BOTH HALVES. tests/unit/workbench-ink-search-direction.test.js drives the four workbenchInk functions rather than the search under them, which is the gap tests/unit/contrast-fixes.test.js states in its own header that it does not close. tests/user-sim/63-workbench-ink-search.spec.js is the rendered half and asserts the SEARCH, not just the floor: 8c already sweeps this panel for AA, and AA is exactly what this defect does not break. It reads the composited ground and the composited ink INCLUDING element opacity - which is alpha too, is not the same thing as an alpha in color(), and is how .plb-name and .plb-role dim themselves - then checks that no grey nearer the house pole also clears. Math.random is stubbed to a CONSTANT 0.02, not a seeded PRNG: the discriminating case is 3.5% of the generator’s space, so on random palettes the assertion is vacuous most runs and flaky the rest, and a constant cannot desynchronise the way a PRNG does the moment any other caller consumes a random. 0.02 yields #8B2822 #C05930 #D79B5B #E4CF91 #F1F0C6, whose #C05930 sits in the dead band. MUTATION EVIDENCE - six call sites reverted ONE AT A TIME, file restored byte-exact by sha256 each time, green no-op control first and last. CAUGHT: readableInk’s walk, mutedInk step 2, and step 3’s candidate collection. NOT CAUGHT: labelGround (unreachable), cardGrounds bg and surface (1 changed output in 48,681, 0 AA breaks), and step 3’s walk (inert). Those four each have a test guarding the PRECONDITION that makes them harmless, and each says in its own body that a mutation there stays green - a test that silently covers nothing is worse than an absent one. The rendered spec was mutation-verified separately: reverting readableInk’s call site fails it in BOTH themes with `.plb-hex ink #000000 is 100L off the house pole on #c05930, but #050505 passes at 98L`, and the restored tree is green. GATE: lint 0 errors / 25 warnings (baseline), build 39 route shells, unit 1696/1696, users 803 passed 0 failed 13 expected skips - all 13 from the single test.describe.skip in 12-ui-system-builder.spec.js, confirmed by --list. LEFT, MEASURED AND NOT FIXED: .plb-name (opacity .95) and .plb-role (opacity .6) inherit their opacity from the SHARED Palette Builder component. The rendered sweep measures them with that opacity composited and allows ONLY those two classes to sit below the floor, as a subset assertion rather than an exact-match one - which of them actually misses depends on the palette rolled, so pinning the set would be a flake with a random trigger. A hard gate on them would fail the build on a property of another tool that has always shipped.' },
  { id: 'unit-suite-source-grep-share', title: 'One in five unit-test files asserts only on source text', priority: 'P2', effort: 'M', area: 'Testing', status: 'done', note: 'Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. MEASURED on tests/unit at 1,417 passing tests: 115 files, 77 (67%) read a source file with readFileSync somewhere, and 23 (20%, 193 test() calls) import NOTHING from src/, api/ or scripts/ - every assertion in them is a regex over source text (ai-provider-path, build-asset-guard, category-hue-contrast, design-tokens, flair-tone-contrast, focus-ring-geometry, gate-doc, hero-entrance, home-asset-budget, home-step-swap, input-specificity, lazy-route-readiness, modal-contract, one-tap-stub, prefers-contrast, reduced-motion-guard, reduced-motion-resolution, semantic-roles, state-token-contrast, surface-vocabulary, theme-resolution, typography-paywall, upgrade-gate-names). Across all files 448 of 3,849 assertions (11.6%) are source-string checks. Most strip comments first and pin a call site the way this repository has learned to; the failure mode is the other one - a helper that is right and unwired passes everything that imports only the helper. A sample of the browser suite (12 includes()/toContain() assertions and both toBeGreaterThanOrEqual(0) sites) found no assertion that cannot fail; the two >= 0 checks are paired with an upper bound. The three cannot-fail assertions found earlier this week were not re-found, so the rate to report is: 0 of 14 sampled browser assertions, 20% of unit files behaviour-free. FIX: for each source-only file, add one behaviour test that imports the module it describes, or say in the file header why none is possible. 2026-09-08, all 23 classified and 6 converted (branch test/source-grep-share): the 23 were not one population. THE ROOT CAUSE IS RESOLUTION, not laziness - node --test cannot import most of src/, because the app imports relative modules with no file extension (useHomeMotion does `from ./useSmoothScroll`; useModalDialog reaches AppearanceContext the same way) and Vite resolves that where Node does not. The repo already had the answer in reduced-motion-resolution.test.js and theme-resolution.test.js: rewrite the imports away and run the module body under node:vm. Both were ALREADY converted before this pass - the 23-file measurement is stale for them. CONVERTED 6. modal-contract now executes useModalDialog against a DOM with a real focus model; it had four regexes plus a 20-line static tracer following document.activeElement through assignments into the cleanup, none of which could tell the fixed hook from the broken one, because they differ only in what is reachable at RUN TIME (a detached opener takes .focus() as a silent no-op). home-step-swap now loads the module: STEP_SWAP is exported and the interpolation is exported beside it as stepSwapTrigger(), because matching the UNEVALUATED template literal `center ${STEP_SWAP.down}%` proved the source contained that text, never that ScrollTrigger is handed "center 52%". category-hue-contrast, flair-tone-contrast, state-token-contrast and prefers-contrast each carried a private ratio helper ending Math.round(r*100)/100 - rounding BEFORE the compare, so 4.4951 rounded to 4.5 and cleared >= AA and none of the four could fail on a near miss, which is the only way a hand-nudged hex ever fails; they now use contrastRatio/luminance from src/utils/colors.js, unrounded, rounding only for the message. KEPT 15 with reasons in each header: the thing guarded IS the source (banned-word and call-site sweeps over many .jsx files, CSS cascade/specificity/ordering invariants, byte budgets, and the four meta-tests that guard the acceptance suite itself). MUTATION RATE: 17 applied, 17 caught, 0 survivors, each restored byte-exact. Two apparent survivors were traced to the test harness, not the tests - a perl $ anchor does not match a CRLF file, so the mutation never applied; always assert the mutation landed (git diff --numstat) before believing a survivor. OUT OF SCOPE, left for the agents holding those files: ai-provider-path (api/ai.js, Admin.jsx, the PromptGenerator pages, firestore.rules) and typography-paywall (ProjectContext.jsx). ---- 2026-09-08, DONE - the last two converted (branch test/per-runner-outdir-and-last-grep-tests). ai-provider-path now EXECUTES api/ai.js under node:vm: the provider race, the health counter, the diagnostic gate and the once-a-day alert are driven against a fetch that answers per host and records its order, a Firestore stub that keeps what it is handed, and process.env under the test’s control - a dead OpenRouter is shown to produce a response that says gemini, the counter is shown to be written before the 502 returns, the alert is shown to send once and not twice. The rules half moved to the emulator: tests/rules/firestore-rules.test.js proves a signed-in client, an anonymous one and one carrying the admin claim are all refused provider-health (a rules mutation naming the collection turned two of them red). typography-paywall now compiles ProjectContext.jsx with the oxc transform Vite uses and runs the provider under node:vm with a React small enough for one render: saveProject and duplicateProject refuse at the cap with the plan’s number, save under it, fire the activation once and not for a blank shell, and refuse signed-out with a sign-in message on Pro as well as Free - the regex it replaced could not tell >= from >. FINAL TALLY, all 23: 10 execute or import the module they describe (ai-provider-path, typography-paywall, modal-contract, home-step-swap, reduced-motion-resolution, theme-resolution, and the four contrast files on src/utils/colors.js); 13 stay source scans with the reason in the header (build-asset-guard, design-tokens, focus-ring-geometry, gate-doc, hero-entrance, home-asset-budget, input-specificity, lazy-route-readiness, one-tap-stub, reduced-motion-guard, semantic-roles, surface-vocabulary, upgrade-gate-names); 0 are regex-only without a stated reason. Where a source assertion survives inside a converted file it is a wiring line - the page keeping data.provider, Admin.jsx fetching the diagnostic behind the admin check, commit() re-resolving the quota - and the header says so. MUTATION RATE this pass: 13 applied across api/ai.js, aiProvider.js and AiPromptGenerator.jsx, 13 caught; 9 across ProjectContext.jsx and SaveTypeSystem.jsx, 9 caught; 1 on firestore.rules, caught by the emulator; every one restored byte-exact and every application asserted with git diff --numstat first - five multi-line mutations that first reported as not applying were the CRLF trap #421 named, not survivors.' },
  { id: 'ui-system-mode-unreachable-for-admins-too', title: 'README and CLAUDE.md say UI System mode is admin-only; the build says nobody can reach it', priority: 'P3', effort: 'S', area: 'Docs', status: 'done', note: 'Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. README.md (Colour section) and the 12-ui-system-builder.spec.js skip note both describe UI System mode as admin-only while it is finished. The sourcemap sweep on [dead-source-files-2026-09-06] finds UiSystemBuilder.jsx, UiSystemLab.jsx, UiSystemMatrix.jsx and UiShadeEditor.jsx in no chunk, PaletteBuilder.jsx:1093 says the builder stays on disk, unimported and unbuilt, and grep finds no importer. So the door is not locked to non-admins; it does not exist for admins either. Either wording is fine - a doc that says admin-only sends the next agent looking for an auth harness that would prove nothing. FIX: change the two sentences to say the surface is unwired, and point at the PaletteBuilder comment. DONE 2026-09-07. THE DOCS WERE CORRECTED; the surface was NOT made reachable, because making it reachable would reverse a founder decision. ESTABLISHED BY RENDERING, not by reading source, which is what the item asked for: "UI System mode is unreachable for an ADMIN too" in 57-signed-in-session.spec.js signs in with signIn(page, { admin: true }) from #407, proves the session really is the founder by opening /admin, proves the Palette Builder really rendered by its own seed field, and only THEN asserts that no Build UI system control, no UI System Pro mode control and no UI System Builder heading exists. The two controls come first on purpose: an absence is also what a page that never loaded looks like, and what a session that is not actually an admin looks like. THE TRUTH. Not admin-only. UNWIRED, for everybody. PaletteBuilder.jsx removed BOTH doors on the founder’s 2026-09-05 instruction - the "UI System / Admin" breadcrumb and the "Build UI system" toolbar button - in favour of a guided walkthrough from the nav (components/UIKitGuide.jsx). canUseUiSystem went with them. Nothing imports components/UiSystemBuilder.jsx, so it is in no chunk of any production build - confirmed by the same sourcemap sweep as [dead-source-files-2026-09-06], which is why the quartet was kept on disk there rather than deleted. FIVE DOCUMENTS CORRECTED, each of which said admin-only and each of which would have sent the next agent looking for an auth gate that does not exist: README.md, docs/RELEASE-READINESS.md 4, docs/build-plan/tool-tree.md, docs/reference/build-and-verify.md, and the skip note at the head of tests/user-sim/12-ui-system-builder.spec.js. That last one was wrong TWICE OVER - it said the suite runs signed out and there is no auth harness, and #407 had already given it one - so the stated reason for the skip was two dead facts at once. Each correction says what the text used to claim, so the record shows the drift rather than hiding it. THE SKIP STAYS and the 13 skips are unchanged: re-enabling that suite needs a ROUTE, not a sign-in and not a restored button.' },
  { id: 'global-css-duplicate-definitions', title: 'global.css defines 514 selectors more than once', priority: 'P3', effort: 'M', area: 'Design system', status: 'done', note: 'DONE 2026-09-08 (perf/stylesheet-dedupe-and-split). CLASSIFIED RATHER THAN SWEPT, and the classification does not support the premise. Reproduced on 73e6dd2 (14,321 lines): 6,015 distinct individual selectors, 863 written more than once for 1,141 extra occurrences. Of the 863: 660 appear ONLY in different at-rule contexts - a root rule and its @media override, or two breakpoints - which is responsive design, not duplication. 203 repeat within the SAME context, and pair by pair those are 135 EXTEND (the later block adds properties the earlier never set), 72 OVERRIDE (a different value, later-wins, intended), 7 restate a value already in force, and 8 EXACT REPEATS. MERGED: only the exact repeats, and only where the proof is total - a later rule with an identical selector list, identical declarations and identical at-rule context cannot lose a cascade to the earlier one whatever sits between them, so deleting the earlier cannot move a computed value. Six rules, 359 bytes of source, ~120 bytes of the minified stylesheet. The other 1,135 extra occurrences are left alone because the classification says they are the stylesheet working. MEASURED BUT NOT SWEPT, so nobody measures it twice: 199 declarations across 88 rules are provably shadowed (a later rule with the same selector list redeclares the same property), worth 4,532 bytes - 0.4% of the file. Sweeping them would edit 88 rules to save nothing measurable, and several are the light-theme token block that four unit tests read by name. PROVEN BY tests/user-sim/64-computed-style-snapshot.spec.js, new here: 33 computed properties on the first painted element for each of the 40 commonest classes on 25 routes at 390 and 1440 - 2,000 elements, ~65,000 values, green before and after. Its mutation evidence is the best argument for it. Nudging --radius-s in the :root block at the top of global.css changed NOTHING on any route, because [data-rounding="default"] redeclares the radius scale further down at higher specificity and the app sets that attribute on the root element. The :root value is dead. A stylesheet reader would have called that a change; the browser did not. Nudging the rule that actually wins turned 12 of the 25 routes red.' },
  { id: 'api-empty-catches-fonts', title: '/api/fonts swallows both catalogue failures with empty catches', priority: 'P3', effort: 'S', area: 'Quality', status: 'done', note: 'Found on the 2026-09-06 engineering review (branch review/eng-fable-2026-09-06); verification method in the note. api/fonts.js:133 and :135 are try { ... } catch {} around the two upstream sources, so when both fail the 502 at line 139 says Unable to load font catalog and the function log says nothing about which host refused, what status, or whether the )]} guard stopped matching. The other empty catches under api/ (ai.js:937, verify-admin.js:24, firebase-admin.js:67, get-prices.js:27) each carry a comment saying why; these two do not. Small enough that it was left for a mechanical pass rather than this review. FIX: console.error with the source name and err.message in each. DONE 2026-09-07. BOTH EMPTY CATCHES GIVEN WORDS, and four more silent paths with them - because the two catch bodies were not the whole of the silence. Each fetcher also returned null on a non-ok response and on a 200 that yielded nothing usable, so an HTTP 403 (restricted key), a 429 (quota), a shape change and a dead host were all indistinguishable in the function log, which is precisely the "which host refused, what status" this item names. Now: each degradation says which source, and why, with the status; a thrown error says which source threw and what it said; a missing GOOGLE_FONTS_API_KEY is reported as unconfigured rather than as broken, since that is a different investigation; the anti-XSSI guard failing to match is called out specifically, because the generic Unexpected token it would otherwise produce buries a fixable upstream change; and the 502 itself is logged, so the record does not stop one step short of the symptom. Degrading is still correct - two sources exist so either may fail - it is now audible. GUARDED BY DRIVING THE HANDLER, not by testing a logging helper: tests/unit/fonts-failures-are-audible.test.js replaces global.fetch with each specific upstream failure, calls the real exported handler, and reads console.error. Seven tests. The wording is deliberately NOT pinned - that would be a change-detector on prose - but the distinguishing fact is: 403 and 429 must produce different lines, or the log cannot tell two different problems apart. THE CONTROL IS A HEALTHY REQUEST that must log NOTHING, because a log that always fires is the same as one that never does. MUTATION-VERIFIED THREE TIMES, independently: restoring catch {} on the metadata source fails only the thrown-source test; restoring its bare return null fails only the status test; removing the final 502 line fails only the 502 test. Restored byte-exact after each, checked by sha256.' },
  { id: 'create-checkout-orphan-customer-race', title: 'Two first checkouts in flight create two Stripe customers and keep one', priority: 'P3', effort: 'S', area: 'Billing', status: 'done', note: 'DONE. api/create-checkout.js:93-104 was read-create-write, so two first checkouts in flight (a double click, two tabs) both read null, both created, and the second write won - orphaning a Stripe customer whose metadata.firebaseUid pointed at the same account, which uidForCustomer in the webhook does not guard because it protects the opposite direction (two accounts claiming one customer). The sequence now lives in ensureStripeCustomer() in api/_lib/billing.js with TWO guards, because neither closes it alone. ONE: a uid-derived IDEMPOTENCY KEY on stripe.customers.create, so Stripe replays the first response and the two concurrent creates collapse to ONE customer - a search-then-create would still leave a window between the search and the create, which is why the review\'s alternative was not taken. TWO: a TRANSACTION that re-reads the document before claiming it, so if another request stored an id first that id wins and ours is discarded - the document can only ever name one customer, and this is the half that still holds if a key has aged out (Stripe keeps them 24 hours). PROVED WITHOUT LIVE STRIPE with a Stripe double that honours idempotency keys the way Stripe does: the race is genuinely reproduced (the test asserts BOTH requests reach the create, or it would be proving nothing), and both come back with one customer id under one key. Separately asserted: the key is stable per uid and differs across uids, a document that gains an id mid-flight wins, and a returning customer creates nothing. A source assertion bans stripe.customers.create( from create-checkout.js, with a positive control. MUTATION: restoring the inline read-create-write turned it red; byte-exact restore green. tests/unit/billing-failure-visibility.test.js. NO MONEY ever moved on the orphan, then or now; what this removes is the duplicate customer record and the ambiguity it left in Stripe.' },
  { id: 'palette-opens-with-wrong-state', title: 'The palette builder sometimes opens with colours the user did not make', priority: 'P1', effort: 'M', area: 'Colour', status: 'done', note: 'Founder report (2026-09-05): “somtimes i open the pallete builder and it has added many colours and its a different swatch”. CAUGHT AND FIXED, not merely instrumented ’— reproduced in a rendered browser before and after. THE MECHANISM: the homepage mini builder staged its hand-off from a <Link onClick>. React Router composes a caller’s onClick UNCONDITIONALLY and only then asks its own shouldProcessLinkClick whether to navigate, which for a Ctrl/Cmd/Shift/Alt click or any non-primary button is NO, because the browser is opening a new tab instead. The new tab starts a fresh module instance and correctly finds nothing ’— the comment on that link said exactly this and stopped there. The ORIGINAL tab was left holding a draft nothing would ever consume, in a slot with no expiry, so it waited for the life of the tab and was imported by whatever visit to /create/palette came next, from the nav, the mega menu or a gallery link, over the visitor’s own saved board. Intermittent because it needs one modified click; ’“a different swatch’” because what lands is the homepage’s five. REPRODUCED: with the pre-fix code, ctrl+click Continue then reach /create/palette in the same tab and the board opens with source=handoff; after the fix the same sequence opens on the visitor’s own board. TWO GUARDS, because either alone leaves a hole: navigatesThisTab() in utils/handoffSlot mirrors react-router’s own test and the homepage stages only when this tab is the one that moves; and the board slot gets a 30s ttl, so a future stager that forgets the first guard leaks for seconds rather than forever (opt-in ’— the File-carrying image/icon slots are unchanged). INSTRUMENTED TOO, because the report was untriageable: the board root carries data-board-source (query | handoff | saved | random) and data-board-cols, and one console.info names the branch that won plus the age of any hand-off. Inert, and no colour values. 7 unit tests added to tests/unit/board-handoff.test.js, each verified by mutation. WHAT WAS ELIMINATED AND NOT FOUND GUILTY: no randomise-on-mount path (the mount write is deliberately skipped when the board came from a random draw), the ?c= query path is precedence-correct, and readSavedPalette’s re-derive check already refuses a stale base written by another tool.' },
  { id: 'palette-toolbar-rendering', title: 'The palette toolbar renders poorly on desktop and overflows on a narrow laptop', priority: 'P1', effort: 'M', area: 'Colour', status: 'done', note: 'Founder report (2026-09-05) with two screenshots. MEASURED FIRST, rail scrollWidth over clientWidth at 21 widths: >=1180 is 712/712, one 57px row, correct; 981-1080 is 712/712 ’— the rail FITS and the toolbar still wrapped to TWO ROWS, 105px, a fault nobody had reported and half of what ’“unresolved on desktop’” means; 769-960 is 1033/425-551, the shipped swipeable ribbon; <=768 is 964/366-744, TWO ROWS and the rail still clipping 3 to 6 controls, which is the founder’s 662px capture. FIXED by a MEASURED collapse (src/utils/toolbarFit.js), following filterFit.js’s contract: the intrinsic width is cached per band while the cluster is still on the row, and the budget comes from the toolbar’s width and a stated lead-group ceiling, never from anything the collapse itself moves. A grid was tried on the library toolbar and reverted; this stays flex. ONE BAND IS EXEMPT AND SAYS SO: 769-960 is the swipeable ribbon pinned by 36-rail-overflow and 23-responsive-mid-band, the founder’s report does not touch it, and overturning it is not this change’s call. The overflow control is a WORD (Mobbin: Substack collapses to a labelled More beside its other labelled dropdowns) because the desktop complaint IS a run of unlabelled squares; inside the panel every control keeps its full label. DESKTOP RHYTHM: at 1440 the row was ten 36px squares interleaved with 135/171/152px controls at a uniform 10px gap with a stray 21px before Save / export, so no gap said which controls belonged together. Four clusters ’— explore, generate, step back, commit ’— with a hairline before the one irreversible control (Mobbin: Substack, Frame). TWO BUGS FOUND ONLY BY RENDERING IT: a display:contents wrapper removes the BOX not the ELEMENT, so every ’“>’” selector aimed at the rail stopped matching and Vision type shrank 171px to 117px at 900px ’— the wrapper is now rendered only in the collapsed form; and the font-swap invalidation, living in the observer effect, re-ran on every collapse flip against an already-resolved promise and the toolbar oscillated forever (981, 1000, 1080 and 662 never settled across three reads). AFTER: 600-768 clip nothing, 981-1080 are one 57px row, 769-960 and >=1180 are byte-identical to before, stable across a resize sweep in both directions. NOT DONE: at 480 and 390 the rail still scrolls (History, and at 390 the Tools trigger itself is part-cut) ’— the promoted order still gives Randomise, Undo, Reset and Save / export for free, which is the shipped contract, but the phone band is not fully resolved.' },
  { id: 'header-misaligned-narrow', title: 'The header controls do not sit on a common line at narrow widths', priority: 'P2', effort: 'S', area: 'Navigation', status: 'done', note: 'Founder report (2026-09-05) with a marked screenshot at 662px: the search circle, the avatar and the hamburger are misaligned; he confirmed the complaint is ALIGNMENT, not crowding and not the avatar rendering. A BOX MEASUREMENT SAYS NOTHING IS WRONG, which is why this survived ’— all three are 40x40 in one align-items:center row and their box centres measure a 0.00px spread at every width from 390 to 1440. The misalignment is in the INK. Measured as an ink centroid (the centre of mass of the pixels that depart from the bar’s own background): the search field and the avatar paint 40x40 bordered discs while the menu button carried border:none;background:none and painted a bare 16x10 glyph floating in an invisible box. Two consequences, and together they are the marked screenshot: the visible gap from the disc to the glyph measured 30px against the 16px the row sets, and the glyph stopped 28px from the right edge of the bar while the logotype starts 16px from the left one, so the row read as pulled in at one end. Nudging would not have worked ’— a circle, a photograph and a three-line glyph have different intrinsic mass and never align by matching heights ’— so the glyph is given the optical container its neighbours already carry (the same border, background, radius and transition as .pnav-iconbtn / .pnav-more / .pnav-search-field). Ink then fills the box, both optical margins come out at 16px. FIXED. Pinned by tests/user-sim/52-header-optical-alignment.spec.js over 390/480/600/662/700/768, whose oracle is a RASTER and not a rect: a getBoundingClientRect assertion passed on the broken build at every width, so it could not have caught this. Mutation-verified.' },
  { id: 'eyedropper-needs-zoom', title: 'Sampling a colour from an image gives no way to see which pixel is under the marker', priority: 'P1', effort: 'M', area: 'Colour', status: 'done', note: 'Founder request (2026-09-05): “when im making a pallete from an image and am dragging around an eye droplet, show me a large zoomed in view so i can exactly what pixel im selecting”. SHIPPED: a magnified loupe follows the active marker ’— 13 source pixels across, nearest-neighbour, a two-ink outline on the sampled pixel and its hex printed underneath. THE PART THAT WAS NOT COSMETIC: the sampler read a canvas downscaled to 320px on its longest side, which was invisible under a 26px dot and would have made the loupe a liar the moment it shipped ’— on a 4000px photo it would draw one region while the readout named a colour from a pixel one in twelve. The loader now keeps TWO canvases: the 320px one still histograms for automatic extraction, and a source-resolution one (capped at 4096) is what every sample and every loupe pixel is read from. Auto-extraction is re-sampled through the same path, because dominantSwatches returns a bucket AVERAGE at a bucket CENTROID and a ring of one colour puts its centroid where the photo is another ’— under a magnifier that reads as the tool contradicting itself. The picker’s test tolerance drops from 20 to 2 as a result. KEYBOARD: the markers were <button>s carrying a pointerdown handler and nothing else, so a keyboard user could focus one and had no gesture to move it at all; arrows now step one SOURCE pixel, Shift steps ten, focus arms the loupe, and a live region announces the value because the loupe is aria-hidden. Mobbin, and which reference drove which decision: Alan’s crop screen parks its magnifier in a CORNER with a crosshair rather than under the finger, because a loupe that follows the pointer covers what it magnifies (data-side flips it away from the marker); Shopee’s ring loupe is why the rim is thick and neutral, so it reads over any photograph; beehiiv’s web colour picker is why the hex rides ON the loupe. 2 user-sim tests added, each mutation-verified with a rebuild.' },
  { id: 'font-usage-two-tabs', title: 'Font examples should separate invented mockups from real-world use', priority: 'P1', effort: 'L', area: 'Typography', status: 'done', note: 'DONE 2026-09-05. The example half is SPLIT into two adjacent tabs and the fourth tab ships EMPTY, which is the founder’s own call - build it empty and he fills it. What ships is the tab, the layout and the per-family slot; what does not ship is one image or one invented example. The existing per-typeface scenes are the mockup tab and are UNTOUCHED, and a rendered control asserts they are still there and still say outright that they are drawn rather than photographed - without it, the tab is empty would be true of a dialog that rendered nothing. Labels are Examples and In use; In use is fontsinuse.com’s own vocabulary and the two have to read as different KINDS of thing from the strip alone. THE SENTENCE THE PANEL MUST NOT SAY, and it is the whole reason this needed care: an empty tab called In use is one careless line from telling a reader that nobody uses this family - a false and damaging claim about a real person’s work, made from a table this product has never filled in. So the empty state is about US. It names what is absent (No cleared photographs of X yet), then the CONDITION under which one appears, then says outright that this is a gap in what we have licensed and not a statement about the typeface, and finally points at the Examples tab, which is full. Xero’s No filed sales tax reports (mobbin.com/screens/84b0c0bb-d346-41ee-995f-15112dfe85ec) drove the CONDITION being stated rather than just a nicer noun; Lyssna’s No sessions yet (mobbin.com/screens/04281c5d-4340-450c-a46b-601993d9b6db) drove the shape at this size. NO ILLUSTRATION, AND THAT IS THE DESIGN DECISION. Jira and Xero both draw one and both work, but neither is a panel ABOUT A MISSING PICTURE: a grey box with a picture glyph in it here is indistinguishable from an image that failed to load, which is the exact failure PRODUCT.md names. Type only, on the panel’s own ground, with no dashed box - a frame would be an empty SLOT, and an empty slot is a thing waiting to be filled rather than a thing deliberately not there yet. THE RIGHTS RULE IS THE PRODUCT HERE. [fonts-in-use-surface] STAYS BLOCKED and nothing here unblocks it. What src/data/fontRealWorldUses.js does is make the answer unavoidable at the moment a row is added: an entry that cannot name its image, alt, title, credit, source AND licence renders NOTHING rather than an uncredited photograph, because an uncredited photograph on a public page IS the takedown surface that item protects against, and the likeliest way one arrives is somebody pasting a path in a hurry. Each of the six is asserted individually - one missing field is the realistic failure and an assertion over the whole object would pass while five of six were checked. THE FILLED STATE WAS MEASURED, NOT ASSUMED. A test entry was seeded, built, driven in a browser and reverted byte-exact: the complete row rendered with its provenance beneath it, the deliberately incomplete second row was DROPPED, and a 1200px image rendered 797px tall inside a 792px dialog and pushed the LICENCE line off the bottom - a licence that scrolls away from its picture is not attached to anything. So the image is capped at min(56vh,420px) with object-fit:contain; cover would have fixed the height by cropping, which on a photograph of type in use removes the thing the reader came to look at. Recheck: 419px tall, caption fully visible, the pinned actions still on screen, dialog 792px, so #305’s warning about the 1,763px dialog does not apply. The fontsinuse link keeps the measured caveat - a miss returns HTTP 200 reading No Uses found with 47 unrelated popular uses underneath, so the miss case is a page that still LOOKS full - and is therefore worded as a search, with the sentence after it saying so. BOTH DIALOGS ARE DRIVEN, not one. The Font Gallery specimen dialog and the FontBrowseDialog picker share the component and have drifted once before; MUTATION: unwiring the panel from the PICKER alone fails only the picker test and leaves the three gallery tests green. 15 mutations in all, every one failing the intended assertion, controls green. The four-tab arrow-key contract is driven in a browser rather than grepped, because a roving tabindex survives a source read and fails in a page.' },
  { id: 'surface-headers-read-as-ai', title: 'The tool page headers read as generated and their primary action is misplaced', priority: 'P1', effort: 'M', area: 'Brand', status: 'done', note: 'DONE (#382 + #386 + #391). Founder report (2026-09-05) with a marked screenshot of the Font Gallery header. He marked AI against TWO elements — the eyebrow breadcrumb (DISCOVER / TYPOGRAPHY) and the three-figure stat strip — and BAD UI against the Build a font pair button. EXPLICIT INSTRUCTION: ’make sure to make that change to all the headers that match’. Closed in three passes. #382: Font Gallery + Font Pair mastheads and the shared DiscoverGalleryHero behind five more routes; the one figure that worked (how much type there is to search) moved into the search placeholder, and the corner text link became a real button in the copy column. #386: the Type Scale, which #382 had not reached — same Create / Typography eyebrow, plus a five-up strip whose every figure was restated within 150px. #391 IS THE SWEEP, and it is not a grep: two of its four remaining eyebrows were not .sec-h-eyebrow at all (.stc-hero-eyebrow on Semantic Colours, .ggn-eyebrow on the Gradient Generator), so all 33 route shells were enumerated from the build and every live page was opened at 1440x900 and measured. SEVEN EYEBROWS: /create/tint, /create/semantic-color and /create/gradient each said Create / Colour; /create/contrast said Colour above an h1 whose first word is Colour; /create/aspect-ratio said Imagery and /create/alt-text said AI Tools (both the Create group just clicked through); /create/file-converter said File Converter 33px above an h1 reading File Converter — the most literal instance in the codebase. FOUR FIGURE STRIPS, every figure checked for restatement before deletion, measured from the strip’s own bottom edge. .ggn-status: Linear restated by .ggn-pill at +131px, 3 editable stops by .ggn-badge (3/12 stops) at +45px which also carries the limit the strip did not, 135 degrees by .ggn-pill at +131px, CSS by .ggn-fmt-btn at +393px — four duplicates above a canvas, inside aria-live. .tt-status: 11 stops per scale restated VERBATIM at +520px by .tt-hint (11 stops per ramp.) and again at +929px; HCT lightness curve restated at +227px by the hint under the control that sets it, which labels the same value Perceived — two vocabularies for one piece of state; 11 generated tints is ramps x labels, the same 11 as the figure beside it. .stc-status was the worst: stateRoleIds is Object.keys(STATE_META), STATE_LABELS is a ten-item module constant and 50 is their product, so THREE of its four figures were fixed at build time inside an aria-live region, and the fourth (the bundle name) was restated 47px below by the card marked Selected. .fpr-status restated .fpr-hero-pair 350px above it — the readout #382 kept on the explicit grounds that the pair appears nowhere else, which this strip falsified — counted the suggestion cards below it, and carried the CATALOGUE COUNTER the founder marked AI on the Gallery, off the same catalogue (and restated 207px above itself by FontCatalogNotice when the catalogue is degraded). TWO FINDINGS ONLY RENDERING SHOWS, both the defects #386 measured on the sibling pages. Font Pair’s 01/02/03/04 panel badges READ RIGHT TO LEFT at 1440 (01 at x=1087, 02 at x=79, one line at y=735) because .fpr-grid puts the config rail in the right-hand column; deleted as .tsc-section-num was, and the headings already name the order in words. The Tint Scale hero tablist was 1348x118px of For designers / For developers cards declaring aria-controls for a panel 922px below them, while a second control for the same state (aria-pressed, no tablist) sat 58px above that panel; the adjacent one survives and is promoted to the real tablist with the ids, roving tabindex and arrow keys, and the designer/developer split is kept. Tint page height 2074 -> 1837px; panel 01 rose from y=498 to y=260. DELIBERATELY LEFT: the eyebrow on /seo, /plans, /settings, /sitemap, /privacy, /help and the Discover and Learn surface landings, where the h1 is a slogan or a tab title and the eyebrow is the only plain statement of the page name — the same rule that kept the Type Scale range in #386, .ggn-badge’s stop limit, .fpr-hero-pair and the Copy 50 CSS variables count. Deleting a true, unrestated fact is not what was asked for. Mobbin: Framer (screens/4ade6802-3d3f-4adb-a256-2fb85fa62782) and Rive (screens/9f95defe-f313-4c2e-90aa-aa1da9e137d9) run a canvas with a right-hand inspector and state no value twice; Pipedrive’s content editor (screens/d4222186-082c-4994-9aa7-e493756857be) keeps family, weight, size, line height and tracking each at its own control; Ditto (screens/357963ee-4c35-4860-88d4-fc5a0b2cefc3) does carry a breadcrumb and every segment is a link to somewhere the user has been — hierarchy is a control you can press, not a label you cannot. CSS: 64 dead rules deleted with their responsive blocks, brace balance asserted before and after (7427 -> 7363, matched). TESTS REPLACED, NOT WEAKENED, and three got stronger: 06 read .stc-status span nth(1) toContainText(5), which passed on the cell that said 50 and could not fail on three constants, and now asserts aria-checked on the bundle radios two-sided; 11 read .fpr-status for a family, a container holding BOTH slots, and now names the slot on .fpr-hero-pair; 05 read 22 generated tints off the strip and now counts 22 rendered .tt-cells. New: 13 tests in 55-header-sweep.spec.js, every absence paired with a presence, eyebrow tests asserting the STRING as well as the class. Gate: lint 0 errors / 29 warnings; build wrote 33 route shells; test:unit 1254 pass 0 fail 0 skipped; test:users 588 passed 0 failed, 13 skipped, all 13 in 12-ui-system-builder. 11 mutations, all breaking a call site, 11/11 RED, 0 survived, every file restored byte-exact — including one that restored the eyebrow STRING under a class name that has never existed, and two (applyPair and the gallery hand-off writing into the wrong font slot) that the deleted strip could not have caught. FOUND EN ROUTE, NOT FIXED: ready() in tests/user-sim/helpers.js treats the static boot shell as arrival, because index.html ships <div id="root"><div class="boot-shell"> into every prerendered shell and root.firstElementChild is therefore satisfied before React runs. Caught /create/semantic-color in that state once under four workers. Worked around locally in 55; widening the helper touches twenty-odd specs. FIXED 2026-09-06 in [ready-accepts-unrendered-states] - the local workaround in 55 is deleted and the wait lives in ready(), where it covers the whole suite.' },
  { id: 'font-picker-shows-handgloves', title: 'The family picker sets every card in the same specimen word instead of the font name', priority: 'P2', effort: 'S', area: 'Typography', status: 'done', note: 'DONE. The founder\'s request - each card set in the family\'s own name - had already shipped; what this closes is the GEOMETRY CLAIM underneath it, which was wrong in a way no test could see. specimenSizeCqw budgeted its lines as Math.ceil(chars / lines), a perfectly BALANCED split: 32 characters over two lines is 16 each. Browsers do not balance. They fill greedily and break at spaces, so \'Noto Sans Inscriptional Parthian\' at a 16-character budget goes \'Noto Sans\' / \'Inscriptional\' / \'Parthian\' - three lines inside a two-line clamp, and the specimen is trimmed. global.css already says why that is the fault and not a fix for it. MEASURED OVER THE WHOLE CATALOGUE, all 1,946 live family names rather than the five samples the test used: 249 of them (12.8%) over-run the balanced budget. After: 0. The name list is now a fixture in tests/unit/fixtures, because a question about how words happen to divide cannot be answered by five hand-picked ones. THE SHIPPED CONSTANT IS NOT OVERTURNED, IT IS EXTENDED, which was the explicit condition on this work. A single word on one line needs a budget equal to its own length, so \'Handgloves\' still solves to 150/10 = 15cqw exactly, and the one-line path FontPicker\'s fixed-height trigger asks for is byte-identical for all 1,946 names - asserted over the fixture, not assumed. Only the two-line grid path moves, only downward, and only for names whose words do not divide evenly: 326 of 1,946. WHY IT SHIPPED, AND IT IS NOT THAT NOBODY LOOKED. The defect is invisible on most faces. Measured in a real browser at 1440: \'Noto Sans Inscriptional Parthian\' is TWO lines under both formulas in a platform sans, which is about 0.50em per character, and THREE under the balanced one in a platform monospace, which is 0.60em - the pessimistic constant the whole derivation is built on. The balanced budget was right for the average face and wrong for the one the arithmetic promises to survive. The rendered guard therefore seeds its long names as monospace ON PURPOSE; against a mid-range face it passes either way, which is exactly what the first version of that test did on a reverted formula. WIDENING THE CLAMP ALONE WAS PRICED AND REJECTED, and it is the obvious cheaper fix. Keeping the balanced budget and clamping at three stops the trimming, but that budget can reach three lines at the 30px CEILING - \'Akaya Telivigala\' does - and three lines at 30px is 103.5px in an 84px tile, which overflows the card instead of trimming it. Solved from the wrap, the tallest case is two lines and 69px at every column width. .fbd-sample still clamps at three, for the TWO names in the catalogue the 13px floor pushes onto a third line at the narrowest column, where three lines is 45px in an 84px box. THE OLD TESTS SHARED THE IMPLEMENTATION\'S MISTAKE, which is why they never saw it: fillFraction computed characters-per-line with the same Math.ceil the implementation used, so it could only ever confirm the formula against itself. The wrap is now simulated the way a browser performs it. The rendered half counts LINE BOXES with the clamp lifted rather than reading scrollHeight - a cursive face\'s ascenders overflow a 1.15 line box, so \'Shadows Into Light\' reports 38px of scroll inside a 35px single line and looks truncated when it is not, and the first draft of the rendered test failed on exactly that. MOBBIN, as evidence rather than taste. Linktree\'s font chips wrap \'Bricolage Grotesque\' onto two lines inside the chip rather than trimming it, which is the pattern extended here (mobbin.com/screens/5e3d1b9f-55bb-4593-aade-10b766825aeb). Gamma clips to one line at a fixed size - \'Instrument...\', \'Libre...\' - but prints the full name in the UI font inside the same card, which is the trade .typ-picker-face makes and this tile deliberately does not (mobbin.com/screens/05db44b6-6347-42df-b903-6bcf6f7ab24a). NOT DONE, and left deliberately: FontDossier\'s weight ladder still sets \'Handgloves\' at every cut. That is a comparison of ONE family\'s weights, where a constant word is the control and the family name would be the same string on every rung - a different question from the one this item asked. THREE MUTATIONS VERIFIED: reverting to the balanced split turns two unit tests red AND produces a real three-line wrap on the rendered page; clamping .fbd-sample back to two lines turns the stylesheet assertion red; pointing the browse tile at the one-line budget turns the call-site assertion red. GATE: lint 0 errors 28 warnings; build clean, 33 route shells; unit 1,323 pass 0 fail; users 590 passed 13 skipped 0 failed.' },
  { id: 'secondary-pages-compress-to-links', title: 'The secondary sales pages should carry a value proposition and a link, not a full pitch', priority: 'P1', effort: 'L', area: 'Brand', status: 'review', note: 'Founder request (2026-09-05): “we also need to look at the marketing flow, copywriting, value propositions of our sales pages. i want to compress our secondary sales pages to mostly act as linking pages showcasing a tools value proposition then a link”. He confirmed the scope covers THREE groups: the /create category landings, the Discover pages and the Learn pages. This is a copy and information-architecture task before it is a design task. Two constraints from work already landed: the word tokens is now Styles in user-facing copy except past the export boundary, and claims must be checkable — a previous pass had to strip a price and three false AI-limit claims from the marketing shell. Compression must not delete a true claim; the honesty lines on the homepage were kept by moving them next to the thing they describe rather than by dropping them. DONE 2026-09-05, AWAITING FOUNDER REVIEW. Applied literally to the THREE pages that are sales pages: /create/color (ColorLanding), /discover and /learn (SurfaceLanding). The Discover and Learn LIBRARIES were deliberately not touched — /discover/palettes, /discover/gradients, /discover/prompts and /discover/resources are the destinations, not the pitch, and /discover/resources had just shipped with 22 curated entries. /plans was not touched either; it has its own item. Each page is now a hero (what it is for, one line on why it is worth opening, ONE link, one honest constraint) plus its grid of real destinations, and then it stops. DELETED, for the same reason each time — not a link and not a fact: the three-up value-pillar rows on all three pages; the decorative world map on /discover and /learn; the closing SystemCTA on all three; and the hero’s second button on all three, which pointed OUT of the surface the visitor had just arrived at. THREE FALSE CLAIMS DIED WITH THE PILLARS AND ARE NOT REPLACED, because none was true. (1) ’Export-ready, everywhere — Copy CSS variables, Tailwind config, JSON, PNG or SVG’ on /create/color: in ExportPanel.jsx only book, html, md, png and jpeg carry live:true, and the CSS-token, JSON-token, Tailwind-theme and SVG+PNG asset-bundle formats all render a Soon badge and a disabled button. (2) ’Save what earns a tab / Build collections’: Collections is soon:true in DISCOVER_SPEC and has no page. (3) ’Browse community UI systems’: Inspiration is soon:true too. TWO STALE NUMBERS ARE NOW DERIVED FROM THE DATA rather than typed: the Discover hint counted ’Four curated libraries’ and had been wrong since the Prompt Library shipped and wrong again since /discover/resources did; Learn’s lede promised growth playbooks while every LEARN_GROUPS row is still soon:true. TRUE CLAIMS SURVIVED BY MOVING NEXT TO WHAT THEY DESCRIBE, the same move the homepage made when its intro paragraph went: live-vs-building is the Soon badge plus the counting hint; the hand-off is each card linking to the library that does it; the Contrast Checker’s ’free, no account’ sits on its own card (ContrastChecker.jsx has no AuthGate, no isPro read, no usage meter); Learn’s ’every figure cited or computed’ is in the hero hint where a reader decides whether to open a guide. Per-tool link text on /create/color is now that tool’s own verb rather than a shared Open, after Shopify’s Explore Tools index. DEAD CODE REMOVED WITH THE SECTIONS: src/components/WorldMap.jsx (no importer left) and the .sl-pillar- / .wmap- rules in global.css, 70 lines, brace balance asserted before the write. TWO TESTS REMOVED DELIBERATELY because both measured the world map: the map-blip target-size test in 07-public-shell-library-palette.spec.js and N4 (tool-map tooltip clipping) in 25-defect-sweep.spec.js; a note stands at each site. npm run build still prints 33 route shells — no route added or removed. NEW GUARD: tests/user-sim/52-compressed-landings.spec.js, 5 tests on this half, each verified by mutation. — — — 2026-09-08: /plans and /help, which this item left to their own rows, now take their value-proposition line from src/data/positioning.js by id (positioning-derived-surfaces). Neither page was redesigned; one sentence each, read from the module, no sentence written. — — — 2026-09-09 (anti-slop-audit-2026-09-09): /discover’s hero, which this item compressed, still carried "Find systems worth stealing." — the homepage line the founder had called bad copy, with Find in front — over an agent lede and a Discover eyebrow above the h1. All three are gone; the h1 is the surface’s name, the derived count under the button stays. Learn was not touched (#431). No sentence was written; a Discover lede is on the founder’s list.' },
  { id: 'homepage-hero-buffer-reference', title: 'The hero and the intro to the tools want a lighter, more human treatment', priority: 'P1', effort: 'M', area: 'Homepage', status: 'review', note: 'Founder request (2026-09-05): “use the mobbin MCP to look at sites like buffer for slight aleration of the hero and intro to tool section on the main homepage i like the Give it try drawn text”. Note the word SLIGHT — this is an alteration, not the shape decision. The A/B/C hero previewer is a SEPARATE and still-open founder choice and must not be pre-empted here. The specific thing he likes is the handwritten Give it a try annotation, which works because it is a human mark pointing at a real control. Copying the mark without the pointing makes it decoration, which is the exact failure the anti-slop bar names. Buffer is the named reference; Mobbin is mandatory and the citation must say which screen drove which decision. DONE 2026-09-05, AWAITING FOUNDER REVIEW. Two additions, no headline, button, copy or layout changed. THE OPEN HERO SHAPE DECISION WAS NOT PRE-EMPTED: the mark renders inside the SHIPPED hero only and HomeHeroDirections.jsx is byte-identical, so ?hero=a|b|c is exactly as it was. Worth knowing for whenever the shape is chosen — all three variants keep the command bar, so the mark still has its control to point at whichever wins. 1 — THE HERO: components/TryItMark.jsx, a stroke-drawn ’give it a try’ hanging in the left margin with a hand-drawn arrow sweeping into the LEFT end of the command bar, at the > prompt where you type — not the Cmd-K badge at the far end, and not the sign-up button, which is a link out rather than something you can try. The command bar is the one thing in the hero a visitor can operate before signing up, which is what stops the mark being decoration. MOBBIN: Cake Equity’s footer CTA is the construction itself (a drawn arrow curving from a script line into the button that line is about); Harvest’s annotated chart is why the lettering is hand-PRINTED rather than cursive, which at this size reads as a squiggle; Dovetail and Air both set one headline word in script and were NOT followed, because the hero already spends its one mark on the --hi highlighter over ’one system’ and a second mark on that line would compete. Letters are SVG strokes, not a font: two families are self-hosted and a third for eleven characters would cost a request and a FOUT. aria-hidden, because the input already carries its own accessible name. Hidden below 1200px where the centred 660px bar leaves no margin; verified at 320/390/768/1000/1024/1100/1180/1200/1280/1440/1920 with 0px horizontal scroll at every width, read in both themes. 2 — THE TOOLS INTRO: the five step kickers now sit beside the heading as a hairline-ruled list with the active one marked. NOT a paragraph — the lede deleted twice for reading as generated does not come back. Buffer’s ’Here’s what you can do with Buffer’ screen is this arrangement exactly: heading left, the short list of what the section walks through beside it, the current one carrying its detail. It fixes two things: the heading claims FIVE and that they connect, which a reader meeting one sticky panel at a time could not check; and .hsteps-head was capped at 720px inside a full-width container, so at 1440px the band was one line of type with 740px of nothing beside it. It reads activeStep, the same index the rail already uses, so the two cannot disagree — verified live moving 01 to 04 as the narrative advances. aria-hidden and not a card, pill or tab, because it is not operable. Hidden below 1024px. Reduced-motion guarded. NEW GUARD: tests/user-sim/52-compressed-landings.spec.js, 3 tests on this half, each verified by mutation.' },
  { id: 'projects-to-user-home', title: 'Projects should become a User Home, and signed-in visitors should land there', priority: 'P1', effort: 'L', area: 'Product', status: 'done', note: 'DONE 2026-09-05, and THE ROUTING CHANGE IS FOUNDER-APPROVED ’ DO NOT RE-ASK OR REVERT IT. The question was put to Dylan directly, because authenticated routing is founder-gated by docs/reference/human-validation-zones.md, and he answered “Yes, build it.” His original wording: “i want the sales page to mostly be for new users if a user is already logged in and navigates to the website it takes them to the dashboard page unless they click they home button or navigate to specifly /home”. That is exactly what shipped, so a future agent finding this surprising should read this line rather than open a proposal. WHAT THE ROUTING DOES. `/` decides: signed out it renders the same sales page IN PLACE (no redirect, so the canonical indexable root and its first paint are untouched), signed in it navigates to /projects. `/home` never decides ’ the early return above the root branch reads no auth state at all, so there is no code path on which a signed-in visitor at /home goes anywhere. The nav logo and the menu foot link already pointed at /home, so the Home button already did what he asked. /dashboard now 301s to /projects instead of /home; sending somebody who typed dashboard to the page selling them one was simply wrong. FIRST PAINT DOES NOT WAIT ON FIREBASE, which was the hard constraint and the reason this needed design rather than a redirect. The obvious build holds a loader until onAuthStateChanged fires, and this repo had already measured that wait in 20-billing-banner.spec.js: “measured at ~1s here”. A second of blank loader on the front door is a worse product than the sales page it replaced, and rendering the sales page first and swapping is a second of the WRONG page. So the decision is made from a synchronous localStorage hint (src/utils/sessionHint.js) written by useSessionHint() the last time auth resolved. MEASURED IN A BROWSER: first-contentful-paint 84ms on /projects with the hint set, with Firebase unreachable ’ the worst case ’ and the sales page never rendered on the way. Resolved auth overrides the hint in both directions, so a stale hint survives exactly one paint. The hint holds one character, no uid and no token, grants nothing, and no gate reads it; being wrong costs a wrong first paint, not access. AuthContext.jsx is UNTOUCHED ’ the hint is written by a consumer of the context, not from inside the gated file. NO LOOP IS POSSIBLE, and this is why /projects came out from behind RequireAuth. A visitor whose session lapsed between page loads still carries the hint, so / sends them to /projects; RequireAuth would send them to /login, and dismissing the popup returns them to /projects, which redirects again. The page renders its own signed-out state instead, so the loop is gone by construction. That panel had been written in Projects.jsx since it was created and was unreachable, because RequireAuth redirected before it could render. Nothing private is exposed: with no user the project list is empty and the free-plan allowance stays behind the same canSaveProjects early return, which is the property 20-billing-banner actually guards. That test moved with the behaviour rather than being weakened ’ it now asserts the rendered signed-out page rather than a page the visitor never reached. WHAT THE DASHBOARD DOES NOW, one structural decision governing the look: THIS PAGE HAS EXACTLY ONE CARD SHAPE, and it is the project. The obvious build gives the stats a card, the next-step a card, the tip a card and the starters cards, which is Kajabi’s returning-user home on Mobbin and is word for word the anti-slop bar’s “every idea enclosed in an equally weighted rounded card” plus “icons filling space without strengthening recognition”. Six card shapes means none of them is an object. So orientation is text on hairline rules with mono eyebrows, and containment is spent only on the things that are objects. EACH CARD: the palette full-bleed at 40px, drawn as the palette with hard stops ’ the old header was a decorative two-stop blend of the first two swatches with the name in white on top, so legibility depended on the user’s own colours; both font families, the scale, the colour and tint counts in mono; four dots with the gap NAMED (“No type scale”, his own example) rather than a percentage, because a percentage says how far along you are without saying which part is missing; and when it was last touched, coarse. QUICK ACTIONS behind one overflow menu, the shape Vercel’s project cards use: Open in (each tool marked empty when that part is unbuilt), Duplicate, Rename, Copy CSS variables, Overwrite, Archive, Delete. Adding three more actions to the old row of five equal pills would have produced eight identical pills with a destructive one hidden among them. Open in LOADS the project and then navigates, because navigating alone opens the tool on whatever was in the working kit and quietly edits the wrong thing. Copy CSS uses buildCSSVars from utils/exportBuilder.js, the shipped export rather than a second format. Duplicate re-checks the cap in ProjectContext and shows the refusal instead of silently doing nothing. Delete still types the name; the menu row only arms it. QUICK DATA TRACKING, zero-suppressed: a figure that comes out zero is NOT PRINTED, so one untouched project shows “1 project” and nothing else. Every figure is counted off the same array the save cap counts, archived records included, so the count beside the allowance cannot contradict the rule that enforces it. This is the “0 saves” lesson from homepage-community-points-outward, enforced by a test. The allowance still only appears once projectQuota says it is worth knowing. NEXT-TOOL SUGGESTION is derived, never random: nothing saved sends you to the palette, a gap sends you to the tool that fills it and names the project, and a complete system sends you to the Contrast Checker to validate it. Every route it can emit is checked against liveToolRoutes(). THREE STATES, NOT TWO: “we do not know yet” is its own state, read from the same hint the router used, so the two agree by construction. Without it a returning user saw the sign-in panel for the ~1s Firebase takes and watched it swap. The masthead, the tip and the starters render immediately; only the project list waits, because it is the only part that depends on knowing who you are. THE TIPS are in src/data/dailyTips.js: 18 that teach something checkable about colour, type or the CSS platform, and 8 that are there to be funny. Deterministic per local calendar day, stepping by a value coprime with the list length so a full cycle reaches every tip exactly once and consecutive days never read as a list being recited. EVERY NUMERIC CLAIM IS RECOMPUTED from the sRGB and WCAG formulae in tests/unit/daily-tips.test.js rather than quoted ’ the 21.6% luminance of #808080, the #BCBCBC true half-grey, the 21:1 ceiling, the 0.2126/0.7152/0.0722 coefficients, the 16/26/42/68 golden-ratio ladder, and darkgray being lighter than gray ’ because a tip that states a wrong number is worse than no tip: it is the one element on the surface that claims to be teaching. A test also fails on flattery, which is the register this must never drift into. SUGGESTED ARTEFACTS come from GALLERY_GRADIENTS and LIBRARY_PALETTES through the existing gradientToolUrl() / paletteBuilderUrl() hand-offs, so the tool opens with the values loaded and there is no second encoding of a tool URL. Pro-locked brand palettes are excluded: a recommendation that turns into an upsell the moment it is taken is the least trustworthy thing a suggestion row can do. NO SUGGESTED ICONS, deliberately. He said “icons, palettes or gradients” and two of the three ship. /create/icons reads the Iconify catalogue over the network, so a suggested icon would be either a third-party request on the first paint of the signed-in front door ’ the exact cost [firebase-critical-path] is open about ’ or a hard-coded SVG that is not “from the library” in any meaningful sense. If he wants icons here, the honest way is a small pre-picked local set with its own provenance, which is separate work. VERIFIED. lint 0 errors / 29 warnings (baseline 30). build 33 route shells, UNCHANGED ’ no public route was added, because /projects was already private and /dashboard is still retired. test:unit 1218 tests, 0 fail, 0 skipped. test:users 534 passed, 0 failed, 13 expected skips, clean run with no findings. MUTATION-VERIFIED 22/22 (16 unit, 6 acceptance). The first mutation pass came back 15/16 and the miss was the important one ’ breaking partsLabel() so it always said “All four parts” left the whole unit suite green, and that sentence is his headline requirement ’ so five tests were added for it and for paletteBands(). Rendered and screenshotted signed-out and through a mounted fixture at 1440, 1280 and 320px in both themes; every new text element clears 4.5:1 with alpha composited, lowest 4.55. TWO EARLIER SUITE RUNS EACH FAILED ONCE with net::ERR_NO_BUFFER_SPACE on a different spec ’ local socket exhaustion, not this branch; the third run is clean, and the specs now call expectRendered() so a dead chunk names its own cause instead of reporting a count of zero. A STALE `vite preview --port 4530` FROM THE MAIN CHECKOUT was squatting the port, bound to ::1 so curl on 127.0.0.1 could not see it; it was killed. FOR THE FOUNDER TO DECIDE, neither blocking: (1) the free cap is still 3 projects and Duplicate now makes it easier to reach ’ the number is his pricing call, not a bug. (2) The project name is set in the UI face, not in the project’s own heading family. Setting it in the project’s family was the first build and it is a lie in the common case, because a family is only fetched when a tool asks for it, so the card rendered in Fraunces for one project and the fallback for the next. Both families are named on the card instead. If he wants the real typeface on the card, that means fetching fonts on the front door and is a performance decision.' },  { id: 'brand-kit-straight-to-step-one', title: 'Build a brand kit should start at step one, with the explanation as a popup', priority: 'P2', effort: 'S', area: 'Product', status: 'done', note: 'DONE, shipped as ONE change with its two siblings. The CTA lands on /create/palette, a working tool, and the orientation card is offered OVER it. ONE DELIBERATE DEVIATION, FOR THE DIRECTOR TO RULE ON. The item asked for the founder-note pattern: focus-trapped, Escape closes, focus returns, AND it must not block the step behind it. THE LAST CLAUSE CONTRADICTS THE FIRST -- a focus trap IS the mechanism by which a modal makes the rest of the page unreachable. The non-blocking half won, because it is the half the founder asked for twice (go straight into step 1, then a popup over it). So the card does NOT declare the aria-modal attribute and does NOT trap focus, paints no scrim and locks no scroll; it DOES take focus on open, close on Escape, and return focus on close -- the correct contract for a NON-modal dialog. Declaring the attribute without trapping would fail tests/unit/modal-contract.test.js, and trapping would block the step, so not declaring it is the only answer that is both. The spec tests this as BEHAVIOUR rather than markup: it types into the tool’s own seed field WHILE the card is open and watches the palette respond, then asserts no scrim element and no scroll lock. The card opens ITSELF once on a fresh step one -- the founder asked for it to be provided -- and stays re-openable from a permanent What’s this? trigger in the rail, which is the failure mode the founder-note item recorded about one-time popups. THE HEADLINE, AND IT WAS NOT A DESIGN PROBLEM: the guided walkthrough ALREADY EXISTED AND WAS DEAD ON ARRIVAL. Pressing Build a brand kit in the Create mega menu set the session flag and navigated to /create/color -- and /create/color had stopped being a tool. App.jsx routes it to ColorLanding, the compressed colour SALES page, and ColorStudio, the only page that rendered the guide’s colour step, now mounts solely on /create/semantic-color. So the front door set a flag and dropped the visitor on a page of five links with no step bar, no popup and no step one. Reproduced in a browser before a line was changed: flag 1, .uikit-stepbar absent, .uikit-intro absent. That ONE defect is BOTH founder complaints, which is why the three items shipped together. Nothing caught it because 43-mega-menu-contents.spec.js asserted the CTA lands on /create/color and justified it as the merged studio -- an assertion that PINNED the defect rather than went stale, because it checked the URL and never whether anything was there on arrival. STEP ONE IS /create/palette NOW: the route SYSTEM_PARTS and FIRST_WINS already name as the colour tool, and the one that writes design.palette. WHERE THE STATE LIVES, DECIDED BEFORE ANY SCREEN: ProjectContext’s design. Every step’s tool already reads and writes it (setPalette, setFonts, setTypeScale), it is mirrored to localStorage under vs-current-design, pushed to Firestore per project, and saveProject snapshots it whole. So the walkthrough adds NO store and NO hand-off slot -- there is no staged draft, therefore nothing a Ctrl/Cmd-click could strand in a tab that never navigates, and no slot needing navigatesThisTab() or a ttl; the nav CTA stayed a plain button for the same reason. Progress is DERIVED through partsPresent(), the same reading the project cards show, so the two surfaces cannot disagree and no counter can drift from the work. Icons are the one step with no field in the design, so getRecentIcons() -- already written by the Icon Library, already read by the dashboard rail -- is injected. A DEFECT FOUND ON THE WAY AND FIXED AT THIS BOUNDARY: partsPresent() reads an ABSENT part as PRESENT, because norm(undefined) is empty and empty differs from inter. The User Home never sees it (a saved project always carries a whole design) but the nav calls this on every render before anything loads, and nothing built must not render as one of four built. RESUMABLE, and it needed almost no code: because completion is a reading rather than a cursor, firstIncompleteStep() just asks the design. The one real change is that the flag moved from sessionStorage to localStorage, since a flow that dies with the tab is not resumable and a full brand system is not one sitting. A FRESH press opens step one (the founder’s literal instruction); pressing it mid-flow RESUMES at the first gap and the label reads Resume: Fonts rather than lying. MOBBIN: Klaviyo Set up your account -- a checklist whose done rows keep a green tick and whose header carries a real percentage -- drove derived completion and finished steps staying visible. Cofounder’s bottom Design Roadmap rail with its persistent Skip drove the rail shape and the always-present exit. Weavy and FLORA, both docking a coach card to the foot of a canvas that stays usable behind it, drove the card being docked and non-modal instead of centred behind a scrim. TESTS: tests/unit/brand-kit-guide.test.js (18) guards the step model, and its first assertion -- every step must be a live Create tool -- fails on /create/color with the reason named, so this cannot rot the same way twice. tests/user-sim/53-brand-kit-walkthrough.spec.js (8) drives the REAL front door: open the mega menu, press the real button, see where a person lands. MUTATION-VERIFIED AT THE CALL SITE rather than the utility -- pointing launchBrandKit back at /create/color fails 7 of the 8, and the survivor is the palette-CTA test, which does not go through the nav. GATE: lint 0 errors and 28 warnings (one BELOW the 29 baseline, the old component carried one), build 33 route shells, unit 1256 pass 0 fail 0 skipped, users 563 pass 0 fail with the 13 expected skips in 12-ui-system-builder. ORIGINAL ITEM BELOW. Founder request (2026-09-05): “build a brand kit should go straight into step 1 then provide a popup with a quick set of information”. So the CTA stops landing on an explanation and starts the work, with the orientation offered over the top of it rather than in front of it. Pairs with [brand-system-guided-walkthrough] and should be built with it or immediately after — the walkthrough defines what step 1 IS. Follow the pattern just shipped for the founder note: the popup is opt-in furniture over a working surface, focus-trapped, Escape closes, focus returns, and it must not block the step behind it.' },
  { id: 'type-scale-ui-overhaul', title: 'The Type Scale tool reads as generated and is not designed around its user', priority: 'P1', effort: 'L', area: 'Typography', status: 'done', note: 'DONE (#386). Founder request (2026-09-05): “the type scale UI needs a overhaul it looks AI generated. design the tool with a user centric design”. DIAGNOSED FIRST, and two of the four findings were motifs the founder has already marked AI on this product, still present because #382 did not reach this page: the taxonomy eyebrow (Create / Typography), deleted from every other tool masthead in that PR, and a FIVE-up figure strip (9 sizes / 16px base / 1.25 ratio / 61px largest / 10px smallest) at 1348x46px — a worse instance than the Gallery three-up, because every one of its five figures was restated within 150px of it: largest and smallest ARE the top and bottom ladder rows, base and ratio were the two controls, and “9 sizes” counted rows you can see. Two more found by measuring: the 01/02/03 panel badges read RIGHT TO LEFT at 1440 (01 at x=1079, 02 at x=71, same line at y=494) because the 1344px grid puts the rail on the right, and the rail held THIRTEEN labelled controls at one weight in a 340x1592px column, with the base and the ratio ranked between “Steps below base” and “Body tracking”. USER-CENTRIC, the harder half: the two numbers that ARE a modular scale came out of the rail and became a full-width scale bar above the ladder — base x ratio = N steps, low to high, with the two terms as the live controls, so you edit the sentence that describes the scale rather than a settings panel beside a preview. The ladder now leads the rail in the DOM and the 1344px column reorder #327 added was DELETED, not adjusted: it existed because the rail held every input, and below 1344 this page is one 940px column, so the old order put ~330px of families and a closed disclosure between the scale bar and the thing it describes. Everything else went behind one plain text disclosure (Dialpad on Mobbin, screens/6087e60e-1bc3-44dd-9f6b-252fb48706ab); NOTHING was removed — all eight controls keep their id, label and aria-label. THE GAP THE MINI EXPOSED: HomeWorkbench’s Typography panel and Font Pair have both carried a Preview text input since they shipped, and the tool whose only job is judging sizes was the exception. It now has one, driving the ladder and the article heading from one string. THE CONSTANT PANGRAM IS KEPT: #327 examined it and the reasoning holds — a scale is judged by holding the text constant and varying only the size — so this change adds your own words rather than overturning that. Copy that argued for the product instead of showing it is gone: the hero workflow recital, “A scale you can defend in a review” inside the specimen, and the “A scale is half the system” aphorism over the cross-links. The h1 came down from clamp(38px,5vw,64px), because on a page about type the largest type was the one string the visitor cannot change. Evidence: MagicPath (screens/8284a8ac-b530-4b95-8d12-ba78a1a905fb) titles the same block with a plain small-caps label and no figure strip; Canva Brand Kit and Sketch Text Styles for the ladder; Dialpad for the disclosure. Five browser tests in 54-type-scale-overhaul.spec.js, all wiring rather than helper checks, each mutation-verified by breaking its call site (5/5). Fixed en route: the mobile base slider had ~90px of usable track in the 340px rail because its viewport-width media query could never fire, and the new disclosure toggle set border-radius inside :focus-visible.' },
  { id: 'prompt-library-live-preview', title: 'The prompt library shows titles where it should show the prompt', priority: 'P1', effort: 'M', area: 'Product', status: 'done', note: 'DONE 2026-09-05. THE CARDS SHOW THE PROMPT. A community card was a title and three tags, which is a filename rather than an artefact; its body is now the prompt’s own text, set in --mono exactly as .pl-modal-prompt sets the same text one activation away, with the title, tags and author beneath it. WRITER’s prompt library (mobbin.com/screens/d578541a-ee45-454e-8be8-fed36ba392af) is the same product decision already shipped by someone else and Sana AI’s workflow grid (mobbin.com/screens/ba9690ab-bc14-42ba-bc12-4d96fadde788) is its grid form; both put the artefact first and the caption second, which is motionsites’ own ordering. THE SCROLL PLAYS ON HOVER AND FOCUS, NOT CONTINUOUSLY, and that is a deliberate departure from the reference rather than a shortcut. motionsites plays every card at once and it works there because its artefact is a PAGE with no text to read; a prompt IS text, and twelve bodies of it moving at once is not a preview. It is also WCAG 2.2 SC 2.2.2 (Pause, Stop, Hide): automatic motion past five seconds alongside other content needs a mechanism to pause it, and this layout has nowhere to put one. Hover- and focus-driven motion is user-initiated and self-cancelling, so it is outside 2.2.2 and it answers the question for the one card the reader is looking at. At rest every card shows the TOP of its prompt, which is the half that says what the prompt is for. THE GATE IS UNCHANGED AND WAS RE-PROVED RATHER THAN ASSUMED. A locked prompt still never reaches PromptCard: splitLockedLibrary runs BEFORE search and sort see the data, and a locked row becomes a preview object with no text key at all. The stakes went up though, because a leaked card would now arrive with its ENTIRE TEXT on screen instead of its title, so 45-prompt-library-gate now READS EVERY RENDERED PREVIEW, pairs it with its card and requires the set to be exactly the twelve free ones under the search box, both sort orders and every category chip. MUTATION: reverting the grid source from browsableCommunity to sortedCommunity fails four tests including both new ones. The locked placeholders carry no preview element at all, so there is nowhere for a payload to land even if a future mapper handed one over. MOTION VERIFIED IN BOTH DIRECTIONS, four contexts, hovering a card in each: OS reduce with no stored choice, animation-name none and translateY 0; OS no-preference with no stored choice, pl-preview-scroll at 27s, travels; OS no-preference with Settings set to REDUCE, none and 0; OS reduce with Settings set to MOTION, 27s and travels. All four correct. ONE CLAIM WAS WRITTEN AND THEN DISPROVED, recorded because the next person will assume the same thing: the first version of the CSS comment said that without the explicit reduced-motion pair the global clamp would run one 0.01ms iteration and leave the text RESTING AT ITS LAST KEYFRAME, snapping every hovered card to the BOTTOM of its prompt. Deleting exactly those two rules at runtime and re-measuring gives translateY 0 - the clamp forces iteration-count to 1, the animation finishes, and with animation-fill-mode none the element reverts to its un-animated transform, which is the top. Forcing fill-mode both did not move it either. The pair still ships for the honest reason: it stops correctness depending on the clamp’s iteration count and this animation’s fill mode both staying put, and deleting it DOES fail 54-prompt-preview-motion, because that spec asserts the animation is GONE rather than only that nothing moved. LENIS IS NOT FOUGHT, IT IS AVOIDED. The window is not a scroll container - overflow:hidden with a transform inside - so there is nothing under the pointer for allowNestedScroll to hand a gesture to. MEASURED: a real wheel over a preview moved the page exactly 500px with the preview’s own scrollTop still 0. Making it overflow-y:scroll fails that test. TWO THINGS FOUND BY MEASURING AND FIXED. The easing was wrong - with var(--ease-standard) the text had travelled 16.5px in the first four seconds of a 27s pass, so the timing function is linear, which is also the only way the length-derived duration produces a constant pace. And the locked placeholder was tuned to the OLD card: its 104px was set against a title-and-tags card, so beside a 172px artefact window the teased row read as a different component. The preview geometry moved up to .pl-gallery and .lockt-lines now READS --pl-preview-h instead of restating it, which is the rule the .lockt-stripes comment already states for the palette placeholders. Measured 172/172 wide and 150/150 narrow. 16 mutations across the two suites, all failing the intended assertion, three controls green. A seventeenth SURVIVED and is the reason a line was deleted rather than kept: a trailing Number.isFinite branch in previewDuration that chars being a string length or 0 makes unreachable. Unreachable defence reads as covered without being covered. WHAT IS NOT SOLVED, and it is not made worse: 6 of the 8 locked prompts still ship their full text in the main entry chunk. That is [locked-library-values-still-in-bundle], still blocked on the 12/12 route limit, and nothing here touches it. What is closed is every route a USER has; what is open is reading the bundle.' },
  { id: 'palette-ui-system-cta-removed', title: 'Take Build UI system off the palette page and make the nav entry the guided route', priority: 'P1', effort: 'S', area: 'Product', status: 'done', note: 'DONE, shipped as ONE change with its two siblings. HALF ONE: BOTH entry points are gone, not one -- the UI System / Admin breadcrumb beside the page title AND the Build UI system toolbar button. Both were already admin-only (canUseUiSystem), so no visitor loses a control they could see. The mode they were the only way to reach went with them. WHAT CARRIED THROUGH IT, AND WHY IT IS DROPPED DELIBERATELY RATHER THAN SILENTLY: applyUiBrandScale transferred the generated nine-shade Brand scale back onto the board, Pro-gated, replacing seed and swatches. Nothing else ever called it, and the mode was the only producer of a scale to apply, so with the doors closed the hand-off has no source -- removed with them rather than left as an unreachable branch. components/UiSystemBuilder.jsx and its children stay on disk, now unimported and out of the bundle, with the 13 tests in 12-ui-system-builder.spec.js still describing the contract and still skipped; re-entering the tool means giving it its own route, not putting this button back. HALF TWO is the sibling [brand-system-guided-walkthrough], shipped in the same change. THE HEADLINE, AND IT WAS NOT A DESIGN PROBLEM: the guided walkthrough ALREADY EXISTED AND WAS DEAD ON ARRIVAL. Pressing Build a brand kit in the Create mega menu set the session flag and navigated to /create/color -- and /create/color had stopped being a tool. App.jsx routes it to ColorLanding, the compressed colour SALES page, and ColorStudio, the only page that rendered the guide’s colour step, now mounts solely on /create/semantic-color. So the front door set a flag and dropped the visitor on a page of five links with no step bar, no popup and no step one. Reproduced in a browser before a line was changed: flag 1, .uikit-stepbar absent, .uikit-intro absent. That ONE defect is BOTH founder complaints, which is why the three items shipped together. Nothing caught it because 43-mega-menu-contents.spec.js asserted the CTA lands on /create/color and justified it as the merged studio -- an assertion that PINNED the defect rather than went stale, because it checked the URL and never whether anything was there on arrival. STEP ONE IS /create/palette NOW: the route SYSTEM_PARTS and FIRST_WINS already name as the colour tool, and the one that writes design.palette. WHERE THE STATE LIVES, DECIDED BEFORE ANY SCREEN: ProjectContext’s design. Every step’s tool already reads and writes it (setPalette, setFonts, setTypeScale), it is mirrored to localStorage under vs-current-design, pushed to Firestore per project, and saveProject snapshots it whole. So the walkthrough adds NO store and NO hand-off slot -- there is no staged draft, therefore nothing a Ctrl/Cmd-click could strand in a tab that never navigates, and no slot needing navigatesThisTab() or a ttl; the nav CTA stayed a plain button for the same reason. Progress is DERIVED through partsPresent(), the same reading the project cards show, so the two surfaces cannot disagree and no counter can drift from the work. Icons are the one step with no field in the design, so getRecentIcons() -- already written by the Icon Library, already read by the dashboard rail -- is injected. A DEFECT FOUND ON THE WAY AND FIXED AT THIS BOUNDARY: partsPresent() reads an ABSENT part as PRESENT, because norm(undefined) is empty and empty differs from inter. The User Home never sees it (a saved project always carries a whole design) but the nav calls this on every render before anything loads, and nothing built must not render as one of four built. RESUMABLE, and it needed almost no code: because completion is a reading rather than a cursor, firstIncompleteStep() just asks the design. The one real change is that the flag moved from sessionStorage to localStorage, since a flow that dies with the tab is not resumable and a full brand system is not one sitting. A FRESH press opens step one (the founder’s literal instruction); pressing it mid-flow RESUMES at the first gap and the label reads Resume: Fonts rather than lying. MOBBIN: Klaviyo Set up your account -- a checklist whose done rows keep a green tick and whose header carries a real percentage -- drove derived completion and finished steps staying visible. Cofounder’s bottom Design Roadmap rail with its persistent Skip drove the rail shape and the always-present exit. Weavy and FLORA, both docking a coach card to the foot of a canvas that stays usable behind it, drove the card being docked and non-modal instead of centred behind a scrim. TESTS: tests/unit/brand-kit-guide.test.js (18) guards the step model, and its first assertion -- every step must be a live Create tool -- fails on /create/color with the reason named, so this cannot rot the same way twice. tests/user-sim/53-brand-kit-walkthrough.spec.js (8) drives the REAL front door: open the mega menu, press the real button, see where a person lands. MUTATION-VERIFIED AT THE CALL SITE rather than the utility -- pointing launchBrandKit back at /create/color fails 7 of the 8, and the survivor is the palette-CTA test, which does not go through the nav. GATE: lint 0 errors and 28 warnings (one BELOW the 29 baseline, the old component carried one), build 33 route shells, unit 1256 pass 0 fail 0 skipped, users 563 pass 0 fail with the 13 expected skips in 12-ui-system-builder. ORIGINAL ITEM BELOW. Founder request (2026-09-05), two halves of one decision: “lets remove the build UI system from the colour pallete also, lets make the one in the navigation the create Brand system should start a guided walkthrough to build a full system”. HALF ONE: the palette page should stop offering Build UI system at all. HALF TWO is the sibling item [brand-system-guided-walkthrough]. Before removing anything, find every entry point and what each one currently does — this repo has repeatedly found the same destination reached by several hand-kept paths, and a removal that misses one leaves a dead-looking gap. Check whether any state or draft carries through the CTA being removed; if it does, that hand-off has to land somewhere or be dropped deliberately, not silently.' },
  { id: 'brand-system-guided-walkthrough', title: 'Create Brand system in the nav should walk you through building a whole system', priority: 'P1', effort: 'L', area: 'Product', status: 'done', note: 'DONE, shipped as ONE change with its two siblings, because it is one founder decision. THE RAIL IS THE ANSWER TO carrying state between steps: it shows all four steps at once with a tick on the built ones AND THE ARTEFACT EACH ONE PRODUCED -- the real swatches, the real family names, the real base and ratio -- so standing on Icons you can see the palette you drew on Colours. Confirmed in a rendered capture, and the rail was measured on all four steps (sticky, pinned 815-880 in a 900px viewport, on documents from 1154px to 3189px tall). STILL OPEN, DELIBERATELY: the four steps are the ones the mega-menu card has always promised (Colours, Fonts, Type scale, Icons), which is NOT the same four as SYSTEM_PARTS (palette, type scale, fonts, tints) -- icons are in the flow but not in the saved design, tints are in the design but not in the flow. Left alone rather than quietly re-cutting a list the founder has already seen. FOR THE FOUNDER: the nav still calls this a brand kit rather than a Brand system; renaming means changing the mega-menu card copy too, so it was not done unasked. Related: [brand-kit-export-variations] is what the finished system should produce. THE HEADLINE, AND IT WAS NOT A DESIGN PROBLEM: the guided walkthrough ALREADY EXISTED AND WAS DEAD ON ARRIVAL. Pressing Build a brand kit in the Create mega menu set the session flag and navigated to /create/color -- and /create/color had stopped being a tool. App.jsx routes it to ColorLanding, the compressed colour SALES page, and ColorStudio, the only page that rendered the guide’s colour step, now mounts solely on /create/semantic-color. So the front door set a flag and dropped the visitor on a page of five links with no step bar, no popup and no step one. Reproduced in a browser before a line was changed: flag 1, .uikit-stepbar absent, .uikit-intro absent. That ONE defect is BOTH founder complaints, which is why the three items shipped together. Nothing caught it because 43-mega-menu-contents.spec.js asserted the CTA lands on /create/color and justified it as the merged studio -- an assertion that PINNED the defect rather than went stale, because it checked the URL and never whether anything was there on arrival. STEP ONE IS /create/palette NOW: the route SYSTEM_PARTS and FIRST_WINS already name as the colour tool, and the one that writes design.palette. WHERE THE STATE LIVES, DECIDED BEFORE ANY SCREEN: ProjectContext’s design. Every step’s tool already reads and writes it (setPalette, setFonts, setTypeScale), it is mirrored to localStorage under vs-current-design, pushed to Firestore per project, and saveProject snapshots it whole. So the walkthrough adds NO store and NO hand-off slot -- there is no staged draft, therefore nothing a Ctrl/Cmd-click could strand in a tab that never navigates, and no slot needing navigatesThisTab() or a ttl; the nav CTA stayed a plain button for the same reason. Progress is DERIVED through partsPresent(), the same reading the project cards show, so the two surfaces cannot disagree and no counter can drift from the work. Icons are the one step with no field in the design, so getRecentIcons() -- already written by the Icon Library, already read by the dashboard rail -- is injected. A DEFECT FOUND ON THE WAY AND FIXED AT THIS BOUNDARY: partsPresent() reads an ABSENT part as PRESENT, because norm(undefined) is empty and empty differs from inter. The User Home never sees it (a saved project always carries a whole design) but the nav calls this on every render before anything loads, and nothing built must not render as one of four built. RESUMABLE, and it needed almost no code: because completion is a reading rather than a cursor, firstIncompleteStep() just asks the design. The one real change is that the flag moved from sessionStorage to localStorage, since a flow that dies with the tab is not resumable and a full brand system is not one sitting. A FRESH press opens step one (the founder’s literal instruction); pressing it mid-flow RESUMES at the first gap and the label reads Resume: Fonts rather than lying. MOBBIN: Klaviyo Set up your account -- a checklist whose done rows keep a green tick and whose header carries a real percentage -- drove derived completion and finished steps staying visible. Cofounder’s bottom Design Roadmap rail with its persistent Skip drove the rail shape and the always-present exit. Weavy and FLORA, both docking a coach card to the foot of a canvas that stays usable behind it, drove the card being docked and non-modal instead of centred behind a scrim. TESTS: tests/unit/brand-kit-guide.test.js (18) guards the step model, and its first assertion -- every step must be a live Create tool -- fails on /create/color with the reason named, so this cannot rot the same way twice. tests/user-sim/53-brand-kit-walkthrough.spec.js (8) drives the REAL front door: open the mega menu, press the real button, see where a person lands. MUTATION-VERIFIED AT THE CALL SITE rather than the utility -- pointing launchBrandKit back at /create/color fails 7 of the 8, and the survivor is the palette-CTA test, which does not go through the nav. GATE: lint 0 errors and 28 warnings (one BELOW the 29 baseline, the old component carried one), build 33 route shells, unit 1256 pass 0 fail 0 skipped, users 563 pass 0 fail with the 13 expected skips in 12-ui-system-builder. ORIGINAL ITEM BELOW. Founder request (2026-09-05): the navigation entry “should start a guided walkthrough to build a full system”, paired with [palette-ui-system-cta-removed]. The intent is a single front door: one place that takes someone from nothing to a finished system, rather than a CTA bolted onto the colour tool. WHAT ALREADY EXISTS AND MUST BE REUSED, not rebuilt: the mega menu already ships a Brand kit card with a numbered 1-4 step list (Colours, Fonts, Type scale, Icons) and a Build a brand kit CTA, and the homepage workbench already runs five ordered steps with real hand-offs into the real tools. Those two are the shape of the answer and they already agree with each other. The walkthrough must carry state between steps — the value of a guided flow is that step 4 knows what happened in step 1 — so establish where that state lives before designing screens, and check it against how projects already save. Mobbin is mandatory: look at multi-step setup flows that can be left and resumed, because a full brand system is not a single sitting. Related: [brand-kit-export-variations] is what the finished system should be able to produce.' },
  { id: 'brand-kit-export-variations', title: 'Premium export should produce a real brand guidelines document, not one file', priority: 'P1', effort: 'L', area: 'Monetisation', status: 'done', note: 'Founder request (2026-09-05): “for our enhanced exporting system … i want multiple export variations for premium users”, with reference material at UI Examples/Brand kit examples. SHIPPED as a SECOND Pro document rather than a longer first one. Pro now offers a real choice: book (A4 portrait, specification, what the system IS) and guidelines (16:9 landscape, presentation, how the brand is USED). utils/brandGuidelines.js builds 9 pages with no logo and 13 with one — cover, numbered section openings with the palette run full-bleed, a rail of section markers, two rationale columns beside every title, swatch cards carrying a NAME as well as a hex (DSC), boxed alphabet grids (OpenPay), the type ladder set at size, and an “In use” spread that sets the name on every colour in the measured ink (Hook’s “make it yours”). SECTIONS ARE NUMBERED AFTER THEY ARE DECIDED, so with no logo Colour is 01 and there is no gap where a chapter used to be. THE LOGO: the founder’s decision was to accept an upload, and utils/brandLogo.js is that contract. Client-side only (no 13th /api route). Always a base64 data: URI, always rendered in an <img> and never inlined as markup, because the export is opened in a browser and an <img> is a replaced element that will not run script in its source; sanitiseSvg() refuses script, event handlers, foreignObject and external references behind that. CAP 32 KB, and it is arithmetic not tidiness: ProjectContext pushes the WHOLE project list into ONE Firestore doc (1 MiB) inside a catch that swallows failures, and Pro has no project cap — 32 KB x 30 projects fits, 128 KB x 30 does not. Rasters step 512-128px until they fit; the cap and the accepted formats are printed in the control before anything is chosen (Mobbin: Deel Custom branding). WHAT WAS REFUSED, and it is stated IN the document on a Scope page that is always present: no reversed or single-colour lockup is generated (recolouring supplied artwork invents a lockup nobody approved) - the mark is placed AS SUPPLIED on paper, ink and every brand colour and the page says a failing ground needs a variant we have not drawn; NO PHOTOGRAPHIC MOCK-UPS at all, which would have repeated the imagery-rights problem blocking [fonts-in-use-surface]; no tone of voice, motion or iconography. Clear space IS drawn, because the geometry is measured from the mark, but the page says X = half the mark height is this document’s default rather than a decision the brand has taken. NO SECOND STORE: the logo lives in ProjectContext design beside the palette, so it travels with the project and the export stays a pure function of the saved design; the data-export disclosure was updated because vs-current-design and vs-projects now carry user-supplied content. Gate: lint 0 errors / 28 warnings, build 33 route shells, 1315 unit tests, users green. 33 new unit tests, all 20 mutations killed - including reverting the ExportPanel call site, un-gating the logo field, and letting readLogo accept any data: string. NOT DONE: the guidelines are not offered as a step in the brand-kit walkthrough, and the logo is not used by the design system book or the free style guide.' },
  { id: 'moderator-role', title: 'A moderator role the founder can assign, for community submissions and feedback', priority: 'P1', effort: 'L', area: 'Account', status: 'partial', note: 'Founder request (2026-09-05): “we need to make a moderator role i can assign to people to moderate the community submissions and feedback”. Pairs with the founder’s [community-backend] decision that nothing publishes until it is approved. SHIPPED in #390 (client half, browser-verified): src/utils/moderation.js (pure role predicate - canAssignModerators is founder-only because a moderator who can appoint moderators is a self-replicating grant; canSeeReporterEmail is founder-only because /api/support takes an address from a PUBLIC unauthenticated form), src/hooks/useModerationRole.js (first thing in src/ ever to read /api/verify-admin’s claimUpdated and force the getIdTokenResult(true) refresh, without which a freshly minted claim sat outside the cached token for up to an hour), src/utils/feedbackQueueApi.js, and the Admin.jsx call sites. That fixed two REAL defects: the feedback read sat in an EMPTY catch so a rules refusal rendered as “No submissions yet”, and the three triage writes swallowed their error and toasted success anyway so a rejected write looked saved until the next refresh. api/_lib/moderators.js holds the roster and claim minting, costs ZERO routes (both route-count tests readdir api/ top level and filter .js, so api/_lib/ is invisible), and is deliberately UNWIRED. NOTE the constraint above is STALE: the admin claim IS minted, at api/verify-admin.js since #241; only the token refresh was missing. STILL INERT, AND THE BLOCKER IS NOT APPROVAL. The founder approved both gated changes on 2026-09-06, but the Claude Code auto-mode classifier refuses to edit or stage firestore.rules and api/verify-admin.js - a tool guardrail SEPARATE FROM and STRICTER THAN docs/reference/human-validation-zones.md, which refuses after consent is given and blocked the coordinator as well as the agent. It needs a settings-level permission only the founder can add; the exact paste is docs/OWNER-ACTIONS.md 1.3. Until then a moderator can read and write NOTHING - every feedback and community-submissions rule is admin-only. The two diffs are written and tested (firestore.rules: isReviewer() replacing isAdmin(), with users/{uid} and analytics-daily deliberately NOT widened; verify-admin.js: reconcileModeratorClaim, founder-only grant/revoke/list folded into the existing route, and a fix for setCustomUserClaims REPLACING rather than merging the claims object, which would delete the founder’s own moderator claim the moment he held one) and are held uncommitted on feat/moderator-role-enable plus written out in PR #390’s body under FOUNDER APPROVAL NEEDED. AuthContext.jsx was NOT touched - the hook goes straight to auth.currentUser precisely so it did not have to be. The api/ to src/ import was considered and refused: no file under api/ imports from src/ today, and a wrong guess about Vercel dependency tracing throws at import time and takes the whole admin handshake down, so the predicate is duplicated and pinned by tests/unit/moderation-role.test.js, which runs BOTH copies over one claim table and has a positive control proving the table reaches all three roles. Evidence: 49 rules tests; 10/10 rules mutations caught, each isolated to its own collection; 20/20 client mutations caught. One honest gap recorded rather than papered over - dropping the request.auth != null guard from isReviewer() leaves every rules test green, because Firestore denies on the evaluation error, so that guard is defensive and the unit test guarding its presence is the right layer for it. Honest limit: revocation is not instant - a minted ID token stays valid up to an hour, and closing that needs revokeRefreshTokens plus checkRevoked on every verifying route. UNBLOCKED ON THE APPLYING SIDE, 2026-09-10: `npm run apply:gated` (scripts/apply-gated-patches.mjs) now applies this row\'s firestore.rules diff for the founder. The diff is docs/design/moderator-role-rules.patch, taken VERBATIM from the refined working copy #390 was tested from, against the same firestore.rules blob (5360bda9) that is still on main - nothing needed refreshing and nothing was re-derived. The command says what it will do and waits for --yes or a typed y; --dry-run prints every line it would change and touches nothing; it refuses to run when those files carry edits it did not make and prints exactly what it would have overwritten; it skips anything already applied by matching the RESULTING rule rather than a patch exit code; and it writes nothing until all four gated patches have applied to a scratch copy, so a run that fails halfway leaves the tree as it found it. Then it runs test:rules (finding the portable JDK 21 itself, because the machine java is 8 and the emulator failure is otherwise an unsupported-class-file stack trace), test:unit and VITE_DEFER_FIREBASE=1 build, stopping at the first failure with the exact undo. Proved end to end on a scratch clone of the branch: all four APPLIED, rules 105/105, unit 1768/1768, `prerender: wrote 39 route shells`; a second run reported all four SKIPPED and left the tree byte-identical (four hashes compared). tests/unit/gated-patch-applier.test.js guards it, 14/14 mutations caught. WHAT IS STILL NOT COVERED, and deliberately: api/verify-admin.js, the half that MINTS the claim. Its diff in #390 is prose plus a partial hunk (\'the two includeUsers returns take the same three extra fields\'), which is an instruction to a reader rather than something a machine can apply, and re-deriving it would mean shipping a version of a security route no review has seen. So after the command runs, the rules UNDERSTAND a moderator and nothing GRANTS one. STATUS UNCHANGED - the founder has not run it yet. THE STRANDED HALF IS FOUND AND SHIPPED, 2026-09-10 (PR feat/moderator-role-last-mile). The verify-admin.js work described above was never lost - it sat uncommitted on feat/moderator-role-enable in .claude/worktrees/agent-moderator-role, tip 443affb8, six files +656/-31. It is now docs/design/moderator-verify-admin.patch, generated as a real git diff against the api/verify-admin.js that is on main (blob 7cc45bb, untouched since #390) rather than retyped, and registered as the SECOND PART of the moderator-role entry in scripts/gated-patches.mjs. npm run apply:gated therefore covers the COMPLETE role - the rules and the mint - and no longer prints a note saying that appointing somebody is a separate job; tests/unit/gated-patch-applier.test.js fails if any patch declares notCovered again, and TARGET_FILES is 5 now, adding api/verify-admin.js. THREE THINGS WERE JUDGED WRONG IN THE STRANDED WORK AND FIXED. (1) role was computed from the token as it ARRIVED, so somebody revoked a minute earlier still read as role moderator for up to an hour, which is exactly the window this row already records as its honest limit; it is now computed from the RECONCILED claim. (2) a roster read that failed left moderator false, silently downgrading a live moderator on a transient Firestore error; it now falls back to what Firebase already signed into the token - no new authority, and no volunteer knocked out of their queue - and returns rosterError, so a verdict that could not be reached is distinguishable from one that came back negative, the same way usersError already is. (3) the grant minted the claim onto ANY uid, including an account with an unverified email. roleFromClaims refuses a role above user without one on BOTH sides of the api/src boundary, but firestore.rules reads the raw claim and has no notion of a verified email, so that combination handed Firestore write access to somebody every JavaScript surface calls a plain user. The mint site is the only place that can reconcile the two and now refuses with a 400; tests/rules/firestore-rules.test.js proves the rules really do not check, which is the other half of that argument rather than a complaint about the rule. THE ROUTE TESTS ARE NOT REGEXES: the patched source and api/_lib/moderators.js run together in one vm against an in-memory Firebase, the harness tests/unit/ai-provider-path.test.js already uses for api/ai.js, so the refusals, the claim merging and the roster ordering under test are the real ones. Proved: a non-founder cannot appoint, a moderator cannot appoint, the admin CLAIM does not open the door (only the verified-email allowlist does), a revoked moderator loses the claim on the next handshake, a moderator whose claim went missing gets it back, neither claim clobbers the other in either direction, and a roster read that fails neither 500s nor grants nor revokes. The twelve emulator tests run against a PATCHED COPY on their own projectId (demo-uil4b-moderator), so the suite is green both before and after the founder runs the command and the published rules keep the coverage they had. One stranded test was DROPPED rather than moved - "anyone can still file feedback" asserts a state the feedback-bounds patch deliberately removes, so it would have gone red on the day the founder succeeded; the property it reached for is asserted in tests/unit/community-queue.test.js as a DIFFERENCE between the two texts instead. Evidence: rules 117/117 (was 105), unit 1823/1823, 26/26 mutations caught at the call site, each restored byte-exact. The Admin Users tab now carries the assignment on the row of the person it is about and DEGRADES HONESTLY: the unpatched route has never heard of moderatorAction and answers with its ordinary 200, so a button wired to res.ok would have reported a grant that never happened on the one screen where a false success is a security statement. The surface probes for a positive signal only the patched route gives (role is a string on every one of its exits), and with the route unapplied it names the state and the command that changes it and offers no control at all. STILL partial, and this is exactly what is left, all of it the founder: run npm run apply:gated, commit the two gated files, publish the rules from the Firebase console, and let the site deploy - which is itself waiting on the Vercel billing block. Nothing engineering-side remains.' },
  { id: 'gated-patch-applier', title: 'One command the founder runs to apply the four changes an agent is not allowed to save', priority: 'P1', effort: 'M', area: 'Infra', status: 'done', note: 'Four reviewed, tested changes could not be committed from any agent branch: firestore.rules, api/verify-admin.js and src/contexts/AuthContext.jsx are refused by the Claude Code auto-mode classifier, which refuses AFTER the founder has approved and blocked the coordinator as well as the agent. So each shipped as a committed, unapplied diff, and docs/OWNER-ACTIONS.md 1.3 asked the founder to paste a permissions block nobody had verified and then find four diffs across four pull request bodies and hand-apply them to two files - in an order nobody had written down, where the wrong order silently produces a rules file that does not compile. SHIPPED: `npm run apply:gated` (scripts/apply-gated-patches.mjs, registry in scripts/gated-patches.mjs). It prints the four and what each one gives him in plain words, waits for --yes or a typed y, and --dry-run prints every line it would change and touches nothing. It REFUSES A DIRTY TREE by telling its own work apart from somebody else\'s rather than guessing: the command applies in a fixed order and writes all-or-nothing, so any tree it produced is HEAD plus a PREFIX of that order; each prefix is replayed from the committed files and compared byte for byte, and if none matches it stops and prints the diff it would have overwritten. It SKIPS what is already in, detecting by the resulting rule or flag and never by a patch exit code, so a half-applied pair can be COMPLETED rather than refused. Nothing is re-derived: each entry points at the artefact the review produced - docs/design/per-project-sync-rules.patch, docs/design/moderator-role-rules.patch (#390\'s refined working copy, verbatim, against the same firestore.rules blob 5360bda9 still on main), tests/rules/pending-firestore-rules.mjs (#418, the module the emulator already runs), docs/design/firebase-deferral-gated.patch (#427). THE ORDER IS LOAD-BEARING and is not the reading order: per-project sync first because the moderator role rewrites the comment that is its trailing context, then the moderator role because the feedback bounds insert forty lines through ITS trailing context - which is also the order #418 verified on the emulator. Then it runs test:rules, test:unit and VITE_DEFER_FIREBASE=1 build, stopping at the first failure with a plain sentence naming the likely patch and the exact `git checkout --` that undoes everything. It detects Java and, finding 8 on this machine where the emulator needs 21, uses the portable JDK by name rather than failing with an unsupported-class-file stack trace. It commits nothing and pushes nothing, and says publishing the rules from the Firebase console is the one step only the founder can do. EVIDENCE, all on a scratch clone of the branch because this environment refuses to run it here - which is the block itself, reproduced: run 1 all four APPLIED, test:rules 105/105, test:unit 1768/1768, `prerender: wrote 39 route shells`; run 2 all four SKIPPED, same three checks green, and the four target files byte-identical to run 1 by hash. A hand edit to firestore.rules made it refuse and print that exact diff, changing nothing. tests/unit/gated-patch-applier.test.js: 12 tests, 14/14 mutations caught with a green control and byte-exact restores - four patches broken two ways each (context corrupted so git apply refuses; applies cleanly and grants nothing), the registry losing a patch, package.json losing the script, the document losing the command, the line-ending fix reverted, a rules patch narrowing the billing lock, and the workspace refreshing only one of a patch\'s two files. TWO REAL DEFECTS THE RUN FOUND THAT READING DID NOT: `git apply` compares context bytes, so the CRLF patch files failed against the LF text `git show` returns - which reads exactly like a stale patch and would have hit the founder on his SECOND run; and one patch touching two files refreshed only the one its detector reads, so SubscriptionContext came back reading as unpatched and half a deferral would have been reported as a whole one. SIX EXISTING GUARDS asserted the UNAPPLIED state as a constant and would all have gone red the day he ran it, taking the applier\'s own verification step with them; each now reads the result and takes the other branch. NOT COVERED: api/verify-admin.js, whose #390 diff is prose plus a partial hunk rather than something a machine can apply - the command says so on every run, and docs/OWNER-ACTIONS.md 1.3 says so in his words.' },
  { id: 'plans-page-overhaul', title: 'The plans page needs a full overhaul', priority: 'P1', effort: 'L', area: 'Monetisation', status: 'review', note: 'OVERHAULED 2026-09-05, open as a PR and NOT merged (Director’s call). THE DIAGNOSIS THE FOUNDER DID NOT GIVE: the page was not badly designed, it was UNVERIFIABLE — it described the product from memory, and prose does not go stale loudly. THREE FALSE CLAIMS, one of them paid. (1) Watermark-free export and full design JSON was sold as a Pro benefit on FOUR surfaces — /plans twice, the upgrade modal, Settings, and Checkout’s FEATURES array, which is rendered at the moment money changes hands and whose own comment calls it the worst possible place for a figure the server will not honour. There is no JSON export: json in the export table has never carried live: true, it renders a Soon badge over a disabled button, and runExport() has no branch that could build one. Somebody could have paid $48 for a file the product cannot make. (2) Unlimited palettes, font pairings, type scales, gradients and exports read as all nine export formats when four are unbuilt and one is Pro-gated. (3) Advanced harmonies, HCT editing and expanded palettes, and the table’s Advanced colour controls: Core controls to Unlocked, were adjectives standing in for two hard numbers the page never gave: Free gets 2 of 8 colour systems and 7 of 37 brand palettes. A reader cannot price an adjective, and the vagueness is where the JSON claim hid. PLUS ONE DEFECT NOBODY HAD SEEN: the entire closing CTA rendered at opacity 0 for every visitor in every browser. SystemCTA puts its content in a [data-reveal] div and global.css starts those at opacity 0; Home drives the reveal with useHomeMotion, ColorLanding and SurfaceLanding call useReveal(), and Plans.jsx called NEITHER — a 472px blank white box at the foot of the pricing page. No test caught it because the node keeps a real bounding box and visibility:visible, so Playwright calls it visible and clicks it: 18-signup-intent has been clicking an invisible button and passing. WHAT SHIPPED. Every figure is now imported from the module that also drives the behaviour (AI_LIMITS, FREE_SAVE_LIMITS, COLOUR_SYSTEMS, BRAND_PALETTES, and the export table). The export table MOVED out of ExportPanel.jsx into src/config/exportFormats.js precisely so /plans could read it: a format cannot be advertised unless the same flag that renders its button says it is built. New Before you pay section states the three things a buyer would otherwise discover afterwards (shared AI capacity, the four unbuilt formats, where the typography wall is). Free card gains an explicit Not on Free exclusions block. The comparison table is grouped and quantified with not one bare tick in it. MOBBIN drove the structure, cited inline in the source: Sketch (a licence states its own exclusions), Clerk and Shuttle (a Soon badge inline against the feature, never a footnote), Mixpanel/Railway/Rox (usage limits grouped first, real numbers per cell), Plain and Parker AI (the shared baseline stated once). No invented metric, testimonial, countdown or urgency was added. THE LOAD-BEARING LINE WAS HANDLED DELIBERATELY, NOT SILENTLY: typography-paywall.test.js pinned the and exports sentence because it is why the typography tools have no gate on copy or export. The QUANTITY promise is kept exactly (unlimited, unmetered, and the table still says the number of exports is unlimited on Free); only the FORMAT implication was removed. Those two assertions were rewritten with the reasoning in place, and mutation-verified. FOUNDER DECISIONS OUTSTANDING, all in the PR body: (1) Cancel any time was REMOVED from the trust row and the Pro card footer because [stripe-retention-config] is blocked and the Stripe Customer Portal has no cancellation flow enabled — the code is defensive (create-portal.js falls back to the default portal) but a default portal with cancellation disabled still gives the user no way out. Put the line back the moment the portal is configured. (2) Export is still ungated and the free tier still advertises unmetered export, which is what justifies the typography gate — changing one changes the other. (3) The four unbuilt export formats are now disclosed by name on the pricing page; if that is not wanted, the alternative is to build them or remove them from the panel, not to go quiet. VERIFIED: lint 0 errors / 29 warnings (baseline), build 33 route shells, unit 1248 pass 0 fail 0 skipped, users 561 passed / 13 expected skips / 0 failed, exit 0, on a clean rerun. The run before it failed once with net::ERR_NO_BUFFER_SPACE on 38-input-specificity at /create/palette — local socket exhaustion on an unrelated spec, the same transient this board already records twice under projects-to-user-home, and it did not reproduce. Rendered and read in a browser signed out at 1280 and 390, and ALSO with the price service stubbed, because a preview has no serverless functions and every earlier screenshot had been the degraded Pricing unavailable path: the happy path renders $48.00/year, Save 43%, $4.00/month, 7-day free trial. A TRUE SIGNED-IN RENDER WAS NOT ACHIEVABLE and is not claimed — the acceptance suite has no mechanism to create a signed-in user (20-billing-banner.spec.js says so in its own header and injects markup instead), so the isPro branches were verified by reading them, not by rendering them. That gap is already tracked on audit-coverage-not-run. NEW GUARDS: tests/unit/plans-truth.test.js (10) and tests/user-sim/54-plans-truth.spec.js (6), the second asserting COMPUTED OPACITY on the closing CTA because opacity is what was wrong and what every other check missed. MUTATION-VERIFIED 19/19 — and the harness itself lied TWICE, which is worth recording because both lies were silent. It first reported 0/10 caught: Python decoded node output in the Windows codepage, the tick markers arrived mangled, and every mutation read as still green. It then reported a SKIP on the single most important mutation (removing useReveal() to reproduce the invisible CTA) because the anchor was written with a bare LF against a CRLF file — the exact trap this repo warns about, hit inside the tool meant to catch traps. It now proves it can see the test markers before trusting its own verdict, and treats a skipped mutation as a miss rather than as a neutral line. ONE THING LEFT DELIBERATELY, FOR THE FOUNDER: the headline price renders $48.00, not $48. planLadder.js exports formatMoney() and documents exactly why a whole amount should lose its cents on a 44px number, and ProUpgradeModal uses it — but /plans quotes useProPrice(), which formats through formatCurrency() and always renders two decimals. So the upgrade modal says $48 and the pricing page says $48.00. It is cosmetic and it is not untrue, the fix touches a hook shared with Settings and Checkout, and it is a taste call, so it is flagged rather than changed.' },
  { id: 'community-monogram-contrast', title: 'Nine of the twelve community tiles print letters nobody can read', priority: 'P1', effort: 'S', area: 'Accessibility', status: 'done', note: 'DONE, and what was left of it was NOT the twelve tiles - those shipped with [gradient-text-below-aa], per-item ink exactly as the founder picked, and this row was the duplicate that never got closed. Re-measured to confirm rather than assumed: all twelve curated monograms clear the 3:1 large-text floor today, worst 3.455 on s11, best 9.284 on s3. WHAT WAS STILL OPEN IS THE GROUND PER-ITEM INK COULD NOT SEE. The guarantee is about a card that HAS a usable pair of hex stops. Nothing validates c1/c2 - sanitizeCommunitySubmission passes them straight through and Community.jsx spreads a server payload over its own defaults - so a card can reach CommunityCard with stops CSS cannot use and fall through to the rule\'s own fallback, linear-gradient(135deg,var(--accent),var(--accent-strong)). That is not per-item DATA, it is a THEME pair, and it was never measured. MEASURED NOW, over the whole ramp rather than the endpoints: the hard-coded #FFFFFF in the var() fallback is 4.43:1 on the light accent pair and 2.41:1 on the dark one, and 2.23:1 under prefers-contrast: more in dark. Under the floor in dark, over it in light - a light-only check reports a clean pass, which is the same theme reversal this board already recorded for --accent-strong on /seo. TWO HALVES OF ONE HOLE, and splitting them was the bug. --c1/--c2 were written unconditionally while --mono-ink was written only when they parsed, so a card with a non-hex stop took its ground from its own bad data and its ink from the stylesheet - two measurements of two different things. A stop CSS cannot parse at all (\'nope\') invalidates the whole gradient at computed-value time, .ch-thumb resolves to `initial`, and the monogram lands on the CARD, where white is 1.00:1 in light. CommunityCard now writes all four properties together or writes none, and .ch-thumb carries a per-theme fallback ink from the same near-poles inkOnGrounds walks: #F2F3F5 at 3.99:1 light (7.36 under prefers-contrast) and #0A0B0D at 6.31:1 dark (8.82). An inline --mono-ink still wins over both, so a card with usable stops is untouched by this. THE WHOLE SPACE, NOT A SAMPLE, because the brief for this item was not to repeat the ink function that searched one direction only. 46,656 gradient pairs on a 6-level RGB grid: inkOnGradient returns an ink below 3:1 on 8,258, and 8,208 of those are pairs where NO ink of any kind clears - the straddle bound the unit test already pins. The other 48 are NOT a one-directional search; inkOnGrounds does walk both poles. They are a RESOLUTION artefact: the walk converges to exactly the target on the 33-sample ramp (3.001 to 3.008) and the true minimum sits between samples (2.978 to 2.999). Every one of the 48 is a pair whose best possible ink clears by under 0.05, and raising the sample count to 129 only moves it to 160 of 46,656 - so the answer is a MARGIN, not more samples, and it was left alone. The shipped seeds carry one: the unit test asserts each clears by at least 0.4 and the worst is 3.455. PINNED BY four new tests in tests/unit/community-monogram-ink.test.js, which read the accent pair AND the fallback ink off global.css rather than repeating them, so moving a brand value fails the test instead of silently invalidating it; and by two seeded cards in tests/user-sim/48-text-over-gradients.spec.js whose stops are the two shapes of unusable (\'red\', which CSS parses and this app cannot measure, and \'nope\', which CSS cannot parse), asserted to have been SCORED at both viewports so the seed cannot quietly become a no-op. THREE MUTATIONS VERIFIED: deleting the fallback rules turns the unit half red and the DARK half of 48 red while light stays green - which is the reversal itself, demonstrated; flipping the dark fallback to the light pole turns the unit half red; restoring the unconditional --c1/--c2 write turns the wiring test red. GATE: lint 0 errors 28 warnings (28 is the new baseline after #390 removed code, not a change from this branch); build clean, 33 route shells; unit 1,323 pass 0 fail; users 590 passed 13 skipped 0 failed, the 13 being the pre-existing 12-ui-system-builder skips. The first full users run failed 36-rail-overflow with /plans rendering a blank shell; it passed 12 of 12 in isolation and the whole suite was green on re-run, so it is the lazy-route load flake this board already records and not this change.' },
  { id: 'locked-library-values-still-in-bundle', title: 'The locked brand palettes are withheld from the page but not from the bundle', priority: 'P1', effort: 'M', area: 'Monetisation', status: 'blocked', note: 'THE HALF THAT SHIPPED, so nobody redoes it: the rendered app no longer hands a paid row over, and that is now true of BOTH paid libraries. /discover/palettes used to print every Pro brand’s five hex codes as visible text and make each swatch a copy-to-clipboard button; /discover/prompts used to let its search box walk straight through the gate [prompt-library-free-tier-is-positional]. Both now split BEFORE they render (src/utils/lockedPreview.js): a locked row is replaced by a preview carrying only whitelisted, already-public facts, and the whitelist strips anything else even from a careless mapper. Proof is tests/user-sim/44-locked-library-tease.spec.js and 45-prompt-library-gate.spec.js, every assertion paired with a free-row positive control, all verified by mutation. THE HALF THAT DID NOT, stated plainly rather than implied: the values are still in the JavaScript bundle, and MEASURED on a production build 2026-09-04 it is both libraries. Every paid brand hex sits in the lazily loaded lockedPreview-*.js chunk. All eight locked prompt titles and their full texts sit in index-*.js, the MAIN entry chunk every visitor downloads on every route, so the prompt exposure is the wider of the two, not the narrower. Both prerendered shells are clean. What is closed is every route a USER has - the DOM, the clipboard, the search box, the sort control, the category chips, the accessibility tree, devtools inspection and CSS-off. What is open is reading the bundle. THE DECISION, which is a founder call rather than an oversight: the real fix is a build-time split (a public module with the free rows, the paid values emitted server-side only) plus an authenticated fetch that verifies entitlement the way api/ai.js already does - Bearer id token, verifyIdToken, planForUser. That machinery exists; the ROUTE does not. The plan is FULL at 12 of 12 and two unit tests (account-deletion, ai-provider-path) fail the build on a 13th, so it must replace a route or fold into an existing one. RECOMMENDATION, so the call can be made rather than deferred a third time - SPLIT THE TWO LIBRARIES, they are not the same problem. Do not spend the route on the brand hexes: brandPalettes.js’ own header says these are public identity colours, not proprietary data, and its provenance rule audits every value against the brand’s own press site, so Figma, Slack and Duolingo publish the hexes themselves. Gating them buys secrecy over information the brands give away, and costs an async fetch on the synchronous data path a 2,300-line PaletteBuilder is built on. What Pro actually sells there is already written on the wall - each brand applies its whole colour SYSTEM rather than its swatches alone - and every row still carries system: custom, so that differentiator is not real yet. Making it real is worth more than hiding a hex and is not blocked by the function limit. The PROMPTS are the opposite case and the one worth a route if anything is: a community prompt’s text was written for this product and is published nowhere else, so it is genuinely secret and is genuinely being handed to every visitor in the main chunk. RE-MEASURED 2026-09-06 on a fresh production build: all EIGHT locked prompt texts (c-4, c-5, c-6, c-7, c-8, c-10, c-11, c-12) are in index-*.js, not six - the sourcemap shows src/data/communityPrompts.js (27.7 KB) bundled into the main entry chunk. Method: import the module, take the first 40 characters of every non-free prompt text, grep the built asset.' },
  { id: 'prompt-library-free-tier-is-positional', title: 'The community prompt gate counts positions, so a search walks straight through it', priority: 'P1', effort: 'M', area: 'Monetisation', status: 'done', note: 'FIXED 2026-09-04. The gate counted POSITIONS: PromptLibrary locked every card past the twelfth of the FILTERED list, so free meant the first twelve of whatever view you had built - and the search box, the sort control and the category chips each rebuilt the view. Every one of the twenty was reachable. THE DECISION IT WAS WAITING ON turned out not to be a pricing decision, which is why it could ship without one: the twelve now carrying free: true in communityPrompts.js are exactly the twelve a signed-out visitor saw in the DEFAULT view (sort popular = saves descending) before the change, asserted equal at the time of the edit. So WHICH prompts are free did not move; only the answer’s dependence on the query did. Widening or narrowing the tier is still founder-owned, and is now pinned as a literal in tests/unit/locked-library-tease.test.js so it has to be done on purpose rather than as a side effect of editing a saves count. WHAT SHIPPED: the community list routes through splitLockedLibrary before search and sort can reach it, which also closes the oracle - the predicate reads p.text, so it used to answer whether a Pro prompt contained a given phrase for anyone who typed one. PromptCard’s locked branch is deleted rather than left dormant: it rendered p.title || p.text.slice(0, 60) into the title AND the aria-label of a card it had just declared locked, and for a prompt the title IS the product. The upgrade banner above the grid is gone - its claim was false under the positional gate, and its +25 line already lives in SubmitPromptPanel beside the submit form. Three placeholders and one wall now close the library, and the locked grid’s seam reads the gallery’s row gap instead of inheriting the 8px toolbar margin, which had it reading as attached to the row above rather than as the next row. WHAT IS STILL OPEN: the locked prompt texts ship in the main JS chunk. Same class of leak as the brand palettes and tracked as one decision in [locked-library-values-still-in-bundle], where the recommendation is that the prompts, not the public brand hexes, are the half worth spending a serverless route on.' },
  { id: 'token-vocabulary-undersells', title: 'The word token is doing too much work and selling too little', priority: 'P1', effort: 'L', area: 'Brand', status: 'done', note: 'FOUNDER DECISION 2026-09-05, recorded in CHANGELOG.md. THE ANSWER IS STYLES - option C of the three in PROPOSALS.md P-019, chosen over the recommended Foundations. IMPLEMENTED - see the DONE block at the end of this note for what shipped and what is left. Rule R is unchanged and was never in question - the word token survives past the EXPORT BOUNDARY only (the export panel, the generated file names, the code blocks, the @uil4b/tokens package), because that is the one place it is correct and renaming it there would break customers code. It comes out of every sales, navigation and tool-heading surface. The homepage behaviour contract tests/user-sim/10-home-chaos-to-calm.spec.js asserts the H1 and needs its assertion moved deliberately alongside it. NO LONGER BLOCKED ON THE FOUNDER. Original item below. Founder request (2026-09-04), verbatim: \u201ci dont like tokens being such an important word we are using around the site, tokens means alot of things and in the context we are using it doesnt make it seem like its a huge value add\u201d. TWO SEPARATE PROBLEMS, and the second is the one that costs money. (1) AMBIGUITY: token now reads as crypto, as an LLM billing unit, or as an auth credential before it reads as a design value \u2014 and this app also uses the word in its real auth sense (the admin token on /api/ai?diag=1), so the collision is internal as well as external. (2) IT NAMES THE MECHANISM, NOT THE BENEFIT: design token is industry jargon for how the value is stored. The thing a visitor actually buys is that the hex they change in the palette builder is the hex their export ships \u2014 one system, everywhere, no drift. The homepage already says that in plain words; the tools then fall back on the jargon. SCOPE, MEASURED: ~390 occurrences across src/pages, src/components and src/locales, and the word appears in ALL TEN locale files. THREE LAYERS THAT MUST BE SCOPED SEPARATELY, because they have different owners and different risks. (a) USER-FACING PROSE \u2014 headings, descriptions, button labels, empty states. This is the founder\u2019s complaint and the whole win; it is a COPY task, and it is translated into ten locales, so a rename is a translation job not a find-and-replace (the same reason #306 declined to rewrite description copy). (b) EXPORTED ARTEFACT NAMES \u2014 the CSS custom properties and the token files a developer actually consumes. RENAMING THESE BREAKS USERS\u2019 CODE and loses the one place where the industry term is correct and expected, since a developer searching for design tokens should find them. Default to leaving these alone and say so. (c) INTERNAL IDENTIFIERS \u2014 variable names, CSS custom property names in global.css, test fixtures. Invisible to users; renaming is churn with no benefit. Leave them. THE HARD PART IS CHOOSING THE REPLACEMENT, not doing the replacement. Do not simply delete the word and leave a gap, and do not reach for a synonym that is equally abstract (variables, primitives, atoms all fail the same test). The replacement should say what the user gets. Mobbin is the right tool for how comparable products name this \u2014 and note that several avoid a collective noun entirely and just name the things: colours, type scale, spacing. Bring the founder two or three options with the reasoning before rewriting ten locales. OPTIONS DELIVERED, NOT A REWRITE — see docs/PROPOSALS.md P-019 for the three named options with exact wording, gains, costs and the Mobbin evidence. Nothing user-facing was rewritten, because the item asked for options first and the word choice IS the decision. RECOMMENDATION: Foundations, with the things named beneath it (Colour, Type, Spacing, Icons), plus the rule that token survives ONLY past the export boundary — the export panel, the generated file names, the code blocks and the @uil4b/tokens package. That split is exactly what Sketch ships (Color Variables in the editor, Color Tokens at export). Foundations is also already our own word: CLAUDE.md opens with build, organize, validate, and export interface foundations, and the simulated docs nav in UiSystemLab.jsx already reads Foundations. EVIDENCE: 22 shipped surfaces checked on Mobbin (Customer.io, Retool, Gamma, Zendesk, GitBook, Whereby, Canva, Mailchimp, HubSpot, Adobe Express, Typeform, Miro, Klaviyo, VEED, Semrush, GoDaddy, Frontify, Cash App Design, Sketch, Framer, Linktree, Teachable, Hex, Kajabi, Copilot) — NOT ONE uses token in navigation or a section heading. TWO CORRECTIONS TO THIS ITEM OWN SCOPE, both measured on the branch, and both make it much smaller than written. (1) The ~390 figure is a raw grep count and badly overstates the copy job: actual user-facing English prose is ~35 strings. UIBuilder.jsx alone contributes 71 lines of tokens.spacing-style template literals, and 9 more hits are Bearer ${token} auth calls. (2) THERE IS NO TRANSLATION JOB — the locale strings are DEAD CODE. All four token strings in each of the ten locale files sit under the top-level designExport namespace, left behind by the retired Design System Export tool (/export, retired to /create/color). Nothing in src reads that namespace; key resolution is static (resolve() in I18nContext.jsx splits a literal dotted path, and the only template-literal import builds a locale FILENAME, not a key); the sole remaining reference anywhere in the repo is a comment in tests/unit/locale-tool-copy.test.js. So layer (a) is English-only BY CONSTRUCTION and the rewriting-ten-locales risk this item is built around does not exist. Those dead strings are still worth reading as evidence FOR the rename: fr renders it Jetons (casino chips) and zh renders it 令牌 (an authorisation tally — literally the auth meaning). No translator carried the intended sense. THIRD MEANING CONFIRMED IN-PRODUCT, which strengthens the ambiguity case beyond what the item claimed: /learn/ai renders stat tiles reading Token context limit and Per session token usage, so UIL4B ships the design sense, the auth sense AND the LLM-billing sense of its most important noun. WHEN THE FOUNDER PICKS: ~35 strings, no locale work, no exported-artefact renames, no internal-identifier churn. The homepage H1 (Colour, type and tokens that stay one system) is one of them, so tests/user-sim/10-home-chaos-to-calm.spec.js needs its assertion updated deliberately in the same slice. DONE 2026-09-05, EXCEPT THE HOMEPAGE. 22 exact-match replacements across 10 source files plus 2 spec assertions; no locale work, no exported-artefact rename, no internal-identifier churn, exactly as the scope correction above predicted. THE RULE AS APPLIED: Styles is the collective noun wherever a collective noun is needed; where the sentence already sits beside a concrete artefact the artefact is named instead (CSS, the scale, tints), which is the most-supported pattern in the P-019 evidence; and token survives ONLY past the export boundary. WHAT DELIBERATELY KEPT THE WORD: the whole of ExportPanel.jsx (the CSS tokens and JSON tokens format names, the book description, the token formats line), the ColorStudio Developer handoff block (Canonical, predictable token names / ready for CSS or a token pipeline), the UiSystemBuilder export gate title and its Token hand-off kicker, and the UiSystemLab DocsScene, which is a simulated docs site for npm install @uil4b/tokens and is therefore past the boundary by construction. That is the Sketch split actually shipping. WHAT WAS LEFT ALONE ON PURPOSE: internal identifiers (uib-token-group, the tokens prop, the --color-* names), the auth sense, the LLM sense on /learn/ai, the ten dead locale files, and mock content inside preview specimens (the Token migration kanban card in PaletteBuilder, the CLI log in FontDossier, the publishing scene in ColorStudio) because those depict other products rather than speaking in the UIL4B voice. TESTS UPDATED DELIBERATELY, both in tests/user-sim: 05-tint-scale-workflows.spec.js from 22 generated tokens to 22 generated tints, and 06-colour-tool-workbenches.spec.js from the Copy all tokens button to Copy all CSS variables. Nothing else in either suite named the old copy. NEW MOBBIN EVIDENCE, 4 surfaces beyond the 22 already cited and not one of them using token in a nav label or a panel heading: Customer.io Design Studio ships a Styles editor tab AND a Styles panel heading with Variables (Colors, Fonts, Radius, Spacing) named beneath it, which is exactly the structure adopted here; the Framer right panel reads Style with a Styles row inside Text; Rive reads Text Styles; and the Sketch documentation index reads Symbols and Styles. TWO OF THE EDITED FILES ARE UNREACHABLE TODAY, which is worth knowing before anyone counts the win: src/pages/Landing.jsx and src/pages/UIBuilder.jsx have NO importer anywhere in src - they export a default nobody mounts. Their strings were corrected anyway (they will be right whenever the pages return) but they reach no visitor now, and eslint does not flag either. The reachable renames are /create/tint, /create/type-scale, /create/semantic-color, /create/color, /projects, /style-guide, the Discover resource note and the UI System Builder lede. DONE 2026-09-06 - the homepage slice this note held open, and the item is CLOSED. FOUR STRINGS AND ONE ASSERTION, in one commit with the share card so the card and the page cannot disagree. (1) THE H1 is now Colour, type and icons that stay one system. The third noun is ICONS, NOT STYLES, and that is this item own rule applied rather than a departure from it: Styles is the collective noun WHERE A COLLECTIVE IS NEEDED, and this line is not a collective - it is a list of three concrete things, and Styles is a SUPERSET of the two beside it, so Colour, type and styles reads as leftovers rather than as a third family. Icons is a live Create group, a workbench tab on the same page, and already named both in the tools heading below and in the Plans comparison table, so it is a claim the site already makes rather than a new one. (2) THE TOOLS HEADING LOST A NOUN rather than gaining a replacement - Colour, type, icons, imagery, export. Tokens was never a sibling of the four beside it (it is how those four are stored) and styles would have repeated the same category error with a different word, so the word is simply gone. Components stay absent because the lede directly below is where they are named, as coming next. (3) THE WORKBENCH UI-PREVIEW ROW is Styles - the ONE place in this slice where a collective noun is what the row needs, because it sits beside Component library and Documentation, the other two things a design system ships. So it takes the founder word directly, and that trio is exactly the Customer.io Design Studio structure the P-019 evidence cites. (4) public/previews/cards.json and og-image.png regenerated with npm run og:cards. The seven SECTION cards were restored to their committed bytes on purpose: they do not read the hero, their cards.json entries did not change, and redrawing them was chromium rendering noise rather than content - a homepage copy edit should not rewrite seven unrelated binaries. ONE OCCURRENCE FOUND BEYOND THIS NOTE OWN LIST, by enumerating rather than trusting the list: public/llms.txt described the UI Builder as a complete design system (colour, type, scale, tokens) - the same category error as the tools heading, on a public sales surface, and BEFORE the export boundary rather than past it. It now reads (colour, type and scale). The llms.txt SUMMARY line was left alone and is correct as written: export production-ready CSS and design tokens names what the export produces. THE SPEC ASSERTION WAS REPLACED, NOT MOVED, because it was never a test. It read toContainText(/colour|color|type|token|system/i), and the h1 second line is that stay one system. which never varied - so system satisfied the alternation on its own and the assertion passed for EVERY possible first line, including the category headline it was written to catch (Every design tool, one search box away) and including the word this slice removed. Same shape as the .tsc-status assertion PR #386 found. THREE assertions stand in its place: a POSITIVE CONTROL that the h1 rendered text; a DERIVED check that at least two of the tool families named in the page own tools heading appear in the headline, which is the wiring between the hero and the section that lists the tools and which the old category headline fails; and a PAGE-WIDE check that no VISIBLE text under #main contains token, which covers all three renamed strings at once and encodes rule R on the one surface where it is checkable whole (innerText not textContent, deliberately - textContent would also return the hidden-but-mounted workbench panels). THREE BORDERLINE OCCURRENCES DELIBERATELY LEFT, classified rather than missed: toolIndex.js type-scale search description (Modular type scale calculator with CSS and token export) names the export and sits on the correct side of rule R; DocsDesign.jsx (Build a design token system before designing any pages) is Learn editorial about how Stripe and Linear work, where the industry term IS the correct term - the same call already made for the LLM sense on /learn/ai; and the UIBuilder.jsx table header (isDense ? Token : Type Style) is mock specimen content in a file with no importer. ALSO LEFT: the comment in tests/unit/locale-tool-copy.test.js and the V2 entry in CHANGELOG.md that quote the old H1, because both are past-tense records of what was on screen at the time and rewriting them would falsify the record. THE HERO SHAPE IS UNTOUCHED and stays with [hero-copy-still-reads-ai], which owns it and is blocked on the founder pick of direction A, B or C. This slice changed one word.' },
  { id: 'mega-menu-feels-generic', title: 'The mega menu works now and still looks like everyone else\u2019s', priority: 'P1', effort: 'M', area: 'Navigation', status: 'done', note: 'DONE. Founder (2026-09-04): “the mega menu could use a UI redesign as it feels a bit AI slop”. FIRST, THE FILE ITSELF WAS BROKEN AND IT IS A LIVE DEFECT, NOT A TYPO. This id existed TWICE. #345 added this row from the founder’s words at 09:01:51; #352 branched before that landed, found no such row, wrote its own from the brief and recorded it as done at 09:02:12 — so the file carried two rows with the same id, and its note opens by correcting a reader about an entry that did exist. Admin.jsx keys onStatusChange, onNotesChange and onDelete on item.id, so marking one done or deleting it acted on whichever the lookup reached first. #352’s row is renamed to [mega-menu-shows-the-product], which is what it actually did; its note is untouched. There is no uniqueness test on these ids and nothing would have caught this. WHAT WAS ACTUALLY WRONG, and it was not taste in two of three places. The panel after #352 was correct and still read as a directory: eighteen rows on Create, each an icon beside a title above a grey line at the SAME size and spacing, so nothing told the eye which of them mattered. THE CAUSE WAS THE SECOND LINE. Eighteen of the thirty-four description lines across the three panels said nothing their label had not already said — “Font Gallery / Browse and compare type families”, “Emoji Library / Find and copy emoji by category”, “Aspect & Resolution / Calculate dimensions and aspect ratios” — and TWO OF THEM DID NOT EVEN FIT. “Reuse prompts proven by the community.” rendered as “…proven by the” and “Structure production-ready image prompts.” as “…production-ready image…”, measured at 1440 and at 1280. Both shipped past #340 AND #352 because -webkit-line-clamp reports overflow as a tidy ellipsis: a row that has lost its last two words looks deliberate in a screenshot and identical to one that fits. TWO RULES NOW DECIDE WHETHER A ROW GETS A SECOND LINE, and both read data the row already carries rather than adding an eighth hand-kept list. (1) A SOON ROW GETS NO DESCRIPTION — a sentence about an unbuilt tool is a claim about something that does not exist, and withholding it is what makes live and unbuilt read apart straight down a column without hunting for a badge, which is exactly the reasoning #352 used to keep a preview off the Learn card. It keys off t.soon, so a tool shipping gives its line back with no edit. (2) AN EXPLICIT ’’ MEANS THE LABEL ALREADY SAYS IT. The `in` test is the subtle part: DELETING a key would not drop the line, it would fall through to the group’s desc in toolTree.js — page copy written for a card on /discover — which is how a clipped blurb got there in the first place. MEASURED, at 1440x900: Create 18 descriptions to 6 and the panel 741 to 702px; Discover 8 to 4; Learn 8 to 0 and the panel 404 to 362. Zero clipped, from two. Rows may now DIFFER IN HEIGHT — min-height 50 to 44px, which is not an arbitrary relaxation but the target-size floor this file already applies to .pnav-tool under 1024, so a one-line row lands ON the floor rather than below it. That mattered: at min-height 50 every row stayed a measured 54px whether it had anything to say or not, so removing the prose would have changed the text and left the tiling exactly as it was. THE WIDTH LEVER WAS MEASURED AND FOUND MOSTLY ALREADY PULLED, which corrects this item’s own diagnosis. Measured with a Range — scrollWidth lies here, because .pnav-tool-line is a flex child stretched to its 1fr column and so every stack reports the column width — Create wants 1250 against 1260 set and Discover wants 1203 against 1200. Both were already content width. Only Learn was loose, 1020 against 953, and is now 960. THE PANEL READS WIDE BECAUSE SHORT COLUMNS LEAVE IT EMPTY BELOW, which is vertical and is not the width: Discover spreads 8 tools across three columns holding 4, 2 and 2. That is the next lever. MOBBIN. Five web mega menus carrying twelve or more destinations — Hers, Selfridges, Fiverr, Pinterest Business hub, lululemon — and NOT ONE puts a description under every row; they build hierarchy from type size and grouping instead. Codecademy, the closest structural analogue (a left rail of sections beside a grid of items), describes the SECTION once and leaves each item as icon plus name; that drove keeping the group heads and dropping the row prose. Later was the counter-case and it proves the rule — it does give every entry a description, and it has three entries. SHOWING THE PRODUCT IN THE ROWS WAS CONSIDERED AND ARGUED DOWN, on evidence, though the brief invited extending it. Lovable’s theme list is the reference for it: each row is a four-dot swatch cluster plus a name, no description, and the swatches ARE the description. It works there because the names are ARBITRARY — you cannot know what “Obsidian” or “Harvest” is without seeing it. UIL4B’s row names are descriptive, so a specimen beside “Palette” repeats the word rather than informing it; the card already shows a real GALLERY_PALETTES entry 260px away; and a strip on every row is twelve uniform things again, which is the failure this item exists to remove. TESTS: 43-mega-menu-contents.spec.js, 10 to 14. #340’s 27-of-27 viewport-by-menu claim is written down for the first time (DESKTOP_MATRIX x TRIGGERS; it had committed two of the nine widths, so seven were a hand check nothing could repeat), and each combination now also asserts that no description clips and no row drops under 44px — the two assertions whose absence let the truncations ship. The fifteen keyboard behaviours are untouched and still pass. MEASURE ONLY AFTER THE ANIMATION SETTLES: every row first came back 43px against a min-height of 44, which is not a layout bug but 44 x 0.975, because .pnav-menu animates in from scale(.975). openWithPointer now awaits getAnimations().finished — the animation layer, not a timeout a slower machine invalidates. NINE MUTATIONS, ALL NINE CAUGHT, plus a control that stays green. TWO CAUGHT REAL WEAKNESSES IN THE FIRST DRAFT. Reverting `in` to a truthiness test was NOT caught at first: the fall-through only has somewhere to fall when the tool id ALSO names a group in toolTree.js, and all four rows the test checked were on Create, which has no such group — the row that actually breaks is Discover’s Font Gallery, whose group desc is a 79-character sentence. And restoring the line clamp alone was inert, because nothing overflows any more; it proved nothing until run together with the over-long copy it used to hide, which is the pair that actually shipped. NOT DONE, ON PURPOSE: the six column heads and their hue-coded rules are exactly as #352 left them, and the three-column Discover layout is exactly as #340 left it. Re-cutting the columns is the vertical-emptiness fix and it would move the same data the mobile sheet reads, while 27 viewport combinations rest on the current shape.' },
  { id: 'handkept-tool-lists-remaining', title: 'Six more copies of the tool list, one of them user-visible and stale', priority: 'P1', effort: 'M', area: 'Design system', status: 'done', note: 'DONE across four PRs, one per copy, in the order the item asked for: #335 the locales, #337 the sitemap, #338 the two tables beside CREATE_GROUPS, #339 the bouncing links. Every one derives both sides and fails the build on divergence, and NO test lists the tools — a test that named them would be the fifth copy. THE LOCALES (#335): five tools.* blocks named tools that do not exist — imageConverter (renamed and rescoped to File Converter, which does video now), videoFrames, designReference, designExport and cadConverter, the last of which has never had a route in any table. Deleted from all ten files. CORRECTION TO THIS ITEM OWN CLAIM, worth recording because the item led with it: they were NOT user-visible today. #332 rebuilt the registry off toolTree.js and narrowed TOOL_I18N_MAP to ten ids, which stopped all five reaching a screen — verified by reading localiseWith and by grepping src, scripts, tests and api for zero readers. What they still were is a loaded gun: wiring file-converter to tools.imageConverter is the obvious next move and would have renamed a live tool after a retired one in nine languages. THE REAL LIVE LOCALE GAP, found while checking: the eight non-English locales carry FOUR of the eight categories blocks, so de, es, fr, it, ja, ko, pt and zh have no translation for categories.resources — one of only three category rows search can offer — and render it in English. Left omitted rather than machine-translated, because inventing nine translations of copy nobody wrote is worse than the English fallback the runtime already performs; a test pins that fallback so the omission is safe by construction. NEEDS A HUMAN TRANSLATOR, and that is the whole of it. THE SITEMAP (#337): now generated by scripts/sync-sitemap.mjs from the route matrix, with the advertised set derived from two rules — / is advertised though the matrix excludes it (the build serves dist/index.html directly), and everything else must be prerendered AND its own canonical, which is what keeps /home out. The 28 URLs are unchanged, verified by diffing the derived set against the hand-written one before replacing the file, and the prerender line is unchanged at 28 route shells. SECOND CORRECTION TO THIS ITEM: containment already DID catch a retired URL, because classifyRoute classifies retired routes out of the matrix. What nothing could catch was everything sitemapRoutes() throws away before an assertion sees it — the origin (an entry on http://uil4b.com passed every test in the repo), the / entry, and duplicates — plus the reverse of containment as a SET. All four are asserted now. priority, changefreq and lastmod are gone: Google ignores the first two, and ours lastmod was hand-typed and had not moved since 2026-08-22 while the pages behind it went through #325 and #332. There is no honest derived value — a git timestamp rewrites the file every commit, a build stamp claims every page changed today — so it is dropped rather than faked, and a test stops one being re-added. THE TWO TABLES (#338): HOME_SATELLITE_SPEC and DISCOVER_SPEC now carry presentation only and read route, hue and soon back out of the tree; an unknown tool id THROWS at import, which fails the build because prerender.mjs imports the module. Three satellite labels had already drifted (Semantic for Semantic Colour, Contrast for Contrast Checker, Gradient Generator for Gradient); they stay as explicit overrides because the chips are small, and a test fails an override that has become identical to the tree label. FOUND AND FLAGGED, NOT ACTIONED: the comment above HOME_SATELLITES said its offsets live in global.css as .hsat-item:nth-child() rules, and there are ZERO occurrences of hsat in global.css — the satellite field was removed from the hero and its CSS with it. The only field any consumer still reads is family. label, hue, icon and zone render nowhere; they are kept because deleting them is a different call from de-duplicating them and those surfaces are being restyled right now. THE BOUNCES (#339): the footer Imagery row pointed at /create/imagery while the row directly above it carried a comment explaining that exact hazard. FOUR MORE were found on the surface that matters more — Home.jsx linked every category head at group.home and four of the six only redirect. One function answers it now, categoryDestination(), restating CreateTool.jsx own rule once and checked against that branch read out of source. Five links lose a redirect; every one already ended at the same destination. Home.jsx FIVE LITERAL STEP-RAIL ROUTES are untouched and stay C9 of #264 in [homepage-parked-prs-triage]. ALSO: the comment stripper that decides whether every source-reading assertion can be trusted was a copy in search-index.test.js and was about to become a third — it is now tests/unit/helpers/source-text.js, with the fixture check travelling beside it so a new caller cannot copy the read without the guard. STILL OPEN, deliberately. The FOUNDER DECISION this item recorded is unchanged and untaken: seven documentation rows now share one destination, /learn, itself a coming-soon surface. Nothing changed for a visitor — that is where the 301 already sent them — but seven entries leading to one placeholder is a content call. And categories.documentation.description still reads "Design principles, marketing references, and external resources" while the registry has narrowed to "Design principles and marketing references"; the locale string is the one that paints, so it is real staleness, but it is translated into ten locales, which makes it a copy task rather than a code task — the same reason #306 declined the same job.' },
  { id: 'serif-token-is-not-a-serif', title: 'The --serif token resolves to Manrope', priority: 'P3', effort: 'S', area: 'Design system', status: 'done', note: 'DONE. Renamed --serif to --display; 28 call sites in global.css plus PillNav, FileConverter and SurfaceLanding. THE CALL, since the item asked for one: renamed rather than backed by a real serif. Design Language V2 budgets exactly two self-hosted families and the @font-face note at the top of global.css records what a third costs — a cold homepage load put the font at ~587ms and dirtied all 851 boxes in a 262ms full-document relayout. Adding a face is a founder call on the type system; making the name honest is not. --display is what the token has always been: Manrope at display sizes. Typographic contrast in this system comes from Manrope vs JetBrains Mono, plus weight and scale, and PRODUCT.md, css-conventions.md and design-language-v2.md now say that instead of describing a misnamed alias. CHANGELOG.md deliberately untouched — it is release history, not a live description. A SEPARATE DEFECT FOUND WHILE SWEEPING, not in this note and now fixed: /style-guide listed ALL THREE font families as “Outfit”, stale since the V2 migration. The admin-facing token reference was telling the truth about none of them. Now Manrope / Manrope / JetBrains Mono. NOT PINNED BY A TEST: nothing asserts the token name, and a rename is caught by the build only where a var() goes unresolved, which is silent in CSS. Verified instead by a zero-residual grep and by reading --display and a rendered heading off the live page — both Manrope.' },
  { id: 'homepage-parked-prs-triage', title: 'Decide the fate of the four homepage PRs parked since August', priority: 'P1', effort: 'M', area: 'Homepage', status: 'done', note: 'RE-TRIAGED AGAINST LIVE main ON 2026-09-04, BY REBASING RATHER THAN ESTIMATING, AND THREE OF THE PREVIOUS PASS’S CONCLUSIONS CHANGED. main had moved 35 commits past the 2026-09-03 pass, so every count below is measured against today, not carried forward. THE STACK IS NOW DISARMED, which was the precondition for everything else: all four PRs are retargeted to main, so no PR is based on another PR’s head any more and no branch deletion can auto-close a dependent. That hazard is gone rather than merely written down. #262 IS REBASED, GREEN AND PUSHED (head worktree-agent-ab3a27f2addfe9846, 2 files). Its defects all still reproduce on today’s main, measured in Chromium at 1440x900: the .hw-shell swing is 215.42px across the five tabs, and the --hi mark paints 130px inside a 108.34px clip, shearing 18px off the TOP and 3.66px off the bottom. After the rebase: swing 0.00px, mark 141.38px inside a 148.27px clip with ~3px clear on both edges. The true rebase cost was ONE conflicted file and FOUR hunks, not three: the docs commit 3e0c795 is already on main by another route and must be dropped (rebase with --onto, or the first replay conflicts on four docs files that are not the work). .hcmd-chips dropped (C7 moot, #320 deleted the row), .hw-tabs took main (#318’s rail doctrine) while keeping #262’s .hw-tab transition because it names border-color and the active tab changes its border, and .home-mark kept BOTH #293’s line-height:1 and C5’s padding-block:.06em, which are different bugs. The fourth hunk is the one the previous pass could not have seen: #341 landed the two-zone panel after it, so .hsteps-sticky is no longer a side-take but a genuine MERGE - #262’s outer frame over #341’s inner zoning, with #262’s .hw-panel{overflow-y:auto} DROPPED because under #341 the panel is not the scroller and letting it scroll re-admits the squeezed-stage defect #341 recorded. AND A COMPOSITION DEFECT NEITHER PR HAS ALONE, caught only because the gate was run rather than assumed: at #262’s original 640px frame bound the control zone collapses to 140px, 92px of which sits under #341’s STICKY hand-off, leaving 48px. Palette’s Generate button then renders permanently beneath the hand-off - the button occupied 743-783, .hw-foot occupied 690.6-783, and document.elementFromPoint at the button’s own centre returned the foot’s note - and FOUR tests in 10-home-chaos-to-calm.spec.js fail on that click. The bound is raised to 760px, the smallest value that leaves every mode at least the control room main’s cap gives today. The swing stays 0.00px, because the swing was never killed by the bound being small, only by height being mode-INDEPENDENT. Gate after: lint 0 errors, build 28 shells, unit 1059/0/0, users 463 passed 0 failed 13 skipped. #269’s C2 IS EXTRACTED AND OPEN AS #363, gated green (unit 1064, users 467). It is a clean 2-file change on main: only the commit’s carried edit to 15-discover-library-parity.spec.js conflicted, and that hunk is superseded - it targets .pgal-use and the pre-migration colour-prefixed palette route (not written out here: redirects.test.js fails the build on any shipped file that names a retired Create URL, and it caught this note doing exactly that), so taking it would have been a route regression. The test is MUTATION-VERIFIED: reverting the trigger to top 55% fails 2 of its 6 assertions. Verified in the browser, the swap now fires with the step box centre at 46.6-51.3% of viewport height against roughly 86% before. Nothing else in #269 survives, so #269 is now empty and can be closed. #264 IS CONFIRMED UNSALVAGEABLE AS A BRANCH, with evidence rather than advice. Of its three commits, 067c827 is already on main (add/add on tests/unit/surface-vocabulary.test.js); 32d237d conflicts on 2 Home.jsx hunks; 551a3ef conflicts on 9 MORE Home.jsx hunks, 1 in PaletteGalleryGrid.jsx, and a MODIFY/DELETE on src/components/TopBar.jsx, which no longer exists on main at all. That last one also kills half of C12 twice over: buildTailwindTheme appears nowhere in src on main either, so there is nothing left to hoist out of nothing. Re-implement the survivors against main, one commit each; do not rebase. C11 and C9 are now their own items. #270’S STAGE 2 costs 5 conflicted files and 17 hunks today, up from 11 in August. Its three new files - HomeSpecimen.jsx, heroSpecimen.js, hero-specimen.test.js - all apply CLEAN, and every dependency resolves on main (grade in styleGuideExport.js, the three colors.js helpers) except HOME_CURATED, whose module src/data/homeGallery.js does not exist on main; GALLERY_PALETTES does, so the decoupling is a few lines. THE GATING CLAIM IS WRONG, and this is the correction that matters most, because it was the stated reason to wait. The previous pass said direction A has no room for the band. Measured at 1440x900: A has 183.9px of empty flow between the bar and its bottom-pinned reassurance line, B has 256.6px below its CTA row and C has 253.2px. At 390x844 the figures are 480.2px for A and 378.1px for C, with zero horizontal overflow. The band’s budget is 96-144px on desktop and 240px on phone, so it FITS IN ALL THREE, on both form factors. The band should still be designed into the chosen shape rather than bolted on, but that is a sequencing preference, not a blocker, and it must stop being recorded as one. NET: #262 revived, rebased and green; C2 shipped as #363; #269 now empty and closable; #264 and #270 to close once their survivors are queued as the separate items now filed. Nothing merged, nothing closed, no branch deleted by this pass. SALVAGE PASS 2026-09-06 AGAINST main AT 34f6d5a, AND THIS PASS’S CLOSING CLAIM WAS WRONG: it said "nothing else in #269 survives, so #269 is now empty and can be closed". One founder item had NOT landed. #269 was not rebased — 144 commits behind, 10 conflicting files, and HomeWorkbench.jsx rebuilt underneath it — so every item was re-verified against main one at a time and only the survivor was re-implemented. THREE OF THE FOUR WERE ALREADY SOLVED, each by a different PR, and each was re-MEASURED rather than taken on trust. C2 shipped as #363: STEP_SWAP {down:52,up:44} is on main keyed to `center`, guarded by tests/unit/home-step-swap.test.js. C4 shipped as #341, and the defect #269 recorded is gone: measured in Chromium at 1440x900 with the sticky column actually pinned (shell top 104, bottom 864, inside a 900px viewport, so an overflow there would be real), settling on geometry stability across six consecutive rAF frames with no finite animation running rather than on a stopwatch, .hw-panel and .hw-stage both overflow 0px in ALL FIVE modes and no stage descendant paints past the shell. image overflowed the frame by 118px in #269 and now overflows by 0px. The clipping chain reads hw-controls[auto] then hw-panel[hidden] then hw-shell[hidden] in every mode, so .hw-controls is the ONLY scroller in the ancestor chain — the C4 contract exactly. The controls drawer still scrolls in three modes (palette +170px, image +177px, typography +101px), which is the residue #269 itself flagged as a product decision. THE FIFTH DEFECT — the index.html bootstrap writing a fabricated data-reduced-motion="false" for every visitor who had never opened Settings — shipped as #273, in BOTH writers, guarded by tests/unit/reduced-motion-resolution.test.js. The separate .pgal-actions a11y defect is solved too, by supersession rather than by fix: .pgal-actions no longer exists, the layer is the shared .lbry-card-actions, and that block deliberately keeps `visibility` OUT of its transition list and says so in a comment, which is precisely the root cause #269 diagnosed. C6 IS THE ONE THAT DID NOT LAND, and it is now open as #397. The founder asked for "one pass … then it stops. No loop." main loops forever: the word getter read hints[index % hints.length], so the term after the last is the first again for as long as the tab is open. MEASURED on the shipped build at 1440x900 with motion on, 75s dwell sampling the placeholder attribute — 13 terms, one pass in ~48s, "Palette" typed a second time at 48.2s, still moving at 75s having repeated 8 terms. After: 0 repeats, the pass ends at ~45s, and the field returns to the resting sentence that still names real tools. Three changes, one item: PASSES bounds the run so index runs off the end of the list and the pass ends there; the stop became a one-way LATCH rather than derived state, because derived state made the stop last exactly as long as focus did (click in, click out, and the demo typed over you from the first term again); and the ⌘K control’s accessible name now says it stops the demo while there is a demo to stop and reverts after, which closes the WCAG 2.2.2 findability gap #269 recorded as residual — that button always WAS the stop, but its name only mentioned the focusing. DELIBERATELY NOT SALVAGED: #269’s term list and its overlay-span technique are superseded by the founder’s later 2026-09-03 decision, which moved the nav animation here and derives the terms from the registry so the bar can never advertise a tool it cannot find; re-implementing #269’s version would have regressed that, and 10-home-chaos-to-calm.spec.js already types every advertised term into the real bar and requires a hit. Only the pacing contract was taken. FLAGGED, NOT ACTIONED: one pass over the registry-derived list is ~45s against the ~14s #269 designed for four hand-picked terms, and shortening it means choosing which tools stop being advertised, which is a product call. MOBBIN re-searched 2026-09-06 and it still returns no evidence for any pacing value — the corpus is screenshots, so it can show that example-query placeholders are common and structurally cannot show whether one animates, at what pace, or whether it loops; every constant stays labelled judgement. All three new tests are MUTATION-VERIFIED against the real page: total=Infinity restores the loop and fails "still typing after 90s"; removing the latch from onFocus fails with the demo restarting at "Palette"; a static sr-only name fails the ⌘K assertion. Gate on #397: lint 0 errors 28 warnings, build 33 route shells, unit 1356/1356, users 606 passed 13 skipped 0 failed, all skips in 12-ui-system-builder.spec.js. #269 IS STILL OPEN AND UNMERGED and this pass did not close it; it is now genuinely empty and the founder’s to close. — — — 2026-09-06, #264’S C8–C13 BATCH RE-EXAMINED AGAINST TODAY’S main, AND THE PREMISE OF THE SALVAGE DID NOT SURVIVE THE READING. THE PROVENANCE IS THE HEADLINE FINDING, because it changes who is allowed to decide the rest: the C8/C10/C11/C12/C13 headline sentences are NOT the founder’s words. Every one of them is an agent-authored “Option 1 — RECOMMENDED” in docs/design/homepage-spec-2026-08.md, offered against a two-option menu with a reasoned pick. What IS verbatim founder in that file is the COMPLAINTS — “i dont like ‘Not a screenshot. The actual tools, running here.’ this just sounds stupid” (§3.5), “‘Six categories. One account.’ … this section makes me feel like skipping over it” (§5), “‘Systems worth stealing.’ is bad copy. this whole section is to showcase the community gallery so pivot towards that” (§6) — plus the one-line section briefs for C12 and C13. NO FOUNDER PICK between Option 1 and Option 2 is recorded anywhere, for any of the five. So restoring one of these lines is not returning founder copy to the page; it is choosing one agent’s sentence over another agent’s, on the surface with the worst record for exactly that. Home.jsx WAS NOT CHANGED BY THIS PASS, and that is the finding rather than a shortfall. LINE BY LINE. C11 “Start from something that already works.” IS ON main AND CORRECTLY PLACED — section.hcomm, six cards, every one a relative link into /create/palette or /create/gradient with the artefact’s values already on the URL, which is the whole point of C11. Its “[ DISCOVER ]” eyebrow is gone, removed along with all four bracketed eyebrows by the later pass that judged a uniformly applied motif to be decoration; the sentence itself is untouched. C8 “Use the tools here, then take the values with you.” HAS A LIVE INCUMBENT, AND IT IS NOT #394’s. #394 never touched #hsteps-title — it rewrote the hero h1 and #htools-title — so the competitor is #379, one day old: “Five tools. Each one hands your work to the full tool.”, written AFTER the founder’s “MEGA AI generated this is horrible copy” and substantiated on screen by the .hsteps-ticks row #379 added for that numeral. Adopting C8 removes the numeral and leaves that row’s stated justification stale (52-compressed-landings.spec.js:173 carries it as a comment; no assertion reads the word, so nothing fails). RECOMMEND KEEPING #379’s until he picks, because it is the only version of that slot written with his rejection in hand. C10 “A value you set in one tool is set in all of them.” IS THE SAME SHAPE AGAINST #394, whose “Colour, type, icons, imagery, export.” executes P-019 rule R. WORTH STATING FOR THE DECISION: C10’s sentence carries no “token” either, so it satisfies P-019 as well — these two are not a rule conflict but the same argument the founder already made once, since his complaint about this section was precisely that an inventory heading reads as skippable, and §5 specified swapping heading and lede. COST IF C10 IS ADOPTED: the h1↔#htools-title wiring assertion #394 added (10-home-chaos-to-calm.spec.js:265 — the h1 must name at least two of the families the tools heading lists) drops to one shared word and must be re-pointed rather than deleted. RECOMMEND KEEPING #394’s until he picks. C12 AND C13 HAVE NOWHERE TO GO. Today’s homepage is hero → hsteps → htools → hcomm → hprice → SystemCTA: there is no export section and no Learn section on it. HomeExport.jsx and HomeGallery.jsx never reached main and do not exist; src contains zero occurrences of hexp- and zero of hlearn. Half of C12’s panel was already recorded dead above — buildTailwindTheme was to be hoisted out of TopBar.jsx, which is deleted. Building either section is a design change, not a copy salvage, and was not done; manufacturing a section to have somewhere to put a line is the failure mode this item exists to avoid. C13’s idea already has a home on another surface — /learn’s own landing heads on “Understand the craft, not just the tools.” — and displacing live copy there is not the placement C13 was written for. WHAT DID SHIP IS THE HALF OF #264 THAT NEEDS NO FOUNDER: tests/user-sim/56-founder-rejected-headlines.spec.js. #264 claimed “the three rejected headlines cannot return” and that test never landed; in the year since, #hsteps-title went through THREE versions, two rejected by name, with nothing in the repo able to stop a fourth attempt reusing a retired sentence. Four strings are pinned OUT, each one reproduced inside the founder’s own complaint, and NO heading is pinned IN — all three live slots are open copy decisions and the file deliberately decides none of them. EVERY ABSENCE IS PAIRED WITH A PRESENCE, because this page has form: the h1 assertion the suite carried until #394 was satisfied by a word that never varied. MEASURED WHILE WRITING IT: .hsteps-head carries data-reveal, which is opacity:0 until the section scrolls in (global.css:5904), and #hsteps-title reports HIDDEN on a freshly loaded homepage — so an innerText rule would have been evadable by the worst possible means, putting a retired headline back below the fold. The rule reads textContent for that reason, with the note beside it explaining why the P-019 “token” check next door correctly does the opposite. The C11 lock goes through the section’s ACCESSIBLE NAME rather than the h2’s text: the suite checked that strip’s data thoroughly and never once checked that the section still has the heading that names it, so the heading could have been reworded or deleted outright and every homepage test would have stayed green. SEVEN MUTATIONS APPLIED, SEVEN CAUGHT, each rebuilt and re-run: the four retired sentences restored one at a time (all four caught, and the hcomm one fails BOTH tests); #hsteps-title deleted rather than reworded (caught by the presence control, which is what stops deletion being the cheapest way green); aria-labelledby dropped from section.hcomm with the heading left intact (caught by the accessible-name wiring, and by no text assertion); and the starting-points grid rendered empty under an intact heading (caught by the region’s own positive control). #264 IS NOT CLOSED and #394 IS NOT REOPENED — the two copy decisions above are the founder’s, and both are one-line, fully reversible changes whichever way he calls them. — — — 2026-09-06, #270 SALVAGED AGAINST main AT 8dd3bac, AND IT IS THE EMPTIEST OF THE THREE: OF THIRTEEN ITEMS, NINE WERE ALREADY SOLVED OR SUPERSEDED, THREE ARE BLOCKED ON AN OPEN FOUNDER DECISION, AND ONE SURVIVED. Not rebased — 39 conflicting files, 144 commits behind, and it conflicts with src/pages/Landing.jsx, which no longer exists. Every verdict below was MEASURED on the rendered production build at 1440x900, not read out of the source. THE FIRST CORRECTION IS TO THIS ITEM’S OWN EARLIER PASS: "#270’S STAGE 2 costs 5 conflicted files and 17 hunks today" was written on 2026-09-04 and is now 39 files; and its "THE GATING CLAIM IS WRONG … the band FITS IN ALL THREE" still holds geometrically but is moot, because the band is blocked on the shape pick rather than on room. STAGE 1 — THE WHOLE INTEGRATION OF #257/#262/#264/#266 — IS ON main ALREADY. legacyRoutes.js and redirects.test.js both exist and vercel.json carries 88 redirect sources; #262’s .28em clip derivation is at global.css .home-hero-line with its reasoning comment intact and tests/unit/hero-entrance.test.js beside it; and Plans.jsx resolves SystemCTA exactly the way #270 resolved the conflict, primaryLabel "Open Create" with primaryTo /create/color. Nothing to rebuild. STAGE 2, ITEM BY ITEM. THE STAT LINE IS ALREADY DELETED AND GUARDED — measured 0 .home-hero-stats on the rendered page, and 04-premium-home.spec.js:70 and 10-home-chaos-to-calm.spec.js:346 both assert count 0 with the message "the hero stat strip is back". THE MARKER PEN ALREADY SITS ON THE CLAIM rather than on a widget 200px below it: the h1 reads "Colour, type and icons that stay one system." with the mark on "one system". THE DECORATIVE GROUND IS GONE, so the .46→.32 opacity change has no subject. THE CHIP THAT RETURNED NOTHING IS SUPERSEDED, AND main’S ANSWER IS STRICTLY BETTER THAN #270’S: #270 swapped one hard-coded chip string (convert → palette) inside a hand-written CHIPS array; main DELETED the array — measured 0 chips on the page — moved its job to a registry-derived typing placeholder (measured: "Search tools — Palette, Semantic Colour, Tint…"), and made the rule unevadable in tests/unit/search-index.test.js, which requires every advertised hint to find itself AND requires searchHints to refuse a workshop tool even when the caller forgets to filter. #270’s derived "Search 13 live tools" placeholder is superseded by the same change. THE ONE APP-WIDE DEFECT #270 FOUND AND DELIBERATELY LEFT UNFIXED — index.html stamping a fabricated data-reduced-motion="false" on every visitor who had never opened Settings, which by contract BEATS the OS query and so killed every html:not([data-reduced-motion="false"]) block in the sheet — SHIPPED AS #273 IN BOTH WRITERS and is recorded above in the #269 pass. RE-MEASURED HERE END TO END rather than taken from that record: with the OS emulating prefers-reduced-motion:reduce and nothing in localStorage, the attribute reads "true" at DOMContentLoaded AND after React commits, and .home-hero-line-in computes animation-name:none; with no-preference it reads "false" and home-hero-clip-up runs. The guard fires. THE HEADLINE IS THE #264 TRAP AGAIN AND WAS NOT RESTORED. "A UI system that survives the [handoff]." is Option A of two in docs/design/anti-slop-and-hero-2026-08.md §2.4, labelled "recommended" by the agent that wrote it; that document’s OWN §2.12 lists it first under "Open decisions — founder judgement required"; docs/PROPOSALS.md:216 records the recommendation and no verdict; and there is no founder pick anywhere in the repo. It has also been overtaken twice — #290 rewrote the words inside this hero and the complaint survived it, and #394 changed one word under P-019 — and the hero SHAPE is now the live open question owned by [hero-copy-still-reads-ai]. THREE ITEMS ARE BLOCKED ON THAT PICK AND WERE LEFT: the specimen band (HomeSpecimen.jsx / heroSpecimen.js, which is the whole point of the PR’s title), the .hspec-open 24x24 target-size fix, and the .hstep-cta locator scoping — the last two have no subject on main because the band that introduced them was never built. WHAT SHIPPED IS ONE TEST AND ONE PROPERTY. (1) THE MARKER-PEN BUDGET, in 10-home-chaos-to-calm.spec.js beside the mark assertions. design-language-v2.md says "Budget: at most one --hi element per viewport" and NOTHING enforced it. The existing check counts .home-mark INSIDE the h1, so its subject is the headline; a second lime element anywhere else in the hero is invisible to it. PROVEN, NOT ASSUMED: adding ui-pill-hi to the hero’s "See it working" pill puts a second --hi element on the first screen and ALL 39 TESTS IN THAT FILE PASS, the mark check included. The new assertion resolves --hi at runtime and compares PAINTED colour over the .home-hero subtree, so it is not a class list to maintain — a new class that paints the marker colour is caught, and renaming .home-mark cannot create a false pass. MUTATION-VERIFIED BOTH WAYS, each rebuilt and re-run: ui-pill-hi on the hero CTA fails with "paints --hi on 2 element(s): mark.home-mark, a.ui-pill.ui-pill-hi.ui-pill-lg"; pointing .home-mark’s background at --bg-3 fails with 0, so the check cannot be satisfied by a hero that has lost its mark. (2) .hero-c-h1 LOST opacity:.45, AND THAT IS THE ONLY THING TOUCHED IN THE HERO-DIRECTION HARNESS. The founder is comparing three SHAPES at ?hero=a|b|c and C was the only one rendering its headline at less than full strength — measured .hero-a-h1 and .hero-b-h1 at opacity 1 against .hero-c-h1 at 0.45. The dimming was there to say "placeholder", which .hero-c-placeholder-flag already says in words in --accent-strong directly above it (measured present, reading "FOUNDER WRITES THIS LINE" in rgb(11,94,215)), so it carried no information and cost the comparison its fairness. It was also a contrast failure: --t0 at .45 over --bg-0 composites to ~2.99:1, under the 3:1 large-text floor, against 16.6:1 at full strength. NO VARIANT WAS IMPLEMENTED, CHOSEN OR PRE-EMPTED — the decision stays entirely open and this makes it fair, it does not make it. #270 IS NOT CLOSED and neither is #269 or #264. — — — 2026-09-10, THE CLOSING SALVAGE PASS AGAINST main AT f0087823, RUN ON THE FOUNDER DECISION TO CLOSE ALL THREE AND SALVAGE FIRST. THREE ITEMS SURVIVED, AND ALL THREE ARE THE SAME SHAPE: A CONTRACT THAT IS ALREADY TRUE ON main AND THAT NOTHING ENFORCES. That shape is what the earlier passes kept handing forward without picking up — each of them correctly recorded a defect as fixed or superseded and then moved on, leaving the fix resting on a comment. Every verdict below was re-checked against today’s files rather than carried from the notes above. (1) THE DISCRETE-PROPERTY RACE #269 DIAGNOSED IS NOW GUARDED, AND #269’S OWN RULE WOULD NOT HAVE GUARDED IT — tests/unit/focus-reveal-layers.test.js. The fix is on main and the pass above recorded it: .pgal-actions is gone, the layer is the shared .lbry-card-actions, and that block deliberately keeps visibility out of its transition list. What the block also says is "Read it before changing the reveal on any library card", and a comment cannot fail a build. #269’s test looked for the hide and the transition INSIDE ONE DECLARATION BLOCK; today they live apart — the transition on the base rule, visibility:hidden inside @media(hover:hover) — so #269’s rule pasted onto main passes today AND KEEPS PASSING AFTER THE REGRESSION. The rule now gathers the discrete properties PER TARGET across every rule that applies to it, and it reads ALL_CSS rather than global.css so a lift into src/styles/pages cannot make it vacuous. FOUR MUTATIONS AT THE CALL SITE IN global.css, FOUR CAUGHT, each reverted byte-exact and proven with git diff: visibility added to the base transition list; visibility:hidden dropped from the hover:hover block; the :focus-within half of the reveal selector deleted; position:static dropped from the (hover:none) fallback. The second mutation also CORRECTED THE TEST — folding the CONTEXT hide .plb-galpopup-body .lbry-card-actions{display:none} into "hidden at rest" made the vacuity control pass with the real hide-at-rest deleted. Found by mutating, not by reading. (2) THE FRONT PAGE COULD HAND A PRO-GATED BRAND SYSTEM TO THE BUILDER, AND NOW CANNOT — three tests appended to tests/unit/home-starters.test.js. #264’s C11 wrote this rule against a 170-item homepage gallery that never reached main, and the 2026-09-06 pass closed C11 as already correctly placed, which it is as far as the six cards that ship. THE GAP IS THE POOL: HOME_STARTER_SPEC resolves palette ids against LIBRARY_PALETTES, which is CURATED_LIBRARY_PALETTES PLUS BRAND_LIBRARY_PALETTES — 101 entries, 30 of them pro:true. The harm is not that a locked card would appear; it is that the card would NOT be locked. paletteBuilderUrl puts every hex on the query string and /create/palette loads ?c= with no entitlement check at all — the gate is pickBrand, and ?c= does not go through it. MEASURED RATHER THAN ARGUED: with { kind: palette, id: brand-slack } in the spec, ALL SIX PRE-EXISTING ASSERTIONS IN THAT FILE STAY GREEN (the artefact is real, the link is inward, the spec names no colour and no URL) and only the new rule catches it. Two mutations, two caught: the Pro brand id in the spec; and the pickBrand gate neutered in PaletteBuilder.jsx, which fails the premise test, so the rule cannot outlive its reason. (3) THE STICKY PANEL’S TWO-ZONE CONTRACT IS GUARDED — tests/unit/workbench-zones.test.js. C4 shipped as #341 and was measured then; the pass above re-measured it and left it there. It has since come within one merge of being undone TWICE, and both near-misses are recorded in this very note: #262 carried .hw-panel{overflow-y:auto} and it had to be DROPPED BY HAND when #262 was rebased onto #341; and #341’s own first attempt let the stage flex down, passing every geometry check while the type ladder had quietly lost two of its four sizes. That second one is why this file asserts the stage’s FLEX BASIS and not only overflow — a preview that silently drops content is invisible to any check that asks whether anything overflows. The mode count is derived from HOME_WORKBENCH_TABS rather than typed. FIVE MUTATIONS, FIVE CAUGHT: panel overflow-y:auto; controls overflow-y:hidden; stage flex:1 1 auto; shell overflow:hidden removed; one mode’s .hw-stage renamed. NOT TAKEN from #269: its control-vocabulary assertions (option rails against property rows, inverted active tiles, sRGB hex field labels), because HomeWorkbench.jsx was rebuilt underneath that branch and those are live design decisions on a surface the founder is still revising — pinning them here would freeze someone else’s open question. NOTHING ELSE SURVIVED, AND THESE ARE THE RE-CHECKS THAT PRODUCED THAT ANSWER. #264: paletteCss has exactly ONE consumer on main (PaletteGalleryGrid), so hoisting it to paletteGallery.js would CREATE the second surface it exists to keep in step rather than reconcile two; PALETTE_LABELS has no second consumer either; buildTailwindTheme is dead twice over, because TopBar.jsx is deleted AND exportFormats.js now carries tailwind as live:false with unbuiltFormats() naming it, which is a stronger answer than hoisting the writer would have been; and the explicit .js extension on the colors import is already in exportBuilder.js. #269: the four founder items and the fifth defect are all on main (#363, #341, #397, #273) and .pgal-actions is superseded. Its docs/build-plan/founder-batch-2026-08-20.md and docs/qa/responsive-audit-2026-08.md were NOT salvaged: an August responsive audit landed against a repo that has since had #405 to #437 on it would be a stale document presented as a current one. #270: stage 1 is main; the stat line, the marker pen, the decorative ground, the chip, the reduced-motion bootstrap and the fold contract are all landed and guarded — 10-home-chaos-to-calm.spec.js already asserts the primary CTA above the fold at the contract viewports, which IS #270’s vertical-budget measurement in test form. The specimen band, the .hspec-open 24x24 fix and the .hstep-cta scoping stay blocked on the hero shape, and .hstep-cta has exactly one user on main so the scoping has no subject either way. ONE THING FLAGGED AND DELIBERATELY NOT FIXED, BECAUSE IT IS AN ENTITLEMENT CLAIM. The one piece of #264/#269 doc work that DID land has since gone false. README.md and docs/reference/positioning.md both say every format is available on the free tier with a visible footer credit and Pro removes the credit. That was true and founder-approved on 2026-08-20. It is not true today: exportFormats.js marks book and guidelines pro:true, proOnlyFormats() returns both, and the homepage pricing panel PRINTS them as what Pro adds (Home.jsx PRO_INCLUDES). Two shipped surfaces now describe the export entitlement two ways. Correcting a sentence about what Pro sells is the founder’s call, and it is one line in each file. GATE ON chore/salvage-parked-homepage-prs: lint 0 errors 25 warnings; build wrote 39 route shells plus a noindex 404 shell; unit 1771 passed 0 failed; users 962 passed 13 skipped 0 failed, every skip the whole of 12-ui-system-builder.spec.js. ELEVEN MUTATIONS APPLIED, ELEVEN CAUGHT, ZERO MISSED. #270, #269 AND #264 ARE ALL STILL OPEN AND UNMERGED — this pass closed none of them; the founder’s decision closes them once this lands.' },
  { id: 'homepage-community-points-outward', title: 'The homepage community strip links to four competitors and prints a zero metric', priority: 'P1', effort: 'M', area: 'Homepage', status: 'done', note: 'DONE for the two harms in the title, and SCOPED DELIBERATELY. The homepage strip no longer renders COMMUNITY_DESIGNS. It renders six real in-product artefacts — gradients from GALLERY_GRADIENTS and palettes from LIBRARY_PALETTES — each linking INWARD through the galleries own hand-offs, gradientToolUrl() and paletteBuilderUrl(), so the tool opens with the values already loaded and there is no second encoding of a tool URL. Zero outbound links and zero external hosts in the section, verified in a browser. The card art is the artefact ITSELF now (a real gradient, or a palette as hard-stop bands) rather than a decorative two-stop blend standing in for something that lived elsewhere. The {design.saves} saves count is gone; the line under each card says what the artefact IS and where it opens, and the only numbers the section prints are catalogue sizes it can count (100 gradients, 101 palettes). THE COLLISION THIS ITEM WARNED ABOUT IS NOT REAL AT THE FILE LEVEL. feat/community-rebuild has a 2026-08-16 merge base and touches exactly five files: src/utils/communityIdentity.js, communityQueue.js, communityTrending.js and their two unit tests. It has no committed or uncommitted edits to Home.jsx or communityDesigns.js — checked by diffing its on-disk worktree against its own branch tip, where the only difference was line endings. It IS building the community logic layer, though, so the substantive overlap is real even though the file overlap is not, and this change stays clear of it: nothing here reads src/utils/community*. WHAT WAS DELIBERATELY LEFT. communityDesigns.js is untouched, so /community still seeds from it, still marks every curated row as curated, and community-seed.test.js still passes unchanged. The curated outbound links are defensible on that interior page, which can honestly say until real submissions exist; they were not defensible on the front page. REMOVED AND WORTH A SECOND OPINION: the Trending / Newest / Most saved tablist went with the old data. Two of its three modes had no signal to sort on and a note underneath said so. A control that cannot do the thing it names is worse than no control, but this is a visible feature removal on the founder’s most-watched surface, so flag it rather than assume it. When the community backend lands, HOME_STARTERS is the single place that would change to make this a real feed again. Design references (Mobbin): Framer community resources for a grid whose caption is what the artefact IS rather than a vanity count, and Mural / Discord colour grids for the swatch-as-card-art treatment. Mutation-verified 4/4: a competitor link restored, a starter id pointed at a nonexistent gradient, a saves binding restored, and a hand-built hand-off URL were all caught.' },
  { id: 'home-step-rail-route-literals', title: 'Five step-rail routes are still typed by hand on the homepage', priority: 'P2', effort: 'S', area: 'Homepage', status: 'done', note: 'DONE. The five literal to:/create/... strings are gone from Home.jsx’s STEPS; each row names a TOOL ID and toolRoute() reads the route back out of CREATE_GROUPS. All five destinations are byte-identical to the literals they replaced, verified by asserting the derived list equals them, so this is a de-duplication and not a routing change. CORRECTION TO THE STEER THIS ITEM WAS GIVEN: reuse categoryDestination(). That would have been WRONG for four of the five, and silently so. categoryDestination() answers where a link to a CATEGORY HEAD should land; the step rail points at specific tools. The Colour group resolves to /create/color (its home renders, so it does not bounce) rather than /create/palette or /create/gradient, and the Type group resolves to /create/font-gallery rather than /create/type-scale. Only Icons coincides. It would have produced five valid URLs, four of them quietly moved — which is why toolRoute() states the tool-level rule once instead of borrowing the category-level one next to it, and why one of the two tests asserts equality with the old literals rather than merely that the routes resolve. Unknown ids throw at import, which fails the build because prerender.mjs imports the module. Mutation-verified 4/4: a route literal put back, a step pointed at the wrong live tool, toolRoute() re-derived through categoryDestination(), and a step pointed at a soon tool were all caught.' },
  { id: 'workbench-handoff-overlays-controls', title: 'The workbench hand-off is sticky inside the zone it can cover', priority: 'P2', effort: 'S', area: 'Homepage', status: 'done', note: 'DONE, by the second of the two fixes this item named — the foot is out of the scroller, not given more room inside it. .hw-controls scrolls and .hw-foot is now its SIBLING below it in .hw-body, in all five modes, so there is no scroll position at which the two can overlap and the class is retired rather than moved. THIS ITEM’S OWN CLAIM THAT IT NO LONGER REPRODUCED WAS OUT OF DATE, and that is the finding worth keeping. Measured in a browser on main at 1440x900, against #262’s 760px frame and before any change: Typography’s Continue with this live scale occupied 790.2-844.2 with the foot top at 784, and document.elementFromPoint at the link centre returned hw-foot-note — a live unreachable control on the front page, not a theoretical one. Image’s compression segmented control spanned 715.4-755.4 against a foot top of 750.6. The note said Image was 304px visible against a 92px foot; the zone is 257px now, which is how the margin was spent. COST, MEASURED MODE BY MODE at 1280x660: the control zone loses 12px (the flex gap the sticky rule used to overlap), and usable control height is otherwise unchanged. FOUND WHILE MEASURING, NOT ACTIONED, AND IT IS THE BIGGER ONE: at 1280x660 the control zone collapses to 0px in Palette and 2.2px in Image, BEFORE and AFTER this change equally. The stage takes its natural height and never shrinks (deliberately, per #341), so at a 528px frame chrome + tabs + a 353px stage + the foot consume the whole shell and the controls absorb a negative remainder. Generate is unreachable at that viewport on main today. That is the frame-versus-stage budget #262 tuned, not the overlay class, so it is left alone here rather than fixed by a number nobody measured. It wants its own item. Two guards: tests/unit/home-handoff.test.js reads the JSX and fails the build in milliseconds if a foot is nested back inside a zone, and tests/user-sim/49-workbench-handoff-clearance.spec.js asserts the rendered invariant — foot and scrollport do not intersect, in every mode. Structural on purpose: the button is clickable at 1440x900 is one instance of the class and passes again the moment a bound is raised, which is how this got closed once already. Mutation-verified 2/2 (a foot nested back in fails the unit guard; the full pre-fix structure fails the spec) — and note that a user-sim mutation needs a REBUILD to take effect, because playwright serves dist and the first run passed against a stale build.' },
  { id: 'suite-flake-class-unreproduced', title: 'Two more full-suite flakes were seen the same day and neither was reproduced', priority: 'P2', effort: 'M', area: 'Testing', status: 'done', note: 'Filed 2026-09-04 while closing [home-generate-loop-flake], which fixed ONE instance of this and should not be read as having closed the class. TWO further full-suite flakes were reported the same day and neither was reproduced here: a Font Gallery h1, and a mobile-ramp spec. Each passed in isolation and on re-run, and each failed once under full-suite load with four parallel workers. NOTHING IS KNOWN ABOUT THEIR MECHANISM - resource contention is the obvious hypothesis and it is only a hypothesis, because it was never tested for these two. WHAT WORKED ON 8c AND SHOULD BE TRIED FIRST: stop waiting for the flake and make it deterministic with Chromium Emulation.setCPUThrottlingRate through a CDP session - page.context().newCDPSession(page) then Emulation.setCPUThrottlingRate. 8c timed out reliably at 10x and passed at 1x, which turned an intermittent report into a before/after measurement. Pick the rate by bisection and CHECK THE FAILURE LANDS WHERE THE REPORT SAID: at 16x, 8c busts on navigation instead of on its loop, which would have measured the wrong thing entirely. A second lever, better for anything that looks like a load or hydration problem, is holding the lazy route chunks back with page.route while leaving the entry bundle alone - that reproduced the readiness class in [raw-goto-bypasses-readiness] with no CPU throttle at all. AND CHECK THE ASSERTION CAN FAIL BEFORE BLAMING THE MACHINE. Three separate walks in this suite reported violations while never reporting how many nodes they examined, so an empty page was a clean run - 39-accent-contrast’s INK_WALK and 8c’s worstOf() were both fixed in that pass, and 43-state-token-contrast had the guard already. A spec that measures nothing looks exactly like a spec that found nothing wrong. ---- 2026-09-05, PARTIAL. The MECHANISM is now reproducible on demand, but NONE of the six named tests was reproduced individually and it is NOT established that this mechanism caused the original reports. WHAT REPRODUCES IT: rebuilding dist/ DURING a run. `vite build` empties dist/ before refilling it and `vite preview` serves it live, so a second `npm run build` — or a second `npm run test:users`, which starts with one — against the same checkout deletes the content-hashed chunks the running workers are mid-fetch on. Measured on the six spec files named above: 130 passed / 0 failed against a stable dist/, and 114 passed / 16 failed with a rebuild loop underneath, the 16 sprayed across 10-home-chaos-to-calm, 11-typography-tools and 24-mobile-overhaul — three unrelated subsystems, one symptom, which is the pattern this item was filed about. Two shapes, both seen: element(s) not found on a page go() considered ready (a partial load — shell mounted, a lazy sub-chunk 404ed), and never got past its lazy-loading fallback with mounted=false bodyChars=0 (the entry bundle itself 404ed). WHAT DOES NOT REPRODUCE IT, both tried first on the Font Gallery h1 exactly as this note prescribed: CPU throttling through a CDP session at 1/2/4/6/8/10/16/20x leaves the h1 present and visible at every rate, and holding the lazy chunk back with page.route at 0-9000ms shows ready() waiting correctly with readyMs tracking the delay exactly. So the readiness door in helpers.js is SOUND for this route, and the flake is neither a CPU-scheduling nor a chunk-latency problem. Harness kept runnable at tests/flakeprobe-a0e1/. WHAT SHIPPED: base.js now records every /assets/ 4xx per browser context and the global teardown fails the run on any — including a GREEN run, because a rebuilt dist/ makes the passes as meaningless as the failures, which is this item’s own point. Verified by mutation both ways: silent on a clean run, and fires with 90 failures across 36 files under a rebuild loop. WHAT IS LEFT: whether the original twelve runs actually had a concurrent build was never instrumented and cannot be recovered now — the guard is the instrument that will answer it the NEXT time. The named tests stay unreproduced because the mechanism picks its victim by timing, so no one test is a reliable target. PREVENTION NOT DONE, deliberately: give each concurrent runner its own outDir keyed on PLAYWRIGHT_PORT (vite build --outDir, vite preview --outDir, and the dist constant in scripts/prerender.mjs) so two agents in one checkout cannot collide at all. SEPARATE LATENT ISSUE, unfixed: under CPU throttle /create/font-gallery settles into TWO states that ready() both calls ready — ownChars 1839 with the catalogue in, and 377 with FontCatalogLoading still up. ready() only knows about .page-loading and is blind to that second, page-level loading state. Harmless for the h1 and for every auto-waiting expect(locator) in 11-typography, but it is the same measure-the-chrome shape this file keeps being bitten by, and a spec that MEASURED the gallery would measure the skeleton. ---- 2026-09-06, THE MECHANISMS ARE NAMED, and they are recorded SEPARATELY because they are different faults wanting different fixes - reading them as one phenomenon is what kept this item saying NOTHING IS KNOWN ABOUT THEIR MECHANISM. (A) dist/ REBUILT UNDER THE RUN. Already covered above; a 4xx under /assets/, fails the run. (B) A BUILD ASSET THE MACHINE COULD NOT DELIVER, which the 4xx watch was SILENT on, because a request that fails this way produces NO HTTP RESPONSE AT ALL and the response listener never sees it. Five specs failed once each on 2026-09-06 under full-suite parallelism and every one passed in isolation - 09-auth-modal-accessibility, 23-responsive-mid-band, 24-mobile-overhaul, 25-defect-sweep, 11-palette-recovery - and TWO of those runs carried the cause in their own feedback-loop section: net::ERR_NO_BUFFER_SPACE against /assets/index-*.js and /assets/en-*.js. The app chunk never loaded, so the first locator found nothing and the failure READ AS IF THE ELEMENT HAD BEEN DELETED. It correlates with several agents running suites at once (40+ node processes). base.js now watches requestfailed as well as response, against an ALLOWLIST of codes meaning the machine or the connection could not deliver the file (ERR_NO_BUFFER_SPACE, ERR_INSUFFICIENT_RESOURCES, ERR_ADDRESS_IN_USE, the connection family) and deliberately NOT ERR_FAILED (route.abort()), ERR_ABORTED (a cancelled navigation), ERR_BLOCKED_BY_CLIENT or ERR_INTERNET_DISCONNECTED (setOffline) - all of which this suite produces on purpose, and a guard that cried wolf on them is a guard people stop reading. It fails the individual TEST as well as the run, so the naming lands where the symptom did. REPRODUCED IN THE WILD WHILE THIS WAS BEING WRITTEN, which is the evidence this item had been waiting for: a full run failed 24-mobile-overhaul - one of the five - with "/settings: the app never replaced the static boot shell in 20000ms - mounted=true booting=true fallback=false dataLoading=false bodyChars=19", plus "1 build asset request(s) NEVER ARRIVED in this browser context (net::ERR_ADDRESS_IN_USE /assets/en-CzS1NYuH.js)". That is ONE OF THE TWO FILES THE ORIGINAL REPORT NAMED, on one of the five named specs, failing as the app never started rather than as a missing Settings section. ERR_ADDRESS_IN_USE had been added to the allowlist an hour earlier on the evidence of a FONT failing the same way in an earlier run; without it that run would have gone quiet. (C) A TOOL THAT HAS ARRIVED AND HAS NO DATA - the SEPARATE LATENT ISSUE this note already predicted, and NOT a build-asset failure at all. 51-typography-paywall failed 2026-09-06 with "the gallery rendered no rows at all / Expected: > 10 / Received: 0", in a run with ZERO occurrences of ERR_NO_BUFFER_SPACE, on a PR whose diff touched no font-related file, passing 8 of 8 in isolation. MEASURED on /create/font-gallery: with /api/fonts answered ready() returns at 188ms with 24 rows; with it held, ready() returns at 148ms with .typ-loading up and ZERO rows, and the gallery settles at 2530ms. Fixed in the readiness door - see [ready-accepts-unrendered-states], which also closes the boot-shell half. WHAT IS STILL NOT ESTABLISHED, and why this stays PARTIAL: it is NOT shown that every one of the original twelve reports belongs to one of these three. The class is INSTRUMENTED rather than eliminated, which was the intent - a run that could not load the app now fails saying so, at the test and at the run. PREVENTION STILL NOT DONE, deliberately: give each concurrent runner its own outDir keyed on PLAYWRIGHT_PORT so two agents in one checkout cannot collide at all. ---- 2026-09-08, DONE: THE PREVENTION IS IN, and the class is still INSTRUMENTED rather than eliminated. Every path a run writes - Playwright’s outputDir (artifacts/), the JSON report, findings.jsonl and the two audit ledgers base.js keeps - is now keyed on PLAYWRIGHT_PORT through tests/user-sim/report-dir.js: set, a run writes to tests/user-sim/report/<port>/; unset, to tests/user-sim/report/ exactly as before, so CI’s artifact upload and test:users:report are unchanged. PROVED by starting two suites from two worktrees 2.5ms apart on 4770 and 4771 (54 passed each, overlapping for nine seconds) and listing both trees afterwards: each held only its own report/<port>/ and neither wrote into the other. tests/unit/per-runner-report-dir.test.js IMPORTS the real config under different values of the variable, not a regex over it, and fails if two ports resolve to one directory, if no variable resolves anywhere but the historical default, or if helpers.js and the config disagree - 5 mutations at the call sites, 5 caught. The guidance printed on a delivery failure (base.js, helpers.js) and docs/reference/build-and-verify.md now say what the port keeps apart. NOT keyed on the port, deliberately: dist/. The documented re-run builds WITHOUT the variable and tests WITH it, which a port-keyed outDir would break, and separate worktrees already have separate dist/ - so the rule stays one checkout per concurrent runner, and base.js still fails a run whose dist/ was rebuilt underneath it. What this closes is the shared-report half of the collision; the original twelve reports remain unattributed and the three named mechanisms remain instrumented, not eliminated.' },
  { id: 'home-generate-loop-flake', title: 'The 24-click Generate loop in 8c times out under suite load', priority: 'P2', effort: 'S', area: 'Testing', status: 'done', note: 'DONE, AND REPRODUCED BEFORE IT WAS FIXED - a flake you cannot make fail on demand is not a flake you have fixed. Made deterministic with Chromium Emulation.setCPUThrottlingRate rather than waited for: at 10x, the 24-round loop TIMES OUT at 40.1s wall, busting the 30s budget on the Generate click, exactly as reported; at 10x with 4 rounds it passes in 18.4s. 16x was tried and REJECTED as a measuring instrument - there the test busts on navigation itself, so it proves nothing about the loop. The loop length was the variable, not the machine. THE FIX IS THE ONE THIS NOTE ASKED FOR, both halves of it. src/utils/workbenchInk.js lifts the pure colour maths out of HomeWorkbench.jsx unchanged - hslToHex, readableInk, labelGround, cardGrounds, mutedInk and the L_RAMP band - and tests/unit/home-workbench-ink.test.js drives it over ALL 16,200 colours the generator can emit plus 2,880 card-ink pairs across both modes, in about 190ms each. That sweep is the one labelGround’s own comment has CLAIMED in prose since it was written and which nothing ever asserted. 8c keeps a FOUR round rendered loop and now runs in 4.7s, down from 7.4s. BOTH HALVES ARE REQUIRED AND THIS REPO HAS ALREADY PAID FOR LEARNING IT: the estimate that skipped the render missed role.text entirely by assuming the ground equalled role.surface - it assumed the thing that was wrong - so a model-only test would repeat that mistake, while a render-only test is the flake above. The unit sweep proves the maths; 8c proves the COMPONENT still calls it, on the ground it actually composites. 8C COULD NOT FAIL ON AN EMPTY PANEL, found while cutting the rounds: worstOf() returned violations and never said how many labels it examined, so a panel that rendered nothing - or a renamed class in SEL - was a clean run. That matters more at four rounds than at twenty-four. It now counts and asserts a floor of 40, verified by breaking SEL. MUTATIONS: labelGround stops moving an impossible ground; mutedInk returns the raw role colour; cardGrounds stops moving either ground; the L_RAMP band is changed; labelGround overshoots to 7:1; SEL matches nothing. All caught. ONE MUTATION IS UNCATCHABLE AND THAT IS A FINDING, NOT A GAP: making readableInk return its raw pole instead of walking it changes NOTHING measurable. Counted over all 16,200 - the raw pole misses 4.5:1 on 570 of them, and on ZERO of those does readableInk return anything other than the pole. Its fixForeground walk is INERT on every colour this generator emits, because walking an achromatic extreme cannot improve it against a mid-luminance chromatic fill. That is exactly why labelGround has to move the GROUND, and it means the two code paths are genuinely equivalent here, so no test of this path can separate them. Left alone rather than deleted: it is a correct guard for any caller outside this colour space. A SECOND PROMISE PINNED WHILE THERE, also prose until now - labelGround says it returns the swatch unchanged usually and moves it barely otherwise. MEASURED: untouched on 96.5% of the 16,200, worst single-channel move 9 of 255. Now asserted with headroom (5% and 12), and the thresholds catch a labelGround retargeted to 7:1 at 69 of 255. NOT ADDRESSED, AND IT IS THE REASON TO READ [suite-flake-class-unreproduced]: two OTHER full-suite flakes were reported the same day - a Font Gallery h1 and a mobile-ramp spec - each passing in isolation and on re-run. Neither was reproduced or touched here. This item closes 8c only, and a green suite is not evidence about either of them.' },
  { id: 'ci-blocked-on-actions-billing', title: 'No CI has run on any branch, including main, since the Actions billing failed', priority: 'P1', effort: 'S', area: 'Infrastructure', status: 'blocked', note: 'EVERY GitHub Actions run in this repo has failed since at least 2026-09-04T04:39Z, INCLUDING runs on main, and none of them ran a single test. The annotation is the whole story: the job was not started because recent account payments have failed or your spending limit needs to be increased. Both jobs die in 1-3 seconds. Vercel is separately reporting Deployment rate limited - retry in 24 hours on at least one PR. WHY THIS IS P1 AND NOT AN ANNOYANCE: gh pr view reports these PRs as UNSTABLE, and UNSTABLE reads as the code is broken. It is not - it means the gate never ran. Any agent or human triaging by check status will draw the exact wrong conclusion about every open PR simultaneously, and the honest signal (a local gate run) is invisible on the PR. Until this is paid, the ONLY evidence a change is green is a local lint/build/test:unit/test:users run pasted into the PR body, and reviewers need to know to look there. NEEDS THE ACCOUNT OWNER: this cannot be fixed from the repository - it is a billing setting in GitHub account settings.' },

  { id: 'input-specificity-sweep', title: 'Bare type selectors are silently outranking component input classes', priority: 'P1', effort: 'M', area: 'Design system', status: 'done', note: 'DONE (#331), and the fix is a different SHAPE from the two that preceded it. #319 and #320 each fixed their own instance with an input. prefix; that approach leaves the next instance to ship broken, and there were plenty. ENUMERATED: 50 input[type=...] selectors, 21 of them unscoped at (0,1,1) or (0,1,2), and 24 component classes losing declarations to them. Six were reachable signed out and ALL SIX were wrong — .cc-hex-input and .tt-hex-input asked for JetBrains Mono and rendered Manrope; .ggn-angle-input and .ggn-stop-hex each painted a white bordered box INSIDE a control that already had chrome. Screenshot diffs made the double-painting visible; no geometry check could have, because nothing overflowed. THE FIX: the reset is wrapped in :where(), which is zero specificity, so every component class wins by default and the 22nd instance cannot regress. ONE EXCEPTION KEPT DELIBERATELY: the @media(max-width:640px) font-size:16px floor stays at element strength, because iOS zooms the page when a focused input is under 16px — only its padding moved into :where(). Also removed the two !important on .pl-input-title, which existed solely to fight this rule.' },
  { id: 'pillnav-search-hints-hand-kept', title: 'PillNav advertises tools from a hand-kept array', priority: 'P2', effort: 'S', area: 'Navigation', status: 'done', note: 'DONE, and it was ALREADY HALF TRUE when it was written: #332 had pointed PillNav at searchHints() before this ran, so the hand-kept SEARCH_HINTS array was gone and only the status field was stale. Verified in a browser at 1440 rather than assumed - the nav field expands 300 to 390px on hover and types Contrast Checker, Emoji Library, Font Gallery, Font Pair Finder, Gradient, Icon Library and Type Scale into its span, none clipped inside a 304px box. WHAT WAS STILL BROKEN is the item’s own trap arriving through the fix: searchHints() did not answer the question its callers were asking. It selected short /create/ labels from whatever list it was handed and left the `soon` filter to the two call sites. Both got it right, so nothing was wrong on screen - ONLY BY LUCK. Handed the plain registry it returned Component Designer, Box Shadow, Auto-Builder, Image Prompt and Landing-Page Prompt, five tools with no page behind them, for the persistent nav on every page. The rule now lives in the helper and no caller can decline it; output is byte-identical for both existing call sites, so this closes a latent hazard rather than a visible bug. THE OLD ASSERTION COULD NOT HAVE CAUGHT IT: it called searchHints(liveTools()), filtering `soon` out of its own input and then checking the output had no `soon` in it - which measures the test’s filter, not the product’s. TWO MORE HOLES, both mutation-verified with the pre-existing assertions left GREEN, which is the point. (1) The PillNav guard banned the identifier SEARCH_HINTS; re-adding the identical hand-kept array as NAV_HINTS walked straight past it. The replacement derives every label from the registry and fails if any is written down in PillNav.jsx as a literal, so the copy cannot return under a different name. (2) THE CAP WAS MEASURED IN THE WRONG LANGUAGE. Both surfaces call localiseTools(t) BEFORE searchHints, so the 20-character cap that decides which tools get advertised is applied to the LOCALE’s label - a string no test read. The nav types Font Pair Finder, sixteen characters, where the registry says Font Pair, nine. It is already live and silent: en offers 13 terms, fr offers 9, because Bibliothèque d’icônes, Bibliothèque d’Emojis, Échelle typographique and Recherche de paires de polices are all over the cap and simply never appear. That is fine; a language falling to one or two is not, and nothing could see it. Every locale is now held to the same floor of 6 that English is held to, placed in tests/unit/locale-tool-copy.test.js beside the locales() loader rather than as a second copy of it. MUTATION: HINT_MAX_LEN 20 to 15, a plausible "the field got narrower" edit, starves fr to 5 while all 18 search-index assertions stay green. NO TEST NAMES A TOOL - both sides are derived, so a renamed tool moves the assertion with it. NOT CHANGED, on purpose: the cap’s comment still justifies 20 by "the input at 390px" while the hero input measures 554px at 1440. The number is conservative rather than wrong, and re-deriving it is a sizing decision, not a duplication one.' },
  { id: 'docs-and-skills-staleness', title: 'Audit every MD and skill file for staleness, because work is going in circles', priority: 'P1', effort: 'M', area: 'Docs', status: 'done', note: 'DONE. Verdicts on all 49 markdown files are in the new docs/reference/doc-authority-map.md, linked from CLAUDE.md. The founder’s circling has a mechanism and the map states it: an agent that follows a wrong instruction confidently does not fail — it does the wrong work, the gate goes green, and it reports success. FOUND, all verified against source rather than assumed: architecture.md claimed “there is no branded 404: the wildcard route redirects to /” and pointed at global-failure-states as an open gap — both false since #235, NotFound.jsx has been behind the wildcard route for weeks and the queue entry reads done; architecture.md listed IconLibrary and EmojiLibrary as live pages when nothing imports either (the exact #306 trap) and still headed its surfaces section Workspace; anti-slop-quality-bar.md named Workspace as a surface, which tests/unit/surface-vocabulary.test.js guards against in shipped copy; FOUR documents answered “which widths do I test at” three different ways and none named 640px, which carries 30 media blocks in global.css — more than any other width — or the 641-900 band where an 834px tablet is served the desktop branch; README.md and engineer.md both said “four unit tests read the prerendered shells” and it is six. #295 missed nothing: the seven deleted-audit paths left in CHANGELOG.md are the record OF the deletion and correctly stay, and all 49 files have zero broken links. NOT DONE ON PURPOSE: nothing archived or deleted. docs/archive/<original-path> is PROPOSED in the map and in the PR; git history is already the archive and CLAUDE.md forbids parallel historical docs, so that is the founder’s call. The two spent QA audits hold ~40 findings whose live-ness needs a rendered pass, and five agents are moving the source. Their durable knowledge is migrated already. The gate doc’s hardcoded counts were deliberately left to #304, which another agent was working concurrently.' },
  { id: 'anti-slop-review-skill', title: 'Build a repeatable anti-AI-slop review skill, and any others the work needs', priority: 'P1', effort: 'M', area: 'Docs', status: 'done', note: 'DONE, and the honest answer to the question the item asked is that the gap was an UNUSABLE skill, not a missing one. uil4b-brand-design/references/anti-slop-quality-bar.md is good and stays — but it is diagnostic lenses and a five-way verdict, with no routes, viewports, capture method, severity scale, finding format or artefact rules. It cannot be run. An agent handed it returns an essay about the design language; the founder asked for evidenced findings. So the vocabulary stays where it is and now says at the top that it is vocabulary, and the procedure is new: .claude/skills/uil4b-surface-review/ (SKILL.md + three references, matching the uil4b-brand-design layout). It encodes what actually worked on 2026-09-01 — every finding names a route, a viewport, the complaint in a person’s words, BROKEN/POOR/POLISH, and a selector VERIFIED in source; capture artefacts are discounted BY NAME rather than silently, with all three verified here (the [data-reveal] opacity:0 rule at global.css:5107 whose .is-in only lands via a ScrollTrigger at top 86%, the windowed grid that mounts 430 of 1,636 emoji, and FontGallery.jsx:561’s honest fallback banner that a blocked webfont makes spurious). It says plainly that measurement alone does not find this class of fault, because that format found nine defects on surfaces an automated geometry sweep rated clean. Mobbin is standing workflow and “the existing pattern is already right” is recorded as a real outcome (#306). Systemic findings beat instance lists (#298 closed six with one). NO SECOND SKILL: responsive review is the same procedure at more widths, so it is references/responsive-bands.md inside this one — different data, not a different method, and a second overlapping skill is the duplication the staleness item exists to remove. Also scoped the 13 imported taste skills the founder installed the same day.' },
  { id: 'font-popup-about-and-examples-tabs', title: 'Font popups gain an About tab and a self-generated Examples tab', priority: 'P1', effort: 'M', area: 'Typography', status: 'done', note: 'DONE (#327). Both tabs ship, as ONE shared component used by the font popups rather than a second divergent copy: src/components/FontDossier.jsx renders them and src/utils/fontDossier.js derives the facts. NEITHER TAB TOUCHES WHAT [fonts-in-use-surface] IS BLOCKED ON — no photographs, no logos, no screenshots, and no claim that any named company uses any family. That item stays blocked and is unchanged. ABOUT IS DERIVED, and the backlog assumption that the catalogue already supplied the fields was HALF RIGHT in a way worth recording: the app was keeping only family/category/variants/subsets, and the Google WebFonts API it preferred carries no designer or date AT ALL. The keyless fonts.google.com/metadata/fonts endpoint does — designer, dateAdded, italic cuts and variable axes, verified at 100% coverage across all 1,946 families — and /api/fonts already knew how to read it but tried it SECOND, so on any deployment with GOOGLE_FONTS_API_KEY set the About tab would have had nothing to show. That order is reversed, WebFonts kept as fallback. Every row is carried straight through and a field the catalogue does not supply is OMITTED, never guessed. dateAdded is labelled “Added to Google Fonts” because that is what it is — for a revival it is decades from when the typeface was drawn, and “Released” would be the quietest lie on the page; a test pins the wording, and a mutation relabelling it fails. EXAMPLES ARE DEMONSTRATED: the family set in four situations the page draws itself — article opening, interface furniture, pricing table, display line — each captioned with the question it answers. Aave’s brand-typography section (a role stated under each face, not adjectives) drove the captions; Spline’s and HoneyBook’s pricing sections are why a pricing table is one of the four, since nothing else in the app tests numerals at display size. The browse dialog had NO per-family view to hang tabs on — clicking a tile picked and closed, so “what is this face, actually?” could only be answered by choosing it and undoing that. It now has one, reached by an info button in LibraryCard’s actions slot; the fast path is untouched. #305’s warning that tabs are how the 1,763px dialog returns was checked and does not apply: head, tabs and action are fixed rows and only the panel scrolls — measured at 1280x900 with Examples open, 1,077px of content, dialog still 828px, the action still on screen. STILL OPEN: the Font Gallery specimen dialog was NOT given the tabs. The shared component is built for it and its existing body is the natural Specimen panel, but that dialog is a 997-line file another agent may be in, and wiring it is a separate, safe commit rather than something to rush at the end of this one.' },
  { id: 'popup-scroll-chaining', title: 'Scrolling inside a popup scrolls the page instead, because Lenis is not prevented', priority: 'P1', effort: 'M', area: 'Design system', status: 'done', note: 'DONE (#316). Founder report: on many popups that require scrolling, the wheel scrolls the page and not the popup. Reproduced exactly — pointer inside the prompt modal body, one wheel gesture moved the page 500px and the modal 0. THE ROOT CAUSE WAS NOT THE POPUPS. Lenis intercepts the wheel globally and drives the page itself, handing an inner element back to the browser only when that element or an ancestor carries data-lenis-prevent — and the app had FOUR of those attributes in total (three in PaletteBuilder, one in IconLibrary) against a dozen scrollable overlays. Adding a fifth is how the count got to four. So the fix is ONE OPTION on the single Lenis instance: allowNestedScroll, after which Lenis measures the element under the pointer and stays out of the way while it can still scroll. Every popup in the app inherits it, including the ones that never touch a hook and the ones nobody has written yet. Two supporting parts. THE BOUNDARY: useModalDialog and usePopover mark their scroll containers so overscroll-behavior stops a finished scroll rather than handing the remainder to the page. Marking is done in JS because CSS cannot ask which elements are scroll containers, and the obvious subtree rule is actively WRONG — contain on a plain span between the pointer and the scroller blocks the gesture reaching it at all, and the specimen dialog scrolled 0px instead of 413. THE LOCK: useModalDialog set document.body.style.overflow to hidden and that never held — measured here, a real wheel still moved the page. New shared module src/utils/scrollContainment.js, wired into useModalDialog, usePopover and useSmoothScroll, so the fix lives where every overlay already routes rather than sprinkled across twelve components.' },
  { id: 'font-pair-full-review', title: 'Full review of the Font Pair page, its popups and its tools', priority: 'P1', effort: 'M', area: 'Typography', status: 'done', note: 'DONE (#327). Reviewed at 390/640/834/1280 against the live catalogue (1,946 families seeded), so none of this is the spurious listing-failure state. Neither #298 fix was re-reported. TWO FINDINGS, both workflow rather than surface. THE CONTROLS CAME AFTER EVERYTHING THEY CONTROL: “Read the pairing” was first in the markup and “Choose the pair” was a sibling after it. Invisible at 1344px+ where the sidebar sits alongside, severe below — the two-column layout only starts at 1344px, so at 1280 the page stacks and the font pickers landed 1,930px BELOW the specimen, after all six suggestions and after the CSS export. The step numbers recorded the inversion (01, 03, 04 in the main card, 02 stranded in the sidebar). Settled by REORDERING THE DOM rather than renumbering to match a broken order: choose then read then suggestions then handoff, numbers 01-04 at every width, sidebar put back on the right by grid placement in the one band that has one (verified at 1440, unchanged). THE SUGGESTIONS ALL SAID THE SAME THING: under an Inter heading all six came back sans-serif with one sentence repeated six times. The serif rule for a sans heading was real, correct and UNREACHABLE — ranking was a global sort where 1/(popularity+1) runs 1.0 to 0.01 while the category preference was a +0.05 nudge, and the popular end of the catalogue is overwhelmingly sans. Each target category is now ranked separately and interleaved, canonical pairing leading; the cards alternate serif and sans with two distinct reasons. Squarespace groups its pairings by character for the same reason. The ranking moved to utils/fontPairing.js because googleFonts.js reads import.meta.env and therefore could never be imported by node --test — none of it had ever been covered; it now has eight tests, and a mutation reinstating the old global sort fails four of them. FontBrowseDialog was checked for #305’s overflow fault and does NOT have it: body scrolls itself (1,804/565), overlay does not (900/900).' },
  { id: 'type-scale-full-review', title: 'Full review of the Type Scale page', priority: 'P1', effort: 'M', area: 'Typography', status: 'done', note: 'DONE (#327). Reviewed at 390/640/834/1280 against the live catalogue. Neither #298 fix was re-reported. THE OPEN RESTRUCTURE IS SETTLED. This item recorded the 01/03/02 numbering as a deliberate design decision left open, and it was worse than a numbering problem: “Tune the scale” holds every input the tool has — both breakpoint bases and ratios, step counts, leading, tracking, rounding and the families — and at 1280 it sat 1,836px BELOW the ladder those inputs drive, because two columns only begin at 1344px. Adjusting a ratio meant scrolling away from the only thing that shows what the ratio did. Settled the same way as Font Pair: reorder the DOM, not the numbers. Order is tune then read then evaluate/ship, numbers 01-03 at every width, sidebar restored on the right at 1344px+ (verified at 1440). THE DESIGNERS/DEVELOPERS SPLIT WAS EXAMINED RATHER THAN PRESERVED, and the verdict is that the SPLIT earns its place but the second control did not. audience had TWO controls: a role=tablist pair of large hero cards whose aria-controls named a panel 1,593px away — about two screens, so pressing either changed nothing you could see — and a segmented switch further down setting the identical variable with aria-pressed and no tablist role. One variable, two controls, two ARIA patterns, and the loudest one had the invisible effect. The hero pair is removed and the surviving switch promoted to the real tablist, now 62px above its panel instead of 1,593px, with arrow keys, roving tabindex and aria-labelledby verified working. The split itself is kept as a genuine either/or on one output panel; what it did not earn was being posed as a workflow choice before the tool has been used. The hero paragraph already serves both audiences, and the hero gets 166px back. The 18 dead .tsc-audience rules went with it rather than being left to rot. #298’s stacked-indicator fix is NOT reverted — the element it corrected no longer exists, and the lesson it recorded is preserved in the .hw-tabs note that cites it. NOT REPORTED, having been considered: the ladder printing one pangram at nine sizes is correct here — it is the type scale tool, and a constant string at each size is how a scale is judged. That is a different thing from the repetition #305 removed from a specimen dialog.' },
  { id: 'unified-colour-picker', title: 'One colour picker, matching the app, used by every surface', priority: 'P1', effort: 'L', area: 'Colour', status: 'done', note: 'DONE, and the inventory it asked for came back with two findings bigger than the redesign. (a) THE PICKER. It was three bordered fields stacked with gaps; it is one instrument now — pad and hue share a frame and bleed to the panel edge, the panel NAMES the colour it is editing (a gradient opens one of these per stop and they were previously identical), both swatch groups are headed and each label IS its list—box accessible name, and the open trigger no longer announces itself only with a border it shares with :hover, which a touch user never sees. ONE control was added, an EYEDROPPER, and the reason is the emit contract rather than taste: EyeDropper.open() resolves to an opaque sRGBHex, which is already exactly what this component emits, so every call site can store it unchanged. Feature—detected, absent outside Chromium rather than present and broken. (b) THE INVENTORY, taken before anything was designed: TWELVE colour—input surfaces, three already on ColorPickerPop and nine native input type=color. FINDING 1, the one worth the whole pass: the hue slider has NEVER painted its rainbow. input[type=range] near the top of global.css is specificity (0,1,1) and sets background:transparent; a bare .cpk-hue is (0,1,0), so the app’s generic slider won every contested declaration and the strip shipped as a 4px grey track with the same blue thumb as a quality slider. Every rainbow declaration was present and none applied, which is why reading the CSS never showed it and rendering the panel showed it instantly. Fixed by qualifying with .cpk-instrument, the same way .snapv—grad already resolves the same collision. FINDING 2: ColorStudio renders only ONE of its four sections, chosen by pathname, and the only route that mounts it is /create/semantic—color, which resolves to states. /create/palette mounts PaletteBuilder, /create/gradient mounts GradientGenerator, and there is no /create/ui in toolTree at all, so its palette, systems and gradient sections are unreachable — which makes all FOUR of its colour inputs dead on every shipped route. Verified in a browser on the built app and against origin/main, so it is the state of the app rather than something this branch caused. USER—VISIBLE UNIFICATION IS THEREFORE FIVE SURFACES, NOT NINE: the contrast pair, the tint ramps, the JPEG matte, the UI—system seed and the homepage workbench stops. Nobody should read nine and picture nine screens. ColorStudio’s four were converted anyway and the commits stay, because they delete a hand—rolled stop popover that had NO dismissal contract (a div with stopPropagation: Escape did nothing, clicking outside did nothing, focus never entered, no edge flip) plus the dead state, refs and CSS around it — and if those sections are ever re-enabled they come back on the shared picker rather than restoring four native inputs. STILL NATIVE, deliberately: BoxShadowGenerator (3) and UIBuilder (5) are imported by nothing at all, and deleting unrouted pages is a founder call per docs/reference/doc-authority-map.md, not an agent’s. UNCHANGED AND RE—ARGUED: the picker still emits #rrggbb, and alpha plus the Solid/Gradient/Image tabs stay declined — the call-site count that argued them down grew, which strengthens the case rather than weakening it, and two of the sites ARE gradient stops, which cannot themselves be gradients. Proof: tests/user-sim/35-colour-picker.spec.js now 15 tests, every new one mutation—verified, including that the declined controls are asserted absent by ROLE as well as by text — the old text check passed a tab strip labelled Fill, and the old .cpk-alpha check passed an opacity slider under any other class name.' },
  { id: 'five-surface-heavy-review', title: 'Heavy UI/UX review of the five surfaces the founder named', priority: 'P1', effort: 'L', area: 'Design system', status: 'done', note: 'DONE (#323). All five reviewed in a real browser at 390/640/834/1280 and shipped one commit per surface. THREE OF THE FIVE RECORDED FINDINGS WERE STALE and are corrected here so nobody re-opens them: (a) the semantic Light/Dark proof pair IS matched at 834 — both panels 396x377, every row 64px, same at 1280; (b) the contrast checker DOES have a mobile override (global.css @media max-width:560px) and its Make-it-pass explanation measures 222px at 390, not 85; (c) the gradient preset rail already has all three affordances via .rail-overflow, pinned by 36-rail-overflow.spec.js:127 — the clipped:true a geometry sweep reports there is the scroller working. WHAT WAS ACTUALLY WRONG, per surface. SEMANTIC COLOUR: the four colour tools ran TWO hero languages — Tint (.tt-hero) and Gradient (.ggn-head) open with eyebrow, large title, description, their own actions and a live status strip; Semantic and Contrast sat on the site-wide .sec-h with none of that. Converged onto the shape two of the four already shipped. Also collapsed three stacked onward-nav blocks to two: Next-in-the-workflow offered contrast/tint/palette, a strict SUBSET of the More-colour-tools footer 200px below it. TINT: the 01/03/02 decision is MADE — restructure. 03 was a div nested inside the 01 card while 02 was that card’s sibling, which is why no CSS order could interleave them. Measured before deciding: .tt-page is capped at 940px and the workbench is a SINGLE column from 0 to 1343px, so 01/03/02 was what every viewport up to and including the 1280 reference desktop rendered, not a wide-screen quirk. GRADIENT: a numbered step 03-Starting-points sat BELOW the canvas and inspector, telling you where to begin after you had exported, and for a user with no saved palette that whole step rendered one apology in a full-height card. Merged with the curated rail into one unnumbered Start-from block. PROMPT LIBRARY: see [prompt-library-hero-and-toolbar], folded in. CONTRAST CHECKER: the worst find of the five — THE PAGE FAILED ITS OWN TEST. Measured on the white card in light theme: the verdict line Passes-some-checks at 2.94:1, the pass glyph at 2.70:1, the fail glyph at 3.43:1, all against the 4.5:1 the page exists to enforce. All three were hard-coded hexes rather than the project’s --ok/--warn/--err, so they never responded to the theme either, and the fail red measured 3.97:1 on the dark card. Fixed in both themes and pinned by a test that computes the ratio the same way the page does. ANTI-SLOP: removed an empty 40x40 accent square painted on every prompt card — centring rules for a glyph never added, reading as a thumbnail that failed to load. NOT DONE, deliberately: ColorStudio’s three unreachable sections were recorded rather than restyled [colorstudio-dead-sections], and the product-wide --accent contrast problem was recorded rather than fixed globally [accent-small-text-contrast].' },
  { id: 'icon-library-in-discover', title: 'Reach the Icon Library from the Discover tab', priority: 'P1', effort: 'S', area: 'Discover', status: 'done', note: 'DONE. Founder request (2026-09-03): “i sohuld also be able to access icon library from the discober tab”. THE WORD THAT DECIDED IT IS “ALSO” — additive reach, not relocation. Of the three options, a Discover ENTRY POINTING AT THE LIVE ROUTE was chosen, and the evidence is that this is not a compromise but the shipped pattern: DISCOVER_GROUPS ALREADY carries `font-gallery` pointing at /create/font-gallery, so a /create URL listed under Discover is precedent the tests already pin (08-public-route-contract asserts the Font Gallery row inside the Discover section of /sitemap). The page is also already a Discover gallery in everything but its URL: #292 moved it onto the shared Library browse components and #306 put it under DiscoverGalleryHero, so the founder’s complaint was reach, not dress. A CANONICAL MOVE was rejected on cost, with the routing rules read first: it would need a legacyRoutes entry (the single table generating both the client Navigate routes and vercel.json’s 301s, with tests/unit/redirects.test.js failing the build if the two disagree), sitemap and routeMetaMap edits, and it would split IconEmojiLibrary.jsx — which /create/icons, /create/emoji and /create/icons-emoji all mount — across two surfaces, breaking the #306 tablist that is the only route between the two libraries. A NEW /discover/icons route was rejected as the same cost plus a second indexable URL for one page. PRERENDER COUNT UNCHANGED at 27 route shells, verified by build, because scripts/prerender.mjs derives its list from public/sitemap.xml and /create/icons was already in it. The tension worth naming rather than papering over: docs/reference/discover.md frames Discover as browse (find, save, remix, submit) against Create’s build — but that same doc names icon resources as Discover content by hand, and a searchable 200k-icon catalogue is a browse surface by any reading. ONE ENTRY, NOT TWO: the Emoji Library is the other tab of this same page, one click away inside the hero #306 shipped. Also here: the Discover landing hint claimed THREE curated libraries and there are now four, and PillNav’s MENU_TOOL_COPY gained a one-line blurb for the new group id so the menu row does not run three lines deep beside its one-line neighbours. Mobbin (web, resource library category card in a browse hub) CORROBORATED rather than changed the design: Duolingo’s brand portal, Coda’s categories and PayPal’s press grid are all directories whose cards link OUT to destinations owned elsewhere — none of them relocate the destination under the hub. No new component, and NO global.css edit. Pinned by two tests in 15-discover-library-parity.spec.js covering the landing card and the nav on desktop and phone; verified rendered at 390, 834 and 1440.' },
  { id: 'palette-random-on-load', title: 'The Palette Builder opens on a random palette, and reset re-randomises', priority: 'P1', effort: 'S', area: 'Colour', status: 'done', note: 'DONE. Founder request (2026-09-03): “the pallete page should auto load a random pallete on the default free settings. reset should reset the settings to default but randomise the colour.” BOTH HALVES TURNED OUT TO BE ONE DEFECT: the page’s DEFAULT WAS A PAID CONFIGURATION. Arrival ran generateHarmony(seed, analogous) and Reset rebuilt generateHarmony(#4338E0, analogous), while the system chip read Auto — and Analogous is flagged free:false, so a free user was shown output they could not themselves produce. P-004 had already moved the chip and ProjectContext to Auto and missed both generator calls. Four more analogous fallbacks (the signature key, undo, history restore, the history menu) and the regen catch path were the same bug and went with it; PaletteBuilder now contains none. WHAT vs-palette-session-seed ACTUALLY GUARANTEED, measured before overriding it: nothing that the saved project did not already guarantee better. It was read exactly once, on a device’s first ever visit — the mount effect wrote the drawn board straight into ProjectContext, so from the second visit readSavedPalette won the precedence and the key was never consulted again; in a fresh tab it was not even written. Probed on the built app: visit, reload and a new tab all returned the same five hexes. So random-on-load held exactly once per device and never again, which is what the founder was reporting. The key is retired, and the actual fix is that AN UNTOUCHED RANDOM DRAW IS NO LONGER PERSISTED — the mount write is skipped only for a board whose source is the draw, so the moment the user changes anything it persists exactly as before, and a shared link, a hand-off or a saved project still persist on mount. Re-probed: three arrivals, three different palettes; press Randomise and the board survives a reload. A SHARED LINK STILL WINS, verified three ways: the precedence moved into the pure initialPaletteBoard (query > hand-off > saved > draw) where tests/unit/palette-defaults.test.js enumerates it, a browser test opens a ?c= link three times over, and the unit test passes a THROWING stub as the randomiser so reaching it at all is a failure. ONE RANDOMISER: defaultPaletteBoard calls randomSystemPalette, the same engine behind the Randomise button and the Space key, injected only so a test can stub it. Reset’s already-reset check moved from comparing whole boards to isDefaultSettings, because two resets no longer produce the same board and the old comparison would have silently killed the double-click guard on the pre-reset snapshot. Undo is unaffected — the pre-reset board still goes into vs-palette-history. THE FREE DEFAULT IS PINNED THREE WAYS: DEFAULT_SYSTEM asserted free:true in config/colourSystems.js, asserted present in the builder’s own FREE_SYSTEMS (the gate that actually holds), and a browser test that reads the ENGINE rather than the label — the tonal engine pins SUBTLE at tone 90 / chroma 8, and over 300 draws each, Auto lands at 89.8-90.1 / 7.4-8.5 while Analogous never gets lighter than tone 83.2 or below chroma 24.5. NOTE FOR REVIEWERS: 11-palette-recovery.spec.js asserted the seed SURVIVED a reload; that was the session seed’s doing and is now the opposite of the request, so it was rewritten to assert the guarantee that protects real work — a TOUCHED board survives. No global.css edit.' },
  { id: 'multi-breakpoint-ux-audit', title: 'A standing UI and UX audit across the breakpoints this app actually breaks at', priority: 'P1', effort: 'L', area: 'Quality', status: 'partial', note: 'PARTIAL. The 2026-09-03 pass ran (#330) and produced 10 new items plus 7 amendments, ranked, each carrying the surface, the width and the measured number. Its biggest finding came from walking a FLOW rather than measuring a page: the app’s own search could not reach 7 of its 13 live tools by name, since fixed in #332. Method note worth keeping: three findings were KILLED by checking them (the peek fade is live, Ctrl+K does work, the Discover handoff is touch-reachable — the automation had checked visibility before hovering), and a novelty claim was withdrawn when the figure turned out to be already documented. WHY THIS IS NOT DONE: whole categories remain unaudited because they are unreachable from a signed-out session — every signed-in surface, real assistive-technology announcement, pointer gestures, and text over gradients and images. Those stay listed on [audit-coverage-not-run] and silence on them is not a pass. A future pass needs an authenticated session. UPDATE 2026-09-06: THE AUTHENTICATED SESSION EXISTS NOW, so this is no longer the reason a pass cannot be finished. `signIn(page, { plan })` in tests/user-sim/helpers.js gives any spec a free, Pro or admin session that RequireAuth, the project cap, useSubscription().isPro and the export gate all accept — see [audit-coverage-not-run] for the mechanism and for the proof it cannot reach a production build. The next pass should walk /projects with real projects, /settings, /admin, /create/alt-text’s tool and the Pro half of every gate at the widths this item already names; of the categories listed above only assistive-technology announcement and pointer gestures are still out of reach. 2026-09-07 PASS — THE SHELL AND THE SALES SURFACES, at TWENTY widths rather than four. The four standard widths are why earlier passes missed what the founder keeps flagging: every issue he raised this session sat BETWEEN them (662, 981–1080, an iPad). This pass swept 320/360/390/414/480/560/600/662/768/820/900/981/1024/1080/1180/1280/1366/1440/1536/1920 across 13 surfaces in BOTH themes — 520 surface×width cells — checking horizontal scroll, nav integrity, target size, covered controls and clipped labels; plus every overlay (three mega menus, the compact popover, the mobile sheet, the login modal, the founder note) at all twenty widths, and the modals again at nine real device viewports including two landscape phones. RESULT: 260 of 260 failing cells down to 1, in each theme. FIXED, one commit each, every guard mutation-verified at its call site: (1) THE HOMEPAGE SOLD A QUARTERLY TIER NOBODY CAN BUY — planLadder.js gives it checkoutPlan:null and says offering it would dead-end at “Invalid selection”; the same panel said “Cancel any time” where /plans had removed that line under a founder flag, and typed a headline planLadder.js exports a function to compute. The front page was keeping a hand-written copy of the ladder that module calls the one source. (2) FIVE CONTROLS UNDER 24×24 in 260 of 260 cells, three of them in the FOOTER so they were on every page in the product (.app-footer-note 109.8×21.4, .app-footer-links a 94.8×21.4, .app-footer-start 133×23.1, .seo-seg button 67.6/59.1×21, .plb-hex 67.5×21). (3) THE FIXED FEEDBACK FAB COVERED THE 404’s footer attribution across its ENTIRE width at 981 and 1440, on a page with ZERO scroll room — unreachable by any means — now impossible by clearance reserved inside the footer. (4) The community card’s platform credit clipped at 360px to save two pixels. FOUR FINDINGS WERE KILLED BY CHECKING THEM, which is the method working rather than a shortfall: the Learn index cards looked stuck at opacity 0 (an instant jump to the bottom had skipped past them — a real scroll reveals all 13); the nav’s “Start for Free” label looked clipped into a 24px pill (measured mid-entrance — .pnav-cta mounts .is-waiting on sales routes and settles at 126×40); “See it working” looked to render in mismatched letter colours (screenshot subpixel AA; one computed colour); and the homepage’s 100 gradients / 101 palettes are derived from the real arrays. A FIFTH NEAR-MISS IS WORTH RECORDING because it nearly shipped as a P1: reading the homepage’s first screen before the hero’s GSAP entrance finished reported a page whose only legible text was UIL4B and whose only controls were the nav. Settled on the hero’s own CTA opacity, both widths carry the headline, the lede, the free-to-use line and both CTAs above the fold. FIRST-VISIT FLOW walked end to end at 390 and 1440 (land → understand → try → hand-off → sign-up) and it HOLDS: the homepage Generate really regenerates, the exact hexes carry into /create/palette, and the sign-up modal promises three things that are all true. FILED RATHER THAN FIXED, both needing a founder call: the hero leaves 272px of dead space with 0px of the next section visible at 1440×900 (402px / 100px at 1920×1080), which is hero SHAPE and therefore his open ?hero=a|b|c decision; and the FAB covers 6 of 9 sample points of the /seo device toggle at 662px, which is recoverable (1613px of scroll room) and whose general answer is a whole-app rule about reserving that corner. STILL NOT COVERED BY THIS PASS, AND NOT CLAIMED: this sweep was signed OUT throughout, so every signed-in surface remains unmeasured at these twenty widths even though the 2026-09-06 update above means the session to do it now exists — the shell and sales surfaces here are the signed-out half only. Real assistive-technology announcement and pointer gestures are still out of reach entirely. Those stay on [audit-coverage-not-run]. PASS 2026-09-07, THE THIRTEEN /create TOOLS ONLY (the app shell, nav, footer, sales and landing pages were another agent’s and are NOT covered here). Method: a dense sweep at TWENTY widths — 320 360 390 414 480 560 600 662 768 820 900 981 1024 1080 1180 1280 1366 1440 1536 1920 — a fresh browser context per width rather than a resize, in light and dark and in a real touch context (isMobile + hasTouch + an iOS UA, verified in-run that hover:hover is false and pointer:coarse is true). 637 measured rows, 0 errors. THE HEADLINE NEGATIVE: not one tool scrolls horizontally at any width from 320 to 1920, so the founder’s breakpoint complaints are not page-overflow. FIXED, each with a rendered assertion mutation-verified at its call site: the palette toolbar wrapped to two rows at 105px instead of one at 57px across 1097–1136px, because fitRail() passed row.clientWidth — which INCLUDES the toolbar’s own 20px-a-side padding — so the cluster expanded at 1097 and did not fit until 1137 (tests/unit/toolbar-fit.test.js could never catch it: its table passes VIEWPORT widths as rowWidth while the call site passed clientWidth, and with the call site reverted the unit suite still reports 1417/1417 while the browser test goes red); seven cross-tool handoff links at 18.1–21.4px, four of them the same declaration copied to four places; the four palette adjustment sliders at 14px tall; three acting controls at 15–23px; and the library search field, which LOOKED 46px tall and accepted a click in its middle 18px only because .lbry-search is a plain div, not a label, and the input sat un-stretched inside it — a height assertion would have passed the whole time, so that one is pinned by a real click. Also removed the lede motif from three more tools: Tint, Gradient and Font Pair all carried the three-clause workflow narration #386 stripped from the Type Scale, two of them sharing the phrase ‘production-ready’ and Font Pair additionally claiming ‘pair type like a creative director’. FOUR FINDINGS KILLED BY CHECKING THEM, which is the part worth keeping: the palette action rail appeared to arrive already scrolled with Randomise off the left edge at 390 and 769–960, and that was ENTIRELY an artefact of resizing a page loaded at 1440 — scroll-snap re-snaps on resize, and on a fresh load scrollLeft is 0 and Randomise is leftmost at every width; .fg-card-open and .fg-card-compare ‘overlap’ by 30px at every width and are a card-sized button with a 30px button stacked on it at z-index 2, both of which pass a trial click with elementFromPoint returning the small one; .ggn-handle’s ‘0%’ is not clipped (overflow is visible and the text is aria-hidden beside a real aria-label); and .stc-sc-link / .stc-sc-ghost at 14 and 21px are INERT specimen elements inside the semantic-colour usage examples, already tabIndex={-1} with no handler, so WCAG 2.5.8 does not reach them. A fifth was discounted as capture artefact #3 — the Font Pair ‘showing a bundled list of 84 families’ banner is this sandbox blocking Google Fonts. WHAT THIS PASS COULD NOT REACH: every signed-in state (another agent is unlocking those in parallel and this pass claims none of them); real assistive-technology announcement; pointer gestures; and /create/alt-text’s actual generator, which is behind auth, so only its signed-out shell was measured. FILED, NOT FIXED, because each needs the founder’s judgement rather than a defect fix: the Font Pair hero fills the entire first screen at 390x844, so the first control a phone user meets is a link to a different tool; .tt-ramp scrolls with a 23px peek and a thin scrollbar but no fade, which is 2 of the 3 layered affordances global.css asks for, and adding the rail-overflow utility would change scroll-snap on a rail that already has a pinned test; .ggn-handle at 20x20 is dragged along a bar whose stops can sit 20px apart; and four bare text inputs (.ggn-stop-hex 293x17, #fpr-text 274x20, a font-gallery filter input 251x17, and a 13x13 native checkbox on Tint with a large associated label) sit un-stretched inside padded wrappers, the same shape as the search field but each needing its own wrapper checked. 2026-09-07 FLOWS PASS — THE SIGNED-IN HALF, which the twenty-width sweep above explicitly did not claim. Nine end-to-end user flows were walked signed in at 390x844 and 1440x900 using signIn(): first visit to sign-up, first project to export, free to Pro, the returning user, the brand-kit walkthrough, community and feedback, the AI Brand Starter, failure states, and settings and account. NO HORIZONTAL OVERFLOW was found on any signed-in surface at 390 (/projects, /settings, /onboarding, /community, /plans all measured scrollWidth == clientWidth). ONE BREAKPOINT DEFECT FILED RATHER THAN FIXED, because it does not block a flow: the project card’s title control is 284x21 at 390, under the 24px floor #413 applied to the shell, and it is the one signed-in surface neither breakpoint audit owned — the same card carries a full-height Load button and the overflow menu, so the project is still reachable. SIX CANDIDATE FINDINGS WERE KILLED BY REPRODUCING THEM, which is the method working: the homepage’s blank price and CTA bands are reveal-on-scroll and fill in when scrolled (visibility:hidden descendants, not an empty section); /plans listing CSS/JSON/Tailwind tokens as Soon while the export panel appeared to offer them was a 70-character truncation in the measuring script, not a contradiction — all four unbuilt formats are badged Soon in BOTH places and the action button disables and relabels itself “Export — Soon”; the Monthly/Yearly controls in Settings are their own tablist with their own aria-label, not eight sections in one; the 7-day free trial is really wired, yearly-only, at api/create-checkout.js trial_period_days = 7; every Soon tool route renders the workshop state rather than a 404; and /admin’s “Server admin verification failed” is vite preview having no serverless functions, not a product defect. UPDATE 2026-09-08 (evening): THE SIX SURFACES BORN OR CHANGED ON 2026-09-07 GOT THEIR FIRST RENDERED PASS — Brand Starter, the sync notice, the Palette Library’s mood filters and closing CTA, the homepage export section, /settings and /projects — at 320/390/430/768/1024/1097/1120/1136/1280/1440/1920, light and dark, reduced-motion on and off, signed out, free, Pro, quota-used, loading, error and keyboard-only: 800 rendered cells, 0 page errors, 0 render failures. Assertions live in tests/user-sim/65-new-surfaces-breakpoints.spec.js (13 tests, each watched fail with its fix reverted: 12 red with everything reverted, the light-theme palette test green by design; then the two SyncNotice fixes reverted one at a time to prove attribution). TEN FIXED, in the order a user would meet them: (1) the sync notice was NEVER MOUNTED ON THE CREATE TOOLS — inside AppInner’s shell, below the CHROMELESS_PATHS early return — so a refused sync showed on /projects and /discover/palettes and was absent from the DOM on /create/palette, /create/type-scale, /create/font-pair, /create/contrast, /create/tint and /create/auto-builder, which is the opposite of what its own header promised; hoisted beside BillingBanner and OfflineBanner. (2) It painted over the offline banner (same fixed top-centre slot, z-index 130 over 119) at every width, saying “this device looks offline” on top of the banner already saying so; it now yields while offline and returns, with Try again, when the connection does. (3) Its ≤520px wrap never fired — the message is flex:1;min-width:0 and shrank instead of the actions wrapping: 11 lines in a 230px column beside Try again at 320, 7 at 390; the message now takes the row less the icon and the actions drop beneath (5 lines at 320, 4 at 390). (4) Brand Starter: a 429 rendered the exhausted sentence TWICE — the wall strip above the field and a red alert beneath it carrying a Try again that could never succeed; the wall alone now, and the brief is kept. (5) Brand Starter targets under the 24px floor at every width: the wall’s “See what Pro includes” 23.1px and Dismiss 21.4px, the result’s three “Open in …” links 23.1px — 58-target-size-24’s inline exemption would have excused all five because their wrappers’ text is longer than theirs. (6) /settings › Privacy & legal: the two pills were an inline flex with no wrap, 284px in a 250px card body at 320, so “Terms of service” ran under the card edge and the feedback button. (7) /settings › Account: the ≤640 profile rules at the top of settings.css were DEAD — the base rules come later at equal specificity, the same trap the file documents for its own section nav — so at 320 the name/email column was 81px of a 250px row beside Sign out, the name broke in two and the email overflowed; the rules now sit after the base rules and Sign out drops to its own row. (8) /settings › Accessibility: the Theme control is a fixed 250px and beside its label ran 23px past the card at 320, cutting “System” to “Syste” with the label crushed into a five-line column; below 520 it takes the row under its label. (9) /projects: the card’s title control was 21.25px at EVERY width, not only at 390 as RELEASE-READINESS §4 recorded; 24px floor, §4 item closed. (10) In dark, --card and --bg-2 are the same hex (#191A1D), so the library tray’s sliding indicator and the mood menu’s active and hover rows painted nothing — rgb(25,26,29) on rgb(25,26,29), the pressed option told apart by text colour alone; dark-only step to --bg-3, light untouched. Plus the homepage export pill: “Open the export panel with this system →” needs 302px of the 286px a 320 phone gives it and pushed its arrow outside the pill; it wraps to two lines below 360 at the same 44px minimum. CHECKED AND LEFT AS THEY ARE, with the reason: the mood trigger’s active state (border + card fill, visible both themes); the closing CTA (no fill, one hairline, 44px btn-accent, clears the feedback button at every width, 120px above the footer — not a banner); the Palette Library toolbar (68px, one row, from 768 to 1920 including 1097–1136; no chip outside its tray at any width); the export section (one column to 980, two above, the page strip never overflows because the deck plans two sections, reduced-motion paints the grid at opacity 1 with no transform); Brand Starter loading (0 animated elements), keyboard order (nav → brief → Generate → Enter → focus lands on the result with a visible ring → the three hand-off links) and the result at every width. LEFT FOR THE FOUNDER: now that the notice reaches the tools, the fixed top-centre slot it shares with the offline banner sits over the Palette Builder’s sticky toolbar until dismissed — measured fully covering SYSTEM, Randomise, Pick seed colour, Seed colour hex, Undo and Reset at 320; those plus Save/export at 390; SYSTEM, TOOLS and Randomise Space at 1024; Image, Explore, Preview, Vision type and Gradient at 1280 — and the offline banner already covers the same controls today. Moving condition banners into the document flow is a shell change across the chromeless layouts, not a breakpoint fix; the cost of doing nothing is a dismiss press before those controls can be reached while sync is failing or the device is offline. Method note: the sweep’s generic geometry audit returned nothing for its first 400 cells because page.evaluate had been handed the function’s SOURCE STRING rather than a call — a sweep that reports zero faults has to be checked against one fault it is known to see before its silence means anything. 2026-09-09 PASS, THE SIX CORE TOOLS (/create/palette, type-scale, font-pair, contrast, tint, gradient) — tests/user-sim/68-core-tools-breakpoints.spec.js. Rendered at 320, 390, 430, 768, 1024, 1097, 1120, 1136, 1280, 1440 and 1920, light and dark, reduced motion on and off, signed out, free with two projects, at the three-project cap, Pro, and with an 89-character project name, with every panel, tab and popover open: 1,446 measured cells. No horizontal overflow anywhere, no control without a focus ring, and the 1097–1136 band clean on all six toolbars (57px, no rail scroll). Fourteen fixes, each pinned by a rendered test and watched fail with its fix reverted: (1) usePopover’s ‘clamp’ alignment centred a panel on its trigger, so the seed picker and the gradient stop picker at 320 rendered from x=-24 (‘ICK SEED COLOUR’) — it now pins the panel 8px from the viewport; (2) the palette’s tints/HCT panel on the first column sat at x=-10 at 1024 and x=-2 at 1097 — clamped 8px inside the column; (3) Save / export on a desktop, signed in: the menu hung left off the row’s last control and ran 109px past the viewport at 1440, 397px with the long name because the box grew to 552px — hung right and fixed at 280px; (4) Type Scale and Font Pair at the cap on a 390 phone: the save menu opened upward and its wall heading sat 24px above the screen — a bottom sheet below 561px, the palette’s own idiom; (5) Font Pair’s specimen heading broke ‘Typography’ mid-word at 320 and 360 (a 235px word in a 188/228px column); (6) the FREE badge in the colour-system menu at 2.69:1 light / 4.07:1 dark; (7) the swatch menu’s Remove at 4.38:1 / 3.97:1; (8) the colour picker’s hue slider at 22px on all four tools that mount it; (9) ‘Use →’ in the Explore popup at 20px; (10) the primary Copy button’s hover on Type Scale, Tint and Font Pair put white on #4D90FF at 3.11:1; (11) the gradient inspector’s Copy faded to 3.64:1 on hover; (12) the gradient CSS block split ‘#7C3AED’ across two lines at 390 and 1024–1136; (13) every gradient import card’s meta line (‘Saved project · 3 colours’, 123px in a 118px column) ended in an ellipsis at 1280–1920; (14) Tint’s endpoints checkbox row was a 17px target. LEFT, WITH REASONS: the gradient stop handle at 20x20 (57’s recorded design call); the Explore popup’s 20px stripes (the same action as the 24px ‘Use →’ beside them); the locked variation rows at 0.55 opacity (a disabled tease, exempt); the toast over the palette slider footer and over the sticky config panels at 1440 (transient, pointer-events none, beside the action that raised it); and the Pro modal’s price-error block running 290–415px below a 320–430 phone screen when opened from the HCT button — ProUpgradeModal is another lane’s file this week, so flagged, not touched. Method note: two ‘findings’ were killed by re-measuring in a clean state — a clipped Reset label and a ring on Save / export were both the sweep’s own Escape keypress caught mid-transition — and the hue slider first measured 23.6px because the popover was still scaling in at .985; the spec settles on the animation layer for that reason. UPDATE 2026-09-09 (PR: media, icon and content tools): the pass over /create/icons, /create/emoji, /create/file-converter, /create/alt-text, /seo and /create/semantic-color — 320, 390, 430, 768, 1024, 1097, 1120, 1136, 1280, 1440 and 1920, both themes, reduced motion on and off, signed out then free and Pro, with a file dropped, loading, refused, results present, every panel open, keyboard through the primary action. 264 signed-out cells and 480 state cells measured for horizontal overflow, controls under 24px (58’s exemption rules), clipped or mid-word text, overlapping fixed elements, focus rings, contrast of own text against its painted ground, and popovers leaving the viewport. No horizontal overflow and no fixed-element overlap anywhere. TEN RENDERED FIXES, each with a test in tests/user-sim/71-media-tools-breakpoints.spec.js seen red with the fix reverted: (1) /create/icons — the masthead pill read navigator.onLine, so with every Iconify host refusing it said “Live library connected” in green above the notice saying the service could not be reached; IconLibrary now reports the same loadError flag that renders the notice and the pill says “Built-in icons” (the grid’s own status line, reused) with the amber dot, every width both themes. (2) /create/emoji — the skin-tone popover was left:0 on the toolbar’s last control: 16px past the viewport at 768, 149px at 1024 and 1280, cut off at 1440 and 1920 (the last two tones unreachable); it joins usePopover and flips at the edge, so Escape, outside-press and focus-in now match the app’s other popovers too. (3) .emoji-section-count and (4) .fc-soon were --t3 on the --bg-3 chip, 4.07:1 in light at 10px and 8px; --t2 gives 4.95:1. (5) /create/alt-text — the error MESSAGE and the failed CARD shared the class alt-card-error, so the message’s tint, 8px padding, 10px radius and 12px type were painted on the whole card; the message has its own class. (6) generateAll toasted “Generated N alt texts” for N it TRIED, so a 500 and a 429 both ended with a green tick over a card saying the opposite; it counts what landed and says nothing when nothing did. (7) /seo Structured data — each “Remove question” was 18x18; 24px minimum, glyph unchanged. (8) /create/semantic-color — the custom-hue range was a 14px-tall control because the gradient sat on the input; the input is 24px of transparent box and the track pseudo-element paints the same 12px bar. (9) /create/file-converter — the batch size-change line was --ok on the page ground, 4.32:1 at 12px in light; sizeDelta uses the -strong inks. (10) Video → GIF — the 32 MB engine download was a spinner and one sentence for its whole duration; the wasm is now read as a stream and the status line shows bytes against the total with a determinate bar (only while the count is consistent with Content-Length — a gzipped transfer reports the compressed size), reusing .fc-progress. @ffmpeg/util’s own progress mode was NOT used: it throws on a byte-count mismatch and then re-reads a consumed body. LEFT, WITH REASONS: the five Live-UI-proof buttons in the semantic-colour scenes (.stc-sc-ghost 21px, .stc-sc-link 14px) are illustrations with tabIndex -1 and cursor default that do nothing — making them 24px would enlarge the miniature they are drawn in, and pointer-events:none would move the 64 baseline for that route for no paint change; flagged for a decision. “Go Pro for unlimited” on My Icons is a 17px-tall button inside a sentence — WCAG 2.5.8’s inline exception, though 58’s implementation only exempts <a>. pre.seo-tags-code breaks content="summary_large_image" mid-token at 320 under overflow-wrap:anywhere — a code block with a Copy control; a scroller would hide the tail on a phone. The colour picker’s hue input (.cpk-hue, shared component) is under 24px on the JPEG background control; not this lane’s file. The pill on the EMOJI tab still says “Live library connected” for a library that is built in — no existing sentence fits and none was invented. .fc-soon stays 8px. Per-card “not converted” / size lines are 10px type. Method notes: the state sweep’s keyboard pass measured Tab counts from the last focused element, not from the top (blur() does not reset the sequential-focus start point), so its “reached in N” numbers are not distances; and the toolbar filter menus at 320–390 are trays, not popovers, so a “click the trigger” scenario times out there by design, not by fault.' },
  { id: 'hero-search-bar-and-animation', title: 'The hero search bar plays the typing animation, and loses its two framing lines', priority: 'P1', effort: 'M', area: 'Homepage', status: 'done', note: 'DONE. All three founder targets landed, plus a fourth defect the change exposed. (1) THE ANIMATION IS ON THE HERO BAR AND RUNS ON LOAD. HomeCommandBar.jsx drives the input’s own `placeholder` ATTRIBUTE rather than an overlaid span — the hero bar is a real <input>, so the browser hides the animation the instant the visitor types and there is no second piece of state to fall out of sync. PillNav needs a span because its “field” is a button with no placeholder to animate; that is the only reason the two differ. Typing starts at 1200ms, after .hcmd’s own entrance (.52s + .62s) has finished, so it reads as a sequence rather than a collision and no timer touches the first-paint path. It pauses on focus — a placeholder moving under a live caret is noise at the moment someone has decided to type. (2) THE CHIPS ARE GONE and their JOB was reassigned, which is the only thing that keeps the animation from being decoration: the cycled terms are SELECTED FROM THE REGISTRY by `searchHints` in data/tools.jsx (non-alpha, /create/ only, label ≤ 20 chars so nothing truncates at 390px), never hand-written. PillNav’s SEARCH_HINTS is still a hand-kept array and is one rename away from advertising a tool that does not exist — worth fixing there too. (3) THE LINE ABOVE IS GONE, THE ACCESSIBLE NAME IS NOT. The <p> stays at the same point in the reading order carrying id=home-search-label, now .sr-only. Deleting it would have been a WCAG 4.1.2 failure that no screenshot could show. (4) FOUND WHILE VERIFYING, NOT LOOKED FOR: the global `input[type="text"]` reset at global.css:573 has specificity 0,1,1 and was outranking `.hcmd-input` at 0,1,0, so the hero bar had been painting a SECOND white 1px-bordered rounded box inside itself — and a second focus ring inside .hcmd-bar’s — and rendering at the reset’s 13px instead of --fs-body, on every viewport since the bar shipped. Fixed with the `input.` prefix, the same idiom input.cs-hue-slider already uses six hundred lines above for the same reason. Worth a sweep: any other bare-class rule on a text input in this sheet has the same bug. REDUCED MOTION gets a decision rather than a default — not the animation slowed, not an empty box, but the same registry-derived names said once and held still, so the preference costs the effect and not the information. TESTS, all mutation-verified against a rebuilt production bundle: the two assertions that pinned “the bar sits below its label” and “accessible name equals the visible label” were rewritten to the new contract, not deleted — they now pin that the name RESOLVES, is non-empty, and is carried by something unpainted, so a future edit cannot satisfy the founder’s half by breaking the accessibility half. Three new tests cover typing without any pointer event, every typed term returning a real result, and reduced motion staying still.' },
  { id: 'hero-copy-still-reads-ai', title: 'The hero copy still reads as AI-generated, on the third report', priority: 'P1', effort: 'M', area: 'Homepage', status: 'done', note: 'DIRECTIONS READY, BLOCKED ON THE FOUNDER’S PICK — deliberately not shipped as a fourth rewrite. DIAGNOSIS FIRST, because the obvious move has already failed twice. #290 answered the second report by rewriting the words INSIDE this hero against the anti-slop bar, and the complaint survived it. That makes #290 a control experiment that has already been run: same structure, different words, same verdict. The variable is therefore not the words. WHAT THE STRUCTURE ACTUALLY IS, measured on the shipped hero at 1440: SIX text elements (kicker, two headline lines, subtitle, search label, reassurance line), each in a different type treatment, every one centred on a single axis, with a <mark> highlight on the final phrase and a micro reassurance line under the buttons. That is not a UIL4B invention — Mobbin’s beehiiv capture is the same arrangement down to the highlight on the last word and the small reassurance under the CTA, and beehiiv sells newsletters. Campsite, ClickUp and Klarna all run the same centred headline/subtitle/search stack. A visitor parses the SHAPE before the words, so words placed inside a shape that is this widely reused inherit its read no matter how good they are. That is why #290 could not have worked. SECONDARY, and real but smaller: the h1 is a tricolon plus a relative clause (“Colour, type and tokens / that stay one system”), which is the single most recognisable machine sentence shape; the subtitle is one balanced clause on a “so” hinge followed by a two-word fragment; and the hero carries TWO separate reassurance lines (“Nothing to install” and “Free to use. No card.”) saying the same thing twice. THREE DIRECTIONS, live at /?hero=a|b|c, shipped hero untouched and still the default. A — the bar opens the page: the instrument becomes the largest object (760px, 84px tall) and the headline drops to a caption in body weight; reference is Grok, which puts its input first and demotes the explanatory line to the foot of the fold. The argument for it here is that the sentence is TRUE of this product in a way it is not for most sites that try it — the bar queries the real registry and its rows are real routes, so a generated hero has nothing it could put in that slot. B — off the centre axis: left-aligned, measure-constrained, right of the fold deliberately empty; reference is Antimetal and Retool. Kills the bilateral symmetry, which is the most identifiable single property of the template, and changes the sentence SHAPE too — two flat declaratives instead of one balanced clause. C — subtraction: no kicker, no subtitle, no <mark>, no reassurance line, no second button. Tests whether the arrangement was ever the problem by deleting nearly all of it. C’S HEADLINE IS AN EXPLICIT ON-SCREEN PLACEHOLDER AND MUST NOT SHIP AS WRITTEN. The founder has asked for “real australian style english to sound like me not an AI written statement”, so writing a sentence in that register would be an agent imitating his voice — the exact failure this item is about. Nothing anywhere in these directions invents biography, metrics or testimonials. VERIFIED at 390, 640, 834 and 1440 with real device metrics: zero horizontal overflow in all sixteen captures, and every variant keeps the search box’s sr-only accessible name, so no direction proposes an accessibility regression. WHEN A DIRECTION IS CHOSEN: delete src/components/HomeHeroDirections.jsx and its CSS block, unwrap the one element in Home.jsx, and MOVE the shipped hero’s assertions in tests/user-sim/10-home-chaos-to-calm.spec.js onto the winner — the one-<mark> check and the headline-names-the-product check are written against the current hero and A, B and C all drop the mark. RESOLVED IN SHAPE, PENDING ONE YES. Founder, 2026-09-07, on being offered the three directions: “Retire it, V2 hero decides.” The ?hero=a|b|c explorer, src/components/HomeHeroDirections.jsx and its CSS block (.home-hero--dir, .hero-dir-*, .hero-a-*, .hero-b-*, .hero-c-*) are DELETED. On the words he chose the other option offered: “build one from my words only”. The shipped h1 is spliced from two of his own 2026-09-07 sentences and contains no word that is not in one of them — “Build and export UI and brand design kits” is the verbatim head of “build and export UI and brand design kits and content for website building”, and “in one unified location” is the verbatim tail of “all the design tools your constantly searching for in one unified location”. The comma is punctuation. Both sources, and the only two edits applied to them (“your” → “you’re”, sentence case), are recorded in src/data/positioning.js and docs/reference/positioning.md; tests/unit/positioning-truth.test.js proves the splice mechanically rather than asserting it in a comment, and 10-home-chaos-to-calm pins the RENDERED h1 to that module. THE ONE THING LEFT IS HIS YES OR NO ON THE ASSEMBLED LINE — it is flagged as such in the PR and nowhere on the page claims he has approved it. If the answer is no, the replacement is another splice of these same sentences, not an agent sentence. Also gone with the rewrite: the “UI system toolkit” kicker and the “Free to use. No card.” reassurance, both retired by him the same day as “a huge AI Slop feature” — so the six-element centred stack this item diagnosed is now four. — — — ANSWERED 2026-09-10 (feat/founder-calls-0910). He said ship it. The assembled line is approved as it stands, unchanged to the character, and the pending-approval framing is off positioning.js, docs/reference/positioning.md, RELEASE-READINESS item 10, Home.jsx’s hero comment and 10-home-chaos-to-calm’s message; positioning-truth.test.js now PINS the exact sentence and its three pieces as literals, tied back to the FOUNDER constants, because the splice check it already had passes for any other splice of the same lines and so could not protect an approved sentence. See founder-calls-2026-09-10.' },
  { id: 'home-v2-influence-and-export-section', title: 'Homepage: the founder’s words as one positioning source, the export kit section, and a hero band with a real peek', priority: 'P1', effort: 'L', area: 'Homepage', status: 'review', note: 'Founder, 2026-09-07, four instructions in one day. (1) The “no credit card required” and “UI system toolkit” taglines removed site-wide — hero kicker, hero micro-line, SystemCTA’s DEFAULT hint, LoginPopup, ColorLanding, three places on /plans, and both HomeHeroDirections under-lines. Removed, never reworded: “a fresh tagline in the same slot is the same feature”. The /plans FAQ “Do I need a card to use Free?” is KEPT deliberately — the user’s own question at the point they ask it — with its answer reworded. (2) docs/reference/positioning.md and src/data/positioning.js now carry his four sentences verbatim as the canonical value proposition; the hero sub-line, the tools-section heading and the share cards derive from them by id. /plans, /help and llms.txt are NOT yet derived — the mapping exists in SURFACE_LINE, the wiring is outstanding and is the next slice. (3) The export kit section, after the tools grid, renders a real page of the real brand-guidelines deck from HomeWorkbench’s live palette and type scale through a new read-only onSystemChange prop; formats derive from exportFormats.js so nothing unbuilt can appear. Format DESCRIPTIONS are deliberately not rendered — the book’s desc ends “and every token” and P-019 rule R keeps that word past the export boundary only. (4) #hsteps-title is his sentence with two proofreading edits. MEASURED: twenty widths, both themes, fresh context per width — zero horizontal scroll, zero overflow, zero sub-24px targets, zero clipped labels. The hero floor came down from min(100svh,980px) to min(78svh,760px) after measuring 0px of the next section visible at 1080/1180/1280/1366/1536; now 165-320px everywhere. 37 new text nodes pass contrast in both themes. TWO TEST HOLES FOUND BY MUTATION, both fatal and neither visible by reading: prerenderRoutes() does not include “/”, so the tagline walk skipped the one page the taglines lived on; and innerText is blind to every [data-reveal] section because GSAP autoAlpha sets visibility:hidden. — — — 2026-09-08, THE OUTSTANDING SLICE LANDED (positioning-derived-surfaces): /plans, /help and llms.txt now derive from positioning.js. /plans renders line(SURFACE_LINE.plansFraming) as its own paragraph above the lede; /help opens its lede on line(SURFACE_LINE.helpOpening); public/llms.txt is written by scripts/llms-txt.mjs from SURFACE_LINE.llmsSummary plus the config truth tables, and prerender.mjs writes the served dist/llms.txt from the same function. positioning-truth.test.js now covers all three surfaces and fails if any SURFACE_LINE key has no consumer. No new sentence was written anywhere: the /plans SystemCTA title ("Build first. Upgrade when your workflow asks for it.") and the /help H1 are typed lines positioning.js has no replacement for, left in place and flagged in the PR for the founder.' },
  { id: 'mega-menu-shows-the-product', title: 'The mega menu is correct and still reads as AI slop', priority: 'P1', effort: 'M', area: 'Navigation', status: 'done', note: 'DONE. FIRST, A CORRECTION FOR WHOEVER READS THIS NEXT: this item did not exist when the work started. The brief named the id mega-menu-feels-generic and said its note here was detailed; there was no such row in this file, and the only mega-menu item was [mega-menu-design], which reads done. The entry was written BY the work rather than found by it, so nothing above was verified against a founder-authored note. #340 WAS NOT REDONE. Its height budget, wrapping labels, three-column Discover, 54px rows, hover-scoping and derived CREATE_MENU are all untouched and still hold; the panel is correct and was still generic, which is a different fault. TWO OF THE THREE CAUSES TURNED OUT TO BE DEFECTS RATHER THAN TASTE. (1) THE CARD DREW A PICTURE OF A UI INSTEAD OF SHOWING ONE. PromoMock rendered grey rect bars standing in for text beside coloured rectangles standing in for swatches -- in a menu that leads to 64 real palettes and 100 real gradients. At the card’s 260px that reads as a component which failed to load. #340 had already proved the fix on this same product: the eight /discover cards now lead with real contents. This is that move applied to the last surface still showing the fake version. Create shows a real GALLERY_PALETTES entry plus the two families that genuinely ship; Discover shows three real gradients at their STORED angles via gradientCss. LEARN GETS NO PREVIEW AND NO FRAME, deliberately -- it is honestly unbuilt, and withholding the preview is what makes live and unbuilt read apart without hunting for a badge. (2) THE DESKTOP PANEL WAS DROPPING THE PRODUCT’S OWN ANSWER TO THE FOUNDER’S QUESTION. The complaint was that nothing says which tool to open first. NAV_SECTIONS already carries promo.guide and promo.cta for Create, the MOBILE SHEET already renders a Build a brand kit button calling launchBrandKit -- and the desktop card ignored both, substituting one link to viewAllHref, which for Create is /sitemap. So a card headed Build your brand kit, step by step offered the sitemap. Desktop now renders the guided path DERIVED from UIKIT_STEPS (never a literal list -- that would be the eighth hand-kept array in a codebase that has deleted seven) and the button that launches it. Verified /create/color is the merged studio and NOT one of the redirect-only category homes, so the flow opens a page instead of bouncing. (3) AN ACCESSIBILITY DEFECT FOUND WHILE THERE, older than this pass: the editorial link and the footer link were KEYBOARD-UNREACHABLE whenever the menu was open. The Tab bridge sends the trigger straight to the first TOOL, Shift+Tab from the first tool back to the trigger, and Tab off the last tool closes the menu -- so both were jumped over in both directions and the card’s only call to action could not be reached at all. Both carry data-pnav-menuitem now, and because the card precedes the columns in DOM order the ring opens on the recommended starting point, which is what ArrowDown should land on. THAT FIX REQUIRED A SECOND ONE, AND IT IS THE SUBTLE PART. megaItems() used a raw querySelectorAll, which happily returns elements inside a display:none subtree, and .focus() on one of those is a SILENT no-op. .pnav-editorial is display:none below 1240px and now holds the first item, so between 769 and 1240 ArrowDown would have appeared to do nothing at all. Filtered on getClientRects(), which unlike offsetParent stays correct inside a position:fixed panel. onTriggerKeyDown held its OWN copy of the raw query and was fixed in the same change; measured at 1200px, the raw list is 21 items starting with the hidden button and the filtered ring is 19 starting with Palette. COLUMN HEADS: six identical accent rules sat above icons that were already hue-coded, so the panel read as one undifferentiated field. data-hue is read off the column’s own tools -- no new data, Discover and Learn fall back to accent -- and the RULE takes the hue while the label stays --t1, because those tokens are measured for non-text contrast and #331 swept small accent text out of this surface. COST: ZERO BYTES, measured rather than assumed. Both gallery modules were ALREADY in the main bundle (grep the built asset for a known palette id and a known gradient id -- paletteLibrary and SurfaceLanding pull them in), so importing them into the nav adds nothing, and the main bundle got SMALLER because three inline SVGs came out: 456.65 to 455.18 kB, gzip 143.73 to 143.69. MOBBIN: Assembly Coffee’s open mega menu drove the weighted card -- a FEATURED lead entry carrying a live action with the rest of the list receding, rather than a tabulated block. Retool’s 01-05 nav sequence and Homerun’s 5 steps rail drove the numbered path collapsed to number plus label. Discord’s brand-colour page drove treating a swatch as a specification rather than a decorative chip. Steep’s help centre was considered for replacing the row descriptions with concrete named chips and DELIBERATELY NOT TAKEN -- see below. TESTS: tests/user-sim/43-mega-menu-contents.spec.js, 10 tests. All FIFTEEN keyboard behaviours are written down for the first time -- #340 verified them by hand and only three were ever committed as assertions, so twelve of them could have been removed by any refactor without a test going red. Nine mutations, all nine caught, plus a control that stays green. TWO OF THE NINE CAUGHT REAL WEAKNESSES IN THE FIRST DRAFT rather than confirming it: the arrow walk read items back out of the live DOM, so it could not see the ring SHRINK (it now asserts the first and last member BY NAME), and a hidden attribute on the CTA did not hide it at all, because .ui-pill sets display:inline-flex and beats [hidden]{display:none} -- worth knowing anywhere else in this codebase that reaches for hidden on a pill. NOT DONE, ON PURPOSE: the twelve tool rows are exactly as #340 left them. Re-weighting them would relitigate measured work (the 54px row, the one-line MENU_TOOL_COPY, the ragged column bottom that was checked against four references and left alone) while other agents move the same file. If the panel still reads flat after this, that is the next lever and Steep is the reference. ONE FOR THE FOUNDER: .pnav-editorial is display:none below 1240px, and #340’s reason was that the card is the decoration and the tools are the function. It is not decoration any more -- it is now the only place that says where to start -- so between 769 and 1240 the shortcut disappears while the tools remain. Left as-is rather than reopening the responsive work that 27 of 27 combinations rest on. The guided flow is still reachable there from the dashboard, and below 768 the mobile sheet has always had the button.' },
  { id: 'mega-menu-design', title: 'Improve the mega dropdown menus', priority: 'P1', effort: 'M', area: 'Navigation', status: 'done', note: 'SHIPPED in #340. Measured in a rendered browser at nine viewport sizes, then looked at. THE DEFECT A PERSON WOULD REPORT: the panel had max-height:none and overflow:hidden, so on any screen shorter than it the bottom was simply gone with no scrollbar to recover it (it is position:fixed and clips) — 71px and the whole footer lost on a 1366x768 laptop AND on an iPad in landscape (1024x768), 137px and three tool rows at 769x900. It now takes a height budget and the COLUMNS scroll inside it, so the footer stays put. SECOND: .pnav-tool-label was white-space:nowrap + text-overflow:ellipsis and the columns fall to 205px between 769 and 900, so the nav offered “Component De…” and “Landing-Page …”; labels wrap now and the description is the line that gives way. THIRD: Discover had three groups in a two-column grid with row-gap:0, so the third wrapped under the first and its “Your library” eyebrow landed flush against the Icon Library row — three columns now, plus a row-gap as a standing guard. Rows were 54/69/84px in one column because the Discover descriptions are page copy (one ran three lines in a nav row); every row is 54px. The editorial card was align-items:stretch with margin-top:auto on its link, leaving a measured 275px of blank card on Create and 209px on Discover; it is content-height and yields below 1240 where the panel is squeezed under its design width. Below 980 the groups FLOW into two columns rather than gridding, because a grid aligns rows and parked the short Colour stack above 190px of nothing. Touch: .pnav-trigger:hover is scoped to @media(hover:hover). Mobile sheet: the sticky 128px sign-up block sat on three tool rows with nothing to say it was a layer — hairline + scrim, worst-case fully-covered rows 3 to 1. STRUCTURAL, and the reason this touched data at all: CREATE_MENU was a SEVENTH copy of the tool list, not among the six [handkept-tool-lists-remaining] names — it repeated every tool id, label, route and soon flag beside the authoritative CREATE_GROUPS in the same file, and had already drifted (“AI Image Prompt” vs the tree’s “Image Prompt”). It is derived now; the menu owns only column grouping, order and icon. Two tests in search-index.test.js, all three mutations verified to fail. Mobbin drove the row and badge decisions (Higgsfield, Hers, Fiverr) and the content-height promo card (lululemon, Selfridges); a ragged column bottom was checked against Fiverr/Selfridges/Walmart/Hers and deliberately LEFT ALONE, so do not re-report it. Verified 27 of 27 viewport x menu combinations clean, and the full keyboard contract re-checked on the new markup and intact.' },
  { id: 'discover-section-ui', title: 'The Explore/Discover section needs a UI pass', priority: 'P1', effort: 'M', area: 'Discover', status: 'done', note: 'SHIPPED in #340. Ambiguity resolved: the homepage has no Discover SECTION, only an “Explore Discover” link, so the surface the founder meant is /discover (SurfaceLanding.jsx) — and the Discover MEGA-MENU, fixed in the same PR. Both improved. THE HERO: SurfaceLanding reuses .home-hero, which carries min-height:min(100svh,980px). The homepage earns that — it holds a search field, eleven satellites and the workbench. This hero holds an eyebrow, a heading, a lede, two buttons and a hint: measured at 1440x900 its content ended at 499px inside a 900px box, so 401px of the first screen of /discover was nothing at all. A .home-hero--surface modifier lets it be as tall as it is (401px void to 48px). Note for whoever touches it next: the modifier is written as a DOUBLED class because .home-hero is declared later in the sheet and would otherwise win at equal specificity — the single-class version was a dead letter. THE CARDS: the eight “Everything worth a tab” cards were eight identical white rectangles carrying a title and a sentence, and not one showed a single thing you could look at on a page whose whole job is browsing. The five live libraries now lead with their real contents, read from the gallery data the tools themselves use — three actual palettes, three actual gradients at their stored angles, a type specimen, the icon set, three real prompt titles — with a meta line (64 palettes, 100 gradients, 20 prompts) beside the call to action. Coming-soon cards get no preview, which is what makes live and unbuilt read apart without hunting for a badge. Card previews are keyed by group id in SurfaceLanding.jsx and DISCOVER_GROUPS was NOT touched, so this stays clear of [handkept-tool-lists-remaining]. Mobbin: Webflow’s Libraries grid is thumbnail-first with a count, and Tines varies card weight — hence also align-items:start, which removed 150px of dead white and a stranded Soon badge from the four unbuilt cards. Verified across 390/640/641/834/1024/1280/1440 on both /discover and /learn (the shell is shared), scrolling first so [data-reveal] actually fires: 14 of 14 captures clean.' },
  { id: 'dead-preview-modal-and-topbar', title: 'Delete UIPreviewModal, TopBar and Sidebar - all three were unreachable', priority: 'P1', effort: 'S', area: 'Design system', status: 'done', note: 'DONE. DELETED, on founder authority, rather than re-wired. UIPreviewModal.jsx (544 lines), TopBar.jsx (806 lines) and Sidebar.jsx (400 lines) are gone, with the 25 lines of .uip- CSS and one @keyframes fadeIn that only the modal used. RE-PROVED AT BUILD LEVEL BEFORE ANYTHING WAS TOUCHED, by the method the finding asked for: build, then grep dist/assets rather than reason about imports. Seven distinctive markers, chosen because they appear in no other file — uip-device-btn, uip-rounding-btn, nav-switch-trigger-label, profile-menu-avatar, sidebar-brand-sub, nav-pinned-empty, nav-cat-toggle — appear in ZERO built JS assets and ZERO prerendered HTML shells. All three components were tree-shaken out entirely. The .uip- CSS did ship: 15 uip- class families were present in dist/assets/index-*.css, because CSS is not tree-shaken. ONE CORRECTION TO THE ORIGINAL FINDING, and it is a nice one: this note used to say the string uip-modal appears in NO built JS asset. That is now FALSE, and it is false BECAUSE of this note. The only occurrences of uip-modal in any bundle were the two sentences in THIS FILE saying it does not occur, carried into dist/assets/Admin-*.js because pipeline.js is bundled into the Admin chunk. A grep proof written in prose falsifies itself once the prose ships. That is why the re-proof used markers taken from the components and found in no note. BUNDLE, before and after a clean build: CSS 688.21 kB gzip 111.00 kB → 685.33 kB gzip 110.59 kB. Total JS+CSS across 95 assets 3,425,741 → 3,422,861 bytes. The whole 2,880-byte saving is CSS, as predicted — no JS moved, because none of it was ever in a bundle. The real saving is not bytes: it is that three files nobody can reach stop being swept, restyled and focus-trapped by agents who do not know that. THE FOUR TEST DEPENDENCIES, four test CASES across three files, each decided on whether it covered anything still reachable. (1) modal-contract.test.js names UIPreviewModal in a dated 2026-08-15 measurement table. KEPT, and the row kept with it — editing a measurement to match today is how a record stops being one. Annotated instead. Its assertions walk src/ live, so the deletion needed no code change; the dialog population fell 17 → 16 against a floor of 10. (2) price-consistency.test.js carried a NOT_A_PLAN_PRICE entry for UIPreviewModal.jsx, excusing $18 — a real ladder amount — in a mock pricing card. The ENTRY was deleted with the component; the test is KEPT and still guards every live surface. (3) price-consistency.test.js also has no-allowlist-entry-outlives-the-thing-it-excuses, and THAT is what makes the entry safe to forget: verified by mutation, re-adding the stale entry turns it red. So the hole this item warned about could not have shipped. (4) auth-switch.test.js read TopBar.jsx as source text to assert the legacy login-popup gate was gone — a /login URL carrying a gate query flag, opened in a new window. PORTED, not deleted: the invariant is real and the file pin was the stale part. It now scans the whole of src/ for that flag with a 150-file population floor, which is stricter than the two file-pinned checks and cannot break the next time the nav is renamed — the exact failure that had just happened. Mutation-verified: writing the flag into PillNav.jsx turns it red. AND IT CAUGHT ITS AUTHOR FIRST: the first draft of THIS NOTE spelled the flag out, pipeline.js lives in src/, and the scan went red on the backlog file — the same self-falsifying-prose trap as the uip-modal claim above, found twice in one change. The note describes the flag now; the test says so in its failure message, so the next person to hit it loses a minute rather than an hour. GATE, against a baseline taken on origin/main in this worktree rather than trusted from the brief: lint 0 errors and warnings 31 → 30 (one warning lived in a deleted file); build clean, prerender 28 route shells + a noindex 404 shell (19 section share card, 8 BreadcrumbList); unit 935/935 pass 0 fail 0 skipped, unchanged either side, which is the point — no coverage of anything real went with the code; users 414 passed 13 skipped 0 failed, identical to baseline, all 13 skips the pre-existing ones in 12-ui-system-builder.spec.js. NOT DONE, and left deliberately. Deleting Sidebar orphaned some exports it was the last consumer of — toolsByCategory and groupBySubcategory in data/tools.jsx, resolveSection in data/sections.jsx. They are unused exports now, invisible to eslint, and pruning a derived registry is not a dead-chrome change. redirects.test.js still describes tools.jsx as the stale Sidebar/search registry, which is now orphaned commentary. Neither is a defect; both are the same kind of tax this item was about, one level down.' },
  { id: 'ui-preview-designs', title: 'Raise the quality of the UI preview designs', priority: 'P1', effort: 'M', area: 'Product', status: 'done', note: 'DONE in #341, for the surface a visitor can actually reach. INVENTORY FIRST, AND THE HEADLINE IS WHAT IS NOT REACHABLE: UIPreviewModal.jsx, the surface this item named first, is DEAD CODE. It is imported by exactly one file, TopBar.jsx, and nothing renders <TopBar> - App.jsx retired the Sidebar + TopBar chrome for PillNav. Confirmed at build level: uip-modal appears in NO built JS asset, so all 544 lines are tree-shaken out, though 25 lines of .uip- CSS still ship because CSS is not. Agents have kept maintaining it without noticing - #258 gave it a focus trap, #331 touched TopBar CSS, price-consistency.test.js carries an allowlist entry for its mock pricing card. It was NOT redesigned, because redesigning it would improve nothing anyone can see; see dead-preview-modal-and-topbar for the decision. WHAT SHIPPED, on the home workbench preview panel (the live surface): (1) the Palette mode stopped being a swatch grid pretending to be a UI. It previewed a palette as five labelled rectangles under app chrome reading UIL4B / Create / Palette and a LIVE PREVIEW dot. Mobbin is unanimous - v0 previews tokens on a revenue chart, an account form and a payments table; Lovable on a metric card, a payment-method chooser and an issue form; GitBook on a whole rendered docs site; Gamma on a page with a primary/secondary button pair. None previews a palette as a colour row, because the row cannot answer the only question a preview is for: not what are these five colours but do they COMPOSE. The swatch row kept every control and every aria-label and moved to the controls zone; above it sits a real product card - area chart, three status rows, filled primary, app mark, avatar, outlined secondary. (2) #269’s C4 extracted: OPTIONS became rails of pill tiles with aria-pressed and an inverted active state (Lovable’s Card/PayPal/Apple, Stitch’s Light/Dark), PROPERTIES became label-left rows with an editable numeric readout (Salesforce prints 58 at the right end of its Border Radius track, MagicPath right-aligns the rem value beside every type-scale step). The three per-mode splits were keyed to VIEWPORT width so they stayed two-column inside a ~590px sticky column; they are gone. TWO CORRECTIONS TO #269, because main moved under it: .hw-num is no longer qualified as input.hw-num - #331 moved the input reset inside :where() so a bare class wins, and keeping the qualification would teach a rule that is no longer true; and the zone capping was rebuilt rather than copied, because #269’s version rests on #262’s fixed frame, which is still unmerged. THE PART WORTH REMEMBERING: stacking one-column made every panel taller (image hit 966px against an 852px budget at 1440x900) and a position:sticky panel taller than its viewport hides its own hand-off. Capping the shell fixed that and EVERY MEASUREMENT THEN PASSED - panel inside budget, hand-off on screen, nothing overflowing - while the type ladder had quietly lost its CAPTION step and been sliced through BODY. Only looking caught it. A second silent clip of the same class was then found by probing: .hw-type-tools has overflow:hidden and was swallowing its third row while its container honestly reported scrollHeight === clientHeight. Both are now guarded by rendered tests (8a, 8b in 10-home-chaos-to-calm.spec.js), and 8a was VACUOUS on its first write - stamping data-theme on the element does not re-render React, so its dark-theme branch asserted nothing until it seeded localStorage vs-t and reloaded. Mutation-verified after the fix. ALSO CORRECTED IN REVIEW: the first version hand-rolled its own luminance sort and role assignment - a seventh copy of maths this codebase already has. It now calls derivePreviewRoles() from utils/colors.js, the same engine the Palette Builder and Colour Studio previews use, so one palette produces one set of roles wherever a visitor meets it, with AA self-healing and mode-awareness for free. LEFT DELIBERATELY, all already previewing on real UI: PaletteBuilder PreviewUI/PreviewBrand/PreviewGraphic, ColorStudio See it shipped, UiSystemLab LabCanvas, UIBuilder component previews, TypeScale article preview. ProHarmonyPreview stays swatch strips because the strips ARE the product there. Discover cards were rebuilt by #340 on this same principle and were left alone to avoid duplicating it. STILL OPEN: the panel-height swing across the five modes is 215px, down from 317px on main - making it CONSTANT is #262’s fixed frame and stays its own item.' },
  { id: 'design-system-book-export', title: 'Premium export: a design system book that looks studio-made', priority: 'P1', effort: 'L', area: 'Monetisation', status: 'done', note: 'DONE. Ships as a Pro export inside the EXISTING export path rather than beside it: a new “Design system book (PDF)” format in ExportPanel, so it inherits the dialog, focus trap and activation wiring that upgrade-activation-events already built. FORMAT — no PDF library, deliberately. jsPDF and pdf-lib cannot set the user’s own Google font without fetching and embedding a TTF at runtime, and the html2canvas route rasterises the type; a type specimen printed as a bitmap is exactly the generic dump the request existed to avoid. The browser’s own print engine embeds the real webfont, keeps type vector and selectable, and costs zero bundle, so the book is one self-contained HTML document with a true A4 print layout that the panel opens and prints. Lazy-loaded: a 28KB chunk, main bundle +30 bytes. STRUCTURE — 12 pages: cover, contents, then numbered sections 01 Colour (full-bleed opening, specification bands, contrast matrix, gradient), 02 Typography (families, scale), 03 Tokens, and a colophon. Section openings, a contents, a running foot with a folio and one grid on every page are what separate a book from a dump. REFERENCES — NASA Graphics Standards Manual for the section openings and for running the specimen full-bleed BEFORE the token table; designsystems.com for what a swatch must carry (name, role, three notations, reading ink), which is why bands are specifications and not chips; Müller-Brockmann for the single column system; Mobbin/Pitch for the inline named unlock at the moment of export; Mobbin/Gamma for the honest “export limitations” disclosure, which became the colophon’s What it does not cover block. ENTITLEMENT — fails closed: the check sits inside runExport() BEFORE anything is generated, not on the button, so a free user cannot reach the artefact by calling it directly; one pro flag drives both badge and gate; named gate: design-system-book-export rather than falling back to the modal title. No watermark and no degraded free book — the free tier keeps the style guide. A BUG THE TESTS FOUND before it shipped: slugRole stripped non-alphanumerics, so ANALOGOUS −30° and ANALOGOUS +30° both became analogous-30 and the second silently overrode the first, losing two colours from every analogous token file; the sign is now spelled out, with a uniqueness guard behind it. Proof: tests/unit/design-system-book.test.js (24), all verified by mutation — the gate test asserts ORDER, so moving the check below the build goes red. NOT DONE, and stated in the colophon rather than faked: logo construction, imagery direction, tone of voice and applied examples, because the design object holds none of them. A spacing section would need a spacing scale the app does not capture.' },
  { id: 'lenis-scroll-behavior-override-inert', title: 'The Lenis scroll-behavior override is scoped to a class Lenis only carries mid-scroll', priority: 'P2', effort: 'M', area: 'Accessibility', status: 'done', note: 'Handed to #331 by #329, which found it and left it because #331 owned global.css. #331 CONFIRMED IT AND DID NOT TAKE IT - the reasoning is below, and the next agent should read it before assuming the impact. CONFIRMED IN SOURCE: global.css sets html{scroll-behavior:smooth} unconditionally, and the Lenis override that is supposed to switch it off is scoped to .lenis.lenis-smooth. lenis@1.3.25 dist/lenis.mjs:1027 adds lenis-smooth only while this.isScrolling === smooth, i.e. DURING its own smooth scroll. It is a transient class, so the override is absent in the resting state and the rule does nothing for the case it was written for. CONFIRMED IN A RENDERED BROWSER: on /help with Lenis active, document.documentElement.className is exactly lenis (no lenis-smooth) and getComputedStyle(document.documentElement).scrollBehavior resolves to smooth. So the override is INERT, not merely narrow. NOT CONFIRMED, AND THIS IS THE PART TO BE CAREFUL WITH: pressing End and sampling window.scrollY every 40ms for 400ms showed the position land on the FIRST sample and never climb, i.e. the keyboard scroll was instant anyway - probably because Lenis intercepts the key and drives the scroll itself, which would make the mis-scoped rule latent rather than actively harmful. That probe is crude and should not be trusted either way. DO NOT SHIP A FIX ON THE SOURCE READING ALONE; establish the behaviour first. THE REDUCED-MOTION SIDE IS ALREADY CORRECT and was verified in the same run: with the browser reporting prefers-reduced-motion, Lenis is not initialised at all (html carries no lenis class), data-reduced-motion is true, and scroll-behavior resolves to auto. Nothing to do there. TWO CANDIDATE FIXES, AND THEIR BLAST RADIUS. (a) Widen the override to html.lenis, so native smooth scrolling is off whenever Lenis is the scroll engine. That is almost certainly the original intent, but it changes EVERY anchor jump and every scrollIntoView in the app: there are 15 scrollIntoView/scrollTo call sites in src/, and only 9 pass an explicit behavior, so the other 6 currently inherit their smoothness from the CSS and would start jumping. (b) Drop html{scroll-behavior:smooth} and pass behavior explicitly at those 6 call sites. More edits, but each one becomes readable at the call site instead of depending on a global. WHY #331 LEFT IT: it changes runtime behaviour rather than paint, so it needs its own behavioural matrix (keyboard scrolling, the reduced-motion clamp, all 15 scroll call sites, and the nav popover) rather than the computed-style and screenshot verification that pass used throughout. It also lands in the same area as [arrowkey-scroll-flake], which is still partial, and [popover-open-scroll-suppression]. Whoever takes it should take those three together. FIXED 2026-09-04. The override is scoped html.lenis now. `lenis` is on the root for as long as the instance is mounted (the className getter in dist/lenis.mjs:1021-1027 always starts with it), while lenis-smooth is appended only when isScrolling === "smooth". VERIFIED IN A RENDERED BROWSER: on /help with motion on the root class is exactly "lenis" and computed scroll-behavior went from smooth to auto. Regression-guarded in BOTH motion directions by tests/user-sim/45-scroll-engine-contract.spec.js, and that test was VERIFIED BY MUTATION - putting the .lenis.lenis-smooth scoping back fails the motion-on case and correctly leaves the reduced-motion case green, because the reduced-motion clamp reaches it by a different route. THE BLAST RADIUS RECORDED ABOVE IS WRONG, AND THIS IS THE PART TO CARRY FORWARD. It says widening the override changes every anchor jump and every scrollIntoView, and that 6 call sites would start jumping. scroll-behavior IS NOT AN INHERITED PROPERTY, so html{scroll-behavior:smooth} only ever governed the ROOT scroller. Measured in the browser rather than recalled: with html forced to smooth, document.body and a freshly built nested scroll container both compute auto. Every non-root call site - the FontBrowseDialog panel at :170, the DocsTOC scroller - was therefore never touched by the global. And `.main`, which App.jsx:218 scrolls, DOES NOT EXIST in the rendered DOM on any route checked, so that call site is dead code. The real blast radius is the root scroller alone, which Lenis owns with anchors: true. WHAT IS NOT PROVEN, AND IT IS A LIMIT OF THE HARNESS RATHER THAN A LOOSE END. The user-visible impact could not be measured, because CHROMIUM UNDER PLAYWRIGHT NEVER ANIMATES A NATIVE CSS SMOOTH SCROLL. Two independent instruments agree - per-frame scrollY sampling, and counting scroll events - and THE POSITIVE CONTROL FAILS IN BOTH: a bare page with no app and no Lenis, given an explicit window.scrollTo({ behavior: "smooth" }), arrives in ONE animation frame and fires exactly ONE scroll event, headless and headed alike. So "the keyboard scroll is instant anyway", measured here and in the paragraphs above, is an ARTIFACT of the harness and is not evidence about a real browser. Lenis’s own JS easing still animates and is still observable; it is the browser’s CSS smooth scrolling that is not. Anyone who wants the real impact figure has to drive a browser outside Playwright.' },
  { id: 'dgh-hero-stacked-dead-space', title: 'The shared Discover masthead is mostly empty when it stacks, and its reported overflow is decoration', priority: 'P2', effort: 'S', area: 'Discover', status: 'done', note: 'DONE (#331), and HALF OF IT WAS NOT A DEFECT - recorded in full so the next geometry sweep does not re-open it. THE REPORT: .dgh-hero reports scrollWidth minus clientWidth of 36px at 390 and 61px at 641, identically on /discover/palettes, /discover/gradients, /discover/prompts, /create/icons and /create/emoji, because DiscoverGalleryHero is shared by all five. THE CAUSE, proved rather than reasoned: .dgh-hero::before carries inset:auto -10% -65% 42%, which puts the decorative radial glow 10% past the right edge ON PURPOSE, where overflow:hidden on the same element clips it. Suppress ::before in the page and the figure is EXACTLY 0 at 390 / 480 / 641 / 834 / 1280; leave it and the figure scales with the viewport (36 / 45 / 61 / 79 / 124), which is the signature of a percentage inset and not of content spilling. The document itself never scrolls horizontally at any width. Nothing was clipped that was meant to be visible. WHAT WAS ACTUALLY WRONG, found by the same measurement: the @media(max-width:720px) block hides .dgh-mark - the 92px serif numeral the 390px desktop floor was sized around - and then RAISED min-height to 430px anyway. The layout with LESS in it got a TALLER box, and the difference was dead black above the copy: 125px at 390, 163px at 480 and 180px at 641, where it was 42% of the whole masthead. It also pushed the glow out of frame, so the one decorative gesture the hero has was invisible on a phone. The author was already reasoning about this exact number - .dgh-hero--controls{min-height:0} sits right below it with a comment explaining why - and fixed only the variant that stacks an aside, which is the one that needed it least. Now 300px: it binds where the copy is shortest (252px of content at 641) and stays out of the way where it is longest (307px at 390). Dead space after: 2px at 390, 33px at 480, 50px at 641. PINNED BY tests/user-sim/40-gallery-hero.spec.js, which asserts the SHAPE of the overflow rather than its absence - it suppresses ::before and requires the remainder to be 0, so the known decoration stops being re-reported while a real overflow still fails. Three mutations verified: restoring min-height:430px fails the dead-space test on both bands, widening .dgh-copy past its box fails the overflow test with the glow suppressed (62px and 58px), and min-height:0 fails the collapse guard.' },
  { id: 'flair-tone-contrast', title: 'The flair pill tones are theme-blind, like the category hues were', priority: 'P2', effort: 'S', area: 'Design system', status: 'done', note: 'DONE. Every named tone now has a light AND a dark value, solved by walking lightness at fixed hue and saturation until the worst real ground clears 4.60 on the pill’s own 12% tint. Before, worst per theme: blue 3.80/2.75, violet 4.14/2.54, rose 3.38/3.12, amber 2.44/4.22, green 3.71/2.82, slate 5.49/2.92, gold 2.78/3.72 — eleven of fourteen missed. THE GROUNDS WERE MEASURED, NOT ASSUMED: a pill was injected into the real rendered /settings and /community in both themes and its composited ground read back — light #FFFFFF and #EFEEE9, dark #191A1D and #101012, with #212327 carried as a margin for inset rows. That mattered: tuning against --bg-3/--bg-4 as well would have forced amber to #864904 and gold to #795015, which is brown, not amber. DO NOT DO WHAT THIS NOTE ORIGINALLY SUGGESTED. Moving the tones onto the --hue-* tokens would have imported a failure rather than fixing one — [accent-tint-grounds-unmeasured] measured --hue-component at 4.34:1 on its own 12% tint in the same pass. THREE DEFECTS NOBODY HAD RECORDED, all found here and all fixed. (1) .flair--gold paints on a linear-gradient, and every contrast walk in this repo returns null for a background-image on purpose, so gold was INVISIBLE to measurement in both themes and scored nothing at all; its dark end measured 3.61:1 once computed by hand. Its stops now resolve through --flair-c/--flair-warm so the dark override reaches them. (2) .flairpick-chip carried opacity:.62, which fades the LABEL as well as the fill and so cannot be compensated for by any choice of colour — 2.52:1 light and 2.80:1 dark, and only 3.68/3.92 even at .82. These are real buttons a user picks their flair from. The resting state now mutes the SURFACE (half the tint, lighter border) and leaves the ink alone. (3) .flairpick-chip.is-on set color:#fff on a solid tone fill, which was correct only while there were no dark tones — white on the dark set measures 2.81-2.87:1 across every tone, so adding them without inverting the ink would have CREATED the bug. --flair-fg is that token, the same shape as the existing --ok-fg/--warn-fg/--err-fg. Earned badges are also no longer dimmed to 50%: they are the one group nobody can act on and the copy above them exists so people know they exist, so they are marked unavailable by a dashed unfilled outline at full ink instead. PINNED BY tests/unit/flair-tone-contrast.test.js, which is a unit test on purpose: .flair renders only signed-in, which is exactly why #331 left this, and a test that cannot run is not a test. It is also the only place gold’s gradient is checked anywhere. Three mutations verified — reverting a dark tone, restoring the opacity, and weakening the gold stop each fail it.' },
  { id: 'accent-small-text-contrast', title: 'The brand blue fails AA as small text, and the fix already exists', priority: 'P1', effort: 'M', area: 'Design system', status: 'done', note: 'DONE (#331). The Director call was made and the sweep run. MEASURED, NOT READ: a Playwright walk over all 28 prerendered routes x 2 themes x 390/640/834/1280 that composites translucent ancestors to find each text node’s true ground and computes the WCAG ratio. Before: 44 distinct (class, colour, size) combinations under their AA floor, 202 node-instances. After: ZERO. WHAT MOVED. 167 rules setting color to --accent/--brand/--info on small text went to --accent-strong; 56 rules deliberately did NOT, because they colour icons, glyphs, pins and large display text where the floor is 3:1 and --accent clears it (the KEEP list is in the PR). 43 white-on-accent fills moved their background AND border to --accent-strong, because white on #0F6FFF is 4.43:1 - the same miss mirrored. 22 inline JSX style objects moved too; the CSS sweep cannot see those, and they are a second population nobody had enumerated. THREE THINGS THE SOURCE SWEEP ALONE WOULD HAVE MISSED, all found by rendering. (1) The Gradient tool aliases the token as --ggn-accent, so no grep for var(--accent) reaches its eyebrow, badge, step, copy button or gallery link; it now has a --ggn-accent-text alias for text while --ggn-accent stays the brand blue for fills, borders and the dial hand. .ggn-btn-accent, .ggn-seg-btn.is-on and .ggn-fmt-btn.is-on reached the readable colour ONLY ON HOVER, so the resting state was the unreadable one - the identical shape of bug .ui-pill-accent carried. (2) var(--hue,var(--accent)) falls back to the unreadable token on every surface with no [data-hue] ancestor, which is what .smap-soon actually paints. (3) .ic-stat-sm drops .ic-stat-num from 24px to 16px and inherited its colour: 24px only needs 3:1, 16px bold needs 4.5, and it measured 4.06. THE CATEGORY HUES WERE THE WORST OF IT. --hue-colour/-type/-component/-imagery/-ai/-icons sat in a :root block labelled theme-independent with NO DARK VALUES AT ALL, while being painted as 9-11px text. On --bg-3 they measured 3.04 / 2.88 / 2.76 / 3.13 / 2.61 / 2.87. The label was not laziness: :root and [data-theme=dark] are both (0,1,0) and the Foundry :root sits LATER in the file than the theme blocks at the top, so there was nowhere to put a dark value that worked. THE FIRST ATTEMPT AT THE FIX WAS DEAD FOR EXACTLY THAT REASON - the dark set was added to the early block, the sheet read correctly, and the rendered page still painted the light values. It only works from a Foundry dark block below that :root, and tests/unit/category-hue-contrast.test.js now asserts the ORDERING as well as the numbers. Two light hues also missed on the page ground (--hue-colour 4.45, --hue-imagery 4.32) and are a shade deeper; the other four were already >=4.71 and are untouched. Redrawing public/previews/*.png was required, because share-cards.test.js pins the committed OG cards to these tokens. PINNED BY: tests/user-sim/39-accent-contrast.spec.js walks real text nodes in both themes and at 390 and asserts a COMPUTED ratio, plus the mirror case, light ink on an accent fill, found by measurement rather than class list - the first draft named .btn-accent and passed VACUOUSLY, because /plans swaps that button out whenever Stripe is unreachable, which it always is in this suite. Four mutations verified: removing the dark hue set, moving it back above the Foundry :root, reverting one text swap and reverting one fill each fail it. ON THE prefers-contrast EVIDENCE ADDED HERE ON 2026-09-03: the token swap it named as the natural fix has now landed, so the users who told their OS they need more contrast get the readable blue like everyone else. What that note found is still OPEN on its own merits - global.css still contains ZERO prefers-contrast blocks, so the signal is still received and ignored; that stays with [prefers-contrast-unimplemented], and it is now about the greys and borders rather than the accent. NOT DONE, deliberately, and measured so nobody has to re-derive it: the .flair tones are theme-blind the same way the category hues were - .flair--blue #2563EB is 4.37 light / 3.18 dark on its own 12% tint, --violet 4.75 / 2.93, --rose 3.89 / 3.57 - but .flair renders only on /settings and in signed-in bylines, so a signed-out capture cannot verify a fix and this pass did not guess at one. The Docs*.jsx pages carry ~20 more inline accent styles and are UNROUTED dead code (nothing imports them), so they were skipped rather than edited. .plb-tgl--on is a dead rule in no component. See [flair-tone-contrast].' },
  { id: 'colorstudio-dead-sections', title: 'Delete ColorStudio\u2019s three sections that can never render', priority: 'P2', effort: 'M', area: 'Colour', status: 'done', note: 'DONE. DELETED, on founder authority. ColorStudio.jsx is 4258 lines to 1233 - the palette, systems and gradients sections, the merged-studio chrome, and every helper only they used. It is now a single-purpose page: the semantic-colour tool. REACHABILITY RE-PROVED IN A RENDERED BROWSER FIRST, all 16 /create/* routes loaded and inspected, not reasoned about. /create/semantic-color mounts ColorStudio and renders section ["states"] and nothing else. /create/ui IS A 404 - it renders the Page-not-found screen, which settles that question for good. /create/color renders ColorLanding, /create/palette PaletteBuilder, /create/gradient GradientGenerator, /create/tint TintTool, /create/contrast ContrastChecker. On the one live route, after a full scroll pass at 1440x900: ZERO nodes matching cs-csys, cs-pv, cs-sw or cs-grad, zero pill-nav items, and no palette, systems, gradients or visualizer section. REMOVED IN DEPENDENCY ORDER: the four dead JSX regions (628 lines); the three trailing popup renders (67), which sat at the component root rather than inside a section and so needed their own proof - setCsysOpen(true) appears ZERO times in the file, openCtxMenu had one caller and it was a dead touch handler, and openSwatchPopup was only ever called from inside CtxMenu own props, so none of the three could be opened; 35 module-scope declarations (1599 lines); 75 component-scope bindings; 15 unused imports; and the merged-studio chrome (127 lines) - SECTIONS, the collapse state, focusSection, the section IntersectionObserver and the pill-nav thumb machinery, all driving elements that no longer exist. TWO WITNESSES AND ONLY ONE OF THEM WORKED, which is the finding worth keeping. THE BUILD AND THE UNIT SUITE BOTH WENT GREEN ON A PAGE THAT RENDERED NOTHING. An automated statement removal cut a multi-line ternary short and left a dangling continuation, turning a useMemo into a conditional that read its own binding: Cannot access cbClash before initialization. Loading the page caught it in seconds; nothing else would have caught it before review. The file was then swept for every other statement of that shape and the only hits were one intact nested ternary. THE SECOND TRAP WAS THE SAME ONE THIS REPO ALREADY DOCUMENTS. The first reference pass stripped comments before counting; the naive block-comment regex read this file own accept="image/*" as an opening comment, blanked the lines to the next close, and reported PATH_TO_SECTION - referenced inside that window - as unreferenced. That is exactly the failure price-consistency.test.js was written for. Counting raw text instead cut the candidate list from 24 declarations to 9, all nine genuinely dead. Anyone automating a deletion here should count raw and let the extra survivors be found by hand. ESLINT IS A WEAK WITNESS IN THIS FILE and that is worth knowing before the next pass: no-unused-vars runs with varsIgnorePattern ^[A-Z_], which exempts every React component and every SCREAMING_CASE constant. Six unused imports were invisible to it, ColorPickerPop among them - which confirms the unified-colour-picker finding that all four of this page colour inputs were dead on every shipped route. no-undef is the half that does work, and it is what proves nothing deleted was still referenced. THREE GUARDS FIRED DURING THE WORK, all three correctly, none of them remembered in advance. (1) price-consistency no-allowlist-entry-outlives-the-thing-it-excuses caught the cs-pv-kpis entry the moment the preview scenes went - the same guard that caught UIPreviewModal earlier in this PR. (2) locale-tool-copy THE-ONE-THAT-MATTERS caught tools.colorStudio in all ten locale files: deleting the merged-studio hero removed the last direct t(tools.colorStudio.description) read in src, so the block is gone from de, en, en-US, es, fr, it, ja, ko, pt and zh. (3) that same file anti-vacuity test was PINNED TO THAT READER, and broke with it. Re-pointing it at whichever page reads a key today would only buy the next deletion the same failure, so it now proves the matcher against a FIXTURE - mutation-verified in both directions, too narrow and too broad. STALE COMMENTS CORRECTED, because each had already misled someone: CreateTool.jsx claimed /create/color was the full merged studio with its tools as sub-routes (App.jsx intercepts it and renders ColorLanding); discoverUtils.js and discoverResources.js both said preset is only honoured by ColorStudio, when every preset hand-off points at /create/gradient and GradientGenerator owns the reader - ColorStudio carried a second copy that nothing ever reached; and docs/reference/architecture.md still listed Sidebar, TopBar and UIPreviewModal. KEPT DELIBERATELY, and this is the line between dead and merely odd. The gradient state (stops, angle, type) survives as read-only seeds because the effect that writes it through to ProjectContext STILL RUNS on every mount. Nothing can change it any more, but removing it would change what a project stores, and that is a product decision rather than a dead-code one. The reason is recorded in the file beside it. FOUND AND NOT ACTED ON: the whole color.* locale block - about 60 keys in each of the ten files - now has ZERO readers, having had exactly one (color.title) before this change. No guard demands it and locale pruning is its own change; recorded here rather than widened into this diff. GATE, against a baseline taken on origin/main in this worktree: lint 0 errors, warnings 31 to 30; build clean, prerender 28 route shells + a noindex 404 shell; unit 935/935, 0 fail, 0 skipped; users 414 passed, 13 skipped, 0 failed, all 13 the pre-existing 12-ui-system-builder skips. Rendered check on /create/semantic-color before and after: 282 stc- nodes, 610 body nodes, 59 buttons, same h1 and three h2s, body text 4975 characters - unchanged.' },
  { id: 'prompt-library-hero-and-toolbar', title: 'The Prompt Library never joined the shared browse language', priority: 'P2', effort: 'M', area: 'Discover', status: 'done', note: 'DONE (#323), folded into [five-surface-heavy-review] because the founder named the Prompt Library there and the hero plus the toolbar IS the main defect on it — splitting one page across two PRs is worse than doing it once. THE HERO: it was the last surface on the site-wide .sec-h, and its eyebrow rendered from the SAME i18n key as the h1, so every locale printed the page title twice. Now on the shared DiscoverGalleryHero with a taxonomy-path eyebrow (Discover / Prompts) and a decorative mark, matching Palette, Gradient, Icon and Emoji. THE TOOLBAR: converged from the one-off .pl-toolbar / .pl-chip onto the shared LibraryToolbar plus two LibraryFilterGroups (sort, category), which is what let #318’s 641-980 collapse-to-a-menu reach it. MEASURED on this branch: 172px of toolbar at BOTH 641 and 834 against 80px at 1280, now 117px at 641 and under the 92px shared ceiling at 834. A REGRESSION THE SUITE CAUGHT AND THE FIX THAT FOLLOWED: the shared masthead is 430px tall, so with the action left in the toolbar the Submit prompt CTA fell out of a 320x800 viewport entirely — 21-reflow-320 has guarded that exact button since the 2026-08-11 audit found it off screen horizontally. The CTA moved into the hero’s action slot, which is where the sibling galleries already put theirs. ALSO REMOVED, an anti-slop find: .pl-card-text-hero::before painted an EMPTY 40x40 accent square on every card — an empty content value carrying centring rules for a glyph that was never added. On text-only cards it read as a thumbnail that had failed to load, twenty at a time, and it competed with the Pro lock badge for the same corner. Removed rather than filled: prompt categories are TAG_CATEGORIES, which has no glyph set, and the shared CategoryGlyph is keyed to the unrelated discoverCategories taxonomy — bridging them is a real task, not a drive-by. Dead CSS retired with zero consumers left: .pl-toolbar, .pl-search-wrap, .pl-search, .pl-search-clear, .pl-sort. .pl-chip and .pl-chips STAY — five non-browse surfaces still render them.' },
  { id: 'icon-pack-label-desktop', title: 'The icon grid still repeats a 9px pack label under every cell on desktop', priority: 'P2', effort: 'S', area: 'Tools', status: 'done', note: 'DONE, and both questions this item raised are answered with evidence rather than taste. IT REPRODUCED: at 1024, 1280 and 1440 all 120 cells on first paint rendered a `.ic-pack` label, every one reading "lucide", at a computed 9px in --t3. At 834 it was already suppressed, as #298 left it. QUESTION ONE, does a per-cell pack label earn its space when every visible cell says the same word. No. MOBBIN, PLATFORM WEB, searched fresh and it confirms what #298 found: Skiff (mobbin.com/screens/85ca7e92-1c51-401d-92ed-6942e6beaf47) and Craft (mobbin.com/screens/992cc0ba-f9d6-41e2-98d6-c624907d6593) give their icon pickers bare glyphs with no per-cell label at all; Notion (mobbin.com/screens/899cb181-5770-47d2-92c8-332ca9d9a733) heads the collection once and labels no cell; and Magnific (mobbin.com/screens/c215558c-23c8-415a-8b3d-917004d27adb), which has this exact job at this exact scale, states "Special Lineal by Freepik · 288.1k icons in this collection" ONCE above the grid and labels not one of 286k cells. Four references, none labelling the cell. The screenshot also shows the cost that was being paid for it: the line that DOES discriminate, the icon name, was truncating (align-horizontal-…, align-vertical-distribute-…) to make room for a word identical across the grid. THE FIX KEEPS THE HALF OF #298 THAT WAS RIGHT. #298 reserved the label for a mixed-pack result set; that was a content condition expressed as a width, so it now IS a content condition. IconLibrary.jsx renders `.ic-pack` only when the list actually holds more than one pack. Measured after: the default Lucide grid shows 120 cells and ZERO labels; the Brand logos set shows 120 cells and 120 labels across four distinct packs — simple-icons, logos, devicon, skill-icons — which is precisely the case where the word tells two similar-looking marks apart. The <=980px suppression stays on top of that, because a phone cell cannot afford a third line even when the word is useful. QUESTION TWO, does 9px clear the legibility floor. No, and the contrast half of the worry was measured rather than inherited. --t3 on the icon grid ground is 4.55:1 in light and 5.77:1 in dark, so it clears AA and stays — the 3.70:1 figure recorded against --t3 elsewhere in global.css is on --bg-3, a darker ground. SIZE is the real problem: 9px is under --fs-micro (11px), the smallest size the scale defines, and a tracked uppercase label is the least legible thing there is at that size. It goes to --fs-micro, which costs nothing now the label is rare. MUTATION-VERIFIED IN BOTH DIRECTIONS, because only the pair is the rule: forcing the label always on fails on "a single-pack grid must not repeat one pack name under every cell", and forcing it always off — the tempting "just delete it" shortcut — fails on "a mixed-pack result set must still name each cell’s pack".' },
  { id: 'gate-doc-hardcoded-counts', title: 'The gate doc hardcodes test counts and has drifted twice', priority: 'P2', effort: 'S', area: 'Docs', status: 'done', note: 'DONE — fix (a). The counts are gone and the gate is stated as five properties. The recommendation was right and nothing found argued against it; what the item did not know is that the evidence is stronger than it says. The table had drifted FOUR times, not two. The fourth was sitting in the file at the moment this was picked up and nobody had ever noticed it: the Firestore rules row read “24 tests (9 standalone + 5 × 3 parameterised)” against a real 37 — sixteen standalone, plus 5 × 3 entitlement fields and 2 × 3 submission kinds. A FIFTH was in the brief for this very task, which quoted 682 unit tests on main against main’s actual 687. Which is the whole argument in one line: a number nobody is accountable for goes stale between being written and being read, and it does it again while you are fixing it. The gate table is now 0 errors on lint, the build PRINTING its prerender line, 0 failures and 0 skipped on unit, and 0 failures on rules and acceptance. Nothing was weakened. Specifically on the route-shell line, because that was the risk: it is KEPT and the doc now says in as many words that it is load-bearing — a bare `npx vite build` writes dist/ and prints nothing, so that line is the only evidence scripts/prerender.mjs ran at all. What was dropped is the “27”. The route set is owned by the sitemap, and tests/unit/prerender-routes.test.js already goes red when the sitemap, routeMetaMap.js and vercel.json stop agreeing. That test is the feedback loop; a number in a doc never was one, and “27” would have gone stale the first time a route was added. Option (b) was rejected in writing rather than silently. Generating the counts, or asserting them in CI so a stale one fails, would also have stopped the drift — it buys permanent maintenance for a figure no reader has ever acted on. Nobody decides anything differently on 659 than on 682. ONE NUMBER SURVIVES and the doc says why: the lint ceiling of 31. The rule extracted from that is the useful part — a number belongs in a gate table only if it is a BOUND that work is not supposed to move. A test count grows with every PR that adds a test, so it can never qualify. A warning ceiling can, and so can the /api 12-function cap, because both are asymmetric: being out of date in the good direction cannot cry wolf. THE SAME FAULT ELSEWHERE, from the grep the item asked for. tests/user-sim/README.md carried a table of FOURTEEN spec files while thirty-seven exist, and signed off with “use the next free number (15-)” — taken for months, so a newcomer following the README would have collided with an existing file. It is now the naming rule plus `ls`, and the numbering instruction is “the next number after the highest one present”, which stays true. docs/build-plan/tool-tree.md restated “13 tests” for the skipped UI System suite — exactly the restatement the gate doc forbids in its own second sentence; the count is gone, the reason stays. CLAUDE.md pointed at the gate doc for “every gate baseline number (lint warnings, test counts)” and now points at what the gate requires. CHECKED AND DELIBERATELY LEFT ALONE: the 12-of-12 API function tally in architecture.md and tech-stack.md is a hard platform cap with tests/unit/account-deletion.test.js failing the build if it is exceeded, and the dated qa/ and design/ audits are point-in-time measurements that cannot go stale. Both are the correct pattern rather than the fault, and the doc now names them as the examples. Proof: tests/unit/gate-doc.test.js (10), which holds the shape in BOTH directions — it fails if a row states a test total again, and it fails if a pass condition is dropped, including the zero-skip clause, the prerender line, and the doc’s quoted line drifting from what scripts/prerender.mjs actually prints. Mutation-verified: 10 mutations, every one RED, each killing its own test.' },
  { id: 'icon-stroke-export-units', title: 'Icon export stroke width is in viewBox units, so it doubles at the default size', priority: 'P1', effort: 'M', area: 'Tools', status: 'done', note: 'DONE. The fix was a DECISION, and the decision taken was (a): the control MEANS PIXELS — the number the user sets is the number they measure in the exported file, at any size and any viewBox. src/utils/iconStroke.js owns the single conversion (strokeAttrForPx) and BOTH the serializer and the Stage’s --ig-stroke call it, so preview and download are the same arithmetic by construction rather than by agreement — pinned by a test that fails if a second call site or a second implementation appears. The scale reads the icon’s OWN viewBox — min(size/w, size/h), because width and height are both `size` and the default preserveAspectRatio letterboxes a non-square box — so pasted 16- and 20-unit SVGs are right too, not just the 24-grid packs. SAVED ICONS were the trap, and they are MIGRATED rather than redefined. Pre-fix custom records stored viewBox units NEXT TO their own size and their own baked svg, so the pixel weight is exactly recoverable; customIconStrokePx converts on read and new records stamp strokeUnit:"px" so nothing converts twice. The round trip is lossless — a legacy record (size 48, viewBox 24, stroke 2) migrates to 4px and re-serialises to stroke-width="2", identical to the markup already frozen in that record, so a saved icon re-opens at the weight it was saved at. A legacy record with Absolute ON needed NO conversion: non-scaling-stroke already resolved in the rendered space, so that number was always pixels. The sticky preference moved to a NEW key (vs-icon-stroke-px) instead of reusing vs-icon-stroke, because the old key held a bare user-unit number with no size beside it and reusing it would have silently redefined every returning user’s digit; the old key is read exactly once and converted at the size the editor has always opened at, then left alone. The RANGE moved with the unit: 0.5-8px, was 1-3 user units, default 4px. 4px is exactly what the old default rendered at size 48, so NO icon’s appearance changed — only the label became true. Keeping 1-3 and calling it pixels would have halved the maximum stroke weight available at every size. The ABSOLUTE TOGGLE was reviewed and KEPT, renamed "Fixed" with a title that says what it does. Both modes now measure the same pixels AT the export size; they differ only once a consumer rescales the file — off, the stroke scales with the artwork, which is what an icon should do; on, it holds that pixel width at any size it is ever drawn at. That is a real and separate job, so the control earns its place. "Absolute" never said any of it. VERIFIED IN THE REAL APP, not on unit tests alone: /create/icons, lucide activity, size 48, stroke 2 — the copied and the downloaded markup are byte-identical (<svg width="48" viewBox="0 0 24 24" stroke-width="1">), and that exact exported file rendered standalone measures a PAINTED stroke of 2.00px, against the pre-fix output’s 4.00px in the same renderer. The Stage measured 2.00px too. A pasted 16-unit viewBox at size 48 exports stroke-width="0.66667" and paints 2.00px. A returning user carrying legacy sticky 3 opens at "6px" with --ig-stroke:3 — the same attribute the old build wrote, so the icon looks identical and the label now tells the truth. Proof: tests/unit/icon-stroke-px.test.js (23), mutation-verified — 22 mutations, every one RED, none survived, every test killed by at least one.' },
  { id: 'font-gallery-one-per-row', title: 'Font Gallery goes one specimen per row, and its popup needs work', priority: 'P1', effort: 'M', area: 'Discover', status: 'done', note: 'DONE. The founder asked for one font per row and one column, which OVERRODE the font-gallery half of PR #298 — that band’s `--lbry-col-min:360px` is deleted, not left to lose a cascade quietly. The real work was that one column BY INTENT looks identical to one column by accident: setting `1fr` and stopping would have shipped exactly the emptiness the 2026-09-01 audit measured. So the row now carries three lines that each want a row’s width — the display line (52px default, 72px ceiling, 1,257px of specimen against the grid’s 386px), a ~105-character body line at 15px where the card fitted one 43-character pangram at 12px, and THE WEIGHT LADDER: each of the family’s real cuts drawn as a numeral SET IN THAT CUT, replacing the string "9w" in the card foot. utils/fontGallery.js gained `ladderWeights` (capped at five — a NETWORK budget, since every drawn weight is a woff2, and `useFontReady` loads exactly the cuts the ladder names so no numeral is painted in a weight it does not label), `formatSubsets` and `weightName`. The list also became the continuous table the `.fg-grid` panel rules at the end of global.css had been arguing for since before the three-column layout: rows lost their border, radius and ground for hairline rules, because on a full-width row a card is chrome around type that is itself the content. Row height 260px → 225px at 1440. PAGE_SIZE 48 → 24 (a page of results is a scroll distance, not a card count) and the reserved sample box now tracks the size slider instead of always reserving the 72px ceiling. THE POPUP: the note above guessed FontBrowseDialog.jsx and that was WRONG — it belongs to FontPicker, which Font Pair and Type Scale use. The Font Gallery opens DetailDialog inside FontGallery.jsx. It was 1,763px tall in a 900px viewport and scrolled the OVERLAY, so every action it offered was below the fold on arrival; "Type scale" printed one sentence six times with five ellipsised mid-word; "All weights" was six "Ag" tiles; and the tags counted facts ("1 subset") the data held by name. It is now a fixed frame — pinned identity, pinned actions, scrolling body — whose body IS the weight list, one full-width line per real cut drawn in that cut and labelled with its OpenType name, set in preview text inherited from the gallery instead of discarded. The type scale and the purposeless weight slider are deleted. Mobbin: Pitch’s font modal is the weight list; Readymag’s specimen pane is the dialog’s frame and its "Language Support" line; Locomotive’s rule-separated catalogue is why the rows lost their boxes; Waka Waka’s index is the rank gutter and the right-aligned script column. NOT touched: CompareDialog (.fg-compare), the gallery’s other popup — flagged as the next candidate rather than widening the diff.' },
  { id: 'icon-library-hero-parity', title: 'The Icon Library hero should feel like the other gallery pages', priority: 'P1', effort: 'M', area: 'Discover', status: 'done', note: 'DONE (#306). Two structural findings reframed it. First, the hero is NOT in IconLibrary.jsx — all three routes mount IconEmojiLibrary.jsx, so the .sec-h block in IconLibrary.jsx is dead on every shipped route, and the Icon and Emoji heroes are literally one element with a different tab. Second, the sibling galleries DO NOT AGREE: three patterns across four pages (Palette+Gradient on the shared DiscoverGalleryHero at 390px tall; Prompt on the generic .sec-h at 123px with an eyebrow that duplicates its own h1; Icon/Emoji a third one-off at 277px). Took DiscoverGalleryHero as the reference — the only shared component and the direction #292 already set — and moved ONLY the Icon Library, adding an optional `aside` slot for real controls (never hidden, unlike the decorative `mark`). The Prompt Library was deliberately left and queued as [prompt-library-hero-and-toolbar]. Caught and fixed a contrast regression it introduced: .lib-keyhint at 3.51:1 on 11px, raised to 4.99. Description copy NOT rewritten — it is translated into ten locales, so that is a copy task.' },
  { id: 'touch-toolbar-breakpoints', title: 'Force toolbar breakpoints on tablet and phone, and stop relying on hover', priority: 'P1', effort: 'L', area: 'Design system', status: 'done', note: 'DONE (#318). Founder direction (2026-09-02): when displaying toolbars on smaller screens we force breakpoints, on tablets and mobile, nesting buttons and menus if needed. .lbry-toolbar had only TWO states — a desktop row, and a full column stack at 640px and below — so everything from 641 to 980 got the desktop branch with none of the desktop room. It is the SHARED Library toolbar, so that one gap was six surfaces: /discover/gradients, /discover/palettes, /create/emoji, /create/icons, /create/font-gallery and the font browse dialog. THE AUDIT LESSON WORTH KEEPING: the automated geometry sweep ran first and came back COMPLETELY CLEAN — zero controls outside their own box, on every surface, at every width — because the failure mode in this band is not clipping but collapse into rows, and every control was inside its box the whole time. Toolbar height at 834px against the same controls at 1440px: gradients 192 to 68, emoji 225 to 68 (1440: 213), icons 178 to 68, palettes 128 to 68, font-gallery 195 to 135 (1440: 135). Two base rules caused it, both rules that only work when the row is wide: .lbry-search is flex 1 1 240px with NO ceiling, so it took 774px of 794 at 834px and pushed every filter group onto a line of its own, and .lbry-toolbar-action then stranded the action hard right on a ragged last line — the same shape #298 recorded as a deliberate cost on .pl-toolbar, reproduced on five more surfaces by one shared rule. Capping the search at 360px reuses the existing .pl-search-wrap desktop ceiling rather than inventing a number.' },
  { id: 'right-click-feedback', title: 'Right-click anywhere to submit feedback, with the context prefilled', priority: 'P1', effort: 'L', area: 'Feedback', status: 'done', note: 'DONE (#313). Founder request: right-click anywhere to report, with the context prefilled. Ships an app-wide context menu that opens the EXISTING feedback modal with the report context prefilled and — the part that makes that acceptable — shows the user every captured fact before it is sent, with a tick per row to drop any of them. ONE PIPELINE, NOT TWO: FeedbackButton now owns the single FeedbackModal instance and both entry points route through it, so the payload shape, the source label and the store are identical whichever way a report was started. The menu YIELDS to the native one wherever that is worth more — text selections, inputs, textareas, links, images, media, contenteditable, and any local menu that already handled the event — and Shift+right-click always returns the browser menu. Shift+F10 and the Menu key are handled as first-class entry points rather than afterthoughts. New pure module elementSignature.js turns the element under the pointer into a short stable selector (button.plb-add) rather than a DOM dump, and hard-guards what may appear in it. Proof: 36 unit tests across the two pure modules — what a signature may contain, what it must reject, when the native menu wins, and that unticking a row genuinely DROPS the value rather than hiding it. Mutation testing found two tests that were not testing what they claimed. UPDATE 2026-09-10, from the stranded-branch sweep: the feature shipped but four of its properties were never true in a browser, and all four were re-measured broken on a real build before being fixed. (1) REACH — FeedbackButton owns the context menu and was mounted inside AppInner’s shell, below the chromeless early return, so a right-click offered to report the element on /projects, /settings and /feedback and did NOTHING on /create/palette, /create/type-scale, /create/contrast, /discover, /learn and /home. Every Create tool — the pages where a tool misbehaving is the thing someone wants to report — had no way in at all. Hoisted beside BillingBanner, OfflineBanner and SyncNotice, which is the fourth time this same bug has been fixed in this neighbourhood; the mount point is now pinned by tests/unit/feedback-reach.test.js so there is not a fifth. (2) ANCHOR — `detail` is 0 for a genuine right-click in Chromium (measured: button 2, detail 0), so the usual “detail === 0 means keyboard” heuristic read every mouse click as keyboard-invoked and anchored the menu to document.activeElement, i.e. <body>: a right-click at (200,200) opened the menu at (8,919) on /projects and (8,580) on /settings, on every single right-click since the feature shipped. invokedByPointer() uses `button` instead. (3) FOCUS — usePopover was handed a closer that does not restore focus and the anchor it would return focus to unmounts in the same commit, so Escape left focus on BODY; it now closes through dismiss(). (4) TOUCH — a long-press fires `contextmenu` too, and in a Pixel 7 context it came back defaultPrevented, so the native callout (select, copy, paste, save image) was being swallowed on every phone, on the one platform with no Shift+long-press to escape with; nativeMenuWins now yields when the only pointer is coarse. Rendered proof in tests/user-sim/74-right-click-feedback-reach.spec.js (12), each fix mutation-verified at its call site. NOT changed, deliberately: the menu’s Cancel row and its two sentences. The stranded branch rewrote both; no measured defect needed it, that row is the only place the Shift+right-click escape hatch is disclosed, and the replacements were invented copy.' },
  { id: 'arrowkey-scroll-flake', title: 'The nav-popover arrow-key test asserts a browser default it cannot guarantee', priority: 'P1', effort: 'S', area: 'Testing', status: 'done', note: 'REOPENED 2026-09-03 — this was marked done by #301 and the symptom came back twice the same day: CI run 33715948705 attempt 1 (PR #325) and CI run 33713466234 (main), both failing 28-account-menu-keyboard.spec.js:144 with Expected: > 0, Received: 0. #301 was serious work and BOTH of its findings still stand — the flake (the assertion waited for movement to START rather than to SETTLE, fixed with keyToRest() counting take-up in animation frames rather than a wall clock) and the deeper one (the test was blind to the regression it named, because usePopover installs its document keydown listener only while the popover is open and the press happened AFTER closing it). Both were real. Neither was this. WHAT #301 DID NOT FIX: the test asserted a CONSEQUENCE (the page scrolls) as a proxy for the CONTRACT (the popover leaves the key alone), and that consequence depends on the browser producing a default arrow-key scroll at that instant. Both CI failures show it did not — scrollY 0 with 7422px of room below it, focus correctly on <main> (the failure snapshot marks it [active]), the panel correctly open, and zero movement. keyToRest therefore reported honestly; there was nothing left for it to wait for. The hypothesis that the page was already at maximum scroll is REFUTED by those same numbers. NOT REPRODUCIBLE on Windows after a deliberate hunt: 68/68 in isolation and at 16 workers, 16/16 under 8x CDP CPU throttling, 12/12 on a per-frame scroll timeline, 6/6 with the GSAP chunk delayed 2.5s, and 376/376 on a full suite — so the pass counts below are evidence about this machine, not about CI. FIX (#329): the spec now asserts the contract itself. A window-bubble keydown probe reads defaultPrevented and where focus stood once every handler had run — window-bubble because usePopover binds on document in the CAPTURE phase, so it is the only position that can see the verdict every earlier handler reached. That is what "the panel did not claim this key" MEANS, and no runner can defeat it. The scroll is kept as corroboration, gated on a control press with the panel CLOSED that proves this browser produced a default scroll at all; a press the popover demonstrably never touched cannot be evidence about the popover. ONE CONTRIBUTING DEFECT LEFT UNFIXED, because src/styles/global.css was another agent’s file this session: the Lenis override at global.css:5214 is scoped `.lenis.lenis-smooth`, but lenis-smooth is a TRANSIENT class Lenis carries only while its own smooth scroll is in flight (lenis 1.3.25, dist/lenis.mjs:1027), so it never applies at the moment a key is pressed. `html{scroll-behavior:smooth}` at global.css:93 therefore governs keyboard scrolling, making every arrow-key scroll an animated main-thread scroll instead of an instant one. Measured on the built app: the root class list at rest is exactly "lenis" and computed scroll-behavior is "smooth". THIRD FAILURE, AND THE PAYOFF: run 33719581296 (#329) failed once more with Received: 0 — but the rewritten assertions were in place by then and BOTH PASSED, so CI itself recorded that the popover called no preventDefault and took no focus while the page still refused to move, and that the same key moved the page with the panel closed seconds later. That splits the old symptom in two: the contract this test names is HELD, and what remains is a separate unexplained defect, now tracked as popover-open-scroll-suppression. The scroll is therefore REPORTED as a finding in this spec rather than asserted, because asserting it again would repeat an accusation CI has disproved on its own evidence. ---- 2026-09-06, CLOSED. Nothing this note left outstanding is still open. The contract assertions from #329 are on main; the ONE CONTRIBUTING DEFECT LEFT UNFIXED - the Lenis override scoped to a class Lenis only carries mid-scroll - was taken by [lenis-scroll-behavior-override-inert] and global.css now carries html.lenis{scroll-behavior:auto!important}; and the residual symptom is [popover-open-scroll-suppression], which stays open. RE-VERIFIED BY MUTATION rather than assumed, each with a full rebuild: deleting usePopover’s containment guard, so an open panel claims every arrow key on the page, turns this file RED - where #301 recorded that the SAME deletion left all six tests GREEN under the old assertions. AND THE PROBE POSITION IS NOW DEMONSTRATED LOAD-BEARING RATHER THAN ARGUED: the same defect, with the keydown listener moved from window-BUBBLE to window-CAPTURE, goes GREEN again, because at capture the probe reads defaultPrevented false before usePopover has touched the event - a capture listener would have shipped a test that passes against the defect it names. A first attempt at that blinding used document-CAPTURE and stayed RED, because two capture listeners on the same node fire in registration order and usePopover registers first; recorded because the mutation LOOKED like it had proved the assertion robust.' },
  { id: 'popover-open-scroll-suppression', title: 'An open nav popover intermittently stops keyboard page scrolling on CI, without claiming the key', priority: 'P2', effort: 'M', area: 'Accessibility', status: 'partial', note: 'Observed on CI only, and NOT explained. With the nav account popover OPEN and focus moved to <main>, an ArrowDown produced zero page scroll; seconds later, on the same page in the same session, the same key with the panel CLOSED moved the page 40px. The evidence that this is not the popover claiming the key is direct: 28-account-menu-keyboard.spec.js reads defaultPrevented and document.activeElement INSIDE the keydown listener, from a window BUBBLE listener that runs after the usePopover capture handler, and on failing run 33719581296 (#329) both passed — nothing called preventDefault and focus stayed on <main>. So an open popover is suppressing the browser default arrow-key scroll by some route other than the key handler. Candidates not yet eliminated, all of them things usePopover does while open: markScrollContainers() stamps data-lenis-prevent on the panel and on every scroll container inside it, which global.css:5230 turns into overscroll-behavior:contain; a CAPTURE-phase scroll listener on window runs placePopover on every scroll event of every element, forcing a synchronous layout (getBoundingClientRect plus offsetWidth/offsetHeight) and writing --pop-max-h each time; and the panel is itself a max-height scroll container. A contributing condition is that html{scroll-behavior:smooth} (global.css:93) is live throughout, because the Lenis override is scoped .lenis.lenis-smooth and lenis-smooth is transient (see arrowkey-scroll-flake), so the scroll being suppressed is an ANIMATED main-thread scroll rather than an instant one. NOT REPRODUCIBLE ON WINDOWS: 24/24 presses scrolled 40px with the panel open and with it closed at 20x CDP CPU throttling, and clean at 8x, at 16 workers, and across five full-suite repeats. Reproducing it needs a Linux runner. 28-account-menu-keyboard.spec.js records it as a critical finding when it sees it, so a run that hits it says so in the feedback summary instead of going quietly green. EVIDENCE ADDED 2026-09-03 (multi-breakpoint audit), AND IT NARROWS ONE OF THE CANDIDATES ABOVE RATHER THAN ADDING ANOTHER. The note says the suppressed scroll is "an ANIMATED main-thread scroll rather than an instant one", because scroll-behavior:smooth is live. THE FIRST HALF IS CONFIRMED AND THE SECOND HALF IS NOT, at least on Windows. CONFIRMED: with motion on, html computes scroll-behavior:smooth and the root class is "lenis" and NOT "lenis lenis-smooth", so the .lenis.lenis-smooth override never matches. Measured on /create/palette and /help. NOT CONFIRMED, and measured directly rather than reasoned about: the resulting keyboard scroll is NOT observably animated. Pressing PageDown and sampling window.scrollY on every animation frame for forty frames, the position reaches its final value in the FIRST sampled frame — 254px on /create/palette (1154px page, 900px viewport) and 450px on /help (1350px page). One distinct position across forty frames, not a ramp. Same result on the reduced-motion path, where html correctly computes auto and Lenis is never instantiated. WHY THAT MATTERS HERE: "the scroll is animated, so it can be interrupted or can fail to arrive" is the most attractive explanation on the list, and on this platform the premise behind it does not reproduce. It may still hold on the Linux runner where the defect actually appears — the note already records that nothing reproduces on Windows — but it should be treated as UNMEASURED on the platform where it was asserted, not as established. The cheap next step is to run the same per-frame scrollY sample on the Linux runner, with the popover open, on a page with a long scroll range: that distinguishes "the scroll never started" from "the scroll started and had not arrived when the assertion read it", and those two want opposite fixes. WORKED 2026-09-04: STILL NOT REPRODUCED, TWO CANDIDATES CLOSED, ONE MITIGATED. NOT REPRODUCED ON WINDOWS AGAIN: 12 trials across /help and /, covering ArrowDown, PageDown and a REAL pointer wheel with the panel open and closed, all moved the page by identical amounts. Every number was read at REST by polling animation frames and Lenis’s own lenis-scrolling class, never a timeout and never two equal pixel reads. CANDIDATE CLOSED - "the suppressed scroll is an ANIMATED main-thread scroll rather than an instant one". It is not, and the argument is now stronger than the platform hedge above. Chromium under Playwright does not animate native CSS smooth scrolls AT ALL: the positive control fails, so the premise cannot hold on ANY Playwright runner, the Linux one included. This is closed rather than merely unmeasured. The source condition is gone too - html computes scroll-behavior: auto whenever Lenis is mounted now that lenis-scroll-behavior-override-inert is fixed. CANDIDATE CLOSED - "the panel is itself a max-height scroll container". Measured at 1440x900 and 1440x560: the signed-out panel DOES NOT OVERFLOW (scrollHeight === clientHeight === 391), and markScrollContainers marks exactly ONE element, the panel. There is no inner scroller, so there is nothing for a nested scroll to chain out of. CANDIDATE MITIGATED BUT NOT PROVEN GUILTY - the capture-phase scroll listener. It really did re-run placePopover for every scroll event of every element: 55 invocations for ONE wheel gesture, each a forced synchronous layout plus three attribute writes, for a panel anchored to a FIXED bar whose placement cannot change. placePopover now writes only when a value would actually change - 110 attribute writes to 0, measured - and the reposition is coalesced into one animation frame behind passive listeners. Recorded honestly: THE COALESCING ALONE CHANGED NOTHING MEASURABLE, because those 55 events already arrive at most one per frame; it is a bound for the several-scrollers-in-one-frame case, and the write guard is where the saving actually came from. WHAT THE WHEEL EVIDENCE NARROWS THIS TO, AND IT IS THE MOST USEFUL THING HERE. Four deliberate attempts to make an open popover swallow a page-aimed wheel ALL FAILED to change the number: overscroll-behavior: contain on body and every scroller inside it; a non-passive window wheel listener calling preventDefault for as long as the popover was open; the lenis-stopped class on <html>, whose rule is overflow:hidden; and an inline overflow:hidden on <html>. The page scrolled anyway every time. While Lenis is live it never uses the browser’s default wheel scroll - it reads the gesture and drives the scroll itself - so preventing the default prevents nothing, and it rewrites its own lenis-* class list every frame, which strips the third attempt. THE KEYBOARD PATH IS THE ONE INPUT LENIS DOES NOT DRIVE, and it is the only input any of the CI failures has ever involved. Whoever takes this next should stop looking for something that suppresses scrolling in general and look only at what can stop the browser’s own default KEY scroll while a popover is open. STILL UNEXPLAINED. The per-frame sample on a Linux runner described above is still the right next step. ---- 2026-09-06, STILL NOT REPRODUCED, AND THE PROBE NO LONGER GOES QUIET WHEN IT CANNOT SEE. The finding this spec records rests ENTIRELY on the CONTROL press producing a default scroll, and CI has recorded that failing three times (scrollY 0 with 7422px of room below it). When it does, "the defect did not appear" and "this run could never have seen the defect" are the same silence, and the second reads as a clean result. 28-account-menu-keyboard.spec.js now records the UNMEASURED case as its own finding - "The control press produced no page scroll with the panel CLOSED (40 -> 40), so this run cannot say anything about whether an open popover suppresses keyboard scrolling. NOT a clean result ... an unmeasured one." - verified by forcing the condition and restored byte-exact. The ordinary result is deliberately NOT recorded: a finding written on every healthy run turns "no findings" into a line people scroll past. NOTHING NEW WAS REPRODUCED ON WINDOWS and no candidate moved; the per-frame sample on a Linux runner is still the next step, and everything above still stands.' },
  { id: 'submissions-account-not-browser', title: 'Tie every user submission to the account, not the browser', priority: 'P1', effort: 'M', area: 'Community', status: 'partial', note: 'PARTIAL. SHIPPED: palettes and designs now publish to the shared review queue as well as writing local, so a submission follows the ACCOUNT — PaletteBuilder.submitToCommunity builds a ‘palette’ record and Community.handleSubmit a ‘design’ one, both local-first so submitting still works offline, both saying plainly when a publish failed rather than claiming a review that did not happen. Community.jsx merges listMySubmissions(uid, ‘design’) over the local list exactly as GradientGallery does, keeping local-only entries marked unsynced rather than dropping them, and shapes every wire document through sanitizeCommunitySubmission so a card can never be handed an unsafe href just because it arrived from Firestore. firestore.rules needed no change and is now PROVEN rather than assumed: it is kind-agnostic, so ‘palette’ and ‘design’ were always permitted for an authed author, and a client-written status other than ‘pending’ was always refused. The signed-in gate was already correct on both surfaces and is untouched. STILL TO DO, and deliberately not half-built: vs-community-handle uniqueness is still checked only against handles set on THIS device. Real uniqueness needs a lowercased handle-registry collection claimed in a Firestore transaction plus a rule tying each submission to the claim — a new collection, new rules, a migration for handles already set locally, and a reservation-release path. That is its own change with its own failure modes, not a rider on this one; it belongs with community-backend, which already names the transactional handle registry as decided architecture. vs-community-saves, vs-palette-likes and vs-gradient-likes remain per-browser: they are counters that need a shared aggregation story (the Community Hub still layers each user’s own saves over illustrative baselines), which is the same backend piece. vs-prompts stays out of scope as a private scratch list. Proof: tests/unit/community-queue.test.js (21, seven new) + tests/rules/firestore-rules.test.js (37, thirteen new covering all three kinds against the emulator).' },
  { id: 'analytics-env-guard', title: 'Keep dev/preview traffic out of production aggregates', priority: 'P1', effort: 'S', area: 'Analytics', status: 'done', note: 'DONE (P-001, founder-approved). utils/environment.js allowlists the production hosts and flushAggregate refuses to write from anywhere else — guarded at the single choke point every shared counter passes through, so a counter added later cannot forget to opt in. Fail-safe: an unrecognised host is never treated as production, and blocked increments are DROPPED rather than queued (queuing would write them the moment someone opened the real site). Historic aggregates are still contaminated by CI and preview traffic and were not back-corrected.' },
  { id: 'upgrade-activation-events', title: 'Instrument the canonical upgrade gate and activation actions', priority: 'P1', effort: 'M', area: 'Analytics', status: 'done', note: 'DONE. The two pieces this item still named are both shipped.  ALREADY IN PLACE before this pass: trackUpgradeGate fired once inside ProModalContext, so all 16 openProModal call sites were covered; trackActivation was wired to saving a project (ProjectContext) and completing a style-guide export (ExportPanel); and time-to-first-value hung off that same trackActivation as ttv__<bucket> / ttv__<bucket>__<tool> plus firstwin__<id>.  1. GATE NAMES. Fourteen call sites still fell back to next.title, which is DISPLAY COPY, not an identifier — and the fallback was actively wrong, not merely vague: three separate PaletteBuilder walls (Add block, insert-between, context menu) all render “Go beyond 5 colours”, and two render “Check contrast, light and dark”, so five walls were reporting as two counters; IconLibrary’s line-styles wall reported as “Upgrade to Pro to use this feature”, which names nothing; and a copy edit silently renamed a counter and split its history. Every wall now names itself (palette-colour-cap, palette-contrast-view, palette-hct-picker, palette-harmony-system, palette-variation-apply, palette-variation-compare, palette-brand-system, palette-preview-scene, gradient-gallery-edit). IconLibrary and UiSystemBuilder DERIVE theirs from the kind they were asked for (icon-${kind}, ui-system-${kind}) so a wall added later is measured by existing — the same reason the event itself lives in the context. The title fallback stays in ProModalContext because analytics must never throw, but tests/unit/upgrade-gate-names.test.js now parses the balanced argument of every openProModal call in src/ and fails the build on an unnamed one, so it is no longer load-bearing.  2. PER-TOOL ACTIVATION for palette, gradient and type scale — the piece this item deliberately did NOT attempt before, because PaletteBuilder has eight copy affordances and TypeScale three, and tagging each guarantees the ninth ships untagged. Built to the shape this note prescribed: ONE declared export hook per tool (onExport, invoked at exactly one place in each file) with the decision in CreateTool, where the single onCopy already lives. src/config/activationExports.js is the one table, on the exportFormats.js precedent, and tests/unit/activation-exports.test.js fails the build in BOTH directions — a tool cannot call the hook without a row, and a row cannot name a route that is not a live tool.  WHAT IS DELIBERATELY NOT COUNTED, and why the number stays honest: copying one hex, one step’s font-size, a share link or a Google Fonts import URL stays on plain onCopy. Those are lookups, not completed work. The event also fires ONLY after the clipboard write resolves true — a copy the browser refused is not an export, and counting the click would report work that never left the page. Both properties are asserted against the rendered page, not a helper: tests/user-sim/56-activation-events.spec.js clicks the real buttons and reads the real counters. Mutation-verified — unwiring onExport in CreateTool leaves every copy button working and fails three tests; routing the hex copy through the hook fails the discrimination test.  PRIVACY: no new payload field. Activation writes activation__<tool>__export to analytics-daily, which is a per-day aggregate counter collection — no email, no uid, no palette contents; the tool id is bounded to 48 chars so a name can never carry data into a Firestore field path. No new api/ route: the client writes the same counter document it already wrote, so the 12/12 route budget is untouched.  STILL OPEN, and it is a naming question rather than a measurement one: the 16 gate ids are named per WALL, not per affordance, so the three ways to reach the colour cap report as one gate. That is the count that answers “which wall converted”; if the funnel later needs “which button”, it needs a second dimension, not a rename.' },
  { id: 'onboarding-completion-truth', title: 'Reconcile onboarding completion and resume truth', priority: 'P1', effort: 'M', area: 'Account', status: 'blocked', note: 'HVZ design required: the one-shot pendingOnboarding flag routes fresh signup, but abandoning then returning directly to /home or a deep link is not re-routed. Root routing checks vs-onboarded in localStorage while profile hydration separately checks Firestore. Avoid bouncing legacy users with no completedAt; then run live Firebase fresh/returning/resume/finish/skip QA.' },
  { id: 'stripe-webhook-ordering', title: 'Add Stripe webhook event dedupe and ordering guards', priority: 'P1', effort: 'M', area: 'Billing', status: 'blocked', note: 'HVZ/security slice: signatures are verified, but processed event.id values and event ordering are not stored. Make retries idempotent and prevent an older subscription update from overwriting newer deletion/revocation state. 2026-09-06 review: still open and still the right shape. A proposed diff - processed-events/{event.id} written with create() as the dedupe (the same trick alertFirstFailoverOfDay uses), and writeSubscription skipping any delivery whose event.created is older than the eventCreated it last wrote for that subscription id - is written unapplied in the review PR body, because the file is founder-gated.' },
  { id: 'plans-injectable-clock', title: 'Give planForUser an injectable now — it reads the ambient clock and is untestable by construction', priority: 'P2', effort: 'S', area: 'Billing', status: 'blocked', note: 'KNOWN DEFECT, found 2026-08-16 when tests/unit/billing-state.test.js went from 484/484 to 483/484 mid-session with NOTHING in the diff. planForUser({ subscription, lifetimeEntitlement, email }) in api/_lib/plans.js takes no clock and calls Date.now() internally, so any fixture with a fixed timestamp silently rots: the test pinned NOW = 2026-08-12 and the 7-day past-due grace window expired in real time, flipping "keeps Pro" to "loses it" on a date nobody chose. Note the failure mode — it is a time bomb, not a flake. It cannot be reproduced by re-running, it will not bisect to any commit, and it presents as a billing regression in whatever unrelated branch happens to be open when it detonates. Its siblings billingAlert() and isWithinPastDueGrace() both already accept an injected now and were never affected, which is the shape of the fix. PROPOSED FIX: add an options bag with an injectable now defaulting to Date.now() — planForUser({ ... }, { now = Date.now() } = {}) — matching the sibling signatures. Additive and fully backwards-compatible: every existing call site keeps working untouched. BLOCKED because api/_lib/plans.js is a founder-gated Human Validation Zone file (billing), so it needs the HVZ + security gate rather than a drive-by edit. MITIGATED meanwhile, not worked around: the test now anchors NOW to the real clock so its fixtures are relative and honest, with every assertion intact and mutation-verified. That makes the suite stable but leaves the production function still unable to be tested at a chosen point in time — which is what this entry is for.' },
  { id: 'subscription-chargeback-revocation', title: 'Revoke access when a subscription charge is refunded or charged back', priority: 'P1', effort: 'M', area: 'Security', status: 'done', note: 'DONE (founder-approved 2026-08-07). The refund/dispute path only ever matched lifetimeEntitlement.paymentIntentId, so a reversed SUBSCRIPTION charge — whose PaymentIntent belongs to an invoice — matched nothing and left the entitlement active. THE USER LINK was the hard part: create-checkout stamps metadata.firebaseUid on the CHECKOUT PaymentIntent, so it is there for the first payment and absent from every renewal (Stripe makes a fresh PaymentIntent per invoice and copies no metadata), and a chargeback almost always lands on a renewal — so the link is the SUBSCRIPTION’s own metadata, traced PaymentIntent → invoice → subscription, with the customer lookup as fallback. Access is revoked through a NEW STICKY subscription.accessRevoked rather than a status, because writeSubscription overwrites status from Stripe on every delivery and a disputed subscription still reads active for a while. Both plan resolvers honour it ahead of the past-due grace (that window is for a payment being RETRIED). It inherits the existing firestore.rules lock on the whole subscription map, so the revoked user cannot clear it — pinned by a test. A won dispute lifts it; a full refund never does. The Stripe subscription itself is deliberately NOT cancelled — an irreversible outward action on a live billing account off one webhook, and Stripe already cancels on a chargeback; that is a founder call and the log line says so. Users see a dedicated payment-reversed banner that does not tell them to update a card. NOT RUN: no live Stripe event was replayed against this — see stripe-checkout-live-qa. Proof: tests/unit/subscription-revocation.test.js (12).' },
  { id: 'support-abuse-hardening', title: 'Rate-limit and abuse-protect /api/support', priority: 'P1', effort: 'M', area: 'Security', status: 'done', note: 'DONE. Two per-IP fixed windows (3/min burst, 15/hour drip) consumed BEFORE any work, through a Firestore-backed limiter in api/_lib/rateLimit.js — an in-memory counter would be per-instance and resettable by a cold start, which is not a limiter. Counter keys are salted SHA-256, so no IP is stored; the rate-limits collection has no firestore.rules entry, so Firestore denies every client read and write to it by default. The limiter FAILS OPEN by design: losing the bug-report channel whenever the database wobbles is worse than the abuse it prevents. Access-Control-Allow-Origin went from * to the shared allowlist (a browser protection only — the limiter is what covers curl). A message that reached neither Firestore nor email now answers 502 instead of {ok:true}. Security gate: run, no HIGH/MEDIUM findings.' },
  { id: 'account-delete-cascade', title: 'Delete accounts through a server-side cascade', priority: 'P1', effort: 'M', area: 'Account', status: 'done', note: 'DONE, shipped in #231 — this entry sat as todo after the work landed, which is exactly how a task gets done twice. The fault it describes was ORDER: deleting the auth user first meant a failed second step orphaned a users/{uid} doc still holding email and display name. api/delete-account.js reverses that — billing, then db.recursiveDelete(userRef) (which takes the sync subcollection with it), then adminAuth().deleteUser(uid) LAST, so a failure at any stage leaves a recoverable account rather than orphaned personal data. It took the twelfth and final Vercel function slot.' },
  { id: 'homepage-field-metrics', title: 'Measure homepage field metrics on a throttled profile', priority: 'P2', effort: 'S', area: 'Performance', status: 'partial', note: 'MEASURED 2026-09-06 on the throttled mobile profile this item asked for, against the tree this change lands on. Eight of the nine budgets are MET; LCP is MISSED, and the reason is measured rather than guessed.  PROFILE, in full, because a latency figure without one is not a field metric: Playwright devices[Pixel 5] (393x851 CSS px, DSF 2.75, isMobile); CDP Network.emulateNetworkConditions at latency 150 ms / down 204800 B/s (1.6 Mbit/s) / up 96000 B/s (750 Kbit/s) — REAL packet shaping, not Lighthouse simulation; CDP Emulation.setCPUThrottlingRate 4x; HTTP cache disabled and a NEW BROWSER CONTEXT PER RUN, because a same-context reload reuses the compiled-script cache and is what turns a ~2.9s cold paint into the 27ms warm figure recorded in #248. TEN cold runs, against a production `npm run build` served by `vite preview`. Settled on the animation layer and on the layout-shift stream (load, then every running animation’s finished promise, then an 800 ms window with no new shift) — never on a stopwatch. The instrument is scripts/home-field-metrics.mjs, so these are re-measurable rather than remembered; it refuses to report a run whose hero never rendered, because a page that renders nothing paints fast and a broken build would otherwise look like a win.  RESULTS (mean / worst of 10). FCP 2857 / 2888 ms. LCP 3605 / 3788 ms — BUDGET 2500 ms, MISSED by ~1.1s at the mean. CLS 0.0000 / 0.0000 with ZERO shift entries in every run — budget 0.05, MET, which also settles "nothing may shift after fonts, motion or thumbnails resolve". Interaction response 89 / 120 ms, read from the Event Timing API after clicking a real workbench tab — budget 200 ms, MET. The LCP element was SPAN.home-hero-line-in in all ten runs.  BUDGETS NOW CLOSED, all previously unrun. Homepage initial executable JS carries 8088 bytes gzip (7.90 KiB) of the workbench — budget 12 KB gzip, MET at 66%. Measured as MARGINAL cost, because HomeWorkbench is a static import of Home and therefore sits inside the entry chunk where the build’s per-chunk table cannot see it: build as shipped, build again with the component stubbed to render null, gzip both entry chunks — 33997 bytes raw, 8088 gzip. The three bundled reference thumbnails total 173596 bytes — budget 180 KB, MET; and none of the three is fetched on a cold homepage load at all, they belong to the converter tab. No remote image/icon/font-catalogue call in any run (0/10): the two sequential third-party round trips are gone and every declared face is served from /fonts. No FFmpeg JS or WASM on the homepage (0/10). GSAP still arrives as its own two chunks, requested at ~3.29s — AFTER first paint at ~2.86s, so it blocks nothing.  WHY LCP MISSES, measured from the page’s own resource timing: it is same-origin BYTES, not third-party round trips. Eight resources are requested in one wave at 191-229 ms and then contend for the same 204800 B/s pipe — index JS 135358 B (2833 ms), firebase chunk 116268 B (2861 ms), index CSS 112028 B (2587 ms), two woff2 subsets 65840 B, icons-data.js 14953 B, plus two smaller chunks: 467156 bytes in the first wave, roughly 2.3s of transfer before any parse or execute at 4x CPU. FCP lands at 2857 ms, which is consistent with that and with nothing else.  FIREBASE, for firebase-critical-path: CONFIRMED on the critical path, and the mechanism is the CHUNK rather than the network calls. firebase-*.js is 116268 B — the second-heaviest thing on the page, 25% of the first wave — requested at 216 ms alongside the entry bundle. Blocking every third-party HOST changed nothing measurable (LCP 3756 vs 3805 ms across 5 paired runs, inside run-to-run noise), so the round trips are not the cost. An attempt to measure what deferring the chunk would recover, by blocking only firebase-*.js, is REPORTED AS INVALID rather than as a saving: the app does not boot without it and the hero rendered zero characters, so its faster 2544 ms LCP is the number for a blank page. What deferral would actually recover is therefore still unquantified, deliberately — it cannot be measured by removal, only by building the deferral.  STILL UNRUN, and why this stays partial: the assistive passes (keyboard-only, screen-reader names, 200% zoom, reduced motion, forced-colours) — and LCP is over budget. The byte budgets are now guarded by tests/unit/home-asset-budget.test.js — thumbnail total, thumbnails still referenced by the workbench, and every @font-face self-hosted AND actually present in public/ — each mutation-verified. The timings are deliberately NOT asserted in the suite: they depend on the machine, and a guard that fails at random on a busy laptop is one people learn to ignore.  UPDATE 2026-09-08: LCP DIAGNOSED WITH THE INSTRUMENT PLUS FRAME-LEVEL PROBES — hero mount time, every LCP candidate entry with its size, the entrance animations’ own startTime, document.fonts at mount, resource timing and long-animation-frame script attribution — on the same profile (Pixel 5, CDP 150 ms / 204800 B/s / 96000 B/s real shaping, CPU 4x, cache off, NEW CONTEXT PER RUN, production build under vite preview), 5 cold runs, medians with ranges. BEFORE: FCP 2576 ms (2544–4276; one run stalled on the pipe and is kept), LCP 3608 ms (3576–5292), LCP element SPAN.home-hero-line-in in 5/5, recorded LCP size 2369 px² in 5/5. THE ENTRANCE IS NOT THE CAUSE, AND NEITHER IS THE FONT: the LCP entry lands 19–22 ms after the clip-up’s own startTime, i.e. on the hero’s FIRST frame, and document.fonts.check(‘800 48px Manrope’) was TRUE at hero mount in every run (self-hosted, preloaded, font-display:swap, arrives at ~910 ms). WHY THE FIRST FRAME COUNTS despite a from-keyframe of translate3d(0,110%,0) behind overflow:hidden: .home-hero-line carries .28em of descender padding INSIDE the mask and 110% of a .98 line box overshoots the content box by only .098em, so ~.18em of the line box is still inside the clip on frame 0 — Chrome records a text block on the first frame it paints with non-zero clipped area and never re-sizes it, which is exactly the 2369 px² sliver. A fully clipped first frame (from-translate past ~125%, or padding under .1em) would move LCP to the first VISIBLE frame — the 80 ms delay plus a frame, ~100 ms later — with no visual change at all; that is now guarded (below). WHERE THE 1.0 s BETWEEN FCP AND LCP ACTUALLY GOES, read off the same runs: the stylesheet (97446 B) lands at ~2530 ms and the boot shell paints (FCP); the entry chunk (139128 B) lands at ~3022 ms and the firebase chunk it statically imports at ~3069 ms, because 526499 gzip bytes of first wave — entry 138765, CSS 96420, 33 modulepreloads 211472 of which firebase 115735, icons-data 14602, two woff2 65240 — contend on one 204800 B/s pipe; then 111 ms of module evaluation, DOMContentLoaded at ~3245 ms, 85 ms of React render, the hero DOM mounted at ~3340 ms, then a 215 ms task of which 199 ms is FORCED style+layout of the whole first commit (1391 nodes — the frame that follows needs only ~5 ms more, so this is the page’s unavoidable first layout done early, not waste), and the first hero frame, which is the LCP, at ~3590–3610 ms. The hero cannot paint before the entry graph has arrived, evaluated, rendered and laid out the whole homepage; the levers are bytes and the app shell, not the hero CSS. NO HERO CSS WAS CHANGED, deliberately: making the first keyframe visible would win at most the ~20 ms between animation start and entry and would change the founder’s entrance, and Manrope is already doing everything asked of it. THE ONE MEASURED LEVER IN REACH is the Firebase deferral (firebase-critical-path): the production TRIAL build on the same 5-run profile gives FCP 2216 ms (2204–2236) and LCP 2984 ms (2964–3044) — minus 360 / minus 624 ms at the median, still 484 ms over budget — and that row records why it is not wired yet. GUARDS ADDED, all geometry, no millisecond asserted: tests/unit/hero-first-paint.test.js (2 tests — the clip-up’s from-translate stays under 100% + padding-block ÷ line-height for every line-height the h1 ships, and fill-mode stays both) and tests/user-sim/65-hero-first-paint.spec.js (3 tests — at the clip-up’s own time 0 both headline lines have non-zero area inside .home-hero-line with the from-transform in effect as the positive control; the final LCP entry of a homepage load is SPAN.home-hero-line-in; under reduced motion the lines run no clip-up and sit wholly inside the clip). MUTATION-VERIFIED 5/5 AT THE CALL SITE in global.css, each restored byte-exact and proved clean with git: from 110%→130% reddens unit test 1 and spec test 1 (spec test 2 correctly stays green — Chrome then records the headline on a later frame); padding .28em→.05em reddens unit test 1; both→forwards reddens unit test 2; opacity:0 on .home-hero-line-in reddens spec test 2, the LCP element stops being the headline; breaking the html[data-reduced-motion="true"] selector reddens spec test 3. STILL PARTIAL because LCP is 3.6 s against 2.5 s and the assistive passes are unrun. LEVERS THE NUMBERS POINT AT, for whoever owns them: the deferral (−624 ms measured); the entry graph, which modulepreloads TWO locale chunks (en 7893 + en-US 7801 gzip), brandGuidelines 8869, styleGuideExport 4030, learnIndex 3481, paletteGallery 2496 and userHome 1727 before a public visitor has done anything; and the fact that the prerendered shell does not carry the headline, so LCP can never precede the entry chunk — carrying the real headline in the shell would put LCP at FCP (~2.55 s, on budget) but only with a decision about what the entrance then does, which is the founder’s.' },
  { id: 'account-menu-arrow-nav', title: 'Add arrow-key navigation to the account menu', priority: 'P1', effort: 'S', area: 'Accessibility', status: 'done', note: 'DONE. usePopover takes an opt-in arrowNav and the nav account panel (avatar signed in, meatball signed out) turns it on: Up/Down wrap at both ends, Home/End reach either end in one press. Escape focus restoration, outside-press dismissal, focus-on-open and Tab-past-the-end-closes already existed in the hook — the browser spec now asserts them alongside the new keys so arrow handling cannot swallow them. Assistive-technology semantics were reviewed and deliberately LEFT as a disclosure rather than made a role="menu": the panel holds a segmented control, links and a status line, so menu semantics would promise a single-tab-stop widget it is not. Proof: tests/unit/popover-keys.test.js (the pure key decision) + tests/user-sim/28-account-menu-keyboard.spec.js (rendered).' },
  { id: 'global-failure-states', title: 'Add global 404 and offline states', priority: 'P1', effort: 'M', area: 'Quality', status: 'done', note: 'DONE. The 404 half shipped in #235 (src/pages/NotFound.jsx behind the wildcard route, replacing a redirect-to-home that made every dead URL a soft 404). The offline half is now done: src/hooks/useOnline.js is the one connection signal and src/components/OfflineBanner.jsx is the one notice, mounted outside AppInner so it reaches the chromeless Create tools. It names what STILL WORKS — colour, typography and export all run in the browser — because a bare “you are offline” sends someone away from a product that is still almost entirely usable. Only navigator.onLine === FALSE is ever trusted: true means an interface is up, not that the internet is reachable. Top-centre under the nav rather than a fourth thing in the bottom corners, and below the nav’s z-index because the nav is how you leave a page that is not working. useFontCatalog and IconEmojiLibrary lost their own listener pairs to the shared hook; the font catalogue’s reconnect-refetch now keys on the transition, so it also fires for a component that MOUNTED offline — a listener attached after the event never hears it. Proof: tests/user-sim/33-offline-state.spec.js (8), which sets navigator.onLine directly rather than using context.setOffline (see #189: setOffline does not reliably flip navigator.onLine, which is what made the last offline test flaky).' },
  { id: 'firebase-critical-path', title: 'Defer Firebase off the public critical path', priority: 'P1', effort: 'L', area: 'Performance', status: 'partial', note: 'DESIGN DELIVERED AND MEASURED 2026-09-07, implementation shipped behind a flag that is OFF by default; ONE founder approval outstanding. The block said this needed an approved auth-loading design and owner validation before Firebase initialization or authenticated routing changed. The design is docs/design/firebase-deferral.md, the diff it needs to a gated file is docs/design/firebase-deferral-gated.patch (UNAPPLIED, 373 lines, two files), and every claim below is a measurement on #402’s exact harness so before and after are comparable.  WHAT IS IN THE 116268-BYTE CHUNK, measured by import graph rather than by reading — each figure is a real rolldown build of exactly the symbols src/ imports from that entrypoint, gzip level 9. app+auth+firestore+storage (today) 468019 raw / 125898 gzip; app+auth+firestore 441358/118068; app+auth 144554/39148; app+firestore 330535/89061; app+storage 61362/18510; app alone 28397/8276; build floor 133/115. MARGINAL gzip: FIRESTORE 80785 (69%), auth 30872 (26%), storage 10234 (9%), app core 8161 (7%) — the marginals overshoot the whole by ~3.5 KB, which is shared SDK code counted twice. The public homepage needs NONE of it before interaction: auth’s only job on a signed-out load is to conclude signed out, and #385’s synchronous localStorage hint already chose the page; every Firestore touch is behind a resolved uid; Storage is community media upload on a lazy route. src/utils/firebase.js calls getFirestore() and getStorage() at module scope, so 78% of the chunk is constructed on every load for work no public visitor does.  THE DESIGN. Two implementation modules per seam, swapped by a build alias: src/utils/firebaseAccess.js (eager, the default, static imports) vs .lazy.js (dynamic import()), and src/components/oneTapMount.jsx vs .lazy.jsx — the second seam exists because GoogleOneTap reads GOOGLE_CLIENT_ID out of utils/firebase, so merely importing it drags the chunk along, and App.jsx (not gated) is the file that had to change rather than the gated component. A BUILD FLAG, not a query param, because the thing being changed is which chunk the SDK belongs to and rolldown decides that at build time: no runtime switch can move bytes the browser requested before any of our code ran. ?firebase=eager is the runtime dial that does exist — within a deferred build it opens the gate at load, for comparing schedules without a rebuild. THE GATE is the part that is not just “load it later”: PATIENT callers (the auth bootstrap listener, and only it — nothing on a public page waits on its answer) wait; URGENT callers (RequireAuth’s spinner, any sign-in action, a Firestore read for a page someone is looking at) OPEN it. It opens on the earliest of the session hint, any urgent caller, the LCP entry plus a frame, requestIdleCallback, THE FIRST pointerdown OR keydown, or a 2500 ms backstop (LCP never fires in a background tab). The first-touch listener is what makes “nobody waits on the optimisation” true by construction instead of by remembering to call an urgent loader at every sign-in affordance. Converted, all non-gated: analytics.js, useFirestoreSync.js, ProjectContext.jsx, App.jsx. useModerationRole.js needed NOTHING — #390 already reached Firebase through await import().  WHAT LCP BECOMES. Same instrument as #402 (scripts/home-field-metrics.mjs, Pixel 5, CDP 150 ms / 204800 B/s / 96000 B/s real packet shaping, CPU 4x, cache disabled, NEW CONTEXT PER RUN, 10 cold runs, production build under vite preview, settled on the animation layer and the shift stream, refuses to report a run whose hero rendered nothing). FLAG OFF: FCP 2823.6 mean / 2844 worst, LCP 3546.8 mean / 3532 median / 3668 worst, CLS 0.0000, interaction 73.6 / 88. FLAG ON with the gated patch applied: FCP 2444.8 / 2500, LCP 3106.8 mean / 3028 median / 3640 worst, CLS 0.0000, interaction 100 / 152. LCP element SPAN.home-hero-line-in in all twenty runs. LCP MEAN IMPROVES BY 440 ms AND STILL MISSES THE 2500 ms BUDGET BY ~607 ms, and it is said here rather than rounded away. WHY: removing 115026 bytes frees 562 ms of pipe at 204800 B/s, and what is left is still 405993 first-wave bytes — entry JS 134436 gzip (33%), CSS 110535 (27%), two woff2 65240 (16%), icons-data 14602, locales 15694, 25 smaller preloads. That is ~1.98 s of transfer, and the LCP element is REACT-RENDERED (the prerendered shell does not carry the hero), so it cannot paint until the 134 KB entry chunk has downloaded, parsed and executed at 4x CPU. FIREBASE WAS THE LARGEST SINGLE REMOVABLE ITEM, NOT THE ONLY ONE; the next two levers are the entry chunk and the stylesheet, 60% of what remains, and neither moves without splitting the app shell or the design-system CSS. Reported rather than buried: interaction worst rose 88 -> 152 ms (budget 200, still MET) because the deferred chunk now parses around LCP where a click can overlap it.  WHAT A SIGNED-IN VISITOR PAYS — the risk this design exists to bound. Five runs per row, same profile; “SDK ready” is the responseEnd of the last chunk carrying the SDK, the earliest onAuthStateChanged could fire. Flag off, signed-out: requested 213 ms, ready 3076 mean / 3087 worst. Flag off, session hint set: 206 ms, 3073 / 3083. FLAG ON, SESSION HINT SET (the returning signed-in visitor): requested 2651 ms, READY 3364 MEAN / 3386 WORST — +291 ms mean, +303 ms worst. Flag on, no hint: 3261 ms, 3881 / 4240, and that visitor is signed out so nothing waits on it. The chunk is requested 2.4 s later and arrives only 0.3 s later, because in the baseline it is requested at 213 ms and then spends 2.8 s contending with seven other resources on one pipe — being early in a queue is not being fast. Set against the ~1 s the onAuthStateChanged round trip already costs (measured in 20-billing-banner.spec.js), it is a third of a cost already being paid, and #385 means that person is looking at their own User Home throughout, not a spinner. POSITIVE CONTROL: with the hint set, /projects rendered 1363 characters of real content in every run, before Firebase existed on the page.  HALF A DEFERRAL IS WORSE THAN NONE, so the build refuses it. Flag on WITHOUT the gated patch splits the SDK across two preloaded chunks (auth stays at 34093 gzip, Firestore moves to its own preloaded index.esm-*.js at 115301) and duplicates shared core: 553193 first-wave bytes against 521019 unflagged, A 32174-BYTE REGRESSION. So with the flag on the build WALKS THE EMITTED STATIC IMPORT GRAPH from the entry chunks and FAILS if the SDK is still statically reachable (assertFirebaseIsDeferred, vite.config.js), naming the patch in the error. Against today’s tree it fails, deliberately. First-wave bytes, the same accounting each time (entry script, stylesheet, all modulepreloads, both preloaded fonts, icons-data.js; gzip for text): 521019 flag off, 553193 flag on unpatched, 405993 flag on patched — MINUS 115026, minus 22.1%.  WHAT REMAINS, AND IT IS ONE APPROVAL. src/contexts/AuthContext.jsx and src/contexts/SubscriptionContext.jsx are founder-gated and still hold the last two static edges. Their exact diff is docs/design/firebase-deferral-gated.patch, UNAPPLIED, the way #390 held its two. Both gated files are BYTE-IDENTICAL on this branch and a unit test proves it against origin/main. The patch does three shapes of thing and nothing else: the bootstrap listener becomes asynchronous (with a cancelled guard and a deferred unsub, because a provider unmounted before the SDK lands would leak a listener nothing could detach; a rejected import resolves as SIGNED OUT rather than holding every RequireAuth spinner forever), every user-initiated action awaits the URGENT loader first, and firebaseAuth.currentUser becomes authNow()?.auth?.currentUser at identity guards that already treat a missing user as “not this session”. In an unflagged build the eager broker resolves every promise immediately and authNow() is never null, so the patched files behave exactly as they do today. BLAST RADIUS as the HVZ doc requires: session resolution and every gate hanging off it (RequireAuth, RequireAdmin, project cap, billing, export gate, useModerationRole); everyone signed in; fully reversible — git apply -R, or simply do not build with the flag, since the patched files are inert without it. It changes no rule, no route, no entitlement and no token handling — only WHEN the SDK is fetched.  HOW THE NUMBERS WERE TAKEN WITHOUT WRITING TO A GATED FILE. scripts/firebase-deferral-trial.mjs applies the committed patch to TEMPORARY COPIES beside the gated originals, aliases those in for one build, and deletes them; the originals are read and never written, and it refuses to run if the patch has gone stale. The patch is the single source of truth for the trial, so a trial build cannot measure something the patch does not say. The auto-mode classifier independently blocked an attempt to edit AuthContext.jsx even for a throwaway measurement — the same guardrail #390 hit — which is why the instrument exists in this shape.  EVIDENCE. tests/unit/firebase-deferral.test.js: 9 tests, 9/9 mutation-verified, each restored byte-exact — deleting a seam’s lazy half, writing one import with a file extension so the alias silently misses it, adding a static firebase import to a boot-path module, reverting analytics.js to firebase/firestore, dropping an export from the deferred broker, static-importing firebase/auth INSIDE the deferred broker, corrupting a context line in the patch, pointing a patch hunk at a third file, and pointing the gated-files guard at a file this branch did change. The sixth of those SURVIVED the first draft, which asked whether the specifier also appeared in an import() somewhere — true for every Firebase entrypoint in that file by design, so a static import added beside them looked perfectly matched; it now reads the static import statements only. tests/user-sim/57-firebase-deferral.spec.js: 3 tests, 3/3 mutation-verified at the CALL SITE — making oneTapMount.jsx inert kills only the One Tap assertion, making the eager broker throw during module evaluation kills all three, and renaming the nav control proves the sign-in locator is not vacuous. That spec deliberately guards only what is live on an UNFLAGGED build, because npm run test:users builds without the flag and a test asserting something true in both builds would exercise nothing — the failure this repo has paid for four times.  EVALUATED FOR THE PRODUCTION BUILD 2026-09-08 AND NOT WIRED — the decision is measured, three ways. (1) `VITE_DEFER_FIREBASE=1 npm run build` against this tree FAILS as designed: assertFirebaseIsDeferred names the entry chunk, because the gated patch is still unapplied (git apply --check passes; AuthContext.jsx line 18 and SubscriptionContext.jsx line 3 still import the SDK statically). There is nothing to wire until the founder applies docs/design/firebase-deferral-gated.patch — putting the flag in front of `npm run build` in package.json or vercel.json today would break every deploy. The same holds for the unit suite: with VITE_DEFER_FIREBASE=1 in the environment 1692/1698 pass and the 6 failures are exactly the tests that run a real vite build (the Admin-chunk walk and test-session-not-in-production), which inherit the flag and hit the same assertion. (2) THE SUITE CANNOT BUILD WITH THE FLAG EVEN WITH THE PATCH, which is new: scripts/firebase-deferral-trial.mjs re-run with `--mode test` fails the same assertion on assets/test-session-*.js, assets/firebase-auth-*.js and assets/firebase-firestore-*.js — the signed-in-session doubles (tests/user-sim/fixtures/firebase-auth.js and firebase-firestore.js) `export * from` the real SDK statically, so under --mode test the SDK is statically reachable from the entry THROUGH THE DOUBLES. A flagged users-suite run needs a third seam (or lazy doubles) first; until then `npm run test:users` can only exercise the eager path, exactly as 57-firebase-deferral.spec.js says. So the full users suite was NOT run against a deferred build, and this row does not claim it was. (3) WHAT WAS MEASURED, same instrument profile as before (Pixel 5, CDP 150 ms / 204800 B/s / 96000 B/s, CPU 4x, cache off, new context per run), 5 cold runs each, medians with ranges, production build vs the production TRIAL build (patch applied to temporary copies by the trial script, gated files unwritten, git clean before and after): FCP 2576 ms (2544–4276) → 2216 ms (2204–2236), minus 360 ms; LCP 3608 ms (3576–5292) → 2984 ms (2964–3044), minus 624 ms, still 484 ms over the 2500 ms budget; LCP element SPAN.home-hero-line-in in 10/10; first-wave gzip bytes by this row’s own accounting (entry, stylesheet, every modulepreload, both preloaded fonts, icons-data.js) 526499 → 411508, minus 114991 (−21.8%); modulepreloads 33 → 32 with no firebase or index.esm chunk among them. The win is real and repeatable, and the homepage-field-metrics diagnosis of the same date shows it is the single largest lever the homepage has: the hero cannot paint until the entry graph, firebase chunk included, has arrived and evaluated. WHAT CLOSES THIS, in order: the founder applies the gated patch; a seam for the test doubles so `vite build --mode test` can honour the flag; the full users suite green on that build; and only then VITE_DEFER_FIREBASE=1 in front of the build in package.json so Vercel’s `npm run build` and the suite build the same shape. Status stays partial. AN APPLIER EXISTS NOW, 2026-09-10. `npm run apply:gated` applies docs/design/firebase-deferral-gated.patch to both gated contexts for the founder, alongside the three founder-gated firestore.rules diffs, and then RUNS VITE_DEFER_FIREBASE=1 npm run build as its last check - the build that refuses against an unpatched tree by walking the emitted chunk graph. On a scratch clone of the branch it succeeded and printed `prerender: wrote 39 route shells`, which is the first time this design has been built from the REAL files rather than from the trial script\'s temporary copies. Rules 105/105 and unit 1768/1768 on the same tree. The command is idempotent (second run: all four SKIPPED, tree byte-identical) and commits nothing. Six existing guards asserted the UNAPPLIED state as a constant and would all have gone red on the day the founder ran it - the static-importer inventory and the `git apply --check` in tests/unit/firebase-deferral.test.js, the auth-epoch spelling in auth-switch.test.js, and three more - so each now reads the result and takes the other branch. STATUS UNCHANGED: the patch is still unapplied on main; what changed is that applying it is one command instead of a paste.' },
  { id: 'openrouter-path-verification', title: 'Verify a production request really routes through OpenRouter', priority: 'P1', effort: 'S', area: 'Infra', status: 'partial', note: 'PARTIAL — deliberately NOT closed. The verification itself needs a founder session and I do not have one, so nothing here claims it was done. WHAT WAS ACTUALLY VERIFIED, from outside, on 2026-09-03: the route is live and correctly gated. GET /api/ai?diag=1 answers 404 with {"error":"Not found"} — the admin gate from the diag hardening is in force in production, and it answers 404 rather than 403 so a non-admin learns nothing. POST /api/ai with no token answers 401. And one useful inference: configured() is checked BEFORE the auth check in the handler, so an unauthenticated POST that returns 401 rather than 500 proves the task’s keys are present — every task returns 401, which proves GEMINI_API_KEY is set in production. WHAT COULD NOT BE VERIFIED, and why: generate-prompt’s configured() is Boolean(OPENROUTER_KEY || GEMINI_KEY), so with Gemini present its 401 says NOTHING about OpenRouter. Getting further needs a Firebase ID token from a verified admin email, or DIAG_CODE, which has no default and is unset by design. Neither is available to an agent, and neither should be. THE ITEM’S OWN INSTRUCTION WAS IMPOSSIBLE, which is the real find. It said to “confirm via the AI diagnostic that OpenRouter reports available”, and docs/OWNER-ACTIONS.md said the same. The diagnostic CANNOT report that. It returns openrouterKey: "set (73 chars)" — presence and length — which is precisely the fact the founder already confirmed on 2026-08-07 and precisely what a revoked, wrong or rate-limited key also looks like. A founder following that runbook would have read “set”, concluded “available”, ticked the row and closed the item without the route ever being exercised. That is the silent failover reproduced inside the procedure meant to catch it. The runbook is rewritten. WHAT ACTUALLY CLOSES IT, now written into OWNER-ACTIONS as a four-step procedure with a PASS and every distinct FAIL spelled out: the generation response already carries a `provider` field, set to "openrouter" or "gemini" by runGeneratePrompt. Sign in as the founder, open devtools Network, generate one prompt on the AI Image Prompt Generator, read `provider` on the /api/ai response. "openrouter" is the pass. "gemini" is a FAIL that produced a perfectly good prompt while you watched — which is why the runbook now says in as many words not to substitute “I checked that it works”. OBSERVABILITY, which the item asked about and is the finding worth more than the original task: the API boundary is fine — `provider` is on every response — but NOTHING downstream keeps it. src/pages/AiPromptGenerator.jsx reads data.prompt and data.platform off the response and DROPS data.provider on the floor; it is not stored on the result, not shown, not logged. The only record that a failover happened is one console.error in api/ai.js, which lands in Vercel function logs with short retention, is never counted, never alerted on and has no admin surface. So a permanently dead OpenRouter is invisible to every surface an operator watches, indefinitely. Filed as ai-failover-invisible. A second, smaller finding filed as ai-config-probe-unauthenticated: the 401-vs-500 ordering above means any anonymous caller can learn which provider keys exist. That is a subset of exactly what the diag endpoint was hardened to stop leaking. Proof that the verification method stays possible: tests/unit/ai-provider-path.test.js (7) pins that a generation reports which provider served it, that the two report DIFFERENT names, that OpenRouter is tried first and Gemini only when the primary produced nothing, that the failover is logged at all, that the prompt tool still survives an OpenRouter outage, that the diagnostic reports a key’s existence and never its value, and that OWNER-ACTIONS no longer asks for the confirmation the diagnostic cannot give. Mutation-verified: 9 mutations, every one RED.' },
  { id: 'ai-failover-invisible', title: 'A dead OpenRouter is indistinguishable from a healthy one at every surface', priority: 'P1', effort: 'M', area: 'Infra', status: 'done', note: 'DONE. The test the item set — would an operator who is not looking at devtools find out, within a day, that every generation is coming from the fallback — now answers yes on three surfaces, chosen cheapest-first and each doing a job the others cannot. (1) THE BADGE. Both prompt tools kept data.prompt and data.platform off the response and dropped data.provider on the floor; they now keep it and render it on the result card — “OpenRouter”, or “Gemini · fallback” in warn colour with a tooltip saying the prompt is fine and the primary provider is not. src/utils/aiProvider.js is shared by both pages. It deliberately shows the HEALTHY case too: a badge that appears only on failure teaches nobody what normal looks like, so its absence would read as “no badge yet” rather than “something is wrong”, and it would be one refactor from never rendering at all — which is this exact fault class. An unrecognised provider name is styled as a fallback, because a name this build does not know is by definition not the primary it shipped with. This alone converts the founder verification procedure from a devtools session into a glance. (2) DURABLE COUNTS. runGeneratePrompt now records, per day, how many generations OpenRouter served, how many it failed, how many the Gemini fallback picked up, how many nothing served, and the time, HTTP status and message of the last failover. Written BEFORE the total-outage return, so the case most worth alerting on is counted rather than skipped. It stores the error MESSAGE and never err.detail — that is the provider’s raw response body and gateways have echoed the offending key back inside one. Storage is a provider-health collection that appears NOWHERE in firestore.rules, which default-denies, so it is unreachable from every client and only the Admin SDK touches it — the same property daily-usage relies on, and the reason this needed no rules change (rules changes are founder-gated and separately published). (3) ADMIN VERDICT. GET /api/ai?diag=1 now returns a providerHealth block and Admin → Overview renders it: a status light, seven-day counts and a sentence. The diag GET was reused rather than a new route invented, and not only for the admin-gate precedent the item pointed at — /api holds twelve route files and the Vercel limit on this plan is twelve, so a health route was not available even in principle. The verdict refuses to read silence as health: an empty week reports “no-data” and says in as many words that it is not a pass, because reading an untested path as healthy is precisely how the previous owner action recorded a check that never ran. WHAT NEEDED THE FOUNDER, AND WHAT DID NOT. A push alert is the one part that cannot be built alone — Vercel gives a serverless function no scheduler and no alerting, and a monitoring service is out of scope by standing instruction. So the alert is BUILT and conditional: the first OpenRouter failure of any day emails the operator through the Resend channel /api/support already uses, deduplicated by a Firestore create() on a per-day marker so an outage sends one mail and not one per request. With RESEND_API_KEY and SUPPORT_NOTIFY_EMAIL unset — the shipped state — it is silence, not an error, and the admin panel states which mode it is in on its last line. THE FOUNDER DECISION, now written into OWNER-ACTIONS as its own row: set those two env vars and a failover comes and finds you; leave them and a failover is still durably counted but you learn about it the next time you open Admin. There is no third option that does not add a paid dependency. NOT CLAIMED: nothing here verifies which provider is live in production. That still needs one founder generation — it is just a glance at a badge now instead of a devtools session. The Resend call is duplicated rather than shared with api/support.js on purpose: api-abuse-hardening.test.js proves the support limiter runs before the outbound email by locating api.resend.com INSIDE support.js, so lifting that string into _lib would leave the assertion green and guarding nothing. Tests extend tests/unit/ai-provider-path.test.js (the provider-path class — the hardening file is for surfaces reachable without credentials, and none of this is), 17 new, and the verdict and counter logic are exported as pure functions so they are executed rather than described. Mutation-verified: 22 mutations, every one RED. One of them was caught only after a fix — repointing the admin fetch at a different endpoint left the suite GREEN because the comment above the fetch still named the old URL, so the source assertions now strip comments first.' },
  { id: 'ai-config-probe-unauthenticated', title: 'An anonymous POST to /api/ai reveals which provider keys are set', priority: 'P3', effort: 'S', area: 'Security', status: 'done', note: 'DONE. The handler now authenticates BEFORE it looks a task up and before it calls task.configured(), so an anonymous POST to /api/ai answers 401 for every task in every configuration state and learns nothing. It used to be an oracle: 401 meant “that provider key is set” and 500 (“AI is not configured on the server: GEMINI_API_KEY is missing”) meant it is not, and because each task declares its own configured() — the vision tasks need Gemini, generate-prompt takes either — three unauthenticated requests enumerated which provider keys the deployment holds. THE DIAGNOSTIC VALUE IS KEPT, which is the reason this was not a one-line move: configured() still runs, just after verifyIdToken, so a signed-in user staring at a broken deployment still gets the specific message naming the env var to set. The task lookup moved behind auth too — left in front, an unknown task would answer 400 while the three real ones answered 401, which is the same enumeration in a smaller alphabet. KNOWINGLY LEFT: the catch around verifyIdToken still returns credentialProblem() to an unverified caller. That is not the same fault. When the Firebase credential is broken verifyIdToken fails for EVERYONE, so the endpoint is 100 percent unusable and its brokenness is already plain from outside — the message adds no hidden fact, and it is the one diagnostic that survives the credential the whole admin gate depends on. Tests live in tests/unit/api-abuse-hardening.test.js, the existing home for this class, and they RUN THE REAL HANDLER rather than describing it: provider keys are deleted from the process first, because api/ai.js reads them into module constants at import time and “no key configured” is the only state where the two orderings differ. Three tests: a stranger gets 401 and a body that names no env var; all five probes (three real tasks, an unknown task, no task at all) return byte-identical replies; and configured() still runs after verifyIdToken with both configError strings intact. Mutation-verified, 3 mutations, every one RED — reinstating the old check ahead of auth (2 red), deleting the configured() guard entirely (1 red), and moving the real lookup plus guard back above the Authorization check (3 red).' },
  { id: 'stripe-checkout-live-qa', title: 'Run live Stripe checkout return/retry QA', priority: 'P1', effort: 'M', area: 'Quality', status: 'blocked', note: 'Owner/HVZ validation on production checkout, abandon, return and retry flows; separate from retention coupon configuration.' },
  { id: 'stripe-retention-config', title: 'Configure Stripe retention and cancellation', priority: 'P1', effort: 'S', area: 'Infra', status: 'blocked', note: 'Owner action: create RETAIN50 and enable cancellation/retention in the Stripe Customer Portal; checkout code is already live.' },
  { id: 'discover-buildout', title: 'Build Discover galleries', priority: 'P1', effort: 'L', area: 'Product', status: 'review', note: 'FOUNDER DECISION 2026-09-05, recorded in CHANGELOG.md. THE END STATE IS A COMMUNITY GALLERY, BUILT IN STAGES: CURATED FIRST, COMMUNITY PUBLISHING LATER. Ship galleries the founder fills himself now rather than launching an empty social feature and waiting for it to fill. That sequencing is the decision - it means this row can proceed WITHOUT community-backend, which was previously the implicit dependency. CURATED GALLERIES SHIPPED. Founder decision 2026-09-05 settled what Discover is FOR: the end state is a community gallery where users publish what they make, but it ships CURATED FIRST, because launching a social feature with nobody in it is worse than launching a curated one. User publishing is [community-backend] and is deferred by that same decision. WHAT WAS ACTUALLY MISSING WAS ONE PAGE, NOT A GALLERY PROGRAMME. Five galleries were already live (gradients, palettes, prompts, fonts, icons + emoji) on a shared browse language (DiscoverGalleryHero + LibraryToolbar + LibraryFilterGroup + LibraryEmpty + DiscoverResultHead). The gap was CURATED RESOURCES: toolTree.js has advertised it in the nav as Soon since Slice 2, src/data/discoverResources.js has held 22 hand-written resources the whole time, and NOTHING HAS IMPORTED THAT FILE SINCE #196 deleted the Discover shell as a dead page and left the data behind. /discover/resources is that data given a route. Prerender 28 to 29 shells. ROWS, NOT A CARD GRID, and the reason is that a curated set is small by design and has to look edited at 22 items across seven categories of three or four. Mobbin: the AI in Design Report 2026 resource list and Webflow’s rule-separated index both hold at five items where a four-up grid of five reads as a grid that failed to fill; Upwork’s Resource Centre is the lead-then-list pacing, applied PER CATEGORY so nothing is duplicated into a separate featured rail. The card grids that do work (Patreon, Kit) work because every tile carries a real product logo — we cannot, because we never fetch a curator-supplied URL, so DiscoverCard generates a monogram from the title hash instead and a grid of letter-tiles is decoration standing in for hierarchy. This is also the repo’s own precedent: #font-gallery-one-per-row took the Font Gallery off cards onto hairline rules. DESIGNED SO PUBLISHING DROPS IN WITHOUT A REWRITE: a resource is a record rendered as a row, so a published artefact is the same record plus an author and a visibility gate, and the approval queue filters the list rather than changing it. No save/view/trending counts anywhere — there are no users, so every one would be a zero dressed as engagement. A REAL BUG FOUND AND FIXED ON THE WAY: GATED_ROUTES in discoverUtils.js was a hand-kept literal and had drifted BOTH ways. /create/file-converter had SHIPPED and was still listed, so coverr, unsplash and transform-tools silently lost their hand-off; /create/box-shadow is soon:true and was NEVER listed, so cssscan-shadows and cubic-bezier offered a button landing on the workshop placeholder. Its own comment had promised it would be updated when a tool shipped and nobody ever did, so it now DERIVES from toolTree’s soon flags and cannot drift again. WHAT THE FOUNDER MUST DO NEXT, and it is curation rather than engineering: NINE OF THE 22 RESOURCES HAVE NO LIVE HAND-OFF — all four Inspiration entries, all three Components entries, plus cssscan-shadows and cubic-bezier. Every one points only at Component Designer or Box Shadow, both still soon, and several carry copy telling the reader to "rebuild it in the UI Builder", a tool that does not exist yet. Either those two tools ship, or those records get repointed at tools that do. PROOF: tests/unit/discover-handoff.test.js (11 tests, 9 mutations all RED, no-op control GREEN) and tests/user-sim/51-curated-resources.spec.js (5 tests, 6 mutations all RED, control GREEN), the latter following EVERY hand-off destination to prove a real tool is on the other side. One mutation survived the first spec — the dead-end check looked for "coming soon" and CreateTool’s SoonState actually reads "<Tool> is still in the workshop", so it matched nothing; it is structural now. NOT DONE, DELIBERATELY: DiscoverCard.jsx (233 lines) and its ~147 .dsc- rules in global.css are the last of the #196 shell and are now provably unreferenced — this page chose rows over its card face. Retiring them is a dead-code sweep and belongs in its own PR, exactly as #196 was. Its NOT DONE - the DiscoverCard.jsx retirement - landed in #420 (2026-09-08).' },
  { id: 'community-backend', title: 'Build the Firebase community publishing backend', priority: 'P1', effort: 'L', area: 'Product', status: 'deferred', note: 'FOUNDER DECISION 2026-09-05, recorded in CHANGELOG.md. MODERATION IS AN APPROVAL QUEUE. NOTHING PUBLISHES UNTIL THE FOUNDER APPROVES IT. He chose this over publish-then-remove ON LIABILITY GROUNDS, as a solo developer - so it is a hard constraint on the design, not a preference to be traded away for throughput. TWO CONSEQUENCES. (1) The approval queue is an ADMIN SURFACE, so it inherits the unmet admin custom claim in docs/OWNER-ACTIONS.md section 4.4 - that is now a blocker for this row and not only for the feedback panel. (2) Per discover-buildout, the curated galleries ship BEFORE this, so this is no longer on the critical path for Discover. Original item below. Architecture is decided: Firestore + Storage, transactional lowercased handle registry and moderation state. Do not consume a new serverless slot without reconciling the 12-function ceiling. WHAT IT ACTUALLY NEEDS, ADDED 2026-09-05 while building [discover-buildout]: (1) Firebase publishing — Firestore + Storage, with the transactional lowercased handle registry; (2) real accounts, because a published artefact needs a durable author and a handle; (3) the approval queue itself — pending/approved/rejected state plus an admin surface to work it, and Admin.jsx ALREADY RUNS EXACTLY THIS MODEL for community prompts, so mirror it rather than inventing a second one; (4) abuse handling — submission rate limits, report, block, and a one-move unpublish. ROUTE BUDGET IS A REAL CONSTRAINT, not a footnote: api/ holds 12 of 12 Vercel functions and TWO tests fail the build on a 13th (account-deletion.test.js and ai-provider-path.test.js each read the directory and assert the count), so any server-side moderation work must reuse an existing route or retire one. THE CURATED SURFACE IS ALREADY SHAPED FOR THIS: /discover/resources renders a record as a row, so a published artefact is the same record plus an author and a visibility gate, and the queue filters the list rather than changing it. STATUS IS deferred, NOT blocked-on-an-unknown: it waits on a founder decision that has been made, and on there being users to fill it.' },
  { id: 'learn-content', title: 'Build the Learn content library', priority: 'P2', effort: 'L', area: 'Content', status: 'partial', note: 'FOUNDER DECISION 2026-09-05, recorded in CHANGELOG.md. TWO ANSWERS, SCOPE AND VOICE. (1) SCOPE: Learn is PROPER DESIGN EDUCATION - real articles on colour theory, typography and accessibility. It is NOT how-to guides for our tools and NOT product documentation, both of which were plausible readings of this row before today. (2) VOICE: neutral and factual, NOT the founder voice, no first person. He chose that deliberately so the content can be written at scale and he only fact-checks it. NOTE THE CONTRAST WITH founder-intro-popup, which is the opposite call for a reason: an educational article is not his to sign, a welcome from the founder is nothing but his. Original item below. Information architecture exists; articles and learning journeys remain. Homepage Soon Learn cards should be non-navigable preview states until scoped article destinations ship, then link to those destinations instead of looping generically to /learn. PARTIAL: the structure is built and three articles are published. STRUCTURE — src/data/learnIndex.js is the article registry (slug, title, topic, dek, meta description, measured word count, sources, section list). It is plain JS with no JSX because routeMetaMap.js spreads its titles and descriptions in and scripts/route-matrix.mjs imports that in bare Node; one entry there is what makes a route render, prerender, appear in sitemap.xml and get a vercel.json rewrite. Prose lives in src/data/learn/*.jsx and renders through src/pages/LearnArticle.jsx at /learn/:slug; an unknown slug renders NotFound rather than redirecting, so a dead guide URL is a real 404. PUBLISHED — /learn/colour-contrast (WCAG thresholds, the relative-luminance formula, what a ratio cannot see), /learn/type-scales (size = base × ratio^step, the clamp() that joins two scales, measure and line height), /learn/colour-spaces (sRGB vs HSL vs OKLCH, gamut, what CSS does between two colours). Each ends at the tool that does the thing it explains. VOICE — neutral reference register, no first person, founder decision of 2026-09-05. Every threshold is quoted from a linked clause and every figure is either arithmetic the reader can repeat or computed on the page by the product’s own functions: TokenContrastTable reads this page’s custom properties and runs contrastRatio(), TypeLadder builds its rows with buildFluidScale(), ReadingMeasure measures the reading column, ColourMixTable paints four color-mix() declarations and reads them back off a canvas. PRERENDERING — 28 shells became 32. /learn left SOON_SURFACES in scripts/route-matrix.mjs (now empty) because the surface has content, which also settles the contradiction recorded there for the founder: the served shell said noindex while robotsFor for /learn had always returned index,follow. NAV — the Learn mega-menu, the /learn landing and /sitemap now lead with the three live guides; the eight LEARN_GROUPS topic rows stay, keep their Soon badges and remain non-actionable, so live and unbuilt read apart. WHAT REMAINS — five of the eight roadmap topics have no article (Principles, Themes, Brand, SEO, Marketing, AI assistants, Help), there is no per-topic index page, no search over article text, and no share card for the Learn section (articles fall back to the homepage card, which is what /learn did before). NEEDS FACT-CHECK: see the PR description for the four claims worth a second pair of eyes. 2026-09-06 — TWO MORE GUIDES, AND THE FIRST ROADMAP ROWS TO STOP SAYING SOON. PUBLISHED: /learn/theme-systems (one set of token names holding two sets of values; why inversion is not a theme; which way elevation runs in the dark; prefers-color-scheme, prefers-contrast, forced-colors and color-scheme quoted verbatim from Media Queries 5 and CSS Color Adjust 1) and /learn/brand-colour (the jobs one hue is asked to do, the ramp that resolves them, and the computed result that NO stop of the product’s own default ramp clears 4.5:1 against BOTH theme grounds — which is the arithmetic behind the rule that a role token holds a value per theme). TWO NEW LIVE TABLES, both measuring rather than quoting: ThemeInversionTable reads this page’s own tokens out of the running stylesheet, inverts each pair and runs contrastRatio() on both halves (the light accent #0B5ED7 inverts to the orange #F4A128 at hue 36, while the real dark accent #4A90FF is hue 217 — the same blue, lifted); BrandRampTable generates the ramp with generateTintScale() at ColorStudio’s own defaults and measures every stop against both grounds. ROADMAP: themes and brand now carry their guide’s route and soon:false, and every surface that shows what is still to be written reads the new LEARN_ROADMAP rather than LEARN_GROUPS — a delivered row and its guide share a route, so listing both put two rows on one data-route in the visual sitemap and two links to one page in the mega menu. The /learn hero hint carries ’every figure cited or computed’ again, which this file already said was its job and which the code had drifted away from. NEW GUARD tests/unit/learn-figures.test.js, built the way daily-tips.test.js is: it recomputes every number in both guides from the sRGB and WCAG formulae written out in the test rather than imported, reads the product hexes back out of global.css, asserts the ramp is still generated the way the Colour Studio generates it, and fails on first-person or marketing register in ANY guide (the neutral voice is the founder’s own decision of 2026-09-05, recorded above). npm run build now prints 35 route shells, up from 33: two guides, two routes. WHAT REMAINS — six roadmap topics with no guide (Principles, Typography, SEO, Marketing, AI assistants, Help), no per-topic index page, no search over article text, and no share card for the Learn section. 2026-09-07 — TWO TYPOGRAPHY GUIDES, AND THE TYPOGRAPHY ROADMAP ROW STOPS SAYING SOON. PUBLISHED: /learn/typeface-metrics (font-size sizes the em box and not the letters, so two faces at one size are not one size; the aspect value, quoted from CSS Fonts 4 as “the x-height of a font divided by the font size”; the adjustment c = (a / a’) s worked at the specification’s OWN example figures — Verdana 0.545 and Times 0.447 give 19.51px, three and a half pixels of difference; numeric weight 1–1000 and font-synthesis-weight; and what WCAG 1.4.4 and 1.4.12 let the reader change underneath a design) and /learn/font-loading (the block, swap and failure periods quoted verbatim from the font display timeline; what “invisible” actually means, which is the FALLBACK’S layout with the ink removed rather than a stable one; why the line re-breaks when the file lands; unicode-range and what a subset costs; size-adjust and the line-box overrides). THREE NEW LIVE TABLES, all three measuring the browser they are rendered in rather than quoting: TypeMetricsTable paints the --font and --mono stacks plus the reader’s own three generic families to a canvas and reads the ink above the baseline off x and H (Manrope measures 0.547 x-height and 0.719 cap height per em here; the system serif measures 0.453, which is why the last column says it needs 19.3px to match) and it uses the product’s OWN detectCanvasFontRendered() to drop any family it cannot confirm rendered, so a row is never a measurement of the fallback under the webfont’s name; FontFaceTable reads the four @font-face rules out of the CSSOM, computes each one’s code-point count from its own unicode-range (387 and 1,162) and asks document.fonts.check() whether the file that would draw the first printable character of that range has been downloaded — the extended subsets answer No, which is the section’s argument standing in front of the reader; FallbackShiftTable paints one sentence in the declared stack and in the stack with the webfont removed and measures both, so the swap is a number rather than a claim. Its rows are font-family VALUES rather than family names, because painting text in a family that is not installed silently measures something else and reports it under that family’s name. ROADMAP: typography now carries /learn/typeface-metrics and soon:false. The other five Soon rows were NOT taken and the reason is a founder question, recorded here rather than papered over — principles and help are being written in parallel on feat/help-and-principles, and seo, marketing and ai-assistants are rows written BEFORE the 2026-09-05 scope decision that made Learn design education rather than marketing. Each would need either a topic outside TOPICS (Colour, Typography, Accessibility) — which learnIndex.js’s own comment warns is how an index page ends up with a category holding one thing — or the kind of remembered statistic #403 cut three of. Whether those three rows still belong in Learn at all is the founder’s call. A DEFECT FOUND IN A BROWSER AND NOT ON DISK: the metrics table first measured at 100px and printed three decimals, but at 100px Chromium returns the ink extents already rounded to whole pixels — Manrope’s x came back as exactly 54 — so every ratio was a multiple of 0.01 and the third decimal was invented. Measuring at 1000px makes the same reading 546.875. Manrope’s real aspect value is 0.547, not the 0.540 the first pass would have published. NEW GUARD tests/unit/learn-type-figures.test.js, built the way learn-figures.test.js is: the unicode-range interval arithmetic and the adjustment formula are written out in the test rather than imported, the font stacks and weight ranges and @font-face rules are read back out of global.css, and one test derives rather than pins — it reads the em and the toFixed() places out of the component and asserts em >= 10^places, so raising the precision without raising the em fails as surely as lowering the em does. It also fails if a standards figure (3s, 100ms, the four Text Spacing multiples) is stated outside the <Spec> that cites its clause. The rendered half asserts that the last printed decimal is not zero in every row, because a digit that cannot vary is a digit that was not measured. npm run build now prints 37 route shells, up from 35. MUTATION: 9 unit and 5 rendered call-site breaks plus the em, all killed, no survivors — and two of the guards only became able to kill their mutation BECAUSE of the exercise (the sources check was comparing base URLs so a second clause on the same spec satisfied it, and the matched-size column could be replaced by a constant 16 with row one still correct). WHAT REMAINS — five roadmap topics with no guide, two of them being written in parallel and three of them awaiting the founder’s call above; no per-topic index page, no search over article text, and no share card for the Learn section. 2026-09-08 — THE THREE PRODUCT GAPS THIS ROW HAS CARRIED SINCE THE FIRST GUIDE ARE CLOSED; THE GUIDE GAPS ARE NOT, AND THIS STAYS PARTIAL. PER-TOPIC INDEX, WITHOUT A NEW ROUTE: src/components/LearnGuideIndex.jsx renders one labelled <section> per topic with a real h3, grouping by each guide’s own `topic` — TOPICS in learnIndex.js, the closed list learn-articles.test.js already enforces, so the index cannot invent a category and cannot leave a guide out; a topic with no guide renders nothing. Accessibility holds one guide today and says so (“1 guide”), which is a fact about the library and not a layout defect. Considered and rejected: a /learn/<topic> route each, because three routes would have raised the shell matrix, the sitemap, vercel.json and llms.txt for pages holding nothing the landing does not, and would have put a second URL on every guide’s topic. npm run build still prints 39 route shells; llms.txt is byte-identical to the generator’s output. SEARCH OVER ARTICLE TEXT, NO LIBRARY, NOTHING ON FIRST PAINT: the guides are JSX, so their prose is extracted at BUILD time by scripts/learn-search-text.mjs — the same proseOf() that measures `words`, with the quote entities kept as marks so “WCAG’s” indexes as wcag’s and not wcags — and served as the Vite virtual module virtual:learn-search-text (vite.config.js). LearnGuideIndex imports it dynamically on the first FOCUS of the field, the way the Emoji Library requests its index. COST, MEASURED: one chunk of 51,651 B raw / 18,869 B gzip / 15,541 B brotli, fetched only when a reader touches the field; /learn’s first paint carries 0 bytes of it. Rendering the article components to string on the landing instead would have pulled the whole article chunk and its measuring tables into a page whose job is to hand over links, and was not done. The matcher (src/utils/learnSearch.js) is 130 lines: every term must match (AND — “ratio” alone is in every guide), title outranks dek outranks section heading outranks body, ties keep registry order, and while a query is live the topic group holding the strongest hit moves first so the best result is the first card on the page. A body-only hit prints the sentence it was found in with the whole word marked, because the card otherwise gives no way to see why a title that says nothing of the sort came back. Results reach a role=status aria-live=polite region (“4 guides match “luminance”.”); the empty state names the topics, says what to try instead, and offers a focusable “Show all 7 guides” that returns focus to the field; Escape clears. THE REVEAL TRAP: the landing’s scroll reveal is a one-shot IntersectionObserver scan at mount, so a card re-mounted by a search would carry data-reveal with nothing left to reveal it and sit at opacity 0 for ever; cards carry the attribute only until the first keystroke, and the spec reads computed opacity because toBeVisible() does not see it. SHARE CARD FOR THE LEARN SECTION: Learn is now a SECTION in scripts/share-cards.mjs, drawn by og-cards.mjs in the same system as the tool cards — flat hue rail (the --hue-ai the /learn landing paints its own cards with), the section name, a blurb that COUNTS (“7 guides on colour, typography and accessibility”) and every published guide by its navLabel as the subject; no gradient, no glow, no tagline. Names, count and routes are derived from learnIndex.js, so a new guide inherits the card and fails share-cards.test.js until `npm run og:cards` redraws it; cards.json now records each section’s blurb beside its tool list for the same reason. The build line moves from 19 to 27 routes on a section share card; the other eight PNGs came back byte-identical from the regeneration. TWO DEFECTS THE RENDERED SPEC CAUGHT BEFORE THEY SHIPPED, AND ONE PRE-EXISTING: the topic heading’s accessible name read “Colour3 guides” (no whitespace between the topic and its count — CSS gap is not text); the announcement said “1 guide match”; and the brand-colour registry entry titled its last section “When the brand hue is spoken for” while the prose h2 says “…is already spoken for”, so the contents rail had been mislabelling that anchor since the guide shipped — corrected to the prose, which is the article. ALSO FOUND, NOT A PRODUCT DEFECT: `vite preview` answers a bare /learn with the SPA fallback and only /learn/ with the prerendered shell (measured: og-image.png vs og-learn.png on one build), whereas Vercel’s rewrites map the bare path to the shell; the rendered share-card test therefore reads the SERVED DOCUMENT at route/ the way an unfurler does, and says why. PROOF: tests/user-sim/67-learn-index-search-card.spec.js (8 tests — topic sections a screen reader can jump between, body-text search with its announcement, the empty state and its keyboard way out, keyboard from the field through a result into the guide, painted results with and without reduced motion, 390px with no sideways scroll and a 24px clear control, measured contrast ≥ 4.5:1 in both themes with animations settled first, and the card on /learn and every guide with a control on /discover). tests/unit/learn-search.test.js (10) and tests/unit/learn-share-card.test.js (6). MUTATION, ALL AT THE CALL SITE: 10 rendered — body text dropped from the haystack, live region removed, every guide under the first topic, empty state’s way out made a span, data-reveal kept after a search, h3 made a div, Learn section claiming no routes, heading whitespace removed, “matches” made “match”, best-group ordering disabled — 10 killed; 6 unit — quote entities dropped, card naming three guides, AND made OR, guide routes typed by hand, virtual module never imported, first guide missing from the index — 6 killed; no survivors. One no-op recorded rather than counted: `hidden` on the empty-state button survived because .ui-pill{display:inline-flex} outranks the UA [hidden] rule, so the control was still there; the mutation was rewritten to remove the control. GATE: lint 0 errors 25 warnings (ceiling), build 39 route shells (27 on a section share card, 16 with a BreadcrumbList), unit 1745/1745 (1,725 before main moved under this branch; #427–#429 added twenty), users 854 passed / 13 skipped (all 12-ui-system-builder) / 1 failed on the rebased tree — 25-defect-sweep’s icon-pack-label test, red again on a fresh test build run alone (16 passed / 1 failed) because api.simplesvg.com, the host IconLibrary.jsx fetches its packs from, is CORS-blocked in this sandbox (100 blocked-fetch lines in that run’s feedback loop); nothing in this lane touches icons and it could not be verified green on main from here. The run before main moved under the branch (836 passed / 13 skipped / 3 failed) had two ERR_NO_BUFFER_SPACE casualties on /create/font-gallery that the solo re-run cleared, plus the snapshot move below. 64-computed-style-snapshot: ONE value moved and the /learn baseline was regenerated the way the spec header says (UPDATE_STYLE_BASELINE=1, --workers=1, 26 passed) — .lidx-card opacity 1 -> 0 at 390 only, because the search field, topic links and topic heading now sit above the first card, which at capture is below the first screen and has not been scroll-revealed; at 1440 the baseline already held 0 for the same reason. No .lidx-* rule changed and /learn/brand-colour is untouched. WHAT REMAINS — the five roadmap topics with no guide: two being written in parallel on feat/help-and-principles, three (SEO, Marketing, AI assistants) awaiting the founder’s call recorded above on whether they belong in Learn at all. Nothing else on this row is open.' },
  { id: 'public-route-prerender', title: 'Prerender eligible public routes', priority: 'P1', effort: 'L', area: 'Growth', status: 'done', note: 'DONE. prerender: wrote 27 route shells -> 28. THE ROUTE LIST WAS THE SITEMAP, and that was right until the two sets diverged. /home is a byte-identical duplicate of / and is what the nav logo links to on every page, so it is the most-linked URL on the site; routeMeta.js already canonicalises it onto /, which means it MUST NOT be advertised in sitemap.xml — and it MUST be prerendered, because it matched no rewrite, fell to the catch-all, and was served dist/404.html. Every share of the site’s most-linked URL unfurled as "UI L4B | Page not found", and a non-JS crawler read noindex on it while the hydrated page said index,follow. One list could not express that, so scripts/route-matrix.mjs is the matrix and the sitemap is its ADVERTISED SUBSET; the test asserts containment, not equality. THE MATRIX: 28 prerendered, 70 excluded, each with a reason derived from the app’s own authorities rather than typed — 50 retired URLs (LEGACY_REDIRECTS), 8 private/auth/admin (isPrivateRoute, the same function the runtime uses to stamp noindex), 6 Soon (isSoonRoute, off toolTree flags), 4 category homes, 1 Soon surface (/learn), 1 root shell. THE FOUR CATEGORY HOMES ARE NOT PAGES: CreateTool.jsx redirects /create/typography, /create/imagery, /create/ai-tools and /create/icons-emoji to their first tool, so prerendering them would mint an indexable shell for a URL that bounces. /create/color is the exception because App.jsx intercepts it above CreateTool — the one fact in the matrix asserted rather than derived, and the test reads both source files so it cannot rot. CANONICAL TRUTH NOW SURVIVES BY CONSTRUCTION: prerender.mjs built ORIGIN + route, which is correct for all 27 routes it happened to emit and wrong for /home; it now calls canonicalUrl() and robotsFor(). FOUND, a green test with an invented reason: not-found.test.js claimed robotsFor(/learn) resolves to noindex. It does not — isSoonRoute only understands the Create tree, so /learn returns index,follow. The conclusion was right and the stated mechanism was fiction, which is the more dangerous of the two. FLAGGED, NOT ACTIONED: /learn’s served shell says noindex and its hydrated page says index; making those agree is a call about whether /learn belongs in the index at all.' },
  { id: 'ai-diagnostic-env', title: 'Protect or disable the AI diagnostic endpoint in production', priority: 'P2', effort: 'S', area: 'Security', status: 'done', note: 'DONE. GET /api/ai?diag= was gated on a string committed to this repository — a published password in front of an endpoint that enumerates which of the deployment secrets exist. It now requires a verified administrator (the shared allowlist + email_verified, same check as /api/verify-admin) and answers 404, not 403, to anyone else. A server-only DIAG_CODE break-glass exists with NO default because the diagnostic’s most valuable report is “the Firebase credential is broken”, and admin auth runs on that same credential; it is compared in constant time. No credential values are exposed — presence and length only, as before. NOT IN SCOPE, STILL OPEN: src/pages/Admin.jsx uses the same literal as a client-side session unlock. That was never a security boundary (it ships in the browser bundle) but it should stop sharing a string with anything that was. docs/OWNER-ACTIONS.md P1 row updated for the new URL. Security gate: run, no HIGH/MEDIUM findings.' },
  { id: 'csv-formula-injection', title: 'Neutralise CSV formula injection in every export', priority: 'P1', effort: 'S', area: 'Security', status: 'done', note: 'DONE. Found during #250 and never recorded until 2026-08-15. Correct RFC 4180 quoting is NOT a defence — a spreadsheet decides a cell is a formula from its first character, quoted or not, so a cell beginning = + - @ (or tab / CR) executes on open. THREE exports had it, not the one that was reported: AltTextGenerator downloadCSV (alt text is model output from a user-supplied image), Admin exportUsersCSV (displayName, company, location are user-controlled), and Admin exportCSV for feedback — which is the sharp one, because /api/support accepts subject, message and email UNAUTHENTICATED and the file is opened by an admin, so the attacker picks the payload and a privileged user runs it. All three now go through src/utils/csv.js (csvCell / toCsv), which prefixes a lone apostrophe. Plain numbers are deliberately exempt so a legitimate -5 is not corrupted. tests/unit/csv-injection.test.js also fails the build if any export hand-rolls quote escaping again, which is how all three got it wrong the first time.' },

  // ── Founder batch, 2026-08-08 ────────────────────────────────────────────
  // Reported by the founder in conversation with the Director. Three of the
  // original items are gone from this block — the Temperature snap, the Font
  // Pair panel overflow and the auth popup closing to /home were reproduced,
  // fixed and covered by tests on fix/nav-login-slider-panel (see CHANGELOG).
  // Every item that REMAINS is OBSERVED ONLY: it is the founder's description
  // of what he saw, and none of it has been reproduced by an engineer, isolated
  // to a cause, or pinned by a failing test yet. Reproduce first, then fix — do not
  // treat the description as a diagnosis. Recorded 2026-08-08.
  { id: 'image-palette-marker-popup', title: 'Enlarge and centre the image→palette popup so markers are visible', priority: 'P1', effort: 'S', area: 'Colour', status: 'done', note: 'DONE — and the report was the visible half of a worse fault. The picker is now a centred 720px modal (useModalDialog, so it also gets Escape, a focus trap, a scroll lock and focus restoration) instead of a 300px toolbar dropdown. Underneath: every picker point is a normalised coordinate in the SOURCE image but was rendered as a percentage of a hard 16/10 stage using object-fit:cover, so any image of another shape was cropped and the markers sat over pixels they had never sampled — and dragging one sampled the source at the cursor’s fraction of the BOX, which on a 4:1 image is a different pixel from the one under the cursor. The stage now takes the image’s own ratio. Proof: tests/user-sim/29-image-palette-picker.spec.js re-derives the object-fit:cover mapping and reads the painted pixel off a canvas, so it is not asserting app arithmetic back to itself.' },
  { id: 'emoji-library-search-perf', title: 'Make Emoji Library search work and the surface responsive', priority: 'P1', effort: 'M', area: 'Imagery', status: 'done', note: 'DONE. The queue item asked which of the two reported symptoms caused the other; the answer is NEITHER — they were independent. The search failure was a data fault (the old filter matched a per-CATEGORY keyword table, so a query selected categories rather than emoji) and #272 replaced it with a real per-emoji CLDR index; the render cost was already bounded by a windowed grid, and the index is loaded on demand rather than bundled, so fixing the first did not cost the second. #272 shipped as WIP with ranking unfinished and this item was never updated — the remainder is closed here. THE RANKING BUG FOUND ON PICKING IT UP: scoreEmoji’s prefix band scores on coverage, and its own comment claimed “Ὡ7 wins car”. It did not — it was TENTH, behind carp streamer, credit card, card index, card file box, card index dividers and flower playing cards, because a three-letter query covers 75% of a four-letter unrelated word and that beat the whole-keyword line. The band now only outranks a keyword when the leftover letters are a suffix English actually adds. Measured: the whole grid renders 430 buttons at 1440x900 against a catalogue of 1,636. Proof: tests/unit/emoji-search.test.js (25) + tests/user-sim/32-emoji-library.spec.js (4).' },
  { id: 'colour-picker-ui', title: 'Replace the colour picker with a richer picker', priority: 'P1', effort: 'M', area: 'Colour', status: 'partial', note: 'PARTIAL, and what remains is a PRODUCT QUESTION rather than work. The queue item said "scope which surfaces adopt it before building", so that is what decided the split. Already existed: the SV pad, the hue slider, the presets. SHIPPED EARLIER: a hex/rgb/hsl notation dropdown — a LENS, the component still emits #rrggbb because every consumer stores hex and widening that contract is far larger than a display preference earns — SAVED SWATCHES, the last 12 colours committed from any picker in the app, shared across surfaces via utils/recentColors.js, and an eyedropper. The field accepts all three notations whatever the dropdown says, so pasting out of devtools works. DECLINED, both for the same reason: ColorPickerPop has twelve call sites and none can consume an alpha or a gradient. A palette swatch must stay opaque (contrast maths, tint scales, exports); a gradient STOP is {color,position} and alpha would have to reach gradientCss, gradientSvg, every export, the saved shape and the library data first; an icon colour is one SVG fill. A control no surface can consume is dead code that looks finished. Raised as P-005 in docs/PROPOSALS.md, which names the three ways forward. TWO DEFECTS FOUND AND FIXED 2026-09-06, both in the saved-swatches feature this item shipped, and both invisible to the tests that shipped with it because all three of them commit through the HEX FIELD. (1) remember() was wired to the pad POINTER release, a swatch press, an eyedropper pick and a typed value — so mixing on the pad with the ARROW KEYS recorded nothing, and moving the HUE STRIP recorded nothing by any input at all. A keyboard-only user could mix a colour, watch it change on screen, and watch the Recent grid stay empty; so could anyone who adjusted only hue. Fixed by giving the hue strip and the pad keyboard the same RELEASE boundary the pad pointer already had, rather than by widening commit(), which fires per pixel of a drag and would spend the whole twelve-slot list on one sweep. The release handlers are guarded to the keys that actually MOVE each control, because Tab and Escape raise keyup on them too and a panel opened and abandoned must not record a colour nobody chose. (2) The pad declared role="slider" with no aria-valuenow, which ARIA REQUIRES — an invalid node that some screen readers announce nothing at all for. It is a 2D control, so valuenow carries saturation and aria-valuetext keeps saying both axes; the hue strip gained a valuetext too, because a bare "212" is not a hue to anyone who cannot see the strip. Proof: tests/unit/color-formats.test.js (16) + tests/unit/recent-colors.test.js (9) + tests/user-sim/35-colour-picker.spec.js (19, up from 15), which asserts the two declined controls are ABSENT so a stub cannot appear without a decision, and now drives the PAD and the STRIP rather than the helper. Verified by mutation in both directions: reverting the pad keyup, the hue handlers and the aria attributes fails exactly their own three tests and leaves all sixteen older ones green — which is the measurement of how wide the hole was — and dropping the key guards fails the abandoned-panel test with one recorded colour where none was chosen.' },
  { id: 'colour-role-names-by-system', title: 'Name colour roles to suit the active colour system', priority: 'P1', effort: 'M', area: 'Colour', status: 'done', note: 'DONE, shipped in #244 — reproduced exactly as the founder reported it ("all systems say the same from left to right primary, secondary, accent, subtle, deep"). src/utils/paletteRoles.js carries HARMONY_ROLE_LABELS, one label set per harmony, mirroring each generateHarmony branch: Monochromatic now reads BASE/LIGHT/LIGHTER/DARK/DARKER instead of calling a subtle tone "Accent". Only the DISPLAY caption changed — ROLES[i] stays the stable export token identity, so saved palettes and exported tokens are unaffected.' },
  { id: 'semantic-pack-extra-colour', title: 'Add a semantic colour to the premade packs and show real usage', priority: 'P1', effort: 'M', area: 'Colour', status: 'done', note: 'DONE. Two halves, two commits. HALF 1, THE FIFTH ROLE, argued by role rather than by count. Success, warning, error and info are four SETTLED conditions: three outcomes and one piece of ambient context. Nothing covered "underway, outcome not known yet". IT IS NOT A COLOUR LOOKING FOR A JOB and that was the objection worth answering: the app already needs a fifth signal and, having no token, spells it FIVE ways, each confirmed to have a live consumer. .alt-card-status ("Generating...") used var(--accent-strong), the BRAND colour. .fc-status ("Converting...") used var(--t2) and gave up on colour entirely - two tools doing the same job answering differently. Admin STATUS_COLORS/STATUS_BGS spelled in-progress as var(--accent)/var(--accent-bg). Admin TYPE_COLORS, TYPE_BGS and DONUT_COLORS[4] hard-coded #a855f7 and rgba(168,85,247,.1) - three raw literals with NO dark value, in an array whose other five entries are all tokens. .adm-check-icon.pending hard-coded rgba(245,158,11,.1), which is WARNING amber. #a855f7 is Tailwind purple-500: the app had already chosen this hue by hand, it just had nowhere to put it. All five sites now consume --pending, so the token is not dead code. CHECKED AND NOT COUNTED, so it is not re-reported as evidence: .fp-dot-progress is also var(--accent) for "in progress", but the whole .fp-* block is DEAD CSS with zero JSX consumers in src/ or tests/. WHY VIOLET: the four reference hues are 0 (error), 38 (warning), 142 (success) and 217 (info); the arc from 217 round to 360 is 143 degrees, the largest gap on the wheel by nearly two, and it is the arc info’s own Custom slider used to spill into unopposed because roleHueArcs caps each role at the midpoint to its neighbours and info had no clockwise neighbour. Pending at 258 narrows info to 179-237 as a side effect. CONTRAST, MEASURED, BOTH THEMES, on the three grounds a state colour lands on: light --pending #7c3aed is 4.91 / 5.70 / 5.22 and carries white at 5.70; dark --pending #a78bfa is 6.98 / 6.65 / 6.39. THE TRAY LISTS EVENLY, and the diagnosis is arithmetic not taste. SEVEN CARDS IS PRIME, so it was ragged at every width: 4,3 at 1280 and 2,2,2,1 at 390/640/834, one row with a hole in all four. No column count fixes a prime; only the cell count does. The extra cell went to the FIRST card, not to an invented eighth pack - the first card is Balanced, the default, selected on load, and its description is the start-here advice. 7 cards plus a 2-cell lead is 8 cells, which fills a 4-column grid in exactly 2 rows and a 2-column grid in exactly 4. Rows with a hole 1 to 0 at all four widths in both themes. The emphasis is TRUE - it marks the recommended option - which a stretched last card or a filler pack would not be. A FILLER EIGHTH PACK WAS DECLINED and is a founder decision if wanted. HALF 2, THE USAGE EXAMPLES, and it was the bigger half. What was there was five identical rows - a coloured circle, the role name, the role’s own definition, a Copy button - which is a swatch with a caption: it shows you the colour, it does not show you the DECISION. Now one scene per role and every scene a DIFFERENT component, which is the whole point, because five identical shapes cannot show that a colour behaves differently as a 1px border than as a 40px fill. SUCCESS is a switch (the one place a semantic colour governs a control rather than a message). WARNING is an advisory form field - border and helper text carry it and the action stays available, which IS the difference between warning and error and is invisible on a swatch. ERROR is the alert and the only solid button in the set. INFO is the quietest object, with no fill at all - if info is as loud as error the scale has no top. PENDING is an activity row with a turning icon and a status badge. Between them they cover the four things the founder named: icons, buttons, switches, alerts. THE FINDING THAT MATTERED, and it could not have been eyeballed: the first cut used FIXED shade steps - text at 800, fills at 600 - the way the old swatch rows did. Measured across all 7 bundles x 2 panels x 5 roles, that was 18 FAILURES. Material’s warning ramp puts a bright orange at 800 (#ef6c00), so "Already used by 2 projects" was 2.90:1 on its own fill; white on Material’s error 600 (#e53935) is 4.23:1, a destructive button whose label misses AA; the info link was 3.46:1. A FIXED INDEX IS AN ASSUMPTION ABOUT A RAMP’S SHAPE, and the packs do not all have the same shape. sceneColours() now asks for the first shade that actually clears its floor - 4.5:1 for text, 3:1 for icons and control boundaries - against the ground it will really sit on, including the dark fill, which is computed in JS the same way the CSS computes it so the number measured is the number painted. Re-measured: 252 pairs, ZERO failures, worst pairing 3.70:1 against a 3:1 floor. The colours still come only from the user’s own ramp; it is just read at the step that works. MOBBIN (web) drove the shape and is cited per decision in the source: Lovable, Stitch and GitBook all pair token editing with a preview built from real product UI; v0 puts a live account form, KPI card and a payments table with a Status column beside its token list; Base44 the same with a finance dashboard; Framer shows one real button in its STATES rather than one swatch per state; MagicPath captions each colour with the job it does. For the roles: Plane gives In Progress its own colour beside Done and Cancelled, Mistral AI makes Pending a first-class tab beside Fetched and Failed, Laravel Cloud greys every pending step (a system with no pending colour), and Confluence’s status picker ships FIVE colours, the fifth purple. ALSO FIXED: the handoff block typed "Copy 40 CSS variables" as a literal and named --color-info-900 as the last token; both derived now from the source the hero strip already used. Back-compat: a project saved with four keys would have dropped the fifth role from the ramps, the CSS, the export and the cache AND pinned the tray to "Custom mix" forever, because activeStateBundle compares by JSON.stringify - stateColors seeds from the default bundle now. Two suite assertions typed the old role count (40 tone cells, 4 preset rows) and are derived from the ramps on the page now, so they guard "no ramp is short" and "no role loses its row" rather than a number. GUARDS: tests/unit/semantic-roles.test.js (7) and tests/user-sim/41-semantic-usage-examples.spec.js (3), verified by ELEVEN mutations between them - a bundle losing the role, a duplicated cue, a too-light --pending in each theme separately, the raw hex returning to Admin, the ramps removed, a ramp rendering 9 stops, a role losing its preset row, the scene colours reverting to fixed steps, the scenes painted from fixed sample hexes, and all five scenes collapsed to one shape. Every one goes red on its own subject and green again on restore. NOT RENDER-VERIFIED, and recorded in the file: Admin.jsx is admin-only and behind auth, so its swaps are source-level. The badge contrast there belongs to [flair-tone-contrast]’s class of text-on-a-tint-of-itself.' },
  { id: 'contrast-checker-overhaul', title: 'Overhaul the contrast checker UI and its live preview', priority: 'P1', effort: 'L', area: 'Colour', status: 'done', note: 'Founder request (2026-08-08): the live preview needs a far better example, and the tool needs a UI/UX overhaul rather than a patch. DONE 2026-09-06. The design specification lives in the header of src/pages/ContrastChecker.jsx rather than in this note — it is a decision about what the page SAYS, so it belongs beside the markup. PREVIEW, done 2026-09-04: it used to render four lines that CAPTIONED THEMSELVES — "Large heading text (24px)", "Normal body text at 16px—", "Small print at 12px — the hardest test of all" and a pill reading "UI component" — so the page answered "what does 4.5:1 look like?" with a specimen of nothing. It is now a real surface (heading, body copy, solid button, outline button, small print with a link) in the pair, each element carrying ITS OWN threshold as a chip. THE RULE, worth reusing anywhere a preview annotates user-chosen colour: a verdict chip sits ON the user background but never PAINTS with it — page tokens only, because a chip drawn in the pair under test goes unreadable exactly when the pair fails, and this page has already shipped that defect once (its own verdict line at 2.94:1 against the 4.5:1 it enforces). LEFT PANEL, done 2026-09-06, which is what made this partial. It stated five checks as abstract TIER NAMES in a list separate from the colour fields, under a verdict reading "Passes some checks". Now: a single AA/AAA level control (Whereby, mobbin.com/screens/e86cb1da-6690-4693-bff7-5632af4a4f9c) that drives the badges, the checks, the preview chips and the fixes together; a measured ratio INLINE beside each colour field, each naming the ground it was taken against ("vs background" / "vs text"), since the two fields here form ONE pair and the number is the same on both sides; three checks written as sentences about real objects (Typeform, mobbin.com/screens/b78e49a7-bf27-4966-b5f6-e8f7fd5033b9) instead of five tiers; and the verdict as a short headline saying what the pair can CARRY (Hotjar, mobbin.com/screens/043235af-448c-414a-b5b9-94ef3f8376a2), deliberately level-independent so it does not change meaning when the radio does. NON-TEXT DOES NOT FOLLOW THE LEVEL: SC 1.4.11 is a AA criterion with no AAA counterpart, so it stays at 3:1 at both settings and the row says so — scaling it would be inventing a rule on the page that teaches the rules. The .cc-grid is no longer 1fr 1fr: the preview renders a PAGE and needs the room, so it is .88/1.12 and collapses at 1040px rather than 900px, where the narrower column made "Buttons, borders and focus rings are visible against it." wrap to three lines. THE DEFECT FOUND UNDER THE FIX ROW, and the largest thing in this item: the page promises "one click nudges either colour just far enough to pass" and it did not. The row was built on fixForeground/fixBackground, which take their direction from the OTHER colour luminance (bgLum < 0.5 means walk lighter) — and 0.5 is not the crossover, black and white are equally readable at 0.179. Every candidate was re-verified before being offered, so the page never showed a fix that failed; it silently showed NO fix. Measured over a 6-level-per-channel grid, 38,594 AA-failing pairs, through the component own memo: a TEXT fix existed but was not offered on 16,485 (42.7%), a BACKGROUND fix on 16,485 (42.7%), and "Make it pass" was EMPTY with a fix available on 6,561 (17.0%). #000099 on #009900 is one — 3.806:1, black clears 5.56:1, and the row was empty because the ground sits at luminance 0.228 so the search went toward white, which tops out at 3.78:1. Replaced by colors.js nearestPassingLightness, which scans BOTH directions along the seed own HSL lightness axis and keeps the SMALLER movement, the same rule pvInkFromFill and readableInk already use; it is a linear scan over the 101 integer lightnesses rather than a bisection because contrast along that axis is not monotone. It returns null — never its input — when no single-side move exists, and the page now SAYS so instead of showing an empty space. That state is only reachable at AAA: pure black clears 4.5:1 for every ground at or above luminance 0.175 and pure white for every one at or below 0.1833, so the ranges overlap and an AA fix always exists. fixForeground/fixBackground are untouched and still serve HomeWorkbench readableInk and labelGround, which pre-select their pole by measured contrast and use the walk as one candidate among six behind a >= 4.5 gate. ALSO FOUND: 06-colour-tool-workbenches AA sweep has listed `.cc-check-name` since it was written and no element ever carried it — the class was `.cc-check-label` — so the check text on the page that enforces contrast was the one thing that sweep never measured, hidden because a positive control over a UNION of six selectors cannot prove each member contributed. The class is now renamed to match, and `.cc-field-badge`, `.cc-check-foot` and `.cc-level-opt` joined the set. Proof: tests/unit/contrast-fixes.test.js (7, sweeping 46,656 pairs at both targets against an independent pole oracle, with the failing-pair population pinned as a positive control) + tests/user-sim/06-colour-tool-workbenches.spec.js (20, up from 12). Verified by mutation: reverting ONLY the call site to fixForeground leaves the unit sweep passing 7/7 and fails three browser tests, which is the exact shape this repo keeps paying for; making non-text follow the level, hard-coding a preview chip at 4.5, dropping the ground name from the badge and restoring 1fr 1fr each fail their own test.' },
  { id: 'font-gallery-ui', title: 'Improve the Font Gallery UI', priority: 'P1', effort: 'M', area: 'Typography', status: 'done', note: 'DONE — the browsing model is now a specimen GRID on the shared Library language (library-ui-language), replacing the full-width list. The rendered diagnosis, so it is not re-derived: the list showed five families of ~1,700 per screen at 1440; each 154px row spent about a quarter of its width on the specimen and left the rest empty; and the family NAME sat in a right-hand column roughly 800px from the face it named, having already been spelled out BY that specimen. The category filter ran outlined pills beside a filled segmented control for sort — the same two-idioms-in-one-row fault the Gradient Library had. Now: three columns at 1440 (a 400px floor, wider than the colour libraries\' 280px because a typeface needs more room to judge than a swatch does), name directly under its own specimen, one filter idiom, one sticky glass toolbar with preview text and size on a second row. Reserved metrics are preserved — the sample box is a fixed 68px and card heights stay uniform, which the suite still asserts. NOTE FOR ANYONE READING THE DIFF: tests/user-sim/11-typography-tools.spec.js previously asserted the OPPOSITE layout ("each typeface should begin on the same full-width line"). That assertion was changed deliberately to encode the new decision, not relaxed to go green — #243\'s row-alignment work was a fix WITHIN the list model, and the founder\'s direction was to change the model.' },
  { id: 'fontpair-browsable-selection', title: 'Make Font Pair selection browsable instead of recall-and-type', priority: 'P1', effort: 'M', area: 'Typography', status: 'done', note: 'DONE, and REPRODUCED first — the founder\'s report was exact and the cause was STRUCTURAL, not cosmetic. src/components/FontPicker.jsx was a native <select> fed by a search box. A <select> renders every option in the UI font, so choosing a typeface meant recognising a NAME in a list of names: there was no arrangement of that control which could ever show a face. The original comment defended it on keyboard/mobile/screen-reader support, which was a fair trade to consider and the wrong side of it for a tool whose entire subject is how type looks. Replaced by a specimen trigger (shows the CURRENT family rendered in itself) opening src/components/FontBrowseDialog.jsx — a browsable grid of specimen tiles on the shared Library language, each tile rendering its own family, with search and category filters. Everything the <select> gave for free is re-earned explicitly: focus trap, Escape, focus restored to the trigger (the new hooks/useModalDialog.js, lifted from FontGallery, which had the app\'s only correct implementation), and every tile a real button. Shared by Font Pair AND Type Scale, so one component fixed both. Also added utils/fontGallery.js isPickableTypeface: LOOSER than isGalleryTypeface on purpose — it keeps monospace (a mono body face is a legitimate choice, and this is a picker, not a curated gallery) and drops only what cannot render a specimen at all (emoji, icon, symbol, math, music, barcode), which would otherwise draw the preview word as a row of boxes.' },
  { id: 'founder-intro-popup', title: 'Add a "welcome / about the founder" popup', priority: 'P1', effort: 'M', area: 'Growth', status: 'partial', note: 'DONE (mechanism), PR pending — and it is deliberately NOT the popup this row describes. The founder redefined it on 2026-09-05, verbatim: "maybe we make it visable when a user clicks a certian section or button, it will just be a short welcome from me and a what this app is for and what state its in also asking for feedback support as im a solo developr". He was offered the auto-popup above and chose this instead, so OPT-IN IS THE SPEC: it never auto-opens, never fires on a timer, a scroll depth or a first visit. That retires two constraints this row carried — the once-per-person rule from #240 and "must not fire on top of onboarding" — because nothing is shown to anyone unasked; and it satisfies "reachable again afterwards" by construction, the trigger being a permanent footer link rather than a one-time card. WHAT SHIPPED: src/components/FounderNote.jsx, an "About this project" link in the footer legal row beside the existing "Built in Brisbane by Dylan Coleman" attribution, opening a dialog through the shared useModalDialog (focus trap, Escape, scroll lock, focus returned to the trigger) with markScrollContainers handling the Lenis nested scroll. One action, the existing /feedback route — no second channel and nothing that needs email, which transactional-email-channel is still blocked on. NOTHING IS PERSISTED: no localStorage, no seen flag, no unread dot, because the only use for knowing you have read it is to change what you are shown next, and every version of that walks back toward the popup that was turned down — the side benefit being that the private-mode throw has no site to happen at. THE WORDS ARE STILL OWED. src/data/founderNote.js holds four empty slots and the panel shows an unmistakable unwritten state until all four are filled, at which point that state stops rendering on its own. No agent may write them: the voice is founder-owned, the hero headline refusal and the twice-rejected "MEGA AI generated" homepage intro are the precedents, and a convincing fake is the failure rather than the success. The four lines are: who you are, what UIL4B is for, what state it is in, what help you want. Verified in a browser keyboard-only, at 390px, and under reduced motion in both directions; 19 assertions, all mutation-checked.' },
  { id: 'typography-paywall-model', title: 'Decide what Pro means for the typography tools', priority: 'P1', effort: 'M', area: 'Monetisation', status: 'done', note: 'FOUNDER DECISION 2026-09-05, and the item shipped on it: BROWSING IS FREE, SAVING IS PRO. Anyone may browse fonts, pair them and build a scale without an account; what costs money is KEEPING a type system. He picked that option out of four because it matches how the colour tools already work, which made consistency with them the requirement rather than a preference WHAT THE COLOUR LINE ACTUALLY IS, read off the code rather than off the intention: PaletteBuilder is fully usable signed out (seed, Auto and Monochromatic systems, up to 8 colours, image extraction, share link); its Save/export button calls requireLogin(..., { free: true }) because saving is FREE and only needs somewhere to save TO; ProjectContext.saveProject() then throws once the account holds plan.limits.projects, which is 3 on Free and Infinity on Pro; and Copy CSS variables, Copy hex values and Download PNG card are UNGATED items in that same menu. IconLibrary says it in words at line 693: "Saving is FREE - it just needs an account". So colour gates the SLOT, not the work and not the export THE ONE PLACE THE DECISION AND THE EXISTING MODEL DISAGREE, and it needs the founder’s eye: he said Pro is required to "SAVE a type system, EXPORT it, or keep it in a project". Export is the odd one. It is free in colour, and the Plans page already SELLS it as free - the Free column reads "Unlimited palettes, scales, gradients and exports" and the comparison table repeats it. Gating Copy CSS in the Type Scale would have contradicted a live promise on the pricing page while calling itself consistency, so export was left free and only the save was gated. If he wants export gated too, that is a real product decision, not a bug, and it needs the pricing page changed in the same commit WHAT SHIPPED. Typography had NO save path at all - the three tools wrote to the working kit (setFonts/setTypeScale) and stopped - so "saving is Pro" gated nothing until the door existed. src/components/SaveTypeSystem.jsx is that door: one control, mounted on Font Pair and Type Scale in their delivery sections after the export block, so the reading order is build, copy, keep. Signed out it opens the FREE login prompt. Signed in it saves into the same 3-project allowance every other tool shares, and Pro lifts it. The Font Gallery and the dossier were left completely alone - they are the browse surface and a test now fails if either grows a Pro modal THE GATE IS ABSENCE, NOT CONCEALMENT. This repo has shipped a gate that only looked like one twice: brand hexes rendered as readable text behind a lock, and a prompt library that counted positions in a filtered list so the search box walked through it. splitLockedLibrary has nothing to wrap here because there is no payload - a save button holds no secret - so the equivalent discipline is structural. At the cap the component renders NO name field and NO save button; not disabled, not hidden by CSS, absent. Measured in a real browser across the three quota panels: 4 save controls exist in total, all in the two under-cap panels, 0 hidden by display/visibility/opacity. commit() re-resolves the quota before calling anything, and saveProject() throws on its own count underneath WHAT A DETERMINED USER CAN STILL GET: everything except the storage. Every font, every pairing, every scale value and every CSS/Tailwind/SCSS export is free BY DESIGN, so there is nothing withheld to extract. The 3-project cap itself is client JavaScript over localStorage, and firestore.rules lets an owner write users/{uid}/sync/projects freely, so editing either exceeds it. That is not new and not typography’s: it is exactly the enforcement colour, icons and Projects have had all along, because api/ is full at 12/12 routes and a test fails the build on a thirteenth. Do not describe this gate as server-enforced anywhere PLANS PAGE. The original finding included "the Plans page does not mention fonts or typography anywhere". It does now - the unmetered row names font pairings and type scales, and the saved-projects row says what a project holds - and both statements were already true of the code, since DEFAULT_DESIGN has carried fonts and typeScale beside palette from the start GUARDED BY tests/unit/typography-paywall.test.js (11 tests) and tests/user-sim/51-typography-paywall.spec.js (8 tests). Nineteen mutations, nineteen caught, none missed. The FREE half is the load-bearing half of both files: a gate is easy to add and nothing fails when someone adds one too many, and the browse experience is what sells the tools. Two of the mutations found weak assertions rather than confirming strong ones - a prefix regex that stayed green when the save control was renamed off the page, and a CSS scan that read its own explanatory comment as the defect it warns against MOBBIN, PLATFORM WEB. Mixpanel (mobbin.com/screens/73beb7a4-4ce4-4a88-acc1-2bd0cda5307a) and Amplitude (mobbin.com/screens/2068eea3-4484-43d2-8231-dd4f5297feb7) both put the allowance INSIDE the save dialog as a live count beside a save control that still works, which is why the number appears where the decision is made rather than as a badge on a locked button. Savee (mobbin.com/screens/ced87a42-02f5-4010-ac2c-c4009cd0fa26) blocks its publish step over a toast still reading "Site saved", which is why the overwrite list survives into the wall - at the cap the new slot is withheld, never the work. Gamma (mobbin.com/screens/ad4dc9a0-ad1b-440c-a7ec-2c7a577af805) makes "Save theme" the terminal action of its Fonts panel, which set the placement. The two REJECTED shapes are Polywork (mobbin.com/screens/e1c3e76e-1625-4355-be2e-478c72275fea), which marks "Font Pack 2 - Premium" against "Font Pack 1 - Free" in its typography picker, and Base44 (mobbin.com/screens/86131911-784e-4258-861a-c2bfd9216662), which answers Export to ZIP with "Upgrade Required" STILL OPEN AND DELIBERATELY NOT DONE HERE: whether the free allowance of 3 is the right number now that typography competes for the same slots. A pairing and a scale are two saves where they used to be zero, so the cap will be met sooner than the number was chosen for. That is founder-owned pricing, not a bug.' },
  { id: 'library-ui-language', title: 'Extend the gradient/palette library UI language to the other browsing surfaces', priority: 'P1', effort: 'L', area: 'Design system', status: 'done', note: 'DONE. src/components/library/ (LibraryToolbar, LibrarySearch, LibraryFilterGroup, LibraryGrid, LibraryCard, LibraryEmpty) plus the .lbry- block is the one implementation. The Icon and Emoji libraries were the last holdouts — both still ran .pl-toolbar with .pl-search-wrap (borrowed from the Prompt Library) and outlined .pl-chip pills, i.e. the fourth implementation this item existed to prevent — and both are now on the shared toolbar (founder request: make them match the palette and gradient galleries). TWO EXTENSIONS RATHER THAN DROPPING BEHAVIOUR TO FORCE A MATCH: LibrarySearch takes an onFocus (the emoji search index is a separate ~31 KB chunk preloaded on focus, and without the hook that surface would have kept its own field), and a filter option takes an icon and a count (the emoji categories were always identified by a glyph). Both are aria-hidden over an unchanged accessible name. THE PACK SELECT STAYS A SELECT: 26 options in six optgroups is not a pill tray, and forcing it into one to look consistent would trade a working control for a matching one — it takes the shared field metrics instead. A MOBILE DEFECT FOUND WHILE DOING IT, now fixed for all five consumers: the toolbar is sticky, and measured at 390x844 it occupies 27–48% of the viewport (409px on Emoji, the widest tray in the app). That decision already existed TWICE as position:static overrides on .pgl-toolbar and .fg-controls, and the other three surfaces never got it — exactly the divergence this component was extracted to end. It is one rule on .lbry-toolbar now. Dead CSS removed: .lib-commandbar and .pl-select had zero remaining consumers; .pl-toolbar/.pl-search-wrap/.pl-chip stay because the Prompt Library still uses them. Proof: 25-defect-sweep S4 measures both surfaces on the shared tray, and the surfaces were render-checked at 390/834/1440.' },
  { id: 'modal-ui-pass', title: 'Improve the popup/modal UI across the app', priority: 'P1', effort: 'M', area: 'Design system', status: 'done', note: 'DONE. The diagnosis was not a styling one. `aria-modal="true"` is a PROMISE to assistive technology that everything outside the element is inert, and most of the app\'s dialogs made that promise while trapping nothing — Tab walked into the page behind, the background scrolled, and closing dropped focus to the top of the document. Measured across the whole app on 2026-08-15: FOUR surfaces already had a correct hand-written trap (LoginPopup, ExportPanel, PillNav, IconLibrary — the last three also pause the app-wide Lenis, which the shared hook does not, so they were deliberately LEFT alone rather than churned). Everything else had Escape at best: ProUpgradeModal, FeedbackModal (reachable from every page), UIPreviewModal, PromptModal, Community submit, GradientGenerator submit, PaletteBuilder preview + submit, and UIKitGuide — which is a new user\'s first screen and had no Escape handler at all. All now use hooks/useModalDialog, which gained two options in this pass: `enabled` (several dialogs stay mounted and toggle on an `open` prop, and hooks cannot be called conditionally) and `initialFocus` (UiShadeEditor opens onto its hue field; it also had a correct local trap but no scroll lock, so it migrated). Two dialogs also had role="dialog" on the OVERLAY rather than the panel, which made the scrim part of the dialog — fixed in UIPreviewModal and UIKitGuide. SEPARATE FINDING, fixed here: ProUpgradeModal carried five filled stars over "Loved by designers who\'d rather build than tab-hop." There are no reviews, no ratings and nobody attributed to that sentence — invented proof on the surface where a user decides whether to trust us with money. Mobbin reference (six comparable upgrade dialogs: Savee, Elicit, Wellfound, Givingli, Adobe Express, Mobbin) shows none of them use social proof there and all of them give an explicit decline; ours had only an X. The panel now carries the feature list, the price/CTA/decline are one bounded object, and "Not now — keep using the free tools" is a real control. Guard: tests/unit/modal-contract.test.js fails the build if any dialog declares aria-modal without trapping focus, by EITHER route — it does not force the shared hook on code that already solved it correctly. STILL OPEN: ColorPickerPop is a popover rather than a modal and was left as-is; the login-modal-ui redesign is still its own queued item.' },
  { id: 'font-examples-per-family', title: 'Make the font Examples tab show what the family is actually for', priority: 'P1', effort: 'M', area: 'Typography', status: 'done', note: 'DONE. Founder request 2026-09-04, the SAME ask a third time (2026-08-08, 2026-08-15): “for the font examples i want real world examples not the same UI examples for each one”, reference fontsinuse.com. THE PANEL NEVER READ font.category ONCE — a module-level array of four fixed scenes (article opening, interface, pricing table, display line) rendered identically for all 1,946 families, so Pacifico got a data table and Libre Barcode 39 got a pricing table. CATEGORY ALONE COULD NOT FIX IT, and that is the part worth keeping: /api/fonts was passing through 9 of the metadata endpoint’s 23 fields and the ones it dropped are the ones that matter. classifications (1,089/1,946) DISAGREES WITH category for 351 families — Playfair Display is category Serif with classifications Display, and so are Anton, Bebas Neue, Archivo Black and DM Serif Display, every one of which was being handed the long-form reading column that IS the complaint. stroke (1,361/1,946) recovers Slab Serif, which category erases by folding it into Serif. classifications is also the ONLY field that finds the 22 SYMBOL families (Libre Barcode, Noto Music, Yarndings, Wavefont), where a pricing table is not a weak example, it is nonsense. Both fields now pass through api/fonts.js and src/utils/fontScenes.js picks the scene set from them; MEASURED OVER THE WHOLE CATALOGUE, 276 families (14.2%) get a different set than category alone would give. ORDER IS LOAD-BEARING and every step earned its place against real data: Symbols first, because 17 of the 22 symbol families are ALSO classified Display; Monospace before stroke, because Courier Prime carries stroke Serif and would otherwise get an editorial column; Handwriting before Display, because 156 families carry both and a brush face is a script that happens to be big. THE DEGRADED PATH IS REAL BUT IT IS NOT THE COMMON ONE: every family in the live metadata carries at least one of the two fields — the count with NEITHER is ZERO — so falling back to category is the whole-catalogue-is-thin path, reachable exactly three ways (the bundled fallbackFonts.js, a deployment with VITE_GOOGLE_FONTS_API_KEY where the client goes straight to the WebFonts API and never reaches /api/fonts, and a localStorage catalogue cached before this shipped, which self-heals inside its 24-hour TTL). On it a script is still a script and a mono still a mono; what is lost is the sharpening, and a test pins that exact loss rather than letting anyone claim the fallback is lossless. A family with NO descriptive field at all still varies by its own cuts rather than getting one fixed template. NO FAKED CAPABILITY, which is the half that could have quietly lied: every weight is snapped to a cut the family really ships and then requested through loadFont (a real cut that was never DOWNLOADED is faux-bolded by the browser exactly like one that does not exist, so snapping alone is only half the guarantee), the ladder is withheld below three cuts so Anton and Bebas Neue get none, and NOTHING is set in italic — injectFontLink requests wght@ and no ital axis, so no italic cut is EVER downloaded for any family in this app and a font-style:italic here would be a synthesised oblique 100% of the time, including on the families that genuinely ship twelve. NO PAIRED SECOND FACE either, though rankPairings is available and correct: a supporting line set in another family would be the one thing on the panel a reader cannot attribute. THE fontsinuse.com LINK SHIPS AS A SEARCH, worded as one. A per-typeface deep link is IMPOSSIBLE — URLs are /typefaces/<numeric-id>/<slug>, the id is not derivable from the name, /typefaces/playfair-display 404s, and /api, /oembed, /sitemap.xml and /developers all 301 to a 500. TWO FALSE LEADS ARE RECORDED IN THE CODE so nobody burns the afternoon again: ?q= returns HTTP 200 and a plausible page which is actually the EMPTY-QUERY page for every family, and counting /typefaces/ links in a curl response finds ~200 for ANY query including nonsense, because results are client-rendered and those links are static nav furniture. Verified in a real browser at their robots.txt Crawl-delay of 10: terms=Playfair%20Display returns 59 real /uses/ links, while terms=Chokokutai returns HTTP 200 reading “No Uses found for Chokokutai” AND 47 unrelated popular uses underneath — so THE MISS CASE IS A PAGE THAT STILL LOOKS FULL, which is precisely why the wording is “search fontsinuse.com for X” and the sentence after it says a newer family may return nothing. Full third-party convention, taken from AppFooter/DiscoverCard/CommunityCard: rel noopener noreferrer nofollow, target _blank, an aria-hidden arrow and an sr-only (opens in a new tab); nofollow is how the link respects their intent to keep crawlers off /search. PROOF: 23 unit tests in tests/unit/font-scenes.test.js whose fixtures are REAL values read out of Google’s metadata rather than invented to pass, verified by 9 mutations plus a no-op control that must stay green; plus a rendered check of ten families in BOTH themes on BOTH catalogues, asserting scene sets differ, no node renders in a synthesised oblique, nothing overflows horizontally, and the link carries the whole rel convention. ONE GUARD CAUGHT ME ON THE WAY: design-tokens.test.js scans global.css WITHOUT stripping comments, so a var(--fdx-ff) written in a design note read as a real fallback-less usage and failed the build — the comment moved, not the test. [fonts-in-use-surface] IS UNCHANGED AND STILL BLOCKED: nothing here hosts, scrapes, hotlinks or embeds a third-party photograph.' },
  { id: 'fonts-in-use-surface', title: 'Build a "fonts in use" surface', priority: 'P2', effort: 'L', area: 'Typography', status: 'blocked', note: 'Founder request (2026-08-08), restated 2026-08-15 and again 2026-09-04: “i want a way of seeing fonts with real life use cases of them and images and abouts”. STILL BLOCKED, and DO NOT MARK IT DONE — but the blocker is now NARROWER than it was, because two thirds of that sentence shipped separately and neither half needed rights clearance. SHIPPED ELSEWHERE, so do not rebuild them here: the ABOUT half is [font-popup-about-and-examples-tabs] (#327), derived entirely from Google’s own family metadata with any field the catalogue does not supply omitted rather than guessed; the per-typeface EXAMPLES half is [font-examples-per-family], which sets each family in situations chosen from what kind of face it IS (a display face gets a poster, a text serif a reading column at a real measure, a mono a code pane and a terminal, a script the thing scripts are actually for) and links out to a fontsinuse.com SEARCH for that family. WHAT REMAINS BLOCKED IS EXACTLY THE IMAGES, and that is now the whole of it: hosting, scraping, hotlinking or embedding photographs of real brand work. Unresolved and FOUNDER-ONLY: who owns the imagery, under what licence it may be shown, who curates it, and what provenance is displayed. Reference: fontsinuse.com — and note their robots.txt sets Crawl-delay: 10 and intends to keep crawlers off /search, so any future ingestion is a LICENSING CONVERSATION and not a scraping task. Do not start the build until the rights question is answered; building it first would create a takedown surface.' },
  { id: 'gradient-randomiser-weighting', title: 'Weight the gradient randomiser toward 2-stop linear gradients', priority: 'P2', effort: 'S', area: 'Discover', status: 'done', note: 'DONE (founder request 2026-08-08). src/utils/gradientRandom.js: type Linear 50% / Radial 25% / Conic 25%, stop count 2 at 60%. A two-stop linear goes 16.7% (tied-least-likely) to 30% (most likely), with the rarest combination still at 10% — a weighting, not an exclusion, and the tests assert both halves of that. Both pickers take the roll as an argument so the distribution is exact in tests/unit/gradient-random.test.js rather than sampled from Math.random.' },
  { id: 'library-filter-ux', title: 'Rebuild the gradient/palette library filter interaction', priority: 'P1', effort: 'M', area: 'Discover', status: 'done', note: 'DONE (founder request 2026-08-08). Two of the three parts were ALREADY SHIPPED when this was picked up — the sliding pill indicator and the Warm/Cool/Dark/Light colour dots both landed with the src/components/library extraction, so check before rebuilding. What was missing was multi-select: LibraryFilterGroup now takes an opt-in multiSelect, value becomes an array, shift/ctrl/cmd-click is additive, and selecting every option collapses to the reset option — as does deselecting the last one, which is the same view of the data. The KEYBOARD EQUIVALENT IS THE SAME GESTURE: a keyboard-activated button carries the live modifier state on the click it dispatches, so Shift+Enter takes the additive path through one code path rather than two that can drift. Measured, not assumed — tests/user-sim/31-library-filter-multi.spec.js. Enabled on the gradient TYPE tray, which is what the request named; every other tray keeps single-select until someone opts it in. Collapse rules: tests/unit/library-filters.test.js.' },
  { id: 'palette-library-categories', title: 'Categorise the Palette Library on scroll', priority: 'P1', effort: 'M', area: 'Discover', status: 'partial', note: 'PARTIAL by design, and the remainder is BLOCKED rather than skipped. Shipped: the library browses in ordered categories with sticky headings — Curated collection, then Brand systems — each with its own count and its own labelled grid. A filter or a search collapses back to one flat grid, because a filter is already an answer to “which subset” and re-splitting it into headed groups (one usually empty) buries that answer. STILL BLOCKED, and deliberately NOT faked: TRENDING needs a real ranking signal and the product has none — see upgrade-activation-events — so the curated set leads in catalogue order and is not labelled trending; COMMUNITY has no source until community-backend is built, and an empty Community heading would be a promise the product cannot keep. Adding either later is one entry in the SECTIONS array in src/pages/PaletteGallery.jsx. A test asserts both are ABSENT, so a future stub cannot quietly appear. The old “36 of these are published brand systems” preamble is gone from the browse view — the Brand systems heading and its count say it next to the cards it is about — and 15-discover-library-parity was updated to assert the new form. Proof: tests/user-sim/34-palette-library-sections.spec.js (8). 2026-09-06: THE RESULTS ROW ABOVE THE SECTIONS WAS NEVER RECONCILED WITH THEM, and that was the unblocked remainder — everything below is now closed, and only trending and community are still blocked. The sections went in underneath a shared DiscoverResultHead that kept passing eyebrow={filter === "brand" ? "Brand systems" : "Curated collection"}, which broke in two ways at once. It MIS-DESCRIBED THE PAGE: browse mode shows curated AND brand, so the row named the first of two groups as if it were the whole library. And it REINSTATED THE MOTIF #391 REMOVED: a taxonomy eyebrow roughly forty pixels above an h3 repeating it word for word. MEASURED ON THE RENDERED PAGE, top to bottom, before: "CURATED COLLECTION / Colours worth building with / 71 palettes" sitting directly on "Curated collection / 64". The same untruth had reached the flat views and was merely less visible there, because a flat grid has no heading under it to disagree: the mood filters select on the colours themselves and so reach into both groups — measured, Dark matches 3 brand + 24 curated, Vivid 6 + 29 — and every one of those views was labelled "Curated collection". THE RULE NOW: name a category only when the view IS that category — brand filter to "Brand systems", curated filter to "Curated collection", browsing to "Everything you can browse", any search or mood filter to "Across both collections". SHAPE TAKEN FROM CuratedResources.jsx, the other Discover library that browses in bands and narrows to a flat list; when it bands, its eyebrow is a claim about scope ("Hand-picked, not scraped") precisely so it cannot restate the band headings. The curated blurb also opened on the same seven words as the hero description ~200px above it ("Colour systems with a point of view…"), so one screen introduced the page twice and distinguished the two groups not at all; the blurbs now say WHEN you would reach for each. 15-discover-library-parity asserted the old string for BOTH libraries and was updated deliberately, with the reasoning left at the site: parity there is of STRUCTURE, and wording is the one thing the two pages cannot share, because the Gradient Library is curated end to end while this one is not. WHO SEES WHAT IS UNCHANGED, which was the constraint: the diff touches presentational strings only — not splitLockedLibrary, not `browsable`, not the palette data — and the free viewer still measures 71 cards (64 curated + 7 free brands: Apple, Material 3 Baseline, Google, Netflix, Discord, Stripe, Spotify) with 30 locked and 3 teased. GATE: lint 0 errors/28 warnings, build 33 route shells, 1323 unit tests, 27 passed across the two specs. MUTATION-VERIFIED, at the CALL SITE rather than on a helper: restoring the old eyebrow expression fails 4 tests (parity:58, and three in 34); restoring the old blurb fails 1. Proof: 34-palette-library-sections.spec.js (12). 2026-09-07, THE MOOD FILTERS LANDED and this row is still `partial` for the same reason as before (trending needs a ranking signal; community publishing needs a backend) — nothing about that changed. Founder: "for our pallete library lets include filters of neutral, and others", then all eight of Neutral, Warm, Cool, Pastel, Vivid, Dark, Light, Monochrome. The three that existed (Dark, Light, Vivid) KEEP THEIR SHIPPED ARITHMETIC byte for byte — re-deriving them in HCT moved 12 palettes across the Dark boundary for no user-visible gain, and #396’s measurements would have stopped describing the page. The five new ones are HCT (hexToHct), which is the space the palette engine already reasons in. All eight live in src/utils/paletteMood.js as classifyPalette(hexes): a PURE function of the hex array, no id, no name, no kind, no entitlement, and NO hand-tag anywhere — there is no `mood:` field on a palette and there must not be one. THE TRAY IS NOW TWO GROUPS, and that is the substantive design change rather than the eight chips: provenance (All/Curated/Brand) and mood were one single-select row, so choosing a mood silently CLEARED the collection and "warm brand palettes" was a question the control could not express. They combine with AND now, which is the shape /discover/gradients has run since #323. DERIVED COUNTS on the shipped library (whole library / free viewer): Neutral 8/8, Pastel 12/12, Vivid 63/35, Warm 35/27, Cool 33/25, Dark 40/27, Light 19/17, Monochrome 31/26. Dark and Vivid on the free view reproduce #396 exactly (3 brand + 24 curated; 6 brand + 29 curated), which is the check that the shipped maths really is unchanged. DECISIONS WORTH KEEPING: a palette that straddles the warm/cool split belongs to NEITHER (chroma-weighted warm share must be ≥0.75 or ≤0.25) — returning it to both would make each control less true. Pastel∩Vivid and Dark∩Light are empty BY CONSTRUCTION, not by luck: Pastel’s ceiling is literally VIVID_MIN_SPREAD and the Dark/Light comparisons face opposite ways across a gap. One palette in 101 (Prussian & Peach) matches no mood at all, and that is honest rather than a bug — it is a two-temperature mid-tone palette. WHO SEES WHAT IS UNCHANGED, again the constraint: splitLockedLibrary, `browsable` and the palette data are untouched, and the free viewer still measures 71 cards (64 curated + 7 free brands) with 30 locked and 3 teased. THE MOOD GROUP IS A MENU AT EVERY WIDTH, not a row of chips, and that was MEASURED rather than preferred: nine chips took the sticky toolbar to 314px at 320px wide (more than half a small phone, permanently on screen) and wrapped it to two rows at 1280px while fitting at 1366 and above, so the page changed height between two ordinary laptop widths. The existing measured collapse could not fix either — it is off below 641px by design (#298) and deliberately blind to sibling groups above the band, which is exactly what let 627px of mood tray sit beside 223px of collection tray at 1280. LibraryFilterGroup gained one additive opt-in prop, `alwaysCollapsed`, used only here. Result: the palettes toolbar is 68px at every desktop width from 1024 to 1920, 64–68px across the 641–980 band, and 240px on a phone — against 235 for /discover/gradients, 235–282 for /discover/prompts and 273–320 for /create/icons, so it is now the LOWEST of its siblings rather than the highest. One more measured fix came with it: `.lbry-search{flex-basis:240px}` is what decided how many rows the 641–980 band got, because flex wraps on BASE sizes before anything shrinks. At 768px the row totalled 737 against 714 and the Create a palette link wrapped, taking the toolbar to 124px over its 92px ceiling. The basis is 120px in that band now, which brings the same row to 581 — exactly what this surface totalled before it grew a second filter group. The field never renders at 120: flex-grow hands it 253px at 768, 130px at 641, and its 360px cap from 980 up. GATE, on the rebased tree: lint 0 errors/25 warnings, build 39 route shells (this branch adds no route), 1549 unit tests, full user-sim 751 passed with the expected 13 skips (all in 12-ui-system-builder). MUTATION-VERIFIED AT THE CALL SITE, three ways, each restored byte-exact against a recorded sha256: neutering the one line in PaletteGallery that reads the classifier fails 3 browser tests (“Neutral rendered the wrong number of cards”); forcing NEUTRAL_MAX_MEAN_CHROMA to 0 fails the unit sweep on both the whole library and the free view; deleting `alwaysCollapsed` from the call site fails the defect sweep at 320px. Proof: tests/unit/palette-mood.test.js (22) and tests/user-sim/57-palette-moods-and-gallery-cta.spec.js (12).' },
  { id: 'gallery-closing-cta', title: 'Every gallery page closes with “Can’t find what you’re looking for?”', priority: 'P2', effort: 'S', area: 'Discover', status: 'done', note: 'Founder, 2026-09-07: “at the very bottom of all gallery pages, include a CTA, ‘cant find what you are looking for’ Create and Submit your own or something.” ONE COMPONENT, src/components/discover/GalleryCloseCta.jsx, rendered as the last child of five pages — not five copies, which would drift on the first edit and drift invisibly. It is a question, one line of detail and one pill: no card (a card at the bottom of a hundred cards reads as the hundred-and-first), no gradient banner, no icon row. Mobbin: Discord sites/sections/430c2298-297c-4c3f-9b40-cf66550ec8af closes a grid with exactly this, “Didn’t find what you were looking for?” over a single New post button; Twingate 071de120-0906-4807-99ca-c1ca3eb1ce95 is the same shape with one line of subtext. Dribbble and Unsplash were also checked and close with NOTHING — they are infinite scrollers — which is the argument for closing rather than a pattern to copy. DESTINATIONS: /discover/palettes → /create/palette; /discover/gradients → /create/gradient; /discover/prompts → the page’s own openSubmit (the submission panel is on that page), which scrolls the panel into view because it mounts thousands of pixels above the CTA; /community → its own openSubmit, which opens a MODAL so no scrolling is needed; /discover/resources → /feedback, and the wording drops the word “submit” because there is no resource queue — SUBMIT_SURFACES is exactly community/gradient/palette/prompt — and /feedback is already where that page’s own “Suggest a resource” button goes. DELIBERATE DEPARTURE, flagged for the founder: the palette and gradient CTAs do NOT stage a submit intent. Staging one fires the tool’s resume effect on arrival and opens “Submit to community” over a default palette, asking the user to publish work they have not done. The gate is not skipped, it is kept where the thing being submitted exists — the tool’s own Submit button, same requireLogin flow 09-auth-modal-accessibility covers. /create/font-gallery is DELIBERATELY EXCLUDED: it browses the live Google Fonts catalogue, so “can’t find it” means the family does not exist on Google Fonts and UI L4B cannot add it; there is no font submission surface; and it already closes with its own “Continue your typography system” nav. A CTA there would be a promise the product cannot keep. CLEARANCE against the fixed .global-feedback-btn measured at 20 widths from 320 to 1920 with a FRESH CONTEXT EACH (no resizing), scrolling the CTA as low in the viewport as it will go rather than to the document end — a tall footer otherwise carries it off screen and the check passes on a CTA nobody can see. MUTATION-VERIFIED AT THE CALL SITE: moving the gradient CTA above its grid fails with “the CTA starts 7756px ABOVE the last card”. Proof: tests/user-sim/57-palette-moods-and-gallery-cta.spec.js (12).' },
  { id: 'login-modal-ui', title: 'Redesign the "Log in to continue" modal', priority: 'P1', effort: 'M', area: 'Account', status: 'done', note: 'DONE (#302). The modal was not short of content — it already named the interrupted action and listed benefits — but said it in three stacked equally-weighted cards ABOVE the form, so nothing was emphasis and the form started far down: 53px of sign-up below the fold at 390x844, 74px for community submission. Mobbin (Qatalog) supplied the load-bearing move: the why gets its own PANE, not a card above the controls, which is what removed the overflows. Also fixed: hierarchy was inverted (focus landed on Google while the accent sat on the slower email submit), reason-less sign-up showed no explanation at all, and the modal hand-rolled the dialog contract instead of using useModalDialog. Found a real bug on the way — useModalDialog cannot restore focus here because PillNav unmounts the menu before the hook captures activeElement; capture moved into requireLogin(). HVZ respected: no auth call, session, Firebase method or One Tap behaviour changed. Deferred to the founder: the two-step email form, and the “Log in” vs “Sign in” naming split.' },
  { id: 'nav-search-hover-expand', title: 'Expand the nav search on hover with a cycling typing animation', priority: 'P2', effort: 'S', area: 'Navigation', status: 'done', note: 'DONE (founder request 2026-08-08). Hover/focus takes the field 300px → 390px and types six real tool names one character at a time, holding and erasing between them. No timer exists until the pointer arrives, so the load-time budgets are untouched; the animation is gated on the resolved reducedMotion, because the global transition-duration:0.01ms rule cannot reach a setState loop; the animated span is aria-hidden so the button keeps its fixed accessible name. NOTE A VISIBLE SIDE EFFECT: .pnav-lead is shrink-to-fit, so the old flex-basis:300px was inert and the resting field was content-sized at ~177px. It is now the 300px the CSS always declared.' },

  // ── Migrated from the retired audit and build-plan documents, 2026-09-01 ──
  // The four documents these came from (docs/audit-2026-08-11.md,
  // docs/account-lifecycle-audit-2026-08-12.md and the two build-plan files)
  // were deleted after every fact in them was moved to its canonical home —
  // see CHANGELOG.md, "One home per fact". EVERY item below was re-verified
  // against the code on 2026-09-01 before being carried over; anything the
  // audits listed that had since shipped was dropped rather than re-queued.
  { id: 'seo-per-route-share-and-schema', title: 'Give each route its own share image and structured data', priority: 'P2', effort: 'M', area: 'Growth', status: 'done', note: 'DONE. THE FALSE CLAIM ABOUT MONEY WENT FIRST, in its own commit. index.html’s WebApplication block declared an Offer of "4.99" AUD and its noscript paragraph quoted the same pre-ladder figure as a monthly price. NOTE THE ABSENT DOLLAR SIGN in that sentence: price-consistency.test.js caught the first draft of this very note for quoting the stale amount in price shape, on a board that renders in the app. The guard was right — the fix is to describe the defect, not to restate the wrong number. price-consistency.test.js guards prices but walks src/, and index.html is not in src/, so the only price a non-JS reader could see stayed pre-ladder for weeks. FIXED BY DERIVATION, NOT BY RETYPING: index.html now holds placeholder comments and NO AMOUNT AT ALL, and scripts/site-pricing.mjs generates the offers from planLadder.js. THE SEAM IS vite.config.js’s transformIndexHtml hook, NOT prerender.mjs — prerender runs only under npm run build and never rewrites dist/index.html, which is the one page the pricing sentence survives on, so a prerender-side fix would have left npx vite build and npm run dev serving the raw placeholder. prerender.mjs asserts the substitution happened, so the hook silently not running fails the build. THREE MORE FALSE CLAIMS IN THE SAME PARAGRAPH: "40 AI generations per day" free (the daily cap is 5; 40 is the MONTHLY cap, so it quoted the wrong period as well as the wrong number), "1,000 per day" Pro (the cap is 30/day — api/_lib/plans.js records 1,000 as abandoned, "not oversold at a thousand users, it is oversold at TWO"), and "higher-quality models", which Plans.jsx answers in the negative in its own FAQ: "Free and Pro run the same model today". Quarterly is excluded from the offers because it has no checkoutPlan and dead-ends in Checkout.jsx, and the currency is now USD rather than the AUD the block claimed over a USD ladder. SHARE IMAGES: one card per SECTION, chosen over 28 pieces of per-route art and over hand-made cards for a few routes. A share card is mostly TYPE and rasterising type needs a real renderer — sharp or resvg means a native binary in the Vercel build, and Chromium on the build path means a browser download in CI on every deploy to redraw seven images that change twice a year. Hand-made cards need a designer per tool and silently fall back to the homepage card for any tool that ships without one, which re-arms the exact defect. Sections win on one property the other two lack: A NEW TOOL INHERITS ITS SECTION’S CARD AUTOMATICALLY. 7 cards, 242KB total, drawn by npm run og:cards from the app’s own --hue-* tokens, its self-hosted Manrope and its real tool names; 19 of 28 routes now unfurl as their category instead of as the homepage. STALENESS, the standing objection to a generated-and-committed artefact, is answered: the generator writes public/previews/cards.json recording what it drew from, and a test recomputes that from source and fails with "run npm run og:cards". SCHEMA: BreadcrumbList only, on the 8 routes with a REAL three-level trail — the five colour tools under /create/color and the three Discover libraries under /discover. Everything else has no eligible parent because its category home redirects, and schema.org allows only the LAST crumb to omit its item URL, so an intermediate crumb would be a link in the hierarchy that is not a page. NO FAQPage on /help despite genuine Q&A living there, for two independent reasons: Google restricted FAQ rich results to authoritative government and health sites in 2023 so it earns nothing here, and the answers sit behind a conditionally-rendered tab so they are not in the DOM — and certainly not in a shell that contains no React output at all. NO Product/Offer on /plans: the offers are already published on the site-level WebApplication every shell carries, and the field that would actually earn a rich result there is aggregateRating, which we do not have. A test reads every built shell and fails on aggregateRating, ratingValue, reviewCount, ratingCount or availability — a rule about what we will never claim, not about today’s code. LEFT DELIBERATELY: /seo is a real tool with no section of its own and keeps the site card.' },
  { id: 'fix-canonical-host-2026-09-13', title: 'Every canonical, og:url, og:image and JSON-LD url pointed at a host that does not serve', priority: 'P1', effort: 'S', area: 'Growth', status: 'done', note: 'DONE 2026-09-13 (fix/canonical-host). MEASURED, WITH CONTROLS: curl https://uil4b.com/ returns 200 in 0.9 s; curl https://www.uil4b.com/ times out on 443, twice, 21 s each; curl https://example.com/ returns 200, which is the control that says the network was reachable from where this ran. The Vercel project ui_l4b lists uil4b.com, uil4b-dylan-coleman.vercel.app and uil4b-git-main-dylan-coleman.vercel.app — www.uil4b.com is not a domain on the project at all. DNS: www.uil4b.com is a CNAME to ns1.vercel-dns.com, a NAMESERVER (198.51.44.13), not the serving endpoint cname.vercel-dns.com. CONSEQUENCE, live on the real site until this shipped: all 40 prerendered shells declared their canonical, og:url, og:image, twitter:image and JSON-LD url on that host, so every shared link unfurled with a broken preview image on Slack, LinkedIn, X, Discord and iMessage, and every page told crawlers its authoritative URL was somewhere that does not answer. prerender.mjs had already called a wrong canonical "an explicit instruction to drop them from the index" — [public-route-prerender] and [seo-per-route-share-and-schema] fixed the per-ROUTE half and nobody checked the HOST. FOUNDER DECISION: point the site at the apex. It already serves and is the only configured domain, so it needs no DNS work and no owner action. THE ORIGIN WAS DEFINED IN SIX PLACES: src/utils/routeMeta.js SITE_ORIGIN, scripts/prerender.mjs ORIGIN, scripts/route-schema.mjs breadcrumbRoutes() default, scripts/site-pricing.mjs ORIGIN, src/utils/environment.js PRODUCTION_HOSTS and src/utils/styleGuideExport.js. There is now ONE: routeMeta.js exports SITE_ORIGIN and the other five import it — two sources of truth for one origin is the defect class itself, which site-pricing.mjs’ own header already says about prices. EIGHT MORE SURFACES carried the dead host as literal text and were corrected or regenerated: index.html (og:url, og:image, twitter:image, canonical), public/robots.txt, public/sitemap.xml and public/llms.txt (both regenerated from their generators, which already derived the origin from canonicalUrl()), the SEO tool placeholders in MetaInspector.jsx and SchemaGenerator.jsx, README.md, .claude/agents/qa.md, and eleven test files that pinned the old host by value. A SEPARATE USER-FACING BUG IN THE SAME CLASS, fixed here: src/utils/styleGuideExport.js wrote "Made with [UIL4B](https://www.uil4b.com)" into every watermarked style-guide export, so every free-tier download ever made carries a dead link back to the site. KEPT DELIBERATELY: src/utils/environment.js still ACCEPTS www as a production host and api/_lib/origins.js still allows it as a Stripe return_url — both answer "is this visitor on the real site", not "what do we advertise", and failing to recognise a host a visitor could arrive on would silently drop real analytics. The apex is now first there and derived from SITE_ORIGIN. GUARD: tests/unit/canonical-host.test.js asserts on the BUILT artefacts, never the source, because the source can be right while a script still writes the wrong string. Nine tests over the 39 route shells + dist/index.html + the 404 shell, their JSON-LD, dist/llms.txt, dist/sitemap.xml, dist/robots.txt, and the watermark taken by IMPORTING the built export chunk and running it — the origin arrives there as a binding, not a folded literal, so reading the source would prove nothing about the bundle. Hermetic, no network. Every group carries a positive control because absence is trivially true of a glob that matched nothing. MUTATION PROOF, both shown to reach dist: hardcoding the dead host in prerender.mjs ORIGIN while leaving SITE_ORIGIN correct changed the shell content hash and flipped the needle from 0 shells to 40, turning four of the nine red while route-meta.test.js stayed GREEN, which is the whole argument for asserting on dist; then restoring that and breaking the watermark line moved the built chunk from styleGuideExport-CwGfVPrS.js to -DlwaZt9J.js and turned exactly the watermark test red. Both restored byte-exact, verified by an empty git diff and the chunk hash returning to CwGfVPrS.' },
  { id: 'audit-coverage-not-run', title: 'Close the coverage gaps the 2026-08-11 audit never checked', priority: 'P2', effort: 'M', area: 'Quality', status: 'partial', note: 'CARRIED FORWARD VERBATIM because silence is not a pass, and this is now the only record that these are unrun. The 2026-08-11 site audit explicitly did NOT check: dark theme contrast (the app force-resets to light); any signed-in state, anywhere — /create/alt-text’s actual tool is behind auth; screen-reader announcement with real assistive technology; text over gradients and images; target size (WCAG 2.5.8), pointer gestures and prefers-contrast; keyboard, contrast and reflow on the 20 routes that were audited for SEO only; and /create/file-converter’s upload-and-convert flow. Do NOT read any of these as passing. Keep this separate from the unrun list on homepage-field-metrics — that one is performance budgets on the homepage, this one is coverage across the app. PARTIAL PROGRESS 2026-09-03, recorded per line so the remainder stays visible. CLOSED BY MEASUREMENT: prefers-contrast is unimplemented and now has its own item (prefers-contrast-unimplemented). Target size (WCAG 2.5.8) was swept at six widths on eighteen routes — the sub-24px hits are real but are almost all footer nav links at about 21x21 and inline swatch controls, so it is queued at POLISH weight inside the audit rather than as its own P1. /create/file-converter’s upload-and-convert flow was walked end to end at 390 and 1280 and IT WORKS — see file-converter-flow-order for the one thing wrong with it, which is layout order, not function. Dark theme contrast is now ANSWERED RATHER THAN CLOSED: dark-theme-unreachable-but-shipped shows no user can reach the theme at all, so its contrast cannot be reviewed until a control exists. Keyboard and reflow were spot-checked at 320 through 1440 with no new defect. DARK-THEME CONTRAST IS NOW CHECKED (2026-09-04, dark-theme-unreachable-but-shipped). Every visible text node on all 29 prerendered routes was measured as rendered, in BOTH themes, at 390/834/1280 — true composited ground through every translucent ancestor, WCAG floor by the node’s own size and weight, text over background-images skipped rather than guessed. Note this is much wider than 39-accent-contrast.spec.js, which sweeps the ACCENT FAMILY on 11 routes: the claim that #331 had measured all 28 routes in both themes was not accurate. RESULT: dark 7 findings, light 10, and ALL SEVEN OF DARK’S ARE IDENTICAL IN LIGHT — there is not one dark-only failure left, so dark is measurably no worse than the light theme that has been shipping all along. Two dark misses found and FIXED here: .arc-ratio-card-use 4.32:1 and .ic-bp-tick-px 4.29:1 dark / 3.70:1 light, both --t3 at 9–10px, both moved to --t2 (light was the worse of the two on the second one). WHAT REMAINS, all theme-blind and all pre-existing in light: .fpr-hero-signature at 1.06:1, which is rgba(255,255,255,.035) — a decorative pointer-events:none watermark, though it is NOT aria-hidden so a screen reader still announces “Aa”; and .stc-cell-tone / .tt-cell-step at 4.14–4.39:1, which are labels printed ON colour swatches the tools themselves generated, not app chrome. None is a dark-theme defect. STILL GENUINELY UNRUN: any signed-in state anywhere, screen-reader announcement with real assistive technology, text over gradients and images, and pointer gestures. UPDATE: the /create/file-converter line is now fully closed — [file-converter-flow-order], the one thing that walk found wrong, is DONE. UPDATE 2026-09-04: TEXT OVER GRADIENTS AND IMAGES IS NOW CLOSED BY MEASUREMENT, and it was not merely unchecked — it was actively EXCLUDED. Both 39-accent-contrast and 43-state-token-contrast resolve a ground by walking ancestors, and both bail with a null on the first background-image they meet, so every node over a gradient was dropped BEFORE it was scored: neither spec’s counts moved and neither could fail, whatever the ratio was. That is the same shape as the rgba-only parser — not a wrong answer, an answer never attempted, with nothing to show it had not been. SCOPE, MEASURED across 24 routes signed out: 21 visible text nodes sit over a background-image and EVERY ONE is a linear-gradient. There is no url() image behind text anywhere in the app, which is why this could be done by arithmetic on the gradient rather than by sampling pixels. tests/user-sim/48-text-over-gradients.spec.js reconstructs the gradient line, projects the text’s own rect onto it so only the slice genuinely behind the text is considered, samples that slice and keeps the worst ratio. TWO PRE-EXISTING FAILURES, both invisible until now: /community .ch-thumb-mono at 1.74 to 2.83:1 against a floor of 3 (eight monograms whose ground is per-item seed data), and /seo .seo-social-img span at 3.00:1 against 4.5. Both are now on [gradient-text-below-aa], listed in the spec as an exact-match set so it fails in BOTH directions — a new surface below its floor is not on the list, and a fixed one still is. THE MODEL IS CHECKED AGAINST RENDERED PIXELS, and it had to be: two wrong answers were caught only by that check, and each produced a perfectly plausible ratio. Interpolating towards transparent in STRAIGHT alpha (CSS premultiplies) invented a #b9b8b4 midpoint under /discover/palettes and reported two AA failures the browser does not paint; and .seo-social-img’s second stop is a color-mix() that computes to color(srgb ...), so per CSS Color 4 that gradient interpolates in OKLAB rather than sRGB — measured at three points on the box, sRGB was out by 13, 12 and 1 on red where oklab matches within 1. STILL GENUINELY UNRUN, which is why this is partial and not done: any signed-in state anywhere, screen-reader announcement with real assistive technology, and pointer gestures. Silence on those three is still not a pass. UPDATE 2026-09-06: “ANY SIGNED-IN STATE, ANYWHERE” IS NO LONGER UNREACHABLE — the line that had survived every pass since 2026-08-11, and the reason [multi-breakpoint-ux-audit] is still partial. THE SUITE CAN NOW BE SIGNED IN. `signIn(page, { plan: “free” | “pro” })` in tests/user-sim/helpers.js declares a session before the page loads and the app’s own Firebase — doubled under `vite build --mode test` and nowhere else — answers with it. src/contexts/AuthContext.jsx is UNTOUCHED: it is founder-gated, and it did not need touching, because every piece of session state in the app descends from one line inside it — `onAuthStateChanged(firebaseAuth, …)` — where `firebaseAuth` is the `auth` export of src/utils/firebase.js. The double sits one level below the gated file, on the two Firebase entry points that file imports (`firebase/auth` and `firebase/firestore`), so RequireAuth, RequireAdmin, useSessionHint, canSaveProjects, the project cap, useSubscription().isPro, the export gate and `auth.currentUser` all see a real session because they see a real Firebase telling them there is one. Same reasoning as useSessionHint (#385) and useModerationRole (#390): read the context as a consumer, never edit it. WHAT IS REACHABLE NOW, every one of these RENDERED rather than read: /projects with real saved projects and the free allowance printed; the cap refusing a fourth save in ProjectContext’s own words, with a one-under-the-cap control so “it refused” is distinguishable from “nothing happened”; /settings, including the Subscription panel naming the live plan off the entitlement document; /admin behind RequireAuth and /style-guide behind RequireAdmin, with a non-admin’s redirect measured rather than assumed; /create/alt-text’s ACTUAL TOOL and its free daily counter — the example this row has named as unreachable since it was written; the Pro half of the export gate, where a Pro session opens the design system book and a free one gets the wall that names that document; BillingBanner, which 20-billing-banner.spec.js had been measuring against a hand-written copy of its own markup since the day it was written; and the `/` → /projects decision, with the sales page proved never to have painted on the way. THE WIRING TEST is tests/user-sim/57-signed-in-session.spec.js (15 tests, every absence paired with a rendered presence). NO FIREBASE HOST IS CONTACTED, which is stronger than the One Tap stub’s promise rather than equal to it: the double answers from memory instead of intercepting requests, so there is no request to escape — asserted with a positive control that reads three values only the Firestore path could have produced. IT CANNOT SHIP, and that is measured, not argued: the plugin is only CONSTRUCTED when the mode is “test”, so a production build carries no disabled copy and no flag that would enable one, and tests/unit/test-session-not-in-production.test.js runs a real production build, scans every emitted file for fingerprints that survive minification, and proves the same scanner finds all of them in a `--mode test` build so a clean production result cannot be a broken scanner. MUTATION-VERIFIED: dropping the mode guard fails both halves of that test; removing the plugin altogether fails 15 of the 21 signed-in acceptance tests, and the six survivors are exactly the signed-out controls. STILL GENUINELY UNRUN, and now two rather than three: screen-reader announcement with real assistive technology, and pointer gestures. UPDATE 2026-09-07: THE SIGNED-IN SURFACES ARE NOW WALKED AS FLOWS, not just reached. tests/user-sim/58-flows-project-lifecycle.spec.js drives duplicate, rename, archive, restore and delete from the project card’s overflow menu and asserts on vs-projects rather than on the menu, because ProjectActions renders six items from a static list and “the menu has a Duplicate item” passes for a Duplicate item wired to nothing — mutation-verified by dropping onDuplicate, onArchive and onDelete at the Projects.jsx call site one at a time, and in all three the menu test kept passing. 58-flows-export-honesty.spec.js reads the DOWNLOADED file to prove the free style guide carries the “Made with UIL4B” line that /plans sells the removal of, and that Pro’s does not — the paid claim had only ever been checked from the Pro side, which would let the benefit be given away free without anything going red. 58-flows-account-and-community.spec.js covers the two irreversible moments: the community submit gate naming what the visitor was in the middle of, and the delete-account panel itemising what it destroys, with the Pro-only “your subscription is cancelled” line paired against a free account that must not see it. STILL GENUINELY UNRUN, and now four: screen-reader announcement with real assistive technology; pointer gestures; a community submission followed from the form into the moderation queue, because that write goes to Firestore and the test session answers from memory, so both ends render but the round trip is read rather than rendered; and resuming a half-finished submission after sign-up — deleting setSubmitIntent(SUBMIT_SURFACE) leaves the whole flow-6 spec green, which was verified deliberately, because the gate’s “Where you left off” sentence comes from the requireLogin reason instead.' },
  { id: 'lazy-route-fallback-passes-for-rendered', title: 'A lazy route’s loading fallback satisfies every has-it-rendered check the suite has', priority: 'P2', effort: 'M', area: 'Quality', status: 'done', note: 'Found while diagnosing a CI failure on the five-defect audit PR, which blamed that branch for a defect it did not have. 43-state-token-contrast walks eight routes and counts state-coloured text; it failed on its own anti-vacuity guard with "no state-coloured text was found to measure in light". THE SAMPLE HAD COLLAPSED AND THE APP WAS FINE. Every route it walks is lazy(), so the Suspense fallback App.jsx renders (a bare .page-loading spinner) leaves the nav, header and footer chrome on screen. MEASURED: at that moment document.body.innerText is 421 characters of chrome, against 6537 once /privacy actually arrives. WHY THAT DEFEATS EVERY READINESS CHECK THE SUITE OWNS. The spec used READY = "main, .landing, #root > *", and #root > * is satisfied by the fallback itself, so it paired that with a flat waitForTimeout(150) — a guess about how long a dynamic import takes. Reproduced deterministically with Chromium Emulation.setCPUThrottlingRate: at 16x, /privacy and /sitemap both measured ZERO, the light total fell from 52 to 13, and the guard fired. It reproduces on a clean main with no feature branch in it. Fixed in that PR for this one spec, which now waits for .page-loading to detach and keeps its thresholds untouched. WHAT IS STILL OPEN, AND IT IS THE REASON THIS IS AN ITEM. That fix is local to one file. .page-loading is waited for by exactly ONE spec in tests/user-sim — the one just fixed — so every other spec that navigates to a lazy route and measures immediately is exposed to the same failure, and will express it as a confident false claim about the app rather than as a timeout. Fifteen of the forty-six specs use a flat waitForTimeout somewhere; that is an upper bound, not a defect count, because some of those waits are legitimately about animation or debounce and want reading one at a time. THE SHARED HELPER IS PART OF THE PROBLEM. helpers.js expectRendered() answers "the page rendered real content (not a blank shell)" with textLen > 40, and the fallback state measures 421 — so it returns true for a route that has not loaded. It also RETURNS a boolean rather than asserting, and its three callers await it without reading the result, so today it decides nothing either way. Worth fixing at the helper: a go()-plus-ready that waits for .page-loading to detach would close the whole class in one place, and helpers.js already argues exactly this doctrine at length for Lenis — wait for the thing to be over, never for a number of milliseconds you hope covers it. FIXED 2026-09-04 AT THE HELPER, which is where this note says it belongs. go() in helpers.js now navigates AND waits for the route to arrive: #root has children AND no .page-loading is on screen, held for three animation frames. Both halves together, because a wait for the fallback to be DETACHED is satisfied by a fallback that has not been ATTACHED yet — the entire pre-hydration window, in which #root is still the empty div scripts/prerender.mjs wrote. expectRendered() now ASSERTS rather than returning a boolean, and counts the route’s OWN content (main plus any open modal) instead of document.body: MEASURED, main.innerText is EXACTLY 0 in the fallback state on every lazy route while body.innerText is 405 to 421, so the chrome is what made the old 40-character threshold meaningless. The modal half is not a special case — /login, and every RequireAuth redirect into it, renders a spinner in main on purpose and keeps its content in a dialog. FOUND WHILE PROVING THE FIX, AND NOW FIXED TOO: a dropped route chunk does NOT leave the fallback up. React hands the import failure to App.jsx’s ErrorBoundary, .page-loading unmounts, and main fills with the Something went wrong card — comfortably over any content threshold — so the JUST-FIXED check still passed a route that had crashed. Nothing under tests/ referenced .error-boundary, so no spec in the suite could tell a crashed route from a working one; watch() does record the console error, but findings are a printed report and the global teardown gates on One Tap alone. renderState() now reports crashed and expectRendered() asserts on it before it counts anything. PROVED BY MUTATION, not argued: with a render-time throw injected into src/pages/HelpCentre.jsx, npm run lint, npm run build and npm run test:unit ALL PASSED, and the PRE-FIX suite passed 38 of 38 — including the test named wandering the main surfaces never hits a blank or broken page. The fixed suite fails 3 and names the route. tests/user-sim/47-lazy-route-readiness.spec.js holds a route chunk open with page.route and states the vacuity as two numbers from one instant: body 421 (the old contract passes) against own 0 (the new one fails). WHY THIS IS PARTIAL AND NOT DONE: go() closes the class for the 28 specs that navigate through it, but 20 specs still call page.goto directly at 51 sites and get no wait at all. That population is pinned by tests/unit/lazy-route-readiness.test.js, which also fails if go() or ready() quietly loses its wait, and the remaining work is carried on [raw-goto-bypasses-readiness]. CLOSED 2026-09-04: the twenty specs and their call sites are converted and the allowlist is deleted, so the residue this note hands to [raw-goto-bypasses-readiness] no longer exists. That item carries the recount (61 sites, not 51), the deterministic proof, and two things this note did not have - page.reload() as a second door, and 39-accent-contrast’s INK_WALK reporting violations without ever reporting a sample size.' },
  { id: 'gradient-text-below-aa', title: 'Two surfaces paint text on a gradient below its AA floor', priority: 'P2', effort: 'S', area: 'Accessibility', status: 'done', note: 'HALF DONE. /seo is fixed and measured; /community is a founder call and the options are now PRICED rather than described, which is the thing this item was missing. (1) /seo .seo-social-img span - DONE, 2.99:1 to 5.45:1 against a floor of 4.5, measured on rendered pixels with the ink hidden and the gradient sampled under the label’s own rect. THIS NOTE WAS RIGHT THAT NO ALPHA BUMP REACHES IT: pure white on --accent measures 4.46 here, still short. Every candidate, same method: opaque ink alone 4.46, an --accent-strong ground with the 70% ink 3.97, a 28% black scrim 4.48, --accent-strong plus opaque white 6.36. AND THAT LAST ONE, THE OBVIOUS FIX, IS WRONG IN DARK - 6.36 light and 3.41 DARK, worse than the defect it replaced. --accent-strong is the darker member of the pair in light and the LIGHTER one in dark (#0B5ED7 against #4A90FF); [prefers-contrast-unimplemented] recorded the same reversal and it is worth repeating that the arithmetic in light is right and the conclusion still does not travel. Only a rendered measurement in the second theme catches it. THE SHIPPED FIX IS THEME-INDEPENDENT, which is the real answer rather than a workaround: this is a mock of a 1200x630 SHARE CARD, and the card it stands for is drawn by scripts/og-cards.mjs, which scopes its token reads to the light block on purpose because LinkedIn and X do not render a card in the author’s theme. A preview that flips with the viewer’s theme is showing something the card will never look like. Two literals from the light accent pair, plus an opaque ink: 5.45:1 in BOTH themes on the same ground #365EE2. KNOWN_BELOW_FLOOR in 48-text-over-gradients.spec.js has shrunk to match, which is what an exact-match list is for. Mutation verified: reverting both declarations turns both theme halves red. (2) /community .ch-thumb-mono - STILL OPEN, AND IT IS A FOUNDER CALL. It is also WIDER THAN THIS NOTE RECORDED: not eight monograms but NINE OF TWELVE below the 3.0 large-text floor, worst 1.47:1 modelled over the whole gradient (s9 Product & checkout, ground #6ee7b7) and 1.77:1 at the worst point actually behind a glyph. Only s1, s3 and s10 pass today. FOUR OPTIONS, ALL MEASURED, none picked. (A) A flat black scrim over every thumbnail: at 32% the worst card reaches 3.02:1, which clears by 0.02 and is not a margin; at 40% it is 3.72:1 and at 45% 4.30:1. Costs every card about a third of its colour, and the colour is the content on a design-community card. (B) PER-ITEM INK chosen from the card’s own gradient, the way readableInk and inkOnSolid already work elsewhere in this codebase: worst 3.69:1 across all twelve and NOT ONE CURATED COLOUR CHANGES. The cost is that ten of the twelve monograms flip from white to black, so the grid no longer has one ink. (C) Darken the seed colours, which is what this note proposed: the required move is 0% for s1/s3/s10 and 10%, 13%, 23%, 24%, 26%, 26%, 26%, 29% and 32% toward black for the rest - it changes curated content and is uneven across the set. (D) Drop the monogram: the anchor already carries its own aria-label so it is never announced, and it is visually redundant with the name printed beside it. WCAG has no exemption for redundant visible text, so this is the only option that removes the requirement instead of meeting it. ON THE EVIDENCE, (B) is the one that keeps the curated colour AND clears the floor everywhere, and Mobbin backs the shape of it - Copilot Money, Origin, Basecamp and Tana all choose the ink for the ground rather than fixing one ink and hoping. But it changes how twelve community cards look, so it is the founder’s pick and not mine. DONE 2026-09-05. THE FOUNDER PICKED (B), PER-ITEM INK, and it shipped exactly as priced: not one curated colour changed, and ten of the twelve monograms flipped from white to black. KNOWN_BELOW_FLOOR in 48-text-over-gradients.spec.js is now EMPTY, which is the assertion - an exact-match empty list fails the moment a new surface drops below its floor AND fails if either of these two regresses. MEASURED, over the whole gradient rather than only the slice behind a glyph, which is the stricter reading: worst BEFORE 1.47:1 with nine of twelve under the 3.0 large-text floor; worst AFTER 3.46:1 with zero under it. Per card: s1 3.35 to 4.63, s2 1.71 to 7.10, s3 9.03 to 9.28, s4 1.81 to 7.76, s5 2.50 to 5.36, s6 2.38 to 3.81, s7 1.60 to 9.17, s8 1.90 to 4.41, s9 1.47 to 5.23, s10 6.85 to 6.96, s11 1.76 to 3.46, s12 1.73 to 5.58. 3.46 AND NOT THE 3.69 THIS ITEM PRICED, and the difference is one deliberate choice: the priced figure assumed PURE black, and inkOnGrounds uses this codebase own near-poles (#0A0B0D / #F2F3F5) so the monogram matches every other ink the app picks. On s11 (#7C3AED to #C4B5FD) pure black measures 3.69 and near-black 3.46. Both clear 3.0; swapping to pure poles here alone would buy 0.23 at the cost of forking shared machinery, so it was not done. Say the word and it is a one-argument change. BOTH THEMES, AND THE ANSWER IS THAT THE THEME CANNOT REACH IT. Read off the rendered page at 1280 in light and in dark: all twelve inks are byte-identical between themes, because the thumbnail ground is per-item DATA (--c1/--c2 from the seed row), not a theme token. That is structurally unlike the /seo fix in this same item, where --accent-strong is the darker member of the pair in light and the lighter one in dark and the obvious fix measured 6.36 light / 3.41 dark. Here there is no token in the ground stack to reverse. THE INK ALSO WENT OPAQUE. rgba(255,255,255,.92) is composited by CSS toward the ground, so the effective ink was a different colour on every stop of every gradient - worth 0.1 to 0.2:1 here and about 0.7:1 on the state tokens (#346). WHERE THE MATHS LIVES: a new exported inkOnGradient() in src/utils/colors.js, which SAMPLES the ramp and delegates to the existing inkOnGrounds pole walk rather than carrying a private copy. SAMPLING IS LOAD-BEARING, not precision theatre. Relative luminance is convex along a gamma-encoded per-channel lerp, so its maximum is at an endpoint but its MINIMUM can be interior - and a dark ink worst ground is the darkest one. #FF0000 to #0080FF is the case that proves it: near-black reads 4.92:1 at the two stops and 2.76:1 sampled, so an endpoint-only model does not merely report a kinder number, it PICKS THE WRONG POLE and ships an illegible monogram. That pair is pinned in the unit test. AN HONEST BOUND, recorded rather than papered over: per-item ink is not total. A gradient that STRADDLES - light enough at one end to defeat a dark ink and dark enough at the other to defeat a light one - has no single ink, and would need option (A) or (C). #000000 to #808080 still clears at 3.56; #000000 to #A0A0A0 fails at 2.61. No shipped pair is close (every one keeps its whole ramp on one side, and the test asserts each clears the floor by at least 0.4), but Community.jsx spreads a queue payload over its c1/c2 defaults, so an admin-authored submission could in principle reach it. Pinned as a bound in the test so nobody reads the function as a guarantee for arbitrary data. PROVED IN TWO PLACES ON PURPOSE. tests/unit/community-monogram-ink.test.js is the cheap half - the guarantee is about the FUNCTION, and the rendered spec only ever walks the twelve rows that happen to be on the page. 48-text-over-gradients.spec.js stays the pixel oracle: its gradient-model test screenshots /community with the ink hidden and checks the reconstruction against what Chromium actually painted, which is what caught the premultiplied-alpha and oklab errors in the first place. The unit file also asserts the wiring (CommunityCard calls inkOnGradient and sets --mono-ink/--mono-glow; the rule reads var(--mono-ink) and is opaque), because correct-but-orphaned arithmetic is a failure this repo has shipped before. VERIFIED BY 5 MUTATIONS, ALL CAUGHT: reverting the CSS to rgba(255,255,255,.92); deleting the --mono-ink hand-off in CommunityCard; dropping inkOnGradient to 2 samples (endpoints only); making it always return white; and darkening the s9 seed instead, which is option (C) and fails the no-curated-colour-changed assertion. The first draft of the sampling test did NOT catch the 2-sample mutation - the probe pair it used still cleared the floor either way - and it was rewritten around #FF0000 to #0080FF for that reason.' },
  { id: 'raw-goto-bypasses-readiness', title: 'Twenty specs navigate around the one door that waits for a route', priority: 'P2', effort: 'S', area: 'Quality', status: 'done', note: 'DONE. The population is CLOSED: every raw page.goto() in tests/user-sim is gone, and the KNOWN_RAW_GOTO allowlist in tests/unit/lazy-route-readiness.test.js is gone with it - the assertion is now unconditional, so there is no list to keep in step with the tree. THE COUNT IN THIS ITEM WAS WRONG AND THE RECOUNT IS THE FIRST FINDING: it recorded 51 call sites, and a comment-blind count finds SIXTY-ONE across the same 20 files (24-mobile-overhaul 5, 30-founder-requests 7, 33-offline-state 7, 35-colour-picker 6, 41-theme-control 9, and so on). Sixty went to go(); ONE went to goRaw(), which now takes an options object. That one is 04-premium-home, which asserts on #boot-shell - the static shell scripts/prerender.mjs writes - BEFORE React replaces it, and needs waitUntil: ’commit’ because module scripts run before DOMContentLoaded fires. Leaving it as a bare goto would have kept a hole in the guard, so goRaw now has two documented callers instead of one and the unit test names both. THE CONVERSION SURFACED NOTHING ON AN IDLE MACHINE - the full suite was 482 passed / 13 skipped / 0 failed before and after - which is what the original note predicted, since most of these navigations are followed by an auto-waiting locator. So the exposure was proved the way this repo proves things: by making it deterministic. WITH EVERY LAZY CHUNK HELD BACK 1500ms AND NO CPU THROTTLE AT ALL, on the eleven routes 39-accent-contrast walks, the OLD contract (page.goto, then the readiness selector, then a flat 150ms) measured NINE of ELEVEN routes at ZERO own content - / and /discover are the two that are not lazy - and 297 accent-ink nodes were never seen. Through go(), all eleven measured full content. THEN PROVED BY MUTATION RATHER THAN ARGUED: a 2.1:1 accent label was injected into /sitemap, a lazy route, and the SAME build run twice with the same chunk delay. The pre-conversion shape PASSED BOTH THEME TESTS while the defect was on screen; the converted shape FAILED both and named it. The control is accidental and worth keeping: the 390px test, which never received the delay, failed in BOTH runs - so the defect was always detectable and the pre-conversion pass was blindness, not absence. A SECOND DOOR, NOT IN THE ORIGINAL NOTE: page.reload() bypasses readiness exactly as goto did. Six sites across three specs. Three of them, all in 41-theme-control, paired the vacuous readiness selector with a reload and then measured the theme with page.evaluate; they now reload and await ready(). The other three (10-home-chaos-to-calm, 11-palette-recovery) wait on a route-specific locator and were left alone. The new guard bans the SELECTOR rather than enumerating navigation verbs, which is the only reason it covers reload - no goto-shaped rule would ever have seen those three. FOURTEEN CAN’T-FAIL CHECKS DELETED: main, .landing, #root > * matched the Suspense fallback three ways, so waiting on it before measuring read the nav and the footer. Seven in 41-theme-control, three in 39-accent-contrast (with its READY const), one in 45-prefers-contrast, and 43-state-token-contrast’s local settle() reduced to the trailing PAINT wait it always also was, because go() is strictly stronger than the READY-plus-detach pair it did for itself. AND ONE ASSERTION THAT COULD NOT FAIL, FOUND AND FIXED: 39-accent-contrast’s INK_WALK reported violations and never how many nodes it examined, so an unarrived route contributed nothing and both theme tests called it a pass - which is precisely what the 297 unseen nodes above were doing. FILL_WALK in the same file already carried that guard, and its comment records the vacuous pass that earned it; the ink half never got one. Both halves now count and assert (floors 30 and 10), mutation-verified by making the walk match nothing. GUARD MUTATIONS, eight, each restored with cp and compared byte-for-byte: go() stops awaiting ready(); ready() stops looking for .page-loading; ready() stops checking React mounted; a third spec calls goRaw(); go() renamed away across the suite (the positive control, without which the no-page.goto assertion would also pass on a suite that had stopped navigating); a spec reverts to page.goto(); the banned selector reappears; and a control edit that correctly stays green. ONE MUTATION FIRST ESCAPED AND THE HARNESS WAS AT FAULT, NOT THE TEST - the pattern for .page-loading also matches inside renderState’s !!document.querySelector(...), so it mutated the wrong function and the gate stayed green for a correct reason. Retargeted, it fails.' },
  { id: 'ready-accepts-unrendered-states', title: 'The readiness door counted two states that render no page as arrival', priority: 'P1', effort: 'M', area: 'Testing', status: 'done', note: 'DONE. go() -> ready() decided a route had ARRIVED in two states where nothing measurable was on screen, so a spec that navigated and then measured reported a confident number about a page that was not there - and expressed it as a MISSING ELEMENT, which sends the next investigation at the branch under test. Same family as [raw-goto-bypasses-readiness] one level further down. (1) THE STATIC BOOT SHELL. index.html ships <div id="root"><div class="boot-shell" id="boot-shell"> and scripts/prerender.mjs clones it into all 33 route shells, so BOTH conditions ready() waited for were satisfied by markup the SERVER wrote: #root has a child, and there is no .page-loading because App.jsx has never rendered. #391 caught /create/semantic-color in exactly that state under four parallel workers - h1 not found, accessibility snapshot reading status: Loading UIL4B, the boot shell’s own live region - and worked around it in 55-header-sweep.spec.js alone. ready() now requires ONE PRESENCE and TWO ABSENCES in the same frame; the shell going away is a POSITIVE fact about React, because createRoot().render() clears its container on the first commit. The local workaround is deleted. (2) A TOOL THAT HAS ARRIVED AND HAS NO DATA. The three typography tools render <FontCatalogLoading> INSTEAD of their workbench while the Google Fonts catalogue is in flight - its own comment says so - and ready() knew only about App.jsx’s .page-loading. MEASURED on /create/font-gallery: /api/fonts answered, ready at 188ms with 24 rows; /api/fonts held, ready at 148ms with .typ-loading up and ZERO rows, settling at 2530ms. That is what 51-typography-paywall hit (see [suite-flake-class-unreproduced] mechanism C). NOT A WIDENED TIMEOUT: requestCatalogJson aborts each source after 2000ms and fetchFontCatalog never rejects, so this waits for a state the app GUARANTEES terminates, and the 20s backstop is unchanged. THE SELECTOR IS .typ-loading > .fg-loader AND THE DIRECT CHILD IS LOAD-BEARING - FontMatcher renders a second .typ-loading for No font catalogue is available right now, a rendered ANSWER rather than a wait, and a bare .typ-loading would burn the backstop on a page that had already finished. THE FAILURE NAMES WHICH OF THE THREE STALLS IT IS (describeStall): the app never replaced the static boot shell / the tool never got past its own data-loading state / the route never got past its lazy-loading fallback. They want opposite next steps, and expected 421 to be greater than 40 once cost a day by naming none of them. ready() also reports how many frames it examined, so a wait that looked at NOTHING is visible in its own message. THE FONT CATALOGUE IS DELIBERATELY NOT STUBBED, and the reasoning is written out in helpers.js: there is no third-party round trip to remove (no VITE_GOOGLE_FONTS_API_KEY in the test build, so the only request is /api/fonts, which vite preview answers locally - measured, one hit, 188ms); the degraded catalogue is a SHIPPED state with its own notice and retry that 33-offline-state and 11-typography-tools exercise on purpose; and a stub would hide this defect rather than fix it, covering one tool family and leaving every other data-loading surface exposed. PROVED BY MUTATION, 14 of 14 as expected, green control first and last, every file restored byte-exact: the boot-shell clause removed (RED in the rendered spec AND the unit guard), renderState dropping booting, index.html renaming the shell (anti-vacuity), the message no longer distinguishing the shell, the examined-frames count dropped, the hint dropped, the data-loading clause removed, renderState dropping dataLoading, the selector losing its direct-child spinner, and FontCatalogLoading losing the spinner the selector keys on. NOT COVERED, said rather than implied: describeStall’s data-loading branch cannot be reached from a test, because the app’s own 2000ms bound guarantees the state ends; it is a backstop for a wedged catalogue, not an asserted path. Contracts: tests/user-sim/47-lazy-route-readiness.spec.js (rendered, entry bundle and catalogue held back) and tests/unit/lazy-route-readiness.test.js (structural, with the index.html and FontCatalogState anti-vacuity pins).' },
  { id: 'home-preview-test-asserts-a-reroll', title: 'A homepage test asserted that a random generator produces a different value', priority: 'P1', effort: 'S', area: 'Testing', status: 'done', note: 'DONE. 10-home-chaos-to-calm test 8a - the palette preview is a real UI painted from the live swatches - read the first chart path’s fill, pressed Generate and polled until the fill was DIFFERENT. On 2026-09-06 a regenerate produced #3B82F6 twice and it failed. NOT BAD LUCK: #3B82F6 is PV_BRAND in utils/colors.js, the focal colour derivePreviewRoles falls back to for an all-grey palette or a primary that clashes with its own background, so the chart fill is a DERIVED value - one of five swatches, or that fallback - and repeats are something the engine does on purpose. It changed was never a property of the component. THE NAME OF THE TEST SAYS WHAT IT SHOULD ASSERT. The chart fill must EQUAL the primary derivePreviewRoles derives from the swatches ON SCREEN at that moment, sampled on arrival, after each of six Generates, and again in dark theme where the engine takes bg from the other end of the ramp and the role can land on a different swatch. That equality holds on every roll, including the one that repeated. The board and the paint are read in ONE evaluate so they cannot straddle a re-render (the torn-frame reading 8c had to be fixed for), and there is no wait for the palette to change - every reading is self-consistent whether or not the click has committed. clashes === 0 is kept, now on every sampled palette rather than on two. NON-VACUITY, which is why the loop exists: the correspondence is trivially true of a preview that FROZE at mount, so the test counts DISTINCT palettes seen and requires more than one. That is not the inequality it replaced - that one asked a derived value with a real collision rate to differ; this asks whether the SOURCE varied across six presses of a generator drawing hue and saturation from continuous ranges. It imports derivePreviewRoles, the same engine PaletteStage calls, so what it pins is the HAND-OFF and not the engine’s choices, which are tests/unit/preview-roles-contrast.test.js. MUTATION-VERIFIED IN APP CODE, 6 of 6, each with a full rebuild, green control first and last: the preview freezing the palette it saw at mount (the defect the old inequality was reaching for), the chart painted with role.accent instead of role.primary, the preview deriving its roles in light mode whatever the theme is (every light assertion passes; the dark correspondence catches it), and Generate no longer re-rolling the board - which keeps the correspondence TRUE and is caught only by the signature count.' },
  { id: 'reduced-motion-rendered-verification', title: 'Verify the ten reduced-motion rules that have never once executed', priority: 'P1', effort: 'S', area: 'Accessibility', status: 'done', note: 'DONE (#330), and the enumeration in this item was wrong before it was checked. Grepping the attribute finds twelve sites, two of which are not in any media block, and MISSES two real ones; counted mechanically, the ten are the ten GUARDED media blocks. VERDICT: nine fire and behave correctly under emulated reduce, and the tenth belongs to a component global.css already documents as deleted. NOTHING BREAKS — including the global clamp that forces transition-duration and animation-duration to 0.01ms and scroll-behavior to auto, which was the one most likely to. The two blocks the old grep missed are now verified by the snap-pull measurement their own comments ask for (pull 0 under reduce, −42 in control). THE RECOUNT IS WHAT MATTERED: it exposed that of 23 prefers-reduced-motion blocks only 10 carry the guard and 13 are BARE, which is the INVERSE of the bug #273 fixed — measured on .skip-link, a visitor who explicitly turned motion back ON gets 0s. That is now its own item, [reduced-motion-guard-missing-on-thirteen-blocks].' },
  { id: 'onboarding-survey-unused', title: 'Use the onboarding survey answers, or stop asking for them', priority: 'P2', effort: 'M', area: 'Account', status: 'done', note: 'DONE — STOPPED ASKING, and made the one remaining answer route. Re-verified before removing: the COMPLETE reader list was the admin table, for use and role. `source` was read by NOTHING, anywhere. The routing test settles all three — which screen, content, default or recommendation changes because of this answer? Nothing did — so they are gone rather than kept and quietly ignored. WHAT REPLACED THEM IS NOT A SHORTER SURVEY. The first-win screen already asks the only question with a consequence — which of three things do you want to make — and its answer picks the next screen and opens that tool. Onboarding is now ONE screen, down from four, and all four removed steps were dead weight. The progress dots and the corner Skip went with the survey: a rail that is always full says nothing, and a Skip beside a Not-now that does the identical thing is two names for one action. Their CSS is removed, not left orphaned. THE REMAINING ANSWER HAS A READER: onboarding.firstWin is persisted AND the admin table now shows and sorts it, so this does not quietly recreate the very defect it removes. A test fails if the field is written without a reader. Role and Category stay as HISTORICAL columns — existing accounts answered them and deleting the view would destroy that — but they will read as an em dash for every new account. RETIRING THEM IS A FOUNDER CALL, not one I made. ATTRIBUTION IS THE ONE REAL LOSS and it is deliberate: ’how did you hear about us’ cannot be derived from behaviour the way role and use can. If it is wanted back, the honest place is AFTER the first win, not in front of it, and it is one entry in a list. REFERENCES — Mobbin/Ghost (’we will personalise your setup based on this’) and Mobbin/Gamma (’we use your answers to personalize your experience’) both state the consequence next to the question; the old copy said ’this helps us improve UIL4B’, which was true of nothing. Mobbin/Intercom shows the other half of the standard — its questions carry a per-question Skip and sit OVER an already-loaded product, so the survey is never the thing between you and the app. Proof: tests/unit/onboarding-once.test.js (11, two rewritten to assert the guarantee rather than the old shape) and the user-sim spec above, which asserts the questions are gone from the RENDERED flow rather than from source text.' },
  { id: 'onboarding-first-win-path', title: 'Put the first real win on the path, in place of the pricing step', priority: 'P1', effort: 'M', area: 'Account', status: 'done', note: 'DONE. THE PRICING STEP IS GONE AND THE FIRST WIN IS IN ITS PLACE. Onboarding used to end by asking for money: a brand-new account, three survey questions deep with nothing built and nothing saved, was shown a two-tier table, a billing toggle and a Stripe button, and the step after that was /projects asking them to go and start again. WHAT THE FIRST WIN IS: still the first SAVED artefact, unchanged from B5. The screen does not claim the win, it points at it: three starting points, each a live Create tool that opens when picked. WHY THOSE THREE, and this is the part that was not a judgement call: they are the three the SIGN-UP PROMISE already names. LoginPopup tells someone creating an account, in the largest type on the dialog, ’Your palettes, type scales and gradients, kept.’ Landing that person on a pricing table changed the subject; landing them on one of those three is the same sentence in the imperative. A test asserts the popup still makes that promise, so the two cannot drift. Routes are checked against liveToolRoutes() rather than trusted, so flipping a tool to soon:true fails the build instead of shipping a card that dead-ends on a still-building screen. THE PRICING IS NOT HIDDEN, only off the blocking path: one honest sentence and a real /plans link stay on the screen, because shortening a path by concealing a cost the user meets later buys an easy finish with distrust. Nothing here touches entitlement and Pro is unchanged — typography-paywall-model stays founder-owned. TIME TO VALUE, built ON upgrade-activation-events rather than beside it: a clock starts when the screen is answered and is read on the FIRST trackActivation, then cleared, so forty saves in an afternoon contribute one reading. Bucketed, not raw milliseconds, because the shared aggregate is increment-only counters and a duration cannot survive that merge. A negative elapsed time is REFUSED rather than clamped, so a clock that goes backwards cannot flatter the metric. New counters: firstwin__<id>, ttv__<bucket> and ttv__<bucket>__<tool>. REFERENCES — Mobbin/Docusign ’What is your primary goal today?’, whose options are TASKS and whose sub-line promises you can return to the others, drove the shape of the replacement step; Mobbin/Cloudflare and Mobbin/Relevance AI (’you can switch any time’) drove the reversibility line; Mobbin/Intercom and Mobbin/Later drove where the pricing went — both keep monetisation as a persistent non-blocking surface while onboarding ends inside the product, never as a step in front of it; Mobbin/Kajabi ends on a finished artefact with the next action on it, which is the standard this screen is trying to meet and does not yet — see below. NOT DONE, and stated rather than implied: the tool a card opens does not yet say ’save this to keep it’. The card copy names the finishing move, but the contextual nudge belongs in PaletteBuilder / TypeScale / GradientGenerator, which a concurrent workstream owns. Until it exists, the gap between choosing a start and reaching the win is measurable but unaddressed — which is exactly what the firstwin__ and ttv__ counters were added to expose. Proof: tests/unit/first-win.test.js (11), tests/unit/time-to-value.test.js (16), tests/user-sim/40-onboarding-first-win.spec.js (9), all verified by mutation — 20 mutations, each confirmed red. One caught a vacuous assertion of my own: a bare includes(ttv__) passed with the headline counter deleted, because the per-tool counter carries the same prefix.' },
  { id: 'downgrade-limit-communication', title: 'Tell a downgraded user what changed, before they hit the cap', priority: 'P2', effort: 'S', area: 'Billing', status: 'done', note: 'DONE, and NO entitlement behaviour changed — ProjectContext still blocks only new saves and still deletes nothing. What changed is what the user is told. Projects.jsx now reads projectLimit (it previously did not mention it at all) and counts the SAME array saveProject() counts, so the counter cannot drift from the rule that does the refusing. THE REAL DECISION WAS WHEN TO SPEAK, not what to draw, so it lives in src/utils/projectQuota.js away from any DOM: four states, silent while there is room, warning once the allowance is nearly gone, explaining itself at the cap. It is a FRACTION (20%, floor of one slot) rather than a flat "warn at one left" because the cap is plan data read live from plan.limits.projects and can move without a deploy — at 3 the two rules agree, at 20 a flat rule would warn with 19 saved, which is a warning too late to act on. The counter says nothing at all over a free user’s first two projects: the founder’s bar is that a counter which nags is worse than no counter, and a permanent "1 of 3" from the first save turns P-003’s foot in the door into a countdown. THE TRAP, found by reading the code rather than trusting the reference: Maze’s at-limit dialog offers "archive a project to create a new one" and archiving is the obvious free path here too — but archiveProject only TOGGLES A FLAG, the record stays in the array saveProject() measures, so an archived project still occupies a slot. Telling people to archive would have been false, and the person most likely to believe it is the one who already archived something. The copy says delete, and mentions archiving ONLY to someone who actually has an archived project, to correct that assumption. NO progress bar: on Mobbin, Mixpanel, HubSpot, GitLab, AutoSend and Sprig all render an empty rail below about 40% usage, spending layout to say "almost nothing is used" — at a cap of 3 the numerals carry every state, so this follows IconLibrary.jsx’s existing n/limit counter rather than inventing a second visual language for the same idea. ZERO global.css changes: Projects.jsx is inline-styled throughout and the note matches it. GAP WORTH KNOWING: the user-sim suite cannot create a signed-in user (20-billing-banner.spec.js records the same limitation about its own feature), so the warned states are pinned exhaustively in tests/unit/project-quota.test.js — every cap 1..40 at every count — and the browser test covers the half only a browser can see, that /projects sits behind RequireAuth and no allowance ever reaches a signed-out stranger. FLAGGED FOR THE FOUNDER, not actioned: should archiving free a slot? It reads like it should, and today it does not.' },
  { id: 'account-identity-gaps', title: 'Close the three gaps in the account identity model', priority: 'P2', effort: 'M', area: 'Account', status: 'blocked', note: 'BLOCKED, and the block is the founder gate rather than the work. ALL THREE GAPS RE-VERIFIED IN SOURCE 2026-09-05 and all three are real: zero occurrences of sendEmailVerification or verifyBeforeUpdateEmail anywhere in src or api - while emailVerified is READ in three places, so the product DISPLAYS a fact it never establishes, including the Admin table’s "unverified" suffix; zero occurrences of linkWithCredential, linkWithPopup or fetchSignInMethodsForEmail; zero occurrences of revokeRefreshTokens. WHY NOTHING SHIPPED. Every one of the three lands in src/contexts/AuthContext.jsx, which docs/reference/human-validation-zones.md lists as founder-gated with the instruction that an agent FLAGS rather than touches it and states the blast radius first. This agent has no founder approval, and an assignment from another agent is not one. BLAST RADIUS, as the doc asks for it. WHAT COULD BREAK: the signup and sign-in path for every visitor - a thrown error inside signup() or a mis-ordered await locks out new accounts, and a linking flow that guesses wrong merges two people’s data. WHO IS AFFECTED: all users, paying ones included, since Pro entitlement is keyed to uid and a wrongly created or wrongly merged uid moves a subscription. REVERSIBLE: the code is; a merged or duplicated uid is not. TWO HARD CONSTRAINTS FOUND WHILE VERIFYING, both of which change the SHAPE of the work and neither of which this item recorded. ONE: SIGN OUT EVERYWHERE HAS NO ROUTE TO LIVE IN. Firebase has no client-side session revocation; it needs adminAuth().revokeRefreshTokens(uid), which needs a serverless function. api/ holds exactly 12 deployed routes and tests/unit/account-deletion.test.js fails the build on a 13th, so it must replace a route or fold into one - the same founder call already parked on [locked-library-values-still-in-bundle]. TWO, AND IT IS THE BIGGER ONE: REVOCATION WOULD BE COSMETIC AS THE API STANDS. Eight call sites verify an ID token and only ONE passes checkRevoked - api/delete-account.js, whose own comment already anticipates this feature. api/ai.js, checkout-status.js, create-checkout.js, create-portal.js, setup-stripe.js, verify-admin.js and _lib/admin.js all call verifyIdToken(token) with the flag defaulted off, so a revoked session keeps working against every one of them for the remaining life of its token - up to an hour - including spending the user’s AI quota and opening the billing portal. So this is not one endpoint; it is one endpoint plus a flag flip in seven files, five of them on the payments gate list. Shipping the button without the flips would hand users a security control that controls nothing, which is worse than not shipping it. FOUNDER DECISIONS NEEDED, so the call can be made rather than deferred again: (a) approve touching AuthContext.jsx at all, and for which of the three gaps; (b) which of the 12 routes sign-out-everywhere replaces, or whether the plan moves; (c) whether the seven checkRevoked flips are in scope, given they touch gated billing files; (d) console work only the founder can do - the Firebase "One account per email address" setting decides whether the LINKING gap is even reproducible, because with it ON Firebase already refuses the duplicate and the real work becomes catching auth/account-exists-with-different-credential rather than creating a link. NOTE ON (d): email verification is NOT blocked on RESEND_API_KEY. Firebase sends that message itself, so it is independent of [transactional-email-channel] even though this item ties them together - it needs the template and action URL configured, nothing more.' },
  { id: 'transactional-email-channel', title: 'Get a deliverable sending domain and the transactional emails behind it', priority: 'P1', effort: 'L', area: 'Growth', status: 'blocked', note: 'From the 2026-08-12 account lifecycle audit, section C. TOTAL USER-FACING TRANSACTIONAL EMAIL: ZERO — re-verified 2026-09-01. The only email the product sends is inbound-to-founder from api/support.js, and it sends from onboarding@resend.dev, which is Resend’s shared sandbox domain and is not production deliverable; the product’s own admin panel already marks it failing. There is no welcome, no quota warning, no trial-ending reminder, no failed-payment notice and no cancellation confirmation. Password reset comes from Firebase, not from us. WHY THIS IS P1 RATHER THAN HOUSEKEEPING: there is no channel at all to reach a user who is not currently in the app, so every re-engagement moment is unreachable — and it is the missing half of the billing work, because BillingBanner only ever reaches someone who has already come back on their own. BLOCKED, and the blocker is not code: it needs a verified sending domain, which is owner work. Stripe may send its own receipts and dunning depending on dashboard configuration — NOT CHECKED. See docs/OWNER-ACTIONS.md.' },
  { id: 'footer-attribution', title: 'Credit the founder in the footer, linking his portfolio', priority: 'P2', effort: 'S', area: 'Growth', status: 'done', note: 'DONE. The footer legal line reads "Built in Brisbane by Dylan Coleman" and the name is a real link to https://dylan-coleman.com/ — the URL settled by decision 3 of the 2026-08-20 founder batch, read out of CHANGELOG.md rather than taken on trust. THE EXISTING BRANCH WAS A DEAD POINTER: feat/footer-attribution sat on a commit that is an ancestor of main and carried no work at all, so this started fresh from origin/main. THE TREATMENT WAS ALREADY DECIDED BY THE APP, twice over — HelpCentre.jsx and Settings.jsx have both linked this exact URL for some time as target="_blank" rel="noopener noreferrer" with NO nofollow, because nofollow belongs to the curated and member-submitted third-party links in Discover and Community and this one is ours. The URL stays a literal rather than passing through safeHttpUrl(): that guard is for URLs arriving at RUNTIME, and a constant in this file has no untrusted path, so sanitising it would only add a branch no test could take. WHAT THE APP DID NOT ALREADY ANSWER was the glyph. Those two precedents sit in prose and chrome; the footer is a nav surface where every sibling is an internal NavLink, so the one link that leaves has to say so — it carries the app’s existing aria-hidden ↗ (FontGallery, FontMatcher, PaletteGallery, DiscoverCard) plus an sr-only "(opens in a new tab)", and a RESTING underline rather than a hover one, since founder direction 2026-09-02 rules out hover-only affordances on touch. THE UNDERLINE NEEDED MEASURING, not just adding: pinned to a fixed token (--bh) it read 1.7:1 on the dark footer, and at color-mix 50% it read 2.4:1 on the light one, so it is 70% of currentColor — 3.73:1 light, 5.34:1 dark, both clear of 1.4.11. Two additive lines in global.css, no restructure. The ask was typed lowercase; the line is sentence case with the proper nouns capitalised, like the line it replaces. Proof: a route loop in tests/user-sim/07-public-shell-library-palette.spec.js walking /, /discover and /create/palette — because the reason this item missed its own batch is that it LOOKS like homepage work when the footer is on every page.' },
  { id: 'surface-redesign-after-homepage', title: 'Bring the other five surfaces onto the homepage design language', priority: 'P2', effort: 'L', area: 'Product', status: 'partial', note: 'Founder batch 2026-08-20, group D. GATED — correctly — on the homepage work the founder parked: these pages must be built against the design language that wins, not the one being replaced, or they get built twice. D1 pricing page redesign to match the homepage and the pricing panel, additionally gated on the price ladder (the display side is settled — see CHANGELOG.md — the Stripe side is an owner action). D2 Learn sales page. D3 Discover sales page — this one also carries a live defect: SurfaceLanding.jsx still uses "Find systems worth stealing." as the /discover H1, the phrase the founder rejected for the homepage, so fixing only the homepage would leave it live one click away. Both change together. D4 a NEW Help & Getting Started page. D5 a NEW Design Principles page. FOUNDER DIRECTION FOR D4 AND D5, verbatim: "these are less documents are more designed like a sales page style of large visual, main point, simple not overly verbose points underneath." — — — 2026-09-07, D4 AND D5 ARE DONE AND THIS ITEM IS NO LONGER BLOCKED. The gate that held them — the homepage design language being unsettled — is closed: the hero mark, the tools intro, the compressed landings and the User Home have all landed, so both pages were built against the language that won rather than the one being replaced. D1, D2 AND D3 WERE CONFIRMED DONE BY OTHER PRs BEFORE ANY WORK STARTED, and were not redone: D1 is #388 (/plans, three false claims removed, one of them paid for), D2 and D3 are #383 (the secondary sales pages compressed to a value proposition and a link) with Learn’s content following in #380 and #403. ONE PART OF D3 IS STILL LIVE AND UNFIXED, and it is recorded here rather than quietly fixed, because it is a copy decision and this item says both halves change together: SurfaceLanding.jsx:159 still carries "Find systems worth stealing." as the /discover H1 — the phrase the founder rejected for the homepage. #383 compressed the page around it and left the sentence. Note that tests/user-sim/56-founder-rejected-headlines.spec.js pins the homepage against the rejected variant "Systems worth stealing." but its SLOTS are the three homepage headings only, so nothing in the repo can see the Discover copy. FOUNDER CALL, one line, fully reversible. D4 IS A REWRITE OF /help, NOT A NEW ROUTE. What it replaced was a three-tab document — About / FAQ / Contact — with a ten-question accordion, a three-card quick-action strip and a second copy of the feedback form, and it was carrying THREE FALSE CLAIMS, all of them found by reading the page against the registries rather than by any test: (1) the tool inventory advertised "Image Converter", "Video to Frames extractor" and "Design Reference guides" as three separate live tools — there is ONE tool, /create/file-converter, frames is a MODE inside it (FileConverter.jsx `mode === "frames"`), and the reference guides were retired in #335; this was the SEVENTH hand-kept copy of the tool list and the only one [handkept-tool-lists-remaining] never found, because it was prose. (2) "There are no third-party analytics trackers" — src/main.jsx mounts <Analytics /> from @vercel/analytics on every route. Note /privacy does not mention Vercel analytics either; that is a SEPARATE gap on a legal surface and was NOT touched here. (3) the Contact tab was a second feedback form posting to the same /api/support as /feedback with none of that form’s accessibility work — no real labels, no radiogroup, no aria-invalid, no pressed state on its type buttons — so the page now hands over to /feedback rather than keeping a worse duplicate of the product’s own reporting path. The new page is a hero, one large computed visual, four first moves and five one-sentence answers. The visual is the value a NEW PROJECT starts from, the eleven stops generateTintScale() makes out of it at the Colour Studio’s own configuration, and the files those stops leave as — read off freeFormats(), so the strip cannot advertise an export ExportPanel renders as a disabled Soon button. D5 IS A NEW ROUTE, /principles. Five rules, each beside the screen that enforces it and a proof computed on the page: contrastRatio() on the product’s own two blues (5.03:1 for --accent-strong, 3.82:1 for --accent, which is exactly why the product ships both roles), the same token’s two theme values measured on their own grounds, the type ladder from stepPx() at base 16 ratio 1.25, the export table split by its live flag, and the live/Soon tool counts off CREATE_GROUPS. THE SUB-THRESHOLD BLUE IS NOT PAINTED AS TEXT, deliberately: illustrating a 3.82:1 shortfall by rendering an unreadable line would have contradicted the page and shipped a real contrast failure, so it is drawn as the boundary its ratio does permit. ROUTE COUNT 35 → 36. /principles added to routeMetaMap.js (which is what makes a route render, prerender, get a vercel.json rewrite and enter sitemap.xml), then vercel.json and public/sitemap.xml REGENERATED with the existing sync:rewrites and sync:sitemap scripts rather than hand-edited — 38 rewrites, 36 advertised URLs — plus STATIC_INDEXABLE_ROUTES in 08-public-route-contract.spec.js. Linked from the footer’s Explore column and the visual sitemap so it is not an orphan. LEARN_GROUPS WAS DELIBERATELY NOT TOUCHED, and this is a judgement worth recording. Two roadmap rows look like these pages — `principles` ("Design Principles") and `help` ("Help & Getting Started"), both soon:true, both pointing at /learn. They are LEARN GUIDE TOPICS, not these surfaces: a guide about design principles is a different artefact from the product stating its own positions, and flipping either row would claim a guide that does not exist. It would also break tests/unit/learn-articles.test.js:207, which asserts LEARN_DELIVERED equals every non-Soon LEARN_GROUPS row — an invariant that a delivered row is a Learn ARTICLE. The `help` row pointing at /learn while /help has existed all along is a real pre-existing inaccuracy in Learn’s own data and belongs to [learn-content], not here. ONE EXISTING GUARD FIRED AND IT WAS RIGHT: tests/unit/share-cards.test.js pinned `{tab === "faq" && <FAQTab />}` to keep checkable the SECOND of the two reasons /help gets no FAQPage schema — "the answers are not in the DOM". They are now. The DECISION is unchanged (Google restricted FAQ rich results to authoritative government and health sites in 2023, always the stronger reason and now the only one), but scripts/route-schema.mjs no longer cites a fact that is false, and the test was rewritten rather than deleted: asserting the old conditional would have been asserting that a page stays a tabbed document so a comment can stay true. It now pins that the answers ARE rendered on load, that the FAQ is not behind a tab again, and — broader than before — that NO built shell claims a FAQPage; the old assertion read one file, so a FAQPage emitted onto every route would have passed on every page except the one it was written about. ALSO FOUND: the unit suite’s coverage depends on whether dist/ has been built. Eleven tests in share-cards.test.js carry `skip: !built`, so `npm run test:unit` on a fresh worktree reports 1422 passed / 11 skipped / 0 failed and never runs them. The FAQPage failure only appeared on the run AFTER a build. Not changed here, but a clean-checkout CI that does not build first is running eleven fewer assertions than it thinks. PROOF. tests/unit/help-and-principles-claims.test.js (16 tests) holds the DATA to the registries; tests/user-sim/57-help-and-principles.spec.js (12 tests) walks the RENDERED pages and compares them to the registry recomputed independently in the spec, so a correct helper beside a page that does not call it fails. ELEVEN MUTATIONS APPLIED, ELEVEN CAUGHT — and two of the first attempts were NO-OPS rather than misses, which is the trap worth writing down: changing DEFAULT_DESIGN.palette.base moved BOTH sides of the comparison (the spec imports it too), and dropping the group-level Soon branch from SOON_TOOLS changes nothing today because every tool inside the Soon `component` group also carries soon:true individually. The replacements break the CALL SITE instead — the strip hard-coding a tint config, the hero hard-coding its two figures, and a Soon GROUP whose tools are individually live — and all three go red. THREE LAYOUT DEFECTS THE SUITE COULD NOT SEE were found by opening the pages, which is the whole reason that step is not optional: `max-width:22ch` on the hero flex container resolved `ch` against the 16px BODY face rather than the 56px display face inside it, so both heroes rendered about 180px wide with the headline on six lines beside 1100px of nothing — while every overflow, visibility and reveal assertion passed. Also each start row’s arrow sat ~900px from its label, and the lead proof on /principles was stretched rather than large. All three fixed and re-measured at 1440 and 390 in both themes. MOBBIN, and the principles search was mostly a NEGATIVE finding worth keeping: Grain, Sprig, Qatalog and ElevenLabs all ship the identical manifesto section — three to six equally weighted cards, each an icon over a two-word abstraction — which is the construction anti-slop-quality-bar.md names twice ("every idea enclosed in an equally weighted rounded card", "icons or abstract shapes fill space without strengthening recognition"), and nothing on any of them is falsifiable. Cited in src/data/designPrinciples.js as what NOT to build. What was taken: Buffer’s "Here’s what you can do with Buffer" for the /help start band (mobbin.com/screens/42990c6e-5536-41b6-b2bb-5332fbe87768 — the same screen the homepage already cites, so this is the product’s established way of introducing a set rather than a new visual universe); Hashnode’s editor section for putting the proof ABOVE the claim on /principles (mobbin.com/sites/sections/ce305258-5218-49f8-8cf0-cc49059d10e4); and Figma’s FigJam index, Loom’s and OpenPhone’s help indexes for the plain-link answers register over Jasper’s and Dub’s icon-tile grids. TryItMark WAS DELIBERATELY NOT REUSED even though the founder picked it out: its own contract is that it points at the one thing in the hero a visitor can OPERATE, and it says in terms "not on the sign-up button, which is a link out rather than something you can try". Both these heroes end in a link out. A drawn phrase with nothing to point at is the decoration that component was written to avoid becoming. GATE: lint 0 errors 25 warnings (baseline), build 36 route shells, unit 1433/1433, users 649 passed 13 skipped 0 failed, all skips in 12-ui-system-builder.spec.js. WHAT REMAINS ON THIS ITEM: D3’s /discover H1 (founder call, above), D1’s Stripe side (owner action, unchanged), and /privacy not naming the Vercel analytics the site actually runs. — — — 2026-09-08, THE /privacy HALF IS CLOSED: the page now carries a "Vercel Web Analytics" heading and one sentence stating what vercel.com/docs/analytics/privacy-policy states (page views with URL, referrer, approximate location, device type, OS and browser; no cookies; nothing tied to an IP address; a request hash discarded after 24 hours). tests/unit/analytics-environment.test.js holds the sentence to the <Analytics /> mount in src/main.jsx in both directions — remove the package and the sentence must go too. Verified by mutation three ways (sentence removed, the 24-hour fact dropped, the mount removed). STILL OPEN, so this stays partial: D3’s /discover H1 (founder call) and D1’s Stripe side (owner action).' },
  { id: 'search-index-misses-live-tools', title: 'The app’s own search cannot find seven of its thirteen live tools', priority: 'P1', effort: 'S', area: 'Navigation', status: 'done', note: 'DONE (#332). The brief’s list of broken queries was INCOMPLETE IN BOTH DIRECTIONS, which is why the fixing agent reproduced it in a browser before changing anything. Confirmed failing: palette, gradient, contrast and tint all landed on /create/color (the sales page); semantic and converter returned nothing; and additionally file converter and alt text were hidden by a stale alpha flag despite being live. The inverse too — box shadow was OFFERED while still in the workshop. PillNav’s own advertised hints were already false: it suggested palette builder and contrast checker, both of which its search answered with No results. THE FIX: CREATE_GROUPS in toolTree.js is authoritative (it already drove the router, the mega-menus, route resolution and the prerender matrix, and is plain .js so node --test can import it and run real queries). LIVE_TOOLS is a .jsx map of React components Node cannot import, so it is now CHECKED rather than trusted — a test reads it from source and fails if the two disagree, replacing a comment that asked a human to keep them in sync. New src/data/toolIndex.js carries search metadata only and never writes a route down; tools.jsx is now just the icon layer. FOUR MORE DEFECTS the same drift had caused, all fixed: ten index rows pointed at retired URLs, four redirect-only category homes were offered as destinations, the alpha flag disagreed with the tree both ways, and Enter opened the first hit in registry order so tint opened the Palette Builder. Proof: tests/unit/search-index.test.js, 14 tests derived on both sides with no hand-listed tools, 12 mutations each killing it, plus one CONTROL that must still pass — SEARCH_HINTS placed in a comment only, proving the source assertion cannot be fooled by a comment. Remaining copies are tracked as [handkept-tool-lists-remaining].' },
  { id: 'palette-save-export-off-rail', title: 'Save / export is off the edge of the palette toolbar at every touch width', priority: 'P1', effort: 'S', area: 'Colour', status: 'done', note: 'Found in the 2026-09-03 multi-breakpoint audit. Measured on .plb-toolbar-group.rail-overflow, ELEVEN children, real device metrics (isMobile, hasTouch, iOS/iPadOS UA, hover:hover false and pointer:coarse true verified in-run). FULLY VISIBLE CONTROLS OF ELEVEN, BY WIDTH: 390 -> 4 visible, 1 part-cut, 6 hidden (669px of scroll). 640 -> 419px hidden. 641 -> 6 visible, 4 hidden (418px). 834 -> 3 visible, 1 part-cut, 7 hidden (691px). 1280 and 1440 -> all eleven fit, 0 hidden. THE HIDDEN CONTROLS ARE NAMED, and the last one is the point: at 390 and 834 they are Vision type, Gradient, Undo, Reset, History and SAVE / EXPORT; at 641 they are Undo, Reset, History and SAVE / EXPORT. So on a phone and on an iPad, the control that ends the journey — the only route from a finished palette to a file — is not on screen, and Undo and Reset go with it, so a mis-tap cannot be recovered without first discovering that the row scrolls. THIS IS NOT THE DEFECT #318 CLOSED and it is not the missing-affordance defect either — both were checked before writing this. The 641-980 band has its own layout now, and the peek fade IS present and correct: computed mask-image resolves to a real 34px right-edge ramp with --rail-r at 1 at every width measured. What is left is ORDERING plus a single thin affordance. The rail offers exactly ONE of the three layered affordances the standing note in global.css asks for where wrapping is off the table: the 34px fade. There is no scrollbar — offsetHeight minus clientHeight measured ZERO at every width, so scrollbar-width:thin reserves no track and an overlay scrollbar paints nothing until a scroll is already under way — and no explicit control or count. THE CHEAPEST HONEST FIX IS ORDER, NOT MORE CHROME. .plb-random already carries order:-1 below 768px to promote Randomise out of the overflow; Save / export has no equivalent and sits LAST in a rail that hides four to seven of eleven. Promote the two irreversible-consequence controls (Save / export, Undo) ahead of the fade the way Randomise already is, or lift Save / export out of the rail entirely. Sites: global.css 1830 (the .rail-overflow utility), 7908 (the min-width:961px opt-out), 7981; PaletteBuilder.jsx 2086 (the group) and 2345 (the Save / export button). FIXED 2026-09-03. MEASURED AFTER, same harness and same real device metrics, fully visible of eleven children: 390 -> 5 full, 1 part-cut, 5 hidden (was 4/1/6, 669px of scroll, now 598px). 640 -> 8 full, 1 part-cut, 2 hidden (was 6/1/4, 419px, now 348px). 641 -> 8 full, 1 part-cut, 2 hidden (was 6/1/4, 418px, now 347px). 834 -> 5 full, 1 part-cut, 5 hidden (was 3/1/7, 691px, now 608px). 1280 unchanged at 11 full, 0 hidden, 0 overflow. THE NUMBER THAT MATTERS IS NOT THE COUNT: at all four touch widths Randomise, Undo, Reset and SAVE / EXPORT are now fully inside the rail at rest, with scrollLeft 0 and no gesture, and the only controls still behind a swipe are the exploratory ones - Explore, Preview, Vision type, Gradient and History. Nothing irreversible is off-screen any more. WHICH PRECEDENT WAS FOLLOWED, AND WHY. Followed #318: extend the order promotion it established here, rather than adding chrome to the rail or restructuring it. The structural alternative - lifting the commit cluster into its own non-scrolling group - was designed, costed and REJECTED, and the reason is worth keeping: two shipped tests deliberately pin the current shape. 36-rail-overflow.spec.js asserts the action group is still a scroller at 834 (its own comment says that is the design, so that which control you get for free is a real choice rather than an accident of fitting), and 23-responsive-mid-band.spec.js asserts both groups stay on ONE row from 769 to 960. A third group would also have silently retargeted all seven .plb-toolbar-group:last-child rules onto it, which is precisely the one-shared-rule-breaks-six-surfaces failure #318 exists to record. So the rail stays a rail and only the running order changed. WHAT CHANGED: .plb-random goes from order -1 to -4 and Undo, Reset and the Save / export wrapper take -3, -2 and -1, in DOM order within the promoted set so there is exactly ONE focus-order jump boundary rather than an arbitrary shuffle - the same WCAG 2.4.3 cost shape #318 already accepted for Randomise, not a new kind. The promotion moved OUT of the two band-local blocks into the shared max-width:960px block, because the 768 block sits after that block and a band-local order would have been overridden at 768 and kept at 769-960 - the same one-band-only split that created THE MISSED BAND in the first place. UNDO AND RESET GO ICON-ONLY below 961, which is what buys the room: labelled they measure 73px and 76px, icon-only 42px, and that 65px is the difference between Save / export being fully visible at 390 and being cut. Both keep aria-label and title, so the accessible name is unchanged - this trades a visible word for a visible BUTTON. GUARDED BY tests/user-sim/36-rail-overflow.spec.js, a new A5 group: the four free controls are asserted fully inside the rail at rest at 390, 640, 641 and 834, plus an assertion that Undo, Reset and Save / export keep their accessible names while the label is hidden. VERIFIED BY MUTATION, four mutations: dropping the Save / export promotion, dropping the Undo and Reset promotions, dropping ALL four, and dropping the icon-only rule - the last of these fails three tests, which is the proof the icon-only rule is load-bearing for reachability and not cosmetic. ONE EXISTING TEST WAS TIGHTENED, NOT WEAKENED. 16-founder-batch-2.spec.js asserted every .plb-icobtn label is open and INSIDE its button below 961; a display:none label has a zero rect at the origin, which that proxy reads as outside. The defect it actually guards is a label rendered OUTSIDE its button (the old absolutely-positioned label that a scroll container clipped), so the row set is now split: every DISPLAYED label must still be open and inside, AND the hidden set must be EXACTLY Undo and Reset. That is a stricter contract than before - an accidental third icon-only label now fails - and it was mutation-checked in both directions, a third label hidden and no label hidden. WHAT WAS NOT DONE: the rail still offers only one of the three layered affordances the .rail-overflow note asks for. The 34px peek fade is live and correct at every width, but offsetHeight minus clientHeight still measures ZERO everywhere, so scrollbar-width:thin reserves no track and there is still no explicit control or count. That is unchanged by this pass and is left recorded rather than quietly dropped.' },
  { id: 'palette-swatch-actions-tablet', title: 'On an iPad the palette is covered by thirty-five unlabelled icons', priority: 'P2', effort: 'M', area: 'Colour', status: 'done', note: 'Found in the 2026-09-03 multi-breakpoint audit, and it is an EYE finding — every control is inside its own box, nothing overflows, nothing fails a target-size or contrast check, and a geometry sweep rates this surface clean. Compare the three captures before deciding it is fine. AT 834px WITH hover:none (iPad landscape, the least-tested band in this codebase) each of the five swatch columns renders its full seven-icon control stack VERTICALLY down the top of the column — drag, lock, adjust, contrast, swap, copy, remove — permanently visible, unlabelled, spread over roughly 450px of swatch height. Five columns times seven icons is THIRTY-FIVE icon buttons on screen at once, sitting on top of the colours. THE SAME CONTROLS AT THE OTHER TWO WIDTHS: at 1280 they are hidden until the swatch is hovered, so the palette is the whole screen and reads as the product. At 390 they sit in a compact HORIZONTAL row directly under the swatch name and hex, which is legible and clearly authored. 834 is the anomaly: it inherits the desktop column layout while hover:none forces the hover-revealed layer permanently visible, and a vertical column has no room to hold it. WHAT THE USER LOSES: the palette is the thing they came to look at and the thing they are judging, and its top third is unreadable behind icons whose meanings are not labelled. Persistent controls on touch are the RIGHT call — founder direction 2026-09-02 rules out hover-only affordances on touch, and that is not in question here. The defect is that the touch treatment was never designed; it is the desktop treatment with the hover guard removed. The 390 row is the pattern that already works and is the obvious place to start. MOBBIN, PLATFORM WEB, AND IT IS UNANIMOUS ON THE SHAPE. Nobody ships seven persistent unlabelled per-object icons. VEED (mobbin.com/screens/17d17ab2-7249-448d-b0e8-0abd81f8d424) puts a timeline clip’s whole seven-action set — Split, Copy, Replace Media, Download Section, Save to Brand Kit, Audio, Delete — behind ONE overflow menu. Jitter (mobbin.com/screens/64f604aa-9b44-4515-9ef6-1a1f50ca38cd) does the same with an eight-item context menu on the selected layer. Adobe Express (mobbin.com/screens/2ebf8cc8-0eb5-43e9-a823-ff210bd3d154) keeps only two or three actions inline on a small floating pill attached to the selected object and hides the rest behind a "...". Slite (mobbin.com/screens/c1dc52c1-fa38-4967-825c-8172d461ec24) is the one that keeps icons visible, and it earns that by grouping them into short labelled rows ("Layers", "Actions") of four and two — not a seven-icon column. So the two directions with real precedent are: keep the one or two actions that are used constantly inline and move the rest behind a single control per swatch, or keep them all visible but group and label them the way 390 already does. What has no precedent is the current 834 rendering. RESOLVED 2026-09-04. Scoped on the CAPABILITY, not a width: @media(min-width:769px) and (hover:none), so it cannot repeat the max-width:768px mistake that stopped being true at exactly 769px. Lock and Copy stay inline, the row moves to the BOTTOM under the name and hex (where 390 already puts it), and the other five move behind ONE overflow control. That control opens .plb-ctx, the labelled ’Colour actions’ menu that already existed for right-click — so the touch path added no second surface to keep in sync, and every action now has a WORD beside its icon, which was the actual complaint. Contrast and the two swap directions had no menu entry and were added rather than lost, which improves the right-click path too. MEASURED in a real browser with genuine hover:none emulation: 834x1194 went from 35 visible icons to 15 (three per column) with 932px of the 990px column back to clean colour; 1180x820 the same; 390x844 unchanged at 35 in its horizontal row, which is the treatment the audit called legible; 1280 still 0 at rest, so the hover reveal on pointer devices is intact. No overlap with name/hex/ramp at any of the four, no target under 24px. Direction from Mobbin, platform web: Magnific (mobbin.com/screens/130b455d-80b8-4971-8cb9-75cf5039d82d) is the closest match — a short horizontal action bar under the object with the tail behind one "..." opening a labelled menu; Google Drive (mobbin.com/screens/f20af80a-306c-498e-af04-6c73c1acd19e) makes the same inline/overflow split; Cosmos (mobbin.com/screens/13584351-6cb4-47ef-ac9c-02a717c4056e) puts a single "..." on the object itself. NO NEW ACCESS PATH: verified that a Pro-gated brand’s colours never enter the DOM at all — splitLockedLibrary strips everything but id/label/tags/slots, so the overflow menu’s Copy hex can only ever reach the viewer’s own working palette. GUARDED BY S11b in 24-mobile-overhaul.spec.js, added after review found the fix shipped with no regression test at all — reverting .plb-col-tools>* to display:flex restored the 35-icon stack with every gate still green. S11b asserts BOTH halves, because reverting either alone brings the defect back: the COUNT (only Lock, Copy and the overflow control survive) and the PLACEMENT (the row sits below the name and hex — dropping the order:2 half sends the stack back over the colour with the count still correct). The overflow control must be PRESENT rather than the others merely absent, since deleting the actions would pass a count assertion and be a worse product; and a companion test pins 390 so over-applying the collapse to phones fails too. It runs on REAL device metrics (openTouch, isMobile + hasTouch) and asserts the emulation took before trusting anything — hover:none is what makes the bug exist, and a desktop Chromium narrowed to 834px reports hover:hover and would pass on a fully reverted stylesheet.' },
  { id: 'gradient-presets-desktop-overflow', title: 'Half the gradient presets are off-screen on a 1440px desktop', priority: 'P2', effort: 'S', area: 'Gradient', status: 'done', note: 'DONE. It reproduced exactly as recorded, verified in a rendered browser before anything was changed: at 1280 AND at 1440 the strip measured 2422px inside a 1142px box, so 1280px of overflow with SEVEN of sixteen presets fully visible, and the eight hidden were Sunset, Lagoon, Grape, Citrus, Twilight, Aurora, Bloom and Ocean — the same eight this item named. 834 measured 1726px of overflow with 4 visible; 390 was unchanged from its already-fixed state. THE FIX IS THE ONE THIS ITEM ARGUED FOR. From 981px up `.ggn-presets` wraps to `repeat(auto-fill,minmax(132px,1fr))`, and the rail affordances are switched OFF on the element — overflow, snap, the scroll-driven edge animation and the mask — because `.rail-overflow` is in the markup. That is the same shape as the existing `.plb-toolbar-group.rail-overflow` opt-out at >=961px. AFTER, measured at the same widths: 1280 and 1440 both give EIGHT columns, sixteen of sixteen visible, zero overflow, mask off — two rows of eight, which is what the Mobbin references (Mural, Aave, Discord) converge on. 981 through 1279 gives six columns and three rows, still the whole library with no gesture. The card grew 113px to 211px, one extra row, into ground the page already had spare. Identical in both themes. 390 and 834 keep the rail and its tuned peek untouched, so nothing that was working was disturbed. TWO TEST CHANGES, and the first is the honest half. `27-motion-guards.spec.js` carried a "Gradient Generator preset rail (desktop)" case at 1440 whose whole premise was that this rail scrolls at every width. That premise is now false, so the case is removed and the comment above it records why; its phone case still scrolls and still carries the reduced-motion guard pair. In its place `36-rail-overflow.spec.js` gains A6, written in the same shape as A2 (/plans at 390): where wrapping is the fix rather than a better hint, the assertion is that NO gesture is needed at all — sixteen tiles, no horizontal scroll, no mask, nothing past the right edge, and more than one row. MUTATION-VERIFIED, not assumed: with the new `@media(min-width:981px)` block deleted the suite reports 4 failed / 88 passed and A6 stops on its own first assertion, "the preset library should not need a horizontal gesture at 1440px".' },
  { id: 'font-specimen-pangram-clipped', title: 'The font specimen’s pangram is sliced mid-word in the tablet band', priority: 'P2', effort: 'S', area: 'Typography', status: 'done', note: 'DONE. It reproduced, and wider than reported. Measured in a rendered browser with the real families painting — 77 distinct computed font-family values across 77 rows, so this is not the blocked-webfont artefact — at 834px the pangram box is 710px, the widest line wants 823px, and 48 of the 77 specimens clip: Comfortaa by 113px, Sora 104, Montserrat 98 (the item said 98, so the two runs agree), Poppins 91, Work Sans 89, the mildest survivors by 4. THE BAND WAS MEASURED RATHER THAN GUESSED, because the item only named 834. Sweeping 721 to 1000: 721 clips 71 families (max 218px), 760 clips 62, 800 clips 58, 834 clips 48, 860 clips 32, 900 clips 12, 920 clips 5, 940 clips ONE by 7px, and 960 (836px of box) clips none. Clean at 720 and below too, where the phone block already wraps it. So the defect is exactly 721-959px and the fix is scoped to it. WHY WRAP RATHER THAN KEEP THE SLICE. The comment above `.fg-card-pangram` defends slicing as the type-specimen convention, and that is right when the line runs off the frame. Two things make it wrong in this band: `.fg-card-preview` carries 26px of right padding, so the cut lands 26px SHORT of the row edge and reads as damage rather than as continuation; and the loss scales with the width of the face, so the specimen is least complete for exactly the widest and most distinctive families, which inverts the job of the page. The file had already made this call one band down — "WRAPPED, NOT SLICED, ON A PHONE" — so this takes the phone answer at the desktop 15px: two clamped lines in a 48px reserved box, with `.fg-card-skeleton--body` moved with it so the skeleton still stands in for the real thing. AFTER: zero clipped at 390, 640, 834, 960, 1280 and 1440. The reserved-metrics contract holds on both sides of the boundary — at 834, 900 and 959 every one of the 77 rows measures a single card height of 249px and a single pangram box of 48px; at 960 it flips cleanly to 225px and 24px. Nothing steps as faces stream in. MUTATION-VERIFIED: with the new `@media(min-width:721px) and (max-width:959px)` block deleted, the new test stops on "the specimen line must not be held on one line at 834px". It asserts the reserved height as well as the clipping, so it cannot pass vacuously if a webfont fails to paint in CI.' },
  { id: 'prefers-contrast-unimplemented', title: 'A visitor who asks their OS for more contrast gets exactly the same page', priority: 'P2', effort: 'M', area: 'Accessibility', status: 'done', note: 'DONE for the graded preference. global.css now carries an @media (prefers-contrast: more) block, placed BELOW the Foundry [data-theme] blocks — a media query adds no specificity, every selector involved is (0,1,0), so above them it would read perfectly and paint nothing, which is the trap that made the first dark hue set dead. Both halves of the test assert the ordering as well as the numbers. WHAT MOVES, worst ground per theme (#EFEEE9 light, #212327 dark), all measured on the ROUNDED hex rather than the float the solver walked — measuring before rounding overstated three values and put two of them a hair under target: --t1 6.99>9.10 / 8.44>9.00, --t2 5.53>8.03 / 5.91>8.08, --t3 4.55>7.08 / 4.78>7.06, --border 1.23>3.03 / 1.11>3.00, --bh 1.62>4.53 / 1.45>4.51. THE TEXT ROLES ARE GRADED 9/8/7 RATHER THAN ALL SOLVED TO 7, because solved flat they collapse onto a single value (#4F4F4B) in light: one value, no hierarchy, on a preference that is about legibility and not about flattening the page. Borders go to the 1.4.11 non-text floor, which they are nowhere near by default — a 1.23:1 hairline is invisible to the exact user asking for more — and --bh goes to 4.5 so hover stays distinguishable, because at 3:1 both solve to the same value. THIS NOTE’S PRESCRIPTION FOR THE ACCENT WAS HALF WRONG and the correction is worth keeping: swapping --accent for --accent-strong is right in light (3.82 to 5.03) and BACKWARDS in dark, where --accent #6FA8FF is 6.54 and --accent-strong #4A90FF is 5.04 — the swap would have LOWERED contrast for the one user who asked for more. Both tokens are pushed to a measured 7:1 per theme instead, which also lifts the fills, borders and icons --accent carries and which a swap would have left alone; the filled mirror is checked, not assumed (#FFFFFF on #094AAA is 8.17, #101012 on #7AAFFF is 8.51). NOT DONE ON PURPOSE: the --hue-* set is exactly what this preference should lift, and it has the least margin of anything in the sheet — but category-hue-contrast.test.js asserts there is exactly ONE dark block declaring --hue-colour, so a second is a deliberate change to that contract and belongs with the retune. See [hue-set-has-no-tint-margin]. PINNED BY tests/unit/prefers-contrast.test.js (numbers, ordering, and that every token moves toward more contrast rather than less) and tests/user-sim/45-prefers-contrast.spec.js, which renders the page with the browser reporting the preference and compares the resolved tokens against a no-preference control — the only way to tell a correct-but-dead block from a live one. Every rendered test first makes the page confirm via matchMedia that it really is being asked for more contrast, so the suite cannot quietly become a comparison of the default page with itself. Two mutations verified: moving the block above the Foundry blocks, and deleting it, each turn both halves red. SCOPE UNCHANGED: this is the graded preference only. Real assistive-technology testing and screen-reader announcement remain unrun and stay on [audit-coverage-not-run].' },
  { id: 'dark-theme-unreachable-but-shipped', title: 'A complete dark theme ships to every visitor and nobody can turn it on', priority: 'P1', effort: 'S', area: 'Design system', status: 'done', note: 'DONE. THE DECISION WAS TAKEN AND IT WAS TO EXPOSE, and the objection that had held it back is now answered by measurement rather than by argument. WHY vs-t-lightreset EXISTED, established before it was removed (git log -S): it was added on 2026-07-04 in the light-first rebuild (#132), whose own comment in index.html read “dark returns later as an explicit opt-in”. It was a ONE-TIME migration that cleared a stale vs-t=dark left over from the pre-rebuild design, because dark was not being maintained at that moment. Dark has now returned as an explicit opt-in, which is the exact condition the migration was waiting on, so honouring a returning visitor’s old choice is now the correct behaviour rather than a regression. RETIRED: nothing reads or writes the flag; an inert vs-t-lightreset=1 may linger in old profiles and means nothing. WHAT SHIPPED: the theme is three-state and follows the SAME contract the reduced-motion layer already used rather than a second one — vs-t ’light’/’dark’ is an explicit choice that beats the OS in BOTH directions, and ’system’ / absent / junk hands the decision to prefers-color-scheme, followed LIVE via matchMedia. html[data-theme] is still always resolved to a concrete value because neither theme’s tokens are declared on a bare :root. ONE control (ThemeChoice.jsx) in four places: the account popover, the compact popover, the mobile sheet and /settings › Accessibility. It renders the raw preference, not the resolved theme — rendering the resolved value would light up Dark for someone in System on a dark device and leave no way back to System. THE SHEET AND SETTINGS MATTER: the only control that existed lived in a popover that .pnav-more-wrap{display:none} hides below 768px, and TWO comments — that media query and the Settings › Accessibility blurb — both pointed at a control that was not where they said it was, which is how it went unnoticed for two months. NO FLASH, measured not reasoned: an addInitScript probe running before the boot script recorded html[data-theme] from document creation through hydration on nine profile/OS/route combinations — one distinct value every time, set at 6–9ms, already correct at the first requestAnimationFrame (which runs before the first paint is committed), and the body background never changed. FOUND AND FIXED WHILE THERE: the boot script had ONE try/catch around every storage read, so a blocked localStorage (Safari private browsing, block-all-cookies, a sandboxed iframe) abandoned the whole function and those visitors got NO data-theme attribute at all — an unthemed page, not a light one, because neither token set lives on :root. Each read is now guarded separately. TESTS: tests/unit/theme-resolution.test.js EXECUTES both writers (the boot script and ThemeContext) across every stored-value × OS combination and asserts they AGREE, because a disagreement is exactly the first-paint flash; tests/user-sim/41-theme-control.spec.js drives the control at 1280 and at 390 (where the popover does not exist) and asserts the first painted frame. Sixteen mutations were applied and all sixteen were caught — including one that mattered: reverting the boot script to the shipped t=’light’ left all eight of the original acceptance tests GREEN, because they read the settled attribute after hydration. The pre-paint probe was added for exactly that, and it catches it.' },
  { id: 'file-converter-flow-order', title: 'You add a file to the converter and the confirmation is below the fold', priority: 'P2', effort: 'S', area: 'Tools', status: 'done', note: 'DONE. Reproduced by actually uploading a PNG in a rendered browser at 390x844 before touching anything. Document order after the file was added: dropzone 381, Output Settings 576, "Convert 1 image" 839, and the uploaded file row 915 — past the 844px fold and 76px BELOW the button that acts on it. (This run measured the row at 915 rather than the 1172 first recorded; the ordering and the below-the-fold verdict are identical, the offset differs with what else is on the page.) The captured screenshot confirms the rest of the report: after picking a file the visible screen still reads "Drop images here or click to browse", with the count inside the Convert label the only evidence anything was accepted. THE FIX IS THE SMALLEST ONE THE ITEM ASKED FOR: the `.img-grid` block moves above the Output Settings block inside `.fc-queue`. Nothing else changed — no new markup, no new state, no scroll. AFTER, same viewport: dropzone 381, file row 576, Output Settings 893, Convert 1155. Object, then decision, then action, and the thumbnail, filename and "99 B - not converted" are on screen without scrolling. MOBBIN, PLATFORM WEB, and four more references were read on top of the three already cited, all agreeing. Whop, Uploading an image (mobbin.com/flows/80a82326-1858-49db-99c4-7a0db3ab755a) is the closest structural match — upload button, then the uploaded thumbnail with its delete control directly beneath it, then the rest of the form. Magnific (mobbin.com/flows/f1270478-c7b4-47f4-a263-3fad86a795a9) drops a placeholder row into that same slot the instant the upload starts. Sana AI (mobbin.com/flows/600fc54e-85d7-4f81-98c1-4d4361343b91 and .../82b9771e-223f-4a39-a325-ca745557d134) puts the new file at the TOP of the list with a green tick and a Ready status. Seven references, none of them confirming the object last. TESTED as 11d in `10-home-chaos-to-calm.spec.js`, which is where 11c already pins that this page must NOT scroll on upload — the two belong together, because it is precisely the no-scroll behaviour that makes whatever is on screen at rest the whole of the feedback. Asserts the document order AND that the row is in view at 390x844. MUTATION-VERIFIED: with the two blocks swapped back it stops on "the object must come before the decision". ONE THING FROM THIS ITEM LEFT UNDONE, deliberately, because it is a different change from the ordering defect: Output Format still defaults to WebP while PNG is listed first, and unlike Adobe Express the recommended option is not marked as such. Cheap, and worth doing next time this file is open.' },
  { id: 'us-spelling-in-default-locale', title: 'The hero says Colour and the search bar under it says Color, in one viewport', priority: 'P2', effort: 'S', area: 'Brand', status: 'done', note: 'DONE, and the pointer was stale A SECOND TIME — the correction of 2026-09-03 is itself wrong now, so read this paragraph before believing any file reference above. ColorStudio.jsx does still contain `t(tools.colorStudio.description)`, but on its NON-SOLO branch, and CreateTool.jsx mounts ColorStudio on exactly one route, /create/semantic-color, which always resolves to its `states` section and therefore always takes the solo branch. Verified by rendering it: /create/semantic-color paints "CREATE / COLOUR / Semantic Colours / … pending colours …", entirely British. That read is dead code. /create/color is the sales page and is British throughout too. So neither surface the correction named was painting the defect. WHAT WAS ACTUALLY ON SCREEN, found by walking the DOM for US spellings at 390, 641 and 1280 rather than by reasoning: `.hcmd-row-cat`, the category badge on the homepage command bar result rows. Type "colour" into the hero bar and FOUR rows answer with a badge reading "COLOR STUDIO" while the row title and description beside it are British — "Semantic Colour / … pending colours derived from your brand / COLOR STUDIO", "Palette / Generate a full palette from one colour … / COLOR STUDIO" — about 700px under an H1 reading "Colour, type and tokens that stay one system". The hero placeholder no longer types "Color Studio"; it types "Semantic Colour". So the item is real and the viewport is worse than described, but every file-and-line pointer it accumulated was out of date. CAUSE, and it is one expression. A row shows the tool label and description from the REGISTRY unless TOOL_I18N_MAP vouches for a translation, and the badge beside it was read straight off the locale regardless: `t(cat.labelKey) || cat.label`. The colour tools are deliberately unmapped, so their words stayed British while their badge went US. One row, two files, two spellings. THE FIX IS ABOUT WHICH FILE A ROW READS FROM, not about rewriting en-US, which legitimately holds US spellings for the strings it does translate. `categoryPillFor()` in toolIndex.js applies the rule TOOL_I18N_MAP already encodes: a row whose own words came from the locale takes a localised badge, a row still carrying the registry keeps the registry category name. Used by HomeCommandBar.jsx and by CommandPalette.jsx, which had the identical expression. AFTER: zero US spellings anywhere on the homepage at 390, 641 or 1280, badges reading "COLOUR STUDIO" beside British rows. The rule also fixes a case nobody had reported — in es and fr those same rows used to show a Spanish or French badge over untranslated English text, implying a localisation that does not exist; they are now consistently English, which is what TOOL_I18N_MAP means by "correct-and-untranslated". A mapped tool is unaffected: its badge is still localised. MUTATION-VERIFIED: the new unit test asserts against a locale that translates EVERYTHING, so the two branches are told apart by the map alone rather than by which keys happen to ship. Deleting the registry branch fails it.' },
  { id: 'reduced-motion-guard-missing-on-thirteen-blocks', title: 'Thirteen reduced-motion blocks ignore the visitor’s own motion choice', priority: 'P1', effort: 'M', area: 'Accessibility', status: 'done', note: 'Found 2026-09-03 while recounting the sites for reduced-motion-rendered-verification, and it is the inverse of the bug #273 fixed. THE CONTRACT, stated in AppearanceContext.jsx and repeated through global.css: an explicit data-reduced-motion attribute BEATS the OS query IN BOTH DIRECTIONS, so a visitor who turns motion back ON inside the app still gets motion even when their OS says reduce. Ten blocks honour that. THIRTEEN DO NOT. THE COUNT, taken mechanically by walking every @media(prefers-reduced-motion:reduce) block in global.css and testing whether its selectors carry html:not([data-reduced-motion="false"]): TWENTY-THREE blocks total, TEN guarded (101, 1336, 1871, 2508, 5040, 5206, 6270, 6609, 8056, 8852) and THIRTEEN bare (1243, 1971, 2981, 3024, 3197, 3268, 3297, 6150, 7490, 8558, 9861, 10370, 10392). A bare block reads the OS only, so the in-app toggle cannot reach it. MEASURED IN A RENDERED BROWSER on .skip-link (global.css 1971), three contexts, one number each — transition-duration: OS-reduce with no stored choice gives 1e-05s (the global clamp, correct). OS-no-preference gives 0.2s (motion plays, correct). OS-REDUCE WITH SETTINGS EXPLICITLY SET TO MOTION-ON GIVES 0s — the attribute is "false" so the global clamp correctly steps aside, and then the unguarded block kills the transition anyway. That middle number is the defect, and it is the one the contract exists to prevent. WHAT THE USER LOSES, and it is small per site but it is the whole point of the setting: someone who has told the app they want motion still gets it suppressed on the skip link, the success toast, the colour landing page (five of the thirteen are .landing-* rules), the workbench map, the palette column transitions, the UI-system cells, the library card actions, the billing banner and the quota fill. The Settings toggle silently does not work in one direction across a third of the surfaces it names. THIS IS A ONE-LINE-PER-BLOCK CHANGE and it should be made in one pass by whoever owns global.css, not piecemeal — thirteen separate fixes is how the fourteenth gets missed. The house pattern is already written at every one of the ten guarded sites; copy it. Consider also a unit test over the stylesheet text asserting that every prefers-reduced-motion block contains the guard, which is what would stop block twenty-four shipping bare. NOT THE SAME AS THE PRESENCE-ONLY PAIR at 4473-4474 and 4676-4677, which are a different wrong shape ([data-reduced-motion] with no value test, matching every visitor) and are recorded on colorstudio-dead-sections because their markup is dead. FIXED 2026-09-03. All THIRTEEN bare blocks now carry the house guard pair, so the file is 23 blocks / 23 guarded / 0 bare. THE FIX IS THE PAIR, NOT ONE LINE, and that is the part the original note under-called: every one of the ten already-guarded sites carries BOTH halves - an html[data-reduced-motion="true"] companion (at 100, 1350, 1892, 2529, 5063, 5273, 6342, 6676, 8124, 8924) AND the media-query half - because the media half alone only covers the OS direction. MEASURED IN A RENDERED BROWSER, four contexts per site, 18 fixtures across the 13 blocks, before and after. The headline site, .skip-link, transition-duration: OS-reduce with no stored choice 1e-05s before and after; OS-no-preference 0.2s before and after; OS-REDUCE WITH SETTINGS SET TO MOTION-ON 0s BEFORE, 0.2s AFTER - the filed defect, closed, and the after value now matches the motion-on control exactly. THE SECOND DIRECTION WAS ALSO BROKEN and the original note did not have it: with OS at no-preference and Settings set to REDUCE, the global clamp at line 100 forces durations and delays to near-zero but does NOT touch opacity or transform, so .landing-reveal measured opacity 0 with translateY(22px) and .lbry-card-actions measured translateY(4px) - motion NOT reduced for someone who asked for it reduced. Both now flatten to opacity 1 and transform none. That is why the companion half went on all thirteen rather than only on the blocks whose properties the clamp can reach. GUARDED BY tests/unit/reduced-motion-guard.test.js, three assertions, UNCONDITIONAL - no ratchet and no allowlist, so block twenty-four cannot ship bare. It strips CSS comments before matching, because global.css and this file are both full of the guard string in prose and a bare block could otherwise be proved guarded by the comment sitting above it. VERIFIED BY MUTATION, seven mutations, each failing the intended assertion: revert one block to bare (test 2); drop the guard from one selector of a three-selector rule (test 2); move the guard into a comment above a bare block (test 2); break the companion on .skip-link, on .quota-fill, and on one selector of the .wmap trio (test 3); and remove every reduced-motion block from the file (test 1, the anti-vacuity assertion). ONE THING FOUND AND NOT FIXED, recorded as landing-page-orphaned: five of the thirteen (3003, 3046, 3219, 3290, 3319) are .landing-* rules belonging to src/pages/Landing.jsx, which is imported by nothing and emits no chunk in dist/assets. Those five fixes are correct but latent. It is also why the measurement used fixtures injected against the real stylesheet rather than live markup: five of the thirteen have no live markup left to measure.' },
  { id: 'landing-page-orphaned', title: 'Landing.jsx ships on no route, but its CSS is still maintained', priority: 'P3', effort: 'S', area: 'Quality', status: 'done', note: 'VERIFIED AT BUILD LEVEL, AND THE VERDICT IS THE FOUNDER’S TO GIVE - which is why this is partial and not done. The orphan claim is CONFIRMED and it is stronger than this note had it. METHOD, because reasoning about imports is not proof: clean dist, npm run build, then grep the built assets for two distinctive prose markers taken from the component itself (a feature heading and a second feature heading - quoted here by description ONLY, and the reason is the trap below). Result: ZERO built JS assets contain either marker, ZERO files anywhere in dist/ contain them including the 28 prerendered route shells, and there is no Landing-*.js chunk. POSITIVE CONTROL, so the grep is not merely failing to find things: a string that IS shipped (the ErrorBoundary card’s message) is found in three chunks by the same command. THE TRAP IS REAL AND IT WAS RE-CONFIRMED HERE. src/data/pipeline.js bundles into dist/assets/Admin-*.js - checked by grepping the built asset for an id in this very file - so a note that QUOTED its markers would ship them and make its own claim false. That is why the two markers are described rather than written. Whoever re-verifies this must take fresh markers from Landing.jsx and must not paste them here. THE CSS IS NOT DEAD AND THE COST IS NOW MEASURED. 156 distinct .landing-* selectors ship in the single stylesheet every visitor loads on every route: 279 rule blocks, 34,581 bytes, 5,933 gzip, 5.0% of the whole file. Attribution: 148 of the 156 are used by src/pages/Landing.jsx and by nothing else; 5 more appear in no JS at all (dead even within the dead page); the remaining 3 are named only in backlog PROSE in this file, not in markup. So the seven class families this note estimated are really 156 selectors, and all of them belong to the orphan. THE PAGE IS A SUPERSEDED PREDECESSOR, which is the fact that should decide it: / renders src/pages/Home.jsx, a STATIC import, and Landing.jsx is the older homepage it replaced. AND THE MAINTENANCE TAX IS NOT HYPOTHETICAL - Landing.jsx was edited TODAY, 2026-09-04, by #354 (the stale brand palettes sweep), and before that by #332, #266, #257 and #253. Five sweeps have paid to reason about markup that cannot render, which is the same tax already recorded on .dsc-rail-track and on colorstudio-dead-sections. RECOMMENDATION, not a decision: DELETE the file and its 279 rule blocks together. 979 lines of JSX that no route can reach, plus 5% of the stylesheet on every page load, against a page the product replaced. If it is instead to be revived, that is a product call and it needs a route, and reviving it would also make five reduced-motion fixes non-latent. NOT DONE HERE because this note already says removing a whole page and its CSS is a product decision and a test-quality pass had no more business making it than a reduced-motion pass did. DONE 2026-09-06 — the verdict came back DELETE, and it is deleted. THE ORPHAN CLAIM WAS RE-VERIFIED FIRST, from a clean build, with FRESH markers taken from the component (three of its section headings) and, as before, DESCRIBED HERE AND NEVER QUOTED: the trap is real and was re-confirmed a third time by grepping the built assets for this row’s own id, which is found in dist/assets/Admin-*.js. Result: zero files anywhere in dist/ contained any of the three, and there was no Landing-*.js chunk. POSITIVE CONTROL, so the grep was not merely failing: two headings taken from src/pages/Home.jsx — the homepage that replaced it — are found by the identical command in one and two built files respectively. THE IMPORT SWEEP WAS THE EASY HALF and it was clean: not one file imports the module. THE HARD HALF WAS EVERYTHING ELSE, and it is where a deletion would have gone wrong. TWO UNIT TESTS READ THE FILE FROM DISK BY PATH and would have failed with ENOENT rather than an assertion — tests/unit/plan-limits.test.js (QUOTA_SURFACES, the surfaces that once quoted an AI allowance) and tests/unit/surface-vocabulary.test.js (PUBLIC_COPY, the pages banned from calling a surface a "workspace"). Both were RUN AGAINST THE DELETED FILE to prove the reference was load-bearing before touching them, and both fail exactly that way. The entry was dropped from each list and the TEST KEPT — each still covers six or seven other surfaces — with the reasoning left at both sites. ONE LIVE SELECTOR: tests/user-sim/helpers.js renderState() looked for `#main, main, .landing`; Landing.jsx was the only thing in the app that ever rendered class="landing", so that branch now matches nothing and is gone. DELIBERATELY NOT TOUCHED: tests/unit/lazy-route-readiness.test.js BANS the literal string `main, .landing, #root > *` from specs. That is a banned literal, not a live selector — a spec can still type it — so the ban stays verbatim and keeps working. FOUR PROSE SITES were repointed so nothing describes a thing that is gone: the focus-ring note in global.css (four search fields, three of which remain), the .bento orphan note in global.css (whose "imported nowhere" example WAS this page), the reduced-motion guard test header (which measured .landing-reveal), and docs/reference/design-language-v2.md (which called it dead code). THE CSS WAS REMOVED BY PARSING, NOT BY LINE RANGE, and the dry run earned its keep: the first version of the remover treated any at-rule with no nested rule as empty and reported four @font-face and two @property blocks as dropped. Fixed to touch only conditional group at-rules; final census @font-face 4→4, @property 2→2, @supports 2→2, @container 1→1. VERIFIED INDEPENDENTLY of the remover by diffing every selector prelude before and after: 290 removed, 0 added, and every removed prelude that is not itself a .landing selector is accounted for — 5 reduced-motion blocks, 14 landing-only responsive blocks, 3 landing-named @keyframes and the keyframe steps inside them. THE REDUCED-MOTION INVARIANT HELD: five of the file’s blocks were landing-only and were removed AS PAIRS, both halves, taking it 28→23 blocks, all still guarded; the unit guard’s floor is >= 20 and still clears, because it is a vacuity guard rather than a ratchet on a count. One keyframe, hero-float, was newly orphaned by the removal and went too; the four pre-existing orphans (cardIn, fx-rise, fx-pop, pop-in) were left alone as not this change’s business. Headless comments left behind by the removal were swept iteratively to a fixed point. BRACE BALANCE WAS ASSERTED BEFORE WRITING, and the writer refuses on imbalance or on losing CRLF: 7376 → 7066, balanced, CRLF preserved, zero LF-only lines. MEASURED: global.css 1,148,133 → 1,110,236 bytes (-37,897, -3.3%), gzip 294,466 → 288,647 (-5,819, -2.0%), 14,064 → 13,711 lines, 7,376 → 7,066 blocks; plus 980 lines of unreachable JSX. GATE: lint 0 errors and 25 warnings — DOWN from the 28 baseline, because the deleted file carried three of them, which is also the point: eslint runs no-unused-vars with varsIgnorePattern "^[A-Z_]" and so could never have reported the component itself as unused. Build still prints 33 route shells, unchanged, which is the check that no route was removed. 1323 unit tests pass. User-sim run twice: 605 then 606 passed, 13 skipped both times and all thirteen in 12-ui-system-builder.spec.js. Three tests failed ACROSS the two runs and NOT ONE OF THEM TWICE — 25-defect-sweep and 47-lazy-route-readiness in the first (that run also logged net::ERR_NO_BUFFER_SPACE on the exact route and viewport of the first), 10-home-chaos-to-calm in the second, which the first had passed. All three pass in isolation, and none of their subjects (.lbry-filters, /discover/prompts, /privacy, .icust-) appears anywhere in the branch diff. Recorded here rather than waved away because a lone user-sim failure that moves between runs is the socket-exhaustion signature this repo already has a note about, and the way to tell it from a real regression is that it does not reproduce and does not touch the diff.' },
  { id: 'state-token-contrast-below-aa', title: 'The --ok, --warn and --err tokens miss AA for small text in the light theme', priority: 'P1', effort: 'M', area: 'Accessibility', status: 'done', note: 'DONE (#346), but NOT as the item described - and the difference is the finding. THE PREMISE WAS MEASURED OFF A DEAD DECLARATION. --ok, --warn and --err are each declared TWICE for the light theme: once in the [data-theme="light"] block at the top of global.css (#16a34a / #ca8a04 / #dc2626) and again in the Foundry [data-theme="light"] block ~5,200 lines further down (#15803D / #A16207 / #DC2626). Both selectors are (0,1,0), so the LATER one wins and the early trio has been dead for as long as both have existed. This item was filed against the early trio - that is exactly where its 2.84 / 3.30 / 3.02 for --ok comes from. The values the page actually paints were 4.32 / 5.02 / 4.60. This is the SAME cascade trap #331 recorded, arriving from the other side: #331 wrote a fix that was dead because a later block won; this item wrote a DIAGNOSIS that was wrong for the same reason. WHAT WAS ACTUALLY BROKEN, measured in a rendered browser over 29 routes x 2 themes x 390/640/834/1280, compositing translucent ancestors: 5 distinct state-token failures, not forty. .smap-stage 4.39:1 (--ok on its own 10% tint), and .stc-cell-tone / .stc-cell-hex at 4.14 and 4.39 on the --err and --ok fills. After: ZERO, with 392 state-token text nodes measured as a positive control. THE ONE MECHANICAL THING THAT MATTERED MOST: color-mix() computes to color(srgb r g b / a) in Chromium, NOT to rgba(). A parser matching only rgba() returns null, the tint is dropped from the ground stack, and every badge then appears to sit on the bare card - a LIGHTER, kinder ground. Nearly every state badge in this codebase paints text on a tint of its own colour, so that single regex decided whether the sweep saw anything at all: .smap-stage read 5.02 and PASSED under an rgba-only parser, and 4.39 once the tint was visible. tests/user-sim/39-accent-contrast.spec.js still carries the rgba-only parser - see [accent-tint-grounds-unmeasured], filed here with the three failures it hides. WHAT SHIPPED: six new tokens, chosen by walking lightness at fixed hue and saturation until every ground (bare AND tinted 6-18%) cleared 4.5 with headroom, because #342 recorded that picking by shade index is an assumption about a ramp shape. --ok-strong #10622F (6.43 / 7.47 / 6.85 / 5.76 bare, worst tinted 4.62), --warn-strong #7B4B05 (6.34 / 7.37 / 6.75 / 5.68, worst 4.61), --err-strong #A21A1A (6.70 / 7.78 / 7.13 / 6.00, worst 4.62). 65 text rules moved. DARK WAS NOT THE SAFE THEME, which is the assumption this pass was warned about: --ok and --warn needed nothing (their -strong companions are the SAME value, kept so call sites never branch per theme), but --err #f87171 falls to 4.30 on its own 18% tint and lifts to #F87B7B. THE MIRROR CASE WAS WORSE IN DARK BY FAR. Six rules paint ink ON a state fill, and white on the dark --ok #4ade80 measures 1.74:1, on --warn 1.53:1, on --err 2.77:1 - the filled Approve and Delete buttons and the gradient queue badge. That needs an ink that FLIPS with the theme, which is why --ok-fg / --warn-fg / --err-fg exist (#FFFFFF light, #0B0D10 dark) rather than a seventh -strong: one token cannot be both lighter for text on a dark ground and darker to carry white ink. .grg-queue-status was also failing in LIGHT at 3.95 with a hard-coded #0B0D10. KEPT AT THE 3:1 FLOOR ON PURPOSE, 9 rules, all standalone glyphs and marks where the base hue is the point: .toast-success/.toast-error .toast-ico, .settings-sync-alert svg, .checkout-success-icon, .checkout-error-icon, .dsc-offline svg, .typ-notice>svg, .offline-banner svg, .fb-done-mark. All 92 background / border / box-shadow declarations keep the base token too - this is a contrast fix, not a rebrand, and the state hues stay themselves wherever they pass. --pending was left alone entirely: #342 built it to clear the bar and it does (4.77 measured on its own tint), so a fourth -strong for one rule would be over-reach. ALSO FIXED, because it was the root of the fill failure: textColorForBg() in src/utils/colors.js returned rgba(255,255,255,.9), and that last 10% is worth about 0.7:1 - white on --err is 4.83 opaque and 4.14 at 90%. It also chose its pole from a fixed luminance threshold, the same mistake #341 removed from readableInk. It now picks by measured contrast and returns an OPAQUE pole. Only ColorStudio uses it; the other caller, UIPreviewModal.jsx, is dead code. THE CONTRAST CHECKER IS NOW CONSISTENT WITH ITSELF: #323 fixed it in halves, putting dark on the tokens and leaving light as three hard-coded hexes because no light token was dark enough. That token now exists, so .cc-pass / .cc-mixed / .cc-fail and the two .cc-check-mark rules are one theme-aware declaration each and the five [data-theme="dark"] override rules are gone. Dark is unchanged in value. PINNED BY tests/unit/state-token-contrast.test.js, which RESOLVES each token the way the cascade does (last matching declaration wins) instead of grepping for a name - so the dead-block trap that produced this item cannot produce another one - plus tests/user-sim/43-state-token-contrast.spec.js, which matches ink by VALUE rather than class list, carries the color(srgb) parser, and asserts a minimum number of measured nodes so a zero cannot mean it measured nothing. Ten mutations verified, all caught, including a later block overriding --ok-strong (the dead-fix shape), --warn-strong set to --warn (bare 4.92 passes, the TINT fails), reverting textColorForBg, and blinding the parser to color(srgb).' },
  { id: 'homeworkbench-random-palette-contrast', title: 'Four in five homepage loads paint a workbench hex label below AA', priority: 'P1', effort: 'S', area: 'Accessibility', status: 'done', note: 'DONE - verified fixed by #341, confirmed by measurement in #346 rather than by reading the diff. #341 rewrote readableInk() to pick its pole by measured contrastRatio() and then walk it with fixForeground() until it clears 4.5:1, and added labelGround() so a mid-luminance chromatic fill that no ink can rescue gets its own ground instead. VERIFIED ON THIS BRANCH by driving the real generator: 80 rerolls across both themes, 400 .hw-pal-hex measurements, ZERO under 4.5:1, worst case 4.52 - against the 82% of loads and 18.3% of individual swatches this item recorded before. .hw-ui-avatar, the other label painted on a generated colour, is on the same guarantee: 80 measured, 0 under, worst 4.57. Nothing in #346 touches HomeWorkbench, so that measurement stands for main. THE FLAKE THIS ITEM BLAMED IS NOT GONE, AND IT NOW HAS A DIFFERENT CAUSE. 39-accent-contrast still fails intermittently (once in six runs here). It is no longer .hw-pal-hex: #341 added a product-card preview whose .hw-ui-crumb, .hw-ui-delta, .hw-ui-metric-label and .hw-ui-row-state paint role.muted straight from derivePreviewRoles() with no AA guarantee at all. Measured over the same 80 rerolls: 480 samples, 216 UNDER 4.5:1 (45%), worst 2.53. Filed as [homeworkbench-muted-role-text] - closing this item does not close that, and the next agent to see 39 go red should look there first.' },
  { id: 'homeworkbench-muted-role-text', title: 'The workbench product-card preview paints muted-role text below AA on 45 percent of generated palettes', priority: 'P1', effort: 'S', area: 'Accessibility', status: 'done', note: 'DONE. The residual #346 left was BIGGER AND WIDER THAN ITS ESTIMATE, and the difference is the lesson. That estimate — 2.5% of samples, 4.22 floor, muted role only — was arithmetic done against role.bg and role.surface. Measured on the RENDERED page instead, 60 rerolls per theme with the true ground composited, it is dark-only and it includes role.text: .hw-ui-app 11.67% worst 3.56 (the product NAME at 14px/700), .hw-ui-crumb and .hw-ui-delta 5.00% worst 3.95, .hw-ui-metric-label and .hw-ui-row-state 3.33% worst 4.29, .hw-ui-metric-num and .hw-ui-row-name 1.67% worst 4.26. The estimate missed role.text because it assumed the ground equalled role.surface — it assumed the thing that was wrong. APPLYING labelGround TO THE TWO CONTAINERS, WHICH IS WHAT THIS NOTE PRESCRIBED, IS NOT ENOUGH ON ITS OWN. Done per-ground it gives each ground whichever achromatic pole suits IT, and the two disagree: bg #248721 (luminance .178) and surface #3b9738 (.234) — black clears surface at 5.10 but only 4.10 on bg, white clears bg at 4.61 but only 3.70 on surface. No single ink can be right, so the card paints one label legibly and the next not; that version still failed 6.25% of dark rolls. cardGrounds() picks ONE pole for the whole card and then walks only the grounds that do not clear it, choosing the pole that needs the least movement, because every unit of movement is the preview showing a colour the palette did not generate. mutedInk’s step 3 also stopped short of a guarantee — it returned the better achromatic POLE even where that pole still failed — and now walks each pole the way step 2 walks the hue. AFTER: 200 rerolls x 2 themes, 6,800 rendered measurements, zero under 4.5, worst 4.50. Verified visually at 1280 in both themes. TEST 8C IN 10-home-chaos-to-calm.spec.js IS THE HOME FOR THIS, as this note said, and it had to be fixed in three ways before it could hold anything. It read only .hw-pal-hex and .hw-ui-avatar via the element’s OWN background, which is transparent for card text, so the wider list needed a composited ground. It ran LIGHT ONLY — the suite sets no colorScheme and every failure here is dark-only. And the obvious fix for that, stamping data-theme with setAttribute, is a VACUOUS PASS: the card’s colours are inline styles React computes from useTheme(), which setAttribute does not touch, so the tokens flipped, the page looked dark, data-theme read back dark, and every colour under test was still computed in light. Caught by mutation — that version passed against the pre-fix build. Dark is now seeded into localStorage before boot. It also read torn frames, reporting #8D2046 sitting on #948719, two colours 200 degrees of hue apart, so it now waits until every swatch label sits on its own hex. Mutation verified: the pre-fix component fails 8c on .hw-ui-app, .hw-ui-metric-label and .hw-ui-row-state; restored, three consecutive green runs. ONE THING LEFT, measured and not fixed: role.onPrimary on role.primary (.hw-ui-mark) fails about 0.6% of generated palettes at worst 4.38 in the ROLE ENGINE itself — derivePreviewRoles in utils/colors.js, shared with PaletteBuilder, so it is not a workbench fix. .hw-ui-mark itself is clean here because it was moved onto labelGround/readableInk like the avatar. Filed as [preview-onprimary-unmeasured].' },
  { id: 'accent-tint-grounds-unmeasured', title: 'The accent contrast spec cannot see a color-mix tint, and three surfaces fail behind it', priority: 'P1', effort: 'M', area: 'Accessibility', status: 'done', note: 'DONE. The parser in tests/user-sim/39-accent-contrast.spec.js now reads color(srgb ...) as well as rgba(), and the moment it could see a tint it found exactly the surfaces this note predicted, at the ratios it predicted: .smap-soon 4.32:1 light (#0b5ed7 on #d4dfec) and 4.34:1 dark (#a070f2 on #292437), .typ-notice-retry 4.04:1 light (#0b5ed7 on #d6d7d6) and 4.11:1 dark. All now pass — 4.56/4.64 and 5.84/5.57, measured rendered. .smap-soon had TWO defects, not one: its ink fell back to --accent-strong while its tint fell back to --accent, so the inline pill painted one token on a tint of another. Both read --accent-strong now, the tint drops 12% to 7%, and a 28% border restores the pill’s presence — a border sits OUTSIDE the text’s ground, so it costs nothing in luminance behind the letters. .typ-notice-retry was stacking a blue --brand-bg on .typ-notice’s amber warn tint; a solid --card surface removes the stack and is the better secondary button anyway. THE REAL FINDING IS BIGGER THAN EITHER FIX, and it is a founder call, so it is stated plainly: THE --hue-* SET HAS ALMOST NO MARGIN. On the bare grounds it was tuned against, the worst hue measures 4.60 light and 4.56 dark. A tint of ANY colour behind hue-coloured text pushes it under 4.5 — its own hue, or a neutral: a --t0 tint at just 5% drops the dark set to 3.98. So the 7% on .smap-soon is a ceiling, not a preference, and any future surface that puts hue text on any tint will fail the same way. The structural fix is a --hue-* retune with real margin, which is a design call on the token set and is pinned by tests/unit/category-hue-contrast.test.js. Filed as [hue-set-has-no-tint-margin]. Two mutations verified: restoring the 12% tint and restoring --brand-bg each turn 39 red. The parser fix needed no mutation — it turned a green spec red on unchanged CSS, which is the same evidence in the other direction.' },
  { id: 'hue-set-has-no-tint-margin', title: 'The six category hues clear AA by hundredths, so any tint behind them fails', priority: 'P1', effort: 'M', area: 'Design system', status: 'done', note: 'DONE, and the answer to the question in the title is NONE - the hue set should carry no margin at all, because it is a FILL colour. That is the finding. --hue-* paints dots, rails, icon tiles, borders and every committed OG card, and it was ALSO being asked to be an ink; a token doing two jobs is why it clears AA on its bare ground by hundredths (4.60 light, 4.56 dark) and why any tint at all takes it under. Buying margin by moving the six brand values, which is what this item proposed, would change the Create categories, the mega-menu, the sitemap, the in-tool shell and every OG card in order to fix FIVE TEXT RULES. So the margin went to a new text role instead: --hue-*-strong. Zero brand values move, public/previews/*.png does not need redrawing, share-cards.test.js and cards.json are untouched, and the OG-card blocker this item records simply does not arise. THE PATTERN IS ALREADY RATIFIED TWICE IN THIS SHEET - --accent / --accent-strong (#331) and --ok / --warn / --err -strong (#346) - and both were reached for the same reason. This is the third. Mobbin agrees and it is worth recording as evidence rather than taste: across Ditto, Gorgias, Height, Basecamp, Origin and Tana, not one paints coloured micro-text on a tint of its own colour - the colour goes in a dot, an icon tile or the ink and the ground stays neutral. Linear ships the split as a product feature, printing ’Contrast has been adjusted’ beside a user-picked status colour. THIS ITEM’S OWN PRESCRIPTION DOES NOT HOLD, measured: ’target the bare grounds at 5.5-6:1 so a 12% self-tint still clears 4.5’ - solved to 5.5 bare, the set measures 4.04 to 4.55 on that tint, still short. The -strong values are solved from the requirement inward instead: 4.5:1 on an 18% tint of the brand hue over the worst ground, UNDER THE HOVER FILM, because .smap-link-a:hover paints --hvr behind hue-coloured labels and a .smap-soon pill inside a hovered row lands on both at once. 18% is the largest tint the sheet actually paints (.hw-continue:hover), so 7, 10, 12 and 14 all sit inside it and a NEW surface can use any of them and clear AA on arrival - which is the thing this item says was impossible. VALUES, light then dark: colour #124CCA / #85A8F4, type #0C5F58 / #19BEB0, component #6316E9 / #BA98F5, imagery #8D4107 / #F68A37, ai #A31450 / #EF85B1, icons #036348 / #06BC88. Bare 6.21-6.52 light and 6.41-6.77 dark; at 12% 4.89-5.13 and 4.84-5.12; at 18% 4.51-4.72 and 4.51-4.70. MEASURED ON THE RENDERED PAGE IN BOTH THEMES, tokens read back off the cascade rather than out of the file: .smap-soon 6.71 light / 6.78 dark against 4.60 / 4.56 before, .smap-link-label on its hover film 6.64 / 6.52, .hw-chrome-here 7.21 / 7.67. The worst .smap-soon node in either theme is now the --accent-strong FALLBACK path (4.60 light, 5.09 dark) on .smap-soon--inline, which has no [data-hue] ancestor - that is #331’s tuning and it still passes. WHAT MOVED AND WHAT DID NOT: five text rules (both .smap-link-label states, .smap-soon’s ink, .hw-chrome-here, the .hw-subtab selected bullet). Fills, dots, rails, borders, outlines and icons keep --hue - 1.4.11 non-text at 3:1, and the base hue is the point of them. .home-foot-feat-title stays on --hue too: it is --fs-h2 at 700, large text at a 3:1 floor, and clears 4.5 bare anyway. .pnav-group-title was on this sheet’s roll-call of hue-coloured text and paints --t0; the comment is corrected. .smap-soon’S 7% TINT IS NO LONGER A CEILING AND STAYS AT 7% ANYWAY, which is the difference between a constraint and a decision - the ink now has room for any percentage, and the reference set above says a faint wash plus a border is the better pattern. THE DARK -strong SET IS DECLARED IN THE SAME BLOCK AS THE DARK BRAND SET, on purpose: the early [data-theme=’dark’] block is above the Foundry :root at equal specificity, so a value declared there is dead. category-hue-contrast.test.js asserts exactly one dark block declares --hue-colour and ’--hue-colour-strong’ contains that substring, so that contract needed no change and still catches the trap - verified by mutation. THIS ALSO UNBLOCKS [prefers-contrast-unimplemented], which was left unable to lift the hues: a prefers-contrast block can now push --hue-*-strong without touching a brand colour. NOT DONE HERE, deliberately, because it is that item’s scope and not this one’s. Pinned by five new tests in tests/unit/category-hue-contrast.test.js: both themes present, AA under the largest tint plus the hover film, -strong genuinely more readable than the brand value, the text rules reading the readable role, and every [data-hue] mapping carrying both. Four mutations verified, each caught by the intended test: -strong collapsed onto the brand value, a mapping that sets the fill role only, a text rule put back on the fill token, and the dark -strong set moved into the early dead block.' },
  { id: 'preview-onprimary-unmeasured', title: 'The role engine emits an onPrimary that fails AA on about one palette in 170', priority: 'P2', effort: 'M', area: 'Accessibility', status: 'done', note: 'DONE, in the engine as this note asked. derivePreviewRoles now derives onPrimary through inkOnSolid(), which picks the pole by MEASURED contrast and, where neither near-pole clears, slides it along the grey axis toward its PURE extreme by the smallest step that does. BOTH HALVES OF THE OLD CODE WERE WRONG, on the same narrow band of grounds, which is why the miss was rare enough to ship. textColorOnSolid picks its pole from a fixed luminance threshold of 0.179 - that is the crossover for PURE black and white, but the poles it RETURNS are #0A0B0D and #F2F3F5, whose crossover is 0.1749 and whose best case AT that crossover is only 4.20:1. So for any ground with relative luminance in roughly (0.16, 0.191) neither near-pole clears AA, and inside that band the threshold can also hand back the worse of the two. And the line that looked like a clamp was not one: fixForeground RETURNS ITS INPUT UNCHANGED when nothing in its walk clears the target, and it takes its direction from the GROUND (bgLum < 0.5 => walk the ink lighter), so from near-black on a mid-luminance chromatic fill it walks toward the ground and tops out short. Same flaw #341 recorded for mutedInk and #346 for the single-ground case, arriving a third time. WHY THE FIX TERMINATES, which is the part worth keeping: pure black clears 4.5 for every ground at or above luminance 0.175 and pure white for every ground at or below 0.1833, so the two OVERLAP and no sRGB colour falls between them. The worst ground in the gamut still gets 4.58:1. The smallest-step rule keeps the common case on the near-poles the previews already use, and where it moves at all it moves at most ten levels per channel. MEASURED. Before: 58 of 12,000 pairs under 4.5 over 6,000 generated palettes x 2 themes (0.48%), worst 4.378, #0A0B0D on #506EE2. After: 0 of 12,000, worst 4.500. Widened past the generator because PaletteBuilder takes arbitrary user hexes - an sRGB grid sweep at step 5 reached the primary role 170,018 times and the mid-luminance HSL band 313,588 times, 483,606 pairs in all, zero under 4.5, worst 4.5000. THE FIRST SWEEP I WROTE FOR THIS PROVED NOTHING AND LOOKED LIKE IT PROVED EVERYTHING, and the guard against that is now in the test. A ONE-COLOUR palette makes primary === bg, the bg-clash guard rejects it, and the engine returns the brand-blue fallback - so the sweep measured #3B82F6 636,056 times and reported a confident worst of 5.35. Test 2 in tests/unit/preview-roles-contrast.test.js therefore asserts how many colours actually REACHED the primary role, and mutating it back to one-colour palettes fails with ’only 0 of 48,778 colours reached the primary role’. Same family as the rgba-only parser: an answer never attempted, with nothing to show it had not been. THE RENDERED HOME FOR THIS WAS ON THE HOMEPAGE ALL ALONG AND 8C COULD NOT SEE IT. This note says .hw-ui-mark was moved onto labelGround/readableInk so the pair is guaranteed there - true, but .hw-ui-btn paints role.onPrimary on role.primary directly and was MISSING FROM 8C’S SELECTOR LIST. Adding it found a second, unrelated defect immediately: .hw-ui-btn--ghost was the one label in the card still reading role.text raw while every sibling took the mutedInk guarantee, measured 4.39:1 (#F2F3F5 on #8B6D22) in dark and 3.66:1 on another roll. One word, role.text to text. 8C IS NOT THE DETERMINISTIC GUARD FOR THE onPrimary PAIR AND SHOULD NOT BE TREATED AS ONE - it rolls 24 random palettes per theme, so a 0.48% defect is caught probabilistically, not reliably. tests/unit/preview-roles-contrast.test.js is the guard; it uses a seeded PRNG for exactly that reason. Three mutations verified there (the original engine, the measured pole choice without the pure-extreme walk at worst 4.211, and the blinded sweep) plus one on 8c for the ghost button, which was itself intermittent - it passed one run and failed the next. palette-systems.test.js turned out not to touch onPrimary at all, so the ’54-shade export assertions’ this note points at are not where it lives. The four surfaces that paint the pair are .plb-pv-cta, .plb-pvb-navcta and .plb-pvb-btn--primary via --pv-onprimary, and .hw-ui-btn. ONE THING MEASURED AND NOT FIXED, filed as [preview-muted-on-surface]: the same sweep shows role.muted on role.surface under 4.5 on essentially every generated palette, worst 2.95. That is a different pair and a much wider surface, so it is its own item rather than scope creep here.' },
  { id: 'preview-muted-on-surface', title: 'The role engine clamps muted against one ground and the previews paint it on two', priority: 'P2', effort: 'M', area: 'Accessibility', status: 'done', note: 'DONE, in the engine as this note asked, and the engine now STATES its grounds instead of assuming one: derivePreviewRoles returns inkGrounds: [bg, surface] and guarantees BOTH ink roles against every member of it. A scene that introduces a third ground adds it there and is covered on arrival. MEASURED, 6,000 generated palettes x 2 themes, before to after: muted on bg 2,314 of 12,000 under 4.5:1 worst 2.481 to 0 worst 4.500; muted on surface 12,000 of 12,000 worst 2.951 to 0 worst 4.500; text on bg 243 worst 4.211 to 0 worst 4.500; text on surface 466 worst 3.481 to 0 worst 4.500; onPrimary unchanged at 0 and 4.500. Widened past the generator because PaletteBuilder takes arbitrary hexes: an sRGB grid sweep reached the bg role on 48,778 of 48,778 palettes, zero under 4.5. RENDERED on /create/palette itself, 40 rerolls per preview theme, the same harness run against both engines: muted 600 of 840 samples under 4.5 in EACH theme (71.4%), worst 4.043 light and 3.306 dark, to 0 of 840 worst 4.501 in both. THREE CORRECTIONS TO THIS NOTE, each of which would have sent the next agent the wrong way. (1) THE .cs-pv-* FAMILY IS DEAD CSS. This note points at a dozen rules reading --pv-muted at 9-12px; ColorStudio’s preview scenes were deleted by [colorstudio-dead-sections] and nothing renders them. Confirmed in a browser on the live route: ZERO cs-pv nodes. The live surface is PaletteBuilder’s .plb-pv / .plb-pvb / .plb-pvg family and nothing else. (2) THE PREVIEW’S THEME IS NOT THE APP’S THEME. PaletteBuilder’s preview modal carries its own Light/Dark control and passes it to derivePreviewRoles as mode; data-theme never reaches these roles. Seeding localStorage vs-t, which is the CORRECT fix for the HomeWorkbench half where the colours do come from useTheme(), measures light twice and proves nothing about dark. The mirror image of the trap #346 recorded, in the same building. (3) MUTED IS NOT BEING ASKED TO BE TWO THINGS in the way the -strong pattern answers. Its only non-text uses in the live family are two placeholder bars, .plb-pv-navline and .plb-pvb-navlink, both at opacity .5. muted IS a text role by its own derivation, so it keeps its name and takes the guarantee; a second role would have been a new token minted for two decorative rules. WHAT DID NOT MOVE, which is what makes this shippable: bg never (0 of 12,000), border never (0 of 12,000), onPrimary never. border is derived from the PRE-net text on purpose, so raising the ink moves no fill and no line in either tool. surface moved on 251 of 12,000 (2.09%), primary on 88 (0.73%) and accent on 53 (0.44%), both of the last two downstream of surface - primary is chosen by a 3:1 floor ON surface, so a shortened step can hand the role to a different swatch. public/previews/*.png is untouched and share-cards.test.js did not need to run. WHY surface HAD TO MOVE AT ALL, and it is not the reason this note assumed. It is not that the step costs contrast; it is that a PAIR of grounds can defeat both poles where a single ground cannot. Pure black clears 4.5 for every ground at or above luminance 0.175 and pure white for every one at or below 0.1833, and those OVERLAP - which is exactly why [preview-onprimary-unmeasured] terminates. A pair can STRADDLE that overlap: bg #167797 with surface #2d85a1 tops out at 4.216 on its best pole, and 251 of 12,000 palettes (2.09%, dark only) are like that, worst best case 4.151. bg is the user’s colour and is shown at full size, so surface - which this engine invents - is the one ground here that is ours to give up. The step is shortened by the smallest amount that restores a common ink, never flipped and never lengthened, and it terminates because at step 0 the pair collapses to a single ground. Flipping was measured to rescue all 251 as well and was REJECTED: a dark panel darker than its canvas reads as an inset well rather than a raised card. THE FOUNDER DECISION THIS LEAVES, measured rather than described. Guaranteeing muted on the harder ground costs the muted/text HIERARCHY on saturated palettes, because on a saturated canvas the whole ink budget is spent clearing bg. muted keeps its full 45% step toward bg on 0% of palettes and a median of 34.7%, and the pairs where muted is visually indistinguishable from text (under 1.15:1 between the two) go from 3.02% to 19.10% of palettes x themes. THAT IS NOT FIXABLE BY TUNING: shrinking the dark surface step from 10% to 4% only moves the collapse from 16.27% to 12.27%, because the binding ground is bg and not surface; and a hue-preserving walk that keeps the 45% mix’s hue and moves only lightness rescues 24 of 1,895 collapses, because contrast is a luminance property and no hue at that luminance is far enough from text. THE OPTIONS ARE (a) ship as is, AA guaranteed everywhere and hierarchy carried by size and weight on the 19%; (b) move bg as well, the way cardGrounds does on the homepage, which buys the hierarchy back and shows a canvas colour the user did not choose; (c) say so in the product, the way Linear prints ’Contrast has been adjusted’ beside a user-picked colour. (a) SHIPPED. MOBBIN AGREES WITH (a) AND IT IS EVIDENCE RATHER THAN TASTE: where the canvas is a saturated user colour, Polywork and Squarespace both paint secondary copy in the SAME ink as the heading and carry the hierarchy with size and weight, while Mailchimp, GitBook and Gamma keep the canvas neutral and put the palette in blocks, images and accents. Not one of the eight references paints a low-contrast tint of the ink on a saturated ground. GitBook ships SUBTLE and BOLD as separate named tokens and Gamma a separate heading colour, which is the -strong split arrived at from the other direction. PINNED BY five new tests in tests/unit/preview-roles-contrast.test.js, asserted against role.inkGrounds rather than a ground list copied into the test, plus tests/user-sim/50-palette-preview-ink.spec.js, which seeds ONE deterministic palette - the worst muted/surface pair of 20,000 generated, chosen for being bad in both themes at once - and measures the rendered page. Test 6 uses a ONE-COLOUR palette ON PURPOSE: that is the WRONG probe for the primary role, for the reason test 2 records, and the RIGHT one for bg, which is the luminance-extreme member and would otherwise be taken by any anchor colour. Seven mutations verified: the whole pre-fix engine (caught by 5, 6, 7 and 9), inkGrounds dropping surface (5), the step never shortened (5, 6, 9, at worst 4.151), muted back to a 45% mix plus fixForeground (5, 6), muted clamped against bg alone (5, 6, at worst 3.705), the step flipped (9), and the pre-fix engine against the RENDERED spec, which fails it with this note’s own recorded pair, #144147 on #368f98 at 2.951 and 2.500 on bg. THE FIRST MUTATION HARNESS I WROTE REPORTED ALL SEVEN AS CAUGHT-NOTHING AND WAS ITSELF THE BUG: subprocess.run with an argument LIST and shell=True on Windows runs only the first word, so every mutation scored a clean pass and the tests looked toothless. Same family as the rgba-only parser - an answer never attempted, with nothing to show it had not been. FOUND AND NOT FIXED, filed as [preview-accent-ink-unmeasured]: role.accent and role.primary carry no ink guarantee at all and are painted as TEXT. GATE: lint 0 errors 30 warnings; build clean, prerender 28 route shells plus a noindex 404 shell; unit 1,111 pass 0 fail 0 skipped; users 488 passed 13 skipped 0 failed, the 13 being the pre-existing 12-ui-system-builder skips. The FIRST full users run failed 11-palette-recovery with .plb-col .plb-hex resolving to 0 elements; it passed 18 of 18 in isolation and the whole suite was green on re-run, so it is the load flake this board already records and not this change.' },
  { id: 'preview-accent-ink-unmeasured', title: 'The preview engine paints accent and primary as text with no contrast guarantee', priority: 'P2', effort: 'S', area: 'Accessibility', status: 'done', note: 'DONE. derivePreviewRoles now emits accentInk and primaryInk, guaranteed 4.5:1 on every member of inkGrounds, and NOT ONE FILL VALUE MOVES. MEASURED ACROSS THE REAL RANGE OF INPUTS, which is the thing this item asked for. Engine, 6,000 generated palettes x 2 themes: accent painted as ink misses 4.5:1 on 22,621 of 24,000 ground pairs and primary on 18,504. After: 0 of 48,000, worst 4.500. Widened past the generator the way [preview-muted-on-surface] did, because PaletteBuilder takes arbitrary hexes: an sRGB grid sweep reaches the bg role on 44,000+ palettes with zero misses. RENDERED on /create/palette: 22 of 22 accent- and primary-inked samples below 4.5, worst 1.031:1, now 22 of 22 above it, worst 4.503. THE SPEC WAS ONLY LOOKING AT A THIRD OF THE SURFACE, and that is worth recording because it is why this item existed at all. 50-palette-preview-ink swept the UI tab alone - 6 of the 17 scenes the preview modal renders - and its sweep did `if (!role) continue` on any ink it could not name, so every accent- and primary-inked node fell out BEFORE being scored. Two of the five rules (.plb-pvb-eyebrow, .plb-pvg-kicker) live on the Brand and Graphic Design tabs, which the spec never opened, so they were unreachable rather than merely unchecked. Same shape as the rgba-only parser: not a wrong answer, an answer never attempted. It now walks all three tabs in both preview themes and keeps every node, classifying the rest as \'other\'. BOTH DIRECTIONS ARE SEARCHED, which is why fixForeground is not reused. It takes its direction from the GROUND - bgLum < 0.5 means walk the ink lighter - and 0.5 is not the crossover; black and white are equally readable at relative luminance 0.179, so on every ground between them it walks away from the answer, and it returns its INPUT unchanged when the walk tops out short. That is the 6,376-of-16,200 miss this board records. Measured here: of 21,596 inks that had to move, 4,662 (21.6%) needed the pole a ground-derived rule would have refused. The walk is a LINEAR SCAN and not a bisection on purpose: contrast over a SET of grounds is not monotone along the seed-to-pole line, and a bisection assumes it is. WHY A SECOND ROLE RATHER THAN A CLAMP ON THE FIRST, which is this item\'s own prescription and it holds: accent is chosen by chroma and primary by chroma subject to a 3:1 floor on surface, and both are FILL colours - they paint the CTA, the chips, the disc and the shapes, where 1.4.11 asks 3:1 and the palette colour is the point. This is the same split ratified a fourth time after --accent/--accent-strong, the state -strong pair and the category hue -strong set. FOUND AND NOT FIXED, both surfaced by widening the sweep and both different in kind from this change, so neither was folded into it. (1) The Brand and Graphic Design scenes leave their plain p/b/span/strong copy on the APP\'s --t0 instead of --pv-text: 21 nodes per tab at 4.281:1 on the dark preview bg, passing in light. Fixing it means giving those scenes a text role, not moving an ink. (2) .plb-pvb-btn inside .plb-pvb-booking loses a specificity fight to a descendant selector, so a PRIMARY CTA\'s label is painted --pv-muted at 7px ON --pv-primary rather than --pv-onprimary at 12px - 2.446:1, and also the third-ground case the muted note predicted. Both are recorded in the spec as an EXACT-MATCH set so they fail in either direction: a new surface below its floor is not on the list, and a fixed one still is. PINNED BY six new tests in tests/unit/preview-roles-contrast.test.js plus the widened rendered spec. SIX MUTATIONS VERIFIED: the engine reverted to ink = fill turns tests 10, 11 and 13 red and reproduces exactly the 22 rendered failures; the search reduced to one ground-derived direction turns 13 red, with a message that shows the case - #c3c02c on a dark pair, where the engine takes an 8% step to #151405 and the one-directional version falls back to a generic #0A0B0D; dropping either --pv-*-ink hand-off, putting a text rule back on its fill token, and swapping a FILL to the ink token all turn 15 red. TEST 13 WAS TOOTHLESS IN ITS FIRST FORM AND THAT IS THE LESSON HERE: it asserted a STATISTIC about the shipped inks, and pvInkFromFill falls back to the shared pole walk when its own search finds nothing - and that fallback searches both directions - so the one-directional mutation kept the statistic healthy while losing the smallest-move property entirely. Comparing the exact hex against a reference walk written in the test is what made it discriminate. GATE: lint 0 errors 28 warnings; build clean, 33 route shells; unit 1,323 pass 0 fail; users 590 passed 13 skipped 0 failed.' },
  { id: 'favicon-is-os-dependent-text', title: 'The favicon is three letters in a font this project does not ship', priority: 'P1', effort: 'M', area: 'Brand', status: 'done', note: 'DONE, and the report was the visible half of a worse fault. public/favicon.svg was 491 bytes and the ONLY icon the site shipped, and it drew its mark as <text>L4B</text> in font-family Inter, system-ui, -apple-system, Segoe UI, sans-serif. Inter is not a face this project has — it self-hosts Outfit/Manrope and JetBrains Mono — so the mark fell back to whatever the visitor’s OS supplied and rendered A DIFFERENT SHAPE ON EVERY MACHINE. Three glyphs set at 248px in a 512 viewport and shown at 16px were a smudge whichever face won, and #3B82F6/#0A0A0A were neither of them tokens. Now: the founder’s OWN mark, rebuilt as geometry. No new logo was invented — the broken ring, centre dot and satellite were MEASURED off the logo standing in the corner of the homepage share card (connected-component analysis: ring centre 99.5/131.5, outer r 34, stroke 6, dot r 15.5, satellite r 7.75 on the ring line at 36.5 degrees) and rebuilt on a 32 grid in scripts/brand-mark.mjs, which is the single definition all five icons and the homepage card now draw from. ONE proportion moved and it is recorded there: the ring had to thicken to 2px at 16px or it antialiases to a grey smudge, and holding the artwork’s 0.456 dot/outer ratio as well left 0.65px of margin, so the dot gives way instead (bands at 16px are 2/2/4/2/2 device pixels, nothing thinner than a whole pixel). Colour is read from --accent, not restated. Shipped: favicon.svg, favicon.ico (16/32/48, because browsers request /favicon.ico off the root with no link telling them to), apple-touch-icon.png (180, square and opaque — iOS masks and composites it itself), icon-512.png and site.webmanifest (display browser, NOT standalone: there is no service worker, so an install prompt would promise offline the product does not have). Deliberately not shipped: the twenty-file generator dump. Two more defects were found and fixed on the way — the icon href was RELATIVE, so Vite fingerprinted it into /assets/favicon-<hash>.svg and the icon shipped twice while /favicon.ico went unanswered; and theme-color was a single light value, wrong for every dark visitor since #343, now light+dark media-scoped plus a media-less tag ThemeContext PREPENDS (a media query cannot read localStorage, so it cannot see an explicit choice that disagrees with the OS). Proof: tests/unit/brand-icons.test.js, 14 tests, all 14 verified by mutation.' },
  { id: 'homepage-share-card-orphaned-and-stale', title: 'The homepage share card is hand-made, 785 KB, and quotes a headline the site deleted', priority: 'P1', effort: 'M', area: 'Growth', status: 'done', note: 'DONE by joining #325’s system rather than maintaining a second path. The homepage card was the one hand-made orphan outside scripts/og-cards.mjs, and it had all three failure modes that predicts. WEIGHT: 785,394 bytes against 28-40 KB for the seven generated section cards, on a 1200x630 image that is flat colour and type — it was 8-bit RGBA, carrying an alpha channel a fully opaque card does not need. RECOMPRESSION WAS TESTED FIRST AND DOES NOT FIX IT: a straight opaque PNG re-export still lands at 638,143 bytes (81% of the original), because the weight is intrinsic to the artwork’s soft gradients; only leaving PNG helps (JPEG q86 is 74 KB) and that rings around the crisp black headline. STALENESS: it advertised “No more tab hoarding”, a line the homepage had already dropped — src/pages/Onboarding.jsx records its removal — so the front door’s picture promised a sentence appearing nowhere on the page it linked to, and nothing could notice, because a hand-made PNG has no relationship to the product. ORPHAN: a second maintenance path for one image. Now generated by og-cards.mjs as a distinct FRONT-DOOR composition, not an eighth section card: a six-band rail carrying every --hue-* token (the claim the homepage makes is the breadth), the rebuilt mark beside the wordmark, and the real h1 read out of Home.jsx with its --hi highlight intact, so the card and the page say the same sentence. 785,394 to 72,443 bytes, an 11x drop. og:image:width/height added — without them some platforms will not commit to the large card and fall back to a small thumbnail. Also fixed while in there: the lime highlight is an INLINE background, which paints over the font content area rather than the line box, so at the composition’s first line-height it sliced the descenders off “type” on the line above — the same mechanism this file already records against .home-mark, where #293’s line-height:1 did not fix it either. Proof: four new tests in tests/unit/share-cards.test.js, all verified by mutation, including one that fails if the hero is rewritten without redrawing the card — which is the original defect, armed.' },
  { id: 'brand-palette-provenance', title: 'Pepsi is the last unverified brand palette, and the file had no record of how any of them were checked', priority: 'P2', effort: 'S', area: 'Colour', status: 'done', note: 'TWO FOUNDER DECISIONS 2026-09-05, recorded in CHANGELOG.md, and this row owns both. (1) PEPSI IS RENAMED TO ITS ERA - option A of the three in PROPOSALS.md P-020. The palette STAYS; the name stops asserting anything about the brand as it is today. Do not spend another pass trying to verify it: every PepsiCo host blocks automated access and their own DAM serves the corporate mark rather than the cola brand, so the sources are closed and the research half of this row is FINISHED. That rename has shipped - see the DONE block at the end of this note. (2) GOOGLE BECOMES FREE - option B of PROPOSALS.md P-021, chosen AGAINST our recommendation and knowingly, having been told the free tier already held six. Set free: true on the google row; material (Material 3 Baseline) stays free beside it, so the free tier becomes seven. tests/unit/free-tier-gates.test.js and locked-library-tease.test.js cover the gate either way. Original item below. Founder request (2026-09-04), verbatim: “also check our brand palletes and ensure they are all correct google looks wrong”. AUDITED AND LARGELY FIXED in the same PR; this entry records what is LEFT and the method, because the method is the reusable part. WHAT THE FOUNDER SPOTTED WAS A NAMING BUG, NOT A COLOUR BUG. The `material` row held #6750A4 / #625B71 / #7D5260 under the name “Material (Google)”. Those hexes are CORRECT — they are Material 3’s baseline primary, secondary and tertiary, confirmed against Google’s own AndroidX source (PaletteTokens.kt Primary40/Secondary40/Tertiary40). The label was the lie: nobody reading “Google” expects purple, they expect the four-colour logo. Fixed BOTH ways — the row is now “Material 3 Baseline” (a design system, named as one) and a real `google` row carries #4285F4 / #EA4335 / #FBBC04 / #34A853 / #202124 from Google’s own Partner Marketing Hub. The same audit caught two hexes in that row that WERE wrong and that nobody had flagged: M3 maps surface to Neutral98 and on-surface to Neutral10, so #FFFBFE (Neutral99) and #1C1B1F (not the current Neutral10) were a revision behind — now #FEF7FF and #1D1B20, per ColorLightTokens.kt. SIX OTHER ROWS WERE STALE, all confirmed against the brand’s own asset: Spotify green #1DB954 was the pre-2015 value, now #1ED760 (pixel-sampled from Spotify’s own newsroom logo PNG); Airbnb #FF5A5F was the old Rausch, now #FF385C; LEGO had BOTH red and yellow wrong — #D01012/#FFCF00 appear in no LEGO publication, LEGO’s own CDN logo SVG says #E3000B/#FFED00/#181716; GitHub #24292F and #2DA44E predate the current Primer primitives (#1F2328, #1F883D); PayPal #009CDE and #0070BA are retired, paypal.com now serves #0070E0 and #001C64. THREE SUSPECTS WERE REFUTED, which matters as much — a needless change is as bad as a stale one. Shopify LOOKED rebranded (shopify.com serves #061A1C and #36F4A4 throughout) but Shopify’s own brand-assets logo SVG and favicon still ship #95BF47/#5E8E3E, so the row was left alone: the marketing site’s theme is not the brand’s logo palette. Twitch’s site serves #9147FF against the row’s #9146FF — one unit, below any perceptual threshold, and #9146FF is the published value; left alone. Discord’s blurple #5865F2 was confirmed current from Discord’s own branding page. STILL UNRESOLVED, AND THE ONE REAL GAP: `pepsi`. PepsiCo’s own newsroom confirms the 2023 identity change (electric blue plus the black of Zero Sugar), so the row’s #004B93/#E32934 is very likely pre-rebrand — but pepsico.com and pepsi.com both refuse automated fetches (403/400) and NO primary source publishes the new hexes. Every hex on offer is from a colour-aggregator site, which is exactly how the other seven stale values survived. So the values were LEFT IN PLACE rather than guessed, and this is the item: get the Pepsi hexes from a PepsiCo brand asset (a logo SVG or the brand guidelines PDF) and correct the row, or relabel it as the historical palette. Do not resolve it from an aggregator. METHOD, worth reusing: fetch the company’s OWN logo SVG and read the fills, or sample the company’s own published logo PNG pixel-wise. That is how LEGO, Spotify and Shopify were settled in minutes after brand pages (JS-rendered) and aggregators (mutually-copying) both failed. Domain-restricted search against the brand’s own host is the fallback. PROVENANCE — RECOMMENDATION, NOT DONE: a `source` + `verified` field on all 37 rows was considered and REJECTED. It is 37 rows of maintenance surface that goes stale silently, and a date nobody re-checks reads as a guarantee the file cannot keep — against the standing instruction to reduce maintenance surface. What shipped instead: one dated PROVENANCE block in the file header naming the method, the corrections and the single unverified row, plus tests/unit/brand-palette-provenance.test.js, which pins the POLICY rather than the data — eleven retired hexes may never reappear, a row named for Google may not hold Material’s purple, parenthetical “Product (Company)” names are limited to the one justified case (X (Twitter), a rename not an owner), and the FREE SET is pinned so a pricing flag cannot drift in a file edited for colour reasons. Verified by eleven mutations, all caught. ONE DECISION FOR THE FOUNDER: the new `google` row was added as free:false, so the free set is byte-for-byte the same six as before — adding a free row is a pricing call and was not mine to make. But note the side effect: renaming “Material (Google)” means the free tier no longer contains anything called Google. If Google should be one of the six free brands, say so and it is a one-word change (the test will fail until the pinned set is updated too, which is the point). NOT DONE, deliberately: no trademark disclaimers, logos or brand assets were added — colour values referenced by name are ordinary descriptive use, shipping brand artwork is a different question. If a line of attribution is ever wanted, it should be a founder decision. BRAND COLOUR IS ASSERTED IN THREE PLACES, not one — found only because correcting Spotify broke a Playwright test. (1) src/data/brandPalettes.js, the real source, fixed here. (2) ColorStudio.jsx BRANDS and DESIGN_SYSTEMS, 16 more entries carrying the SAME stale values plus a Google that is worse than the one the founder flagged — #DB4437 / #F4B400 / #0F9D58 are Google’s PRE-2015 logo colours, retired for a decade. DELIBERATELY NOT FIXED: both render sites sit in the sections [colorstudio-dead-sections] proves are unreachable (zero cs-csys nodes in a rendered browser), so correcting them would be churn in a 4000-line file that entry already schedules for deletion. Whoever does that deletion should know there is nothing worth salvaging in those 16 rows. (3) Landing.jsx DEMO_PALETTES, which IS live on the homepage and led its Spotify entry with the retired #1DB954 — fixed here to the same five values as the brandPalettes.js row. THE REAL DEFECT IS THE DUPLICATION: three copies means a correct fix in one place leaves the product still telling the old lie in another, and nothing fails. Once ColorStudio’s dead sections go, Landing should import from brandPalettes.js rather than keep a fourth copy. The Playwright spec that broke had hard-coded #1DB954 as a search TERM; it now derives the hex from the data, so a future brand correction cannot fail an unrelated test. RE-ATTEMPTED 2026-09-04 AND STILL UNRESOLVED — nothing was guessed, and the values are unchanged. The method was applied in full and the negative is now RECORDED in the brandPalettes.js header so the third attempt does not repeat the second. Blocked to all THREE fetchers tried (plain curl, the agent fetch tool, and a real headless Chromium with a browser UA): pepsico.com, design.pepsico.com, contact.pepsico.com, investor.pepsico.com, pepsico.co.uk, pepsi.co.uk, pepsico.de, pepsicobeveragefacts.com — all 403, so it is a WAF at the edge and a better user-agent will not help. pepsi.com answers 400 to curl and geo-redirects a real browser to a Facebook page, so there is no asset behind it at all. THE FIND WORTH KEEPING: digitalassets.pepsico.com, PepsiCo’s own DAM, answers 200 directly — but every asset reachable from it through archived corporate pages is the PEPSICO CORPORATE mark, not the Pepsi cola logo (and PepsiCo refreshed the corporate identity separately in 2025), so those files answer a different question. Start there next time hunting a Pepsi-brand asset id, rather than re-testing the blocked hosts. ONE COINCIDENCE NAMED ON PURPOSE: pepsicojobs.com, pepsicopartners.com and pepsicorecycling.com are all reachable and all serve #0065C3 in their site chrome, which is already one of this row’s values — that is the Shopify trap from the method above, a portal theme is not the brand’s logo palette, and a value matching an existing one does not verify it. WHAT SHIPPED: a guard that couples the claim to the data. It does NOT assert the hexes are right (nobody knows that) — it asserts that while the header says these values were left in place unverified, they must BE those values, so ’correcting’ Pepsi from an aggregator while the header still reassures the next reader is a failing test. That is exactly how the other seven stale rows survived their rebrands. Verified by 2 mutations, both caught. SECOND FOUNDER DECISION, alongside the google one above: a row we cannot substantiate is still shipping under the bare name "Pepsi". The options are keep it as-is, rename it to name the era it actually holds (note the provenance test deliberately pins the parenthetical set to X (Twitter), so "Pepsi (pre-2023)" would have to be argued for, not pasted in), or drop the row. That is a product call about what the product claims, not a data one. BOTH FOUNDER DECISIONS IMPLEMENTED 2026-09-05, and the item closes on them. (1) GOOGLE IS NOW FREE. free: false to true on the google row; the free set goes from six to seven and the pinned literal in brand-palette-provenance.test.js moved with it, deliberately, which is what pinning it was for. Note what the flag actually decides, because it is more than pricing: splitLockedLibrary ships colors for a free row and strips a gated one to id/label/slots, so this DISCLOSES Google five hexes on a public page. They are public Material and logo values, which is the reason it is a safe free-tier draw. Verified on the rendered /discover/palettes: seven brand cards, the Google card paints all five swatches, three teased placeholders remain. The 44-locked-library-tease fixture derives FREE and PAID from the data so it adapted on its own - its discriminating-fixture floor is more than 19 telltale paid hexes and it now stands at 96. 1 mutation verified. (2) PEPSI IS RENAMED TO ITS ERA: the row is now Pepsi (pre-2023) and the hexes are untouched. NOTHING WAS RE-FETCHED - the three blocked routes above were not retried, and the #0065C3 coincidence in the PepsiCo portal sites is still recorded as a coincidence, not a source. The era label is read off the VALUES and pre-2023 is the tightest claim they support: the row carries NO BLACK, and PepsiCo own newsroom (quoted above) describes the 2023 identity as electric blue plus the black of Pepsi Zero Sugar, so a five-swatch palette with no black cannot be it; the rest is the blue/red/white globe triad plus two supporting blues; and these are the values this file has deliberately never updated. A start year (2008, 2003) is NOT asserted - that would stack a second unverified claim on the first, and nothing reachable here dates the palette beginning. THE ROW IS STILL UNVERIFIED and the NOT VERIFIED header block stays, so the guard that couples the claim to the hexes still holds; what changed is that the NAME now carries the warning too, where a user can see it, instead of only a comment. No per-row source field was added: it was considered and rejected for the whole file above, and adding it to one row would contradict the rule the other 36 follow. The provenance test pinned parentheticals to X (Twitter); it is now the two justified cases, with the distinction written down - an OWNER parenthetical is the Material (Google) defect, an ERA parenthetical is what stops a row claiming to be current. A new test pins the era label and forbids black in the row, so shortening it back to Pepsi fails. 2 mutations verified, both caught. WHAT IS LEFT, and it is not blocking: if anyone ever gets a real Pepsi-brand asset out of digitalassets.pepsico.com, the guard test says exactly what to do - move the header claim with the data, and rename the row to whatever era the new hexes belong to.' },
  { id: 'focus-ring-changes-control-geometry', title: 'The focus ring reshapes the control it is drawn around', priority: 'P1', effort: 'S', area: 'Accessibility', status: 'done', note: 'DONE (this PR). Founder report 2026-09-04, verbatim: “the search bar has a weird visual, i have noticed this on many of the search bars”, with a screenshot of a blue ring whose corners did not follow the field. REPRODUCED and the cause is one line. global.css carried, near the top of the file, :focus-visible{outline:2px solid var(--accent);outline-offset:2px;border-radius:var(--radius-s)}. An outline follows its OWN element’s border-radius, so that declaration never rounded the ring — it rounded the ELEMENT, and the ring then traced the shape it had just been handed. The corners changed at the instant of focus. WHY IT LOOKED INTERMITTENT: the rule sits near the top of the stylesheet, so any later .class{border-radius} at the same (0,1,0) specificity beats it. Pill controls (.lbry-search at radius-xl, .pnav-search-field and .ui-pill at radius-pill) were therefore never touched, which is why the obvious suspects all measured clean. What it DID reach was every focusable element with no radius of its own, plus everything left to the :where() element resets at (0,0,0) after #331 lowered them. MEASURED IN A RENDERED BROWSER over ten routes, resting radius → focused radius: 21 distinct control shapes changed geometry on focus, now 0. The clearest three were input[type=search].adm-search 0px → 10px (a square field wearing a rounded ring — this is the founder’s screenshot), input[type=range] on /create/font-gallery 2px → 10px (a 2px track wearing a 10px ring), and button.pgal-stripe 0px → 10px at outline-offset:-3px, which drew a rounded ring INSIDE a hard-cornered colour swatch. The rest were a.pnav-logo on every route, a.hstep-cta, a.pgal-use, a.grg-open, a.cc-more-link, a.fg-hero-pair-link, a.app-footer-start, a.app-footer-attrib, bare inline <a>, button.plb-ramp, button.plb-add, button.plb-gap and button.hw-pal-copy. THE FIX IS THE GLOBAL RULE, NOT A PATCH PER CONTROL: the border-radius declaration is gone, so the outline traces whatever shape the control actually has. FIVE COMPONENT RULES CARRIED THE SAME MISTAKE and are corrected the same way — .cs-pillnav-item, .dsc-feat-top, .dsc-card-body, .htool-head and .fdx-panel each declared a border-radius on :focus-visible, every one of them a local workaround for the global rule. None of the five paints a background or border at rest, so their radius moved to the element at rest: visually inert, and it makes the ring correct for free. That is where a shape decision belongs. ALSO FIXED, because it is the control that was screenshotted: input[type="search"] was missing from the shared :where() field reset, so .adm-search rendered as a bare UA box with square corners and no ground. It now takes the same radius, border and --inp background as every other field. ACCESSIBILITY HELD, NOT TRADED. The ring stays 2px solid var(--accent) at 2px offset. A rendered sweep over six routes found 0 controls left without a visible focus indicator. Ring contrast against every adjacent surface, both themes: light 3.42:1 (worst, vs --bg-3) to 4.43:1; dark 6.54:1 to 7.89:1 — all clear of the 3:1 floor for a non-text indicator. FOUR SEARCH FIELDS had each independently defended against the global rule by deleting the inner input’s outline (.lbry-search input{outline:0}, .topbar-search input{outline:none}, .landing-icontool-search input:focus{outline:none}, .proj-search input:focus{outline:none}) and painting a :focus-within ring on the wrapper instead. Those stay — the wrapper is the visible field and a second ring would double up — but four local workarounds for one global fault is the evidence that the global rule was the thing to fix. GUARDED by tests/unit/focus-ring-geometry.test.js: no :focus rule anywhere in global.css may set border-radius, the global rule must still paint a ring, and search must stay in the field reset. It strips comments before scanning, because the explanatory comment now quotes the removed declaration verbatim and a raw scan would pass vacuously. Verified by five mutations, all caught. LEFT: .proj-search signals focus by border-colour alone with no ring, which is a weaker indicator than the other three; not touched here because it is a different question from the one reported.' },
  { id: 'brand-lockups-letterboxed-into-square-tiles', title: 'A third of the Brand logos pack renders as an illegible strip', priority: 'P1', effort: 'M', area: 'Create', status: 'done', note: 'DONE (this PR). Founder report 2026-09-04, verbatim: “check our rendering for alot of the icons, alot of the icons are not rendering correctly see image 2”, screenshot of /create/icons under the Brand logos filter. REPRODUCED AND MEASURED at 1440x900: logos/aerospike painted 24 x 2.1px inside its 24 x 24 slot, logos/active-campaign 24 x 2.4px, logos/500px 24 x 6px. IT IS NOT A FETCH FAILURE, which was the first hypothesis worth killing: all 120 mounted tiles loaded and every one carried real artwork. The grid was asking the Iconify API for a SQUARE render of art that is not square, then giving it a square box. TWO CAUSES, IDENTICAL SYMPTOM, and a fix for one leaves half the grid broken. Censused against the Iconify API across the four packs in ICON_GROUPS.brands, 6,773 icons: simple-icons (3,457) all square; skill-icons (400) all square; devicon (1,036) all square viewBoxes BUT 422 are -wordmark entries with a horizontal lockup drawn inside the square canvas; logos (1,880) genuinely wide — 839 off-square (45%), 625 over 2:1 (33%), worst rolldown at 11.8:1 (512 x 43.39). So (a) wide-aspect artwork in a square box AND (c) the request pinning both dimensions were true for logos, and (d) genuinely what the SVG contains was true for devicon. The founder named entries from both families in one breath — adonisjs-wordmark, aerospike-wordmark, aframe-wordmark, aiassistant-wordmark, akka-wordmark, algolia-wordmark are devicon; active-campaign, 500px are logos. THE FIX IS ONE MEASUREMENT AND ONE SLOT. The request changes from ?width=24&height=24 to ?height=48, so the element’s intrinsic aspect finally tells the truth about the art. The canvas pass that already ran on every one of these glyphs to pick the chip tone now also returns the bounding box of the opaque pixels, at a 64px raster rather than 24 (a 10:1 lockup is two rows at 24 — enough to see, not enough to measure). src/utils/glyphShape.js combines the two into one aspect that is correct for both causes: because the raster is square, drawImage stretched the art to fill it, so the measured ink box is the true one divided by the artwork’s own aspect, and multiplying it back out composes the two signals exactly. Above 2.2:1 the glyph takes .ig-chip--wide, a slot the full width of the cell and the SAME 36px height as the square chip. NOTHING IS CROPPED AND NO ROW CHANGES HEIGHT. The helper also returns a cap — how many multiples of the slot height the element may be drawn wide before the ink grows taller than the slot — and the CSS is width:min(100%,calc(36px * cap)). A two-line wordmark gets a narrower element instead of losing its top and bottom rows. Cell heights measured 115px before and after, on every row. MEASURED RESULT at 1440x900, painted glyph height: logos/active-campaign 2.4px → 10.6px, logos/500px 6px → 26.1px, logos/adobe 6.3px → 27.4px, logos/aerospike 2.1px → 9.1px, devicon wordmarks drawn across 104px instead of 24px. 24 of 120 mounted tiles take the wide slot (13 logos, 11 devicon; 0 simple-icons, 0 skill-icons, which are correctly untouched), and all 24 show 100% of their ink. TWO REGRESSIONS FOUND BY LOOKING AT THE SCREENSHOT RATHER THAN THE NUMBERS, both fixed: the first attempt cropped two-line devicon wordmarks top and bottom, which the cap now prevents; the second rendered eleven devicon tiles completely BLANK because .ig-chip computes display:flow-root rather than flex, so a 104px-tall element sat top-aligned in a 36px slot and showed its empty top margin. Absolute centring fixed it. Neither was visible in any measurement I had taken. MOBBIN, and it changed the answer. Searched for how asset libraries present wide logotypes beside square marks. Squarespace’s Asset Library gives a wide logotype a double-width cell in an otherwise uniform grid; Air lays assets out at a fixed row height with each cell’s WIDTH following its aspect; Krea AI does the same; Google Ads’ “choose one logo” picker keeps a uniform landscape thumbnail, fits the art inside at its natural aspect and labels the aspect on the card; Arcade’s Brand Kit goes furthest and splits “Logotypes” from “Icons” into two sections with different cell shapes. Not one of them letterboxes a lockup into a square. This ships the Google Ads answer — uniform slot, art at natural aspect — because it is the only one of the four that transplants into a dense icon grid WITHOUT re-ordering it. A column span was the obvious alternative and was rejected: it needs grid-auto-flow:dense to avoid holes, and #298 already refused visual re-ordering on this codebase’s grids on WCAG 2.4.3 grounds. GUARDED by tests/unit/glyph-shape.test.js, six tests over the pure geometry, including one asserting the ~4x gain and one asserting the cap can never crop. Verified by seven mutations, all caught — including the two that regress exactly one of the two causes, which is the shape of failure this fix exists to prevent. LEFT, and worth a look one day: simple-icons ships its own wordmark-shaped glyphs (abbott, acer, accenture, acm, aboutdotme) on a square 24 x 24 monochrome canvas. They are small but legible and they are that pack’s own design decision, so they are out of scope here; they would need the same ink measurement extended past COLORED_PACKS. Flags (flag, flagpack, cif) are 50-100% off-square at a mild 1.33:1 and stay on the square chip deliberately — a 24 x 18 flag reads fine.' },
  { id: 'library-toolbar-height-follows-chip-count', title: 'Switching between the Icon and Emoji tabs moves the page 145px', priority: 'P1', effort: 'M', area: 'Create', status: 'done', note: 'DONE (this PR). Founder report 2026-09-04, verbatim: “the header design changes height switching from the emoji to icon library. the bar is broken visually on a standard desktop”. REPRODUCED at 1440x900: /create/icons rendered a 68px toolbar and /create/emoji a 213px one, so switching tabs moved the page by 145px. All three routes — /create/icons, /create/emoji, /create/icons-emoji — mount IconEmojiLibrary, which keeps both libraries alive and toggles hidden, so this is one page moving under the cursor. Confirmed by loading the routes: the .sec-h block in IconLibrary.jsx is behind !embedded and never renders on any shipped route, exactly as the brief warned. THE CAUSE IS A TRAY THAT CANNOT FIT ITS ROW, NOT A BREAKPOINT. #318 met this same shape and answered it with a media query because 641-980 was the band with no layout of its own, and its reasoning was right: a search field with flex:1 1 240px and no ceiling takes the whole row and pushes every filter group onto a line of its own, and margin-left:auto then strands the action hard right and alone. But that is not a fact about 641-980. It is a fact about a row a tray cannot share. Measured intrinsic tray widths: the emoji category tray wants 1478px (twelve categories, each with a glyph, a label AND a count) and the icon group tray wants 493px. So on the emoji side the row broke into three lines at every desktop width — a 1321px search field alone on line one, two wrapped chip rows on line two, the skin-tone control orphaned on line three — while five other surfaces looked fine and nobody saw it. The emoji tray needs 1928px of row before it fits, which is roughly a 2050px viewport, so no wider breakpoint was ever going to rescue it. CHOSEN: RESTRUCTURE SO THE CHIP COUNT CANNOT CHANGE THE HEIGHT, not reserve a height. LibraryFilterGroup already ships a complete, accessible collapsed form — the labelled trigger plus menu #318 built for tablets, with the same option renderer, the same multi-select gesture and the usePopover dismissal contract. The collapse condition is now MEASURED rather than assumed: collapse when the tray is wider than the row can give it. The band stays as a floor, because below 981px the reason is touch room rather than fit. WHY IT CANNOT OSCILLATE, which is the real hazard in a measure-then-change-layout rule. Both inputs are chosen so that collapsing cannot move them: the tray’s intrinsic width is a property of the OPTIONS (measured once with flex-wrap:nowrap and cached, so it survives the tray leaving the DOM), and the budget is the row width less the search field’s CEILING and the action’s width — never the search field’s or the filter container’s measured width, which are the two that do move. Asking “would the tray fit if the search field were at its cap?” is also the honest question, since an uncapped field eats the row and then every tray looks like it fits on a line of its own. Verified by sweeping 1200 and 2100px five times: settles at 68px every time, no flap. A GRID WAS TRIED AND REVERTED, recorded so nobody spends the afternoon on it again. Three explicit tracks (minmax(0,360px) minmax(0,1fr) auto) do make the row deterministic, but a fixed first track cannot give width back the way flex:1 1 240px does, and flex was quietly relying on that: measured on the branch, the Prompt Library at 1024px went 68 → 124px and the Icon Library at 834px went 68 → 120px, because the search field held 360px the filter row needed. Only two lines of CSS ship: the 360px search cap extends from the 641-980 band to every width from 641 up, which is also what makes the budget honest. MEASURED RESULT, toolbar height, before → after, all six LibraryToolbar surfaces at 390 / 834 / 1024 / 1440: Palette 215/68/68/68 unchanged; Gradient 217/68/122/68 unchanged; Font Gallery 297/135/135/135 unchanged; Prompt 217/68/68/68 unchanged; Icon 261/68/118/68 → 261/68/68/68; Emoji 373/68/213/213 → 373/68/68/68. Zero regressions, two improvements, no element overflowing its toolbar at any width. The founder’s gesture, clicking between the two tabs at 1440: 68px → 68px → 68px, jump 0, down from 145. THE TRADE, stated plainly because it is a real one: at desktop widths the Emoji library’s twelve category chips now sit behind a “Category · All” trigger instead of being visible in the bar. That is the pattern #318 already researched and shipped for tablets (Magnific, Care.com, Deel, Vanta all reflow filter bars this way, and the trigger states its group AND its current selection so the state is not hidden), and it is the only option that makes the height independent of the chip count at a width anyone actually has. All twelve options remain in the menu with the group’s accessible name intact — verified by opening it. If the founder would rather see the chips than have a stable bar, the alternative is to shorten what each chip carries (dropping the counts takes the tray from 1478px to about 1185px, still short of the 871px available), not to widen the breakpoint. GUARDED by tests/unit/filter-fit.test.js over src/components/library/filterFit.js: the two headline cases, the 1928px figure, the monotonicity of the decision in row width, and the anti-oscillation property that the budget uses the cap and not a measurement. Verified by six mutations, all caught, including the one that restores the pre-fix behaviour. LEFT: the Icon Library is 118px at 700px wide, two rows, because a 220px pack select plus a filter trigger plus an Add icon button cannot share a 660px row with a 360px search field. That is unchanged from before this PR (118px on both sides) and is a different question — it wants the select to shrink or the action to move into the hero, the way #323 moved the Prompt Library’s CTA. The Gradient Gallery is 122px at 1024, also unchanged and also pre-existing.' },
  { id: 'home-mini-tools-fidelity', title: 'The homepage mini tools shared zero classes with the tools they open', priority: 'P1', effort: 'L', area: 'Home', status: 'done', note: 'DONE (this PR). Founder report 2026-09-05, verbatim: “on the homepage the mini tools can be better representations of the actual sections, visually and via user experience”, narrowed by him to “they don’t look like the real tool”. THE MEASUREMENT THAT MADE IT CONCRETE: the five mini tools shared ZERO class names with the tools they hand off to — PaletteBuilder has 153 `plb-` classes and the homepage reused none, FileConverter 22 `fc-` and none, the icon surfaces 32 `ig-` plus 53 `icust-` and none. Instead the workbench carried 118 bespoke `hw-` classes: a parallel visual system invented from scratch, which is why the complaint could not be answered by nudging colours. Two inversions made it worse than a style mismatch. Palette led with a mocked “Acme” product card and filed the palette itself under CONTROLS as a 96px strip — but in Palette Builder the BOARD is the screen and that card’s equivalent is behind a Preview button, in a modal, with its later scenes gated to Pro. Image had no drop zone at all, which is the single most recognisable thing about File Converter. WHAT SHIPPED, panel by panel. Palette renders `.plb-board` / `.plb-col` / `.plb-ramp` / `.plb-name` / `.plb-hex` / `.plb-role` through a shared `colRef`/`barRef` contract (new utils/paletteBoard.js, imported by both surfaces) with `colorName()`, `roleLabel()` and `tonalRamp()` — the same functions the tool calls. Gradient renders `.ggn-preview` with the tool’s type/angle pills, `.ggn-stop` rows, `.ggn-field`/`.ggn-label` inspector grammar, the `.ggn-dial` angle dial (same atan2 maths as `dragDial`) and `.ggn-css`/`.ggn-copy`; `.ggn` was split into a token-alias rule (`.ggn, .hw-ggn`) and a page-layout rule so this needed no duplication. Icon renders the editor’s `.icust-stage` driven by the SAME --ig-size / --ig-stroke properties IconCustomizer writes, `.icust-meta`/`.icust-tag` chips, `.icust-row`/`.icust-seg` settings, and the library’s `.ig`/`.ic` grid — which restores the icon NAME under each glyph, the string you search for and copy. Image renders `.fc-tabs`/`.fc-tab` and puts the reference photo inside a real `.img-drop-zone.fc-drop`, with drag-and-drop wired for real. Typography renders `.tsc-row`’s gutter with the TOKEN NAME (`--text-2xl`) and px·rem; `stepName()` moved to utils/fluidType.js so the preview and the export cannot disagree about what a step is called. Deleted with them: the whole `.hw-pal*` block, `.hw-grad-preview`, `.hw-stop`, `.hw-input-hex`, `.hw-fields`, `.hw-field`, `.hw-label`, `.hw-label-space`, `.hw-field-err`, `.hw-out`, `.hw-code`, `.hw-prop-range`, `.hw-subtabs`/`.hw-subtab`, `.hw-icon-stage`/`-preview`/`-meta`/`-grid`/`-cell`, `.hw-type-meta`. HONESTY WAS THE BINDING CONSTRAINT, because a higher-fidelity mock is exactly where capability creep happens. A reused class must not import a capability: the board renders ONLY the lock (not grip, HCT, contrast or More), its ramp is an inert aria-hidden div rather than the tool’s tints button, the gradient inspector omits the stop rail, the position/lock/remove controls and the CSS/Tailwind/SVG tablist, and the type gutter drops `.tsc-row-sub` because its three facts (second breakpoint, weight, line height) are all things this panel neither shows nor hands off. The drop zone is the case that went the other way: a control that LOOKS like a drop target and silently swallows a drop is the lie this whole change exists to stop, so drag-and-drop is wired through the same `handOff` path as the picker. “Nothing here is saved” moved onto the Generate note, at the point of use. A REAL CONTRAST DEFECT FELL OUT OF IT. Board labels sit on the raw generated fill with no chip underneath, and the new sweep found 6,376 of 16,200 generated colours under 4.5:1, worst 4.36. `readableInk` picked the better of #141414/#FFFFFF and walked it with `fixForeground`, which searches in ONE direction chosen from the background’s luminance — so on a mid-luminance fill it asked for a lighter ink, started at white and returned white unchanged. The pole that lost would have cleared it: pure black on #D1491F is 4.688 and was never tried, because #141414 is not black. #000000 is now an explicit candidate. Over the full 16,200: zero under AA, worst 4.50, and `labelGround` now moves ZERO grounds (was 3.5%) — one absolute pole clears 4.579:1 against every colour in sRGB, so the swatch a visitor reads the colour from is never repainted, which is what labelGround’s own doc comment always wished for. FOLLOW-UP, filed not done: labelGround is now provably an identity for every input, so its remaining callers pay for a no-op. Removing it touches PaletteStage’s render path and cardGrounds/mutedInk, which solve a different problem. THE 1280×660 CONTROL-ZONE DEFECT [workbench-control-zone-collapses] WAS NOT FIXED AND WAS NOT WORSENED — it improved in all five modes as a side effect of the right structure, because the board replaced a 353px mock plus a 96px strip. Palette 0px⇒ 28.6px, image 2.2⇒ 19.0, typography 36.6⇒ 58.3, gradient 144.6 unchanged, icon 107.9 unchanged. Two of those needed defending mid-change: `.fc-tabs` carries a 24px page margin that took image to 0 (dropped, and the reference cap went 34vh⇒ 31vh, which that rule’s own note sanctions), and the type gutter first grew the stage 79px before `.tsc-row-sub` came out. TWO DEFECTS CAUGHT BY LOOKING AT SCREENSHOTS, NOT BY THE SUITE, both invisible to a green build. Giving the specimen `.tsc-row-text` — a rule driven entirely by page-scoped --tsc-* variables — made EVERY STEP RENDER AT THE SAME SIZE: a type-scale preview showing no scale, with four correct numbers in the gutter beside it. And `.hw-type-row .tsc-row-meta{flex-basis:116px}` becomes a 116px HEIGHT once the shared ≤768 rule turns the row into a column, opening a chasm between each step and its own specimen; the generator already resets it and was losing at (0,1,0) to (0,2,0). MOBBIN drove the shape: monday.com’s “Try it out” section (sites/sections/cb5efa4f-8757-45c7-a26f-ffadd272a4fb) embeds the literal board UI with one coaching callout and restyles nothing; Framer (sites/sections/9caf7169-91a3-419e-a787-6f61843eeda0) puts its actual canvas on the page with real route names and no marketing chrome inside the frame; MagicPath’s design-system panel (screens/e7a8b966-6fad-474a-ac17-53efda7268e8) is why each column carries three lines rather than one — role name, value, and what the role is for. TESTS: eleven selectors re-pointed with every assertion preserved; 8c’s “settled” check got STRICTER (exact --plb-c vs printed hex, replacing a ±24-per-channel tolerance); the icon size check moved from an SVG width attribute to the RENDERED box, so it proves the CSS consumed the property rather than that it was written; two labelGround assertions inverted with the arithmetic recorded; four new assertions added (the board ink sweep, the dial reporting the declaration’s angle, the drop zone being live, and its two honesty strings), all verified by mutation — five mutations run, five caught. Gate: lint 0 errors / 30 warnings, build 28 route shells, unit 1115/1115, users 488 passed 0 failed 13 expected skips. Screenshots before and after at 1440×900, 1280×660, 900×1000 and 390×844, light and dark. ALSO IN THIS PR, same surface: the workbench section lede was DELETED rather than rewritten a third time. Founder on the previous copy: “MEGA AI generated this is horrible copy”. It had already failed once in a different register (“Not a screenshot. The actual tools, running here.”), so the diagnosis is the move rather than the wording: both spent every word litigating one question — is this real? — that prose cannot settle and the reader had not asked. The heading stays because `aria-labelledby` points at it and because it is the one thing the panels cannot show themselves; it now reads “Five tools. Each one hands your work to the full tool.” The lede’s three honesty claims are all still made at the point of use, where they are checkable. LEFT, MEASURED AND NOT FIXED: `.plb-name` (opacity .95) and `.plb-role` (opacity .6) carry their opacity from the SHARED component, so a strict AA gate on them would fail the build on a property of Palette Builder that has always shipped; 8c measures `.plb-hex` only, and this is recorded rather than quietly dropped.' },
  { id: 'ffmpeg-core-off-origin', title: 'The converter shipped a 32 MB wasm from our own origin and spent the Vercel transfer allowance', priority: 'P1', effort: 'S', area: 'Performance', status: 'done', note: 'DONE 2026-09-06, and the trigger was a bill rather than a measurement: Vercel notified the founder that the project had hit its 10 GB limit.  On the Hobby plan the only 10 GB/month limit is FAST ORIGIN TRANSFER — bytes served from the origin rather than from an edge cache — so the question was which bytes.  THE ANSWER WAS ONE FILE. dist/assets was 36,045,200 bytes and 32,129,114 of them were ffmpeg-core-[hash].wasm, the video converter engine, imported by src/pages/FileConverter.jsx through Vite so the bundler emitted it as a content-hashed static asset.  91% of everything we deploy, in one file — and the content hash is the mechanism that makes it expensive: every deploy renames it, so the warm copy in every edge region is discarded and the next visitor in each region pulls the whole file from origin again. What one miss costs depends on whether the edge compresses wasm, which is not verifiable from here because production is not reachable from the sandbox — the measurable bracket is 32,129,114 bytes uncompressed and 9,260,281 compressed, both read in Chromium against jsDelivr, so somewhere between about 300 and 1,100 region-first-hits spends the entire monthly allowance. The bracket does not change the decision: after this the number is zero.  IT WAS NEVER ON THE FIRST WAVE, and that was checked rather than assumed. homepage-field-metrics measured no FFmpeg JS or WASM on the homepage in 10 of 10 cold runs, and the url import yields a string rather than a fetch, so a visitor who never pressed Convert never paid a byte for it. The cost was per-deploy-per-region, not per-pageview, which is why nobody saw it in a page metric.  FIXED BY LOADING THE CORE FROM jsDelivr AT CONVERT TIME, pinned to the exact version 0.12.6 — the same bytes the npm package ships, from a host that already serves them with immutable caching and a wildcard CORS header. Zero origin bytes. dist/assets is now 3,801,737, measured by building the same tree twice with and without the change.  A SECOND CDN AS A FALLBACK WAS CONSIDERED AND REJECTED: a mirror is a code path that only executes during another host outage, plus a second pinned version to keep in step, and the honest failure is already built — getFfmpeg throws, engineState goes to error, and the UI says the engine failed and that the Image tab still works. Image conversion is pure canvas and needs none of this.  A STATIC BUCKET WITH A STABLE URL WAS ALSO REJECTED: it needs infrastructure the founder must set up, and five owner actions are already blocking engineering. This one needed nothing from him.  NO HEADER WORK WAS NEEDED, and that was verified in a browser rather than read: the core is the single-threaded build and not core-mt, so there is no SharedArrayBuffer and no COOP/COEP cross-origin-isolation requirement, and vercel.json declares no CSP to widen.  GUARDED IN TWO PLACES, every assertion mutation-verified. tests/unit/ffmpeg-core-off-origin.test.js fails if anything under src/ imports the core again, if the pinned CDN version and the installed package disagree, if any wasm appears in dist/assets, or if dist/assets passes 8 MB — with a positive control that the scan can see the entry chunk, the stylesheet and the FileConverter chunk, because every one of those assertions is trivially true against an empty list.  tests/user-sim/57-converter-engine-cdn.spec.js is the wiring half: it records a real WebM in the page with MediaRecorder, converts it to a GIF at 390 and 1280 with the pinned CDN URLs fulfilled from node_modules, asserts the resulting img decoded to real pixels from a blob, asserts the engine is fetched only after Convert is pressed, and asserts the page never asks our own origin for a core. Pointing the base URL one directory sideways turns both conversion tests red.  TWO THINGS FOUND ALONG THE WAY. The loader comment claimed it was shared by Video to GIF and Video to Frames; Frames seeks a video element and paints each frame to a canvas and has never touched ffmpeg, so only one mode of four ever paid the download — the comment is corrected.  And the two remaining heavyweights are now the largest things we ship and neither was touched here: they are filed as admin-chunk-ships-the-ops-notes and one-stylesheet-for-every-route.  THE FIX CANNOT REACH PRODUCTION UNTIL THE DEPLOY BLOCK CLEARS — see the deploy row above and OWNER-ACTIONS section 1.2. The live site still serves the 2 September build, so the origin transfer keeps accruing on every deploy that does happen until this one lands.' },
  { id: 'admin-chunk-ships-the-ops-notes', title: 'The Admin chunk is 693 KB, and most of it is this file', priority: 'P2', effort: 'M', area: 'Performance', status: 'done', note: 'MEASURED 2026-09-06 while removing the ffmpeg core, and filed rather than fixed because the fix is a design decision about where ops notes live, not a one-line change.  dist/assets/Admin-[hash].js is 693,340 bytes raw, 257 KB gzip — the largest chunk we ship. src/data/pipeline.js, which Admin.jsx imports for the Pipeline tab, is 588,666 bytes of source on its own, and almost all of that is prose: the long DONE and MEASURED notes on each row, this one included. src/pages/Admin.jsx is a further 139,507.  So the Admin panel downloads the whole engineering history of the project as JavaScript in order to render a table of it, and the file grows by a few KB every time an agent records what it did.  WHY IT IS P2 AND NOT P1. The chunk is lazy and admin-only: no ordinary visitor fetches it, it is not in the first wave measured on homepage-field-metrics, and one admin on a fast connection is not a user-experience problem. It is a byte-mass and origin-transfer problem that will keep growing on its own.  WHAT WOULD ACTUALLY FIX IT, in rough order of how much it changes: fetch the notes as JSON instead of importing them, so they are data rather than code and can be cached separately; or split the notes out of the row objects entirely so the table renders from a small index and loads a note when it is opened. Either way the file stays the single source the tab renders from — that property is the reason it exists.  NOT ATTEMPTED HERE. Nobody has measured how much of the 693 KB is prose versus code, and that measurement should come first. CLOSED 2026-09-07 AS A DUPLICATE of [admin-chunk-is-the-backlog], which is the survivor and carries the fix, the measurements and the guards. Same finding, filed a day apart by #406 and #405 from the same measurement of the same chunk. This row is kept rather than deleted, and its text above is kept intact, for two reasons: the board should show that the same problem was found twice independently, and the WHY IT IS P2 AND NOT P1 argument above is the better-argued half of the pair and is quoted into the survivor. The one open question it left - "nobody has measured how much of the 693 KB is prose versus code, and that measurement should come first" - was the right instinct and is now answered: 84% of the chunk was this file. See the survivor for the numbers, the dynamic-import fix, the graph guard that replaced a first attempt which passed while the bytes were still on a static edge, and the browser test.' },
  { id: 'one-stylesheet-for-every-route', title: 'Every visitor downloads all 686 KB of CSS to render one route', priority: 'P2', effort: 'M', area: 'Performance', status: 'done', note: 'DONE 2026-09-08 (perf/stylesheet-dedupe-and-split). Eleven single-page selector families lifted into eleven per-page stylesheets, each imported by the lazy page module that owns it so Vite emits it as that route\'s chunk CSS and links it with the chunk. MOVED: tt->TintTool, fpr->FontMatcher, stc->ColorStudio, lart->LearnArticle, seo->SeoInspector, uh+proj->Projects, settings->Settings, plans->Plans, cc->ContrastChecker, bs->BrandStarter, alt->AltTextGenerator. OWNERSHIP NEEDED TWO SIGNALS TO AGREE: the class is named only by modules inside one chunk\'s static closure, AND a browser opening all 36 routes counted elements matching that family on that route only. WHAT STAYED IS THE HALF THAT MATTERS. plb (50 KB), ggn (19 KB) and tsc (15 KB) are rendered ON THE HOMEPAGE by HomeWorkbench, which is the real tool markup used as the hero demo - the homepage paints 101 .plb-* elements. A static reading alone would have moved ggn and tsc off the page that renders them; the browser caught it. fg (21 KB) is named by ten chunks, lbry (12 KB) paints on five routes, pnav (24 KB) is the shell. cs (51 KB) and uis (27 KB) are not owned, they are ORPHANED: ColorStudio names 16 of cs\'s 284 classes and nothing names the rest, and /create/ui is not in LIVE_TOOLS so uis paints on no route at all. Those two, 78 KB, are the biggest remaining opportunity and belong to a dead-code pass, not a split. adm (15 KB) is left out on purpose: /admin is behind auth so the browser could not render it, and its ownership cannot be verified to the same standard as the rest. THE CASCADE-ORDER TRAP, because the next person will hit it: a lifted rule lands in a stylesheet that loads AFTER global.css, so it starts winning every equal-specificity tie it used to lose on document order. .tt-grid moved into tint.css and began beating the shared \'workbench two-column threshold\' block, so Tint\'s working column got NARROWER as the window got WIDER between 981 and 1343px - the exact fault that block was written to fix. 23-responsive-mid-band caught it. A rule now moves only if nothing staying behind it can outrank it, and that test needs to know whether two rules EVER MEET ON AN ELEMENT, which the file cannot say: \'they share a class\' is unsound (.card and .fpr-panel sit on one element and share no token), and dropping the clause pins 1,146 of 1,182 candidates. So co-occurrence is measured in the browser over all 25 routes at both widths: 192 dangerous pairs, 22 rules pinned. MEASURED on the build\'s own size table, against 73e6dd2: global stylesheet 687.20 kB -> 588.41 kB (-98.79 kB, -14.4%), gzipped 112.56 kB -> 97.68 kB (-14.88 kB, -13.2%); eleven page sheets totalling 99.0 kB raw / 21.6 kB gzipped, each paid only by its own route (largest TintTool 16.15 kB / 3.29 kB gzipped). FIELD METRICS, scripts/home-field-metrics.mjs, Pixel 5 / Slow 4G / 4x CPU / cache off / new context per run / 10 cold runs, both builds measured back to back on one machine: FCP 2867 ms -> 2565 ms (-302 ms, -10.5%), and the run ranges do not overlap (before 2852-2888, after 2556-2588). That is the render-blocking CSS leaving the critical path. LCP 3804 ms -> 3705 ms (-100 ms, -2.6%), and those ranges DO overlap (before 3672-4060, after 3564-3956), so it is not distinguishable from noise. THE LCP BUDGET IS STILL MISSED, BY ABOUT 1.2 s, AND THIS CHANGE DID NOT FIX IT. The LCP element is SPAN.home-hero-line-in - hero text whose paint waits on the entrance animation and the display font, not on stylesheet bytes. Taking 15 kB off a 113 kB stylesheet moves first paint and leaves LCP where it was; the rest needs the hero\'s own critical path, which is a separate item and should be filed as one. NO FLASH OF UNSTYLED TOOL UI, which is what this was filed as too risky for: Vite links a chunk\'s CSS and awaits it before the dynamic import resolves. Proven across all eleven moved routes by 64-computed-style-snapshot, mutation-verified by deleting the tint.css import (fails /create/tint alone, 63 values) and by changing a value inside the lifted learn-article.css (fails /learn/brand-colour alone, 17 values).' },
  { id: 'positioning-derived-surfaces', title: 'Every sales surface reads the value proposition from positioning.js: /plans, /help, llms.txt derived; /privacy names Vercel Analytics', priority: 'P1', effort: 'M', area: 'Brand', status: 'done', note: 'Founder decision 2026-09-07: the value proposition has ONE source, src/data/positioning.js, and every sales surface reads from it. Home.jsx and og-cards.mjs already did; this brought /plans, /help and public/llms.txt onto it and fixed /privacy on the way. WHAT CHANGED: /plans renders line(SURFACE_LINE.plansFraming) above its lede (H1, price ladder and every derived figure untouched; one margin rule in plans.css); /help opens its lede on line(SURFACE_LINE.helpOpening); public/llms.txt is now GENERATED by scripts/llms-txt.mjs (npm run sync:llms) from positioning.js, site-pricing.mjs, plans.js, aiGeneration.js, exportFormats.js, planLadder.js, the tool tree, learnIndex.js and routeMetaMap.js — every URL through canonicalUrl() and asserted against the prerender matrix — with prerender.mjs writing the served dist/llms.txt from the same function so a stale commit cannot ship. WHAT THE HAND-TYPED llms.txt HAD WRONG: the Brand Starter (live, beta) missing from the tools while "UI Auto-Builder" sat under not-yet-built; "Five published reference guides" against seven; and a summary selling CSS and design-token exports that exportFormats.js has never marked live. The AI limits and the $4 lead price were correct on the day and are now derived rather than lucky. /privacy: one sentence under a "Vercel Web Analytics" heading, wording checked against Vercel’s own privacy page on 2026-09-08. GUARDS: tests/unit/llms-txt-truth.test.js (committed file equals generator; every composed number is the server-enforced figure or the ladder price; no unbuilt format offered; Soon tools under Optional only; every description is its routeMetaMap entry; prerender wiring), positioning-truth.test.js extended to /plans, /help, the generator and SURFACE_LINE consumer coverage, rendered assertions in 54-plans-truth and 57-help-and-principles, and analytics-environment.test.js holding /privacy to the <Analytics /> mount in both directions. MUTATIONS, eleven, every one red on the guard it targets and every file restored byte-exact (git diff identical before and after): literal instead of line() on /plans and /help; framing paragraph deleted; llms.txt hand-edited; generator typing a number; prerender write removed; generator typing the summary; Soon tools leaking into Tools; privacy sentence removed; the 24-hour fact dropped; <Analytics /> unmounted. NOT WRITTEN, because positioning.js has no line for it and the founder called invented lines slop: the /plans SystemCTA title and hint, the /help H1, and any llms.txt sentence about what Pro is FOR beyond the derived pricing sentence — each left as it was and flagged in the PR. GATES: lint 0 errors 25 warnings, build 39 route shells, unit 1709/1709.' },
  { id: 'icon-tests-depend-on-live-iconify', title: 'The icon-library tests depended on the live Iconify API, and its rate limit failed the gate', priority: 'P2', effort: 'M', area: 'Testing', status: 'done', note: 'Filed 2026-09-08. MEASURED: /create/icons fetches the catalogue live from api.iconify.design with fallbacks api.simplesvg.com and api.unisvg.com — twenty-five /collection requests on first paint, a /search per query and an .svg per cell — and nothing in the acceptance suite stood between those requests and the network. After a day of full-suite runs from one machine, curl with the suite’s own Origin got 429 Too Many Requests from api.iconify.design and 403 without CORS headers from both fallbacks; a bare /collection call was Cloudflare error 1015. The browser suite then failed 25-defect-sweep’s pack-label test with “one pack name under every cell” (the Brand logos group resolved one pack instead of four), every /create/icons visit logged ~100 CORS findings (BrandGlyph draws the .svg onto a canvas through crossOrigin=anonymous, and a 403 carries no CORS header), and the gate for unrelated PRs went red on a third party’s rate limit. DONE, FIRST HALF: tests/user-sim/iconify-stub.js routes the three hosts on every browser context from the same browser.newContext wrap that installs the One Tap stub, and answers from tests/user-sim/fixtures/iconify/ — collections.json, one collection/<pack>.json for each of the 25 packs the page browses on first paint (Lucide with 132 real names so the default grid’s first page is one pack, as the real catalogue makes it), search.json as a mixed-pack index the stub filters by query and prefix(es), and icon.svg for every glyph with the CORS headers the real API sends. Every stubbed response carries a header; the global teardown fails the run if a response from an Iconify host lacks it or the page asked for something the fixture does not hold. 66-icon-library-offline-fixture.spec.js is the positive control: it counts the requests the page made through the fixture (25 collections, glyphs, a search that mixes packs), so a grid that renders nothing cannot pass. UIL4B_LIVE_ICONIFY=1 installs nothing and stands the guard down, documented in tests/user-sim/README.md, for a human checking the real catalogue — never the gate. api.iconify.design is no longer expected console noise in helpers.js. DONE, SECOND HALF — what a visitor sees when every host refuses, checked by rendering with all three routed to 429/403: within a second the page showed “Couldn’t reach the icon service — showing built-in icons.” with a Try again button and the 259 built-in icons from public/icons-data.js underneath, at 1280 and at 390 in both themes; Tab reached the button and pressing it once the hosts answered again brought the catalogue back. Not an empty grid, so the state stays. Two gaps closed in IconLibrary.jsx: the notice is a role=status region now (nothing announced it), and the summary line says “Built-in icons” instead of “Offline” (the browser was online the whole time; only the service refused). NOT DONE, FLAGGED: the hero pill still reads “Live library connected” while the catalogue is refused — it is navigator.onLine in IconEmojiLibrary.jsx, outside this change. MUTATIONS, each seen red then restored byte-exact: emptying the logos, devicon and skill-icons fixtures fails the pack-label test; cutting search.json to one prefix fails the search test; dropping role=status fails the refused-state tests; not counting /collection fails the positive control. GATES on 2026-09-08, on the branch rebased onto main at #429: lint 0 errors 25 warnings, build 39 route shells, unit 1734/1734, test:users 852 passed 13 skipped (all 12-ui-system-builder) 0 failed; the run’s Iconify ledger read 1117 contexts audited, 2677 requests served from the fixture, 225 refused on purpose, 0 reached the network. The same chain before the rebase (main at #427) was 1716/1716 and 839 passed, 13 skipped, 0 failed. UPDATE 2026-09-09: the flagged half is closed. IconLibrary.jsx reports its loadError flag to IconEmojiLibrary through an onCatalogue callback — the same flag that renders the “Couldn’t reach the icon service” notice — and the masthead pill reads the catalogue, not navigator.onLine: “Built-in icons” with the amber dot while every host refuses (the grid’s own status line, reused rather than a new sentence), “Live library connected” again after Try again, the offline pill unchanged. Rendered at 320, 768, 1280 and 1920 in both themes with the fixture refusing (429/403); pinned by three tests in tests/user-sim/71-media-tools-breakpoints.spec.js, seen red with the iconFallback branch removed. The emoji tab is out of scope of the flag: it never asks the catalogue, and its pill still reads the browser.' },
  { id: 'quality-marketing-2026-09-11', title: 'Rate every marketing and wayfinding surface out of 10 and lift everything under 8', priority: 'P1', effort: 'M', area: 'Quality', status: 'partial', note: 'Founder’s standing ask — rate every aspect of every page out of 10, improve anything under 8, re-rate. This is the marketing and wayfinding lane: /, /plans, /help, /info, /principles, /privacy, /terms, /sitemap and the 404, plus the footer and nav that ride on every page. Rated by rendering, not by reading: 11 widths (320 to 1920, including the 1097-1136 band), both themes, reduced motion on and off, signed out and signed in. The hero band and HERO_HEADLINE were scored and NOT touched (founder-approved 2026-09-10). THREE dimensions came in under 8, all of them measured. (1) /sitemap adaptive layout 4 → 9 and accessibility 4 → 9: every row printed its route in a `1fr auto` grid with white-space:nowrap, and grid will not size an auto column below min-content — so /create/component-designer (172px) claimed 172px of a card whose inner width is 227px and the row overflowed. The route painted 75px OUTSIDE the card border at EVERY width from 1024 to 1920, pushed the page into a horizontal scrollbar at 1136 through 1320 (1136 → scrollWidth 1191), and failed WCAG 1.4.10 Reflow at 200% zoom (640px → 665px). No media query could have caught it: .smap-grid is auto-fill minmax(260px,1fr), so a card is 227-652px wide at every viewport and 1920 is one of the worst cases. Fixed with flex-wrap on the row, which resolves per row instead of per breakpoint; the route is unchanged and still shown in full. (2) /principles accessibility 5 → 9: the page walked its headings h1, h2, h2, h2, h4, h4, h4, h2, h2 — a two-level skip (WCAG 1.3.1), and the only one on any marketing surface. ExportProof’s three column labels were h4. They were NOT promoted to h3: .prn-proof renders before .prn-say, so as h3 they would have nested under the PRECEDING principle and told a screen-reader user the export columns belonged to the type scale. They are figure column labels, so they are now <p> with the lists bound by aria-labelledby — which is what all four sibling proofs in SystemProofs.jsx already do. (3) /principles accessibility, second finding: the .prn-go call to action under every principle measured 23.1px tall, 0.9px under WCAG 2.2 AA 2.5.8, and does not qualify for the Inline exception (it is standalone, not inside a sentence). /principles is not one of the four routes 58-target-size-24 measures, which is how a fifth control stayed under the bar after that sweep fixed the other four. Everything else scored 8 or above and was left alone. TWO reported findings were measured and dismissed rather than "fixed": /info’s breakpoint ruler ticks look like 4.4999:1 to any DOM-walking contrast checker because the nearest painted ancestor is a 2px tick, but the labels are position:absolute outside it and sit on --bg-3, where they measure 4.95:1; and .hcmd-input reports no focus ring because the indicator is on .hcmd-bar:focus-within. Both are recorded at the foot of 76-marketing-quality.spec.js so the next sweep does not re-investigate them. STILL OPEN, needs the founder, not an agent: the two Pro documents (OWNER-ACTIONS §2.3) — /plans and the homepage price panel both state they are Pro-only, README.md and positioning.md both say every format is free with a credit. Three pages and two documents disagree and this lane did not decide it. Also named and deliberately not changed: /terms sets its section headings in .legal-h--sm (16px, body font, 700) while /privacy uses .legal-h (22px, display, 500) — the same content type solved two ways. Left because it may be a deliberate choice for the denser page, and a wrong guess about a legal page’s typography is worse than an inconsistency that has been written down. Tests: tests/user-sim/76-marketing-quality.spec.js, 14 tests. Mutation-verified with all four call sites reverted in one build — 13 of 14 failed; the one that passed is the guard against a BAD fix (routes truncated or hidden), which is correctly true in both states. The first mutation attempt left min-width:0 on .smap-link-label and all eleven sitemap tests passed against the reverted row, which is what caught that min-width:0 on a grid item is itself half the fix. 64-computed-style-snapshot regenerated for /sitemap only: 8 rows, 4 selectors × 2 widths, every value attributable (display grid→flex, gap 8px→2px row, grid-template-columns→none, justify-content→space-between, text-align→start, and margin-top 0→6px on .smap-link-note, which holds that note at the 8px the old grid gave it). No other route moved.' },
  { id: 'anti-slop-audit-2026-09-09', title: 'Anti-slop audit of the marketing and wayfinding surfaces below the hero', priority: 'P1', effort: 'M', area: 'Brand', status: 'partial', note: 'Founder’s standing ask — run anti-AI-design audits, user-centric choices, and his three rejections of copy that reads as AI-generated. Audited by rendering at 390 and 1280 in both themes and by reading source, against anti-slop-quality-bar.md: / below the hero, /discover and its libraries, /help, /plans, the 404, the footer, the nav promos and the share cards. The hero (item 10 in RELEASE-READINESS) was not touched. COPY RULE OBEYED THROUGHOUT: slop was deleted and hierarchy restructured; the only sentences added are the founder’s, read from positioning.js by id — two new SURFACE_LINE keys (homeExportHeading → build-and-export, plansClosing → one-unified-location), no new line. FIXED, each with a rendered test in tests/user-sim/70-anti-slop-marketing.spec.js that was seen red with the fix reverted: (1) the homepage closing SystemCTA banner (eyebrow, “From first decision to clean handoff.”, “Upgrade only when you’re ready” hint, beams) deleted — the page ends on the price panel, the same deletion the founder approved on the secondary landings; (2) the price panel’s lede (“…no trial clock…”) and four typed Pro lines, three false against config (Free has every tool and saves projects; “full system exports”; submissions need sign-in not Pro), replaced by /plans’s own derived delta from plans.js, colourSystems.js and exportFormats.js; (3) the tools section’s three-up figure strip (.htools-facts) and its “so”-hinge sentence deleted, leaving the canonical “Component tooling is coming next.”; (4) the export section heading “…not a screenshot.” (the defensive negation he rejected) is now line(build-and-export); (5) the starting-points note trimmed to its count — its “nothing to X, nothing to Y” pair and sign-up reassurance are gone; (6) /discover: “Find systems worth stealing.” (his rejected line plus Find), its lede and the Discover eyebrow above the h1 deleted, h1 is the surface name; (7) /help lede trimmed to the founder line — “No account, no setup, no blank canvas.” and the sign-in sentence gone; (8) /plans closing band: eyebrow and “No trial clock on Free” hint gone, title is line(one-unified-location), description keeps only its derived figures; SystemCTA’s eyebrow became optional; (9) the footer tagline “The operating workspace for…” deleted from every page; 64’s baseline regenerated for exactly that (34 .app-footer-tagline rows, 2 .home-eyebrow rows on /discover, three comma-only lines); (10) the 404’s four suggestions derived from the tool tree — “Palette Generator” and “Community palettes” were both wrong; (11) Gradient and Palette Library masthead sentences (one template, nouns swapped) and the “Curated collection” / “Hand-picked, not scraped” result-head eyebrows deleted; (12) the share cards no longer print a “Design toolkit” eyebrow — seven section PNGs redrawn, home and Learn cards byte-identical. 62-retired-taglines walks seven new phrases across every prerendered route plus /. NOT FIXED, reported for the founder: the nav promo copy in toolTree.js (“Inspiration worth the tab” / “Community UI systems…” — Inspiration is Soon, so that claim is false), left because toolTree.js is shared with concurrent lanes; the /plans FAQ and intro paragraph; the gallery grid headings (“Gradients worth building with”, “Everything worth a tab”); the Curated Resources masthead sentence (“genuinely worth a tab”); index.html’s “UI L4B | Design Toolkit” title and og:image:alt, which are the hero page’s; the Home step bullet “Copy the declaration, not a screenshot”. SENTENCES ONLY HE CAN WRITE: a Discover lede; a /help h1 if “Open any tool. It starts with something in it.” is not his; the nav promo lines for Create, Discover and Learn; an export-section heading if reusing build-and-export under a hero that already splices it reads as repetition. — — — CORRECTION, same day, from the full acceptance run: item (11) is reverted for the Palette and Gradient Library mastheads. Three standing contracts pin that masthead as title + description (15-discover-library-parity, 34-palette-library-sections) and, without the sentence, the 390px masthead measured 183px of dead space — the exact defect 40-gallery-hero was written for. A hole is worse than a templated line, so the two “…with a point of view … make it yours.” sentences and the gradient grid’s “Curated collection” label are back as they were, and the sentences move to the founder’s list. Only the Curated Resources result-head eyebrows stay removed.' },
  { id: 'quality-learn-discover-2026-09-11', title: 'Learn and Discover rated out of 10 and brought to 8', priority: 'P1', effort: 'M', area: 'Quality', status: 'partial', note: 'The founder’s standing instruction — rate every aspect of every page out of 10, anything under 8 gets improved, re-rate after — applied to the CONTENT and BROWSING surfaces: /learn and its seven guides, /discover and its four libraries, /community. RATED BY RENDERING, NOT BY READING: 14 routes at 11 widths (320/390/430/768/1024/1097/1120/1136/1280/1440/1920) in both themes, reduced motion on and off, plus every gallery’s no-results state and the /learn search. FIVE DEFECTS FOUND, ALL FIXED, NONE OF THEM A WRONG WORD. (1) THE ARTICLE HEADER NEVER SHARED AN EDGE WITH ITS OWN BODY, on all seven guides at every width from 1080 up — learn-article.css has carried the comment “so the crumb, the h1 and the first paragraph share one left edge” since it was written, and measured at 1440 they were 667.2, 693.6 and 561.6. TWO independent causes. (a) .lart-head kept max-width:56ch into the ≥1080 layout, clamping the 845px shared block back to 581px, after which its own 264px padding-left left the h1 a 317px column — 45% narrower than the 581px prose, indented 132px PAST it, and wrapping the title to three lines at 1440. (b) --lart-measure was an UNREGISTERED custom property holding 56ch, so it was substituted as TOKEN TEXT and re-resolved against each consumer’s own font: 580.72px on .lart-prose (17px Manrope) but 369.6px on .lart-crumb (11px JetBrains Mono). One token named “the measure”, two different widths. PROVED IN ISOLATION before touching the page: a registered <length> computed 529.457px at the declaring element and inherited that same px value to an 11px monospace child, while the unregistered twin re-resolved to 338.67px on the same child. Fixed with @property --lart-measure{syntax:’<length>’} (the mechanism global.css already uses for --rail-l/--rail-r) plus max-width:none in the ≥1080 block. AFTER: crumb, h1 and first paragraph all at 561.6 at 1440, and at one shared edge at 1097/1136/1920 too; h1 column 317px→581px; the dek 33→60 characters a line. Below 1080 is byte-identical. (2) THE MEASURE GOT LONGER AS THE TYPE GOT SMALLER, because the prose column is a fixed 581px: body 17px = 67 characters a line (Bringhurst’s 66, correct), but .lart-next-lede 15px = 76 and .lart-aside 14.5px = 79 — and the aside is also the lowest-contrast text on the page (--t3). 79 is past the 45–75 band the file’s own comment cites Bringhurst for and one character inside WCAG 1.4.8’s ceiling. Capped both in ch, which ties each block to its OWN font size: 72 and 72 now, narrow widths untouched. (3) WCAG 2.2 SC 2.5.8 ON THE CITATION LINKS, every guide, every width: each Sources <li> holds nothing but its link, so the Inline exception does not apply under the rule 58-target-size-24 already implements, and they measured 19.0px (plus .lart-spec-cite’s at 19.7px). 8 failures on /learn/colour-contrast, 5 on /learn/brand-colour. These are the links that make the guides checkable and they were the hardest thing on the page to hit. inline-flex + min-height:24px, type size unchanged. NOTE spec 58’s own route list is /, /seo, /privacy and /plans — not one Learn or Discover route had ever been measured. (4) THE PROMPT LIBRARY WAS THE LAST GALLERY STILL RENDERING A DEAD END. components/library/LibraryEmpty.jsx states the rule in its own header comment — “an empty grid whose only escape is for the user to work out which of three filters they set is the dead end murphys-law.md exists to forbid” — and the Palette, Gradient, Font and Curated Resources galleries all adopted it. /discover/prompts kept a bare <p>: no control, and its role=“status” region rendered the EMPTY STRING, so the grid emptying was announced to nobody (SC 4.1.3) while its three siblings all announced a count. It already used LibraryToolbar and LibraryFilterGroup, so this was an unfinished migration rather than a new pattern. Swapped to the shared component; THE SENTENCE IS UNCHANGED (“No prompts match your search”) and no detail line was invented, because none exists to read. Also h1→h3: the locked-Pro group’s sr-only heading was an h3 directly under the page h1 with the closing CTA’s h2 after it, so heading-level navigation read h1→h3→h2. Now h2. (5) A TRAILING SLASH COST EVERY ROUTE ITS TITLE AND DESCRIPTION. App.jsx looked PAGE_TITLES up by the raw pathname while the other two readers of that same value in the same effect — canonicalUrl() and isUnknownRoute() — both strip a trailing slash. Measured on the built preview: /learn/, /discover/, /discover/palettes/, /community/ and /learn/colour-contrast/ all rendered “UI L4B | Design Toolkit” and the homepage’s description while their canonical tag correctly named the unslashed URL. vercel.json sets no trailingSlash, so BOTH spellings are served 200 in production — this reached a real visitor’s tab, their bookmark, and any crawler that runs JS, on the pages search and AI crawlers actually land on. One normalised lookup key; the shared file was touched for one expression. (6) /discover/resources lead links were 23.0px — one pixel under SC 2.5.8, at 320 and 390 only, seven of them. .cur-lead-title holds only its link so it is not exempt, while the sibling .cur-row-title IS (it also carries a PriceTag). inline-block, which still wraps internally where inline-flex would not. SCORECARD, before → after, the eight per-page dimensions. /learn 8/9/8/8/9/8/8/8 unchanged — nothing was under 8. LEARN ARTICLES (all seven) 9/8/3→9 6→9 readability, 6→9 visual, 8/7→9 accessibility, 6→9 adaptive, 7→9 code. /discover 7/9/8/8/8/8/8/8 — purpose stays 7, see below. /discover/palettes and /discover/gradients 7/9/8/8/9/9/8/8 — purpose stays 7, see below. /discover/prompts navigation 6→8, interaction 5→9, accessibility 6→9, rest 8. /discover/resources accessibility 7→9, rest 8–9. /community 8/8/8/8/8/8/8/8. WHAT IS STILL UNDER 8, AND WHY IT CANNOT MOVE HERE — all of it is [#433]’s founder-sentence list, extended rather than filled in. /discover still has NO lede: its h1 is the bare word “Discover” and nothing in positioning.js is about Discover, so purpose-and-content stays 7 on the landing. The Palette Library and Gradient Library mastheads still run the shared template “…with a point of view … make it yours.”, which is not the founder’s sentence, so both stay 7 — and note 40-gallery-hero measured 183px of dead space in the 390px masthead when that sentence was deleted, so it cannot simply come out. NEWLY ADDED TO THAT LIST: /discover/prompts’ lede “Save, organize, and reuse your favorite design prompts.” carries two US spellings on a lang=“en-AU” document — it comes from src/locales/en-US.json, which I18nContext serves to en-GB navigators too by deliberate design (“en-US users don’t see the Colour→Color flash”), so this is a locale-system call and not a page edit. NOT DEFECTS, CHECKED AND CREDITED: zero horizontal overflow on any of the 14 routes at any of the 11 widths in either theme; zero elements left at opacity 0 under prefers-reduced-motion on five surfaces; zero missing focus rings across 38–45 tab stops on five surfaces; the article data tables correctly scroll inside a KEYBOARD-FOCUSABLE .lart-table-scroll rather than clipping; /learn’s search empty state already announced through role=status and already offered a 40px “Show all 7 guides” way out; and the palette and gradient galleries examined 390 and 335 controls respectively with not one under 24px. MOBBIN, consulted before both pattern changes. Article header: Mailchimp, Upwork, Discord, Homerun and Better Stack all align the article title with its body column and NOT ONE indents it past — Mailchimp and Upwork are the closest structural match (left rail + measure + title over body). Empty state: Kajabi ships an explicit “Reset All” with its filter row and Dropbox a “Reset filters” above the results; the decisive argument was internal, though, since three of this site’s own four galleries already do it. GATES AND MUTATION TALLY are in the PR. 64-computed-style-snapshot’s baseline was regenerated for ONE route at ONE width: /learn/brand-colour @1440, four values, and they prove the fix rather than merely absorb it — .lart-crumb and .lart-head had DIFFERENT auto margins (273.203 and 299.641) because they were two differently-sized blocks, and both are now 167.641, which is the two-blocks-became-one-block change stated numerically. 24 other routes and the 390 column are byte-identical. public/llms.txt is untouched: no guide metadata and no index entry changed.' },
  { id: 'flow-audit-2026-09-09', title: 'Every user flow rendered end to end, and the friction fixed where this lane owns the file', priority: 'P1', effort: 'M', area: 'Product', status: 'done', note: 'DONE FOR THE FILES THIS LANE OWNS; PARTIAL because four findings sit in files it does not. Six flows walked as a person takes them — rendered at 390 and 1280 in both themes with a keyboard-only pass on each step, Mobbin references (Maze’s cap modal, Savee’s checkout return, Obvious’s sign-in gate) read before each change — and every sentence added is either ProjectContext’s own refusal rendered where it was thrown or a phrase the app already uses for the same edge; nothing was invented. FIXED, all rendered and mutation-verified at the call site in tests/user-sim/69-flow-audit.spec.js (28 tests): (1) the free cap on /projects answered “Save Current” with the SUCCESS toast — green tick, 1.8 seconds, no link — reading “Free plan saves up to 3 projects — go Pro for unlimited.” and left the form open with the name still typed; the refusal now stays under the field it refused, in ProjectContext’s words, with “See what Pro adds” → /plans (the type-scale menu’s phrase), the same inline treatment inside the New project dialog, and Duplicate’s toast is an error toast rather than a success one; 57-signed-in-session’s helper reads the inline refusal. (2) The Pro modal painted “FROM $4 /month” directly above “We couldn’t load current prices just now” — the element carried hidden={priceUnavailable} and .ui-pro-from’s display:flex outranks the UA [hidden] rule, so the contradiction the file’s own 2026-09-05 comment says was fixed had been shipping; it is a branch now, not an attribute. (3) The New project and project-detail dialogs on /projects closed on Escape but dropped focus to <body>; both use useModalDialog. That exposed a second defect, caught by the trace keydown@input → keypress@button[New Project] → click: Enter in the name field created the project, the hook restored focus to the New Project button in the same tick, and the same key’s keypress reopened the dialog over the project it had just made — fixed by cancelling the keydown’s default. (4) /settings Display name ignored Enter entirely (only the mouse could commit); Enter saves, Escape cancels, the suggestion list keeps first claim on its keys. (5) Opening the delete confirmation unmounted the button that had focus (activeElement was <body>) and “Permanently delete” was live with an empty password; focus lands on the password field, the panel itself for a Google-only account, and the button waits for a password. (6) /checkout/return’s error state offered “Try again” → /checkout, which without ?plan renders “Invalid checkout selection — choose Monthly or Yearly from Plans”: a retry that landed on a page calling the retry invalid; it is “Back to Plans” → /plans now, Checkout’s own phrase. VERIFIED HONEST, no change: the sign-in gate names the interrupted action and the return promise with focus on Continue with Google and Escape handing focus back to Save / export; every Pro modal amount is planLadder.js’s (asserted against resolvePlanLadder with a stubbed price service, trial on the yearly tier only, “Day 7 — $48 is charged”); the three return-page states; the Brand Starter touches nothing until asked, now proved on the real route from vs-current-design rather than the #429 fixture, and Font Pair opens on the pairing; the four live free export formats download as <slug>-style-guide.{html,md,png,jpg} and both Pro documents gate by name; theme choice; onboarding is one question that routes, skippable, opens the real tool, and its “Not now” honours the stashed destination — the resume branch 58-flows recorded as unverified. WHAT REMAINS, measured and not this lane’s files: (a) the Palette Builder save menu is unusable at both audited widths — at 1280×800 its right edge is at x=1415 so the Save button (x=1353) is off the viewport, and at 390 it is position:fixed at y=384 but elementFromPoint on its Save button returns .plb-col, the swatch columns paint over it — so “first result → keep it” from the palette tool does not work and only /projects → Save Current does; (b) the export panel is unreachable on a phone: .pnav-iconbtn{display:none} inside @media(max-width:768px) hides the nav’s only Export button and the phone sheet has no Export row, while the palette menu’s “Export” is copy-CSS/hex/PNG-card only — the style guide, the book and the guidelines cannot be opened below 768px; (c) PaletteBuilder’s doSave still answers the cap with the success-styled toast; (d) useToast holds every message 1.8 seconds, refusals included; (e) useModalDialog restores focus synchronously in its cleanup where LoginPromptContext deliberately defers a frame for exactly the reopen reason above. FOUNDER DECISIONS, in the PR: ProjectContext seeds a “Default Project” into any account with none, so the /projects “No projects yet · Create your first project” state is unreachable signed in — Onboarding’s “take me to my projects” and 40-onboarding’s “teaching empty state” describe a screen that never renders — and the seed occupies one of the three free slots; the sign-in gate’s “Not now? Closing this changes nothing” line is display:none below the 781px split; /settings opens on Subscription; /onboarding for an onboarded account re-renders the flow by URL. MUTATION TALLY: 8 call-site mutations, 8 red, every file restored byte-exact by hash, control green after each restore. GATES 2026-09-10 on the branch rebased onto main at #435: lint 0 errors 25 warnings, build 39 route shells, unit 1750/1750, test:users on port 4820 925 passed 13 skipped (all 12-ui-system-builder) and 1 delivery failure — ERR_NO_BUFFER_SPACE on /assets/usePopover in 24-mobile-overhaul, which re-run alone with --workers=1 after a --mode test build passed 29/29; those numbers were taken on the rebase onto #434, and #435 touched none of this lane’s files. FINAL GATE on the rebase onto #435, 2026-09-10, plain foreground commands: lint 0 errors 25 warnings, build 39 route shells, unit 1750/1750, test:users on port 4820 938 passed 13 skipped (all 12-ui-system-builder) 0 failed — the #434 delivery failure did not recur. #435 changed 38 lines of global.css outside this lane’s two .ui-pro phone blocks and the rebase was clean. FOLLOW-UPS 2026-09-10 (fix/flow-audit-followups-0910): the eight findings this record left in other lanes’ files are fixed and held in tests/user-sim/72-flow-followups.spec.js (24 tests at 390 and 1280, both themes where paint matters), each mutation-verified at its call site and every restore proven byte-exact. (1) Export on a phone: the sheet carries Saved projects and Export rows with the bar’s own gating (a sales route shows no Export); Mobbin’s Noom and Cash App menus read first. (2) The palette’s save and colour-system sheets at 390 were boxed by the toolbar — .plb is display:flex and a flex item with a z-index is a stacking context even at position:static, so the footer (30, later in the DOM) painted over the toolbar’s 30 and the rail’s mask boxed the save sheet again; while a sheet is open the toolbar outranks the footer and the rail drops its mask. (3) doSave at the cap renders SaveRefusal (moved to src/components) inside .plb-savemenu, in ProjectContext’s words with See what Pro adds → /plans. (4) Toasts scale with the message (utils/toastDuration.js: a second plus 50ms a character, 1.8–6s), an error holds at least six seconds, announces as an alert and carries a keyboard-reachable Dismiss; a hidden toast is inert. (5) /settings opens on Account and remembers the last section chosen (vs-settings-section); signed out it still opens on a section that exists. (6) The gate’s way-out line shows at every width; the band layout pays for it in aside spacing, measured at 390×844 (signup’s switch control ends at y=826 inside a box ending at 828). (7) /onboarding sends a finished account to the User Home via onboardingDestination — the router’s own rule; the derived profile carries no onboarding field and AuthContext is not this lane’s file — with a fresh flag from App.jsx so a new sign-up on a browser holding another account’s flag still gets the flow. (8) useModalDialog hands focus back a frame after cleanup, takes over a pending restore when its effect re-runs, and yields to focus that moved elsewhere; modal-contract.test.js executes all three. (9) Found while rendering 4 at 390: a project card’s Actions menu painted under the next card because the hover lift’s transform boxed it — .uh-card:has(.uh-menu){z-index:2}. What remains is the founder’s: the seeded Default Project and the two agent-sentence questions. CORRECTION, later the same day: item 6 is the interrupt gate’s line only. 09-auth-modal-accessibility pins the sign-up pane to zero overflow at 390 and the line overflows it by 21px, and nobody is interrupted into that pane — so LoginPopup marks a reason-bearing dialog .ui-login--interrupt and the sentence shows at every width there (measured 390×844: dialog y=36–808, the line ends at y=275, zero overflow) while the sign-up pane is byte-identical to before. Three existing specs (51, 57, 69) were updated for the fixes themselves: focus after a close is read a frame later, and /settings is opened on Subscription where a test wants the plan sentence. — — — THE SEEDED DEFAULT PROJECT IS ANSWERED, 2026-09-10 (feat/founder-calls-0910): drop it. New accounts start empty, the /projects empty state this record found unreachable now renders for every one of them, and the free cap means three rather than two. Existing accounts keep a “Default Project” untouched — the effect only ever wrote, and only into an empty list — proved by rendering an account that holds one with work in it and reading every figure back off the card and then out of the store after a reload. This record’s assertion that a new account sees the seed is inverted in place rather than deleted. See founder-calls-2026-09-10.' },
  { id: 'founder-calls-2026-09-10', title: 'The founder’s two answers of 2026-09-10: the hero headline ships, and the seeded Default Project stops', priority: 'P1', effort: 'S', area: 'Product', status: 'done', note: 'Two decisions that had been sitting open in docs/RELEASE-READINESS.md with an agent’s measurement under them and no answer. He answered both on 2026-09-10 and both are implemented, with every claim rendered in tests/user-sim/73-founder-calls-0910.spec.js (15 tests at 390 and 1280, both themes where paint matters). DECISION 1 — THE HERO HEADLINE IS APPROVED. “Build and export UI and brand design kits, in one unified location.” was assembled rather than written, because he chose “build one from my words only” over an agent draft: the lead is the verbatim head of his build-and-export sentence, the marked run is the verbatim tail of his one-unified-location sentence, and the comma between them is punctuation. He had never read it, and five places said so — positioning.js’s hero block, docs/reference/positioning.md’s heading and paragraph, RELEASE-READINESS item 10, Home.jsx’s hero comment and 10-home-chaos-to-calm’s failure message. He read it beside the two phrases it was cut from and said ship it. Every one of those now carries the FACT instead: approved by the founder on 2026-09-10, assembled only from his own words, do not reword without him. NOT ONE CHARACTER OF THE SENTENCE CHANGED — the diff on src/data/positioning.js is comments only and HERO_HEADLINE.lead, .mark and .tail are byte-identical to main. RELEASE-READINESS item 10 is struck the way §4 strikes a closed item and the release-blocking subset sentence reads 1 and 4. THE FLAG WAS REPLACED BY A GUARD, because a fact in a comment protects nothing. positioning-truth.test.js gains three tests: the approved sentence and its three pieces pinned as literals in the test file (a second record, the same shape the four founder lines already have), the pin tied back to the FOUNDER constants so the two cannot drift apart in the one direction that leaves both individually true and the pair of them a lie, and a positive control. THE SPLICE CHECK THAT ALREADY EXISTED COULD NOT DO THIS JOB and the reason is the point: it asks whether every fragment is a verbatim run of a founder line, so it passes just as happily for a DIFFERENT splice of the same lines — drop the full stop, move the comma, cut the lead one word shorter, mark a different run — and would have let a sentence he personally approved be rewritten by an agent tidying punctuation. Proved by mutation WITH THE SHARE CARD REGENERATED: HERO_HEADLINE.tail “.” → “!”, then npm run og:cards, and the only red left in the file was the new pin. The pre-existing cards.json equality goes green the moment the card is regenerated, which is exactly why it was a staleness check and never an approval guard. DECISION 2 — THE SEEDED “DEFAULT PROJECT” IS GONE. ProjectContext auto-created a project called “Default Project” in any account that had none. The flow audit (#436) measured the two consequences and put the choice to him: it spent one of the three free save slots, so a new free account had two and its first “saved” project was an untouched copy of DEFAULT_DESIGN nobody had made; and it made the /projects “No projects yet · Create your first project” panel unreachable while signed in — dead code that Onboarding’s “Not now — take me to my projects” and first-run-destination.test.js’s “the projects empty state still teaches” both describe as the landing. His call: stop seeding it, so the empty state does the teaching and the cap means three. The effect is removed and replaced by a note saying why and saying it must not come back. EXISTING ACCOUNTS LOSE NOTHING, and it is proved rather than asserted. The effect only ever WROTE, and only into an account whose list was empty; nothing in the codebase deletes or rewrites a project on load, here or anywhere. The test signs in an account holding a “Default Project” with real work in it — five colours, Fraunces/Manrope, a moved 17px scale, six tints — reads every one of those figures back off the rendered card at both widths (so the whole record is checked through the product’s own reading of it, not just its name), then reloads the tab and reads the store the APP wrote back: one record, the same name, the same five colours. WHAT DEPENDED ON THE SEED: nothing that breaks. PaletteBuilder’s and ColorStudio’s overwrite lists and SaveTypeSystem’s recent list are behind projects.length > 0; IconLibrary’s save picker falls through to its “+ New project” row; nextToolSuggestion() has a no-projects branch that names the palette; homeStats() returns nothing at all rather than a row of zeroes. Every Create tool works on the live working design (vs-current-design) and never needed a saved project. THE EMPTY STATE WAS READ AS NEW SURFACE, because it had never been on a real screen. Two structural defects, and NOT A WORD OF COPY CHANGED — the sentence already names the two tools that make a project and the control already says what it does. (1) Its heading was an <h3> directly under the page’s single <h1> and BEFORE the “Starters, rotating daily” <h2> in the DOM, so the outline read 1 → 3 → 2: a level-3 with nothing above it and then a level-2 after it, which is not an outline (WCAG 1.3.1). Same defect and same fix the legal pages’ section headings got — the tag changed, the size deliberately did not, and the universal *{margin:0} reset means the two tags compute identically here. (2) The folder mark was an unlabelled <svg> exposed to the accessibility tree between the heading and the sentence; it is aria-hidden now rather than given a name it does not need. RENDERED at 390 and 1280 in both themes: the panel is visible, its two tool links resolve to /create/color and /create/font-pair, the button is 44px tall at 390 and elementFromPoint returns the button itself, and it opens the New project dialog. Reachable by TAB FROM THE TOP OF THE DOCUMENT rather than by .focus() (which answers a different question), paints a focus indicator, Enter opens, Escape hands focus back a frame later (#436 follow-up 8), and Enter-through creates the first project of the account’s life, after which the empty state stops rendering because it stops being true. THE CAP NOW MEANS THREE: from empty, three Save Current saves all land with no refusal, the header then reads “3 of 3 projects” and “You’ve used all 3 projects on the free plan”, and the fourth is refused under the field it was asked in, in ProjectContext’s own words, with See what Pro adds → /plans (#437’s SaveRefusal, untouched by this lane). With the seed, the third of those was the fourth save and was refused. ON THE SLOT COUNT: /projects deliberately prints NO counter at zero, and that is not an oversight. homeStats() drops any figure that is zero and projectQuota resolves 0-of-3 to ‘clear’, whose documented judgement is that a countdown starting on the first project turns a foot in the door into a meter (P-003). What the seed used to make this page say was “1 project · 1 colour kept” for an account that had built nothing. The test pins the absence of both figures AND gives it a positive control — make one project and the count arrives reading 1, which is only possible from a start of 0 — with the three-then-refused walk as the end-to-end proof that the zero is real. UPDATED, NOT DELETED: 69-flow-audit’s assertion that a new account sees a seeded card is inverted, with a note saying what it used to read and why. 52-user-home and tests/unit/user-home.test.js keep a FIXTURE project called “Default Project”; that is a fixture name and not the seed, and it was left alone. MUTATION TALLY: 5 call-site mutations, 5 red, control green after each restore, every restore proved byte-exact by SHA-256 rather than by git checkout — which on the first attempt took the comment edits with it and had to be re-applied and re-hashed, so the harness snapshots the file instead. M0 positioning.js HERO_HEADLINE.tail with cards.json regenerated → 1 red, the new pin alone. M1 HERO_HEADLINE.mark → “in one place” → 2 red of 15. M2 the seeding effect restored verbatim → 9 red of 15. M3 the empty state’s <h2> → <h3> → 1 red, the outline test alone. M4 the empty state’s onClick → () => {} → 6 red of 15. GATES 2026-09-10, plain foreground commands on the branch off main at #438: lint 0 errors 25 warnings, build 39 route shells, unit 1760/1760, test:users on port 4860 977 passed, 13 skipped (all 12-ui-system-builder), 0 failed. 64-computed-style-snapshot was NOT regenerated and did not need to be: it measures /projects signed out, where the empty state does not render, and it passed unchanged.' },
  { id: 'stranded-branches-2026-09-10', title: 'Close the three agent branches whose commits never reached main', priority: 'P2', effort: 'M', area: 'Process', status: 'done', note: 'DONE. A census of 125 agent worktrees found 15 branches carrying commits that never merged. Eleven had landed under a different branch (their subjects are all present in main’s history) and docs/consolidation was another lane’s. The remaining three were adjudicated here on 2026-09-10, each against a REBUILT main rather than against its own commit message. THE BRANCHES ARE NOT DELETED and their worktrees stay in place. (1) fix/right-click-feedback-followups — BROUGHT FORWARD, rewritten rather than cherry-picked. All four defects it fixed were re-measured still live: the menu absent from every chromeless route, the menu anchored at (8,919) for a click at (200,200) because `detail` is 0 on a real Chromium right-click, Escape dropping focus on BODY, and a phone long-press coming back defaultPrevented. Full evidence appended to [right-click-feedback]. One part was LEFT OUT and said so: the branch also rewrote the menu’s Cancel row into two new sentences, which no defect required and which invented copy. (2) test/one-comment-stripper — FINISHED, partially. Its premise held: 65 of 148 unit test files carried their own comment-stripping code and tests/helpers/strip-comments.js did not exist. The helper is shipped as written; its own test failed on arrival because the magnitude assertion named src/pages/ColorStudio.jsx and the accept="image/*" inside it, and that attribute is gone — the file was reworked and lost half its length. The assertion now asks the whole tree instead, so no single refactor can fake it. Measured today: 19 files in src/ where the naive regex eats more than the shared walk, 3,165 lines in total, worst 178 in brandGuidelines.js, and 0 files anywhere where the shared one eats MORE. EIGHTEEN files converted, each proved by its own test run before and after (same test count, same pass count, zero failures); price-consistency.test.js held a 51-line verbatim copy, confirmed byte-identical on all 635 source files in both modes (1,270 comparisons, 0 differences). THE FINDING WORTH KEEPING: breaking the shared helper back to the naive regex turns exactly ONE of the eighteen red — price-consistency’s canary, the suite’s only real one. The other seventeen do not notice, so their comment-stripping is not load-bearing today and the protection they gain is latent. That is the argument for consolidating, not against it: it is handled once instead of eighteen times the day one of those sources gains the breaking pattern. LEFT ALONE: moderation-role.test.js and community-queue.test.js (another lane’s this week), and 53 files carrying an inline .replace() rather than a named function — the four that strip global.css that way were measured and produce byte-identical output today, so converting them would be churn with nothing to verify. (3) chore/v2-programme-pipeline — WRITTEN OFF, and the evidence is worth keeping so nobody re-derives it. It added 13 “stacked PR” workstreams plus two condition rows, in an OBSOLETE SCHEMA (name/stage/progress/summary; this file uses title/priority/effort/status/note), so it could not have merged as written. Every workstream is already represented, further along, under a different id: v2-token-foundation is the [design-system] condition now reading “v2.8 shipped”; v2-homepage is [home-v2-influence-and-export-section] (P1, review); v2-shared-ui, v2-page-sweep and the three v2 tool slices are [surface-redesign-after-homepage] (P2, partial) and [multi-breakpoint-ux-audit]; feedback-context-menu is [right-click-feedback], DONE and improved again above; community-rebuild is [community-backend], [submissions-account-not-browser] and [moderator-role]; admin-real-analytics is [analytics-env-guard] and [homepage-field-metrics]; v2-library-gating is some twenty library and gating rows including [typography-paywall-model] and [locked-library-values-still-in-bundle]. Its two condition rows are both false now: merge-path claimed the founder cannot merge from this environment and PRs merge routinely (main’s head is #442), and it would have moved [phase] to “V2 design-language rollout” where main deliberately still reads “Post-v2.8 hardening”. NOT ONE of its rows names work that is both still open and unrecorded, so nothing was carried forward.' },
  { id: 'backlog-shipped-to-the-public', title: 'The engineering backlog was served to anyone who asked for it', priority: 'P0', effort: 'M', area: 'Security', status: 'done', note: 'DONE 2026-09-11. src/data/pipeline.js — this file — was compiled into the client bundle and served as a public static asset. PROVED BEFORE ANYTHING WAS CHANGED, against a real npm run build, with an anonymous client and no login: GET /admin 200 and the HTML names the entry chunk; GET /assets/index-CFUOkE0Z.js 200 and it names Admin-rjGyizH8.js; GET /assets/Admin-rjGyizH8.js 200 and it names pipeline-DUbq1XL3.js; GET /assets/pipeline-DUbq1XL3.js 200, 824,007 bytes, no auth of any kind. That file contained in plain text the literal string “allow create: if true” — the exact unfixed Firestore rule docs/OWNER-ACTIONS.md is still asking the founder to close — plus 23 mentions of firestore.rules, 3 permission-denied diagnostics, 246 of “founder”, unshipped plans, security findings and founder decisions. TWO DEFECTS AT ONCE: an information disclosure, and the largest asset in the deploy by a wide margin. BEFORE: deploy 5,645,356 raw / 2,252,097 gzip across 203 files, of which JS 3,432,459 / 1,179,802; largest asset pipeline-*.js at 824,007 / 319,711, against 454,901 / 140,310 for the app entry chunk. AFTER: deploy 4,821,874 raw / 1,932,510 gzip across 202 files, of which JS 2,608,977 / 860,203; pipeline-*.js is not emitted at all and the largest asset is the entry chunk again. The deploy loses 823,482 raw and 319,587 gzip; JS gzip falls 27%. The 500 kB chunk warning stopped printing. THE FIX: PipelineBoard reads GET /api/ai?backlog=1, gated on a verified administrator by requireAdmin() in api/_lib/admin.js — the same gate /api/ai?diag=1 already uses, which the Admin page already calls with a bearer token. FOLDED INTO THAT ROUTE, not a new one: api/ holds twelve functions and Vercel allows twelve, and three unit tests fail the build on a thirteenth. NOT api/verify-admin.js, which is where includeUsers set the precedent for exactly this, because human-validation-zones.md gates that file to the founder and the change could not have shipped today — the board would have had to stay broken until somebody typed a patch in. api/ai.js is not gated, so this ships whole. The route reads the module with import() of a literal path inside the branch: it cannot go stale the way a generated JSON would, and the hot POST path never parses 832 KB of prose on a cold start. Rejected: stripping the notes at build time, because the titles are the disclosure too — “firestore.rules lets anyone create feedback documents directly, bypassing /api/support entirely” is a title — and because the notes ARE the board. THE GUARD WAS AIMED ONE AXIS AWAY. tests/unit/admin-chunk-carries-no-backlog.test.js walked the chunk graph and asserted the backlog was reachable by no STATIC edge and, as its positive control, by some DYNAMIC edge. Both were true for the whole life of the disclosure — the control was asserting the leak was present. Static or dynamic decides WHEN a browser fetches the bytes; a static asset is served to everybody either way. It now reads EVERY file on disk in the build output for strings taken out of this file at test time, with three controls: the scanner proven against pipeline.js itself, the build proven real, and the founder’s board proven to still have a source behind the admin gate. PROOF THAT THE SWAP MATTERS: on a tree with the import put back, the old guard is 4/4 green and the new one is red naming assets/pipeline-DUbq1XL3.js (824007 bytes) carries 359 rows. FOR THE FOUNDER: the board is unchanged to look at, but it now needs the deployed functions. vite preview and vite dev serve none, so the Pipeline tab shows its named failure state locally; that is honest rather than broken. Worth one look at /admin after the next deploy. THE FIX WAS INCOMPLETE, AND THE REVIEW CAUGHT IT. src/data/moduleBoard.js is a SECOND internal board — 20,332 bytes, 20 entries carrying summary, recentChanges, nextSteps, health and dated owner actions per module — and it was a plain STATIC import in Admin.jsx, so its bytes were inlined into Admin-*.js rather than given a chunk of their own. A guard written to look for the BACKLOG could not see it. On a build where pipeline.js was already gone, dist/assets/Admin-*.js still carried, verbatim and fetchable with no auth: nextSteps: [“Owner: verify aggregate analytics and Feedback reads after the published Firestore rules”, “Do not assume the admin custom claim exists; resolve any permission-denied result explicitly”, “Add export/print for board view”]. Instructions addressed to the owner. A twentieth of the size, so the weight argument barely applies and the trust argument applies in full. THE LESSON IS THE GUARD, NOT THE FILE. Naming pipeline.js was itself the bug: a guard that names its subject can only ever find its subject. It now DERIVES the internal boards from src/data/ by SHAPE — each exported array of objects scores one point per project-status field (note, nextSteps, recentChanges, health, priority, effort, progress, stage, blockers, owner, status, area, summary, updated) and three or more makes it an internal board. THE THRESHOLD IS A MEASURED GAP, NOT A GUESS: across all 30 importable modules in src/data/, moduleBoard scores 7, pipeline scores 5, learnIndex scores 1, everything else scores 0, and NOTHING scores 2, 3 or 4. A separate control fails if anything ever reaches 2, so the band closing is loud rather than a near miss. A module the detector finds that is not declared fails the build by name with instructions — verified by writing a third board, src/data/releaseBoard.js, and watching it say so: “found: moduleBoard.js, pipeline.js, releaseBoard.js / declared: moduleBoard.js, pipeline.js”. So the next board is covered the day it is written. RECORDED SO IT IS NOT REDISCOVERED: src/data/fontRealWorldUses.js matches a “data module reaching the client” grep and is NOT an internal board. It is product content — the per-family slot for photographs of real work on the public Font Gallery, shipping deliberately empty on the founder’s 2026-09-05 call, with isShowable as the licensing rule. It scores 0. THE GENERAL DISTINCTION: a module that describes the PRODUCT ships; a module that describes the PROJECT does not. BOTH boards now come from one GET /api/ai?backlog=1 through useInternalBoards(), cached at module scope so moving between the Pipeline and Board tabs does not refetch — one request for two tabs, asserted in a browser. FINAL, both boards out: deploy 5,645,356 raw / 2,252,097 gzip before, 4,805,311 / 1,925,775 after (-840,045 raw, -326,322 gzip); JS 1,179,802 → 853,440 gzip; the Admin chunk 118,313 → 102,275 raw and 30,936 → 24,267 gzip once the module board left it too. A dist scan of 508 needles from both boards across all 202 files finds nothing. PROOF THE GENERALISATION MATTERS, the same shape as the first one: on a tree with the MODULE_BOARD static import restored, the pipeline-only guard is 6/6 GREEN and the shape-derived guard is red naming assets/Admin-*.js carries 139 rows from moduleBoard.js.' },
  { id: 'quality-type-media-2026-09-11', title: 'Rate every type and media tool out of ten on eight dimensions, and lift everything under 8', priority: 'P1', effort: 'M', area: 'Quality', status: 'done', note: 'DONE 2026-09-11 for the ten surfaces this lane owns. THE METHOD: every surface rated out of ten on eight dimensions BEFORE anything was changed, from rendered evidence only — 220 signed-out cells (eleven widths, 320/390/430/768/1024/1097/1120/1136/1280/1440/1920, both themes) measuring horizontal overflow, controls under 24px, text contrast against its painted ground, clipped text, elements outside the viewport, heading order and page errors; 80 more with reduced motion on; a keyboard walk of all ten surfaces; an ARIA-pattern probe of every tablist, popover and dialog; and both AI tools driven signed out, free, Pro, AT CAP, with a file dropped, while working and with /api/ai refused. THE HEADLINE POSITIVE, and it is a verdict on #429/#430/#435 rather than on this pass: in all 300 cells there was NOT ONE horizontal overflow, NOT ONE control under 24px, NOT ONE contrast failure, NOT ONE clipped string and NOT ONE page error, and all 321 tab stops across the ten surfaces carried a visible focus ring. The geometry work is finished on these tools. WHAT THE BREAKPOINT PASSES COULD NOT SEE, and what this one fixed, is what a keyboard or a screen reader meets. SCORECARD, before → after, the eight dimensions being purpose/nav/copy/visual/states/a11y/layout/code: /create/type-scale 9,9,9,8,8,9,9,8 unchanged; /create/font-pair 8,8,9,8,8,8,9,8 unchanged; /create/font-gallery 8,8,8,8,8,8,9,7→8; /create/icons 8,8,8,8,8,7→8,9,7→8; /create/emoji 7→8,8,8,8,8,6→8,9,7→8; /create/file-converter 8,8,8,8,8,8,9,8 unchanged; /create/alt-text 8,8,8,8,8,7→8,9,8; /create/aspect-ratio 8,8,8,8,8,7→8,9,8; /seo 8,8,8,8,8,6→8,9,8; /create/auto-builder 9,8,9,8,9,9,9,8 unchanged. Nothing is left below 8 on any of the ten. THE SIX FIXES, each with a rendered test in tests/user-sim/79-type-media-quality.spec.js watched red with its fix reverted: (1) /seo carried role=tablist with three role=tab buttons and NO keyboard contract — all three at tabIndex 0 and ArrowRight moving nothing, so a screen reader announced “tab, 1 of 3” and then the arrow keys the reader reaches for did nothing; roving tabIndex plus Arrow/Home/End, the answer TypeScale’s .tsc-view-switch and the icon/emoji .lib-switch already give twice. Two of the three tabs also pointed aria-controls at an id the document does not carry, because this router renders one panel. (2) /create/icons — a search matching nothing took the grid from 120 cells to 0 and said so on screen while announcing NOTHING; the sibling emoji tab under the same masthead has announced its count since it shipped. role=status on the message already there, no new sentence. (3) /create/emoji — the category headings were h3 directly under the page h1, so 30 of 30 rendered cells skipped a level in a 1,655-cell grid whose fastest route is the heading list; h2, paint asserted identical. (4) /create/emoji — the masthead still said “Live library connected” for a set compiled into the bundle that asks the network for nothing. #435 found this and left it because no existing sentence fits and none may be invented; OMITTING the claim needs no sentence, so the pill is absent on that tab while online and the offline reading, which is true of both libraries, stays. A truthful online sentence for a built-in library is still the FOUNDER’S TO WRITE. (5) /create/aspect-ratio — the preset panel is role=listbox with role=option rows and the arrow keys moved nothing; Tab was the only way through a fourteen-row device list. Arrows, Home and End now walk it, clamped not wrapped — and moving focus INTO the panel created a second defect that is fixed with it, because closing a subtree holding the focused element drops focus to body: Escape and a pick both return it to the trigger now. (6) /create/alt-text — a refused generation rendered its message and announced nothing, so pressing Generate and hearing silence was indistinguishable from a press that did not register; role=status, politely, matching .alt-card-warn in the same card. PLUS: FontGallery.jsx’s three react-hooks/set-state-in-effect warnings are gone without a behaviour change (repo lint 25 → 22 warnings, 0 errors) — two of them were also a committed frame of the OLD value under the NEW input, including the outgoing family painted in the incoming family’s specimen row; and both library modules lost an UNREACHABLE masthead behind {!embedded && …}, kept alive for a route that has not existed since the two merged, carrying a duplicate h1 and the .sec-h-eyebrow the founder marked “AI” and #382 deleted everywhere else. METHOD NOTE WORTH KEEPING: one of the fourteen tests was VACUOUS when first written and only the mutation found it. Both “renders exactly one h1” tests PASSED while mutated, with two h1s and an eyebrow on screen — go() returns when the ROUTE is on screen and the route is the WRAPPER, so toHaveCount(1) was answered by the wrapper’s own h1 before the lazy library underneath had mounted. They wait for .ic / .emoji-virt now. A count assertion that runs before the DOM it counts has arrived passes for the same reason an empty page would. A SECOND method note: the mutation script used `git checkout origin/main -- <files>` and origin/main MOVED two commits under the lane mid-session (#447), so it staged a newer blob than the branch point and git diff --stat then showed 86 changed lines in global.css that were not this lane’s; use HEAD, not origin/main, to revert a fix you have not committed yet. LEFT FOR THE FOUNDER, because each needs a sentence this product does not own: the emoji tab has no truthful online status line (above); /create/icons has no persistent result-count line above the grid, which is the shape Magnific, Behance, Dropbox Dash and Surfshark all use (Mobbin, web) and which the emoji tab and the Font Gallery both already have — “N icons matching X” would be a new sentence, so it was not written; /seo does not carry its active tool in the URL, so Back leaves the page and a tab cannot be linked or bookmarked; /create/alt-text announces a failure but not PROGRESS, so a batch generation is silent until it ends; and /create/font-pair’s hero still fills the first screen at 390x844, which is already filed under [multi-breakpoint-ux-audit] as his call. NOT COVERED AND NOT CLAIMED: real assistive-technology announcement (the live regions here are asserted by role and content, not heard), pointer gestures, and the video half of /create/file-converter beyond its first tab.' },
  { id: 'quality-discover-purpose-2026-09-13', title: 'The three Discover surfaces rated out of 10, and the structural half of purpose fixed', priority: 'P1', effort: 'M', area: 'Quality', status: 'done', note: 'DONE 2026-09-13 (quality/discover-purpose). THE MISSING-SENTENCE COMPLAINT WAS HIDING A STRUCTURAL HALF, and that half is now measured and fixed on all three surfaces. HOW IT WAS MEASURED: Chrome’s own accessibility tree over CDP (Accessibility.getFullAXTree), walked from the root through childIds so the landmark list is in TREE order — the flat array is not in document order and reports a believable wrong answer. Built preview, signed out and signed in (free and Pro), 11 widths (320/390/430/768/1024/1097/1120/1136/1280/1440/1920) in both themes. FAULT 1 — /discover had no content landmark at all. The list read navigation("Primary") | main | contentinfo | navigation("Footer") at every one of the 11 widths in both themes: four landmarks, none naming content. The <section> holding the eight library cards — the block SurfaceLanding.jsx’s own comment calls "the page" — had no accessible name, and a <section> without one computes to generic, not region. Both galleries BENEATH this surface already named their results region, so the index above them was the only page in the set whose subject could not be reached from the landmark list. NOW: navigation("Primary") | main | region("Every library, and what is in it.") | contentinfo | navigation("Footer"). Named from the h2 already on the page — no sentence was written. FAULT 2 — the results region vanished when a filter emptied the grid, on BOTH galleries. The <section aria-labelledby> wrapped only the populated arm of the branch. /discover/palettes on arrival: 6 landmarks including region("Colours worth building with"); after a search matching nothing: 5, with the results landmark gone and region("Can’t find what you’re looking for?") — the closing CTA — left as the only landmark describing content, while the heading naming the results was still rendered inside no landmark at all. Same on /discover/gradients (100 gradients to 0). NOW the section wraps the result head AND both arms, so the list is byte-identical in both states, and the heading lives inside the region it labels. FAULT 3 — LibraryGrid discarded every name it was given. Its labelledBy prop was spread onto a bare <div>, and a <div> with no role computes to generic, which PROHIBITS an accessible name. /discover/palettes passed it three names (pgl-section-curated, pgl-section-brand, pgl-locked-brands) and not one appeared anywhere in the accessibility tree — markup that read perfectly and did nothing. NOW role="group" is applied when and only when a name is supplied, and the three resolve as group("Curated collection"), group("Brand systems"), group("Brand systems included with Pro"). group and not region on purpose: three more landmarks here would repeat the /create/palette failure. Landmark count unchanged at 6. FAULT 4 — the /discover Palette Library card counted the wrong array. It read GALLERY_PALETTES.length and printed "64 palettes"; /discover/palettes renders LIBRARY_PALETTES, which is that curated set PLUS 37 brand systems = 101, and announces "71 palettes" to a signed-out visitor and 101 to Pro. The card under-counted the library by 37 while its own description sold the brand systems as the reason to open it — and the comment above it claimed a wrong count was impossible because the data was real. Count and swatch preview now both come from LIBRARY_PALETTES. 64 to 101. SCORES, before to after, evidence in brackets. /discover: 1 purpose 7 to 7 [landmarks 4 to 5, card count 64 to 101, but see below] · 2 navigation 8 to 8 [skip link is tab stop 1 and reaches #main; 6 of 8 cards link, 2 are non-link Soon cards, no dead links] · 3 copy 7 to 7 [the hint "6 libraries open · 2 still being built" is derived and true against DISCOVER_GROUPS; the h1 is still the bare word] · 4 visual 8 to 8 [previews draw real gallery data; live and Soon cards differ meaningfully] · 5 states 8 to 8 · 6 accessibility 7 to 8 [content now reachable by landmark; 0 heading-level jumps; 12/12 tab stops show a focus indicator; no h-scroll at 320px under 200% zoom] · 7 adaptive 8 to 8 [no horizontal overflow at any of the 11 widths in either theme; landmark count constant; the 1097-1136 band is clean] · 8 code 7 to 8 [one array now feeds both the count and the preview]. SCORES /discover/palettes: 1 purpose 7 to 7 · 2 navigation 8 to 8 [toolbar search, two filter groups, Clear filters in the empty state] · 3 copy 7 to 7 · 4 visual 8 to 8 · 5 states 7 to 8 [count announced aria-live polite, 71 to 0 measured; results landmark now survives the empty state] · 6 accessibility 7 to 8 [three discarded names now resolve; region stable; 0 heading jumps; 12/12 focus indicators] · 7 adaptive 8 to 8 · 8 code 7 to 8 [LibraryGrid labelledBy was dead in every consumer]. SCORES /discover/gradients: 1 purpose 7 to 7 · 2 navigation 8 to 8 · 3 copy 7 to 7 · 4 visual 8 to 8 · 5 states 7 to 8 [100 to 0 measured; region stable] · 6 accessibility 7 to 8 · 7 adaptive 8 to 8 [hero 296 to 414px across the band, no overflow] · 8 code 7 to 8 [redundant single-child fragment removed]. TEST: tests/user-sim/83-discover-purpose.spec.js, 7 tests, asserting on the COMPUTED role and name rather than on markup — on two of the four faults the markup read perfectly while the browser discarded it. Controls: an injected named <section> plus an injected role=group that fail if the probe stops resolving names or roles; every test proves its route arrived; the empty-filter tests prove the grid was populated first. The count test is CROSS-PAGE — it reads the number off the card and off the page the card links to, signed in as Pro so both describe the same set — because a constant pinned in a spec is a number the test rewrites on demand. MUTATION-PROVED THREE TIMES, each on a whole unique line at the call site, each proved to reach dist (chunk content hash changed AND the needle count in dist/assets went 1 to 0), each restored byte-exact (chunk hashes returned to index-acCmL8mz.js and PaletteGallery-CKMabU_R.js, and git status came back clean). A: dropping aria-labelledby from the /discover grid section, red with the original defect quoted back verbatim — "Landmarks were: navigation(Primary) | main | contentinfo | navigation(Footer)". B: LIBRARY_PALETTES.length minus 37, red with "claims 64 but that page holds 101", reproducing the historical bug number for number. C: dropping aria-labelledby from the palette results section, red on the explicit region assertion, which is the one added precisely to catch a list kept "stable" by deleting the region from both states. WHAT IS STILL UNDER 8 AND WHY IT CANNOT MOVE HERE. Dimension 1 stays 7 on all three surfaces, and every remaining reason is a sentence only the founder can write. (1) /discover HAS NO LEDE. SurfaceLanding.jsx SURFACES.discover.lede is null and the slot renders when it is not; Learn has one. positioning.js carries four founder lines and none is about browsing a library; the closest, forget-the-app-name, is already assigned to helpOpening in SURFACE_LINE, and using it here would have two surfaces claim one line for two jobs. positioning.js says it itself: "DO NOT ADD A LINE TO THIS ARRAY. It is not a copy deck an agent tops up." NEEDED: one sentence from the founder saying what Discover is. (2) /discover META DESCRIPTION IS FALSE, and this is a NEW finding rather than the known lede gap. src/data/routeMetaMap.js line 88 reads "The best external design resources — gradients, palettes, fonts and inspiration — with a one-tap hand-off into the UI L4B tools that use them." Measured against DISCOVER_GROUPS, the array the page renders: of the eight groups exactly ONE (Curated Resources) is external — the other seven are UIL4B own libraries — and inspiration is soon:true, so the description advertises as delivered the very thing the page badges Soon two hundred pixels below it. "The best" is also an unearned superlative. This is the one sentence a search result and a share card show. NEEDED: one sentence from the founder. (3) BOTH GALLERY MASTHEADS still run the shared template "Colour systems with a point of view … make it yours." and its gradient twin, which is not the founder sentence. AND THE DEAD-SPACE CONSTRAINT ON (3) IS NOW MEASURED FIRST-HAND, AND IT IS NOT QUITE WHAT THE EARLIER ROW SAID. The space does not open BELOW the content, it opens ABOVE it, because .dgh-hero is align-items:end inside min-height:300px — the masthead is bottom-aligned against a floor, so removing the paragraph does not shrink the box, it pushes the h1 down. Deleting the sentence and re-measuring: Palette Library leaves 217px empty above the h1 in a 280px masthead at 320px (77% of the masthead), 210px at 390, 138px at 1440 — not viable at any width, and WORST ON A PHONE rather than on desktop. Gradient Library is different: 111px at 320 and 97px at 390, but only 3px at 1440 and 9px at 1280, so that masthead would survive deletion on a desktop and not on a phone. THE CAUSE IS THE HEIGHT RULE, NOT THE SENTENCE: a content-driven masthead height would let the founder delete rather than write. That rule was NOT changed here — it is the visual treatment of a signature surface the founder has reviewed personally, 40-gallery-hero.spec.js pins its dead-space behaviour, and with the sentence present there is no defect today (29 to 65px trailing space at every width). So it is reported as the option that makes his choice free, not taken unilaterally. NOT COVERED AND NOT CLAIMED: real assistive-technology announcement (roles and names are read from the computed tree, not heard in a screen reader), colour contrast on these three surfaces (unchanged by this lane), and /discover/prompts and /discover/resources, which are Discover surfaces this lane did not own.' },
  { id: 'design-nav-antislop-2026-09-13', title: 'The mega menu and its sub-category hover read as AI generated', priority: 'P1', effort: 'M', area: 'Navigation', status: 'done', note: 'DONE 2026-09-13 (design/nav-antislop). THE COMPLAINT: the mega menu feels AI generated as well as the hover animation for the sub categories within the menu. Diagnosed against the anti-slop bar rather than argued about, and every number below is computed style or per-frame geometry on /create/palette at 1440, not a reading of the stylesheet — global.css declares .pnav-tool three times 4,000 lines apart and the last transition declaration wins outright, so grepping the file proves nothing either way. WHAT WAS ACTUALLY WRONG. Six transitions in the nav animated a LAYOUT property, which forces a layout pass per frame and slides content out from under the pointer. The worst was transition:width on .pnav-menu itself: the three sections are 1260 / 1200 / 960px, so crossing from Create to Learn interpolated 300px of panel — sampled per animation frame, 25 distinct widths painted over 37 frames and the first tool row travelled 150px sideways while the visitor was reaching for it. On top of that, three entrances layered on one open: the panel scaled from .975 and faded, .pnav-menu-body faded AGAIN inside it, and every row rose 6px on an eight-step nth-child staircase to 168ms. 19 animations per open, the last finishing 306ms after the click. The staircase replayed on every section switch (rows are keyed by tool id, so React replaces the set) and each column ran its own cascade, so the order it implied was not a reading order, an importance order or a load order. The row hover the founder named ran three effects at once: background and colour, a 1px icon lift, and a hue rail that faded in AND grew from scaleY(.4). WHAT CHANGED, ALL SUBTRACTION. The panel now fades and descends 10px from the bar that spawned it, and that is the only animation on an open. A row hover is background, colour, and the category rail appearing. Nothing moves under the pointer. Also dropped transition:gap from .pnav-items, .pnav-actions and .pnav-editorial-link (the arrow that slid 8px to 12px on hover), and replaced transition:all with the two properties that actually change on .nav-item, .nav-cat-toggle and .nav-cat-link — all was animating font-weight and border-radius into the active state. MEASURED AFTER: layout-animating transitions 6 to 2, animations per open 19 to 1, row travel on a section switch 150px to 0px, distinct widths painted 25 to 1, staircase replays 8 rows to 0. WHAT WAS DELIBERATELY KEPT, because two of the brief’s targets turned out to be shipped decisions rather than slop. The search field’s 300 to 390px hover expansion and its typed placeholder are founder-directed and pinned by 30-founder-requests-0808; the expansion moves the three centre menus 0px (re-measured), and transform cannot do a job that reflows the control’s own text. .pnav-cta is NOT a hover at all — it is the sales-page scroll gate, it fires once, and 24-mobile-overhaul S15 asserts the grid track interpolates rather than jump-cuts. The hue dash on .pnav-col-label was left alone too: 43-mega-menu-contents asserts at least three distinct ::before colours across the column heads. TEST: tests/user-sim/84-nav-motion-contract.spec.js, nine assertions on computed style, with a positive control that injects transition:gap 1s on .pnav-toollist at run time and requires the same probe to report it — a sweep that finds nothing is otherwise indistinguishable from a sweep that looked nowhere — and it also asserts it scanned over 100 elements. The layout-property assertion is an exact allow-set of two rather than zero, so it fails BOTH ways: when a seventh grows back, and when one of the founder’s two goes missing. MUTATION, both proven to reach dist: transition:width put back on .pnav-menu flipped the emitted chunk global-Bp6L6iEn.css to global-BbMSTHJo.css and the needle 0 to 1, two tests red; the 1px icon lift put back gave global-D3La62vg.css, one test red. Restored byte-exact each time and the rebuild returns to global-Bp6L6iEn.css at sha256 967ce141. Both anchored on a unique code-only line and never on the expression: translateY(-1px) now occurs 8 times in global.css and every one is a COMMENT quoting the removed code, so a regex mutation on that expression finds a comment and goes green — the trap that has produced a false green here twice. NOT DONE, and it is why visual design does not reach 9: the Create panel leaves roughly 250px of empty column below Box Shadow and the editorial card bottoms out well above the grid. global.css already admits this in its own comment on the 1200px Discover width (the panel reads wide because short columns leave it EMPTY below, which is a vertical problem and not that number). Fixing it is a layout and IA change rather than a motion one, and wants the founder on it.' },
  { id: 'quality-tools-breakpoints-2026-09-13', title: 'The tool surfaces’ control patterns, swept at ten breakpoints in both themes', priority: 'P2', effort: 'M', area: 'Quality', status: 'done', note: 'FOUNDER, 2026-09-13: “im finding alot of basic UI problems that can be replaced with better UI design such as replacing some buttons with a drop down or other large issues”, with a screenshot of seven icon-only buttons per list row. THE GEOMETRY WAS ALREADY CLEAN — #451 swept these ten surfaces for overflow, target size and contrast and found none, and this pass re-confirmed it: 16,196 visible controls read across 10 routes x 10 widths x 2 themes, zero horizontal overflow, and the only sub-24px hit was the shared footer attribution link, an inline link in a sentence. SO THE DEFECT WAS THE PATTERN, NOT THE PIXEL, AND IT WAS ON /create/emoji. Twelve category chips rendered as a wrapped tray below 641px: SEVEN rows at 320 and six at 390, 290px and 249px of chips, taking the toolbar to 414px — 49% of an 844px phone. First emoji cell at y=969 (320) and y=912 (390), so ZERO emoji were on the first screen at 320, 360, 390 or 430. An emoji library showing no emoji. THE FIX WAS ALREADY IN THE CODEBASE AND UNUSED HERE: LibraryFilterGroup’s `alwaysCollapsed`, whose own note settles the rule — a facet with nine or more values is a labelled control that opens a list, not a row of chips that is a row of chips at some widths. /discover/palettes applies it to nine moods; emoji has twelve and did not. AFTER: toolbar 414→170px at 320 and 373→170px at 390; first cell y=969→725 and y=912→709; emoji on the first screen 0→18 (320), 0→21 (390), 0→32 (430). Desktop is byte-identical at 68px because the measured `overflows` path had already collapsed this group at every width from 768 up — so this REMOVED a shape change rather than adding one. AND THE MENU IT COLLAPSES TO WAS NOT CLICKABLE, which was a DEAD END ON MAIN AT EVERY DESKTOP WIDTH. placePopover took the room above a trigger to be `anchor.top`, but `.pnav` is `position:fixed; z-index:120`. The menu flipped up to y=10 and its FIRST option — “All”, the only way back to the unfiltered library — hit-tested to `.pnav-inner` at 700, 768, 1024, 1280, 1440 and 1920. Filter to Flags and the control that undoes it sits behind the navigation bar. Subtracting the fixed nav from the flip decision and the height cap moved it clear: top y=10→70 at 1280, y=36→104 at 390. A THIRD CAUSE BELOW 641: `.lbry-toolbar` is z-index 45, but the max-width:640 block set `position:static`, and z-index does nothing on a static box — while `backdrop-filter` opened a stacking context regardless. The menu was trapped in a context painting at auto and lost to `.dgh-copy` (relative, z-index 1), so seven of twelve options hit-tested to the hero H1 and its paragraph. `relative` keeps the bar unstuck, costs no layout and restores the 45. Hit-test of every option centre across 320/390/430/700/768/1024/1280/1920: BLOCKED 7,7,7,1,1,1,1,1 → 0 at all eight. ALSO: a single-select menu now CLOSES on choice and hands focus back — picking a category left aria-expanded true and a 492px panel over the grid it had just filtered; multi-select deliberately stays open. And /create/icons emptied its grid 120→0 on a no-match search with NO empty state whenever the catalogue came from the offline fallback, because `searchEmpty` carried `&& !loadError`: the guard is right when there is nothing to search, but this surface falls back to 120 built-in icons and says so, so a no-match search there is the SEARCH’s doing. Now announced in the sentence the block already carried. No new copy anywhere in this lane. transition:all removed from this lane’s three surfaces (.rc-flip, .svt-item, .emoji-skin-btn — none animated a layout property but all asked the compositor to consider every one); global.css 49→48, deferred/type.css 2→0, CRLF preserved. TEST: tests/user-sim/87-tools-breakpoints.spec.js, 19 cases, with THREE positive controls — a non-zero count of visible controls per route/width before any “nothing overflows” claim, a non-zero count of hit-tested options before any “nothing is blocked” claim, and an explicit wait for virtualiser cells after the fold probe returned -1 for “no cells” and -1 sailed through a “must be above the fold” check. MUTATION, twice, both proven into dist/: removing the `alwaysCollapsed` line flipped EmojiLibrary-BS7zgojK.js (sha 1ea42cc2) to EmojiLibrary-DQMYDLqy.js (sha 90c77566) with the needle 1→0 and turned 5 tests red; reverting `roomAbove` to `anchor.top` flipped usePopover-J1cMOCQA.js (sha b5f0f8d9) to usePopover-D_fXqxBt.js (sha 66f58a76) with `.pnav` 1→0 and turned the two hit-test cases red naming DIV.pnav-lead. Both restored byte-exact — same filenames, same shas, git diff clean. 25-defect-sweep’s emoji row was UPDATED, not deleted: it asserted twelve chips in one tray at 320 and 390, which was a description of the defect rather than of the baseline. It now names zero rows and one collapsed trigger, exactly as /discover/palettes’ mood facet does. 64-computed-style-snapshot was NOT regenerated and did not need to be: `.lbry-toolbar` is not in its frozen class set (only `.lbry-toolbar-row`, `.lbry-filters` and `.lbry-filter` are), and neither /create/emoji nor /create/icons is among its 25 routes. NOT COVERED AND NOT CLAIMED: real screen-reader audio (roles and names were read from Chrome’s AX tree over CDP, not heard); the icon tray’s own 7 chips, which wrap to 3 rows at 320 for a 302px toolbar but stay one row on desktop where they are the page’s primary control, so collapsing them everywhere would trade a desktop regression for a phone fix; and the nine-value weight pickers on /create/type-scale and /create/font-pair, which wrap to two rows at 390 but show which weights a family actually HAS — a select would hide that, and it is 64px, not 249px.' },
  { id: 'quality-colour-breakpoints-2026-09-13', title: 'The six colour tools across every breakpoint, in both themes — and the palette page’s heading slot', priority: 'P1', effort: 'M', area: 'Quality', status: 'done', note: 'Founder report, 2026-09-13: “im finding alot of basic UI problems that can be replaced with better UI design such as replacing some buttons with a drop down or other large issues”, with a screenshot of /create/palette at ~660px. Five hypotheses came with it; three were real, one was half right, one was not. RENDERED, not read off CSS: 320, 360, 390, 430, 660, 768, 834, 1024, 1097, 1120, 1136, 1280, 1440 and 1920, both themes, signed out and signed in at the free cap, reduced motion on and off, plus the short-viewport cases 390x568, 390x640 and 320x568. (1) SEVEN ICON-ONLY BUTTONS PER SWATCH ROW — REAL. Measured 7 visible .plb-tool per column at 320/360/390/430/660/768, times five rows: 35 unlabelled icon targets on one screen. The <=768 block pins them open because there is no hover at that width to reveal them with; it never chose which. Collapsed to Lock + Copy + the existing .plb-tool--more, which is the SAME collapse the (min-width:769px) and (hover:none) block already performed — the two bands had the identical problem, and a second answer to one problem is how two surfaces start disagreeing. Nothing was lost: the “Colour actions” menu it opens already carried Copy hex, Use as seed, Edit in HCT, View tints, Lock, Show contrast, Swap left/right, Insert left/right and Remove, each as a labelled row. The grip went because HTML5 drag does not fire from a touch. Row heights byte-identical before and after: 88/88/88/96/111 at 320/360/390/430/768. (2) “COPY CSS” LOOKS LIKE A TEXT INPUT — HALF REAL. It is NOT left-aligned; it is a centred pill, so that reading of the screenshot was off. But it and .plb-hexfield, a real <input> two rows above it, shared a white ground and the identical 1px rgb(218,216,207) border, and at <=768 it stretches the full viewport (628x29 at 660px). It carries the same IcoCopy the save menu puts on “Copy CSS variables”, the row that calls the same copyCssExport(). A leading glyph is the one thing a field never has. 77px -> 98px at 1280. (3) COLOUR NAMES CONTRADICT THEIR COLOURS — REAL, AND WORSE THAN REPORTED. themeForHue cut the wheel with boundaries spaced for an sRGB angle while every caller feeds it hexToHct(hex)[0], and the two wheels are rotated by up to 45 degrees in the blues. Measured by walking the sRGB wheel through hexToHct: azure lands at HCT 269 and blue at 285, both past the old h<255 ocean cut, so EVERY ORDINARY BLUE DREW THE PURPLE BANK. The founder’s “Bauhaus Mulberry” on #97ADCD is HCT 256 — one degree past it. #3558B9 was “Dusky Iris”, #405C9D was “Smoked Mauve”. Indigo (300) and violet (318) sat past the cosmic cut, so purples drew the PINK bank: #72518A was “Sun-Washed Bubblegum”. And h<45 with chroma<=45 fell through to the all-GREEN bank, so #904C2A, a burnt orange, was “Deep Thyme”. Re-cut on the measured anchors; no word was added, moved or invented. After: Bauhaus Delft, Faded Teal, Cold Glacier, Dusky Wine, Faded Rust. His second example was already correct — #918F9C really is a near-grey and “Deco Monochrome” is right. STILL OPEN, narrower: the noun is drawn from the whole bank by hash rather than ordered by hue, so a teal can still draw “Delft” and #835507, a bronze, still draws “Firebrick”. The FAMILY is right; the word inside it is not. Fixing that means re-banking about 84 nouns by hue and halving the pool the determinism contract depends on — a change of its own, not a release-week edit. (4) ROLE LABELS ABSENT AT THAT WIDTH — REAL. .plb-role was display:none below 769px, so from 320 to 768 the board could not say which swatch was the PRIMARY, while exports, tint scales and the UI preview all key off exactly that. The three icons freed 128px; it is back at every width in both themes. (5) ROWS TOO TALL, COLOUR FIELD EMPTY, CONTENT CRAMMED LEFT — NOT REAL as described. At 660px the name ends at x=108 and the tool cluster ran x=384..644, so content sat at BOTH ends with a 276px gap between them, not crammed left. Row height is 88-111px, which is the 32px tool row plus padding, and it did not change. Not chased further: the <=430 rule that wraps the tools onto their own line is heavily documented as the fix for an unclickable-controls regression at 390x640, and trading a proven fix for 40px of height in release week is the wrong trade. BEYOND THE LIST. (a) THE HEADING SLOT, founder-approved (“Build the slot, then I’ll write the line”). /create/palette had no heading area: its only heading was h1.plb-title at 15px/700 in the toolbar — the SAME size and weight as .plb-hex — and no h2 anywhere, at all 13 widths. It has .plb-hero now, with the three declarations copied verbatim from .tt-hero h1 and .tt-hero-copy>p in tint.css that .stc-hero already restates, so this page is the third user of them and not a fourth opinion. The name reads “Palette Generator”, which is what routeMetaMap.js already calls the route in the tab and the share card. PALETTE_LEDE is an empty string and the empty string IS the deliverable — the paragraph renders only when there is something in it, so there is no blank gap now and no second edit later, and its comment carries the three sibling sentences 58-tool-lede pins so the shape to match sits beside the slot. NO COPY WAS WRITTEN. Measured: 38 -> 64px at weight 500, flush under the nav (gap 0) and left-aligned to the nav’s content edge (delta 0) at every width, board still 492-606px tall. (b) THE CONTRAST CHECKER’S SPECIMEN, founder-approved — this closes item (d) of quality-colour-tools-2026-09-11, which flagged the line and left it for his eye. “Free while in beta. Terms apply.” is a pricing statement about a product and is untrue of UIL4B (planLadder.js is Free plus Pro at $7/month). The 24px row was a tagline (“Ship a palette you can defend”) and the 16px row described the tool. All three are pangrams now — what a type specimen has used for two centuries precisely BECAUSE it asserts nothing — and they still show 24/16/12px of the chosen pair against its own threshold. The 24px specimen was an h2, and the page’s ONLY h2, so the whole outline a screen-reader user heard was the page title followed by a tagline from inside a preview. It is a p now; .cc-spec-h carries the size and weight and p is already margin-zero here. That left no h2, so the two card titles — label elements with no htmlFor and no wrapped control, labelling nothing — became h2. Outline now: h1 “Colour Contrast Checker”, h2 “Live preview”. ZERO PIXELS MOVED: .cc-preview 623x250, .cc-spec-h 510x30, both .cc-panel boxes 535x471 and 681x444, and document height 1235, all identical before and after; only .cc-spec-link moved, 65px -> 61px, because the label is shorter. (c) MOTION THAT RELAID OUT THE BOARD. .plb-add animated flex-basis 92 -> 116px on hover and .plb-col is flex:1 1 0, so the 24px came out of the board: at 1280x900 every column went 238px -> 233px and each swatch’s name, hex and tools slid left under the pointer, on every crossing of the rail. Nothing was revealed by the extra width. Removed; the ink and ground change already says it is hoverable, and it composites. After: 238 at rest AND hovered. .plb-add-dot carried the only transition:all on any of these six routes, and on that element “all” resolves to exactly one animatable property because border-style is discrete; it is named now. global.css still has 48 uses elsewhere, none on these routes. SCORES, before -> after, on the eight per-page dimensions (purpose / navigation / copy / visual / states / a11y / adaptive / code), against the measured baseline in quality-colour-tools-2026-09-11. /create/palette 7/8/8/9/8/8/9/7 -> 8/8/8/9/8/9/9/7. Purpose 7 -> 8: the page names itself at a heading size and the slot for its sentence exists; it reaches 9 when he writes the line. A11y 8 -> 9: the role is readable at every width and 20 of the 35 unlabelled icon targets are gone. Visual and adaptive stay at 9 — both were already there. Code stays 7: still 3,340 lines in one component, unchanged and out of scope. /create/contrast 9/9/8/9/8/9/9/9 -> 9/9/9/9/8/9/9/9. Copy 8 -> 9: the only claim-shaped sentence on the page is gone and the specimen reads as a specimen. /create/color, /create/semantic-color, /create/tint and /create/gradient were re-rendered across the full matrix and are unchanged from the 2026-09-11 scorecard — nothing on them scored under 8 and nothing regressed. TWO READINGS THAT LOOK LIKE DEFECTS AND ARE NOT, recorded so the next sweep does not re-raise them. The toolbar action rail reports controls outside the viewport at 320-430 and at 834 — that is .rail-overflow, the designed horizontal scroller with fade masks and its own spec (36-rail-overflow), documented in global.css with the 834px eleven-control case named explicitly; it scrolls, so they are reachable. And /create/tint’s 13x13 checkbox sits inside a label.tt-check with min-height:24px and cursor:pointer, so the TARGET is the label, which conforms. HELD IN tests/user-sim/85-colour-breakpoints.spec.js, 7 tests, every one carrying a positive control because every assertion in it is a count or an absence and a route that failed to boot satisfies all of them. Plus 2 unit tests in palette-names.test.js pinning the hue families and proving the wheel is walked without a family being skipped. MUTATION: the two-line collapse appears TWICE in global.css, so the <=768 one was removed BY INDEX rather than by regex — a regex on that file finds the comment explaining the rule before it finds the rule. It reached dist: global-BpGhtv5n.css -> global-Be24ApM1.css, and the needle’s count in dist/ went 2 -> 1. 3 of 7 red on “320px, column 0: inline tools”, expected 3 got 7. Restored byte-exact (SHA-256 4bff8a1a…c020 on both sides, git diff empty), needle back to 2, 7 passed. A second mutation on themeForHue (h<290 back to h<255, anchored on the whole code line so the identical string inside the comment was not hit) turned the hue-family unit test red and was restored the same way. DELIBERATE TEST CHANGES, both because the h1 moved: 82-palette-purpose now reads h1#plb-page-title “Palette Generator” and the board’s resolved name follows it; 11-palette-recovery’s flush-to-nav probe reads .plb-hero instead of .plb-toolbar, keeps the toolbar’s own left edge as a second assertion, gains the sticky case the hero creates, and gains a positive control because a detached node reports 0 for both deltas and would have passed. 64-computed-style-snapshot was NOT regenerated and did not need to be: it samples .cc-spec-row, .cc-spec-btn and .seg-label, none of which changed a computed value, and the .seg-label it samples on /create/contrast is the first one on the page — label-for on the Text colour field, still a label, still display:inline — verified by rendering rather than by reading the fixture.' },
  { id: 'quality-account-breakpoints-2026-09-13', title: 'The projects page stops inventing a community, a taxonomy and a second counter', priority: 'P1', effort: 'M', area: 'Quality', status: 'done', note: 'THE FOUNDER SAID, UNPROMPTED, “i dont like the projects page”, AND SAID NOTHING ELSE ABOUT IT. So it was rendered rather than asked about: the built preview at 320/360/390/430/768/834/1024/1280/1440/1920, both themes, signed out, empty, one project, at the free cap of three and Pro, reduced motion on and off. THE GEOMETRY WAS ALREADY CLEAN — 100 rendered cells, zero horizontal overflow, zero elements painting outside their container, zero operable targets under 24px — so the answer was not a layout bug. It was what the page was made of. FOUR NAMED PATTERNS, ALL CORRECTED BY SUBTRACTION; nothing was added and no sentence was written. (1) IT INVENTED A COMMUNITY, and it was the same fabrication this repository already deleted once. A “Community” tab beside a person’s own saved work rendered four hard-coded design systems credited to four people who do not exist — Sunset Brand by Maya R., Fintech Blue by Devon K., Forest Co. by Sam T., Mono Minimal by Alex P. — under the sentence “Explore design systems shared by the community.” “Maya R.” is the SAME invented designer the 2026-08-11 site audit deleted from the real Community surface (“Aurora Analytics by Maya R., 342 saves”), and tests/unit/community-seed.test.js was written to stop exactly that coming back. It read COMMUNITY_DESIGNS and never read a surface, so a second copy went on shipping for a month, on the page where a user’s own work lives. Tab, data and the ColorRow that painted it are gone; the guard now scans the SOURCE of every surface that renders a community, with a positive control proving the comment-stripper is not what hides a name. (2) IT INVENTED A TAXONOMY — the founder’s call to drop folders as an entitlement, finished. #452 had already deleted the false “3 folders · Upgrade for 10” sentence and its orphan folderLimit and LEFT THE MECHANISM: five chips, All / Brand / App / Marketing / Personal, from a fixed array with no creation, rename or delete control anywhere in the repository, above a list that holds at most three items on the free plan. Rendering it added the argument #452 could not make from the source: the filing lived in vs-project-folders in localStorage and NOWHERE ELSE (utils/dataExport.js marks it pii:‘local’), so a project sorted on a laptop was unsorted on the phone, silently, on a page whose own subtitle promises “saved together, and yours to open anywhere you sign in”. NO PRICE, QUOTA OR OTHER ENTITLEMENT MOVED: planLadder.js, aiGeneration.js, exportFormats.js and plans.js are byte-identical to main, and there was no folder entitlement left in any config to remove — #452 had taken the last one. (3) THE PROMOS SAT ABOVE THE WORK, and this is the defect a person actually hits. It is the same class as #458 one state over: #458 moved the NEXT|TIP band below the EMPTY state and left it above the list for an account that HAS projects. Measured at 390x844, free plan, on pristine main: with ONE project the first card opened at y=781 — 709px of page between the h1 and the user’s own work, 63px of card inside an 844px fold; AT THE CAP it opened at y=882, THE WHOLE LIST BELOW THE FOLD, on the page that holds everything the account has made; at 1440 at the cap, y=607. The band is 258px of that at phone widths because it collapses to one column at <=860px — widest exactly where vertical space is scarcest — and what it spends those 258px on, above a person’s own saved work, is a rotating typography tip. One unconditional render site below the grid now, which is the order the empty account already used and the order this file’s own note at the foot gives for StarterRow. (4) THE SAME NUMBER TWICE: “3 of 3 projects” at y=203 and “3 projects” at y=567 at 1440, 364px apart. The toolbar count now prints only when a search has narrowed the list, which is the only time it says something the masthead does not. AFTER, same harness: first card at 390 goes 781→396 with one project and 882→497 at the cap (it now ends at y=739, inside the fold); 607→350 at 1440; document height at 390 at the cap 3453→3161; the empty state’s CTA rose 494→438, so #458’s fix is kept and improved. Controls inside the page container 22→15 for a one-project account, and the tab order now reaches the user’s own project BEFORE the tip band’s link, which it did not before. READ OFF CHROME’S OWN ACCESSIBILITY TREE through CDP rather than off the markup: the two tabs and five folder chips all reported selected=undefined pressed=undefined current=undefined — seven controls whose only statement of which view and which filter was active was a CSS class. They are gone with the features they belonged to, so the defect is deleted rather than papered over with aria-pressed on a control nobody asked for. TWO transition:all declarations left projects.css with the chips and tabs (2→0 in that stylesheet) and three more on this lane’s surfaces are named explicitly in global.css (49→46): .fb-type, .lang-tile and .sub-billing-toggle button. .lang-tile’s was also animating font-weight and padding, which are layout properties. HELD IN tests/user-sim/86-account-breakpoints.spec.js, 20 tests, asserted on rendered geometry and on COMPUTED ROLES rather than on markup — a markup assertion passes the day someone rebuilds the same control under a different class name. THE SPEC’S OWN PROBE WAS WRONG TWICE AND MUTATION CAUGHT BOTH, which is the most useful thing in this record. M2 put the folder chips back and the file stayed GREEN. Cause one: ‘main ’ + ‘a,button,input,…’ scopes only the FIRST item of a CSS selector list and leaves the rest global, so every probe that claimed to read the page’s own controls was also reading the primary nav and the footer. Cause two: the folder assertion was a DENYLIST of five capitalised labels, and re-adding the same row with lowercase labels passed it. The focusable list is an array now and every probe maps the scope over it; the folder assertion is an ALLOWLIST naming the four controls that belong above the grid and failing on anything else, whatever it is called. The positive control then did its job on the way through: tightening the scope from <main> to .sec.uh turned IT red while the four absence tests stayed green. MUTATION TALLY: 3 call-site mutations, 3 red, all restored byte-exact by SHA-256 (743538af9bae486c before and after each), and each proved to have REACHED dist/ — the chunk’s content hash changed AND a needle flipped. M1 the fabricated community restored → 9 red of 20; needles “Maya R.” and “Sunset Brand” 0→1 file in dist/assets; chunk Projects-C0YqU4NQ.js 41ffeee07cf5fd91 → Projects-BVCFsCQb.js 4c5f130dc5af7e9b. M2 the folder chips restored with their real capitalised labels → 3 red; needle “proj-folder-chip” 0→1; chunk → Projects-Bs_-dRMe.js 3e6a4ae61cb78ae3. M3 the tip band back above the work as two conditional render sites → 5 red; the DailyBand render sites carrying `resolving` in the chunk went 1→2; chunk → Projects-D1bRcmCn.js bd8893b57775ebef. The unit guard was mutation-proved separately: re-adding a COMMUNITY_PROJECTS array with author ‘Maya R.’ turns “no surface invents a designer to credit” red on its own. 64-computed-style-snapshot was NOT regenerated and did not need to be: it measures /projects SIGNED OUT, where none of the removed elements ever rendered, and its /projects baseline holds no .proj-* selector at all. STILL UNDER 8 AND NAMED FOR THE FOUNDER, because fixing them needs a decision rather than a measurement. /projects dimension 1 stays at 7: the masthead carries TWO creation controls, “Save Current” (accent) and “New Project”, and “New Project → Start from → Current design” IS “Save Current” — one operation, two controls, two different UIs, and on a brand-new account the accent one offers to save a design the person has never touched. Collapsing them is the founder’s “replacing some buttons with a drop down” note exactly, and it was NOT done here because three specs (57, 69 and 73) drive the cap and the refusal through Save Current, 73 being his own decision record walking an empty account through three saves — rewriting his record to make a taste change is not this lane’s call on a ship week. /projects dimension 4 stays at 7: the card art is a full-bleed palette band, and a project saved before its palette exists paints an empty slab. /feedback dimension 1 is 8 now, not the 6 the 2026-09-11 rating left — the signed-out contact gap it flagged is closed, the form carries an Email field labelled “Optional — but without it there is no way to reply to you.” GATES, REBASED ONTO main AT #462 AND RE-RUN THERE (the branch was cut at #460; #461 and #462 landed while it was open, and #462 edits src/data/pipeline.js and src/pages/Admin.jsx). The rebase is clean and was checked rather than assumed: #462’s corrected deploy row still reads Shipping, its Admin.jsx sentence about api/verify-admin.js survives, and this row is still ONE added line with nothing removed (git numstat 1/0) — which is the pipeline.js hazard, since that file packs many items per line and a line-level merge silently reverts another lane’s row. Plain foreground commands on the rebased branch: lint 0 errors / 22 warnings (the ceiling, not raised), build printed 39 route shells, unit 1901 passed 0 failed, test:users on port 5170 1184 passed / 13 skipped (all 12-ui-system-builder) / 3 failed. ALL THREE WERE DELIVERY FAILURES, NOT REGRESSIONS, and all three named the same signature — “the app never replaced the static boot shell… React has NOT RUN AT ALL” — on /info, /settings and /sitemap. Re-run alone on port 5172 after a --mode test build: 24-mobile-overhaul 29 passed, 65-new-surfaces-breakpoints 13 passed, 76-marketing-quality 14 passed, 0 failed. The pre-rebase run at #460 was 1186 passed / 13 skipped / 1 failed, that one also a delivery failure (74-right-click-feedback-reach, “build asset request(s) never arrived”, 12 passed alone). TWO OF THIS LANE’S OWN TESTS HIT THE SAME HARNESS TRAP AND ARE WORTH THE LINE: signIn() seeds localStorage ONCE PER TAB (the __uil4b_test_seeded guard exists so the app’s own writes survive a navigation), so a loop that signs in twice inside one test measures the FIRST account twice. Both loops are one-test-per-state now. NOT COVERED AND NOT CLAIMED: real assistive-technology announcement (roles, names and selection states are read from the computed tree through CDP, not heard in a screen reader), colour contrast on these surfaces (unchanged by this lane), and the account-deletion path, which was audited in the UI and never executed.' },
  { id: 'quality-marketing-breakpoints-2026-09-13', title: 'The gallery masthead is sized by its content, both template sentences are gone, and /terms matches /privacy', priority: 'P1', effort: 'M', area: 'Quality', status: 'done', note: 'Founder decisions taken 2026-09-13 on the marketing, wayfinding and Discover-browse surfaces, plus the two target-size defects found while measuring them. RATED BY RENDERING, NOT BY READING: 10 widths (320/360/390/430/768/834/1024/1280/1440/1920), both themes, reduced motion on and off, signed out. (1) THE GALLERY MASTHEAD’S HEIGHT RULE, AND BOTH TEMPLATE SENTENCES. .dgh-hero carried min-height (300px, 280 below 720px) TOGETHER WITH align-items:end, and those two declarations cannot both be right at two content lengths: the floor holds the box open, the end alignment drops the copy to the bottom, so everything the floor adds paints as black ABOVE the headline and the masthead with LESS in it gets the BIGGER hole. Three passes tuned that floor — 430, then 300, then 280 — and each one was tuning the wrong number. The recorded reason the sentences “could not simply be deleted” (183px of dead space at 390) was a misreading of the same fact: the space opens above, not below. MEASURED ON THE SHIPPED BUILD WITH BOTH SENTENCES STILL THERE, the hole was ALREADY 70px at 768 and 59px at 834 on /discover/palettes, 50px at 768 and 43px at 320 on /discover/prompts, 26px at 1024. Deleting the Palette Library sentence against the floor took it to 189px past the padding — 217px from the top border to the top of the h1 in a 280px box at 320, 68% of the masthead, WORST ON A PHONE; the Gradient Library cost 4px at 1440, which is the founder’s own “3px”. A SECOND MECHANISM nobody had measured: in the two-column --controls variant the row’s height is the TALLER column, so where that is the aside, align-items:end pushed the COPY down — /create/emoji carried 94px of black above its h1 at 768, 82px at 834 and 49px at 1024, with NO spare height in the box at all, which is why 40-gallery-hero’s copy+aside SUM read dead:-64 there and saw nothing. FIXED: no min-height anywhere, align-items:start, and .dgh-aside takes align-self:end so a control column still sits on the slab’s baseline. AFTER: dead space is 1px (sub-pixel rounding) on all six masthead surfaces at all ten widths; /discover/palettes is 92px at 320 and 227px at 1440, /discover/resources 198px and 394px — the box varies because its contents do. Then both sentences deleted: Palette Library’s and Gradient Library’s both ran “…with a point of view … make it yours.” Deleted, NOT rewritten. (2) /discover’S META DESCRIPTION WAS FALSE, and it is the sentence Google prints and every share card shows. It called eight groups “the best external design resources”; seven of the eight in DISCOVER_SPEC are UI L4B’s OWN libraries and only Curated Resources is external, and TWO of them (inspiration AND collections, not just inspiration) carry soon:true and are badged Soon. Replaced with 142 plain chars naming what the eight are, with no quality adjective and no claim. AWAITING THE FOUNDER’S WORD — shipped so the false one is not live while he reads it, and marked as such in routeMetaMap.js. (3) /terms NOW SETS ITS SECTION HEADINGS LIKE /privacy: .legal-h--sm (16px, body font, 700) retired, .legal-h (22px, display, 500) on all eight. TYPOGRAPHY ONLY — not one word of the legal text touched. /terms was .legal-h--sm’s only caller, so the modifier went with it. (4) WCAG 2.2 SC 2.5.8, TWO SURFACES THIS SUITE HAD NEVER MEASURED. /discover/resources painted 19 row-title links and 5 hand-off links at 19.8px at EVERY width — the whole outbound half of the page. The written reason the row title was exempt (“it also carries a PriceTag”) is not what the Inline exception says: a link beside a badge in a flex container is not in a sentence and its line box is not set by non-target text. And all seven /learn guides painted the breadcrumb’s “Learn” link — the only route back to the index from the top of an article — at 36.3x18.1px; the 2026-09-11 pass lifted this page’s citations and missed the control above them. Both fixed with inline-flex + min-height:24px; no type size changed. (5) The two transition:all rules exclusive to /info narrowed to the properties that actually change. TESTS: tests/user-sim/88-marketing-breakpoints.spec.js, 21 tests, every probe carrying a positive control. 40-gallery-hero.spec.js updated and SAYS SO: its floor guard (heroH>=280 at 641) is INVERTED, because that is the shipped design’s opposite now — /discover/palettes measures 133px there — and its probe measured content as copy.height+aside.height, which misses the grid gap when stacked (reporting a declared 32px gap as dead space) and DOUBLE-COUNTS when side by side. Both files measure the union box of the hero’s children now. MUTATION, two batches, 23 reds: putting min-height + align-items:end back turned 14 tests red and reproduced the historical 183px at 390 verbatim; reverting the other five call sites in one build turned 8 user-sim tests and 1 unit test red. Both batches restored byte-exact and proved with an empty git diff, and both proved to have reached dist — content hash changed and the needle flipped inside the built .dgh-hero, .lart-crumb, .legal-h--sm and dist/discover/index.html’s description tag. ONE FALSE GREEN CAUGHT AND FIXED: the new Terms.jsx assertion matched the class name inside the explanatory COMMENT, and only the CSS assertion failing first had hidden it — it strips comments now, and was re-proved by mutating Terms.jsx alone. NOT DEFECTS, MEASURED AND RECORDED SO THEY ARE NOT RE-REPORTED: zero horizontal overflow on any of 23 routes at any width (every off-viewport element sits inside a declared overflow-x:auto or hidden ancestor — the /learn data tables, /help’s run strip, the homepage marquee, /plans’ decorative glyphs); the nav’s 2x40px “Start for Free” pill is the scroll-gated CTA at opacity 0 with pointer-events:none, aria-hidden and untabbable, not a broken control; and /discover/resources’ and /help’s inline links do take the Inline exception properly. NOT FIXED, HANDED OVER WITH MEASUREMENTS: [1] at load with motion on, Chrome’s accessibility tree for / holds h1 then five h3s and NOT ONE of the five h2s between them, at 390 and 1440 alike — useHomeMotion.js opens [data-reveal] with GSAP autoAlpha:0, which is visibility:hidden, and visibility:hidden removes an element from the tree entirely; with prefers-reduced-motion set all 17 headings are present at load and the outline is correct, so the fix belongs to the homepage motion lane that owns the hero’s CLS budget. [2] 47 transition:all rules remain in global.css, all on SHARED classes (.btn, .card, input) that no single surface owns. [3] STILL OWED BY THE FOUNDER and deliberately not written: /discover has no lede (its h1 is the bare word “Discover”), and the /create/palette sentence. Both pages stay at 7 on purpose-and-content and are honest about it.' },
  { id: 'founder-night-2026-09-14', title: 'The overnight run of 2026-09-14: eleven founder calls, an SEO truth fix, a storage diagnosis and two audits triaged', priority: 'P1', effort: 'L', area: 'Quality', status: 'done', note: 'ONE SESSION, WORKED AUTONOMOUSLY ON THE FOUNDER’S INSTRUCTION. Recorded as one row because the items share a method rather than a surface: every claim below was MEASURED by rendering, and four reported defects were disproved that way before any code moved. SHIPPED. (1) The homepage shipped FOUR disagreeing descriptions — meta 131 chars, og/twitter 89, JSON-LD ~169, and DEFAULT_DESCRIPTION 158 which every other page uses. `/` is the one route prerender.mjs skips, so its head is hand-typed. App.jsx repairs the three META tags on hydration but NOT the JSON-LD, and social unfurlers (Slack, X, LinkedIn, Discord, iMessage) never run JS — so the most-shared link on the site previewed with the wrong copy and the structured data shipped a fourth variant permanently. All four now resolve to one imported constant, guarded by index-html-description.test.js. (2) robots.txt had drifted from PRIVATE_PREFIXES: /login was crawlable while the runtime treated it as private. Guarded one-way so redirect sources like /dashboard may stay. (3) The AI function bundled `src/data/*.js` — 977 KB kept across the two files it imports, 280 KB of 29 files it does not, on every deployment. Narrowed and guarded. THE SPIKE ITSELF IS NOT A BUG: pipeline.js is 956 KB and grows ~5 KB a commit (659 at HEAD~60, 812 at ~30, 939 at ~10), times 244 deploys since 14 August. It cannot become a static asset — /api/ai?backlog=1 is admin-gated and the backlog leaked publicly once. Shrinking it is a product decision. (4) Aspect Ratio returned fractional pixels: 9:16 at width 1920 gave height 3413.33, which fed the copy buttons, broke the preset lookup `find(o => o.w === out.width)`, and was written back into the ratio inputs. The 2-decimal round() was on a pixel count. (5) The tint ramp showed 318px of an 814px track at 390 — six of eleven stops behind a NESTED horizontal scrollbar, the pattern the founder rejected on Font Pair the same day. Eleven cells at min-width:60px cannot fit 318px, so it wraps now: 3 rows, 11/11 on screen, 0 overflow at 320/390/430/560/700/768. The caption “Scroll horizontally” went with it. Breakpoint moved 560 → 768 after the first attempt left it scrolling from 561 to 760. (6) Six of twelve /community monograms carried an ampersand (D&, P&, E&, B&, F&, C&) because split(’ ’).map(w => w[0]) treats “&” as a word. (7) The homepage price panel inverted WITH the theme — background:var(--t0) over color:var(--bg-0), so it measured rgb(15,15,16) on a light page and rgb(242,241,236) on a dark one, making the inverted showcase the brightest surface in dark mode. Pinned by freezing the two tokens locally; every text now measures 16.49:1 in BOTH themes. Its rows became a radiogroup with roving tabindex, and the choice travels to /plans via ?billing=, validated against BILLING_OPTIONS. (8) Brand Starter promised “a palette, a font pairing and a type scale” and then showed only a lock, on a site whose nav says Start for Free. A fixed, labelled example of the three bands now sits above the gate. Its h1 lost the italic-second-word formula. (9) Outbound mail had the sender written down twice as Resend’s sandbox; both routes now call mailFrom(), MAIL_FROM switches it the day the domain verifies, and reply_to reaches admin@uil4b.com TODAY because a reply-to header needs no verified domain. (10) 4.3 MB of QA screenshots deleted — evidence for the sixteen defects of the 2026-08-26 mobile audit, all fixed, showing a UI that no longer exists; the sibling directory was already gitignored for the same reason. (11) RELEASE-READINESS.md retired: 251 lines that restated OWNER-ACTIONS with a second set of numbers which HAD disagreed (it said he was paying for OpenRouter; he is on the free tier). Its §4 moved to build-and-verify.md as “What a green gate does NOT prove”. FOUR REPORTED DEFECTS DISPROVED, and this is the part worth reading. Two audit agents each led with a finding that was wrong, and both failures share a shape — they needed INTENT to judge. The homepage “dead CTA” is a designed scroll-gated reveal (2px at the top, 126x40 and clickable at #workbench). The Icon Library “rendering blank” is the sandbox having no route to api.iconify.design; the app correctly falls back to 120 built-in icons and says so. /login is not a dead end — dismissing navigates to /home with 3287 characters. The palette collapse does not delete features: 95 controls at 1440, 96 at 390. I CONFIRMED THE FIRST ONE BEFORE CHECKING, which is the mistake to avoid repeating: I measured at the top of the page without scrolling. A finding that needs intent to judge must be reproduced against the designed behaviour, not against a single measurement. The two that held up were pure measurement, and both are fixed above. ALSO DIAGNOSED, NOT OURS: the Firebase “insecure rules” email is `allow create: if true` on /feedback — real, ours, and already fixed in item 3 of `npm run apply:gated`, which the founder has not run. Nothing in src/ writes there; every report goes through /api/support with the Admin SDK. And a reported motion regression could not be reproduced: wheel scrolling yields 52 eased positions and .plb-toolbar stays stuck at top 60, while prefers-reduced-motion reproduces his symptoms exactly — Lenis never initialises, scroll drops to 2 positions, reveals appear unanimated. (12) FOUNDER CALL #6, the condition notices: the offline banner was position:fixed at calc(var(--nav-h) + 10px) and the sync notice at + 12px, which is exactly where .plb-toolbar, .uis-command, .lbry-toolbar and .pgl-section-head all park — he reported it on the Palette Builder, it was true of every sticky toolbar. They are rows in one .notice-stack now; NoticeStack.jsx measures it into --notice-h and --chrome-h folds that into every offset below the nav, so the strip reserves room instead of covering. Ten hand-rolled nav offsets collapsed into two tokens and .pgl-section-head picked up the --nav-top it had been missing. (13) THE HOMEPAGE TOOL GRID, audit item 1 of three: six cards of identical weight for six unequal families, where Colour has five live tools and UI Component Builder has none. Order and span are counted off liveCount now — half a row at three or more live, a third at one or two, the whole row as a low strip at zero — over six columns, because five cards tile exactly over six and cannot tile over four. The dead space went with it: three equal columns stretched every row to its tallest card, so Icons & Emoji carried one chip row above 180px of nothing. Audit item 2, the 38px pictograms, is recorded as a decision for him rather than done: a 38px square cannot hold the real output the mega menu was fixed with, so the honest options are delete or keep. Item 3 is his six sentences and was not touched. GATES throughout: eslint 0 errors / 22 warnings, unit 1903 → 1934, and the full browser suite green before each merge — with one baseline regeneration, /create/font-pair, where the four values that moved are the four the reasoning disclosure changed.' },
  { id: 'workbench-anchor-and-ratio-hierarchy', title: 'Two findings that need a design decision rather than an edit', priority: 'P2', effort: 'M', area: 'Quality', status: 'review', note: 'Both measured 2026-09-15, both left deliberately rather than forced. (1) TWO ELEMENTS SHARE id="workbench". HomeWorkbench.jsx puts it on its own <section class="hw"> and Home.jsx puts it on the .hsteps wrapper that CONTAINS that panel. Duplicate ids are invalid HTML and every consumer resolves the FIRST in document order, which is the wrapper - so href="#workbench" and PillNav’s getElementById both scroll to the narrative section rather than to the panel. Measured after clicking "See it working": 45% of the panel below the fold at 1280x900, 60% at 1280x720, and at 390 the panel sat 2,437px away, so the link does nothing useful on a phone. REMOVING THE WRAPPER ID MAKES IT WORSE - tried, measured, reverted. The panel is .hsteps-sticky, and scrolling to .hw inside it lands with the sticky not yet parked: the panel moved from top 485 to top 1103, 127% below the fold at 1280x900. The fix is a scroll target that accounts for the sticky container, not a moved attribute. (2) THE ASPECT RATIO PAGE HIERARCHY, founder report: "its heiracrhy is wrong". The Result sits about 1100px down at 1280, beneath eleven preset cards that are the loudest element on the page, so the answer is the last thing a visitor sees while the shortcuts dominate. The measurement half of his report shipped the same day - the nearest-standard suggestion became a control and any ratio now gets a computed size ladder - but reordering the page is a rework and wants his eye on a proposal first.' },
  { id: 'workbench-entrance-focus-and-learn-third-state', title: 'Focusable controls inside a running entrance, and two shipped pages badged Soon', priority: 'P2', effort: 'M', area: 'Quality', status: 'deferred', note: 'Both found 2026-09-15 while fixing something else, both verified, neither taken. (1) THE WORKBENCH ENTRANCE LEAVES FOCUSABLE CONTROLS AT OPACITY 0. Tabbing into the homepage workbench switches its mode, which replays the `hw-panel-in` CSS keyframe, and for the length of that animation `.hw-controls` and `.hw-foot` compute to opacity 0 while their buttons stay focusable and pointer-events:auto. Measured at 1440 on the built preview: 20 Tab stops affected, two distinct culprits, first hit at step 21 with the page at scrollY 3200. NOT the scroll-reveal system - closest("[data-reveal],[data-reveal-group]") is null for both, so this predates and is independent of the autoAlpha-to-opacity change that found it. Severity is genuinely lower than an unrevealed section: the keyframe finishes on its own and the controls come back, so this is a transient rather than a stuck state, which is why it was filed instead of fixed inside an accessibility commit about something else. It surfaced because the new Tab sweep in 88-marketing-breakpoints first measured effective opacity without asking WHICH ancestor caused it; that test now identifies the culprit and counts only reveal units, and its comment records this row as the reason. (2) TWO SHIPPED PAGES ARE BADGED SOON AND LINK TO THE INDEX INSTEAD OF THEMSELVES. LEARN_GROUPS carries `principles` ("Design Principles") and `help` ("Help & Getting Started") with route:"/learn" and soon:true. Both pages exist, are prerendered, and have their own titles and descriptions in routeMetaMap.js - "UI L4B | Design Principles" at /principles and "UI L4B | Help & Getting Started" at /help. So the Learn mega menu badges two delivered pages as unwritten and its only route to them points back at the index, and /learn’s grid lede ("Nothing in this grid is written yet - that is what Soon means.") is false for 2 of the 5 rows LEARN_ROADMAP renders. THE OBVIOUS FIX IS WRONG AND WAS TRIED. Flipping either row to soon:false with its real route makes it a DELIVERED LEARN GUIDE, which tests/unit/learn-articles.test.js correctly rejects - a soon:false row is asserted against LEARN_ARTICLE_ROUTES, and neither page is in LEARN_ARTICLES. That attempt was reverted. The data model has two states, delivered-guide and not-written, and these two pages are a third thing: shipped, useful, reachable, and not a Learn article. Adding that state changes what the mega menu, the visual sitemap and the /learn grid each mean by a row, so it wants the founder’s eye on a proposal rather than a quiet schema change in a bug-fix branch.' },
  { id: 'library-filter-collapse-below-641', title: 'Whether filter chips become a menu below 641px, across all six library toolbars', priority: 'P2', effort: 'M', area: 'Quality', status: 'deferred', note: 'FOUNDER DECISION WANTED: whether the library filter trays collapse to a menu BELOW 641px as well as inside the 641-980 band. THE REPORT AND THE CORRECTION. The 2026-09-15 type-and-media re-score scored /create/font-gallery adaptive 9 to 7 and proposed applying LibraryFilterGroup’s `alwaysCollapsed` to the category and sort chips below 641px, on the measurement that 0 of 24 specimens sit on the first screen at 320/390/430 with the first card at y=1167/1062/1049. The measurement reproduces - 0 of 24, first card y=1185/1074/1061 - but the proposed remedy does not follow from it, and the depth figure is inflated. Broken down at 390 (viewport 844): masthead .fg-hero 80 to 459 (379px); .typ-notice 477 to 673 (196px); gap; .lbry-toolbar 766 to 1075 (309px); first specimen 1074. The 196px block is the "Showing a bundled list of 77 fonts / The full family listing did not load / Try the full catalogue again" OUTAGE NOTICE - the Google Fonts catalogue is unreachable from this sandbox, the same way Iconify is - so it is an artefact of where the measurement was taken and would not be on a healthy load. Both the re-score’s number and this one carry it. WHAT THE CHIPS ACTUALLY COST: the two .lbry-filters trays are 50px each, 100px of that 309px toolbar. Collapsed they become one row of two triggers, about 46px, so the saving is roughly 68px - not the 158px the report attributes to "toolbar row 1", which also contains the 46px search field. With the outage notice discounted the first specimen would sit near 848px at 390, and the 68px would take it to about 780 - so the claim that collapsing clears an 844px fold is probably right, for a smaller reason than stated, and only on a healthy load. WHY IT WAS NOT JUST DONE. `alwaysCollapsed` is the wrong instrument and its own precedent says so. PaletteGallery passes it to ONE group and the note there is explicit: nine options is a menu everywhere, the three-way provenance split stays a visible segmented row because it is the page’s primary control, and the two groups are given different shapes on purpose. Font Gallery’s groups are FIVE categories and THREE sorts; neither is a nine-value facet, and Category is this page’s primary control. Passing `alwaysCollapsed` would also collapse them at 1440, where the toolbar already fits in one 135px row and nothing is wrong. THE INSTRUMENT THAT FITS is COLLAPSE_QUERY in LibraryFilterGroup.jsx, currently (min-width:641px) and (max-width:980px). Dropping the lower bound collapses every tray below 641 too - which is arguably where a menu helps most, since that is where chips wrap to three rows. But it is ONE line changing six surfaces at once: Palette, Gradient, Prompt, Curated Resources, Emoji and Font galleries, each of whose narrow layout was tuned separately. That is a look-at-all-six change, not a font-gallery fix, and the sibling row for /create/icons adaptive was already handed to the founder for the same reason on the same day.' },
]
